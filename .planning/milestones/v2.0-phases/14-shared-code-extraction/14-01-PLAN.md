---
phase: 14-shared-code-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/constants.js
  - src/shared/matching.js
  - src/content/constants-globals.js
  - src/manifest.json
  - src/background/service-worker.js
  - tests/unit/shared-matching.test.js
  - tests/unit/shared-constants.test.js
autonomous: true
requirements:
  - SHARED-02
  - SHARED-03

must_haves:
  truths:
    - "shared/constants.js exports MSG (16 keys), STATUS (8 keys), and PATENT_TYPE (2 keys) as ES module named exports"
    - "shared/matching.js exports all 10 canonical matching functions as ES module named exports"
    - "service-worker.js imports constants from shared/constants.js instead of defining them inline"
    - "manifest content_scripts loads content/constants-globals.js instead of shared/constants.js"
    - "Chrome extension still works from src/ without a build step"
  artifacts:
    - path: "src/shared/matching.js"
      provides: "All 10 matching functions as ESM exports (canonical from offscreen version)"
      exports: ["normalizeText", "findAllOccurrences", "pickBestByContext", "whitespaceStrippedMatch", "bookendMatch", "resolveMatch", "formatCitation", "fuzzySubstringMatch", "levenshtein", "matchAndCite"]
    - path: "src/shared/constants.js"
      provides: "MSG, STATUS, PATENT_TYPE as ESM exports with all 16 MSG keys"
      exports: ["MSG", "STATUS", "PATENT_TYPE"]
    - path: "src/content/constants-globals.js"
      provides: "Classic script wrapper defining MSG/STATUS/PATENT_TYPE as globals"
    - path: "tests/unit/shared-matching.test.js"
      provides: "Smoke test verifying all 10 exports from shared/matching.js"
    - path: "tests/unit/shared-constants.test.js"
      provides: "Smoke test verifying MSG (16 keys), STATUS (8 keys), PATENT_TYPE (2 keys)"
  key_links:
    - from: "src/background/service-worker.js"
      to: "src/shared/constants.js"
      via: "ESM import statement"
      pattern: "import.*MSG.*STATUS.*PATENT_TYPE.*from.*shared/constants"
    - from: "src/manifest.json"
      to: "src/content/constants-globals.js"
      via: "content_scripts.js array entry"
      pattern: "content/constants-globals.js"
---

<objective>
Create the shared ESM modules (constants + matching), wire the constants consumers (service worker + manifest), and establish test coverage for the new shared modules.

Purpose: Establish the shared module foundation that Plan 02 will wire to the remaining consumers (offscreen.js and content/text-matcher.js). Constants are done fully in this plan; matching module is created but only consumed by tests here.
Output: `src/shared/matching.js` (10 exported functions), updated `src/shared/constants.js` (ESM exports, 16 MSG keys), `src/content/constants-globals.js` (classic wrapper), updated manifest, updated service-worker.js, two new smoke test files.
</objective>

<execution_context>
@/home/fatduck/.claude/get-shit-done/workflows/execute-plan.md
@/home/fatduck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-shared-code-extraction/14-CONTEXT.md
@.planning/phases/14-shared-code-extraction/14-RESEARCH.md

@src/shared/constants.js
@src/background/service-worker.js
@src/offscreen/offscreen.js
@src/manifest.json
@vitest.config.js

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/shared/constants.js (current - classic script, no exports):
```javascript
const MSG = {
  PATENT_PAGE_DETECTED: 'patent-page-detected',
  PDF_LINK_FOUND: 'pdf-link-found',
  PDF_LINK_NOT_FOUND: 'pdf-link-not-found',
  FETCH_PDF: 'fetch-pdf',
  PDF_FETCH_RESULT: 'pdf-fetch-result',
  PARSE_PDF: 'parse-pdf',
  PARSE_RESULT: 'parse-result',
  GET_STATUS: 'get-status',
  LOOKUP_POSITION: 'lookup-position',
  CITATION_RESULT: 'citation-result',
  GENERATE_CITATION: 'generate-citation',
  FETCH_USPTO_PDF: 'fetch-uspto-pdf',
  USPTO_FETCH_RESULT: 'uspto-fetch-result',
  // MISSING 4 keys that service-worker.js has:
  // CHECK_CACHE, CACHE_HIT_RESULT, CACHE_MISS, UPLOAD_TO_CACHE
};
const STATUS = { IDLE, FETCHING, READY, PARSING, PARSED, NO_TEXT_LAYER, ERROR, UNAVAILABLE };
const PATENT_TYPE = { GRANT, APPLICATION };
```

From src/background/service-worker.js (lines 11-47 - inline constants to delete):
```javascript
// Has 16 MSG keys (the 13 above + CHECK_CACHE, CACHE_HIT_RESULT, CACHE_MISS, UPLOAD_TO_CACHE)
// After: import { MSG, STATUS, PATENT_TYPE } from '../shared/constants.js';
```

From src/manifest.json (line 29 - content_scripts.js array):
```json
"js": ["shared/constants.js", "content/text-matcher.js", "content/paragraph-finder.js", "content/citation-ui.js", "content/content-script.js"]
// Change first entry: "shared/constants.js" -> "content/constants-globals.js"
```

From src/offscreen/offscreen.js (matching functions to extract - lines ~570-1029):
The 10 canonical functions to create shared/matching.js from:
1. normalizeText(text) - called normalizeTextOffscreen in offscreen
2. findAllOccurrences(haystack, needle) - ONLY in offscreen, not in text-matcher.js
3. pickBestByContext(positions, matchLen, concat, contextBefore, contextAfter) - ONLY in offscreen
4. whitespaceStrippedMatch(normalized, concat, boundaries, positionMap, contextBefore, contextAfter) - has context params in offscreen
5. bookendMatch(normalized, concat, boundaries, positionMap)
6. resolveMatch(matchStart, matchEnd, boundaries, positionMap, confidence) - called resolveMatchOffscreen in offscreen
7. formatCitation(startEntry, endEntry) - called formatCitationOffscreen in offscreen
8. fuzzySubstringMatch(needle, haystack) - called fuzzySubstringMatchOffscreen in offscreen
9. levenshtein(a, b) - called levenshteinOffscreen in offscreen
10. matchAndCite(selectedText, positionMap, contextBefore='', contextAfter='') - called matchAndCiteOffscreen in offscreen
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared ESM modules and wire constants consumers</name>
  <files>src/shared/constants.js, src/shared/matching.js, src/content/constants-globals.js, src/manifest.json, src/background/service-worker.js</files>
  <action>
**Step 1 — Update `src/shared/constants.js`:**
- Add 4 missing MSG keys from service-worker.js: `CHECK_CACHE: 'check-cache'`, `CACHE_HIT_RESULT: 'cache-hit-result'`, `CACHE_MISS: 'cache-miss'`, `UPLOAD_TO_CACHE: 'upload-to-cache'` (total: 16 MSG keys)
- Add `export` keyword before each `const` declaration (`export const MSG`, `export const STATUS`, `export const PATENT_TYPE`)
- Remove the bottom comment about "No export" since the file is now pure ESM
- Update the top docstring to reflect that the file is now a pure ES module (content scripts use wrapper instead)

**Step 2 — Create `src/content/constants-globals.js`:**
- New file. Classic script (NO `import`, NO `export` keywords)
- Contains the same 3 constants verbatim: `const MSG = { ... }`, `const STATUS = { ... }`, `const PATENT_TYPE = { ... }` — all 16 MSG keys
- Add header comment: `// Classic script wrapper — defines MSG/STATUS/PATENT_TYPE as globals for content scripts.` and `// Source of truth: src/shared/constants.js — keep in sync until Phase 15 esbuild replaces this file.`
- These become globals when the manifest loads this file as a classic content script

**Step 3 — Update `src/manifest.json`:**
- In `content_scripts[0].js` array, change first entry from `"shared/constants.js"` to `"content/constants-globals.js"`
- All other entries remain unchanged: `["content/constants-globals.js", "content/text-matcher.js", "content/paragraph-finder.js", "content/citation-ui.js", "content/content-script.js"]`

**Step 4 — Update `src/background/service-worker.js`:**
- Replace lines 11-47 (the inline constants block + comment) with a single import: `import { MSG, STATUS, PATENT_TYPE } from '../shared/constants.js';`
- Delete the 3 const blocks and the "Keep in sync" comment. Keep the rest of the file unchanged.

**Step 5 — Create `src/shared/matching.js`:**
- Extract the 10 matching functions from `src/offscreen/offscreen.js` (the CANONICAL version — NOT from text-matcher.js)
- Strip the `Offscreen` suffix from all function names: `normalizeTextOffscreen` -> `normalizeText`, `matchAndCiteOffscreen` -> `matchAndCite`, `resolveMatchOffscreen` -> `resolveMatch`, `formatCitationOffscreen` -> `formatCitation`, `fuzzySubstringMatchOffscreen` -> `fuzzySubstringMatch`, `levenshteinOffscreen` -> `levenshtein`
- Add `export` keyword before each function declaration
- Update all internal cross-references within the file to use the non-suffixed names (e.g., calls to `normalizeTextOffscreen()` inside `matchAndCite` become `normalizeText()`)
- The function `matchAndCite(selectedText, positionMap, contextBefore = '', contextAfter = '')` must have default params for contextBefore and contextAfter so callers that omit them still work
- Do NOT import anything from offscreen/ or content/ — shared/matching.js is the bottom of the dependency tree
- Add header comment: `// Canonical text-matching functions for the Patent Citation Tool.` and `// All entry points (content script, offscreen, service worker) consume these via import or wrapper.`
- Verify that `findAllOccurrences` and `pickBestByContext` are included (these exist ONLY in the offscreen version)

**CRITICAL:** Run `npm test` after completing all steps. All 108 existing tests must still pass. The text-matcher.test.js and offscreen-matcher.test.js tests will NOT break yet because their imports still point to the old files — those are updated in Task 2 and Plan 02 respectively.
  </action>
  <verify>
    <automated>cd /home/fatduck/patent-cite-tool && npm test</automated>
  </verify>
  <done>
- `src/shared/constants.js` has `export const MSG` with 16 keys, `export const STATUS` with 8 keys, `export const PATENT_TYPE` with 2 keys
- `src/shared/matching.js` exports 10 functions (normalizeText, findAllOccurrences, pickBestByContext, whitespaceStrippedMatch, bookendMatch, resolveMatch, formatCitation, fuzzySubstringMatch, levenshtein, matchAndCite)
- `src/content/constants-globals.js` exists as a classic script with MSG/STATUS/PATENT_TYPE as globals
- `src/manifest.json` content_scripts[0].js[0] is "content/constants-globals.js"
- `src/background/service-worker.js` imports from '../shared/constants.js' with no inline constants
- All 108 existing tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create smoke tests for shared modules</name>
  <files>tests/unit/shared-matching.test.js, tests/unit/shared-constants.test.js</files>
  <behavior>
    - shared-matching.test: importing { normalizeText, findAllOccurrences, pickBestByContext, whitespaceStrippedMatch, bookendMatch, resolveMatch, formatCitation, fuzzySubstringMatch, levenshtein, matchAndCite } from src/shared/matching.js succeeds (all are functions)
    - shared-matching.test: normalizeText('Hello  World') returns a normalized string (basic sanity)
    - shared-matching.test: matchAndCite('', []) returns null (empty input)
    - shared-matching.test: matchAndCite with a simple positionMap entry returns a citation result
    - shared-constants.test: importing { MSG, STATUS, PATENT_TYPE } from src/shared/constants.js succeeds
    - shared-constants.test: MSG has exactly 16 keys
    - shared-constants.test: STATUS has exactly 8 keys
    - shared-constants.test: PATENT_TYPE has exactly 2 keys
    - shared-constants.test: MSG.CHECK_CACHE === 'check-cache' (verifies the 4 new keys exist)
  </behavior>
  <action>
**Create `tests/unit/shared-constants.test.js`:**
- Import `{ MSG, STATUS, PATENT_TYPE }` from `../../src/shared/constants.js`
- Test that MSG is an object with 16 keys (use `Object.keys(MSG).length`)
- Test that STATUS is an object with 8 keys
- Test that PATENT_TYPE is an object with 2 keys
- Test that the 4 newly-added MSG keys exist: `CHECK_CACHE`, `CACHE_HIT_RESULT`, `CACHE_MISS`, `UPLOAD_TO_CACHE`
- Test specific values: `MSG.FETCH_PDF === 'fetch-pdf'`, `STATUS.READY === 'ready'`, `PATENT_TYPE.GRANT === 'grant'`

**Create `tests/unit/shared-matching.test.js`:**
- Import all 10 functions from `../../src/shared/matching.js`
- Smoke test: all 10 imports are typeof 'function'
- Sanity test: `normalizeText('Hello  World')` returns a string
- Sanity test: `matchAndCite('', [])` returns null
- Sanity test: `matchAndCite` with a single-entry positionMap and matching text returns an object with a `citation` property
- Sanity test: `levenshtein('abc', 'abc')` returns 0
- Sanity test: `levenshtein('abc', 'abd')` returns 1

Run `npm test` — all tests including the new smoke tests must pass.
  </action>
  <verify>
    <automated>cd /home/fatduck/patent-cite-tool && npm test</automated>
  </verify>
  <done>
- `tests/unit/shared-constants.test.js` exists and passes (MSG has 16 keys, STATUS has 8 keys, PATENT_TYPE has 2 keys)
- `tests/unit/shared-matching.test.js` exists and passes (all 10 functions importable and basic sanity checks pass)
- Full test suite passes (108 original + new smoke tests)
  </done>
</task>

</tasks>

<verification>
- `npm test` passes with all original 108 tests + new smoke tests (expected ~120 total)
- `grep -c 'export' src/shared/constants.js` returns 3 (one per const)
- `grep -c 'export function' src/shared/matching.js` returns 10
- `grep 'shared/constants.js' src/manifest.json` returns NO matches (removed from content_scripts)
- `grep 'constants-globals.js' src/manifest.json` returns a match
- `grep "import.*from.*shared/constants" src/background/service-worker.js` returns a match
- No `const MSG` in service-worker.js (inline block removed)
</verification>

<success_criteria>
- Shared ESM modules exist and are importable
- Service worker uses shared constants (no inline copy)
- Manifest loads classic wrapper for content scripts
- All existing tests continue to pass
- New smoke tests verify shared module contracts
</success_criteria>

<output>
After completion, create `.planning/phases/14-shared-code-extraction/14-01-SUMMARY.md`
</output>
