---
phase: 15-esbuild-build-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified: []
autonomous: false
requirements: [BUILD-04, BUILD-05]

must_haves:
  truths:
    - "Built Chrome extension loaded from dist/chrome/ produces correct citations on Google Patents"
    - "Extension behavior is functionally identical to the pre-build src/ version"
  artifacts: []
  key_links:
    - from: "dist/chrome/manifest.json"
      to: "dist/chrome/content/content.js"
      via: "Chrome loads content script from manifest"
      pattern: "content/content\\.js"
    - from: "dist/chrome/content/content.js"
      to: "chrome.runtime.sendMessage"
      via: "IIFE bundle runtime messaging"
      pattern: "sendMessage"
---

<objective>
Verify the built Chrome extension is functionally identical to the current raw source.

Purpose: BUILD-04 requires the built Chrome extension to behave identically to loading from src/. This requires a human loading dist/chrome/ in Chrome via "Load unpacked" and testing citation generation on a real Google Patents page.

Output: Verified working built extension, confirming the build pipeline is correct end-to-end.
</objective>

<execution_context>
@/home/fatduck/.claude/get-shit-done/workflows/execute-plan.md
@/home/fatduck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-esbuild-build-pipeline/15-CONTEXT.md
@.planning/phases/15-esbuild-build-pipeline/15-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure fresh build and run test suite</name>
  <files></files>
  <action>
Run a clean build and full test suite to confirm everything is in order before human verification:

1. `npm run build` (clean build)
2. `npm test` (all Vitest tests pass)
3. List key dist/chrome/ files to confirm structure
4. Read dist/chrome/manifest.json and verify content_scripts points to content/content.js
5. Spot-check dist/chrome/content/content.js is IIFE (read first 5 lines)
6. Spot-check dist/chrome/offscreen/offscreen.js has external pdf.mjs import
  </action>
  <verify>
    <automated>cd /home/fatduck/patent-cite-tool && npm run build && npm test && echo "Pre-verification checks passed"</automated>
  </verify>
  <done>Fresh build succeeds, all tests pass, dist structure confirmed correct.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of built Chrome extension</name>
  <files></files>
  <action>
Present the user with verification steps to load and test the built extension in Chrome. The user must load dist/chrome/ via "Load unpacked" and test citation generation on real Google Patents pages.

Verification steps:
1. Open Chrome and go to chrome://extensions/
2. If the existing Patent Citation Tool is loaded from src/, click "Remove" or disable it
3. Click "Load unpacked" and select the `dist/chrome/` directory from this project
4. Navigate to a Google Patents granted patent page, e.g.: https://patents.google.com/patent/US11427642B2
5. Wait for the PDF to be analyzed (toolbar icon should change from gray to colored)
6. Select some text from the patent specification
7. Click the "Cite" floating button
8. Verify a column:line citation appears (e.g., "4:12-15")
9. Check the Chrome DevTools console (F12) for any errors
10. Navigate to a published application page, e.g.: https://patents.google.com/patent/US20210012345A1
11. Select text and verify paragraph citation works (e.g., paragraph [0045])
12. Open the extension options page and verify settings load

Resume signal: Type "approved" if citations work correctly, or describe any issues found.
  </action>
  <verify>User confirms the built extension produces correct citations and has no console errors</verify>
  <done>Human verified that dist/chrome/ extension is functionally identical to the pre-build src/ version. Citations work on both granted patents and published applications.</done>
</task>

</tasks>

<verification>
- Human confirmed dist/chrome/ loads in Chrome without errors
- Citations are generated correctly on granted patents (column:line format)
- Citations work on published applications (paragraph format)
- No console errors in Chrome DevTools
</verification>

<success_criteria>
- BUILD-04: Built Chrome extension is functionally identical to current raw source -- verified by human testing real citations on Google Patents
- BUILD-05: Vitest 71-case corpus passes (verified by automated npm test)
</success_criteria>

<output>
After completion, create `.planning/phases/15-esbuild-build-pipeline/15-03-SUMMARY.md`
</output>
