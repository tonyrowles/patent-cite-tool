---
phase: 23
plan: 03
type: execute
wave: 2
depends_on:
  - 23-01
  - 23-02
files_modified:
  - tests/fixtures/US10203551.json
  - tests/test-cases.js
  - tests/golden/baseline.json
autonomous: false
requirements:
  - ACCY-04
  - ACCY-05
user_setup:
  - service: network
    why: "scripts/generate-fixture.js fetches the PDF from patents.google.com and patentimages.storage.googleapis.com. The execution host must have outbound HTTPS to those domains. No credentials or accounts needed."
    env_vars: []
must_haves:
  truths:
    - "tests/fixtures/US10203551.json exists, is valid JSON, contains a PositionMap array of at least 50 entries"
    - "Every column value in tests/fixtures/US10203551.json is an integer <= 200 (the structurally-derived upper bound from success criterion #2)"
    - "tests/test-cases.js contains a TEST_CASES entry with id starting 'US10203551-' that references tests/fixtures/US10203551.json"
    - "tests/golden/baseline.json contains a key for the new US10203551 test case"
    - "tests/golden/baseline.json has 76 keys total (was 75 before this plan; new case = +1)"
    - "npm run accuracy-report exits 0 with zero regressions against the pre-existing 75 cases"
    - "npm test exits 0 (full suite: build + src tests + chrome + firefox + lint)"
  artifacts:
    - path: "tests/fixtures/US10203551.json"
      provides: "PositionMap fixture generated by buildPositionMap from the real US10203551 PDF (the trigger case for ACCY-04)"
      contains: "column"
    - path: "tests/test-cases.js"
      provides: "TEST_CASES registry updated with at least one US10203551 entry (count: 75 -> 76)"
      contains: "US10203551"
    - path: "tests/golden/baseline.json"
      provides: "Golden baseline regenerated to include US10203551 expected citation (75 -> 76 keys)"
      contains: "US10203551"
  key_links:
    - from: "scripts/generate-fixture.js"
      to: "src/offscreen/position-map-builder.js (buildPositionMap)"
      via: "dynamic import at script line 220; processes PDF text items through the two-pass column inference"
      pattern: "import.*position-map-builder"
    - from: "tests/test-cases.js US10203551 entry"
      to: "tests/fixtures/US10203551.json"
      via: "patentFile field"
      pattern: "tests/fixtures/US10203551.json"
    - from: "scripts/accuracy-report.js"
      to: "tests/golden/baseline.json"
      via: "regression comparison: every TEST_CASES entry's matchAndCite output is checked against the baseline value"
      pattern: "regression"
---

<objective>
Generate a real-PDF integration fixture for the trigger case US10203551, add a TEST_CASES entry, regenerate the golden baseline (75 -> 76 cases), and run the full regression suite. This produces the literal evidence for success criterion #1 ("running the test suite against US10203551 returns correct column numbers — no more impossible values like column 203") via an end-to-end PDF parse-through rather than only synthetic unit tests.

This plan implements the Wave-1 recommendation from the retroactive_context block (Open Question #1: include the integration fixture). It depends on Plan 01 (algorithm invariants verified) and Plan 02 (CACHE_VERSION pinned + Firefox manifest aligned) — those must be green before this plan re-records the golden baseline.

**Criterion-#5 interpretation:** Phase success criterion #5 reads "All 75 existing golden baseline cases continue to pass after the inference change (zero regressions)." Because this plan adds US10203551 to TEST_CASES, the baseline grows from 75 -> 76. The phase interprets criterion #5 as: **"The 75 pre-existing cases continue to produce identical citations; the new US10203551 case is additive and is the criterion-#1 evidence."** State this interpretation in the phase summary.

Purpose: Produce auditable evidence that the on-main algorithm correctly handles US10203551 (the case that originally triggered the v2.3 work). Anchor success criterion #1 to a real-PDF parse rather than a synthetic unit-test shape.

Output:
- `tests/fixtures/US10203551.json` (committed; ~tens to hundreds of KB JSON, NOT a PDF).
- `tests/test-cases.js` with one US10203551 TEST_CASES entry.
- `tests/golden/baseline.json` regenerated with 76 keys.
- `npm run accuracy-report` exits 0 with zero regressions on the pre-existing 75 cases.
- `npm test` exits 0 end-to-end.

Open Question resolved:
- Open Question #1 (US10203551 integration fixture): RESOLVED HERE — fixture is added. Criterion #1 is now anchored to a real-PDF run, not only synthetic unit tests.

**Autonomous flag:** `false`. This plan includes a `checkpoint:human-verify` task at the end to confirm the user accepts the new baseline before phase sign-off. The network fetch in Task 1 is also a potential failure point that may require human inspection if the Google Patents scrape fails repeatedly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-column-inference-for-headerless-pdfs/23-RESEARCH.md
@.planning/phases/23-column-inference-for-headerless-pdfs/23-VALIDATION.md
@scripts/generate-fixture.js
@scripts/update-golden.js
@scripts/accuracy-report.js
@tests/test-cases.js
@tests/golden/baseline.json
@src/offscreen/position-map-builder.js

<interfaces>
<!-- The scripts and registries this plan touches. -->

From scripts/generate-fixture.js (CLI entry):
```
node scripts/generate-fixture.js <patentId>
# Steps: scrape Google Patents -> download PDF -> extractTextFromPdf -> buildPositionMap -> write tests/fixtures/<patentId>.json
# On success: writes JSON file; on no-text-layer or <50-entry maps: skips with warning.
```

From tests/test-cases.js (current count: 75 entries in TEST_CASES array; verified by inspection).
Each entry shape:
```javascript
{
  id: 'US{patent}-{suffix}',
  patentFile: './tests/fixtures/US{patent}.json',
  selectedText: '<text concatenated from PositionMap entries>',
  category: 'modern-short' | 'modern-long' | 'pre2000-short' | 'pre2000-long' | 'chemical' | 'cross-column' | 'claims' | 'repetitive' | 'ocr' | 'gutter',
}
```

From package.json:
```
"update-golden": "node scripts/update-golden.js --confirm"
"accuracy-report": "node scripts/accuracy-report.js"
"test": "npm run build && npm run test:src && npm run test:chrome && npm run test:firefox && npm run test:lint"
```

US10203551 specifics (from research):
- "Headerless" patent — column numbers rendered as graphics, not text. The two-pass fallback (isLikelySpecPage) is what makes this case work.
- Pre-fix bug: PDF.js extracted "203" from the patent number "10203551" as a standalone header item, producing impossible column 203.
- Post-fix expectation: columns should be sequential 1, 2, 3, 4, ... (via fallback inference), all <= ~120 (real patent column count) and trivially <= 200.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate tests/fixtures/US10203551.json via scripts/generate-fixture.js</name>
  <files>
    tests/fixtures/US10203551.json
  </files>
  <read_first>
    - scripts/generate-fixture.js (full file — to understand the URL-scrape + PDF-download + buildPositionMap pipeline and the skip conditions at lines 222–231)
    - tests/fixtures/US10234567.json (read first ~50 lines as a reference for the expected fixture JSON shape — an array of objects with column/line/text/section fields)
    - .planning/phases/23-column-inference-for-headerless-pdfs/23-RESEARCH.md ("Pitfall #4: US10203551 PDF size in fixture commits" — the PDF MUST NOT be committed; only the JSON fixture)
  </read_first>
  <action>
    Step 1: Confirm the fixture does not already exist:
    ```
    test ! -f tests/fixtures/US10203551.json && echo "ok: fixture absent" || echo "FAIL: fixture already present"
    ```
    Expect: "ok: fixture absent". If it exists, STOP and ask the user before overwriting.

    Step 2: Run the fixture generator:
    ```
    node scripts/generate-fixture.js US10203551
    ```
    Expected output ends with a line like: `done. Wrote {N} entries to tests/fixtures/US10203551.json` where N >= 50. If the script reports `WARNING: PositionMap has 0 entries` or `WARNING: PositionMap has only X entries`, STOP and report — this means the algorithm did not infer columns for the headerless PDF and Plan 01 must be revisited. (This is unlikely given the on-main algorithm; failing here would indicate algorithm drift.)

    If the script fails with `HTTP 403` or `No PDF URL found` (network / Google Patents scraping failure), retry up to 2 more times. If it still fails, STOP and report the error verbatim. Do NOT fabricate or hand-write the fixture.

    Step 3: Validate the produced fixture by running this inspection command and recording its output for the summary:
    ```
    node -e 'const fs=require("fs");const m=JSON.parse(fs.readFileSync("tests/fixtures/US10203551.json","utf-8"));const cols=m.map(e=>e.column);console.log("entries:",m.length,"minCol:",Math.min(...cols),"maxCol:",Math.max(...cols),"uniqueCols:",[...new Set(cols)].sort((a,b)=>a-b).slice(0,20).join(","))'
    ```
    Expected output (illustrative):
    - `entries:` >= 50 (matches generate-fixture's skip threshold at line 228).
    - `maxCol:` is a small integer <= 200. **Critical:** any value > 200 means the structural validators failed; STOP and report.
    - `minCol:` is exactly 1 (columns start at 1).
    - `uniqueCols:` starts with `1,2,3,4,5,...` — sequential.

    Step 4: Confirm reasonable file size (PositionMap fixtures in this project are tens to hundreds of KB; multi-megabyte would suggest something went wrong):
    ```
    wc -c tests/fixtures/US10203551.json
    ```
    Expect: < 5,000,000 bytes (5 MB). Typical existing fixtures are 20–500 KB.

    Step 5: Confirm NO PDF file was accidentally written under tests/fixtures/ (Pitfall #4):
    ```
    find tests/fixtures/ -name '*.pdf'
    ```
    Expect: empty output.
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs");if(!fs.existsSync("tests/fixtures/US10203551.json")){console.error("FAIL: fixture missing");process.exit(1)};const m=JSON.parse(fs.readFileSync("tests/fixtures/US10203551.json","utf-8"));if(!Array.isArray(m)||m.length<50){console.error("FAIL: bad array len="+m.length);process.exit(1)};const cols=m.map(e=>e.column);const max=Math.max(...cols);const min=Math.min(...cols);if(max>200){console.error("FAIL maxCol="+max);process.exit(1)};if(min!==1){console.error("FAIL minCol="+min);process.exit(1)};const pdfs=require("child_process").execSync("find tests/fixtures/ -name *.pdf").toString().trim();if(pdfs){console.error("FAIL: pdf committed: "+pdfs);process.exit(1)};console.log("OK entries="+m.length+" min="+min+" max="+max)'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/fixtures/US10203551.json` exits 0
    - `jq 'length' tests/fixtures/US10203551.json` returns an integer >= 50
    - `node -e 'const m=JSON.parse(require("fs").readFileSync("tests/fixtures/US10203551.json","utf-8"));console.log(Math.max(...m.map(e=>e.column)) <= 200)'` prints `true`
    - `node -e 'const m=JSON.parse(require("fs").readFileSync("tests/fixtures/US10203551.json","utf-8"));console.log(Math.min(...m.map(e=>e.column)) === 1)'` prints `true`
    - `find tests/fixtures/ -name '*.pdf'` returns no results (Pitfall #4: no PDF committed)
    - `wc -c tests/fixtures/US10203551.json` reports < 5,000,000 bytes
    - `git status tests/fixtures/US10203551.json` shows the file as new (untracked)
  </acceptance_criteria>
  <done>
    `tests/fixtures/US10203551.json` exists with >= 50 entries, max column <= 200, min column 1, no PDF written; ready for TEST_CASES entry in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add US10203551 TEST_CASES entry and regenerate the golden baseline</name>
  <files>
    tests/test-cases.js
    tests/golden/baseline.json
  </files>
  <read_first>
    - tests/test-cases.js (full file — to understand existing TEST_CASES entry shape and category conventions; current count: 75 entries; the array closes around line 575 just after the synthetic-gutter block)
    - tests/fixtures/US10203551.json (the fixture just created in Task 1 — read at least the first 100 entries to identify a stable `selectedText` from a known column/line position)
    - scripts/update-golden.js (full file — to understand the regeneration flow and the --confirm safety check)
    - scripts/accuracy-report.js (lines 268–310 — regression detection logic, so we know how the report will treat the new case)
  </read_first>
  <action>
    Step 1: Identify a stable `selectedText` for the new TEST_CASES entry. Read `tests/fixtures/US10203551.json` and inspect candidate passages from somewhere in the first 200 entries (description section, column 1 or 2) that are 80–200 characters long and contain distinctive technical content (not generic patent-boilerplate). Run:
    ```
    node -e 'const m=JSON.parse(require("fs").readFileSync("tests/fixtures/US10203551.json","utf-8"));const e=m.filter(x=>x.column<=2 && x.section==="description").slice(0,30);for(const x of e) console.log(JSON.stringify({col:x.column,line:x.line,text:x.text.slice(0,120)}))'
    ```
    From the output, pick 2–3 consecutive entries from the same column (preferably column 1) whose concatenated text forms a coherent 80–200 character selection. Record the chosen `selectedText` exactly as it appears in the fixture (preserving spaces, punctuation, capitalization).

    Step 2: Edit `tests/test-cases.js`. Append the following block just BEFORE the closing `];` of the `TEST_CASES` array (which is around line 575 — the `// Synthetic gutter-number validation` block and its entry precede the closing bracket). Use exactly this structure, substituting the actual `selectedText` chosen in Step 1:

    ```javascript
      // =========================================================================
      // Headerless PDF trigger case — US10203551 (Phase 23, ACCY-04)
      // The bug: PDF.js extracted "203" from patent number "10203551" as a
      // standalone header item, yielding impossible column 203. The fix
      // (structural validators + two-pass fallback inference) now produces
      // correct sequential columns. This entry is the integration-level proof.
      // =========================================================================
      {
        id: 'US10203551-spec-short',
        patentFile: './tests/fixtures/US10203551.json',
        selectedText: '<INSERT_EXACT_TEXT_FROM_STEP_1>',
        category: 'modern-short',
      },
    ```

    Replace `<INSERT_EXACT_TEXT_FROM_STEP_1>` with the actual text. Escape any single quotes or backslashes in the selectedText as needed for JavaScript string literals — match the conventions of nearby entries (some use `\\"` escaping for embedded quotes; see lines 456, 468, 474 for examples).

    Step 3: Confirm the file still parses as JavaScript and the count is 76:
    ```
    node -e 'import("./tests/test-cases.js").then(m=>{console.log("entries:", m.TEST_CASES.length);if(m.TEST_CASES.length!==76){process.exit(1)}}).catch(e=>{console.error(e);process.exit(1)})'
    ```
    Expect: `entries: 76` and exit 0.

    Step 4: Regenerate the golden baseline:
    ```
    npm run update-golden
    ```
    The package.json script automatically passes `--confirm`. Expected: the script writes `tests/golden/baseline.json` and exits 0.

    Step 5: Validate baseline growth and the new entry's citation:
    ```
    jq 'length' tests/golden/baseline.json
    ```
    Expect: `76`.

    ```
    jq 'has("US10203551-spec-short")' tests/golden/baseline.json
    ```
    Expect: `true`.

    ```
    jq '."US10203551-spec-short"' tests/golden/baseline.json
    ```
    Expect: an object with `citation` (e.g., `"1:5-8"` or `"2:12-15"`) and `confidence` (a number >= 0). The citation MUST parse as `{column}:{startLine}-{endLine}` where column <= 200. If `citation` is `null`, the test case's `selectedText` is not findable in the fixture — go back to Step 1, pick a different passage, and re-run.

    Programmatic assertion:
    ```
    node -e 'const b=require("./tests/golden/baseline.json");const e=b["US10203551-spec-short"];if(!e||!e.citation){console.error("FAIL: no citation");process.exit(1)};const col=parseInt(e.citation.split(":")[0],10);if(!Number.isInteger(col)||col>200||col<1){console.error("FAIL: bad column "+col);process.exit(1)};console.log("OK citation="+e.citation+" confidence="+e.confidence)'
    ```
    Expect: exit 0 with `OK citation={col}:{lines} confidence={n}`.
  </action>
  <verify>
    <automated>node -e 'const cp=require("child_process");if(parseInt(cp.execSync("jq length tests/golden/baseline.json").toString().trim(),10)!==76){console.error("FAIL: baseline length");process.exit(1)};const b=require("./tests/golden/baseline.json");const e=b["US10203551-spec-short"];if(!e||!e.citation){console.error("FAIL: no citation for new case");process.exit(1)};const col=parseInt(e.citation.split(":")[0],10);if(!Number.isInteger(col)||col>200||col<1){console.error("FAIL: bad column "+col);process.exit(1)};console.log("OK new-case col="+col+" citation="+e.citation)'</automated>
  </verify>
  <acceptance_criteria>
    - `jq 'length' tests/golden/baseline.json` returns `76`
    - `jq 'has("US10203551-spec-short")' tests/golden/baseline.json` returns `true`
    - `jq -r '."US10203551-spec-short".citation' tests/golden/baseline.json` returns a string matching pattern `^[0-9]+:[0-9]+(-[0-9]+)?$` whose first numeric segment is between 1 and 200 inclusive
    - `node -e 'import("./tests/test-cases.js").then(m=>process.exit(m.TEST_CASES.length===76?0:1))'` exits 0
    - `grep -c "US10203551-spec-short" tests/test-cases.js` returns 1 (one new entry; no duplicates)
    - `grep -c "./tests/fixtures/US10203551.json" tests/test-cases.js` returns 1
    - All 75 pre-existing entries in `tests/golden/baseline.json` still exist (no key deletions). Verify with: `jq 'keys | length' tests/golden/baseline.json` returns 76; the new key is the only addition relative to the pre-plan baseline.
  </acceptance_criteria>
  <done>
    TEST_CASES has 76 entries including US10203551-spec-short; the golden baseline has 76 keys with the new case's citation in `{col}:{lines}` form where column <= 200; the pre-existing 75 keys are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run accuracy-report and full npm test to confirm zero regressions on pre-existing cases</name>
  <files>
    <!-- No files modified; this task runs verification commands only. -->
  </files>
  <read_first>
    - scripts/accuracy-report.js (full file — to understand regression definitions and exit codes; lines 268–310 specifically for the regression-detection branch)
    - tests/golden/baseline.json (updated in Task 2)
    - tests/test-cases.js (updated in Task 2)
  </read_first>
  <action>
    Step 1: Run the accuracy report:
    ```
    npm run accuracy-report
    ```
    Expected: exit 0. The output reports per-category accuracy and a regression list. The regression list MUST be empty (no pre-existing case has a different citation than its golden baseline). The new US10203551-spec-short case is "new" relative to `tests/golden/pre-fix-baseline.json` (if present) and will not appear in the regression list because it has no prior baseline entry to compare against.

    Capture the full output to `/tmp/v23p3t3-accuracy.log` for the phase summary:
    ```
    npm run accuracy-report 2>&1 | tee /tmp/v23p3t3-accuracy.log
    ```

    Step 2: Assert "0 regressions" appears in the output. The exact wording in accuracy-report.js is "Regressions:" followed by an ID list when nonzero; when zero, the script may print "Regressions:" with empty value or omit the line. Be lenient about wording but strict about exit code: the exit code from `npm run accuracy-report` MUST be 0.

    Step 3: Run the full test suite end-to-end:
    ```
    npm test
    ```
    This runs `npm run build && npm run test:src && npm run test:chrome && npm run test:firefox && npm run test:lint` in sequence. Expect: exit 0 across all five sub-steps.

    Capture the output to `/tmp/v23p3t3-fulltest.log`:
    ```
    npm test 2>&1 | tee /tmp/v23p3t3-fulltest.log
    ```

    Step 4: Spot-check that the new US10203551 test case produces a citation when run through the algorithm directly (not just via baseline comparison):
    ```
    node scripts/spot-check.js US10203551-spec-short
    ```
    If `scripts/spot-check.js` does not accept a single-case argument or does not exist, skip this step. The accuracy-report run from Step 1 already exercises every TEST_CASES entry via matchAndCite. (Confirm spot-check.js existence with `test -f scripts/spot-check.js` first; if absent, document the skip in the summary.)
  </action>
  <verify>
    <automated>bash -c 'set -e; npm run accuracy-report; npm test'</automated>
  </verify>
  <acceptance_criteria>
    - `npm run accuracy-report` exits 0
    - The accuracy-report output does not list any of the original 75 case IDs in a "Regressions:" line (manual scan of `/tmp/v23p3t3-accuracy.log`; the line either reads "Regressions:" with no IDs or is absent)
    - `npm test` exits 0 (covers build + test:src + test:chrome + test:firefox + test:lint)
    - `/tmp/v23p3t3-accuracy.log` exists and is non-empty
    - `/tmp/v23p3t3-fulltest.log` exists and is non-empty
  </acceptance_criteria>
  <done>
    `npm run accuracy-report` reports zero regressions on the 75 pre-existing cases; `npm test` is green end-to-end; output logs are captured for the phase summary.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — accept new baseline and confirm phase exit</name>
  <files>
    <!-- No files modified by this task; it is a human-verification gate. -->
  </files>
  <read_first>
    - tests/fixtures/US10203551.json (the new fixture)
    - tests/golden/baseline.json (the regenerated baseline; jq the new key)
    - .planning/phases/23-column-inference-for-headerless-pdfs/23-RESEARCH.md (success criteria interpretation)
  </read_first>
  <action>
    This task is a checkpoint:human-verify gate. The executor has completed all automated work in Tasks 1–3 of this plan, plus Plans 01 and 02. Before declaring Phase 23 complete, the executor MUST pause and ask the user to confirm the diff scope and the new baseline are acceptable.

    What was built (for the user to verify):
    - New integration fixture `tests/fixtures/US10203551.json` generated from the real PDF via `scripts/generate-fixture.js`.
    - New TEST_CASES entry `US10203551-spec-short` in `tests/test-cases.js`.
    - Golden baseline regenerated: 75 -> 76 keys.
    - Accuracy report shows zero regressions on the 75 pre-existing cases.
    - Full `npm test` is green end-to-end.
    - Plan 01 added 4 guard tests to `tests/unit/position-map-builder.test.js` (total: 34).
    - Plan 02 added `tests/unit/cache-version.test.js` and bumped Firefox manifest to 2.3.0.

    How the user should verify (executor presents these commands; user runs and reports back):

    1. Inspect the new fixture for sanity:
       ```
       jq 'length' tests/fixtures/US10203551.json
       jq '[.[].column] | unique | sort' tests/fixtures/US10203551.json | head -30
       ```
       Confirm length >= 50 and columns are sequential starting at 1, with max column <= 200 (likely <= 120).

    2. Inspect the new baseline entry:
       ```
       jq '."US10203551-spec-short"' tests/golden/baseline.json
       ```
       Confirm citation is in `{col}:{lines}` form with column in 1..200.

    3. Re-run the accuracy report:
       ```
       npm run accuracy-report
       ```
       Confirm exit 0 and no regression IDs listed.

    4. Inspect the diff to confirm scope is correct:
       ```
       git status
       git diff --stat
       ```
       Expected changed files across the phase:
       - `tests/fixtures/US10203551.json` (new file, from Plan 03)
       - `tests/test-cases.js` (one new entry block, from Plan 03)
       - `tests/golden/baseline.json` (one new key + possibly reformatting if update-golden rewrites the whole file, from Plan 03)
       - `tests/unit/position-map-builder.test.js` (one new describe block "Phase 23 structural-validator guards", from Plan 01)
       - `tests/unit/cache-version.test.js` (new file, from Plan 02)
       - `src/manifest.firefox.json` (one-line version bump 2.2.0 -> 2.3.0, from Plan 02)
       NO changes to `src/offscreen/`, `src/firefox/pdf-pipeline.js`, `worker/`, or any production code paths. NO new PDF files.

    5. Confirm Plans 01 and 02 also landed cleanly:
       ```
       grep -c "Phase 23 structural-validator guards" tests/unit/position-map-builder.test.js  # expect 1
       test -f tests/unit/cache-version.test.js                                                # expect exit 0
       grep -c '"version": "2.3.0"' src/manifest.firefox.json                                  # expect 1
       ```

    Resume signal: User responds "approved" to mark Phase 23 complete, or describes specific issues to be addressed before approval.
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — automated check runs the same assertions as Plan 01/02/03 acceptance" && bash -c "set -e; grep -q 'Phase 23 structural-validator guards' tests/unit/position-map-builder.test.js; test -f tests/unit/cache-version.test.js; grep -q '\"version\": \"2.3.0\"' src/manifest.firefox.json; test -f tests/fixtures/US10203551.json; test \$(jq length tests/golden/baseline.json) -eq 76; echo OK"</automated>
    <human>Type "approved" to confirm the baseline and diff are acceptable, or describe required fixes.</human>
  </verify>
  <acceptance_criteria>
    - User responds with "approved" (or equivalent affirmative).
    - If user reports any issue (e.g., wrong column in fixture, regression in accuracy report, unexpected file in diff), the plan is NOT complete and the executor must fix-or-revert before re-running this checkpoint.
    - The automated portion of <verify> exits 0 (mechanical preconditions for the human to approve).
  </acceptance_criteria>
  <done>
    User explicitly approves the baseline change and the diff scope. Plan 03 is then marked complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network -> developer machine | `scripts/generate-fixture.js` fetches HTML from `patents.google.com` and a PDF from `patentimages.storage.googleapis.com`. Both are trusted Google-operated endpoints over HTTPS. The PDF is then handed to PDF.js (already validated by existing buildPositionMap pipeline). |
| Generated fixture -> golden baseline | The fixture's column values flow through `matchAndCite` to produce the golden baseline citation. If the fixture were tampered with, the baseline citation could be wrong but the structural validators (Plan 01) would catch any column > 200. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | Tampering (integrity) | tests/fixtures/US10203551.json (the generated fixture) | mitigate | Task 1 acceptance criterion asserts `max(column) <= 200` and `min(column) === 1`. Any fixture that violates either would be caught before TEST_CASES is updated. |
| T-23-08 | Tampering (integrity) | tests/golden/baseline.json (the regenerated baseline) | mitigate | Task 3 runs `npm run accuracy-report` which compares every pre-existing case's matchAndCite output to its stored baseline. Any regression on the 75 pre-existing cases would surface in the regression list. |
| T-23-09 | Denial of Service | scripts/generate-fixture.js network fetches | accept | Generator is run once, manually, in this plan. If Google Patents is unreachable, Task 1 retries up to 3 times then halts with a clear error message. Low severity (one-time operation, not a production code path). |
| T-23-10 | Information disclosure | committed PDF (Pitfall #4) | mitigate | Task 1 acceptance criterion asserts `find tests/fixtures/ -name '*.pdf'` returns empty — only the derived JSON fixture is committed, matching the existing convention of 22 JSON-only fixtures. |

No new attack surface in production code — the fixture is test-only and never executed in the user-facing extension. All severities below "high".
</threat_model>

<verification>
- `tests/fixtures/US10203551.json` exists with >= 50 entries, columns 1..N where N <= 200 (Task 1).
- `tests/test-cases.js` has 76 TEST_CASES entries including `US10203551-spec-short` (Task 2).
- `tests/golden/baseline.json` has 76 keys; new key's citation column is between 1 and 200 (Task 2).
- `npm run accuracy-report` exits 0 with zero regressions on the pre-existing 75 cases (Task 3).
- `npm test` exits 0 end-to-end (Task 3).
- No PDFs committed under tests/fixtures/ (Tasks 1 + 4).
- Human approves the diff scope (Task 4).
</verification>

<success_criteria>
Plan 03 is complete when:
- Task 1: Fixture exists, valid JSON, >= 50 entries, max column <= 200, min column 1, no PDF artifacts.
- Task 2: TEST_CASES count is 76, baseline.json has 76 keys including US10203551-spec-short with a sensible citation.
- Task 3: `npm run accuracy-report` and `npm test` both exit 0.
- Task 4: User approves the final diff.

Phase success criteria addressed by this plan:
- #1 (correct columns for US10203551 — real-PDF evidence): Task 1's fixture is generated from the real PDF; the max-column-<=-200 assertion proves the algorithm produces sensible values, not impossible 203.
- #5 (75 existing cases continue to pass): Task 3's accuracy-report run confirms zero regressions on the pre-existing 75 cases. The new US10203551 case is additive (criterion interpretation stated in objective).
- #4 (CACHE_VERSION invariant — indirectly verified): The full `npm test` run in Task 3 includes `npm run test:src` which exercises the cache-version guard test from Plan 02.

Phase exit gate (after all three plans):
- Plan 01 -> 34/34 unit tests pass, all grep invariants hold.
- Plan 02 -> 4/4 cache-version guard tests pass, Firefox manifest is 2.3.0, `web-ext lint` clean.
- Plan 03 -> US10203551 integration fixture exists, baseline at 76 entries with zero regressions on the original 75, `npm test` green.
- Phase summary `.planning/phases/23-column-inference-for-headerless-pdfs/23-SUMMARY.md` written documenting:
  - Structural validators (odd-left + sequential cross-page) in extractPrintedColumnNumbers/buildPositionMap.
  - Two-pass design with isLikelySpecPage gating the fallback.
  - CACHE_VERSION='v3' invariant at both client sites + the new guard test.
  - Firefox manifest version aligned to 2.3.0.
  - US10203551 fixture provenance (generated, not hand-written; no PDF committed).
  - Criterion-#5 interpretation note.
</success_criteria>

<output>
After completion, create `.planning/phases/23-column-inference-for-headerless-pdfs/23-03-SUMMARY.md` documenting:
- The generate-fixture.js output showing the fixture was produced from the real PDF.
- The chosen `selectedText` and the rationale for picking it (which column/line in the fixture).
- The new baseline entry's citation and confidence.
- The accuracy-report output (zero regressions on the 75 pre-existing cases).
- The full `npm test` exit status and any noteworthy output.
- The git diff stat showing only test-side files changed (no `src/`, no `worker/`, no PDFs).

Then write `.planning/phases/23-column-inference-for-headerless-pdfs/23-SUMMARY.md` (phase-level summary) consolidating findings across Plans 01, 02, and 03, including the criterion-#5 interpretation note.
</output>
