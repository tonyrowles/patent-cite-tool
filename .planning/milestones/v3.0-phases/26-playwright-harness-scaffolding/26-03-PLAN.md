---
phase: 26-playwright-harness-scaffolding
plan: 03
type: execute
wave: 2
depends_on: ["26-01", "26-02"]
files_modified:
  - tests/e2e/specs/smoke.spec.js
  - package.json
autonomous: true
requirements: [HARN-01, HARN-02]
tags: [playwright, e2e, smoke, npm-script, integration]

must_haves:
  truths:
    - "`npm run e2e:smoke` exits 0 against a freshly-built dist/chrome/"
    - "The smoke spec uses only imports from tests/e2e/lib/ — no inline harness logic (HARN-05 enforcement)"
    - "The smoke spec asserts the extension's chrome-extension://<id> is a valid 32-char extension ID (HARN-01)"
    - "The smoke spec asserts the SW probe completed (extensionId non-empty implies HARN-02 path was traversed)"
    - "The smoke spec asserts the addInitScript shadow-open shim is functional (probe shadow root created with mode:'closed' is readable via host.shadowRoot)"
    - "The smoke spec runs in headless via channel:'chromium' — no xvfb-run needed"
    - "package.json contains an `e2e:smoke` script that chains `npm run build:chrome` before `playwright test`"
  artifacts:
    - path: "tests/e2e/specs/smoke.spec.js"
      provides: "Single smoke spec proving load + navigate + SW ready + shadow shim works"
      exports: []
      min_lines: 15
    - path: "package.json"
      provides: "e2e:smoke npm script chained from build:chrome"
      contains: "e2e:smoke"
  key_links:
    - from: "tests/e2e/specs/smoke.spec.js"
      to: "tests/e2e/lib/extension-loader.js"
      via: "import { loadExtension } from '../lib/extension-loader.js'"
      pattern: "from '../lib/extension-loader"
    - from: "tests/e2e/specs/smoke.spec.js"
      to: "tests/e2e/lib/navigation.js"
      via: "import { gotoPatent } from '../lib/navigation.js'"
      pattern: "from '../lib/navigation"
    - from: "package.json scripts.e2e:smoke"
      to: "tests/e2e/playwright.config.js"
      via: "playwright test --config tests/e2e/playwright.config.js"
      pattern: "playwright test --config tests/e2e/playwright\\.config\\.js"
    - from: "package.json scripts.e2e:smoke"
      to: "dist/chrome/"
      via: "npm run build:chrome && playwright test"
      pattern: "build:chrome && playwright test"
---

<objective>
Compose Plan 01's installed runner and Plan 02's library into a working smoke spec, and wire it into npm:

1. `tests/e2e/specs/smoke.spec.js` — one Playwright test that calls `loadExtension`, navigates to `US11427642`, asserts the extension is loaded (HARN-01) with a valid extension ID (HARN-02 SW probe path), and confirms the addInitScript shadow-open shim is functional (HARN-03 sanity probe).
2. `package.json` — add `"e2e:smoke": "npm run build:chrome && playwright test --config tests/e2e/playwright.config.js"` so a single command produces a fresh dist/chrome/ and runs the smoke spec.
3. Run `npm run e2e:smoke` and confirm green.

Purpose: This is the phase's success-or-fail gate. If `npm run e2e:smoke` exits 0, HARN-01, HARN-02, and HARN-05 are all live, and HARN-03 has a working sanity test. (HARN-04 is installed but unexercised in smoke per CONTEXT.md — Phase 27 owns the clipboard end-to-end test.)

Output: One spec file (under 30 lines), one new npm script, one verified green run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-playwright-harness-scaffolding/26-CONTEXT.md
@.planning/phases/26-playwright-harness-scaffolding/26-RESEARCH.md
@.planning/phases/26-playwright-harness-scaffolding/26-VALIDATION.md
@.planning/phases/26-playwright-harness-scaffolding/26-01-PLAN.md
@.planning/phases/26-playwright-harness-scaffolding/26-02-PLAN.md
@tests/golden/baseline.json
@package.json

<interfaces>
This plan consumes the public API created in Plan 02:

```
// from ../lib/extension-loader.js
loadExtension({ extensionPath }):
  Promise<{ context, page, extensionId: string, cleanup: () => Promise<void> }>

// from ../lib/navigation.js
gotoPatent(page, patentId): Promise<Response>
```

Smoke seed (per CONTEXT.md): `US11427642` — first entry in tests/golden/baseline.json, post-2000, non-OCR.

Extension ID validation: a Chrome unpacked-extension ID is a 32-character string of lowercase letters a-p. Regex: `/^[a-p]{32}$/`.

Shadow shim probe (per CONTEXT.md Specifics #d and RESEARCH.md "Pattern 1: Library-first / Runner-thin"): create a `<div>`, call `el.attachShadow({mode:'closed'})`, then expect `el.shadowRoot` to be truthy. Without the shim, `el.shadowRoot` would be `null`.

What the smoke MUST NOT do (per CONTEXT.md Specifics):
- Do NOT trigger any text selection (Phase 27).
- Do NOT assert the extension's citation host element exists on the page (it's created lazily on selection — see RESEARCH.md Open Question #5).
- Do NOT call `readCitationPill` or `readClipboardShim` (no selection means nothing to read).

What the smoke SHOULD assert (per RESEARCH.md "Pattern 1" sketch):
- `extensionId` matches `/^[a-p]{32}$/`
- Navigation returns 200 (gotoPatent already throws on non-2xx, so absence of throw is the assertion)
- The addInitScript shim is functional via a probe shadow root in `page.evaluate`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/e2e/specs/smoke.spec.js</name>
  <files>tests/e2e/specs/smoke.spec.js</files>
  <read_first>
    - .planning/phases/26-playwright-harness-scaffolding/26-RESEARCH.md section "Pattern 1: Library-first / Runner-thin" — the smoke.spec.js sketch at lines ~177-209
    - .planning/phases/26-playwright-harness-scaffolding/26-CONTEXT.md "Specifics" section (smoke spec assertions list)
    - .planning/phases/26-playwright-harness-scaffolding/26-VALIDATION.md "Per-Task Verification Map" rows 26-03-01 and 26-03-02
    - tests/e2e/lib/extension-loader.js (must exist from Plan 02 — confirms the import shape)
    - tests/e2e/lib/navigation.js (must exist from Plan 02)
    - tests/e2e/playwright.config.js (must exist from Plan 02 — confirms testDir './specs')
    - tests/golden/baseline.json (confirms US11427642 is the first entry)
  </read_first>
  <action>
    Create `tests/e2e/specs/smoke.spec.js`. First run `mkdir -p tests/e2e/specs`. The spec MUST be a thin recipe — all logic lives in tests/e2e/lib/. Target length: 25-30 lines.

    Exact file content:

    ```
    // tests/e2e/specs/smoke.spec.js
    //
    // Phase 26 smoke spec. Proves the harness chain works end-to-end:
    //   1. Playwright loads the unpacked Chrome extension (HARN-01).
    //   2. Service-worker readiness probe completes (HARN-02 path — extensionId
    //      is non-empty only if the probe succeeded).
    //   3. Navigation to the seed patent succeeds (HARN-01).
    //   4. The shadow-open shim is installed and functional (HARN-03 sanity —
    //      a closed-mode shadow root created in a probe page.evaluate is
    //      readable via host.shadowRoot, which is only possible because the
    //      addInitScript override flipped mode to 'open').
    //
    // The smoke deliberately does NOT trigger a selection. Phase 27 exercises
    // selection, the citation pill, and the clipboard shim end-to-end.

    import path from 'node:path';
    import { fileURLToPath } from 'node:url';
    import { test, expect } from '@playwright/test';
    import { loadExtension } from '../lib/extension-loader.js';
    import { gotoPatent } from '../lib/navigation.js';

    const __dirname = path.dirname(fileURLToPath(import.meta.url));
    const EXTENSION_PATH = path.resolve(__dirname, '../../../dist/chrome');
    const SEED_PATENT = 'US11427642';

    test.describe('Phase 26 smoke', () => {
      test('loads extension, navigates seed patent, SW ready, shadow shim functional', async () => {
        const { page, extensionId, cleanup } = await loadExtension({
          extensionPath: EXTENSION_PATH,
        });
        try {
          // HARN-01 + HARN-02: extensionId is derived from sw.url() AFTER the
          // chrome.runtime.id probe — non-empty means both passed.
          expect(extensionId).toMatch(/^[a-p]{32}$/);

          // HARN-01: navigate to a real Google Patents page; gotoPatent throws
          // on non-2xx or missing readiness markers, so absence-of-throw asserts.
          await gotoPatent(page, SEED_PATENT);

          // HARN-03 sanity: prove the addInitScript shadow-open shim is live.
          // Without the shim, attachShadow({mode:'closed'}) would return a root
          // that is unreadable via host.shadowRoot (null). With the shim,
          // host.shadowRoot is the same root that attachShadow returned.
          const shimOk = await page.evaluate(() => {
            const el = document.createElement('div');
            const sr = el.attachShadow({ mode: 'closed' });
            return sr !== null && el.shadowRoot !== null && el.shadowRoot === sr;
          });
          expect(shimOk).toBe(true);
        } finally {
          await cleanup();
        }
      });
    });
    ```

    Critical constraints:
    - The spec is under 50 lines including comments; the body of the test (between try and finally) is ~10 LOC of actual test logic. HARN-05 enforcement.
    - DO NOT add a `getCitation` or `readCitationPill` call — no selection is triggered, so no pill exists.
    - DO NOT add a clipboard shim assertion — no copy event is triggered, so `__lastCopiedText__` remains empty (which is correct, but uninteresting to assert).
    - DO NOT add a `data-testid` assertion on the extension UI — the host is created lazily on selection (RESEARCH.md Open Question #5).
    - DO NOT add retry logic, screenshot logic, or trace logic — playwright.config.js handles those at the runner level (trace: retain-on-failure, screenshot: only-on-failure).
    - DO NOT add multiple test cases or `test.each` — one spec, one test. Phase 27 owns the 76-case replay.
    - Always wrap the loadExtension call in try/finally so cleanup runs even if assertions throw.

    The EXTENSION_PATH resolution computes: `tests/e2e/specs/smoke.spec.js` -> `../../..` is repo root, then `/dist/chrome` is the build artifact. Verify the resolved path:
    ```
    realpath tests/e2e/specs/smoke.spec.js
    # repo/tests/e2e/specs/smoke.spec.js
    # ../../../dist/chrome from that → repo/dist/chrome
    ```
  </action>
  <verify>
    <automated>test -f tests/e2e/specs/smoke.spec.js && grep -q "from '@playwright/test'" tests/e2e/specs/smoke.spec.js && grep -q "from '../lib/extension-loader.js'" tests/e2e/specs/smoke.spec.js && grep -q "from '../lib/navigation.js'" tests/e2e/specs/smoke.spec.js && grep -q "US11427642" tests/e2e/specs/smoke.spec.js && grep -q "toMatch(/\^\[a-p\]{32}\$/)" tests/e2e/specs/smoke.spec.js && grep -q "attachShadow({ mode: 'closed' })" tests/e2e/specs/smoke.spec.js && grep -q "cleanup()" tests/e2e/specs/smoke.spec.js && node --check tests/e2e/specs/smoke.spec.js && [ "$(wc -l < tests/e2e/specs/smoke.spec.js)" -lt 60 ]</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/specs/smoke.spec.js` exists
    - Contains literal `import { test, expect } from '@playwright/test'`
    - Contains literal `from '../lib/extension-loader.js'`
    - Contains literal `from '../lib/navigation.js'`
    - Contains literal `US11427642` (the seed patent)
    - Contains regex literal `/^[a-p]{32}$/` (extension-ID shape check)
    - Contains literal `attachShadow({ mode: 'closed' })` inside a `page.evaluate` (the shim probe)
    - Contains `cleanup()` call inside a `finally` block
    - File is fewer than 60 lines total (HARN-05 thin-spec enforcement)
    - Does NOT contain any inline `chromium.launchPersistentContext` call (must use loadExtension)
    - Does NOT contain any inline `page.goto('https://patents.google.com/...')` (must use gotoPatent)
    - Does NOT contain `readCitationPill` or `readClipboardShim` calls (no selection in smoke)
    - `node --check tests/e2e/specs/smoke.spec.js` exits 0
  </acceptance_criteria>
  <done>One smoke spec exists under tests/e2e/specs/, imports only from ../lib/, asserts the three HARN-01/02/03 conditions, wraps in try/finally for cleanup, and stays under 60 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Add `e2e:smoke` npm script to package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json (CURRENT state — Plan 01 added @playwright/test devDependency but no new scripts)
    - .planning/phases/26-playwright-harness-scaffolding/26-CONTEXT.md "npm Scripts" section
    - .planning/phases/26-playwright-harness-scaffolding/26-RESEARCH.md section "package.json script additions" (lines ~538-547)
    - tests/e2e/playwright.config.js (must exist from Plan 02 — confirms the config path)
  </read_first>
  <action>
    Add ONE new npm script to `package.json` under `"scripts"`. The script chains `npm run build:chrome` (to ensure dist/chrome/ is fresh) before invoking Playwright. Use the existing `build:chrome` (Chrome-only) rather than `build` (which builds Chrome + Firefox + test-exports) for speed — Phase 26 smoke only uses dist/chrome/.

    Add the following key/value into the `"scripts"` object. Preserve all existing scripts unchanged. Maintain alphabetical-ish ordering: insert `e2e:smoke` between `dev` and `test` (the existing scripts go: build, build:chrome, build:firefox, dev, test, test:src, test:chrome, test:firefox, test:lint, test:watch, update-golden, accuracy-report, generate-icons). The new key sorts after `dev` and before `test`:

    Exact new script line (with comma where required):

    ```
    "e2e:smoke": "npm run build:chrome && playwright test --config tests/e2e/playwright.config.js",
    ```

    After insertion, the `scripts` block reads (showing surrounding lines for context):

    ```
    "scripts": {
      "build": "node scripts/build.js",
      "build:chrome": "node scripts/build.js --chrome-only",
      "build:firefox": "node scripts/build.js --firefox-only",
      "dev": "node scripts/build.js --watch",
      "e2e:smoke": "npm run build:chrome && playwright test --config tests/e2e/playwright.config.js",
      "test": "npm run build && npm run test:src && npm run test:chrome && npm run test:firefox && npm run test:lint",
      "test:src": "vitest run",
      "test:chrome": "vitest run --config vitest.config.chrome.js",
      "test:firefox": "vitest run --config vitest.config.firefox.js",
      "test:lint": "npx web-ext lint --source-dir dist/firefox --ignore-files 'lib/**'",
      "test:watch": "vitest",
      "update-golden": "node scripts/update-golden.js --confirm",
      "accuracy-report": "node scripts/accuracy-report.js",
      "generate-icons": "node scripts/generate-icons.mjs"
    },
    ```

    Critical constraints:
    - DO NOT modify any existing script command. DO NOT modify the existing `"test"` script to include e2e (that would couple unit tests to e2e — and CI/PR runs already run `npm test`).
    - DO NOT add `--workers=` or `--retries=` overrides on the command — playwright.config.js owns those.
    - DO NOT explicitly pass `specs/smoke.spec.js` — the config's `testDir` and Playwright's auto-discovery handle it. Passing it explicitly would couple this script to one spec, which works against Phase 27's later expansion.
    - DO NOT add a `e2e` or `e2e:full` script yet — Phase 27 will add the full-suite command.
    - Maintain valid JSON. Run `node -e "JSON.parse(require('fs').readFileSync('package.json', 'utf8'))"` to confirm.

    Validate the script is correctly wired by running it. This is the phase's success gate:

    ```
    npm run e2e:smoke
    ```

    Expected outcome: exit 0, with output showing the Chromium build completes, Playwright launches, the one smoke test passes, and the persistent context cleans up. Total runtime ~20-40s on a healthy machine.

    Failure-mode triage:
    - If `npx playwright: command not found` -> Plan 01's install did not run. Re-execute Plan 01 Task 1.
    - If `chromium not installed` -> Plan 01 Task 1's `npx playwright install chromium` did not run; re-execute.
    - If `loadExtension: no manifest.json found at .../dist/chrome` -> `npm run build:chrome` did not produce dist/chrome/. Investigate scripts/build.js.
    - If shadow probe fails (`shimOk` is false) -> the addInitScript wiring in Plan 02's extension-loader.js is broken; verify both `addInitScript` calls precede `newPage()`.
    - If SW probe times out -> Plan 02's loader is missing the `waitForEvent('serviceworker', { timeout: 10_000 })` call, OR the test machine is slow. Bump the timeout to 20_000 only if multiple retries confirm it's a machine-speed issue, not a logic bug.
    - If Google Patents nav 4xx/5xx -> transient network issue or rate limit. Retry once. If persistent, RESEARCH.md notes the seed `US11427642` is the most-stable choice; alternative seeds are listed in `tests/golden/baseline.json`.
  </action>
  <verify>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('package.json', 'utf8')); if (p.scripts['e2e:smoke'] !== 'npm run build:chrome && playwright test --config tests/e2e/playwright.config.js') process.exit(1); console.log('script ok')" && npm run e2e:smoke</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `scripts` object contains exactly the literal key/value: `"e2e:smoke": "npm run build:chrome && playwright test --config tests/e2e/playwright.config.js"`
    - `package.json` parses as valid JSON (`node -e "JSON.parse(require('fs').readFileSync('package.json', 'utf8'))"` exits 0)
    - All pre-existing scripts (`build`, `build:chrome`, `build:firefox`, `dev`, `test`, `test:src`, `test:chrome`, `test:firefox`, `test:lint`, `test:watch`, `update-golden`, `accuracy-report`, `generate-icons`) remain present and unchanged
    - The `"test"` script value remains `"npm run build && npm run test:src && npm run test:chrome && npm run test:firefox && npm run test:lint"` (NOT modified to include e2e)
    - `npm run e2e:smoke` exits 0 with the smoke spec reported as passed (1 passed, 0 failed)
    - The run produces no test failures, no flake warnings, and no Playwright deprecation errors on `channel: 'chromium'` or addInitScript usage
    - `dist/chrome/content/content.js` (regenerated by build:chrome inside the script chain) contains both `pct-citation-host` and `pct-citation-pill` literals — verifies the HOOK-01 source edits flow through the build correctly
  </acceptance_criteria>
  <done>`e2e:smoke` script is added to package.json; running `npm run e2e:smoke` against a fresh-built dist/chrome/ exits 0 with the smoke spec passing — proves HARN-01, HARN-02, HARN-03 (sanity), and HARN-05 are all live.</done>
</task>

</tasks>

<threat_model>
  <threat id="T-26-02" asvs="L1" stride="Information Disclosure" owner="26-01">
    <description>data-testid attributes added to citation UI elements (HOOK-01). Reference-only here — the source change is owned by plan 26-01; plans 02 and 03 only consume those attributes via selectors.</description>
    <mitigation>Owned by plan 26-01 — attributes are HTML-only, no JS/secret/PII change; verifiable by grep that no functional code paths changed (see 26-01-PLAN.md T-26-02 mitigation).</mitigation>
  </threat>
  <threat id="T-26-01" asvs="L1" stride="Tampering">
    <description>Supply-chain risk from the @playwright/test devDependency (installed by Plan 01) being executed in this plan via `npm run e2e:smoke`. A compromised package could exfiltrate from the test environment.</description>
    <mitigation>Plan 01 pinned the exact version 1.60.0 (published 2026-05-11, no caret) and committed package-lock.json. The smoke spec runs in a fresh tmpdir userDataDir (loader contract), cleaned on test end. No production secrets, credentials, or PII are exposed to the test environment — only public Google Patents URLs are visited. The CI/cron exposure is owned by Phase 29 (out of scope here).</mitigation>
  </threat>
  <threat id="T-26-05" asvs="L1" stride="Information Disclosure">
    <description>The smoke spec navigates to a real Google Patents URL, which could leak the test machine's IP / user-agent to Google's logs.</description>
    <mitigation>Acceptable risk — Google Patents is a public site, and the URL accessed (US11427642) is a public document. Headless Chromium's default user-agent is well-known. No new data is sent that wouldn't be sent by a developer manually visiting the same page. Phase 30 (X-PCT-Test-Mode) handles the related Worker cache concern.</mitigation>
  </threat>
</threat_model>

<verification>
Plan-level checks after both tasks complete (this is the phase success gate):

```
# 1. Smoke spec exists with the right shape
test -f tests/e2e/specs/smoke.spec.js
grep -q "loadExtension" tests/e2e/specs/smoke.spec.js
grep -q "gotoPatent" tests/e2e/specs/smoke.spec.js
grep -q "US11427642" tests/e2e/specs/smoke.spec.js
[ "$(wc -l < tests/e2e/specs/smoke.spec.js)" -lt 60 ]

# 2. npm script wired
node -e "const p = JSON.parse(require('fs').readFileSync('package.json', 'utf8')); console.log(p.scripts['e2e:smoke'])"
# Expected output: npm run build:chrome && playwright test --config tests/e2e/playwright.config.js

# 3. Smoke runs green — the actual phase gate
npm run e2e:smoke
# Expected: exit 0, one test passed, no flakes

# 4. dist/chrome/ has the HOOK-01 testids after the build step inside e2e:smoke ran
grep -q "pct-citation-host" dist/chrome/content/content.js
grep -q "pct-citation-pill" dist/chrome/content/content.js

# 5. Unit suite remains green
npm run test:src
```
</verification>

<success_criteria>
- `npm run e2e:smoke` exits 0 and reports "1 passed" with no flakes (the phase success gate per CONTEXT.md and ROADMAP Phase 26 success criterion #1)
- HARN-01 verified: Chrome extension loaded into a persistent context with channel:'chromium', navigation to https://patents.google.com/patent/US11427642/en returned 200
- HARN-02 verified: The SW readiness probe + chrome.runtime.id check completed (extensionId matches /^[a-p]{32}$/)
- HARN-03 verified at sanity level: addInitScript shadow-open shim is functional (probe shadow root readable). Full Phase 26 ROADMAP success criterion #2 ("Playwright test can read the extension's citation UI from its closed-mode Shadow DOM") will be exercised in Phase 27 by an actual selection trigger.
- HARN-04 verified at install level: clipboard-observer shim is installed (window.__lastCopiedText__ exists). Full end-to-end clipboard read deferred to Phase 27 (per VALIDATION.md "Manual-Only Verifications" table).
- HARN-05 verified: smoke.spec.js contains zero inline harness logic — imports only loadExtension and gotoPatent from ../lib/; under 60 lines total
- HOOK-01 verified at build-output level: both `pct-citation-host` and `pct-citation-pill` literals appear in dist/chrome/content/content.js after `npm run build:chrome` runs as part of `e2e:smoke`
- Existing vitest unit suite (`npm run test:src`) remains green
- No xvfb-run installed or required
</success_criteria>

<output>
After completion, create `.planning/phases/26-playwright-harness-scaffolding/26-03-SUMMARY.md` documenting:
- Final line count of `tests/e2e/specs/smoke.spec.js` (target: under 60)
- The exact `e2e:smoke` script value added to package.json
- Captured output of a green `npm run e2e:smoke` run (test name, runtime, pass count)
- Confirmation that `dist/chrome/content/content.js` contains both HOOK-01 testids after the smoke run's build step
- Any deviations from the assumed `channel: 'chromium'` / headless behavior (e.g. if a deprecation warning was emitted, document it for Phase 29)
- A pointer to where Phase 27 will pick up: implementing tests/e2e/lib/selection.js (currently a stub), writing the 76-case replay, exercising HARN-04 end-to-end.
</output>
