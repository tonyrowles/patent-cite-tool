# Phase 28 — Deferred Items

Issues discovered during Phase 28 execution that are NOT caused by Phase 28
changes and are deferred per the SCOPE BOUNDARY rule.

## Pre-existing failures: tests/unit/text-matcher.test.js

**Status at base 3ef1916:** 15 failed / 72 passed (87 total) — confirmed independently by both Plan 28-01 and Plan 28-02 executors.
**Status after Wave 1 (28-01 + 28-02):** 15 failed / 72 passed — unchanged.

The 15 failures are all in the `matchAndCite corpus` describe block — golden-baseline mismatches between `tests/golden/baseline.json` and the current `src/shared/matching.js` output. Examples:

- `US8352400-cross-col`: expected `'1:62-2:3'`, got `'1:60-2:3'`
- (14 more cases in similar shape)

These are golden-baseline divergences. They surfaced after Phase 27's `baseline.json` recalibration (Plan 27-06): the recalibration aligned baseline.json with the LIVE EXTENSION's observed citations (running against fresh PDF parses), but `tests/unit/text-matcher.test.js` runs the same matcher against the SAVED `tests/fixtures/*.json` PositionMaps — which were generated by an older parser revision. The fixtures and the live parser have drifted by 1-2 lines in some cases.

**Why deferred:**
- The failures pre-exist Plan 28-01 and Plan 28-02 (confirmed at base `3ef1916` before any 28-* work).
- The failures are in unit-tests against stored fixtures, not in the Phase 28 verifier or its E2E harness.
- Phase 28's verifier (VFY-02) is independent of `src/shared/matching.js`; the matcher's fixture drift is a separate concern.
- Fixing requires regenerating `tests/fixtures/*.json` (the PositionMaps) to match the current parser output OR re-aligning the unit-test expectations.

**Recommended action:**
- Phase 28-05 calibration may identify which baseline rows are stale vs which matcher behaviors are real regressions. After Phase 28 lands, file a v3.1 quick task: "regenerate `tests/fixtures/*.json` PositionMaps from current `src/shared/matching.js` parser, re-snapshot baseline accordingly."

## Other items

### Stale `eslint-disable no-await-in-loop` directives in tests/e2e/lib/settings.js

**Status (Plan 28-04):** 2 ESLint warnings (not errors) for unused `eslint-disable` directives at `tests/e2e/lib/settings.js:85` and `:104`.

These pre-exist the ESLint install in Plan 28-04 — someone wrote `// eslint-disable-next-line no-await-in-loop` lines anticipating future enforcement, but the rule was never actually enabled, so the disable directives are unused. With ESLint 10's flat config + the `reportUnusedDisableDirectives` default (`warn`), they surface as warnings.

**Why deferred:**
- Phase 28 scope is the VFY-02 independence boundary ONLY (28-PLAN frontmatter, Plan 28-04 docstring). Stylistic enforcement is explicitly out of scope.
- Warnings do not fail `npm run lint` (exit 0).
- The plan instructed "DO NOT add stylistic rules (no-unused-vars, semi, etc.) — Phase 28 only enforces the independence boundary". Removing the stale disable comments would be a stylistic clean-up — touches files outside the verifier independence claim.

**Recommended action:**
- Trivial v3.1 cleanup: delete the two `// eslint-disable-next-line no-await-in-loop` lines in `settings.js` (or actually enable `no-await-in-loop` if the project wants that rule enforced).
