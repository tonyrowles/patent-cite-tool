---
phase: 32-human-uat-verification
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/e2e/fixtures/.gitkeep
  - tests/e2e/scripts/e2e-explore-phase-flag.test.js
  - tests/e2e/scripts/e2e-upload-llm-report.test.js
  - tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js
autonomous: true
requirements: [UAT-01, UAT-02, UAT-03]

must_haves:
  truths:
    - "tests/e2e/fixtures/ directory exists as a tracked location (resolves Pitfall 8)"
    - "Schema-guard test file exists, fails initially because fixture is absent, becomes green when Wave 3 produces real fixture"
    - "Phase-flag test file exists and asserts the rejection paths for --phase value validation"
    - "Upload-helper test file exists and skeletons the DI-mock-execSync orchestration assertions"
  artifacts:
    - path: "tests/e2e/fixtures/.gitkeep"
      provides: "Tracked-empty marker so the fixtures directory commits to git"
    - path: "tests/e2e/scripts/e2e-explore-phase-flag.test.js"
      provides: "Vitest spec asserting --phase parsing (valid numeric, rejected v32, rejected =syntax, rejected missing value)"
    - path: "tests/e2e/scripts/e2e-upload-llm-report.test.js"
      provides: "Vitest spec asserting upload-helper orchestration via mocked execSync (skeletoned, expect-fail until Wave 2 ships helper)"
    - path: "tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js"
      provides: "Vitest spec asserting fixture round-trips through appendLlmIteration; absent-fixture test is .skip until Wave 3"
  key_links:
    - from: "tests/e2e/scripts/e2e-explore-phase-flag.test.js"
      to: "scripts/e2e-explore.mjs"
      via: "spawnSync invocation"
      pattern: "spawnSync.*e2e-explore\\.mjs"
    - from: "tests/e2e/scripts/e2e-upload-llm-report.test.js"
      to: "scripts/e2e-upload-llm-report.mjs"
      via: "ES-module import of pure-function export"
      pattern: "import.*from.*e2e-upload-llm-report"
    - from: "tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js"
      to: "tests/e2e/lib/llm-report.js::appendLlmIteration"
      via: "round-trip via REQUIRED_ENTRY_FIELDS"
      pattern: "appendLlmIteration"
---

<objective>
Stand up the Wave 0 test infrastructure that downstream waves depend on: create the missing `tests/e2e/fixtures/` directory (Pitfall 8), and stub three new Vitest spec files so test contracts exist BEFORE the code that satisfies them lands. Per D-04 the regression baseline (461 Vitest + 76 Playwright) MUST keep passing — Wave 0 tests are expected to start RED or SKIP and turn GREEN as Waves 1–3 ship the implementations.

Purpose: Resolve a missing-directory blocker (RESEARCH Pitfall 8), and embed Nyquist-style test contracts up front so executors have a concrete acceptance target. This plan is pure scaffolding — no production code is touched.

Output: One `.gitkeep`, three skeletoned spec files, all committed and discoverable by Vitest. The phase-flag spec runs RED until Wave 2 ships `--phase`. The upload-helper spec is `describe.skip()`-gated until Wave 2 ships the helper module. The schema-guard spec uses `it.skipIf(!fs.existsSync(FIXTURE))` so it's GREEN-on-skip until Wave 3 commits the real fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-human-uat-verification/32-CONTEXT.md
@.planning/phases/32-human-uat-verification/32-RESEARCH.md
@.planning/phases/32-human-uat-verification/32-PATTERNS.md
@.planning/phases/32-human-uat-verification/32-VALIDATION.md

<interfaces>
<!-- Key contracts the executor needs to write test skeletons against -->

From tests/e2e/lib/llm-report.js (already exists — REUSE):
```javascript
// Line 55:
const REQUIRED_ENTRY_FIELDS = ['iteration_n', 'iso', 'classification'];
// Line 197:
export function appendLlmIteration(reportPath, entry) { /* throws on schema miss */ }
```

From tests/e2e/scripts/e2e-explore-ci-guard.test.js (analog — MIRROR shape):
```javascript
import { describe, it, expect } from 'vitest';
import { spawnSync } from 'node:child_process';
import path from 'node:path';
import { fileURLToPath } from 'node:url';
const __dirname = path.dirname(fileURLToPath(import.meta.url));
const SCRIPT_PATH = path.resolve(__dirname, '../../../scripts/e2e-explore.mjs');
// Test 3 pattern (lines 51–55) — wipe CI env vars to bypass CI guard:
const r = spawnSync('node', [SCRIPT_PATH, '--iterations', '1'], {
  env: { ...process.env, CI: '', GITHUB_ACTIONS: '' },
  encoding: 'utf8', timeout: 3000,
});
```

From tests/unit/e2e-report-issue.test.js (analog — MIRROR mock-ghClient DI pattern):
```javascript
const ghCalls = [];
const ghClient = {
  listOpenNightlyIssues: () => openIssues,
  createIssue: (title, body) => { ghCalls.push({ op: 'create', title, body }); return { number: 999 }; },
};
processReport(REPORT_FIXTURE, { ghClient, runId: '...', repo: 'owner/repo' });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create fixtures directory + .gitkeep</name>
  <files>tests/e2e/fixtures/.gitkeep</files>
  <read_first>
    - tests/e2e/lib/llm-report.js (lines 50-80 — see emptyReport() and REQUIRED_ENTRY_FIELDS to understand what the fixture must conform to)
    - .planning/phases/32-human-uat-verification/32-RESEARCH.md (Pitfall 8 — explains why this directory must exist)
    - .planning/phases/32-human-uat-verification/32-PATTERNS.md (section "tests/e2e/fixtures/uat-phase32-llm-report.json" — analog reference is tests/unit/fixtures/sample-llm-report.json)
  </read_first>
  <action>
    Create directory `tests/e2e/fixtures/` and add a single empty file `tests/e2e/fixtures/.gitkeep` so git tracks the otherwise-empty directory. This resolves Pitfall 8 (directory currently does not exist; downstream waves' fixture commit and helper code that resolves paths under this dir would fail). The `.gitkeep` file MUST be exactly zero bytes (or contain only a single newline). Do NOT add any other content. No README, no inline notes.
  </action>
  <verify>
    <automated>test -d tests/e2e/fixtures &amp;&amp; test -f tests/e2e/fixtures/.gitkeep &amp;&amp; [ ! -s tests/e2e/fixtures/.gitkeep -o $(wc -c &lt; tests/e2e/fixtures/.gitkeep) -le 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `ls -la tests/e2e/fixtures/.gitkeep` returns the file (size 0 or 1 byte)
    - `git ls-files tests/e2e/fixtures/` after staging shows `.gitkeep` (directory is tracked)
    - No other files exist under `tests/e2e/fixtures/` at this point in Wave 0 (the real fixture is committed in Wave 3)
  </acceptance_criteria>
  <done>The fixtures directory exists, is git-trackable via the .gitkeep marker, and downstream tasks can safely reference paths under it without ENOENT errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stub --phase flag Vitest spec</name>
  <files>tests/e2e/scripts/e2e-explore-phase-flag.test.js</files>
  <read_first>
    - tests/e2e/scripts/e2e-explore-ci-guard.test.js (FULL FILE — copy file structure verbatim; mirror Test 3 at lines 46-62 for env-wiping pattern)
    - scripts/e2e-explore.mjs (lines 30-36 for exit-code documentation; lines 80-107 for parseArgs current shape; lines 110-120 for checkClaudeCli)
    - .planning/phases/32-human-uat-verification/32-PATTERNS.md (section "tests/e2e/scripts/e2e-explore-phase-flag.test.js" — pattern source)
    - .planning/phases/32-human-uat-verification/32-RESEARCH.md (Pitfall 2 — equals-syntax rejection requirement; Pitfall 7 not applicable here)
  </read_first>
  <behavior>
    - Test "rejects --phase v32" — exit code 2, stderr contains the phrase "invalid --phase value" (or similar specific message referencing the bad arg "v32")
    - Test "rejects --phase=32 (equals syntax)" — exit code 2, stderr indicates equals syntax is not supported (Pitfall 2)
    - Test "rejects --phase with no value" — exit code 2, stderr indicates a missing value for --phase
    - Test "accepts --phase 32 (proceeds past parseArgs)" — does NOT exit 2 from parseArgs (may exit 3 if `claude` CLI absent on this machine, or proceed further); the assertion is "exit code is not 2" (proves parseArgs accepted the value); env must wipe CI/GITHUB_ACTIONS to bypass CI guard (mirrors ci-guard Test 3)
    - All tests use `spawnSync('node', [SCRIPT_PATH, ...args], { env: {...process.env, CI: '', GITHUB_ACTIONS: ''}, encoding: 'utf8', timeout: 5000 })`
  </behavior>
  <action>
    Create a new Vitest spec file at `tests/e2e/scripts/e2e-explore-phase-flag.test.js`. Mirror the structure of `tests/e2e/scripts/e2e-explore-ci-guard.test.js` verbatim (imports, `__dirname` resolution, `SCRIPT_PATH` constant). Add a `describe('--phase flag (Phase 32)', ...)` block with four `it(...)` tests matching the four behaviors above. Because the implementation (Wave 2's Plan 32-03) does not yet exist, these tests are EXPECTED TO FAIL in Wave 0 — that is the contract. Do NOT use `.skip` for these — they should run RED and turn GREEN when Plan 32-03 ships. Add a top-of-file comment block (mirroring `e2e-explore-ci-guard.test.js` lines 1-15 style) explaining: "These tests are RED in Wave 0 by design; Wave 2 Plan 32-03 ships --phase parsing in scripts/e2e-explore.mjs which makes them GREEN." Use `[e2e-explore]` as the stderr line prefix the assertions look for (matches existing convention). Reject equals syntax per Pitfall 2 (the test asserts the rejection; the production code in Plan 32-03 enforces it). Strict-regex value validation per RESEARCH (recommends `^\d+$`).
  </action>
  <verify>
    <automated>npx vitest run tests/e2e/scripts/e2e-explore-phase-flag.test.js 2&gt;&amp;1 | grep -E "(4 failed|4 tests)" | head -2</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `tests/e2e/scripts/e2e-explore-phase-flag.test.js`
    - File imports `{ describe, it, expect }` from `vitest` and `spawnSync` from `node:child_process`
    - `SCRIPT_PATH` constant resolves to absolute path of `scripts/e2e-explore.mjs` via `path.resolve(__dirname, '../../../scripts/e2e-explore.mjs')`
    - Exactly four `it(...)` blocks exist matching the four behaviors above (`grep -c "it(" tests/e2e/scripts/e2e-explore-phase-flag.test.js` returns 4)
    - All four tests RUN (not skipped); they are expected to FAIL until Plan 32-03 ships (this is the Wave 0 contract)
    - Each `it` block uses `spawnSync` with `env: { ...process.env, CI: '', GITHUB_ACTIONS: '' }` to bypass the CI guard at e2e-explore.mjs:72
    - Top-of-file comment block explicitly documents "RED in Wave 0 by design — Plan 32-03 makes them GREEN"
    - No fenced ```code blocks``` inside the test file's comment blocks (use plain JS line/block comments)
  </acceptance_criteria>
  <done>Vitest discovers the new spec; it runs (RED is acceptable in Wave 0); the file structurally mirrors the analog `e2e-explore-ci-guard.test.js`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stub upload-helper Vitest spec (skipped until Wave 2 ships helper)</name>
  <files>tests/e2e/scripts/e2e-upload-llm-report.test.js</files>
  <read_first>
    - tests/unit/e2e-report-issue.test.js (lines 282-363 — `processReport()` describe block; canonical mock-ghClient DI pattern in this repo)
    - .planning/phases/32-human-uat-verification/32-PATTERNS.md (section "tests/e2e/scripts/e2e-upload-llm-report.test.js" — analog reference + assertion checklist)
    - .planning/phases/32-human-uat-verification/32-RESEARCH.md (Pattern 1 for race-mitigation filter; "Two-Stage Upload Helper Mechanics" section for the orchestration steps the test must cover)
    - tests/unit/fixtures/sample-llm-report.json (sample shape — useful for the inline minimal-report fixture the test uses)
  </read_first>
  <behavior>
    - Test "happy path: orchestrates ingest → run-list → nightly → browser-open" — calls the helper's pure-function export with a mock ghClient; asserts call sequence (auth-status, workflow-run ingest with payload_b64=@- via stdin, run-list with --json databaseId,createdAt, workflow-run nightly with -f llm_run_id=&lt;captured&gt;, run-view --web)
    - Test "race-mitigation filter rejects stale entries" — mock ghClient returns two runs from `gh run list`: one with `createdAt` before the trigger timestamp (stale, must be filtered), one after (the real one); assert the captured run_id is the post-trigger one
    - Test "oversized payload (&gt;60KB base64) exits with code 2 BEFORE any gh call" — feeds a synthetic large report; assert no gh client method was invoked
    - Test "gh auth status failure exits with code 7 BEFORE any other call" — mock the auth-status call to throw; assert no subsequent gh calls happen
    - The entire `describe(...)` block is wrapped in `describe.skipIf(...)` keyed off whether `scripts/e2e-upload-llm-report.mjs` exists (so Wave 0 is SKIP, Wave 2 is RUN-and-GREEN)
  </behavior>
  <action>
    Create a new Vitest spec file at `tests/e2e/scripts/e2e-upload-llm-report.test.js`. Mirror the import + fixture-load + mock-ghClient DI pattern from `tests/unit/e2e-report-issue.test.js` (lines 282-363). The spec MUST import the pure-function orchestration export from `../../../scripts/e2e-upload-llm-report.mjs` (which does not exist yet — Wave 2 Plan 32-04 creates it). Because the import would crash at module-load time, wrap the entire `describe(...)` in `describe.skipIf(!fs.existsSync(HELPER_PATH))` where `HELPER_PATH = path.resolve(__dirname, '../../../scripts/e2e-upload-llm-report.mjs')`. Use a dynamic `await import(HELPER_PATH)` INSIDE the `it` blocks (not a top-level static import) so file-absence doesn't crash the file-parse. Write the four `it(...)` blocks per the behavior list. The mock ghClient interface MUST be: `{ authStatus(): void|throws, workflowRun(workflowFile, inputs, opts): void, runList(workflowFile): Array<{databaseId, createdAt}>, runView(id, opts): void, repoView(): {nameWithOwner: string} }`. Use a synthetic minimal `llm-report.json` payload inline (don't depend on the real fixture). Add a top-of-file comment block documenting: "SKIP in Wave 0 — turns RUN-and-GREEN once Plan 32-04 ships scripts/e2e-upload-llm-report.mjs". Reference Pattern 1 in RESEARCH for the stale-entry filter behavior. Reject the oversized payload at the 60KB base64 threshold per RESEARCH "Payload Size Constraint".
  </action>
  <verify>
    <automated>npx vitest run tests/e2e/scripts/e2e-upload-llm-report.test.js 2&gt;&amp;1 | grep -iE "(skipped|passed|failed)" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `tests/e2e/scripts/e2e-upload-llm-report.test.js`
    - File top has imports: `{ describe, it, expect }` from `vitest`, `fs`, `path`, `fileURLToPath`
    - `HELPER_PATH` constant resolves to absolute path of `scripts/e2e-upload-llm-report.mjs`
    - `describe.skipIf(!fs.existsSync(HELPER_PATH))` (or equivalent skipping mechanism) wraps the entire describe block
    - Running `npx vitest run tests/e2e/scripts/e2e-upload-llm-report.test.js` reports the describe block as SKIPPED (not FAILED) in Wave 0
    - Exactly four `it(...)` blocks exist matching the four behaviors above (`grep -c "it(" tests/e2e/scripts/e2e-upload-llm-report.test.js` returns 4)
    - Top-of-file comment documents "SKIP in Wave 0 — turns RUN-and-GREEN once Plan 32-04 ships"
    - Mock ghClient interface signatures match the documented shape (auth-status, workflow-run, run-list with --json databaseId+createdAt, run-view, repo-view)
    - No top-level static `import` of the not-yet-existing helper (must be dynamic `await import` inside `it` blocks)
  </acceptance_criteria>
  <done>Vitest discovers the file and reports it as SKIPPED without crashing on import; the test contracts are concrete and the assertions describe exactly what Plan 32-04 must implement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Stub fixture schema-guard Vitest spec (skipped until Wave 3 commits fixture)</name>
  <files>tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js</files>
  <read_first>
    - tests/unit/llm-report.test.js (lines 304-316 — Test 13, the canonical fixture-roundtrip pattern using REQUIRED_ENTRY_FIELDS via appendLlmIteration)
    - tests/e2e/lib/llm-report.js (line 55 REQUIRED_ENTRY_FIELDS = ['iteration_n', 'iso', 'classification']; line 133-143 emptyReport; line 197 appendLlmIteration signature)
    - .planning/phases/32-human-uat-verification/32-PATTERNS.md (section "tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js" — analog + critical-comment requirement)
    - .planning/phases/32-human-uat-verification/32-RESEARCH.md (Pattern 3 lines 285-313 — round-trip-via-tmpfile pattern; Pitfall 1 — schema-only-not-semantic comment is REQUIRED)
  </read_first>
  <behavior>
    - Test "fixture exists" — asserts `fs.existsSync(FIXTURE)` is true (PASSES once Plan 32-05 commits the real fixture)
    - Test "fixture has >=10 iterations" — asserts `report.iterations.length >= 10` (D-10 pass bar)
    - Test "every iteration passes appendLlmIteration schema guard" — round-trips every iteration through a tmpFile via the appendLlmIteration validator; throws on missing REQUIRED_ENTRY_FIELDS
    - All three tests are gated via `it.skipIf(!fs.existsSync(FIXTURE))` so Wave 0 is GREEN-on-skip and Wave 3's fixture commit auto-turns them GREEN
  </behavior>
  <action>
    Create a new Vitest spec file at `tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js`. Imports: `{ describe, it, expect, beforeEach, afterEach }` from `vitest`, `fs`, `os`, `path`, `fileURLToPath`, and `{ appendLlmIteration }` from `../lib/llm-report.js` (relative path from `tests/e2e/fixtures/` → `tests/e2e/lib/`). Resolve `FIXTURE = path.resolve(__dirname, 'uat-phase32-llm-report.json')`. Each test uses `it.skipIf(!fs.existsSync(FIXTURE))(...)` so Wave 0 reports them as skipped (not failed). Use `beforeEach`/`afterEach` to create/cleanup a `tmpDir` via `fs.mkdtempSync(path.join(os.tmpdir(), 'pct-uat-phase32-'))` mirroring `tests/unit/llm-report.test.js` lines 47-61. The round-trip test reconstructs the empty-report shape (from `emptyReport()` at llm-report.js:133-143 — `run_id`, `started_iso`, `finished_iso`, `iterations_total: 0`, `summary`, `iterations: []`) and then calls `appendLlmIteration(tmpReport, it)` for every entry in the real fixture's `iterations` array. The schema validator throws on missing REQUIRED_ENTRY_FIELDS — that's the gate. Add a top-of-file comment block: "Per RESEARCH Pitfall 1 — semantic correctness is Phase 33's gate; this test validates STRUCTURE ONLY. Do not add verdict-distribution assertions here." Use plain JS comments, no fenced code blocks.
  </action>
  <verify>
    <automated>npx vitest run tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js 2&gt;&amp;1 | grep -iE "(skipped|passed|failed)" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js`
    - `FIXTURE` constant resolves to `tests/e2e/fixtures/uat-phase32-llm-report.json` (absolute path)
    - Import of `appendLlmIteration` uses relative path `../lib/llm-report.js`
    - Each `it(...)` uses `.skipIf(!fs.existsSync(FIXTURE))` (or an equivalent guard) so all three tests SKIP in Wave 0 when fixture absent
    - Running `npx vitest run tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js` reports SKIPPED (not FAILED) in Wave 0
    - Top-of-file comment explicitly states "semantic correctness is Phase 33's gate; this test validates STRUCTURE ONLY" (Pitfall 1 requirement)
    - `beforeEach`/`afterEach` create/cleanup a tmpDir for the schema-roundtrip test
    - Exactly three `it(...)` blocks exist matching the three behaviors above
    - No fenced code blocks in comments
  </acceptance_criteria>
  <done>Vitest discovers the file and reports it as SKIPPED in Wave 0; Wave 3's fixture commit makes the same tests turn GREEN without any further code change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner ↔ filesystem | Vitest reads/writes tmpDirs; no untrusted input crosses this boundary |
| Spawned `node` process ↔ this test process | `spawnSync` in phase-flag test launches the production script in a child process with controlled env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | Test fixtures pollution into git | mitigate | `.gitkeep` is exactly empty; no test writes to `tests/e2e/fixtures/` outside Wave 3's manual fixture commit; round-trip test uses `mkdtempSync(os.tmpdir(), 'pct-uat-phase32-')` for throwaway files |
| T-32-02 | Tampering | Stale skipped tests masking real regressions | mitigate | Top-of-file comments document EXACTLY when each skip flips to RUN (Wave 0 contract); reviewers know to remove `.skipIf` if it lingers past Wave 3 |
| T-32-03 | Information disclosure | Test inadvertently logging Max 5 subscription credentials | accept | No subscription-credential codepaths are exercised in Wave 0 tests; helper auth-status mock returns void/throws only |
| T-32-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan; Wave 0 uses only existing Vitest 3.x + Node 22 built-ins (RESEARCH Package Legitimacy Audit confirms zero-new-deps milestone constraint) |
</threat_model>

<verification>
After all four tasks complete, run:

```bash
# 1. fixtures dir + .gitkeep present
test -d tests/e2e/fixtures && test -f tests/e2e/fixtures/.gitkeep

# 2. All three new spec files discovered by Vitest (3 files = 3 lines)
npx vitest list 2>/dev/null | grep -c -E "(e2e-explore-phase-flag|e2e-upload-llm-report|uat-phase32-llm-report\.schema)"

# 3. Phase-flag spec runs (RED is acceptable in Wave 0)
npx vitest run tests/e2e/scripts/e2e-explore-phase-flag.test.js 2>&1 | tail -10

# 4. Upload-helper spec SKIPS (must not crash with import error)
npx vitest run tests/e2e/scripts/e2e-upload-llm-report.test.js 2>&1 | grep -i "skipped"

# 5. Schema-guard spec SKIPS (fixture absent, must not fail)
npx vitest run tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js 2>&1 | grep -i "skipped"

# 6. Regression baseline still green (D-04 — 461 Vitest tests pass unchanged)
npm run test:src
```

The phase-flag spec is allowed to be RED in Wave 0 (by design — Plan 32-03 makes it GREEN). The upload-helper and schema-guard specs MUST be SKIPPED (not FAILED) so Wave 0 commits cleanly. The existing 461-test regression baseline MUST stay green per D-04.
</verification>

<success_criteria>
- `tests/e2e/fixtures/.gitkeep` exists and is tracked by git
- `tests/e2e/scripts/e2e-explore-phase-flag.test.js` exists, structurally mirrors `e2e-explore-ci-guard.test.js`, runs four tests (RED in Wave 0 is acceptable)
- `tests/e2e/scripts/e2e-upload-llm-report.test.js` exists, runs as SKIPPED (helper module absent), contains four `it` blocks with concrete assertions
- `tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js` exists, runs as SKIPPED (fixture absent), contains three `it` blocks with `it.skipIf` guards
- `npm run test:src` continues to pass (461 existing Vitest tests untouched per D-04)
- No production code (under `scripts/`, `tests/e2e/lib/`, `.github/workflows/`) modified in this plan
</success_criteria>

<output>
Create `.planning/phases/32-human-uat-verification/32-01-SUMMARY.md` when done. Capture:
- The four test files created and their expected status (RED / SKIPPED in Wave 0)
- Confirmation `npm run test:src` still passes 461 tests
- The git-trackable directory marker
- Any deviations from the plan (none expected — pure scaffolding)
</output>
</content>
</invoke>