---
phase: 33-re-run-validator
plan: 03
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - scripts/e2e-explore.mjs
  - scripts/_verify-phase33-callsites.mjs
autonomous: true
requirements: [RERUN-03]
requirements_addressed: [RERUN-03]

must_haves:
  truths:
    - "D-13: 4 capture-state keys (scroll_y, viewport_width, viewport_height, selected_node_xpath) appended on every appendLlmIteration call — schema-guard compliance side"
    - "D-14: capture happens in e2e-explore.mjs between Step 8 (selectText) and Step 9 (getCitation); null on pre-browser/error paths, populated on success path"
    - "scripts/e2e-explore.mjs captures scroll_y, viewport_width, viewport_height, and selected_node_xpath between Step 8 (selectText) and Step 9 (getCitation)"
    - "All 6 appendLlmIteration call sites at lines 283, 337, 364, 390, 430, 457 include the four new keys: real captured values on the success path (line 430) and null on the five pre-browser/pre-selection failure paths"
    - "Each call site individually includes all four keys (per-site check, not just a global grep count)"
    - "The XPath derivation degrades to null if window.getSelection().rangeCount === 0 (does not throw)"
    - "All existing vitest tests pass after the modification (no regressions introduced)"
  artifacts:
    - path: "scripts/e2e-explore.mjs"
      provides: "Capture block between selectText and getCitation that records scroll_y, viewport size, and an nth-child XPath of the selection anchor; threads all 4 values through all 6 appendLlmIteration call sites"
      contains: "selected_node_xpath"
    - path: "scripts/_verify-phase33-callsites.mjs"
      provides: "One-shot static analysis script that proves every appendLlmIteration call site in e2e-explore.mjs carries all four new keys"
      contains: "appendLlmIteration"
  key_links:
    - from: "scripts/e2e-explore.mjs"
      to: "tests/e2e/lib/llm-report.js"
      via: "appendLlmIteration call sites passing the four new keys"
      pattern: "selected_node_xpath"
    - from: "scripts/e2e-explore.mjs capture block"
      to: "Playwright page.evaluate / page.viewportSize"
      via: "extInstance.page.evaluate window.scrollY; page.viewportSize; page.evaluate xpath derivation"
      pattern: "page.evaluate"
---

<objective>
Land D-14: capture `scroll_y`, `viewport_width`, `viewport_height`, and `selected_node_xpath` in scripts/e2e-explore.mjs between Step 8 (selectText) and Step 9 (getCitation), and thread the four values through ALL 6 existing `appendLlmIteration` call sites — real captured values on the post-selectText success path (line 430), and `null` literals on the five pre-browser / pre-selection failure paths (lines 283, 337, 364, 390, 457). The XPath derivation uses the nth-child walking algorithm from RESEARCH.md §"Code Examples" (Claude's discretion per CONTEXT.md but the canonical shape is documented).

Purpose: Plan 33-01 ships the schema, but the schema-guard now requires the keys on every iteration written by `e2e-explore.mjs`. Without this plan, the next `npm run e2e:explore` run would throw on the first iteration with "missing required field 'scroll_y' (null permitted)". This plan closes the capture side of RERUN-03.
Output: A modified `scripts/e2e-explore.mjs` (capture block + 6 updated call sites) plus a one-shot per-site verification script `scripts/_verify-phase33-callsites.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-re-run-validator/33-CONTEXT.md
@.planning/phases/33-re-run-validator/33-RESEARCH.md
@.planning/phases/33-re-run-validator/33-PATTERNS.md
@scripts/e2e-explore.mjs
@tests/e2e/lib/llm-report.js

<interfaces>
<!-- Key contracts extracted from scripts/e2e-explore.mjs. -->

Step 8/9 boundary — current code structure around lines 411-414 (verify exact line numbers when reading):
- Line ~411: `await selectText({ page: extInstance.page, uniqueSubstring: sel.selectedText });`
- Line ~414: `const obs = await getCitation(...);`
- The capture block inserts BETWEEN these two lines.

All 6 appendLlmIteration call sites (line numbers from research; verify on read):
- Line 283: `if (!parsed.ok)` block → LLM_API_ERROR classification (pre-browser path; before selectText runs)
- Line 337: retry-failure block → LLM_API_ERROR (pre-browser path)
- Line 364: off-corpus patentId block → LLM_API_ERROR (pre-browser path)
- Line 390: hallucination-guard failure → LLM_HALLUCINATED_SELECTION (pre-browser path)
- Line 430: SUCCESS PATH after selectText + getCitation + verifyCitation → PASS / WRONG_CITATION / VERIFIER_DISAGREE
- Line 457: catch block → HARNESS_ERROR (selection may or may not have succeeded depending on where the throw came from; the safe default is null since the capture block runs AFTER selectText)

Capture block (per CONTEXT.md D-14 + RESEARCH.md §"Code Examples" XPath section):
- `scroll_y = await extInstance.page.evaluate(() => window.scrollY)`
- `vp = extInstance.page.viewportSize()` returns `{width, height}` synchronously
- `selected_node_xpath = await extInstance.page.evaluate(<nth-child XPath derivation>)` — degrade to null if rangeCount === 0

Schema-guard contract (from Plan 33-01):
- All 6 call sites MUST include scroll_y, viewport_width, viewport_height, selected_node_xpath as KEYS
- Values may be null (Plan 33-01's option-a split-list permits null on these four)
- Missing the KEYS throws "missing required field '<f>' (null permitted)"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert the D-14 capture block between selectText and getCitation</name>
  <files>scripts/e2e-explore.mjs</files>
  <read_first>
    - scripts/e2e-explore.mjs (around lines 405-420 — confirm exact line of selectText call and the next getCitation / obs assignment)
    - .planning/phases/33-re-run-validator/33-PATTERNS.md §"scripts/e2e-explore.mjs" (full capture block + call-site table)
    - .planning/phases/33-re-run-validator/33-RESEARCH.md §"Code Examples" XPath capture section (canonical nth-child algorithm)
    - .planning/phases/33-re-run-validator/33-CONTEXT.md D-14 (capture semantics + null on pre-browser paths)
  </read_first>
  <behavior>
    - A new block of code appears in scripts/e2e-explore.mjs immediately after `await selectText(...)` and before the next observation step (getCitation or obs assignment).
    - The block declares three constants: `scroll_y`, `vp` (object with width and height), and `selected_node_xpath`.
    - The XPath function evaluated in the page context returns null if `window.getSelection()` is null or `rangeCount === 0` or `anchorNode` is null; otherwise walks the parentNode chain building nth-child indices and returns a string of the form `/html/<tag>[idx]/<tag>[idx]/...`.
    - The block is wrapped in a comment header naming the phase + D-14 + RERUN-03.
    - No existing code is deleted, only inserted.
  </behavior>
  <action>
    Open scripts/e2e-explore.mjs. Use grep -n "await selectText" to locate the call. Research lists this at line ~411 — confirm the exact line on read. Immediately AFTER this line and BEFORE the next observation line (which begins the getCitation flow), insert the capture block from 33-PATTERNS.md §"scripts/e2e-explore.mjs" Capture block section: declare `const scroll_y = await extInstance.page.evaluate(() => window.scrollY);`, `const vp = extInstance.page.viewportSize();` (synchronous Playwright API returns object with width and height), and `const selected_node_xpath = await extInstance.page.evaluate(() => { ... nth-child XPath derivation ... });`. Use the exact XPath function body from 33-PATTERNS.md (nodeType===3 walk-up, while loop counting previousElementSibling by nodeName, unshift `${nodeName.toLowerCase()}[${idx}]`, return `parts.length ? '/html/' + parts.join('/') : null`). Prefix the block with a comment header `// --- D-14 Phase 33 capture block (RERUN-03) ---` and close with `// --- end capture ---` so future readers can locate it. The block adds 3 new lexical names `scroll_y`, `vp`, `selected_node_xpath` to the enclosing scope (verify by reading the surrounding function body — confirm the names are not already used; the success-path call site at line 430 will reference them).
  </action>
  <verify>
    <automated>grep -c "D-14 Phase 33 capture block (RERUN-03)" scripts/e2e-explore.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "D-14 Phase 33 capture block (RERUN-03)" scripts/e2e-explore.mjs` outputs at least 1
    - `grep -c "scroll_y = await.*page.evaluate" scripts/e2e-explore.mjs` outputs at least 1
    - `grep -c "viewportSize()" scripts/e2e-explore.mjs` outputs at least 1
    - `grep -c "selected_node_xpath = await.*page.evaluate" scripts/e2e-explore.mjs` outputs at least 1
    - `grep -n "rangeCount === 0" scripts/e2e-explore.mjs` matches at least 1 line (XPath degrades to null)
    - `grep -n "previousElementSibling" scripts/e2e-explore.mjs` matches at least 1 line (nth-child walk)
    - `node --check scripts/e2e-explore.mjs` exits 0 (syntactic validity)
    - The capture block appears BETWEEN the selectText call and the getCitation call: `awk '/await selectText/,/getCitation/' scripts/e2e-explore.mjs | grep -c "D-14 Phase 33 capture block"` outputs at least 1
  </acceptance_criteria>
  <done>Capture block inserted between selectText and getCitation; declares scroll_y, vp, selected_node_xpath; XPath function handles rangeCount===0 degradation; file parses as valid Node ES module.</done>
</task>

<task type="auto">
  <name>Task 2: Thread the four new keys through all 6 appendLlmIteration call sites</name>
  <files>scripts/e2e-explore.mjs</files>
  <read_first>
    - scripts/e2e-explore.mjs (the 6 call sites at lines 283, 337, 364, 390, 430, 457 — confirm exact lines via `grep -n "appendLlmIteration" scripts/e2e-explore.mjs`)
    - .planning/phases/33-re-run-validator/33-PATTERNS.md §"scripts/e2e-explore.mjs" — call-site table mapping each line to null vs real values
    - .planning/phases/33-re-run-validator/33-RESEARCH.md §"Common Pitfalls" anti-pattern "Adding the 4 new keys to ONLY the post-selectText success path"
  </read_first>
  <behavior>
    - All 6 `appendLlmIteration(reportPath, ...)` calls in scripts/e2e-explore.mjs include the four new keys.
    - 5 call sites at lines 283, 337, 364, 390, 457 pass `scroll_y: null, viewport_width: null, viewport_height: null, selected_node_xpath: null` (literal nulls).
    - 1 call site at line 430 (success path) passes `scroll_y, viewport_width: vp.width, viewport_height: vp.height, selected_node_xpath` — references the variables declared in Task 1's capture block.
    - Global grep gate (excluding comments): at least 24 non-comment occurrences of the four key names across the file (4 keys × 6 call sites = 24 minimum; the capture block in Task 1 adds another 4-5 references).
  </behavior>
  <action>
    Use `grep -n "appendLlmIteration" scripts/e2e-explore.mjs` to enumerate the six call sites and confirm they match the line numbers in research (283, 337, 364, 390, 430, 457; line numbers may have drifted slightly — use the grep output as ground truth). For each of the FIVE pre-browser / pre-selection / catch-block sites (283, 337, 364, 390, 457), locate the closing brace+paren of the iteration object literal and insert (immediately before that closing brace) a comment-prefixed block of four `null` literals: `// D-14 Phase 33 — null on pre-browser/pre-selection failure paths (RERUN-03)`, then `scroll_y: null,`, `viewport_width: null,`, `viewport_height: null,`, `selected_node_xpath: null,`. For the ONE success-path site at line 430, insert the same comment header and instead reference the locally-scoped variables from Task 1's capture block: `scroll_y,`, `viewport_width: vp.width,`, `viewport_height: vp.height,`, `selected_node_xpath,`. CRITICAL nuance for line 457 (catch block, HARNESS_ERROR): the capture block in Task 1 runs AFTER selectText; if a throw originates BEFORE selectText (path arrives via the early throws above line 411), `vp`/`scroll_y`/`selected_node_xpath` are not in scope. Pass `null` for all four — this is correct because the catch block sits at a higher scope than the per-iteration try body where Task 1's `const` declarations live. Confirm by reading the surrounding `try { ... } catch { ... }` structure: if the consts are declared inside the try, they are NOT visible in the catch. (Worst case: if the catch IS at the same level as the const declarations and the throw happens AFTER capture, the values would technically be capturable — but D-14 explicitly says to pass null on all pre-browser/error paths so this is the locked semantics regardless.) Do NOT delete or modify existing fields in any of the six iteration object literals — these are pure additions.
  </action>
  <verify>
    <automated>bash -c 'count=$(grep -v "^[[:space:]]*//" scripts/e2e-explore.mjs | grep -cE "scroll_y|viewport_width|viewport_height|selected_node_xpath"); echo "non-comment occurrences: $count"; test "$count" -ge 24'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' scripts/e2e-explore.mjs | grep -cE 'scroll_y|viewport_width|viewport_height|selected_node_xpath'` outputs an integer >= 24 (4 keys × 6 call sites = 24 minimum, comments excluded)
    - `grep -c "scroll_y: null" scripts/e2e-explore.mjs` outputs at least 5 (five pre-browser/catch-path null literals)
    - `grep -c "viewport_width: null" scripts/e2e-explore.mjs` outputs at least 5
    - `grep -c "viewport_height: null" scripts/e2e-explore.mjs` outputs at least 5
    - `grep -c "selected_node_xpath: null" scripts/e2e-explore.mjs` outputs at least 5
    - `grep -c "viewport_width: vp.width" scripts/e2e-explore.mjs` outputs at least 1 (success-path real value)
    - `grep -c "viewport_height: vp.height" scripts/e2e-explore.mjs` outputs at least 1 (success-path real value)
    - `grep -c "appendLlmIteration" scripts/e2e-explore.mjs` outputs exactly 6 (no call site added or removed)
    - `node --check scripts/e2e-explore.mjs` exits 0
  </acceptance_criteria>
  <done>All 6 appendLlmIteration call sites carry the four new keys; 5 pre-browser/catch paths pass null literals; success path references the captured locals; non-comment grep gate >= 24; file is syntactically valid.</done>
</task>

<task type="auto">
  <name>Task 3: Per-call-site static verification and full test:src</name>
  <files>scripts/_verify-phase33-callsites.mjs</files>
  <read_first>
    - tests/e2e/lib/llm-report.js (REQUIRED_NULLABLE_FIELDS list from Plan 33-01)
    - scripts/e2e-explore.mjs (modified by Tasks 1 and 2)
    - .planning/phases/33-re-run-validator/33-RESEARCH.md §"Validation Architecture" (RERUN-03 grep gate spec)
  </read_first>
  <behavior>
    - Running `node scripts/_verify-phase33-callsites.mjs` confirms that EACH of the 6 appendLlmIteration call sites in scripts/e2e-explore.mjs individually includes ALL four of scroll_y, viewport_width, viewport_height, selected_node_xpath keys. (Per-site check; not just a global grep count — guards against the failure mode where one site has 8 occurrences and another has 0 yet the global count is 24.)
    - The script exits 0 with stdout "OK: all 6 call sites contain all 4 keys" if every chunk passes; exits 1 with a diagnostic message naming the offending chunk index and missing key otherwise.
    - All vitest tests still pass (`npm run test:src`) — this catches any accidental syntactic breakage introduced by Task 1 or Task 2.
  </behavior>
  <action>
    Create scripts/_verify-phase33-callsites.mjs as a Node ES module. The script imports `fs` from `'node:fs'`, reads `scripts/e2e-explore.mjs` as text via fs.readFileSync, splits on the regex `/appendLlmIteration\s*\(\s*reportPath\s*,\s*\{/`, and processes the resulting array. If `parts.length !== 7` (six call sites = seven split chunks because the array begins with the prefix before the first match), the script prints `Expected exactly 6 call sites, found ${parts.length - 1}` to stderr and exits 1. Otherwise for each chunk at index 1..6: take everything up to the next closing brace+paren by splitting on the regex `/\}\)\s*;/` and keeping the first piece; for each of the four keys (`scroll_y`, `viewport_width`, `viewport_height`, `selected_node_xpath`), check `chunk.includes(key)` and on failure print `call site ${i} missing ${key}` to stderr and exit 1. After all six chunks pass, print `OK: all 6 call sites contain all 4 keys` to stdout and exit 0. Add a header comment naming Phase 33 RERUN-03 and explaining "one-shot static analysis; commit alongside the e2e-explore.mjs change so future audits can re-run it". Run the script and confirm exit 0; then run `npm run test:src` and confirm exit 0.
  </action>
  <verify>
    <automated>node scripts/_verify-phase33-callsites.mjs && npm run test:src</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/_verify-phase33-callsites.mjs` exits 0 with stdout containing "OK: all 6 call sites contain all 4 keys"
    - `npm run test:src` exits 0 (no vitest regressions)
    - scripts/_verify-phase33-callsites.mjs has a header comment naming Phase 33 RERUN-03
    - The script handles the negative case correctly: temporarily renaming one `scroll_y` reference inside a single call site (e.g. via `sed -i 's/scroll_y/SCROLL_Y/' scripts/e2e-explore.mjs` on the FIRST occurrence only) makes the verifier exit 1; restore via `git checkout scripts/e2e-explore.mjs`. (This negative test is informational — the executor may skip it; the positive `exit 0` path is the binding acceptance.)
  </acceptance_criteria>
  <done>scripts/_verify-phase33-callsites.mjs proves every appendLlmIteration call site individually carries all four new keys; npm run test:src exits 0; no vitest regressions introduced by the capture block or call-site threading.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extInstance.page.evaluate(...) → page DOM | The XPath derivation runs in the page context (Google Patents content). The function returns a string built from DOM node nodeName values. Mitigation: the XPath is stored as captured data, NOT executed; downstream consumers (future "full replay" mode) must treat it as untrusted input and use safe document.evaluate XPath APIs. This plan only ships the field; no execution path consumes it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03-01 | Tampering | selected_node_xpath value | accept | XPath is captured untrusted page-DOM data. No code consumes it in Phase 33 (verifier-only rerun does not navigate). A future Playwright-driven replay phase will need to validate via document.evaluate, not string interpolation. Flagged in research §"Assumptions Log" A3. |
| T-33-03-02 | Information Disclosure | llm-report.json | accept | viewport dimensions and scroll position are non-sensitive (browser-dimension metadata already implicit in test runs). XPath references page structure — no PII, no cross-site identifiers. |
| T-33-03-03 | Denial of Service | page.evaluate XPath walk | mitigate | The while loop walks parentNode up to HTML; loop is bounded by DOM depth (typically < 50). XPath derivation runs ONCE per successful selection (Step 8/9 boundary). Loop termination guaranteed by HTML-node check. |
</threat_model>

<verification>
- `node --check scripts/e2e-explore.mjs` exits 0
- `node scripts/_verify-phase33-callsites.mjs` exits 0
- `grep -v '^[[:space:]]*//' scripts/e2e-explore.mjs | grep -cE 'scroll_y|viewport_width|viewport_height|selected_node_xpath'` >= 24
- `grep -c "appendLlmIteration" scripts/e2e-explore.mjs` outputs exactly 6
- `npm run test:src` exits 0
</verification>

<success_criteria>
- D-14 capture block lands between Step 8 and Step 9 of scripts/e2e-explore.mjs
- All 6 appendLlmIteration call sites carry the four new keys per RESEARCH.md §"Common Pitfalls" anti-pattern (no missed call site)
- XPath degrades to null on selection-not-present (no throw)
- Pitfall 1 mitigated end-to-end (schema-guard now succeeds on every iteration write path)
- RERUN-03 capture side fully delivered; combined with Plan 33-01's schema side, RERUN-03 is complete
</success_criteria>

<output>
Create `.planning/phases/33-re-run-validator/33-03-SUMMARY.md` when done.
</output>
