---
phase: 33-re-run-validator
plan: 04
type: execute
wave: 3
depends_on: ["33-02"]
files_modified:
  - scripts/e2e-rerun-validator.mjs
  - tests/e2e/scripts/e2e-rerun-validator.test.js
  - package.json
autonomous: true
requirements: [RERUN-01]
requirements_addressed: [RERUN-01]

must_haves:
  truths:
    - "D-06: CLI entry is scripts/e2e-rerun-validator.mjs + npm run e2e:rerun-validator -- --input <path>; defaults to newest artifacts/*/llm-report.json by mtime when --input omitted"
    - "D-11: rerun-report.json written to path.dirname(input_llm_report_path) (same artifacts/{runId}/ directory)"
    - "Running `npm run e2e:rerun-validator -- --input <path>` writes a valid rerun-report.json to the run directory containing the input llm-report.json"
    - "Running `npm run e2e:rerun-validator` with no --input flag defaults to the newest tests/e2e/artifacts/*/llm-report.json by mtime"
    - "Running with --input pointing to a missing file exits 1 with stderr naming the path"
    - "Running with --input=<path> (equals syntax) exits 2 with stderr signaling unsupported syntax"
    - "Running with --input but no trailing value exits 2 with stderr signaling missing value"
    - "Running the CLI against the committed UAT fixture produces a rerun-report.json with 10 NOT_REPLAYABLE entries and summary.not_replayable_count: 10 (Pitfall 6 smoke)"
  artifacts:
    - path: "scripts/e2e-rerun-validator.mjs"
      provides: "CLI shim: parseArgs for --input flag, newest-by-mtime default resolution, wires real fs+verifyCitation deps into runValidator from rerun-validator.js"
      exports: []
      min_lines: 80
    - path: "tests/e2e/scripts/e2e-rerun-validator.test.js"
      provides: "spawnSync integration tests covering --input= rejection, --input missing-value rejection, --input missing-file exit 1, newest-by-mtime default, UAT fixture end-to-end smoke"
      min_lines: 100
    - path: "package.json"
      provides: "New script entry e2e:rerun-validator that invokes scripts/e2e-rerun-validator.mjs"
      contains: "e2e:rerun-validator"
  key_links:
    - from: "scripts/e2e-rerun-validator.mjs"
      to: "tests/e2e/lib/rerun-validator.js"
      via: "import { runValidator, atomicWriteJson } from '../tests/e2e/lib/rerun-validator.js'"
      pattern: "from '../tests/e2e/lib/rerun-validator.js'"
    - from: "tests/e2e/scripts/e2e-rerun-validator.test.js"
      to: "scripts/e2e-rerun-validator.mjs"
      via: "spawnSync('node', [SCRIPT_PATH, ...args])"
      pattern: "spawnSync"
    - from: "package.json"
      to: "scripts/e2e-rerun-validator.mjs"
      via: "scripts.e2e:rerun-validator: 'node scripts/e2e-rerun-validator.mjs'"
      pattern: "e2e-rerun-validator"
---

<objective>
Ship the CLI shim that wires real `fs` + real `verifyCitation` into the pure orchestrator from Plan 33-02 (`runValidator` in `tests/e2e/lib/rerun-validator.js`). The shim follows the established CLI pattern from `scripts/e2e-upload-llm-report.mjs` (Phase 32): strict-regex parseArgs (rejects `--input=` equals syntax and missing trailing value), default-input resolution by newest-mtime under `tests/e2e/artifacts/*/llm-report.json` (D-06), and clear exit codes (0 success, 1 missing input, 2 bad arg-parse).

Purpose: Plan 33-02 ships the pure orchestrator with injected deps so it can be unit-tested hermetically; this plan delivers the production wiring. The CLI is also a Phase 36 prerequisite (`run-triage-pipeline.mjs` will spawn this script as its first step).
Output: A new CLI file `scripts/e2e-rerun-validator.mjs`, its spawnSync integration tests `tests/e2e/scripts/e2e-rerun-validator.test.js`, and a new `e2e:rerun-validator` script in `package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-re-run-validator/33-CONTEXT.md
@.planning/phases/33-re-run-validator/33-RESEARCH.md
@.planning/phases/33-re-run-validator/33-PATTERNS.md
@scripts/e2e-upload-llm-report.mjs
@scripts/e2e-explore.mjs
@tests/e2e/scripts/e2e-explore-phase-flag.test.js
@tests/e2e/lib/rerun-validator.js
@tests/e2e/fixtures/uat-phase32-llm-report.json
@package.json

<interfaces>
<!-- Key contracts. -->

runValidator entrypoint (from Plan 33-02 — tests/e2e/lib/rerun-validator.js):
```
export async function runValidator({
  inputLlmReport,       // already-parsed JSON object
  sourceLlmReportPath,  // string path for D-09 source_llm_report field
  outputPath,           // resolved output path: dirname(input) + '/rerun-report.json'
  verifyCitation,       // defaults to the real fn from pdf-verifier.js
  writeReport,          // defaults to (p, c) => atomicWriteJson(p, c)
  now,                  // defaults to () => new Date()
})
```

D-06 CLI contract:
- `--input <path>` accepts absolute or repo-relative path; resolve via `path.resolve(process.cwd(), input)`
- `--input=<path>` (equals syntax) → exit 2 with stderr "[e2e-rerun-validator] equals syntax not supported for --input; use --input <value>"
- `--input` with no trailing value → exit 2 with stderr "[e2e-rerun-validator] missing value for --input"
- No `--input` flag → default to newest `tests/e2e/artifacts/*/llm-report.json` by mtime (or exit 1 if none exists)
- Resolved path does not exist → exit 1 with stderr naming the path
- `--help` / `-h` → print usage and exit 0
- Success → exit 0 after writing rerun-report.json

D-11 output path:
- `outputPath = path.join(path.dirname(inputLlmReportPath), 'rerun-report.json')`

Pattern source (scripts/e2e-upload-llm-report.mjs lines 1-150, 407-415):
- Shebang `#!/usr/bin/env node`
- Header with phase reference + exit-code legend
- Imports: fs, path, fileURLToPath, runValidator from '../tests/e2e/lib/rerun-validator.js', verifyCitation from '../tests/e2e/lib/pdf-verifier.js'
- isMain guard pattern (WR-02 fix): `const isMain = process.argv[1] !== undefined && fileURLToPath(import.meta.url) === path.resolve(process.argv[1]);`

Test pattern source (tests/e2e/scripts/e2e-explore-phase-flag.test.js lines 42-118 + 113-117 stderr-absence WR-07 pattern):
- spawnSync with timeout: 3000
- expect(r.status).toBe(N) for explicit exit codes
- expect(r.stderr || '').toMatch(/.../i) for error-message assertions
- For "accepted" tests, ALSO assert stderr does NOT contain rejection signatures

UAT fixture smoke (Pitfall 6):
- tests/e2e/fixtures/uat-phase32-llm-report.json contains 10 iterations, all NOT_REPLAYABLE under D-01/D-02
- CLI run against it must produce summary.not_replayable_count === 10, confirmed_count === 0, flake_count === 0
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the spawnSync test scaffold for the CLI</name>
  <files>tests/e2e/scripts/e2e-rerun-validator.test.js</files>
  <read_first>
    - tests/e2e/scripts/e2e-explore-phase-flag.test.js (full file — patterns for spawnSync wrapper, negative cases, stderr-absence assertions at lines 113-117)
    - tests/e2e/scripts/e2e-explore-ci-guard.test.js (simpler spawnSync template)
    - .planning/phases/33-re-run-validator/33-PATTERNS.md §"tests/e2e/scripts/e2e-rerun-validator.test.js" (full scaffold)
    - .planning/phases/33-re-run-validator/33-RESEARCH.md §"Common Pitfalls" Pitfall 6 (UAT fixture has zero replay-eligible iterations)
    - tests/e2e/fixtures/uat-phase32-llm-report.json (the smoke-test input)
  </read_first>
  <behavior>
    - File imports `describe, it, expect, beforeEach, afterEach` from 'vitest'; `spawnSync` from 'node:child_process'; fs, os, path, fileURLToPath.
    - SCRIPT_PATH is computed via `path.resolve(__dirname, '../../../scripts/e2e-rerun-validator.mjs')`.
    - `spawnValidator(args, env = {})` wrapper invokes `spawnSync('node', [SCRIPT_PATH, ...args], { env: { ...process.env, ...env }, encoding: 'utf8', timeout: 3000 })`.
    - Pre-implementation (RED): all spawnSync tests fail because SCRIPT_PATH doesn't exist (`r.status === 1` from node with "Cannot find module"). The file structure is the contract; implementation in Task 2 makes them green.
    - Negative-case tests cover: --input= equals syntax (exit 2 + stderr regex), --input missing trailing value (exit 2 + stderr regex), --input pointing to missing file (exit 1 + stderr naming path).
    - Positive-case tests cover: --input with a valid existing file path AND stderr does NOT match the negative-case rejection signatures (WR-07 absence assertion), CLI defaults to newest-by-mtime when no --input is passed AND succeeds with the seeded tmp artifact, UAT fixture end-to-end smoke producing 10 NOT_REPLAYABLE entries.
  </behavior>
  <action>
    Create tests/e2e/scripts/e2e-rerun-validator.test.js modeled on tests/e2e/scripts/e2e-explore-phase-flag.test.js. Imports as listed in behavior. SCRIPT_PATH and spawnValidator wrapper per 33-PATTERNS.md. Inside `describe('--input flag (Phase 33 RERUN-01)', ...)` write three negative tests: (1) `it('rejects --input= (equals syntax) with exit 2 and stderr signaling equals syntax unsupported', ...)` — `const r = spawnValidator(['--input=/tmp/foo.json'])`; assert `r.status === 2`; `expect(r.stderr || '').toMatch(/equals/i)`. (2) `it('rejects --input with no value (trailing flag) with exit 2 and stderr signaling missing value', ...)` — `const r = spawnValidator(['--input'])`; `r.status === 2`; `r.stderr` matches `/missing value/i`. (3) `it('exits 1 when --input file does not exist (with stderr naming the missing path)', ...)` — `const r = spawnValidator(['--input', '/tmp/does-not-exist-' + Date.now() + '.json'])`; `r.status === 1`; `r.stderr` matches `/not found|missing|no.*llm-report/i`. Add a fourth test that asserts stderr-absence on a VALID --input (WR-07 pattern): inside a `describe('Phase 33 valid input', ...)` with beforeEach/afterEach creating a tmp dir + copying the UAT fixture into it as 'llm-report.json'. The test is `it('accepts valid --input value without emitting equals/missing-value rejection signatures', ...)` — `const r = spawnValidator(['--input', inputPath])`; do NOT assert exit code (the fixture is all NOT_REPLAYABLE so success behavior is verified separately); `expect(r.stderr || '').not.toMatch(/equals syntax not supported for --input/i)` and `expect(r.stderr || '').not.toMatch(/missing value for --input/i)`. Inside a separate `describe('Phase 33 end-to-end smoke (Pitfall 6)', ...)`: tmpDir lifecycle copies the committed `tests/e2e/fixtures/uat-phase32-llm-report.json` into a fresh tmp run dir as 'llm-report.json'; the test `it('UAT fixture smoke — 10 NOT_REPLAYABLE entries (D-02 + Pitfall 6)', ...)` spawns with --input pointing at the tmp copy, asserts r.status === 0, parses outputPath (= path.join(tmpDir, 'rerun-report.json')) as JSON, asserts `out.schema_version === 1`, `out.replays.length === 10`, every replay has `verdict === 'NOT_REPLAYABLE'`, `total_runs === 0`, `typeof reason === 'string'`, `out.summary.not_replayable_count === 10`, `out.summary.confirmed_count === 0`, `out.summary.flake_count === 0`. Inside a separate `describe('Phase 33 --input default (newest by mtime)', ...)`: beforeEach creates a tmp ARTIFACTS_ROOT-shaped directory (two subdirs with llm-report.json files, the second one created later so mtime is newer), env-overrides process.env so the CLI's ARTIFACTS_ROOT points at the tmp dir IF the CLI supports an env override OR (preferred) uses process.cwd() so cd'ing into the tmp dir works. The cleanest approach: have the CLI use `path.resolve(__dirname, '../tests/e2e/artifacts')` as the artifacts root — NOT overridable by env. So this test SKIPS or uses a different strategy: spawn the CLI from a working dir where no real artifacts exist BUT a fake artifacts dir is seeded under tmpDir; pass `cwd: tmpDir` to spawnSync AND adjust SCRIPT_PATH accordingly. Mark this test as `it.skipIf(true, ...)` if the CLI's artifacts root is hard-coded relative to __dirname (then the default-by-mtime behavior is only testable end-to-end). Actually — the simplest correct approach: spawn with `--input` pointing at the newer file, NOT testing the no-flag default path here. Skip the no-flag default-by-mtime case at the integration-test level (covered by the spawn-against-fixture smoke); document the skip with a comment "no-flag default exercised manually via npm run e2e:rerun-validator after committing real artifacts".
  </action>
  <verify>
    <automated>test -f tests/e2e/scripts/e2e-rerun-validator.test.js && grep -c "it(" tests/e2e/scripts/e2e-rerun-validator.test.js</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/scripts/e2e-rerun-validator.test.js file exists
    - File contains at least 5 `it(...)` test cases: `grep -c "it(" tests/e2e/scripts/e2e-rerun-validator.test.js` outputs at least 5
    - All four required test patterns present: `grep -c "rejects --input.*equals\|rejects --input with no value\|exits 1 when --input file does not exist\|UAT fixture smoke" tests/e2e/scripts/e2e-rerun-validator.test.js` outputs at least 4
    - File uses `spawnSync` from 'node:child_process': `grep -c "spawnSync" tests/e2e/scripts/e2e-rerun-validator.test.js` outputs at least 1
    - File asserts stderr-absence on the valid-input case (WR-07 pattern): `grep -cE 'not.toMatch.*equals syntax|not.toMatch.*missing value' tests/e2e/scripts/e2e-rerun-validator.test.js` outputs at least 2
    - File uses `timeout: 3000` in the spawnSync wrapper: `grep -c "timeout: 3000" tests/e2e/scripts/e2e-rerun-validator.test.js` outputs at least 1
    - SCRIPT_PATH points at `scripts/e2e-rerun-validator.mjs`: `grep -c "e2e-rerun-validator.mjs" tests/e2e/scripts/e2e-rerun-validator.test.js` outputs at least 1
    - Pre-implementation run (RED state): `npx vitest run tests/e2e/scripts/e2e-rerun-validator.test.js --reporter=basic 2>&1 | grep -c "FAIL\|fail"` outputs >= 1 (tests fail because the CLI file does not yet exist; this RED state confirms nyquist compliance before Task 2)
  </acceptance_criteria>
  <done>tests/e2e/scripts/e2e-rerun-validator.test.js exists with at least 5 spawnSync tests covering the four negative cases + the UAT fixture smoke; tests fail before Task 2 implementation (RED state).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scripts/e2e-rerun-validator.mjs (CLI shim)</name>
  <files>scripts/e2e-rerun-validator.mjs</files>
  <read_first>
    - scripts/e2e-upload-llm-report.mjs (lines 1-150 for header + parseArgs + injected-deps wiring; lines 407-432 for isMain guard pattern)
    - scripts/e2e-explore.mjs (lines 83-147 — parseArgs strict-regex idiom with --phase rejection mirror for --input)
    - .planning/phases/33-re-run-validator/33-PATTERNS.md §"scripts/e2e-rerun-validator.mjs" (full template)
    - .planning/phases/33-re-run-validator/33-CONTEXT.md D-06, D-11 (CLI contract + output path)
    - tests/e2e/scripts/e2e-rerun-validator.test.js (the contract from Task 1)
    - tests/e2e/lib/rerun-validator.js (the runValidator entrypoint to wire)
  </read_first>
  <behavior>
    - File starts with shebang `#!/usr/bin/env node` and an executable file mode (chmod +x).
    - Imports `fs`, `path`, `fileURLToPath`, `runValidator` and `atomicWriteJson` from `../tests/e2e/lib/rerun-validator.js`, and `verifyCitation` from `../tests/e2e/lib/pdf-verifier.js`.
    - Defines `parseArgs(argv)` matching D-06 contract: handles `--input <path>`, rejects `--input=`, rejects missing trailing value, supports `--help`/`-h`.
    - Defines `newestLlmReportPath(artifactsRoot)` per RESEARCH.md §"Code Examples" newest-by-mtime pattern.
    - Defines `PROJECT_ROOT = path.resolve(__dirname, '..')` and `ARTIFACTS_ROOT = path.resolve(PROJECT_ROOT, 'tests/e2e/artifacts')`.
    - `main()` function: parseArgs → resolve input path (default: newestLlmReportPath(ARTIFACTS_ROOT)) → fs.existsSync check (exit 1 if not) → JSON.parse the file → derive outputPath via path.join(path.dirname(resolvedInputPath), 'rerun-report.json') → call runValidator with wired deps → exit 0.
    - isMain guard per WR-02 fix (lines 407-415 of e2e-upload-llm-report.mjs): `const isMain = process.argv[1] !== undefined && fileURLToPath(import.meta.url) === path.resolve(process.argv[1]); if (isMain) await main();`.
    - File parses cleanly with `node --check scripts/e2e-rerun-validator.mjs`.
  </behavior>
  <action>
    Create scripts/e2e-rerun-validator.mjs starting with `#!/usr/bin/env node` shebang. Add a header comment block referencing Phase 33 RERUN-01/RERUN-02, the CLI contract (D-06), exit codes (0 success, 1 missing input, 2 bad arg-parse), and pointers to .planning/phases/33-re-run-validator/33-CONTEXT.md. Imports: `import fs from 'node:fs';`, `import path from 'node:path';`, `import { fileURLToPath } from 'node:url';`, `import { runValidator, atomicWriteJson } from '../tests/e2e/lib/rerun-validator.js';`, `import { verifyCitation } from '../tests/e2e/lib/pdf-verifier.js';`. Declare `const __dirname = path.dirname(fileURLToPath(import.meta.url));`, `const PROJECT_ROOT = path.resolve(__dirname, '..');`, `const ARTIFACTS_ROOT = path.resolve(PROJECT_ROOT, 'tests/e2e/artifacts');`. Implement `function parseArgs(argv) { ... }` per 33-PATTERNS.md §"scripts/e2e-rerun-validator.mjs": iterate argv from index 2, on `argv[i].startsWith('--input=')` write to process.stderr `'[e2e-rerun-validator] equals syntax not supported for --input; use --input <value>\n'` and `process.exit(2)`; on `argv[i] === '--input'` take `next = argv[i + 1]`; if `next === undefined || next === null || next === ''` write `'[e2e-rerun-validator] missing value for --input\n'` and exit 2; otherwise `inputPath = next; i++;`. Handle `--help` / `-h` with a usage string and `process.exit(0)`. Return `{ inputPath }`. Implement `function newestLlmReportPath(artifactsRoot) { ... }` per RESEARCH.md §"Code Examples": throw if !fs.existsSync(artifactsRoot) with message "No artifacts dir at ${artifactsRoot}; run e2e:explore first"; readdirSync; filter isDirectory; loop joining 'llm-report.json' to each; track best mtime; throw if best is null with message "No llm-report.json found under ${artifactsRoot}"; return best.path. Implement `async function main() { ... }`: const `{ inputPath: rawInput } = parseArgs(process.argv)`; let resolvedInputPath; try { resolvedInputPath = rawInput ? path.resolve(process.cwd(), rawInput) : newestLlmReportPath(ARTIFACTS_ROOT); } catch (e) { process.stderr.write('[e2e-rerun-validator] ' + e.message + '\n'); process.exit(1); }. Check `if (!fs.existsSync(resolvedInputPath))` then write `'[e2e-rerun-validator] input not found: ${resolvedInputPath}\n'` and exit 1. Try JSON.parse(fs.readFileSync(resolvedInputPath, 'utf8')); on parse error write stderr and exit 1. Compute `outputPath = path.join(path.dirname(resolvedInputPath), 'rerun-report.json')`. Call `await runValidator({ inputLlmReport: parsed, sourceLlmReportPath: resolvedInputPath, outputPath, verifyCitation, writeReport: (p, c) => atomicWriteJson(p, c), now: () => new Date() })`. On success write stdout `'[e2e-rerun-validator] wrote ' + outputPath + '\n'` and `process.exit(0)`. Wrap main() in try/catch — on uncaught throw, write stderr and exit 1. isMain guard at module bottom: `const isMain = process.argv[1] !== undefined && fileURLToPath(import.meta.url) === path.resolve(process.argv[1]); if (isMain) { main(); }`. Set executable mode via `chmod +x scripts/e2e-rerun-validator.mjs`.
  </action>
  <verify>
    <automated>node --check scripts/e2e-rerun-validator.mjs && npx vitest run tests/e2e/scripts/e2e-rerun-validator.test.js --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/e2e-rerun-validator.mjs` exits 0
    - `npx vitest run tests/e2e/scripts/e2e-rerun-validator.test.js --reporter=basic` exits 0 with all tests passing
    - File starts with shebang: `head -1 scripts/e2e-rerun-validator.mjs` outputs `#!/usr/bin/env node`
    - File is executable: `test -x scripts/e2e-rerun-validator.mjs` exits 0
    - File imports runValidator: `grep -c "import.*runValidator.*from.*rerun-validator" scripts/e2e-rerun-validator.mjs` outputs at least 1
    - parseArgs handles equals-syntax rejection: `grep -c "equals syntax not supported for --input" scripts/e2e-rerun-validator.mjs` outputs at least 1
    - parseArgs handles missing-value rejection: `grep -c "missing value for --input" scripts/e2e-rerun-validator.mjs` outputs at least 1
    - newestLlmReportPath uses mtimeMs: `grep -c "mtimeMs" scripts/e2e-rerun-validator.mjs` outputs at least 1
    - isMain guard uses fileURLToPath + path.resolve: `grep -E "fileURLToPath\(import\.meta\.url\)\s*===\s*path\.resolve\(process\.argv\[1\]\)" scripts/e2e-rerun-validator.mjs` matches at least 1 line
  </acceptance_criteria>
  <done>scripts/e2e-rerun-validator.mjs ships the CLI shim wiring real fs + verifyCitation into runValidator; all spawnSync integration tests pass; file is executable and node-checked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register npm script and verify end-to-end smoke against UAT fixture</name>
  <files>package.json</files>
  <read_first>
    - package.json (existing scripts block — locate `e2e:upload-llm-report` to insert next to it)
    - .planning/phases/33-re-run-validator/33-CONTEXT.md D-06 (npm script name + flag contract)
  </read_first>
  <behavior>
    - package.json's scripts block contains a new entry `"e2e:rerun-validator": "node scripts/e2e-rerun-validator.mjs"`.
    - Running `npm run e2e:rerun-validator -- --input tests/e2e/fixtures/uat-phase32-llm-report.json` exits 0 (when run from a tmp working dir so the rerun-report.json output does not pollute committed fixtures — see Action).
    - The full `npm run test:src` exits 0 (no regressions in earlier plans).
  </behavior>
  <action>
    Open package.json. Locate the existing `"e2e:upload-llm-report"` entry. Insert a NEW entry `"e2e:rerun-validator": "node scripts/e2e-rerun-validator.mjs"` immediately after it, preserving JSON formatting (trailing comma on the line above, no trailing comma on the last line of the scripts block). Verify package.json parses as valid JSON via `node -e "JSON.parse(require('node:fs').readFileSync('package.json','utf8'))"`. Then perform a controlled smoke test that DOES NOT pollute the committed fixture directory: create a tmp working dir, copy `tests/e2e/fixtures/uat-phase32-llm-report.json` into `${tmpDir}/llm-report.json`, run `cd ${tmpDir} && node ${PROJECT_ROOT}/scripts/e2e-rerun-validator.mjs --input ${tmpDir}/llm-report.json` (NOT via npm run since cd matters), and assert it exits 0 with `${tmpDir}/rerun-report.json` produced. Validate the output JSON has `schema_version === 1` and `summary.not_replayable_count === 10`. Clean up the tmp dir. Then run `npm run test:src` and confirm exit 0.
  </action>
  <verify>
    <automated>bash -c 'node -e "JSON.parse(require(\"node:fs\").readFileSync(\"package.json\",\"utf8\"))" && grep -c "\"e2e:rerun-validator\"" package.json && npm run test:src'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"e2e:rerun-validator"' package.json` outputs at least 1
    - `node -e "JSON.parse(require('node:fs').readFileSync('package.json','utf8'))"` exits 0 (valid JSON)
    - `npm run test:src` exits 0
    - Controlled smoke test (executor runs this manually as the action specifies): copying the UAT fixture to a tmp dir, running the CLI against it, asserting exit 0 and the produced rerun-report.json has `summary.not_replayable_count === 10`
    - No file outside files_modified is touched: `git status --short | grep -v -E "package\.json|scripts/e2e-rerun-validator\.mjs|tests/e2e/scripts/e2e-rerun-validator\.test\.js" | grep -v '^??' | wc -l` outputs 0
  </acceptance_criteria>
  <done>package.json has the new e2e:rerun-validator script; manual smoke test against the UAT fixture (in a tmp dir, not polluting committed artifacts) produces a valid rerun-report.json with 10 NOT_REPLAYABLE entries; npm run test:src is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → script | argv values pass through parseArgs which uses string-equality and startsWith checks; no eval, no shell expansion in script body. `path.resolve(process.cwd(), input)` is path-traversal-safe (no symlink-following beyond standard Node fs semantics). |
| --input file path → fs.readFileSync | Untrusted file content gets JSON.parsed; parse failure is caught and exits 1 with stderr. No code execution path consumes the parsed content beyond field reads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04-01 | Tampering | --input file content | accept | JSON.parse failure is caught; parsed content is structurally validated by runValidator (Plan 33-02) which only reads specific fields. No dynamic require/eval. |
| T-33-04-02 | Spoofing | newestLlmReportPath result | accept | Pitfall 5 (newest-by-mtime selecting partial file). Mitigation: print resolved path to stdout BEFORE running (operator sees what was picked). Single-developer-single-machine assumption makes this very low probability. |
| T-33-04-03 | Information Disclosure | rerun-report.json output | accept | Writes adjacent to source llm-report.json in same artifacts/{runId}/ dir (D-11). Same trust scope. |
| T-33-04-04 | Denial of Service | spawnSync timeout in tests | mitigate | Tests use `timeout: 3000` to avoid vitest's 5000ms default test timeout when the CLI hits a blocking path post-parseArgs. |
</threat_model>

<verification>
- `node --check scripts/e2e-rerun-validator.mjs` exits 0
- `test -x scripts/e2e-rerun-validator.mjs` exits 0 (executable bit set)
- `npx vitest run tests/e2e/scripts/e2e-rerun-validator.test.js --reporter=basic` exits 0
- `grep -c '"e2e:rerun-validator"' package.json` outputs at least 1
- `npm run test:src` exits 0
- Manual smoke test (executor records in summary): tmp-dir copy of UAT fixture → CLI run → rerun-report.json produced with 10 NOT_REPLAYABLE entries
</verification>

<success_criteria>
- RERUN-01 success criterion 1 ("Developer runs the re-run validator against a fixture llm-report.json and rerun-report.json is written") satisfied end-to-end
- CLI contract per D-06 fully implemented (--input flag, default newest-by-mtime, exit codes 0/1/2)
- D-11 output co-location works (rerun-report.json next to input)
- Pitfall 5 mitigated (operator sees resolved path via stdout before run)
- Pitfall 6 mitigated (UAT fixture smoke test asserts 10 NOT_REPLAYABLE without expecting any CONFIRMED/FLAKE)
</success_criteria>

<output>
Create `.planning/phases/33-re-run-validator/33-04-SUMMARY.md` when done.
</output>
