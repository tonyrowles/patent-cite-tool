---
phase: 35-rich-issue-filer-+-quarantine-corpus
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/update-golden.js
  - tests/unit/update-golden-case-flag.test.js
  - package.json
autonomous: false
requirements: [QUAR-05]
requirements_addressed: [QUAR-05]
tags: [phase-35, prereq, update-golden, github-labels, plan-0, vitest]

must_haves:
  truths:
    - "D-14-Prereq: `scripts/update-golden.js --case <id> --confirm` regenerates ONLY the baseline entry for <id>; all other entries byte-identical (Pitfall 5 mitigation)"
    - "D-14-Prereq: Missing `--case` value (e.g., `--case` with no trailing arg, or `--case=foo` equals syntax) exits non-zero before any baseline I/O"
    - "D-14-Prereq: `--case <id>` with no matching TEST_CASES entry exits 1 and prints a clear error (never silently regenerates everything — Pitfall 5)"
    - "D-14-Prereq: Without `--case`, existing behavior unchanged — regenerates all 76 baselines (preserves Phase 22 contract)"
    - "Open Question 3: GitHub labels `triage` and `quarantine:ready-for-promotion` exist on the repo before Plan 02/04 issue-creation paths run (Pitfall 7 mitigation)"
  artifacts:
    - path: "scripts/update-golden.js"
      provides: "Per-case baseline regeneration via --case <id> flag"
      contains: "--case"
    - path: "tests/unit/update-golden-case-flag.test.js"
      provides: "Vitest coverage for --case path: per-case regen + missing-id exit"
      exports: ["describe", "it"]
  key_links:
    - from: "scripts/update-golden.js"
      to: "tests/test-cases.js"
      via: "TEST_CASES filter when --case present"
      pattern: "TEST_CASES.*filter|TEST_CASES.*find"
    - from: "scripts/update-golden.js"
      to: "tests/golden/baseline.json"
      via: "read existing baseline, mutate only one key, write back"
      pattern: "readFileSync.*baseline\\.json|writeFileSync.*baseline\\.json"
---

<objective>
Wave-1 prerequisites for Phase 35. Adds the `--case <id>` flag to `scripts/update-golden.js` so that `promote-from-quarantine.mjs` (Plan 05) can regenerate a SINGLE golden baseline entry instead of the full 76-case baseline (Pitfall 5 — Open Question 1 in 35-RESEARCH.md). Also gates a one-shot human checkpoint that ensures the two new GitHub labels (`triage` and `quarantine:ready-for-promotion`) exist on the repo before downstream plans invoke `gh issue create --label triage` or `gh issue edit --add-label quarantine:ready-for-promotion` (Pitfall 7 — Open Question 3 in 35-RESEARCH.md).

Purpose: Unblock Plan 05 (promote-from-quarantine spawnSync target needs `--case`) and Plan 02/04 (gh CLI label args must reference labels that exist on the repo). Both are external-state prerequisites that downstream plans assume but no prior phase delivered.

Output: `scripts/update-golden.js` extended with strict `--case <id>` parsing + Vitest coverage; two GitHub labels created on the repo via `gh label create --force` (idempotent).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-rich-issue-filer-+-quarantine-corpus/35-CONTEXT.md
@.planning/phases/35-rich-issue-filer-+-quarantine-corpus/35-RESEARCH.md
@.planning/phases/35-rich-issue-filer-+-quarantine-corpus/35-PATTERNS.md

# Sources to be modified or read first
@scripts/update-golden.js
@tests/test-cases.js
@scripts/e2e-triage-classifier.mjs

<interfaces>
<!-- Identifiers downstream tasks must know -->

scripts/update-golden.js currently exports nothing — it is a top-level CLI script. After this plan it MUST:
- accept `--case <id>` flag (strict positional value; `--case=<id>` rejected with exit 2; missing trailing value rejected with exit 2)
- when `--case` is present, regenerate ONLY that entry in tests/golden/baseline.json (read existing baseline.json into a JS object; reassign one key; write back)
- when `--case` references no TEST_CASES entry, exit 1 with stderr line naming the missing id
- when `--case` is absent, behavior unchanged (regenerate all 76 entries)

scripts/e2e-triage-classifier.mjs lines 58-98 — STRICT parseArgs template to copy (equals-syntax rejection, missing-value rejection, --help → exit 0).

tests/test-cases.js TEST_CASES export — array of { id, patentFile, selectedText, category }. id matches /^[A-Z]{2,}\d+[A-Z]?\d*-[a-z0-9-]+$/ (see 35-PATTERNS.md §Schema-guard).

tests/golden/baseline.json shape — object keyed by id, value = { citation: string|null, confidence: number } (verified by 35-RESEARCH.md A4).

Two GitHub labels required (verified missing on the repo per 35-RESEARCH.md A7 + Open Question 3):
  triage                              — color 6F42C1
  quarantine:ready-for-promotion      — color FFA500
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add --case <id> flag to scripts/update-golden.js</name>
  <read_first>
    @scripts/update-golden.js
    @scripts/e2e-triage-classifier.mjs (lines 58-98 — strict parseArgs template)
    @tests/test-cases.js (lines 1-50 — TEST_CASES shape)
    @.planning/phases/35-rich-issue-filer-+-quarantine-corpus/35-PATTERNS.md (§"scripts/update-golden.js")
    @.planning/phases/35-rich-issue-filer-+-quarantine-corpus/35-RESEARCH.md (§"Pitfall 5", §"Open Question 1")
  </read_first>
  <behavior>
    - Test 1: `--case US11427642-spec-short-1 --confirm` updates only that key in tests/golden/baseline.json; all other entries in baseline.json are byte-identical to the pre-run snapshot.
    - Test 2: `--case=US11427642-spec-short-1 --confirm` (equals syntax) exits 2 with stderr matching /equals/i (per D-14 strict parseArgs).
    - Test 3: `--case` with no trailing value exits 2 with stderr matching /missing value/i.
    - Test 4: `--case NO-SUCH-CASE --confirm` exits 1 with stderr naming the missing id; baseline.json untouched (Pitfall 5 — never silently regenerate everything).
    - Test 5: Without `--case`, behavior unchanged — all 76 entries regenerated (use a Vitest spy or count `Object.keys(baseline)` after run against a tmpDir clone of fixtures).
    - Test 6: `--case US11427642-spec-short-1` WITHOUT `--confirm` exits 1 with the existing safety-check message (matches behavior before this change; `--confirm` rule unchanged).
  </behavior>
  <action>
    Extend `scripts/update-golden.js` with a `parseArgs(argv)` helper modeled on `scripts/e2e-triage-classifier.mjs` lines 58-98. Add three flags:
      - `--confirm` (existing behavior preserved — keep the current `process.argv.includes('--confirm')` safety check OR fold it into parseArgs)
      - `--case <id>` (new) — strict positional value; reject `--case=<value>` equals syntax with `process.stderr.write('[update-golden] equals syntax not supported for --case; use \`--case <value>\`\n'); process.exit(2)`. Reject missing trailing value with exit 2 + stderr `missing value for --case`.
      - `--help` / `-h` (new) — print usage including the new `--case` flag, exit 0.

    Per-case codepath (Pitfall 5 mitigation):
      1. If parsed `caseId` is non-null:
         a. Import `sanitizeCaseId` from `./e2e-report-issue.mjs` (already exported at line 83); validate `caseId` via that helper. On throw, write the error to stderr and exit 1.
         b. Find the matching entry in `TEST_CASES`. If none, write `[update-golden] --case <id> matched no entry in TEST_CASES — refusing to regenerate (Pitfall 5)` to stderr and exit 1. NEVER fall through to the existing all-cases loop.
         c. Read the existing `tests/golden/baseline.json` (`readFileSync` + `JSON.parse`). If the file does not exist, treat as empty `{}`.
         d. Compute `{citation, confidence}` for ONLY that one case using the existing `matchAndCite(selectedText, positionMap)` flow.
         e. Mutate `baseline[id] = {citation, confidence}` in memory. Leave all other keys byte-identical.
         f. Write the whole object back via `writeFileSync(outputPath, JSON.stringify(baseline, null, 2) + '\n', 'utf-8')` — match existing line 65 trailing-newline behavior.
      2. If `caseId` is null, fall through to existing all-cases loop (lines 40-65) UNCHANGED.

    Constraints:
      - DO NOT change behavior when `--case` is absent — preserve the Phase 22 all-cases contract.
      - DO NOT mutate `tests/test-cases.js` from this script.
      - Sanitize via `sanitizeCaseId` BEFORE any filesystem read — defense-in-depth for the shellout-adjacent flows downstream.
      - Place the new parseArgs block above the existing `--confirm` safety check; keep `--confirm` as a hard requirement (so `--case <id>` alone still errors without `--confirm`).
      - Keep all imports at top; do not introduce new npm deps (D-16).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/update-golden-case-flag.test.js</automated>
  </verify>
  <done>
    1. `grep -n "case " scripts/update-golden.js` shows the new `--case` parse branch.
    2. `grep -n "equals syntax not supported for --case" scripts/update-golden.js` returns ≥1 hit.
    3. `node scripts/update-golden.js --case=foo --confirm; echo $?` prints 2.
    4. `node scripts/update-golden.js --case --confirm; echo $?` prints 2.
    5. `node scripts/update-golden.js --case NO-SUCH-CASE --confirm; echo $?` prints 1 AND `tests/golden/baseline.json` is byte-identical (verify with `git diff --exit-code tests/golden/baseline.json`).
    6. Vitest suite passes (Tests 1-6 above).
    7. `npm run update-golden -- --confirm` (no `--case`) still regenerates all 76 entries (smoke verify by re-running and comparing `git status tests/golden/baseline.json`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Vitest coverage for the --case flag</name>
  <read_first>
    @tests/unit/cache-version.test.js (schema-guard test shape; static fixture pattern)
    @tests/e2e/scripts/e2e-rerun-validator.test.js (lines 76-103 — tmpDir + spawnSync pattern)
    @scripts/update-golden.js (after Task 1)
    @.planning/phases/35-rich-issue-filer-+-quarantine-corpus/35-PATTERNS.md (§"scripts/update-golden.js" — test pattern)
  </read_first>
  <behavior>
    See Task 1 behavior list (Test 1..Test 6). This task delivers the test file that runs those cases via spawnSync against the real script with a tmpDir clone of fixture inputs.
  </behavior>
  <action>
    Create `tests/unit/update-golden-case-flag.test.js` (placed under `tests/unit/` so it auto-discovers in `npm run test:src`). Use the tmpDir-clone pattern from `tests/e2e/scripts/e2e-rerun-validator.test.js` lines 76-103: `mkdtempSync('pct-update-golden-')`, copy `tests/test-cases.js` and the necessary `tests/fixtures/*.json` into the tmpDir's sibling layout, and invoke `scripts/update-golden.js` via `spawnSync('node', [SCRIPT_PATH, ...args], { encoding: 'utf8', timeout: 30000, cwd: tmpDirCloneOfRepo })`.

    Alternative if tmpDir-clone of fixtures is too heavy: stub the matchAndCite call by manually pre-creating a synthetic `tests/golden/baseline.json` with two keys, then run `--case <one-id> --confirm` and assert the OTHER key is byte-identical (read both before/after, compare exact JSON.stringify).

    Test 1 (per-case path): pre-write a synthetic baseline.json with at least two valid TEST_CASES ids; run `--case <id1> --confirm`; assert `baseline[<id2>]` is byte-identical (preserves Pitfall 5).
    Test 2 (equals syntax): `spawnSync` with args `['--case=foo', '--confirm']`; expect `status === 2`, stderr matches `/equals/i`.
    Test 3 (missing value): args `['--case', '--confirm']` (no trailing); expect `status === 2`, stderr matches `/missing value/i`.
    Test 4 (no-match id): args `['--case', 'NO-SUCH-CASE-99', '--confirm']`; expect `status === 1`, stderr matches `/matched no entry/i`, baseline unchanged.
    Test 5 (all-cases preserved): args `['--confirm']` (no --case); expect `status === 0` AND all TEST_CASES ids appear in the resulting baseline.json. Run inside the existing repo (not tmpDir clone) is acceptable as a smoke; if so, restore baseline.json from `git checkout` in afterEach.
    Test 6 (--case without --confirm): args `['--case', '<valid-id>']` (no --confirm); expect `status === 1` and the existing safety-check message in stderr.

    WR-07 stderr-absence guard (mirror `e2e-rerun-validator.test.js` lines 92-102) on the per-case happy path: after a successful `--case <valid-id> --confirm` run, assert `expect(r.stderr || '').not.toMatch(/equals syntax not supported for --case/i)` AND `not.toMatch(/missing value for --case/i)`.

    Add timeout `30000ms` per spawnSync call (matchAndCite over even one case can take a few seconds with PDF parsing).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/update-golden-case-flag.test.js</automated>
  </verify>
  <done>
    1. `tests/unit/update-golden-case-flag.test.js` exists; Vitest discovers it under `npm run test:src`.
    2. All 6 tests pass on a clean run.
    3. `grep -c "spawnSync" tests/unit/update-golden-case-flag.test.js | grep -v '^#'` returns ≥1.
    4. `grep -c "equals syntax not supported for --case" tests/unit/update-golden-case-flag.test.js | grep -v '^#'` returns ≥1 (WR-07 negative assertion).
    5. Existing `npm run test:src` suite remains green (no regressions to other Vitest tests).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Create the two new GitHub labels on the repo</name>
  <what-built>
    Phase 35 Plans 02 (`--source triage` issue create) and 04 (`stable_runs >= 3` label add) both call `gh issue create --label triage ...` and `gh issue edit <n> --add-label quarantine:ready-for-promotion`. These calls fail with HTTP 422 if the labels do not exist on the GitHub repo (Pitfall 7). The labels MUST be created before Plan 02 or Plan 04 is executed in any non-mocked path.

    Labels required (verified missing on the repo via 35-RESEARCH.md A7 + Open Question 3):
      triage                              — purple (#6F42C1) — "Filed by --source triage path (Phase 35)"
      quarantine:ready-for-promotion      — orange (#FFA500) — "Quarantine entry stable across ≥3 nightly runs — eligible for human promotion to golden corpus"
  </what-built>
  <how-to-verify>
    Run these two `gh label create --force` commands locally (developer machine; `gh auth status` must be authenticated):

    1. `gh label create triage --color 6F42C1 --description "Filed by --source triage path (Phase 35)" --force`
    2. `gh label create quarantine:ready-for-promotion --color FFA500 --description "Quarantine entry stable across ≥3 nightly runs — eligible for human promotion to golden corpus" --force`

    Both use `--force` so re-running is safe (existing labels are updated rather than failing). After running, verify by:
      - `gh label list | grep -E 'triage|quarantine:ready-for-promotion'` — both labels MUST appear.

    Notes:
      - `--color` values use NO `#` prefix (gh CLI requirement — verified by Phase 29 STATE.md decision).
      - This is a one-shot bootstrap; do not commit any scripted automation for it. The label set is an external invariant of the repo, not a code artifact.
      - Per D-13, this is local-developer-only; no CI workflow runs this step.
  </how-to-verify>
  <resume-signal>Type "approved" after both labels appear in `gh label list`, OR describe blockers (e.g., gh auth issues).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flag → filesystem | `--case <id>` value is user-supplied; bounds the file-read path indirectly via `TEST_CASES` lookup |
| CLI flag → shell | None (no flag value reaches a shell in this script — `spawnSync` not used here) |
| Developer → gh CLI | `gh label create` invocations rely on the developer's authenticated `gh` session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-00-01 | Tampering | `--case <id>` value | mitigate | Validate via `sanitizeCaseId` (existing export from `scripts/e2e-report-issue.mjs` line 83) before any filesystem read; regex `/^[A-Z]{2,}\d+[A-Z]?\d*-[a-z0-9-]+$/` rejects shell metacharacters and path traversal sequences |
| T-35-00-02 | Information Disclosure | tests/golden/baseline.json overwrite | mitigate | Per-case path reads existing baseline before mutating; preserves all keys other than the targeted one; no race condition (single-developer codebase per RESEARCH §"Security Domain") |
| T-35-00-03 | Denial of Service | Silent all-cases regen on typo'd `--case` | mitigate | Pitfall 5 — explicit `exit 1` on no-match (never fall through to all-cases loop when `--case` was supplied); Vitest Test 4 pins this behavior |
| T-35-00-04 | Elevation of Privilege | `gh label create` runs with developer's PAT scope | accept | Labels are public metadata on a private repo; PAT scope is the developer's existing dev-session token; no escalation surface |
</threat_model>

<verification>
After all 3 tasks complete:

1. `npm run test:src` green — no regression from existing 26+ unit-test files; new `tests/unit/update-golden-case-flag.test.js` discovered and passing.
2. `gh label list` shows BOTH `triage` and `quarantine:ready-for-promotion` labels.
3. `git diff scripts/update-golden.js` shows only the additive `--case` branch + parseArgs refactor; no behavior change in the all-cases path.
4. `node scripts/update-golden.js --help; echo $?` prints usage including `--case` and exits 0.
5. `node scripts/update-golden.js --case=foo --confirm; echo $?` prints 2 (equals-syntax rejected).
</verification>

<success_criteria>
- `scripts/update-golden.js` accepts `--case <id>` strict-positional flag; per-case path mutates only one baseline key; no-match exits 1 with clear stderr; all-cases path unchanged.
- `tests/unit/update-golden-case-flag.test.js` passes 6 cases including WR-07 stderr-absence on the happy path.
- GitHub labels `triage` and `quarantine:ready-for-promotion` exist on the repo (verified via `gh label list`).
- No new npm dependencies (D-16 honored).
- `npm run test:src` and `npm run lint` both green.
</success_criteria>

<output>
Create `.planning/phases/35-rich-issue-filer-+-quarantine-corpus/35-00-SUMMARY.md` when done. Include:
  - Confirmed `--case <id>` per-case path mutates only the targeted key (Pitfall 5 mitigated).
  - Confirmed both GitHub labels exist on the repo (Pitfall 7 mitigated).
  - Confirmed parseArgs additions DID NOT touch the all-cases fallback path (Phase 22 contract preserved).
</output>
