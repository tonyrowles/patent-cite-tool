---
phase: 36-quarantine-ci-integration-+-pipeline-orchestrator
plan: "02"
type: execute
wave: 2
# depends_on 36-01 is a SEQUENCING dependency ONLY: both plans edit package.json
# (36-01 adds e2e:quarantine; 36-02 adds e2e:triage-pipeline + extends lint).
# Ordering them sequential avoids a package.json merge conflict. 36-02 consumes
# NO 36-01 code artifact (no quarantine.spec.js / e2e:quarantine reference).
depends_on: ["36-01"]
files_modified:
  - scripts/run-triage-pipeline.mjs
  - tests/e2e/fixtures/phase36-pipeline-llm-report.json
  - tests/e2e/fixtures/phase36-pipeline-rerun-report.json
  - tests/e2e/scripts/e2e-run-triage-pipeline.test.js
  - package.json
autonomous: true
requirements: [ORCH-01]
requirements_addressed: [ORCH-01]

must_haves:
  truths:
    - "D-05: run-triage-pipeline.mjs chains rerun-validator → triage-classifier → issue-file (--source triage) → quarantine-append via spawnSync with cwd: PROJECT_ROOT (Pitfall 6)"
    - "D-06: the pipeline EXITS 0 ALWAYS — a non-zero stage status is logged into a summary, never aborts the chain, never propagates"
    - "D-07: run dir resolved via path.dirname(--llm-report); rerun-report.json + triage-report.json land in that same artifacts/{runId}/ dir feeding the next stage"
    - "D-08: in CI (CI=true||GITHUB_ACTIONS=true) the triage-classifier stage exits 1 BY DESIGN; the pipeline logs 'skipped in CI', stages 3-4 no-op on the missing triage-report, exit stays 0"
    - "D-16: e2e:triage-pipeline npm script exists; the spawnSync integration test runs against a single LLM_HALLUCINATED_SELECTION fixture (heuristically resolved → critical, ZERO LLM calls) with CI='' so the full chain executes — exit 0, both reports written, mock issue filed, exactly 1 quarantine entry"
    - "WR-05: run-triage-pipeline.mjs bounds --llm-report to tests/e2e/artifacts/ + tests/e2e/fixtures/ — the SAME ALLOWED_INPUT_ROOTS the 4 chained CLIs enforce (Plan 36-04 YAML must place the artifact under artifacts/{runId}/)"
    - "ORCH-01: lint extended to cover scripts/run-triage-pipeline.mjs"
  artifacts:
    - path: "scripts/run-triage-pipeline.mjs"
      provides: "spawnSync 4-stage orchestrator, exit-0-always, WR-05-bounded --llm-report, sibling-dir intermediates"
      contains: "spawnSync"
      min_lines: 70
    - path: "tests/e2e/fixtures/phase36-pipeline-llm-report.json"
      provides: "single LLM_HALLUCINATED_SELECTION iteration — heuristically resolves to a critical, file-eligible finding through the REAL pipeline with ZERO LLM calls and ZERO cached-PDF dependency (drives the SC-2 count==1 assertion deterministically)"
    - path: "tests/e2e/fixtures/phase36-pipeline-rerun-report.json"
      provides: "matching single-replay rerun-report (verdict NOT_REPLAYABLE) — a CONTEXT fixture only; stage 1 (rerun-validator) OVERWRITES the live rerun-report.json, so this is the source of truth for shape, not the value the chain reads"
    - path: "tests/e2e/scripts/e2e-run-triage-pipeline.test.js"
      provides: "spawnSync integration tests: full chain (CI='') asserting the POST-PIPELINE filed-count invariant, CI-gated no-op (CI=true), exit-0-on-stage-failure, AND zero-LLM-call assertion"
      contains: "run-triage-pipeline"
    - path: "package.json"
      provides: "e2e:triage-pipeline script + run-triage-pipeline.mjs added to lint"
      contains: "e2e:triage-pipeline"
  key_links:
    - from: "scripts/run-triage-pipeline.mjs"
      to: "scripts/e2e-rerun-validator.mjs + e2e-triage-classifier.mjs + e2e-report-issue.mjs + quarantine-append.mjs"
      via: "spawnSync('node', [path.join(PROJECT_ROOT, scriptRel), ...args], { cwd: PROJECT_ROOT })"
      pattern: "spawnSync\\('node'"
    - from: "tests/e2e/scripts/e2e-run-triage-pipeline.test.js"
      to: "scripts/run-triage-pipeline.mjs"
      via: "spawnSync with CI='' GITHUB_ACTIONS='' + QUARANTINE_CORPUS_PATH_OVERRIDE + mock-gh on PATH"
      pattern: "CI: ''"
---

<objective>
Build `scripts/run-triage-pipeline.mjs` — the one genuinely new file in Phase 36. It chains the four existing CLIs (rerun-validator → triage-classifier → issue-file `--source triage` → quarantine-append) via `spawnSync` with `cwd: PROJECT_ROOT` (Pitfall 6), resolves intermediate report paths as siblings of `--llm-report` (D-07), and **exits 0 always** (D-06) — a failed stage is logged into a summary and skipped, never aborts the chain. Add the `e2e:triage-pipeline` npm script, extend `lint`, create a **new single-finding fixture pair built around an `LLM_HALLUCINATED_SELECTION` iteration** (so the finding resolves HEURISTICALLY to `critical` — NO live LLM call, NO cached-PDF dependency), and write the spawnSync integration test that exercises the full chain with `CI=''` (so the triage-classifier CI gate does not fire) plus a separate CI-gated no-op test (D-08).

Purpose: ORCH-01 — the nightly's local→CI handoff orchestrator that materializes `rerun-report.json` + `triage-report.json` and (locally) files issues + appends quarantine entries.
Output: `scripts/run-triage-pipeline.mjs`, two new fixtures, the integration test, `package.json` edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-quarantine-ci-integration-+-pipeline-orchestrator/36-CONTEXT.md
@.planning/phases/36-quarantine-ci-integration-+-pipeline-orchestrator/36-RESEARCH.md
@.planning/phases/36-quarantine-ci-integration-+-pipeline-orchestrator/36-PATTERNS.md

# CLI scaffold analog (parseArgs, WR-05 path-bound, isMain guard, PROJECT_ROOT)
@scripts/quarantine-append.mjs

# spawnSync integration-test harness analog (mock-gh shim, QUARANTINE_CORPUS_PATH_OVERRIDE, fixture copy)
@tests/e2e/scripts/e2e-quarantine-append.test.js

# Phase 35 SUMMARY — the harness + corpus-override pattern this test reuses
@.planning/phases/35-rich-issue-filer-+-quarantine-corpus/35-04-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — no exploration required. -->

The 4 chained CLIs and their argument contracts (VERIFIED by code inspection):

  scripts/e2e-rerun-validator.mjs    --input <llm-report>   → writes dirname(input)/rerun-report.json (D-11). Exit: 0 ok | 1 input/sibling err | 2 bad flag.
  scripts/e2e-triage-classifier.mjs  --input <llm-report>   → writes dirname(input)/triage-report.json. CI GATE: exits 1 FIRST when CI=='true' || GITHUB_ACTIONS=='true' (line 154) BY DESIGN (D-08). Exit: 0 ok | 1 (CI gate OR input/sibling missing) | 2 bad flag.
  scripts/e2e-report-issue.mjs       --source triage --triage-report <triage-report>  → files issues; exits 1 if triage-report or siblings (llm-report.json, rerun-report.json) missing (lines 639-655). Exit: 0 | 1 | 2.
  scripts/quarantine-append.mjs      --input <triage-report>  → upserts quarantine entries; exits 1 if input/siblings missing (lines 195-212). Exit: 0 | 1 | 2.

All four enforce WR-05 internally: ALLOWED_INPUT_ROOTS = [tests/e2e/artifacts/, tests/e2e/fixtures/]. Therefore the report MUST live under one of those roots no matter what — the orchestrator's own WR-05 bound is the SAME set (this is the resolution of the Open-Item-1 path conflict: keep the artifact under artifacts/{runId}/; Plan 36-04 relocates the CI download there).

From scripts/quarantine-append.mjs (copy these scaffolds verbatim):
  lines 8-30   imports + __dirname + PROJECT_ROOT + ARTIFACTS_ROOT + FIXTURES_ROOT + ALLOWED_INPUT_ROOTS
  lines 36-70  parseArgs (strict: --flag=value → exit 2; bare --flag → exit 2; next.startsWith('--') → exit 2; --help → exit 0). Change --input to --llm-report.
  WR-05 path-bound block (resolve --llm-report, assert insideAllowedRoot, else stderr + exit 1)
  isMain guard: const isMain = process.argv[1] !== undefined && fileURLToPath(import.meta.url) === path.resolve(process.argv[1])

From tests/e2e/lib/rerun-validator.js:
  export function atomicWriteJson(filePath, obj)   // optional pipeline-summary.json write (Claude's Discretion, D)

CRITICAL RUNTIME FACTS the fixture design depends on (VERIFIED against source — DO NOT contradict these):
  - tests/e2e/lib/rerun-validator.js::runValidator OVERWRITES dirname(input)/rerun-report.json. ANY seeded rerun-report.json is discarded by stage 1. The seeded rerun fixture is a SHAPE reference + the issue-file/quarantine sibling-read fallback, NOT a value the chain trusts.
  - isEligibleForReplay (rerun-validator.js line 41-43) = classification ∈ {WRONG_CITATION, VERIFIER_DISAGREE}. LLM_HALLUCINATED_SELECTION is NOT eligible → runValidator writes a NOT_REPLAYABLE replay with ZERO verifyCitation calls (lines 164-175). NO cached PDF needed. NO throw.
  - triage-classifier.js Rule 3 (line 472-498): NOT_REPLAYABLE (or missing rerun entry) + classification ∈ RULE3_CLASSIFICATIONS = ['LLM_HALLUCINATED_SELECTION','LLM_API_ERROR','HARNESS_ERROR','PASS'] → severity from map; LLM_HALLUCINATED_SELECTION → 'critical', path_taken: 'heuristic'. This is the HEURISTIC path — invokeLlm is NEVER called (ambiguous[] stays empty). callCount === 0.
  - filterFindingsForFiling (e2e-report-issue.mjs lines 181-194): admits a finding when severity ∈ {critical, high} OR rerun verdict==='CONFIRMED', AND NOT (category==='HARNESS_ERROR' || path_taken endsWith '_parse_error'). A heuristic LLM_HALLUCINATED_SELECTION finding (critical, category 'LLM_HALLUCINATED_SELECTION', path_taken 'heuristic') is ADMITTED.
  - WHY NOT WRONG_CITATION: WRONG_CITATION IS replay-eligible → stage 1 calls real verifyCitation 3× on a synthetic patent → throws (no cached PDF) → NOT_REPLAYABLE 'replay threw'. Then triage Rule 1/2/3 all miss (Rule 3 excludes WRONG_CITATION) → AMBIGUOUS → group of 1 < CLUSTER_THRESHOLD(5) → invokeLlm → with CI='' a REAL `claude -p` spawns (llm-driver.js line 384 gate does NOT fire) → no creds in the test → HARNESS_ERROR/parse_error → filtered out → 0 issues. SC-2 FAILS. The HALLUCINATED path avoids this entirely.

Phase 35 fixtures available (existing): tests/e2e/fixtures/phase35-{triage,llm,rerun}-report.json
  NOTE: phase35-triage-report.json has 2 CONFIRMED findings (VERIFIED) AND its iteration 1 is WRONG_CITATION (the LLM-routing trap above). D-16 / SC-2 say "1 finding" AND require a heuristic path → this plan creates a NEW single-finding LLM_HALLUCINATED_SELECTION fixture pair (see Task 2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/run-triage-pipeline.mjs — spawnSync 4-stage chain, exit-0-always, WR-05-bounded</name>
  <files>scripts/run-triage-pipeline.mjs</files>
  <read_first>
    - scripts/quarantine-append.mjs (CLI scaffold: lines 8-30 imports/roots, 36-70 parseArgs, WR-05 path-bound, isMain guard — copy verbatim, change --input→--llm-report)
    - 36-RESEARCH.md §"Pattern 1: spawnSync stage chain" (the runStage function + chain, the CI-gate nuance, the exit-0-always rule)
    - 36-PATTERNS.md §"scripts/run-triage-pipeline.mjs" (load-bearing copy guidance + CRITICAL triage-classifier CI-gate note)
    - scripts/e2e-rerun-validator.mjs (line 12-19 header — confirms --input + dirname(input)/rerun-report.json output contract)
    - scripts/e2e-triage-classifier.mjs (lines 12, 154-160 — the CI gate that exits 1 by design)
  </read_first>
  <action>
    Create scripts/run-triage-pipeline.mjs (ORCH-01, D-05/D-06/D-07). It is the only new production file in the phase.

    Header comment: cite ORCH-01, D-05 (spawnSync chain), D-06 (exit-0-always), D-07 (sibling-dir intermediates), D-08 (triage CI gate is expected, not an error), Pitfall 6 (cwd: PROJECT_ROOT).

    Scaffold (copy from quarantine-append.mjs):
    - Imports: { spawnSync } from 'node:child_process'; path; fs; { fileURLToPath } from 'node:url'. Optionally import { atomicWriteJson } from '../tests/e2e/lib/rerun-validator.js' for the discretionary pipeline-summary write.
    - __dirname, PROJECT_ROOT, ARTIFACTS_ROOT (= tests/e2e/artifacts), FIXTURES_ROOT (= tests/e2e/fixtures), ALLOWED_INPUT_ROOTS = [ARTIFACTS_ROOT, FIXTURES_ROOT].
    - parseArgs: copy quarantine-append.mjs lines 36-70 BUT rename the flag to --llm-report. Keep the equals-syntax → exit 2, missing-value → exit 2, next.startsWith('--') → exit 2 (WR-04), and --help/-h → exit 0 guards. (The orchestrator's OWN bad-flag handling exits 2; this is distinct from D-06's exit-0 for the pipeline RUN — bad CLI invocation is a usage error, not a stage failure.)
    - WR-05 path-bound: resolve --llm-report against process.cwd(); assert it is inside ALLOWED_INPUT_ROOTS (resolved === root || resolved.startsWith(root + path.sep)); else write a "--llm-report must reside under tests/e2e/artifacts/ or tests/e2e/fixtures/" stderr message and exit 1. This MUST match the 4 chained CLIs' own bound (Open-Item-1 resolution: the report always lives under artifacts/{runId}/; Plan 36-04's YAML relocates the CI download there). Do NOT relax ALLOWED_INPUT_ROOTS to include a download dir — the chained CLIs would reject it anyway.

    Run-dir resolution (D-07):
      const runDir = path.dirname(resolvedLlmReportPath);
      const rerunReport  = path.join(runDir, 'rerun-report.json');
      const triageReport = path.join(runDir, 'triage-report.json');

    runStage helper (RESEARCH Pattern 1): spawnSync('node', [path.join(PROJECT_ROOT, scriptRel), ...args], { cwd: PROJECT_ROOT, encoding: 'utf8', stdio: ['ignore','pipe','pipe'] }); ok = r.status === 0; write a `[run-triage-pipeline] {label}: ok|FAILED (status=N)` line to stdout; forward r.stdout; on failure forward r.stderr prefixed '[stderr] '. Return a small result object { label, ok, status } for the summary. cwd: PROJECT_ROOT is load-bearing (Pitfall 6) — each chained CLI resolves its sibling outputs via path.dirname(input) against the report path, not the spawner CWD.

    The 4-stage chain (D-05), each via runStage, capturing results into a summary array:
      1. runStage('rerun-validator',   'scripts/e2e-rerun-validator.mjs',   ['--input', resolvedLlmReportPath])
      2. runStage('triage-classifier', 'scripts/e2e-triage-classifier.mjs', ['--input', resolvedLlmReportPath])  // exit 1 in CI BY DESIGN (D-08)
      3. runStage('issue-file',        'scripts/e2e-report-issue.mjs',      ['--source', 'triage', '--triage-report', triageReport])
      4. runStage('quarantine-append', 'scripts/quarantine-append.mjs',     ['--input', triageReport])

    D-08 handling — DO NOT abort on a non-zero status. When stage 2 exits 1 in CI, triage-report.json is never written; stages 3-4 then exit 1 on the missing input — these are logged FAILED and the chain continues. Emit an explicit log line "LLM second-pass skipped in CI (triage-classifier CI gate)" when the triage stage fails AND (process.env.CI === 'true' || process.env.GITHUB_ACTIONS === 'true') so the D-08 path is greppable. Do NOT add any env: { CI: '' } override (anti-pattern, Pitfall 3).

    Summary + exit (D-06): after the chain, write the summary object (stages + their ok/status) to stdout always. Discretionary (D, RESEARCH OQ2): also atomicWriteJson it to path.join(runDir, 'pipeline-summary.json') for forensics — recommended; do NOT wire any GitHub Actions artifact upload. Then process.exit(0) — ALWAYS, regardless of stage failures.

    isMain guard (WR-02 Windows-safe, copy quarantine-append.mjs form): wrap main() so even an uncaught exception logs '[run-triage-pipeline] uncaught: ...' and STILL process.exit(0) (D-06).

    NO fenced code blocks belong in the running file beyond what the analogs already contain — implement as ordinary JS. Target ~80-130 LOC (RESEARCH: "~80-line spawnSync chain"; the scaffolds add the rest). min_lines 70.
  </action>
  <verify>
    <automated>node --check scripts/run-triage-pipeline.mjs && grep -q "cwd: PROJECT_ROOT" scripts/run-triage-pipeline.mjs && grep -q "process.exit(0)" scripts/run-triage-pipeline.mjs && grep -q "ALLOWED_INPUT_ROOTS" scripts/run-triage-pipeline.mjs && node scripts/run-triage-pipeline.mjs --llm-report=/x 2>&1 | grep -qi "equals" && test "$(node scripts/run-triage-pipeline.mjs --llm-report=/x; echo $?)" != "0"</automated>
  </verify>
  <done>run-triage-pipeline.mjs parses clean; uses spawnSync with cwd: PROJECT_ROOT; bounds --llm-report to ALLOWED_INPUT_ROOTS; rejects --llm-report=value with exit 2; and contains process.exit(0) for the normal-run path. The 4 stage labels (rerun-validator, triage-classifier, issue-file, quarantine-append) all appear.</done>
</task>

<task type="auto">
  <name>Task 2: Create the single-finding fixture pair built around an LLM_HALLUCINATED_SELECTION iteration (heuristic critical, ZERO LLM calls) for the SC-2 assertion</name>
  <files>tests/e2e/fixtures/phase36-pipeline-llm-report.json, tests/e2e/fixtures/phase36-pipeline-rerun-report.json</files>
  <read_first>
    - tests/e2e/fixtures/phase35-llm-report.json (shape analog — iterations[] with iteration_n, case_id, seed, classification, citation, llm_selection{patentId, selectedText}, verifier_verdict)
    - tests/e2e/fixtures/phase35-rerun-report.json (shape analog — replays[] with iteration_n, original_verdict_status, runs[], confirmed_count, total_runs, verdict, reason)
    - tests/e2e/lib/rerun-validator.js lines 26-43 (isEligibleForReplay: ONLY WRONG_CITATION + VERIFIER_DISAGREE eligible) + lines 163-175 (ineligible → NOT_REPLAYABLE, ZERO verifyCitation calls)
    - tests/e2e/lib/triage-classifier.js lines 472-498 (Rule 3: NOT_REPLAYABLE + classification ∈ RULE3_CLASSIFICATIONS → severity map; LLM_HALLUCINATED_SELECTION → 'critical', path_taken 'heuristic')
    - scripts/e2e-report-issue.mjs lines 181-194 (filterFindingsForFiling: critical ∈ {critical,high} admits; HARNESS_ERROR + *_parse_error excluded)
    - 36-PATTERNS.md §"Phase 35 fixtures" + §"Open Items for Planner" item 2 (the 1-vs-2-finding mismatch + the heuristic-path resolution)
  </read_first>
  <action>
    Resolve the D-16 / SC-2 "1 finding" requirement (Open-Item-2) WITH a finding that resolves HEURISTICALLY — no LLM call, no cached-PDF dependency. The existing phase35 fixtures route a WRONG_CITATION iteration to a LIVE `claude -p` call (replay-eligible → verifyCitation throws on a synthetic patent → NOT_REPLAYABLE → triage Rule 3 excludes WRONG_CITATION → ambiguous → invokeLlm with CI='' → real claude -p, no creds → HARNESS_ERROR → filtered out → 0 issues). That path is non-deterministic and incurs real LLM spend. The deterministic heuristic path is LLM_HALLUCINATED_SELECTION (NOT replay-eligible → NOT_REPLAYABLE with zero verifyCitation calls → triage Rule 3 maps it to 'critical', path_taken 'heuristic', ZERO invokeLlm calls → filterFindingsForFiling admits it). Build the fixture around that.

    Create tests/e2e/fixtures/phase36-pipeline-llm-report.json: a valid llm-report.json with EXACTLY ONE iteration (iteration_n: 1) whose classification is "LLM_HALLUCINATED_SELECTION". Copy the field set from phase35-llm-report.json iteration 1: iteration_n, case_id (use a real-patent id matching CASE_ID_RE, e.g. 'US11427642-spec-short-1'), seed, classification ('LLM_HALLUCINATED_SELECTION'), citation (may be null — a hallucinated selection produced no valid citation), llm_selection { patentId: 'US11427642', selectedText: a short verbatim-looking string }, and verifier_verdict (may be null or a non-strong-agreement verdict — it is irrelevant because Rule 2 never runs for a NOT_REPLAYABLE iteration). Set run_id (e.g. 'phase36-pipeline-test'), started_iso/finished_iso, and a summary block consistent with phase35 (total_iterations: 1, classified_count: 1, error_count: 0). VERIFY against the rule chain in triage-classifier.js: with rerun verdict NOT_REPLAYABLE, Rule 1 misses (not FLAKE), Rule 2 misses (not CONFIRMED), Rule 3 MATCHES (NOT_REPLAYABLE + LLM_HALLUCINATED_SELECTION ∈ RULE3_CLASSIFICATIONS) → critical/heuristic. ambiguous[] stays empty → invokeLlm callCount === 0.

    Create tests/e2e/fixtures/phase36-pipeline-rerun-report.json: a matching rerun-report with EXACTLY ONE replay (iteration_n: 1) whose verdict is 'NOT_REPLAYABLE' (with reason naming the ineligible classification, mirroring phase35-rerun-report.json replay 2), runs: [], confirmed_count: 0, total_runs: 0, original_verdict_status set from the iteration's verifier_verdict.status (or null). Match the replays[]/summary schema (summary.not_replayable_count: 1, confirmed_count: 0, flake_count: 0). NOTE FOR THE EXECUTOR: stage 1 (rerun-validator) OVERWRITES the live rerun-report.json at runtime — for the LLM_HALLUCINATED_SELECTION iteration it will itself produce exactly this NOT_REPLAYABLE shape (ineligible → no verifyCitation call). This fixture therefore matches what the pipeline regenerates; it exists as a shape reference + the issue-file/quarantine sibling-read source, NOT as a value the chain depends on surviving.

    Do NOT make the finding a HARNESS_ERROR, a *_parse_error, OR a WRONG_CITATION/VERIFIER_DISAGREE (the first two are excluded by filterFindingsForFiling; the latter two are replay-eligible and route to the LLM trap). Keep both fixtures minimal (single iteration / single replay) — they exist only to drive the SC-2 count assertion through the deterministic heuristic path.

    Validate both are parseable JSON before finishing.
  </action>
  <verify>
    <automated>node -e "const l=require('./tests/e2e/fixtures/phase36-pipeline-llm-report.json'); const r=require('./tests/e2e/fixtures/phase36-pipeline-rerun-report.json'); if(l.iterations.length!==1){console.error('llm iterations !=1:',l.iterations.length);process.exit(1);} if(l.iterations[0].classification!=='LLM_HALLUCINATED_SELECTION'){console.error('iteration not LLM_HALLUCINATED_SELECTION:',l.iterations[0].classification);process.exit(1);} if(r.replays.length!==1){console.error('rerun replays !=1:',r.replays.length);process.exit(1);} if(r.replays[0].verdict!=='NOT_REPLAYABLE'){console.error('replay verdict not NOT_REPLAYABLE:',r.replays[0].verdict);process.exit(1);} console.log('single LLM_HALLUCINATED_SELECTION fixture pair OK (heuristic path, zero LLM, zero PDF)');"</automated>
  </verify>
  <done>Both fixtures are valid JSON; the llm-report has exactly 1 iteration classified LLM_HALLUCINATED_SELECTION, the rerun-report has exactly 1 replay with verdict NOT_REPLAYABLE; the finding therefore resolves through triage Rule 3 to severity 'critical' / path_taken 'heuristic' (ZERO invokeLlm calls, ZERO cached-PDF dependency) and is admitted by filterFindingsForFiling (so exactly 1 issue + 1 quarantine entry).</done>
</task>

<task type="auto">
  <name>Task 3: Add e2e:triage-pipeline script + lint extension, and write the spawnSync integration test (full chain CI='' with filed-count + zero-LLM assertions, CI-gated no-op, exit-0-on-failure)</name>
  <files>package.json, tests/e2e/scripts/e2e-run-triage-pipeline.test.js</files>
  <read_first>
    - tests/e2e/scripts/e2e-quarantine-append.test.js (EXACT harness analog — lines 17-94: imports, beforeEach fixture-copy + mock-gh shim + corpus override, afterEach cleanup, spawnAppend helper)
    - package.json (line 15 e2e:rerun-validator analog for the new script; line 17 lint list to extend)
    - 36-RESEARCH.md §"Validation Architecture → Detailed validation specs" item 2 (the exact integration-test assertions) + §"Open Questions" Q1 (the CI='' requirement)
    - 36-PATTERNS.md §"e2e-run-triage-pipeline.test.js" (the spawnPipeline env-difference: CI='' GITHUB_ACTIONS='')
    - tests/e2e/lib/triage-classifier.js lines 472-498 (Rule 3 heuristic path) + tests/e2e/lib/rerun-validator.js lines 26-43 (isEligibleForReplay) — confirm the heuristic LLM_HALLUCINATED_SELECTION path so the test asserts ZERO LLM calls
    - tests/e2e/lib/llm-ledger.js (LEDGER_PATH — if asserting "no LLM ledger entry written" as the zero-LLM proxy, read the ledger shape + path)
  </read_first>
  <action>
    Part A — package.json:
    - Add the script after e2e:triage-classifier (line 16): "e2e:triage-pipeline": "node scripts/run-triage-pipeline.mjs" (D-16).
    - Extend the lint script (line 17) to append " scripts/run-triage-pipeline.mjs" to the eslint file list (D-16 done-criterion; RESEARCH §"Lint extension").

    Part B — tests/e2e/scripts/e2e-run-triage-pipeline.test.js (new spawnSync integration test, analog: e2e-quarantine-append.test.js):
    - Copy the harness scaffold (lines 17-76): __dirname, PROJECT_ROOT, SCRIPT_PATH (= scripts/run-triage-pipeline.mjs), FIXTURE_DIR, ARTIFACTS_DIR; beforeEach that mkdirs artifacts/test-<rand>/ and copies fixtures into it; afterEach rmSync cleanup of runDir + mockGhDir.
    - In beforeEach, copy the NEW phase36 fixtures into runDir as the chain's expected names: copy phase36-pipeline-llm-report.json → runDir/llm-report.json. Copying phase36-pipeline-rerun-report.json → runDir/rerun-report.json is OPTIONAL (stage 1 overwrites it; for the LLM_HALLUCINATED_SELECTION iteration rerun-validator regenerates the identical NOT_REPLAYABLE shape with zero verifyCitation calls — no cached PDF needed). Do NOT pre-copy a triage-report.json — the triage-classifier stage writes it when CI=''.
    - mock-gh shim: copy the lines 53-66 pattern; issue list returns [] (so issue create fires — note: --source triage uses listOpenWithSearch which must also return []; make the mock answer `issue list --search` with [] and `issue create` with a URL). Write the transcript so the test can assert `issue create` appeared.
    - corpusOverridePath: a fresh empty corpus in mockGhDir (so the committed tests/e2e/test-cases-quarantine.js is never mutated — Phase 35 pattern).
    - spawnPipeline(args, extraEnv={}) helper: spawnSync('node', [SCRIPT_PATH, ...args], { env: { ...process.env, PATH: mockGhDir+':'+process.env.PATH, GITHUB_REPOSITORY: 'test/test', QUARANTINE_CORPUS_PATH_OVERRIDE: corpusOverridePath, CI: '', GITHUB_ACTIONS: '', ...extraEnv }, encoding: 'utf8', timeout: 30000 }). The CI='' + GITHUB_ACTIONS='' default is LOAD-BEARING (D-08 / RESEARCH OQ1 / Assumption A4): without it the triage-classifier CI gate fires and stages 3-4 no-op, breaking the count assertions. Higher timeout (30s) because it chains 4 node spawns.

    Three tests:
    1. ORCH-01 full chain (D-16, SC-2): spawnPipeline(['--llm-report', path.join(runDir, 'llm-report.json')]). Assert the POST-PIPELINE filed-count invariant (NOT the seeded-fixture verdict — stage 1 discards/regenerates rerun-report.json): r.status === 0; fs.existsSync(runDir/rerun-report.json) && fs.existsSync(runDir/triage-report.json); the triage-report.json on disk has exactly 1 finding with severity 'critical' and path_taken 'heuristic' (proves the heuristic path fired); the mock-gh transcript contains 'issue create'; the override corpus now has exactly 1 entry (read corpusOverridePath, regex-count TEST_CASES_QUARANTINE objects OR dynamic-import it and assert .length === 1). ZERO-LLM ASSERTION (load-bearing): assert no real claude -p was spawned via the heuristic-path proxy — read the parsed triage-report.json and assert summary.llm_pass_count === 0 && summary.cluster_pass_count === 0 && summary.heuristic_count === 1 (this is the deterministic invokeLlm-callCount===0 evidence the report exposes). Do NOT mock invokeLlm — the heuristic path means it is never reached.
    2. D-08 CI-gated no-op (separate test): spawnPipeline([...], { CI: 'true' }). Assert: r.status === 0; rerun-report.json written; stdout contains the triage stage FAILED log OR the "skipped in CI" line; the override corpus is UNCHANGED (length 0).
    3. ORCH-01 exit-0-on-stage-failure: point --llm-report at a fixtures-root file that exists but is malformed/empty so a stage exits non-zero (e.g. copy an empty {} into runDir/llm-report.json for this test, or use a missing-sibling setup). Assert r.status === 0 regardless (D-06). Keep it WR-05-compliant (path under artifacts/).

    Use describe/it blocks mirroring the analog's G1-G8 naming style (e.g. P1 full-chain, P2 ci-gated, P3 exit-0-on-failure).
  </action>
  <verify>
    <automated>npx vitest run tests/e2e/scripts/e2e-run-triage-pipeline.test.js && npm run lint && node -e "const p=require('./package.json'); if(!p.scripts['e2e:triage-pipeline']){console.error('missing e2e:triage-pipeline');process.exit(1);} if(!p.scripts.lint.includes('run-triage-pipeline.mjs')){console.error('lint not extended');process.exit(1);} console.log('package.json OK');"</automated>
  </verify>
  <done>The integration test passes all three cases: (1) full chain exit 0 + both reports + triage-report has exactly 1 critical/heuristic finding (heuristic_count===1, llm_pass_count===0, cluster_pass_count===0 — zero LLM) + mock issue + exactly 1 quarantine entry; (2) CI-gated no-op exit 0 + corpus unchanged; (3) exit-0-on-stage-failure. `npm run lint` passes with run-triage-pipeline.mjs in the list. package.json has the e2e:triage-pipeline script. No real claude -p is spawned in any test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| --llm-report path → orchestrator | Untrusted file path; bounded by WR-05 ALLOWED_INPUT_ROOTS before any read or spawn |
| orchestrator → 4 child CLIs | spawnSync('node', [...]) — args are fixed literals + the bounded report path; no shell string interpolation |
| child CLI stderr/stdout → pipeline log | Captured and forwarded; never eval'd |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02-01 | Tampering | --llm-report path traversal (`--llm-report ../../../tmp/x.json`) | mitigate | WR-05 ALLOWED_INPUT_ROOTS bound (resolve + startsWith(root+sep)); exit 1 on violation. Mirrors quarantine-append.mjs lines 182-193. The 4 chained CLIs enforce the same bound as defense-in-depth. |
| T-36-02-02 | Elevation of Privilege | shell injection via spawnSync | mitigate | spawnSync('node', [argv-array], {...}) — NOT a shell string; args passed as an array so no shell metacharacter interpolation. cwd is a fixed PROJECT_ROOT constant. |
| T-36-02-03 | Info disclosure / cost | LLM API billing leak during the integration test | mitigate | The SC-2 fixture is an LLM_HALLUCINATED_SELECTION iteration that resolves via the triage HEURISTIC path (Rule 3) — invokeLlm is never reached (heuristic_count===1, llm_pass_count===0 asserted). No real `claude -p` spawns even with CI=''. The pipeline never overrides CI; in CI the triage CI gate fires by design (D-08). No env: CI='' in any production path (Pitfall 3). |
| T-36-02-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (zero-new-dependency lock, RESEARCH §Package Legitimacy Audit). The new fixtures are static JSON; the test reuses the existing mock-gh bash shim. |
</threat_model>

<verification>
- `node --check scripts/run-triage-pipeline.mjs` passes; bad-flag invocation exits 2; WR-05 violation exits 1.
- `npx vitest run tests/e2e/scripts/e2e-run-triage-pipeline.test.js` green (3 tests), including the zero-LLM (heuristic_count===1, llm_pass_count===0) assertion.
- `npm run lint` passes with run-triage-pipeline.mjs covered.
- `npm run test:src` stays green (no existing tests broken).
- Optional manual: `CI= GITHUB_ACTIONS= node scripts/run-triage-pipeline.mjs --llm-report tests/e2e/fixtures/phase36-pipeline-llm-report.json` (note: fixtures live under an allowed root) exits 0, logs a per-stage summary, and spawns NO claude -p (the LLM_HALLUCINATED_SELECTION fixture resolves heuristically).
</verification>

<success_criteria>
- ORCH-01 / SC-2 satisfied: the orchestrator chains the 4 CLIs (cwd: PROJECT_ROOT), exits 0 always, and against the single LLM_HALLUCINATED_SELECTION fixture (CI='') writes both reports, files a mock issue, and appends exactly 1 quarantine entry — all through the deterministic HEURISTIC path with ZERO live LLM calls.
- D-08 covered: a separate test proves the CI-gated path exits 0 with the triage stage skipped and the corpus unchanged.
- Open-Item-1 (WR-05 path) resolved: the orchestrator's ALLOWED_INPUT_ROOTS = [artifacts, fixtures]; Plan 36-04's YAML must place the downloaded artifact under artifacts/{runId}/.
- Open-Item-2 (fixture count + LLM-routing trap) resolved: a NEW single LLM_HALLUCINATED_SELECTION fixture drives the count===1 assertion via the heuristic path — no WRONG_CITATION live-LLM dependency, no cached-PDF dependency.
</success_criteria>

<output>
Create `.planning/phases/36-quarantine-ci-integration-+-pipeline-orchestrator/36-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
