---
phase: 36-quarantine-ci-integration-+-pipeline-orchestrator
plan: "04"
type: execute
wave: 3
depends_on: ["36-01", "36-02", "36-03"]
files_modified:
  - .github/workflows/e2e-nightly.yml
  - tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js
autonomous: true
requirements: [QUAR-04, ORCH-02, ORCH-03]
requirements_addressed: [QUAR-04, ORCH-02, ORCH-03]

must_haves:
  truths:
    - "D-09: the triage-pipeline step AND the quarantine-spec step are BOTH gated on `if: inputs.llm_run_id != ''` — the same expression the existing download step uses; without llm_run_id both skip and regression behavior is byte-identical (ORCH-02 / SC-3)"
    - "D-10: the quarantine spec step has continue-on-error: true (non-gating, QUAR-04) — a quarantine failure never fails the job"
    - "D-11: a post-quarantine step gated on steps.quarantine.outcome == 'failure' invokes e2e-report-issue.mjs --source quarantine (e2e-quarantine label, SC-4)"
    - "D-12: step order after the existing fault-injection steps: (2) ensure e2e-quarantine label, (3) run-triage-pipeline, (4) quarantine spec (continue-on-error + timeout-minutes), (5) quarantine-failure issue filer — steps 2-5 gated on llm_run_id"
    - "D-13: the e2e-quarantine label is self-bootstrapped via `gh label create e2e-quarantine --color <hex> --force` (mirrors the e2e-nightly label step), gated on llm_run_id"
    - "D-14 / ORCH-03 / SC-5: a TIMEOUT BUDGET comment documents the arithmetic (job cap 30 min; quarantine adds N×~90s/case; N=0 today; ceiling ~20 cases) and the quarantine spec step has its own timeout-minutes (15) so it cannot starve the job"
    - "WR-05 resolution (Open-Item-1): the download step relocates the artifact to tests/e2e/artifacts/${{ github.run_id }}/ so run-triage-pipeline.mjs's --llm-report path satisfies the orchestrator's + the 4 chained CLIs' ALLOWED_INPUT_ROOTS bound"
  artifacts:
    - path: ".github/workflows/e2e-nightly.yml"
      provides: "5 gated quarantine/triage steps + relocated download path + timeout-budget comment; existing regression/fault-injection steps unchanged"
      contains: "e2e-quarantine"
    - path: "tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js"
      provides: "grep-based YAML assertions (gating, continue-on-error, timeout comment, per-step timeout, label-ensure)"
      contains: "e2e-nightly.yml"
  key_links:
    - from: ".github/workflows/e2e-nightly.yml (Run triage pipeline step)"
      to: "scripts/run-triage-pipeline.mjs --llm-report tests/e2e/artifacts/${{ github.run_id }}/llm-report.json"
      via: "node scripts/run-triage-pipeline.mjs"
      pattern: "run-triage-pipeline.mjs --llm-report"
    - from: ".github/workflows/e2e-nightly.yml (quarantine spec step)"
      to: "npm run e2e:quarantine"
      via: "run: npm run e2e:quarantine + continue-on-error: true + timeout-minutes"
      pattern: "npm run e2e:quarantine"
    - from: ".github/workflows/e2e-nightly.yml (failure filer step)"
      to: "scripts/e2e-report-issue.mjs --source quarantine"
      via: "if: ... && steps.quarantine.outcome == 'failure'"
      pattern: "--source quarantine"
---

<objective>
Wire the quarantine corpus + triage pipeline into `e2e-nightly.yml` (D-09 through D-14). Append five steps after the existing fault-injection steps, all gated on `if: inputs.llm_run_id != ''` (the SAME expression the download step uses — Pitfall 5): (2) ensure the `e2e-quarantine` label, (3) run the triage pipeline, (4) run the quarantine spec (`continue-on-error: true` + its own `timeout-minutes`), (5) file quarantine-failure issues with `--source quarantine`. Relocate the existing LLM-report download so the artifact lands under `tests/e2e/artifacts/${{ github.run_id }}/` (resolving the WR-05 path conflict — the orchestrator and all four chained CLIs require inputs under that root). Document the timeout budget arithmetic in a YAML comment (ORCH-03/SC-5). Add a dependency-free grep-based vitest test asserting the gating, continue-on-error, timeout comment, per-step timeout, and label-ensure are all present.

Purpose: QUAR-04 (CI side) + ORCH-02 (gated nightly wiring) + ORCH-03 (timeout budget) — make the nightly run the full pipeline + quarantine suite only when dispatched with an `llm_run_id`, while keeping the cron-triggered regression path byte-identical.
Output: modified `.github/workflows/e2e-nightly.yml`, new `tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-quarantine-ci-integration-+-pipeline-orchestrator/36-CONTEXT.md
@.planning/phases/36-quarantine-ci-integration-+-pipeline-orchestrator/36-RESEARCH.md
@.planning/phases/36-quarantine-ci-integration-+-pipeline-orchestrator/36-PATTERNS.md

# The workflow to modify (self-analog — every idiom already exists in this file)
@.github/workflows/e2e-nightly.yml

<interfaces>
<!-- Contracts the executor needs. Extracted from .github/workflows/e2e-nightly.yml — no exploration required. -->

EXISTING idioms to mirror (line numbers in the current file):
  line 27-34   llm_run_id workflow_dispatch input, default: '' (the gating value — Pitfall 5: already correct, reuse as-is)
  line 39-41   concurrency: group e2e-nightly (DO NOT create a second workflow — Pitfall 1 collision)
  line 50      job timeout-minutes: 30 (the budget the quarantine step must not starve)
  line 97-105  "Ensure e2e-nightly label exists" step: gh label create "e2e-nightly" --color "0075ca" --description "..." --force 2>/dev/null || true ; env: GH_TOKEN. (Color = 6-char hex, NO # prefix.)
  line 107-147 "Download and validate LLM report" step: if: inputs.llm_run_id != '' ; env LLM_RUN_ID hop + numeric guard ; `mkdir -p downloaded-llm-report` ; `gh run download "$LLM_RUN_ID" -n llm-report -D downloaded-llm-report`. ← THIS DIR is the WR-05 conflict; relocate it (Task 1).
  line 188-199 "Run E2E regression" step: id: regression ; continue-on-error: true (the continue-on-error analog).
  line 201-206 "File issues for failures" step: if: steps.smoke.outcome == 'success' && steps.regression.outcome == 'failure' (the outcome-gated filer analog).
  line 208-233 fault-injection steps (the new steps go AFTER these, before "Upload E2E artifacts" at 235).

Downstream contracts this plan wires (from Plans 36-01/02/03):
  npm run e2e:quarantine                              (Plan 36-01) — exits 0 on empty corpus; --retries=0 --pass-with-no-tests baked in
  node scripts/run-triage-pipeline.mjs --llm-report <path>   (Plan 36-02) — exits 0 always; --llm-report MUST be under tests/e2e/artifacts/ or fixtures/ (WR-05)
  node scripts/e2e-report-issue.mjs --source quarantine      (Plan 36-03) — reads tests/e2e/artifacts/{PLAYWRIGHT_RUN_ID}/report.json; files with e2e-quarantine label
  PLAYWRIGHT_RUN_ID is already set job-wide to github.run_id (line 52) — so report.json + the relocated llm-report.json share the artifacts/{run_id}/ dir.

Claude's Discretion values (CONTEXT §Claude's Discretion; recommended in RESEARCH):
  - e2e-quarantine label color: d93f0b (distinct from e2e-nightly's 0075ca) — RESEARCH A2
  - quarantine step timeout-minutes: 15 — RESEARCH A3 / D-14
  - failure detection: steps.quarantine.outcome == 'failure' (simplest; processReport already iterates report.json) — PATTERNS Open-Item 4
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate the LLM-report download under tests/e2e/artifacts/{run_id}/ (WR-05 resolution)</name>
  <files>.github/workflows/e2e-nightly.yml</files>
  <read_first>
    - .github/workflows/e2e-nightly.yml (lines 107-147 — the download+validate step; line 52 PLAYWRIGHT_RUN_ID = github.run_id)
    - 36-PATTERNS.md §"Open Items for Planner" item 1 (the WR-05 vs download-dir conflict and the agreed resolution)
    - 36-02-PLAN.md must_haves (the orchestrator's ALLOWED_INPUT_ROOTS = [artifacts, fixtures] — the constraint this step must satisfy)
  </read_first>
  <action>
    Resolve Open-Item-1 (the WR-05 path conflict) by relocating the downloaded artifact under the artifacts root the orchestrator + the 4 chained CLIs require. Edit the existing "Download and validate LLM report" step (lines 107-147):

    - Change the download destination from `downloaded-llm-report` to `tests/e2e/artifacts/${{ github.run_id }}` (this matches PLAYWRIGHT_RUN_ID at line 52, so the relocated llm-report.json sits as a sibling of the report.json the quarantine spec + filer use).
      Concretely:
        mkdir -p tests/e2e/artifacts/"${{ github.run_id }}"
        gh run download "$LLM_RUN_ID" -n llm-report -D tests/e2e/artifacts/"${{ github.run_id }}"
      Keep the env-var hop (LLM_RUN_ID) + the numeric `case "$LLM_RUN_ID" in ''|*[!0-9]*) ... exit 1` guard (CWE-94 mitigation) UNCHANGED.
    - Update the inline node schema-validate snippet (lines 127-139) to read from the new path: `tests/e2e/artifacts/${{ github.run_id }}/llm-report.json` instead of `downloaded-llm-report/llm-report.json`. Keep the appendLlmIteration schema-gate logic (it is the hard validity gate — D-03/D-06).
    - Leave the gating (`if: inputs.llm_run_id != ''`, line 108), the step name, and the comment block otherwise intact; add one line to the comment noting "Downloaded under tests/e2e/artifacts/${{ github.run_id }}/ so run-triage-pipeline.mjs's --llm-report satisfies the WR-05 ALLOWED_INPUT_ROOTS bound (Phase 36 Open-Item-1)."

    Note `github.run_id` is a GitHub-controlled numeric — safe to interpolate in the path (not user input). The `$LLM_RUN_ID` value (user-controlled) stays behind the env hop + numeric guard and is NOT used in the path.
  </action>
  <verify>
    <automated>grep -q 'tests/e2e/artifacts/.*github.run_id.*' .github/workflows/e2e-nightly.yml && ! grep -q "downloaded-llm-report" .github/workflows/e2e-nightly.yml && grep -q 'case "\$LLM_RUN_ID" in' .github/workflows/e2e-nightly.yml</automated>
  </verify>
  <done>The download step writes to tests/e2e/artifacts/${{ github.run_id }}/, the old `downloaded-llm-report` dir is gone everywhere in the file, the numeric LLM_RUN_ID guard is preserved, and the schema-validate snippet reads from the new path.</done>
</task>

<task type="auto">
  <name>Task 2: Append the 5 gated quarantine/triage steps + timeout-budget comment</name>
  <files>.github/workflows/e2e-nightly.yml</files>
  <read_first>
    - .github/workflows/e2e-nightly.yml (lines 97-105 label-ensure analog; 188-206 continue-on-error + outcome-gated filer analogs; insert AFTER line 234, before "Upload E2E artifacts" at 235)
    - 36-RESEARCH.md §"Pattern 4: GitHub Actions gated + non-gating + per-step timeout" (the exact YAML for all 5 steps + the full timeout-budget comment text)
    - 36-PATTERNS.md §".github/workflows/e2e-nightly.yml (MODIFIED)" (the 5-step recipe + anti-patterns)
  </read_first>
  <action>
    Append five steps to the e2e-nightly job AFTER the existing fault-injection issue-filer (after line 234) and BEFORE "Upload E2E artifacts" (line 235). All five gated on `if: inputs.llm_run_id != ''` (D-09 — reuse the EXACT expression from line 108; do NOT invent a new gating expression, Pitfall 5). Use the RESEARCH Pattern 4 YAML verbatim, adapting paths to Task 1's relocated artifact.

    Precede the steps with the TIMEOUT BUDGET comment block (D-14 / ORCH-03 / SC-5). It MUST contain the literal token `TIMEOUT BUDGET` and document: job cap = 30 min (line 50); quarantine adds N_quarantine × ~90s/case (per-test timeout in tests/e2e/playwright.config.js); CURRENT N=0 (empty corpus) → 0 added minutes; CEILING at ~20 cases (20×90s = 30 min would exceed the cap); mitigation = the quarantine step's own timeout-minutes (15) so it cannot starve the job; >20 cases → daily --grep rotation (deferred, D-14).

    Step (2) Ensure e2e-quarantine label — mirror the e2e-nightly label step (lines 97-105), swap name/color:
      - name: Ensure e2e-quarantine label exists
        if: inputs.llm_run_id != ''
        run: gh label create "e2e-quarantine" --color "d93f0b" --description "Auto-filed by nightly quarantine suite" --force 2>/dev/null || true
        env: GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      (Color d93f0b is 6-char hex, NO # prefix — gh CLI requirement. Discretion A2.)

    Step (3) Run triage pipeline — points at Task 1's relocated artifact:
      - name: Run triage pipeline
        if: inputs.llm_run_id != ''
        run: node scripts/run-triage-pipeline.mjs --llm-report tests/e2e/artifacts/${{ github.run_id }}/llm-report.json
      Comment: exits 0 always (D-06); triage-classifier CI gate fires → LLM 2nd-pass skipped (D-08); do NOT add env: CI:'' (Pitfall 3 anti-pattern).

    Step (4) Run quarantine spec (non-gating) — mirror regression's continue-on-error (line 191), add timeout-minutes (D-14):
      - name: Run quarantine spec (non-gating)
        id: quarantine
        if: inputs.llm_run_id != ''
        continue-on-error: true
        timeout-minutes: 15
        run: npm run e2e:quarantine
      Comment: continue-on-error mirrors regression — a quarantine failure never fails the job (QUAR-04); timeout-minutes:15 (D-14, Discretion A3) so quarantine can't consume the whole 30-min budget; --retries=0 --pass-with-no-tests are baked into the npm script (Plan 36-01).

    Step (5) File quarantine issues on failure — mirror the regression outcome-gated filer (line 202):
      - name: File quarantine issues on failure
        if: inputs.llm_run_id != '' && steps.quarantine.outcome == 'failure'
        run: node scripts/e2e-report-issue.mjs --source quarantine
      Comment: outcome == 'failure' (Discretion: simplest detection; processReport already iterates report.json so no extra parse). Reads tests/e2e/artifacts/${PLAYWRIGHT_RUN_ID}/report.json (PLAYWRIGHT_RUN_ID = github.run_id, line 52) and files with the e2e-quarantine label (Plan 36-03 / SC-4).

    Anti-patterns (RESEARCH): do NOT create a separate e2e-quarantine.yml (concurrency collision, Pitfall 1); do NOT fork playwright.config.js; do NOT add env: CI:'' to the triage step (Pitfall 3). Keep the existing regression/fault-injection/upload steps byte-identical (ORCH-02 / SC-3).
  </action>
  <verify>
    <automated>grep -q "Ensure e2e-quarantine label exists" .github/workflows/e2e-nightly.yml && grep -q "run-triage-pipeline.mjs --llm-report" .github/workflows/e2e-nightly.yml && grep -q "npm run e2e:quarantine" .github/workflows/e2e-nightly.yml && grep -q -- "--source quarantine" .github/workflows/e2e-nightly.yml && grep -q "TIMEOUT BUDGET" .github/workflows/e2e-nightly.yml && grep -q "timeout-minutes: 15" .github/workflows/e2e-nightly.yml && test "$(grep -c "if: inputs.llm_run_id != ''" .github/workflows/e2e-nightly.yml)" -ge 5</automated>
  </verify>
  <done>All 5 steps are present, gated on `if: inputs.llm_run_id != ''` (the expression now appears ≥5 times: the original download step + the 4 newly-gated steps; the failure filer adds the compound `&& steps.quarantine.outcome == 'failure'`); the quarantine step has continue-on-error: true + timeout-minutes: 15; the TIMEOUT BUDGET comment is present; the e2e-quarantine label-ensure step uses --color d93f0b --force. Existing regression/fault-injection/upload steps unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add a grep-based YAML assertion test (dependency-free)</name>
  <files>tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js</files>
  <read_first>
    - tests/e2e/scripts/e2e-quarantine-append.test.js (vitest structure analog — imports, describe/it)
    - 36-RESEARCH.md §"Validation Architecture → Detailed validation specs" item 4 (the exact grep assertions; grep-based, NO js-yaml dep — zero-new-dep lock)
    - 36-VALIDATION.md §"Wave 0 Requirements" (the YAML verification checklist)
  </read_first>
  <action>
    Create tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js — a dependency-free vitest test that reads .github/workflows/e2e-nightly.yml as TEXT and asserts the Phase 36 wiring. Do NOT add js-yaml or any yaml-lint dependency (zero-new-dep lock); use string/regex assertions on the file content (RESEARCH item 4).

    Setup: import { describe, it, expect } from 'vitest'; fs, path, fileURLToPath; resolve YAML_PATH = PROJECT_ROOT/.github/workflows/e2e-nightly.yml; read it once into a `yaml` string in a beforeAll or module scope.

    Assertions (one it() each, named for ORCH-02/ORCH-03/QUAR-04):
    - ORCH-02 gating: the quarantine spec step and the triage-pipeline step are gated — assert the file contains `if: inputs.llm_run_id != ''` and that the count of that exact expression is ≥ 5 (download + 4 new steps). Also assert the file contains `node scripts/run-triage-pipeline.mjs --llm-report` and `npm run e2e:quarantine`.
    - QUAR-04 non-gating: assert `continue-on-error: true` appears AND that the quarantine step block (the text window around `id: quarantine` / `Run quarantine spec`) contains both `continue-on-error: true` and `timeout-minutes: 15` and `npm run e2e:quarantine`. (Use a regex spanning the step, or assert all three tokens are present within the substring from "Run quarantine spec" to the next "- name:".)
    - QUAR-04 label: assert `gh label create "e2e-quarantine"` and `--source quarantine` both appear; assert `steps.quarantine.outcome == 'failure'` gates the failure filer.
    - ORCH-03 timeout budget: assert the literal `TIMEOUT BUDGET` token is present AND `timeout-minutes: 15` is present.
    - Regression-unchanged guard (SC-3): assert the existing markers are still present and intact — `specs/regression.spec.js`, `id: regression`, `Run fault-injection spec`, and that the relocated download path `tests/e2e/artifacts/` appears while `downloaded-llm-report` does NOT (Task 1 resolution).

    Keep assertions resilient to incidental whitespace (match on the meaningful tokens, not exact indentation). This test is the QUAR-04 + ORCH-02 + ORCH-03 automated gate; it runs in test:src (no GitHub Actions dispatch needed).
  </action>
  <verify>
    <automated>npx vitest run tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js</automated>
  </verify>
  <done>The grep-based YAML test passes: it asserts the gating (≥5 occurrences of the llm_run_id expression), the triage-pipeline + e2e:quarantine invocations, continue-on-error + timeout-minutes on the quarantine step, the e2e-quarantine label-ensure + --source quarantine failure filer, the TIMEOUT BUDGET comment, and that the download was relocated under tests/e2e/artifacts/ (no downloaded-llm-report). No js-yaml dependency added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow_dispatch llm_run_id → runner shell | User-controlled dispatch input; reaches `gh run download` |
| github.run_id → artifact path | GitHub-controlled numeric; interpolated into the download/artifacts path |
| quarantine report.json → gh CLI | Filed via --source quarantine (sanitizeCaseId guard lives in e2e-report-issue.mjs, Plan 36-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04-01 | Tampering / Elevation | llm_run_id → shell injection (CWE-94) | mitigate (existing) | Env-var hop (LLM_RUN_ID) + numeric `case ''|*[!0-9]*) exit 1` guard — preserved unchanged in Task 1. The relocated path uses github.run_id (GitHub-controlled), NOT the user value. |
| T-36-04-02 | Tampering | label value injection in `gh label create` | accept | The label is a hardcoded literal "e2e-quarantine" (no input interpolation); `|| true` makes a --force rejection non-fatal. |
| T-36-04-03 | Denial of Service | quarantine corpus growth starving the 30-min job | mitigate | Per-step timeout-minutes: 15 on the quarantine step (D-14) caps it; TIMEOUT BUDGET comment flags the >20-case ceiling for future --grep rotation. |
| T-36-04-04 | Tampering | concurrency-group mutex collision | mitigate | Steps stay INSIDE e2e-nightly.yml (the existing e2e-nightly concurrency group); no second workflow created (Pitfall 1). |
| T-36-04-SC | Tampering | npm/pip/cargo installs | n/a | No package installs; the YAML test is grep-based (NO js-yaml dependency — zero-new-dep lock). |
</threat_model>

<verification>
- All Task 2 grep gates pass; the relocated download path replaces `downloaded-llm-report` everywhere (Task 1).
- `npx vitest run tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js` green.
- `npm run test:src` stays green (no other tests touched).
- Manual (deferred to live dispatch, per 36-VALIDATION §Manual-Only): `gh workflow run e2e-nightly.yml -f llm_run_id=<id>` runs steps 2-5; a cron tick (no llm_run_id) skips them (regression byte-identical — SC-3). Adding a known-failing quarantine entry and dispatching verifies the e2e-quarantine label in the GitHub UI (QUAR-04).
</verification>

<success_criteria>
- ORCH-02 satisfied: triage-pipeline + quarantine steps gated on `inputs.llm_run_id != ''`; cron-triggered regression path unchanged (SC-3).
- QUAR-04 (CI side) satisfied: quarantine spec runs continue-on-error with its own timeout; failures file issues via --source quarantine (e2e-quarantine label).
- ORCH-03 satisfied: TIMEOUT BUDGET comment documents the arithmetic + the >20-case ceiling; per-step timeout-minutes: 15 enforces it.
- WR-05 Open-Item-1 resolved: the download lands under tests/e2e/artifacts/${{ github.run_id }}/ so run-triage-pipeline.mjs's --llm-report path is inside ALLOWED_INPUT_ROOTS.
</success_criteria>

<output>
Create `.planning/phases/36-quarantine-ci-integration-+-pipeline-orchestrator/36-04-SUMMARY.md` when done.
</output>
