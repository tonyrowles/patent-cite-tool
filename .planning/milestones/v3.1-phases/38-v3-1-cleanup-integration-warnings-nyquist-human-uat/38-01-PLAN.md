---
phase: 38-v3-1-cleanup-integration-warnings-nyquist-human-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/specs/quarantine.spec.js
  - tests/unit/quarantine-spec-import.test.js
  - scripts/weekly-digest.mjs
  - tests/e2e/scripts/e2e-weekly-digest.test.js
  - .github/workflows/e2e-nightly.yml
  - tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js
  - .planning/v3.1-MILESTONE-AUDIT.md
autonomous: true
requirements:
  - QUAR-01
  - QUAR-03
  - QUAR-04
  - DIGEST-04
user_setup: []
tags:
  - v3.1
  - integration-fixes
  - tech-debt
must_haves:
  truths:
    - "quarantine.spec.js imports QUARANTINE_REPORT_FILENAME from scripts/e2e-report-issue.mjs (single source of truth) and no local re-declaration remains"
    - "scripts/weekly-digest.mjs runtime drift guard validates SUMMARY_KEYS presence against real aggregated metric data, not against a SUMMARY_KEYS-seeded tally — a key drift produces a descriptive throw at runtime"
    - "e2e-nightly.yml Upload E2E artifacts step `if:` includes steps.quarantine.outcome == 'failure' inside the existing parenthesised clause so quarantine failures upload diagnostic artifacts"
    - "Three vitest regression tests pin each contract; one commit per fix (3 atomic commits) preserves bisectability"
    - ".planning/v3.1-MILESTONE-AUDIT.md gaps.integration block and tech_debt[].cross-cutting items reflect the post-fix state (the 3 fragility warnings resolved)"
  artifacts:
    - path: "tests/e2e/specs/quarantine.spec.js"
      provides: "Quarantine Playwright spec, consuming QUARANTINE_REPORT_FILENAME via import"
      contains: "import.*QUARANTINE_REPORT_FILENAME"
    - path: "tests/unit/quarantine-spec-import.test.js"
      provides: "INT-FIX-01 regression: spec-source grep asserts import and absence of local re-declare"
      min_lines: 25
    - path: "scripts/weekly-digest.mjs"
      provides: "Aggregator + drift guard; new exported aggregateBySummaryKey helper feeds validateSummaryKeys"
      contains: "export function aggregateBySummaryKey"
    - path: "tests/e2e/scripts/e2e-weekly-digest.test.js"
      provides: "INT-FIX-02 regression: synthetic-drift aggregated object throws naming missing SUMMARY_KEY"
      contains: "aggregateBySummaryKey"
    - path: ".github/workflows/e2e-nightly.yml"
      provides: "Nightly workflow with upload-artifact step gated on all four outcomes including quarantine; new id: upload-artifacts for grep-test stability"
      contains: "id: upload-artifacts"
    - path: "tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js"
      provides: "INT-FIX-03 regression: step-window grep asserts quarantine clause is INSIDE the upload step `if:` parens"
      contains: "Y6"
    - path: ".planning/v3.1-MILESTONE-AUDIT.md"
      provides: "Authoritative audit YAML; gaps.integration emptied + tech_debt cross-cutting entry updated to reflect resolution"
      contains: "gaps:"
  key_links:
    - from: "tests/e2e/specs/quarantine.spec.js"
      to: "scripts/e2e-report-issue.mjs"
      via: "ESM import"
      pattern: "import\\s+\\{[^}]*QUARANTINE_REPORT_FILENAME[^}]*\\}\\s+from\\s+['\"][^'\"]*scripts/e2e-report-issue\\.mjs['\"]"
    - from: "scripts/weekly-digest.mjs runDigest()"
      to: "validateSummaryKeys(aggregateBySummaryKey(...))"
      via: "function call on aggregated real data"
      pattern: "validateSummaryKeys\\(aggregateBySummaryKey"
    - from: ".github/workflows/e2e-nightly.yml Upload E2E artifacts step"
      to: "quarantine step outcome"
      via: "if: always() && (... || steps.quarantine.outcome == 'failure')"
      pattern: "steps\\.quarantine\\.outcome == 'failure'"
---

<objective>
Resolve the three v3.1 integration fragility warnings catalogued in `.planning/v3.1-MILESTONE-AUDIT.md` `gaps.integration`. Each fix lands as one atomic commit paired with one vitest regression test that pins the contract. Final task updates the audit YAML in place to reflect post-fix state.

Purpose: Eliminate three silent-failure classes — (a) one-sided rename of QUARANTINE_REPORT_FILENAME silently producing zero filings, (b) self-referential SUMMARY_KEYS drift guard that can never detect drift, (c) lost diagnostic artifacts when only quarantine fails — without altering external behavior of any consumer that exercises the happy path today.

Output: 3 source-code/YAML fixes + 3 vitest regression cases + 1 audit-doc update. Three atomic commits in the order INT-FIX-01 → INT-FIX-02 → INT-FIX-03 (matching Phase 37 CR-01/CR-02 commit pattern), then the audit-doc edit as a fourth doc commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v3.1-MILESTONE-AUDIT.md
@.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-CONTEXT.md
@.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-RESEARCH.md
@.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Exports the spec must import + helper functions involved. -->

From scripts/e2e-report-issue.mjs (line 50):
  export const QUARANTINE_REPORT_FILENAME = 'quarantine-report.json';
  (already exported; consumer side only needs to import)

From tests/e2e/lib/llm-report.js (line 123-131):
  export const SUMMARY_KEYS = Object.freeze([
    'passed', 'wrong_citation', 'verifier_disagree',
    'llm_hallucinated_selection', 'llm_api_error',
    'harness_error', 'total_cost_usd',
  ]);

From tests/e2e/lib/llm-report.js (around line 142-152): closed-enum mapper
  function classificationToSummaryKey(classification): 'PASS' -> 'passed', 'WRONG_CITATION' -> 'wrong_citation', etc.
  (NOT exported today — the helper for INT-FIX-02 will re-implement the same mapping locally in weekly-digest.mjs to avoid widening llm-report.js's export surface)

From tests/e2e/lib/error-codes.js (already imported by weekly-digest.mjs line 33):
  export const ERROR_CLASSES (closed enum)

From scripts/weekly-digest.mjs (line 78-87): existing validator
  export function validateSummaryKeys(obj) { for k of SUMMARY_KEYS: if !(k in obj) throw `weekly-digest: summary missing required SUMMARY_KEY '${k}'.` }

Existing aggregator (line 98) returns shape:
  { findingsCount, breakdown, top3, quarantineGrowth }
  — NOT SUMMARY_KEYS-shaped. INT-FIX-02 adds a new aggregateBySummaryKey helper.

Existing YAML target (.github/workflows/e2e-nightly.yml, around line 303-313):
  - name: Upload E2E artifacts
    if: always() && (steps.smoke.outcome == 'failure' || steps.regression.outcome == 'failure' || steps.fault_injection.outcome == 'failure')
    uses: actions/upload-artifact@v4
    (no id: today — INT-FIX-03 adds `id: upload-artifacts` for grep-test stability per RESEARCH Open Q #2)

Existing YAML test scaffold (tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js):
  Y1..Y5 existing it() blocks; Y6 added by INT-FIX-03. Window-scoping pattern at Y2 (lines 50-72) is the template — locate the step by `id:` (preferred) or `- name:`, slice until next `- name:` boundary, assert clause membership within window.

Existing throw-name test pattern (tests/e2e/scripts/e2e-weekly-digest.test.js lines 145-165):
  Build obj missing one SUMMARY_KEYS member; expect validateSummaryKeys to throw with key name in message.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: INT-FIX-01 — quarantine.spec.js imports QUARANTINE_REPORT_FILENAME from scripts/e2e-report-issue.mjs</name>
  <files>tests/e2e/specs/quarantine.spec.js, tests/unit/quarantine-spec-import.test.js</files>
  <read_first>
    - tests/e2e/specs/quarantine.spec.js (lines 1-70 — see the existing block comment at 50-57 documenting the sync requirement, and the local re-declare at line 58)
    - scripts/e2e-report-issue.mjs (lines 45-55 — confirm `export const QUARANTINE_REPORT_FILENAME = 'quarantine-report.json';` at line 50)
    - tests/unit/e2e-report-issue.test.js (lines 40-50 — established import pattern; line 43 imports QUARANTINE_REPORT_FILENAME from the script)
    - .planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-RESEARCH.md (§"Pattern 1: Spec-source grep regression test (INT-FIX-01)" lines 243-273; §"Code Examples › Spec-source grep" lines 413-425)
  </read_first>
  <behavior>
    - Test 1 (must FAIL before code change, PASS after): `quarantine.spec.js` source contains an import statement matching /import\s+\{[^}]*QUARANTINE_REPORT_FILENAME[^}]*\}\s+from\s+['"][^'"]*scripts\/e2e-report-issue\.mjs['"]/
    - Test 2 (must FAIL before code change, PASS after): `quarantine.spec.js` source does NOT contain /^const\s+QUARANTINE_REPORT_FILENAME\s*=/m (no local re-declaration)
    - Both tests read the spec file as text via node:fs (Playwright env not booted) — pattern derived from `tests/unit/e2e-report-issue.test.js:43` import + `tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js` file-as-text approach
  </behavior>
  <action>
    Step 1 (RED): Create `tests/unit/quarantine-spec-import.test.js` per the Wave-0 scaffold in `38-VALIDATION.md` line 51. Use `import { describe, it, expect } from 'vitest'` + `import fs from 'node:fs'` + `import path from 'node:path'` + `import { fileURLToPath } from 'node:url'`. Resolve SPEC_PATH relative to __dirname (test lives at tests/unit/ — spec at tests/e2e/specs/quarantine.spec.js — relative path is '../e2e/specs/quarantine.spec.js'). Read the file with fs.readFileSync(SPEC_PATH, 'utf8'). Two it() blocks: (a) the import-statement regex MUST match (Test 1 above); (b) the local-const regex MUST NOT match (Test 2). Use a top-of-file describe block labelled 'INT-FIX-01: quarantine.spec.js imports QUARANTINE_REPORT_FILENAME'. Run `npx vitest run tests/unit/quarantine-spec-import.test.js` and confirm BOTH assertions fail (because spec still has the local re-declare).
    Step 2 (GREEN): Edit `tests/e2e/specs/quarantine.spec.js`. (a) After the existing import block at the top of the file, add: `import { QUARANTINE_REPORT_FILENAME } from '../../../scripts/e2e-report-issue.mjs';` — verify the relative path resolves from tests/e2e/specs/ up to project root then down to scripts/. (b) Delete the block comment at lines 50-57 that warns about the sync requirement AND delete the line 58 local `const QUARANTINE_REPORT_FILENAME = 'quarantine-report.json';` declaration. Preserve line 59 `const REPORT_PATH = reportPathFor(RUN_ID, QUARANTINE_REPORT_FILENAME);` (now resolves to the import). Also delete the matching block comment at `scripts/e2e-report-issue.mjs` lines 48-49 ("Must stay in sync with QUARANTINE_REPORT_FILENAME in tests/e2e/specs/quarantine.spec.js.") since the dual-source class no longer exists. Re-run `npx vitest run tests/unit/quarantine-spec-import.test.js` and confirm BOTH assertions pass.
    Step 3 (COMMIT): Stage exactly the 3 modified files (the spec, the script's comment removal, the new test). Commit with message: `fix(38-01): INT-FIX-01 — quarantine.spec.js imports QUARANTINE_REPORT_FILENAME (closes silent-zero-filings risk)` — match Phase 37 CR-01 commit pattern (`4cac665`). Single atomic commit.
  </action>
  <acceptance_criteria>
    - `grep -E "import\s+\{[^}]*QUARANTINE_REPORT_FILENAME[^}]*\}\s+from\s+['\"][^'\"]*scripts/e2e-report-issue\.mjs['\"]" tests/e2e/specs/quarantine.spec.js` exits 0
    - `grep -E "^const\s+QUARANTINE_REPORT_FILENAME\s*=" tests/e2e/specs/quarantine.spec.js` exits 1 (no local re-declaration)
    - `npx vitest run tests/unit/quarantine-spec-import.test.js --reporter=dot` exits 0 (both it() blocks pass)
    - Playwright still resolves: `node -e "import('./tests/e2e/specs/quarantine.spec.js').then(() => console.log('ok'))"` does not error on the import statement (smoke-imports only; full Playwright run not required at task level)
    - Git log shows exactly one new commit with subject matching `^fix\(38-01\): INT-FIX-01 —`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/quarantine-spec-import.test.js --reporter=dot</automated>
  </verify>
  <done>quarantine.spec.js imports QUARANTINE_REPORT_FILENAME from scripts/e2e-report-issue.mjs; no local re-declare; regression test in tests/unit/ pins the contract; single atomic commit landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: INT-FIX-02 — weekly-digest.mjs validates SUMMARY_KEYS against real aggregated data via aggregateBySummaryKey helper</name>
  <files>scripts/weekly-digest.mjs, tests/e2e/scripts/e2e-weekly-digest.test.js</files>
  <read_first>
    - scripts/weekly-digest.mjs (lines 1-100 — imports + validateSummaryKeys at 78-87 + aggregate at 98-140; AND lines 350-380 — the self-referential summaryTally construction at 359-360 and its surroundings)
    - tests/e2e/lib/llm-report.js (lines 120-160 — SUMMARY_KEYS frozen array AND classificationToSummaryKey switch at 142-152 — DO NOT modify llm-report.js; the new helper re-implements the mapping locally in weekly-digest.mjs)
    - tests/e2e/scripts/e2e-weekly-digest.test.js (lines 145-170 — existing throw-naming test pattern; the new test extends this describe with one synthetic-drift case against the aggregator output)
    - .planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-RESEARCH.md (§"Pattern 3: Synthetic-drift throw assertion (INT-FIX-02)" lines 298-323; §"Pitfall 2: aggregateBySummaryKey must map ERROR_CLASSES → SUMMARY_KEYS correctly" lines 372-376; §"Open Questions §1" lines 495-498 — Option A is locked)
    - .planning/v3.1-MILESTONE-AUDIT.md (gaps.integration entry for DIGEST-04 lines 19-21)
  </read_first>
  <behavior>
    - Test 1 (must FAIL before code change, PASS after): calling the new `aggregateBySummaryKey({ nightlyIssues, quarantineIssues, monthlyTotalCostUsd })` against label-bearing issue fixtures returns an object containing ALL seven SUMMARY_KEYS as own properties with finite numeric values
    - Test 2 (must FAIL before code change, PASS after): synthetically deleting `harness_error` (or any single key) from an `aggregateBySummaryKey` result and feeding it to `validateSummaryKeys` throws an Error whose message contains the missing key name
    - Test 3 (must FAIL before code change, PASS after): when `runDigest` (or the equivalent end-to-end path) is exercised against the fixture in dry-run mode AND a synthetic test patches `aggregateBySummaryKey` to omit one key, the call throws an Error containing that key name — proving the runtime drift guard is wired to real aggregated data, not a SUMMARY_KEYS-seeded tally
    - Existing tests at e2e-weekly-digest.test.js:145-170 must still pass (validateSummaryKeys unchanged)
  </behavior>
  <action>
    Step 1 (RED): Edit `tests/e2e/scripts/e2e-weekly-digest.test.js`. In the `describe('missing SUMMARY_KEY throws naming the key', ...)` block (anchor at the existing describe near line 145), append a new nested describe block: `describe('INT-FIX-02: validateSummaryKeys throws against real aggregated data', ...)`. Inside add three it() blocks: (i) `it('aggregateBySummaryKey returns object with all SUMMARY_KEYS', ...)` — load existing fixture (use the same splitFixture pattern already in the file), call `aggregateBySummaryKey({ nightlyIssues, quarantineIssues, monthlyTotalCostUsd: 12.34 })`, assert `expect(Object.keys(result).sort()).toEqual([...SUMMARY_KEYS].sort())`. (ii) `it('aggregateBySummaryKey result missing a key causes validateSummaryKeys to throw naming that key', ...)` — call the helper, delete 'harness_error', wrap `validateSummaryKeys(driftedTally)` in try/catch, assert caught.message contains 'harness_error'. (iii) `it('runDigest validates real aggregated data (not SUMMARY_KEYS-seeded tally)', ...)` — read the post-fix runDigest source via fs.readFileSync and grep: `expect(src).toMatch(/validateSummaryKeys\s*\(\s*aggregateBySummaryKey\s*\(/)`. Import the new symbol: extend the top-of-file import from `'../../../scripts/weekly-digest.mjs'` to include `aggregateBySummaryKey`. Run `npx vitest run tests/e2e/scripts/e2e-weekly-digest.test.js --reporter=dot` and confirm ALL THREE new it() blocks fail (helper not yet exported; runDigest still uses the seeded tally).
    Step 2 (GREEN): Edit `scripts/weekly-digest.mjs`. (a) Add a new exported function `aggregateBySummaryKey({ nightlyIssues, quarantineIssues, monthlyTotalCostUsd })` near the `aggregate` function (after line 140). Body: instantiate `const tally = Object.fromEntries(SUMMARY_KEYS.map((k) => [k, 0]));`. Walk the merged-deduped issues using the same dedup-by-issue.number pattern as `aggregate` (lines 100-105). For each issue, find the FIRST label whose name is in ERROR_CLASS_SET (line 33 import) — this is the closed-enum classification. Map ERROR_CLASS → SUMMARY_KEY using an inline switch mirroring `tests/e2e/lib/llm-report.js:142-152` classificationToSummaryKey (PASS → passed; WRONG_CITATION → wrong_citation; VERIFIER_DISAGREE → verifier_disagree; LLM_HALLUCINATED_SELECTION → llm_hallucinated_selection; LLM_API_ERROR → llm_api_error; HARNESS_ERROR → harness_error; everything else → no increment). Increment the mapped key. After the loop, set `tally.total_cost_usd = +(monthlyTotalCostUsd || 0).toFixed(6)` (handles Pitfall 2 — cost is metric data, not classification data). Return tally. (b) At the runDigest call site lines 355-360, REPLACE the self-referential block: delete lines 356-360 (the comment + `const summaryTally = Object.fromEntries(...)` + `validateSummaryKeys(summaryTally)`). Insert just AFTER the aggregate() call at line 370: `const summaryByKey = aggregateBySummaryKey({ nightlyIssues, quarantineIssues, monthlyTotalCostUsd: monthlyTotal });` — `monthlyTotal` is already in scope at this site from the existing `monthlyTotal(readLedger(...))` flow (verify by reading the runDigest body 340-380). Then add: `validateSummaryKeys(summaryByKey);` immediately after. This restores the intended runtime drift detection — if a future llm-report.js rename adds/removes a SUMMARY_KEYS entry, the validator throws with the offending key name. Re-run `npx vitest run tests/e2e/scripts/e2e-weekly-digest.test.js --reporter=dot` and confirm all three new it() blocks pass AND the existing throw-name tests (lines 145-165) still pass.
    Step 3 (COMMIT): Stage exactly `scripts/weekly-digest.mjs` + `tests/e2e/scripts/e2e-weekly-digest.test.js`. Commit message: `fix(38-01): INT-FIX-02 — validateSummaryKeys guards real aggregated data (repairs DIGEST-04 self-referential check)`. Single atomic commit, matching Phase 37 CR-02 pattern.
  </action>
  <acceptance_criteria>
    - `aggregateBySummaryKey` is exported from `scripts/weekly-digest.mjs`: `grep -E "^export\s+function\s+aggregateBySummaryKey" scripts/weekly-digest.mjs` exits 0
    - `runDigest` calls `validateSummaryKeys(aggregateBySummaryKey(...))` (not on a SUMMARY_KEYS-seeded tally): `grep -E "validateSummaryKeys\s*\(\s*(summaryByKey|aggregateBySummaryKey\()" scripts/weekly-digest.mjs` exits 0
    - The self-referential `summaryTally = Object.fromEntries(SUMMARY_KEYS.map` line is gone from runDigest's body: `grep -v '^#' scripts/weekly-digest.mjs | grep -c "summaryTally = Object.fromEntries(SUMMARY_KEYS.map"` returns 0
    - `npx vitest run tests/e2e/scripts/e2e-weekly-digest.test.js --reporter=dot` exits 0 (new + existing tests pass)
    - Existing single test `npx vitest run tests/e2e/scripts/e2e-weekly-digest.test.js -t "names the missing key"` still passes (regression of existing line 156-164 case)
    - Git log shows exactly one new commit with subject matching `^fix\(38-01\): INT-FIX-02 —`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/e2e/scripts/e2e-weekly-digest.test.js --reporter=dot</automated>
  </verify>
  <done>aggregateBySummaryKey exported; runDigest validates real aggregated tally; DIGEST-04 drift guard now non-inert; regression test pins the contract; single atomic commit landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: INT-FIX-03 — e2e-nightly.yml upload-artifact step gates on quarantine failure (with id: upload-artifacts for grep-test stability)</name>
  <files>.github/workflows/e2e-nightly.yml, tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js</files>
  <read_first>
    - .github/workflows/e2e-nightly.yml (lines 295-320 — the `Upload E2E artifacts` step at line 303-313 with the existing `if: always() && (steps.smoke.outcome == 'failure' || steps.regression.outcome == 'failure' || steps.fault_injection.outcome == 'failure')`)
    - tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js (lines 1-100 — header comment listing Y1..Y5; existing Y2 step-window pattern at line 50-72 is the verbatim template for Y6)
    - .planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-RESEARCH.md (§"Pattern 2: `if:` condition grep with step-window scoping (INT-FIX-03)" lines 275-296; §"Pitfall 3: `if: always() && (...)` clause grouping" lines 378-382; §"Anti-Patterns" line 348 — adding `id: upload-artifacts` is a Claude-discretion improvement; §"Code Examples › Step-window YAML scope" lines 441-452)
  </read_first>
  <behavior>
    - Test 1 (must FAIL before code change, PASS after): `e2e-nightly.yml` contains a step with `id: upload-artifacts` adjacent to the `- name: Upload E2E artifacts` line
    - Test 2 (must FAIL before code change, PASS after): the step window for `id: upload-artifacts` contains the literal string `steps.quarantine.outcome == 'failure'`
    - Test 3 (must FAIL before code change, PASS after): the quarantine clause appears INSIDE the closing `)` of the parenthesised `always() && (...)` group (NOT after a `) || steps.quarantine...` — see Pitfall 3) — assert via regex matching `/&&\s*\([^)]*steps\.quarantine\.outcome == 'failure'[^)]*\)/`
    - Existing Y1..Y5 tests must still pass (no regression to other contract assertions)
  </behavior>
  <action>
    Step 1 (RED): Edit `tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js`. After the existing Y5 `it()` block, append a new `it('Y6 — INT-FIX-03: Upload E2E artifacts step gates on quarantine failure', () => { ... })` block. Body: (a) anchor by `const startIdx = yaml.indexOf('id: upload-artifacts');` — assert `expect(startIdx).toBeGreaterThan(-1)` (this provides Test 1 above). (b) Bound the window using the verbatim Y2 pattern (lines 50-72 of the same file): slice from startIdx, find the next `- name:` boundary, define stepBlock. (c) Assert `expect(stepBlock).toContain("steps.quarantine.outcome == 'failure'")` (Test 2). (d) Assert the parens-grouping: `expect(stepBlock).toMatch(/if:\s*always\(\)\s*&&\s*\([^)]*steps\.quarantine\.outcome == 'failure'[^)]*\)/)` (Test 3 — guards against the Pitfall 3 outside-parens regression). Update the header comment in the file (top docblock listing Y1..Y5) to include Y6's description. Run `npx vitest run tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js --reporter=dot` and confirm Y6 fails (Y1..Y5 still pass).
    Step 2 (GREEN): Edit `.github/workflows/e2e-nightly.yml` at the `Upload E2E artifacts` step (around line 303). (a) Add `id: upload-artifacts` as a new line directly below `- name: Upload E2E artifacts` (matches surrounding step convention — see `id: quarantine`, `id: regression`, `id: smoke` already in this file). (b) Modify the `if:` line to insert ` || steps.quarantine.outcome == 'failure'` INSIDE the closing paren of the existing parenthesised group. Concretely: change the existing `if: always() && (steps.smoke.outcome == 'failure' || steps.regression.outcome == 'failure' || steps.fault_injection.outcome == 'failure')` to `if: always() && (steps.smoke.outcome == 'failure' || steps.regression.outcome == 'failure' || steps.fault_injection.outcome == 'failure' || steps.quarantine.outcome == 'failure')`. The new clause sits BEFORE the closing `)` — per RESEARCH Pitfall 3 — preserving the always() short-circuit and operator precedence. Do not touch the rest of the step (`uses:`, `with:`, the trailing comment block remain intact). Re-run `npx vitest run tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js --reporter=dot` and confirm Y6 passes AND Y1..Y5 still pass.
    Step 3 (COMMIT): Stage `.github/workflows/e2e-nightly.yml` + `tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js`. Commit message: `fix(38-01): INT-FIX-03 — Upload E2E artifacts step gates on quarantine failure (id: upload-artifacts + inside-parens clause)`. Single atomic commit.
  </action>
  <acceptance_criteria>
    - `grep -E "^\s+id: upload-artifacts" .github/workflows/e2e-nightly.yml` exits 0
    - `grep -E "if:\s*always\(\)\s*&&\s*\([^)]*steps\.quarantine\.outcome == 'failure'[^)]*\)" .github/workflows/e2e-nightly.yml` exits 0 (clause INSIDE parens — Pitfall 3 guard)
    - `npx vitest run tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js --reporter=dot` exits 0 (Y1..Y6 pass)
    - actionlint (if available on PATH) reports no new errors: `which actionlint && actionlint .github/workflows/e2e-nightly.yml || echo "actionlint not installed — skipping (vitest grep is authoritative per RESEARCH)"`
    - Git log shows exactly one new commit with subject matching `^fix\(38-01\): INT-FIX-03 —`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js --reporter=dot</automated>
  </verify>
  <done>Upload E2E artifacts step has id: upload-artifacts and gates on all 4 step outcomes (including quarantine); regression test Y6 pins the contract; single atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 4: Update .planning/v3.1-MILESTONE-AUDIT.md — clear integration warnings + update cross-cutting tech_debt</name>
  <files>.planning/v3.1-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/v3.1-MILESTONE-AUDIT.md (full file — lines 1-173; focus on YAML frontmatter lines 1-68, specifically `scores.integration` line 8, `gaps.integration:` block lines 12-24, and `tech_debt: - phase: cross-cutting` block lines 63-66; also markdown sections "Cross-Phase Integration" lines 109-131 and "Why tech_debt" lines 168-172)
    - SUMMARY of the three fixes just landed (git log --oneline -3 to confirm the three commits exist before editing the audit)
  </read_first>
  <acceptance_criteria>
    - Audit frontmatter `scores.integration` updated from `27/29` to `29/29` (or, if planner prefers, `29/29 — fragility warnings resolved 2026-05-29 in Phase 38`)
    - Audit frontmatter `gaps.integration:` block is now an empty list `[]` (the three seam entries removed)
    - Audit frontmatter `tech_debt: - phase: cross-cutting` items list: the "3 integration fragility warnings (see gaps.integration above)" entry is removed; the "5 of 6 phases carry draft VALIDATION.md" entry is UNCHANGED (Plan 38-02 owns it)
    - Audit markdown section "Integration warnings (fragility — not breaks)" (lines 126-130) — the three numbered items are converted to ✅ struck-through or annotated with `RESOLVED in Phase 38 commit <SHA>` references (one line per fix); the heading is updated to "Integration warnings (fragility — not breaks) — RESOLVED in Phase 38"
    - Audit markdown section "Cross-Phase Integration" scoreline (line 113) updated from `14/14 seams WIRED · 0 broken · 0 orphaned exports · 0 missing connections` (no change — already accurate) but the partial-warning counts in the "Scores" table (line 85) update: `Integration | 29/29 | All cross-phase seams fully wired; 3 fragility warnings resolved in Phase 38`
    - The "human_verification:" block is NOT modified (Plan 38-03 owns it)
    - The "nyquist:" block is NOT modified (Plan 38-02 owns it)
    - Audit `audited:` timestamp at line 2 updated to the ISO timestamp of this task's completion
    - File still parses as valid YAML+markdown (one frontmatter block, then markdown body)
  </acceptance_criteria>
  <action>
    Step 1: Run `git log --oneline -3 -- scripts/weekly-digest.mjs .github/workflows/e2e-nightly.yml tests/e2e/specs/quarantine.spec.js` and capture the three commit SHAs from Tasks 1-3 (INT-FIX-01, INT-FIX-02, INT-FIX-03). Note them for use in step 2.
    Step 2: Edit `.planning/v3.1-MILESTONE-AUDIT.md`. (a) Update the `audited:` timestamp at line 2 to the current ISO timestamp (e.g., `2026-05-29T<HH:MM:SS>Z`). (b) Change `scores.integration` (line 8) from `27/29` to `29/29` and update the trailing comment to `# all seams fully wired; 3 fragility warnings resolved in Phase 38`. (c) In `gaps.integration:` (lines 12-24), replace the three entry list with an empty list: `  integration: []`. (d) In `tech_debt:` under `- phase: cross-cutting` (around lines 63-66), remove the bullet `"3 integration fragility warnings (see gaps.integration above)"` — KEEP the bullet `"5 of 6 phases carry draft VALIDATION.md ..."` untouched (Plan 38-02 will close it). (e) In the markdown body, update the "Scores" table row for Integration (line 85) to `| Integration | 29/29 | All cross-phase seams fully wired; 3 fragility warnings resolved in Phase 38 |`. (f) Edit the "### Integration warnings (fragility — not breaks)" section (line 126): rename the heading to `### Integration warnings (fragility — not breaks) — RESOLVED in Phase 38`; for each of the three numbered items (1, 2, 3), prepend `✅ RESOLVED (commit <SHA-from-step-1>)` to the item text. (g) Update the "Why tech_debt" section (line 168) to remove the "3 integration fragility warnings" from the list of accumulated deferred work — keep the remaining items intact (5 Nyquist-partial phases, 7 outstanding live confirmations, Phase 37 WR-01..06 + IN-01..04 deferrals).
    Step 3: Verify YAML parses: `node -e "const m=require('fs').readFileSync('.planning/v3.1-MILESTONE-AUDIT.md','utf8').split('---\n'); console.log('blocks:', m.length); console.log('frontmatter-len:', m[1].length)"` — expect blocks=3 (empty + frontmatter + body).
    Step 4: Commit: stage `.planning/v3.1-MILESTONE-AUDIT.md` only. Commit message: `docs(38-01): close gaps.integration in v3.1 audit — all 3 fragility warnings RESOLVED`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('.planning/v3.1-MILESTONE-AUDIT.md','utf8'); const fm=t.split('---\n')[1]; if(!fm.includes('integration: 29/29')) throw new Error('scores.integration not updated'); if(!/integration:\s*\[\]/.test(fm)) throw new Error('gaps.integration not emptied'); if(fm.includes('3 integration fragility warnings (see gaps.integration above)')) throw new Error('cross-cutting tech_debt still mentions fragility warnings'); console.log('AUDIT YAML coherent');"</automated>
  </verify>
  <done>v3.1 audit YAML reflects post-fix state: scores.integration 29/29, gaps.integration empty, cross-cutting tech_debt 3-fragility-warnings entry removed, markdown body annotates each fix with its commit SHA. Other YAML blocks (nyquist, human_verification) untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GH Actions workflow execution | YAML changes execute under GitHub-Actions-provided runner; the `if:` expression is evaluated by GH, not the repo |
| Test/spec consumer ↔ script export | quarantine.spec.js (test runtime) consumes a constant exported by scripts/e2e-report-issue.mjs (CI runtime); rename safety depends on a single source of truth |
| Weekly-digest runtime ↔ llm-report schema | scripts/weekly-digest.mjs (CI runtime) consumes SUMMARY_KEYS shape from tests/e2e/lib/llm-report.js (import-time) and must catch runtime drift against ACTUAL aggregated data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01-01 | Tampering | quarantine.spec.js QUARANTINE_REPORT_FILENAME local re-declare | mitigate | INT-FIX-01 imports the exported constant — eliminates the dual-source class entirely so any rename in scripts/e2e-report-issue.mjs propagates as an ESM-import error, not a silent zero-filings symptom. Regression test pins the contract. |
| T-38-01-02 | Tampering / Information Disclosure | weekly-digest.mjs SUMMARY_KEYS self-referential validation | mitigate | INT-FIX-02 replaces the SUMMARY_KEYS-seeded tally with aggregateBySummaryKey applied to REAL issue data. Drift now surfaces as descriptive throw with key name (not silent zero metric). Regression test verifies the throw on synthetic drift. |
| T-38-01-03 | Information Disclosure (via absence) | e2e-nightly.yml upload-artifact step skipping quarantine outcome | mitigate | INT-FIX-03 adds `steps.quarantine.outcome == 'failure'` to the parenthesised `always() && (...)` group. Pitfall 3 (clause grouping precedence) addressed by inserting clause INSIDE the parens. Regression test Y6 grep-asserts the parenthesised position. |
| T-38-01-04 | Elevation of Privilege | new id: upload-artifacts on existing step | accept | Adding an `id:` attribute does not change permissions, secrets exposure, or runtime privileges — it's a YAML label for downstream `steps.<id>.outcome` references. The step's `uses:`, `with:`, and `if:` (other than the new quarantine clause) are unchanged. No new attack surface. |
| T-38-01-SC | Tampering | npm/pip/cargo installs | accept | Phase 38 installs ZERO new packages per STATE.md v3.1 Pre-locked Decisions and 38-RESEARCH.md §"Package Legitimacy Audit" lines 139-148. Audit table is empty by construction; no slopcheck or blocking checkpoint required. |
</threat_model>

<verification>
Per-plan checks after all four tasks complete:
1. All three regression tests pass: `npx vitest run tests/unit/quarantine-spec-import.test.js tests/e2e/scripts/e2e-weekly-digest.test.js tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js --reporter=dot` exits 0.
2. Full test:src suite still green (no regression to other 678 tests): `npm run test:src` exits 0.
3. Lint clean: `npm run lint` exits 0.
4. Git log shows exactly four new commits in order: INT-FIX-01, INT-FIX-02, INT-FIX-03, audit-update — `git log --oneline -4 | grep -cE "^[0-9a-f]+ (fix|docs)\(38-01\):"` returns 4.
5. Audit YAML coherence per Task 4 `<verify>` block.
</verification>

<success_criteria>
- 3 integration fragility warnings resolved with one atomic commit per fix
- 3 regression tests pin each contract; total test count grew by ≥4 (1 new file + ≥3 new it() blocks)
- `npm run test:src && npm run lint` exits 0
- `.planning/v3.1-MILESTONE-AUDIT.md` `gaps.integration` is empty list `[]`; `scores.integration` is `29/29`; cross-cutting tech_debt no longer mentions fragility warnings
- Other audit blocks (nyquist, human_verification) untouched (claimed by Plans 38-02 and 38-03 respectively)
</success_criteria>

<output>
Create `.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-01-SUMMARY.md` per the standard template, recording: the three commit SHAs, test-count delta, regression-test file paths, audit YAML block before/after for gaps.integration and scores.integration.
</output>
