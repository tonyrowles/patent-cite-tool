---
phase: 38-v3-1-cleanup-integration-warnings-nyquist-human-uat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/32-human-uat-verification/32-VALIDATION.md
  - .planning/phases/33-re-run-validator/33-VALIDATION.md
  - .planning/phases/34-hybrid-triage-classifier/34-VALIDATION.md
  - .planning/phases/35-rich-issue-filer-+-quarantine-corpus/35-VALIDATION.md
  - .planning/phases/37-weekly-analytics-digest/37-VALIDATION.md
  - .planning/v3.1-MILESTONE-AUDIT.md
autonomous: false
requirements:
  - UAT-01
  - UAT-02
  - UAT-03
  - RERUN-01
  - RERUN-02
  - RERUN-03
  - RERUN-04
  - TRIAGE-01
  - TRIAGE-02
  - TRIAGE-03
  - TRIAGE-04
  - TRIAGE-05
  - TRIAGE-06
  - ISSUE-01
  - ISSUE-02
  - ISSUE-03
  - ISSUE-04
  - QUAR-01
  - QUAR-02
  - QUAR-05
  - DIGEST-01
  - DIGEST-02
  - DIGEST-03
  - DIGEST-04
user_setup: []
tags:
  - v3.1
  - nyquist
  - validation
  - retroactive
must_haves:
  truths:
    - "Each of the 5 partial-Nyquist phases (32, 33, 34, 35, 37) has had `Skill(gsd-validate-phase, <N>)` invoked against its draft VALIDATION.md"
    - "Every phase whose auditor returns GAPS FILLED is stamped `nyquist_compliant: true` in its VALIDATION.md frontmatter"
    - "Any phase whose auditor returns PARTIAL or ESCALATE has its gap documented in this plan's SUMMARY and a new tech_debt entry surfaced (the phase is NOT auto-stamped compliant)"
    - "Manual-Only entries in each draft VALIDATION.md are NOT treated as auditor escalations (per RESEARCH Pitfall 4) — they are PRE-CLASSIFIED COVERED-MANUAL and the auditor is instructed not to generate tests for them"
    - "After all 5 stamping attempts, `.planning/v3.1-MILESTONE-AUDIT.md` `nyquist:` block reflects new compliance scores (compliant_phases / partial_phases / missing_phases / overall)"
  artifacts:
    - path: ".planning/phases/32-human-uat-verification/32-VALIDATION.md"
      provides: "Phase 32 VALIDATION.md with Nyquist audit table; frontmatter nyquist_compliant: true OR documented gap"
      contains: "nyquist_compliant:"
    - path: ".planning/phases/33-re-run-validator/33-VALIDATION.md"
      provides: "Phase 33 VALIDATION.md stamped or gap-documented"
      contains: "nyquist_compliant:"
    - path: ".planning/phases/34-hybrid-triage-classifier/34-VALIDATION.md"
      provides: "Phase 34 VALIDATION.md stamped or gap-documented"
      contains: "nyquist_compliant:"
    - path: ".planning/phases/35-rich-issue-filer-+-quarantine-corpus/35-VALIDATION.md"
      provides: "Phase 35 VALIDATION.md stamped or gap-documented"
      contains: "nyquist_compliant:"
    - path: ".planning/phases/37-weekly-analytics-digest/37-VALIDATION.md"
      provides: "Phase 37 VALIDATION.md stamped or gap-documented"
      contains: "nyquist_compliant:"
    - path: ".planning/v3.1-MILESTONE-AUDIT.md"
      provides: "Updated nyquist: block reflecting post-stamping compliance"
      contains: "nyquist:"
    - path: ".planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-02-SUMMARY.md"
      provides: "Per-phase stamping outcome table (GAPS FILLED | PARTIAL | ESCALATE) and any new tech_debt surfaced"
  key_links:
    - from: "Skill invocation"
      to: "draft *-VALIDATION.md"
      via: "gsd-validate-phase reads VALIDATION.md as State A input; gsd-nyquist-auditor sub-agent fills gaps"
      pattern: "Skill\\(gsd-validate-phase"
    - from: "Per-phase stamping outcome"
      to: ".planning/v3.1-MILESTONE-AUDIT.md nyquist: block"
      via: "explicit YAML edit to compliant_phases / partial_phases / missing_phases / overall after all 5 invocations complete"
      pattern: "compliant_phases:"
---

<objective>
Drive the 5 phases carrying draft `*-VALIDATION.md` (`nyquist_compliant: false`) — phases 32, 33, 34, 35, 37 — to formal Nyquist coverage by invoking `Skill(gsd-validate-phase, "<N>")` once per phase. After all 5 invocations complete, rewrite the `nyquist:` block in `.planning/v3.1-MILESTONE-AUDIT.md` to reflect the new compliance state.

Purpose: Close the v3.1 formal-coverage gap surfaced by the milestone audit. Test suites are strong (678 vitest tests); this plan stamps existing draft VALIDATION.md files to the standard already practiced by Phase 36 (the one COMPLIANT phase that serves as the template).

Output: 5 updated `*-VALIDATION.md` files + 1 updated audit YAML block + 1 SUMMARY.md tabulating per-phase outcomes. If any phase cannot be stamped compliant (auditor returns PARTIAL/ESCALATE on a real gap, NOT on Manual-Only entries), the gap is documented in the SUMMARY and surfaced as new tech_debt — Phase 38 does not block on it (cleanup, not gap-closure).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/workflows/validate-phase.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/skills/gsd-validate-phase/SKILL.md
@$HOME/.claude/agents/gsd-nyquist-auditor.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v3.1-MILESTONE-AUDIT.md
@.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-CONTEXT.md
@.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-RESEARCH.md
@.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-VALIDATION.md
@.planning/phases/36-quarantine-ci-integration-+-pipeline-orchestrator/36-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Skill invocation contract + draft VALIDATION.md state -->

Skill invocation (canonical form):
  Skill(skill="gsd-validate-phase", args="<phase-number>")

Skill State A semantics (per ~/.claude/get-shit-done/workflows/validate-phase.md lines 38-45):
  - If VALIDATION.md exists with `status: draft` or `status: ready`, the skill enters State A (audit + fill gaps)
  - It does NOT overwrite the file's existing per-task verification table or manual-only entries
  - It spawns `gsd-nyquist-auditor` sub-agent which returns one of:
      * GAPS FILLED — all auditable rows have automated commands; stamps `nyquist_compliant: true`
      * PARTIAL — some rows have no command and no manual-only justification; surfaces to user
      * ESCALATE — irrecoverable (e.g., test framework broken, conflicting decisions); surfaces to user

Per-phase draft VALIDATION.md locations (all currently `nyquist_compliant: false`):
  .planning/phases/32-human-uat-verification/32-VALIDATION.md
  .planning/phases/33-re-run-validator/33-VALIDATION.md
  .planning/phases/34-hybrid-triage-classifier/34-VALIDATION.md
  .planning/phases/35-rich-issue-filer-+-quarantine-corpus/35-VALIDATION.md
  .planning/phases/37-weekly-analytics-digest/37-VALIDATION.md

Phase 36 template (the one COMPLIANT example):
  .planning/phases/36-quarantine-ci-integration-+-pipeline-orchestrator/36-VALIDATION.md
  Frontmatter sets `nyquist_compliant: true` at draft time; per-task verification map has automated commands for every QUAR-/ORCH- row; manual-only rows are explicitly tabled.

CLAUDE.md C1/C2/C3 enforcement:
  If the auditor's interactive gate (AskUserQuestion) returns an empty/generic result, the task MUST fall back to a plain-text numbered list and wait for explicit user response. Do NOT auto-pick the (Recommended) option.

Audit YAML target block (in .planning/v3.1-MILESTONE-AUDIT.md frontmatter):
  nyquist:
    compliant_phases: [...]
    partial_phases: [...]
    missing_phases: [...]
    overall: <partial|complete|...>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — verify all 5 draft VALIDATION.md files exist and read each header</name>
  <files>(read-only)</files>
  <read_first>
    - .planning/phases/32-human-uat-verification/32-VALIDATION.md (header + Per-Task Verification Map + Manual-Only section)
    - .planning/phases/33-re-run-validator/33-VALIDATION.md (header + Per-Task Verification Map + Manual-Only section)
    - .planning/phases/34-hybrid-triage-classifier/34-VALIDATION.md (header + Per-Task Verification Map + Manual-Only section)
    - .planning/phases/35-rich-issue-filer-+-quarantine-corpus/35-VALIDATION.md (header + Per-Task Verification Map + Manual-Only section)
    - .planning/phases/37-weekly-analytics-digest/37-VALIDATION.md (header + Per-Task Verification Map + Manual-Only section)
    - .planning/phases/36-quarantine-ci-integration-+-pipeline-orchestrator/36-VALIDATION.md (full file — the COMPLIANT template)
    - .planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-RESEARCH.md (§"Pitfall 4: Nyquist auditor escalates a real gap" lines 384-388)
  </read_first>
  <acceptance_criteria>
    - All 5 draft VALIDATION.md paths exist: `for d in 32-human-uat-verification 33-re-run-validator 34-hybrid-triage-classifier 35-rich-issue-filer-+-quarantine-corpus 37-weekly-analytics-digest; do ls .planning/phases/$d/*-VALIDATION.md || echo MISSING:$d; done` reports zero MISSING lines
    - Each file's frontmatter `nyquist_compliant` field is `false` (none already stamped from a previous run): `grep -E "^nyquist_compliant:\s*false" .planning/phases/{32-*,33-*,34-*,35-*,37-*}/*-VALIDATION.md | wc -l` returns 5
    - For each file, the Manual-Only Verifications section is identified (so per Pitfall 4 the auditor knows not to treat those rows as gaps)
    - A pre-flight notes file is captured in the executor's working memory listing, per phase: file path, `status:` field, number of rows in Per-Task Verification Map, number of Manual-Only entries
  </acceptance_criteria>
  <action>
    Execute pre-flight inspection. For each of the 5 phases (32, 33, 34, 35, 37): (a) `ls .planning/phases/<phase-slug>/*-VALIDATION.md` to confirm presence — note the actual filename (likely `<N>-VALIDATION.md`). (b) Read the frontmatter (top YAML block) — capture `phase`, `slug`, `status`, `nyquist_compliant`, `wave_0_complete`. (c) Count the rows of the `## Per-Task Verification Map` table (rows after the header separator). (d) Count the rows of the `## Manual-Only Verifications` table. Record the inspection in the executor's notes (not a separate committed file — this is a planning pass) so each subsequent Skill invocation knows the starting state. Per RESEARCH Pitfall 4: Phase 32's 3 UAT items (UAT-01, UAT-02, UAT-03) are in Manual-Only — the auditor MUST NOT escalate these; they are PRE-CLASSIFIED COVERED-MANUAL. Note this for Task 2.
  </action>
  <verify>
    <automated>bash -c 'count=$(grep -lE "^nyquist_compliant:\s*false" .planning/phases/{32-*,33-*,34-*,35-*,37-*}/*-VALIDATION.md 2>/dev/null | wc -l); [ "$count" -eq 5 ] || { echo "expected 5 partial-Nyquist files, found $count"; exit 1; }; echo "5 partial-Nyquist VALIDATION.md files confirmed"'</automated>
  </verify>
  <done>All 5 draft files confirmed present and at `nyquist_compliant: false`; Manual-Only sections identified per phase to inform auditor expectation per Pitfall 4.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Invoke Skill(gsd-validate-phase, "32") — Phase 32 (HUMAN-UAT Verification)</name>
  <files>.planning/phases/32-human-uat-verification/32-VALIDATION.md</files>
  <read_first>
    - .planning/phases/32-human-uat-verification/32-VALIDATION.md (full file — the starting state)
    - $HOME/.claude/skills/gsd-validate-phase/SKILL.md (skill contract)
    - $HOME/.claude/get-shit-done/workflows/validate-phase.md (workflow steps)
    - $HOME/.claude/agents/gsd-nyquist-auditor.md (auditor return contract)
  </read_first>
  <what-built>The 32-VALIDATION.md file currently in draft state (`nyquist_compliant: false`); the skill audits its per-task map + manual-only section and either stamps `nyquist_compliant: true` (GAPS FILLED) or surfaces a gap (PARTIAL / ESCALATE).</what-built>
  <action>
    Step 1: Invoke the skill — `Skill(skill="gsd-validate-phase", args="32")`. The skill reads 32-VALIDATION.md, spawns gsd-nyquist-auditor sub-agent, returns one of: (a) GAPS FILLED — frontmatter updated to `nyquist_compliant: true`, Validation Audit table appended; (b) PARTIAL / ESCALATE — surfaces a question OR gap report.
    Step 2: Per RESEARCH Pitfall 4 — if the auditor flags UAT-01/UAT-02/UAT-03 (the 3 Manual-Only rows) as escalations, correct in-loop: "These are COVERED-MANUAL per the Manual-Only Verifications section; do not generate tests."
    Step 3: Per CLAUDE.md C1/C2/C3 — if the skill emits an AskUserQuestion call and the tool result returns empty/generic text (does not name the user's choice), fall back to a plain-text numbered list of options and wait for an explicit response. Do NOT auto-select "(Recommended)" or fabricate an answer.
    Step 4: After skill returns, record outcome in executor's running notes for Task 7 (audit YAML update): one of {GAPS_FILLED | PARTIAL | ESCALATE | MANUAL_ONLY_CLASSIFIED}, plus a one-line gap description if not GAPS_FILLED.
    Step 5: Pause for user "approved" confirmation before proceeding to Task 3 — this is a blocking checkpoint.
  </action>
  <how-to-verify>
    1. After skill returns: `grep -E "^nyquist_compliant:" .planning/phases/32-human-uat-verification/32-VALIDATION.md` returns `true` (stamped) or `false` (gap-documented below in file body).
    2. Executor's notes record outcome state for Task 7.
    3. If AskUserQuestion fired, the user's explicit choice (not "(Recommended)") was captured.
  </how-to-verify>
  <verify>
    <automated>bash -c 'val=$(grep -E "^nyquist_compliant:" .planning/phases/32-human-uat-verification/32-VALIDATION.md | head -1); echo "Phase 32 post-skill: $val"; echo "$val" | grep -qE "nyquist_compliant:\s*(true|false)" || { echo "frontmatter malformed"; exit 1; }'</automated>
  </verify>
  <resume-signal>Type "approved" once Skill(gsd-validate-phase, "32") has returned AND the outcome (GAPS_FILLED / PARTIAL / ESCALATE) has been recorded for use in Task 7. If the auditor prompts via AskUserQuestion and the result is empty, the fallback numbered-list response must be received before proceeding.</resume-signal>
  <done>Skill invocation for Phase 32 complete; 32-VALIDATION.md frontmatter reflects outcome (true if stamped, false with documented gap if PARTIAL/ESCALATE); per-phase outcome recorded in running notes for Task 7.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Invoke Skill(gsd-validate-phase, "33") — Phase 33 (Re-run Validator)</name>
  <files>.planning/phases/33-re-run-validator/33-VALIDATION.md</files>
  <read_first>
    - .planning/phases/33-re-run-validator/33-VALIDATION.md
    - $HOME/.claude/skills/gsd-validate-phase/SKILL.md
  </read_first>
  <what-built>33-VALIDATION.md draft (nyquist_compliant: false) audited via the skill; per-task verification map already enumerates 32 tests for RERUN-01..04 (per audit), so the auditor's gap analysis should be small.</what-built>
  <action>
    Step 1: Invoke `Skill(skill="gsd-validate-phase", args="33")`.
    Step 2: Handle AskUserQuestion fallback per CLAUDE.md C1/C2/C3 — plain-text numbered list if tool result is empty/generic.
    Step 3: Verify outcome: `grep -E "^nyquist_compliant:" .planning/phases/33-re-run-validator/33-VALIDATION.md` returns `true` (expected — Phase 33 has 32 vitest tests across RERUN-01..04).
    Step 4: Record outcome for Task 7.
    Step 5: Pause for user "approved" confirmation.
  </action>
  <how-to-verify>
    1. Frontmatter `nyquist_compliant` value captured (true or false-with-documented-gap).
    2. Outcome state stored in running notes.
  </how-to-verify>
  <verify>
    <automated>bash -c 'val=$(grep -E "^nyquist_compliant:" .planning/phases/33-re-run-validator/33-VALIDATION.md | head -1); echo "Phase 33 post-skill: $val"; echo "$val" | grep -qE "nyquist_compliant:\s*(true|false)" || { echo "frontmatter malformed"; exit 1; }'</automated>
  </verify>
  <resume-signal>Type "approved" once Skill(gsd-validate-phase, "33") has returned AND outcome recorded.</resume-signal>
  <done>Skill invocation for Phase 33 complete; outcome recorded for Task 7.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Invoke Skill(gsd-validate-phase, "34") — Phase 34 (Hybrid Triage Classifier)</name>
  <files>.planning/phases/34-hybrid-triage-classifier/34-VALIDATION.md</files>
  <read_first>
    - .planning/phases/34-hybrid-triage-classifier/34-VALIDATION.md
    - $HOME/.claude/skills/gsd-validate-phase/SKILL.md
  </read_first>
  <what-built>34-VALIDATION.md draft (nyquist_compliant: false) audited; per audit Phase 34 has 6/6 TRIAGE requirements satisfied via vitest including CI guard test and cluster pre-filter spy test.</what-built>
  <action>
    Step 1: Invoke `Skill(skill="gsd-validate-phase", args="34")`.
    Step 2: Handle AskUserQuestion fallback per CLAUDE.md C1/C2/C3.
    Step 3: Verify outcome: `grep -E "^nyquist_compliant:" .planning/phases/34-hybrid-triage-classifier/34-VALIDATION.md` returns `true` (expected).
    Step 4: Record outcome for Task 7.
    Step 5: Pause for user "approved" confirmation.
  </action>
  <how-to-verify>
    1. Frontmatter `nyquist_compliant` captured.
    2. Outcome stored.
  </how-to-verify>
  <verify>
    <automated>bash -c 'val=$(grep -E "^nyquist_compliant:" .planning/phases/34-hybrid-triage-classifier/34-VALIDATION.md | head -1); echo "Phase 34 post-skill: $val"; echo "$val" | grep -qE "nyquist_compliant:\s*(true|false)" || { echo "frontmatter malformed"; exit 1; }'</automated>
  </verify>
  <resume-signal>Type "approved" once Skill(gsd-validate-phase, "34") has returned AND outcome recorded.</resume-signal>
  <done>Skill invocation for Phase 34 complete; outcome recorded for Task 7.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Invoke Skill(gsd-validate-phase, "35") — Phase 35 (Rich Issue Filer + Quarantine Corpus)</name>
  <files>.planning/phases/35-rich-issue-filer-+-quarantine-corpus/35-VALIDATION.md</files>
  <read_first>
    - .planning/phases/35-rich-issue-filer-+-quarantine-corpus/35-VALIDATION.md
    - $HOME/.claude/skills/gsd-validate-phase/SKILL.md
  </read_first>
  <what-built>35-VALIDATION.md draft (nyquist_compliant: false) audited; Phase 35 has ISSUE-01..04 + QUAR-01/02/05 (7 reqs) satisfied per audit. Phase 35 also has Manual-Only entries (live filer + label promotion live confirmations) — auditor must classify those as COVERED-MANUAL not ESCALATE.</what-built>
  <action>
    Step 1: Invoke `Skill(skill="gsd-validate-phase", args="35")`.
    Step 2: Per Pitfall 4 — if auditor flags the live-filer / live-label rows as gaps, correct it: these are explicit Manual-Only entries per 35-VALIDATION.md.
    Step 3: Handle AskUserQuestion fallback per CLAUDE.md C1/C2/C3.
    Step 4: Verify outcome and record for Task 7.
    Step 5: Pause for user "approved" confirmation.
  </action>
  <how-to-verify>
    1. Frontmatter `nyquist_compliant` captured.
    2. Manual-Only rows NOT counted as escalations.
    3. Outcome stored.
  </how-to-verify>
  <verify>
    <automated>bash -c 'val=$(grep -E "^nyquist_compliant:" .planning/phases/35-rich-issue-filer-+-quarantine-corpus/35-VALIDATION.md | head -1); echo "Phase 35 post-skill: $val"; echo "$val" | grep -qE "nyquist_compliant:\s*(true|false)" || { echo "frontmatter malformed"; exit 1; }'</automated>
  </verify>
  <resume-signal>Type "approved" once Skill(gsd-validate-phase, "35") has returned AND outcome recorded.</resume-signal>
  <done>Skill invocation for Phase 35 complete; outcome recorded for Task 7.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: Invoke Skill(gsd-validate-phase, "37") — Phase 37 (Weekly Analytics Digest)</name>
  <files>.planning/phases/37-weekly-analytics-digest/37-VALIDATION.md</files>
  <read_first>
    - .planning/phases/37-weekly-analytics-digest/37-VALIDATION.md
    - $HOME/.claude/skills/gsd-validate-phase/SKILL.md
  </read_first>
  <what-built>37-VALIDATION.md draft (nyquist_compliant: false) audited; Phase 37 has 12/12 automated DIGEST-01..04 verifications per audit. Manual-Only entry: live Monday-cron-tick (deferred per CONTEXT.md — auditor must mark COVERED-MANUAL, not escalate).</what-built>
  <action>
    Step 1: Invoke `Skill(skill="gsd-validate-phase", args="37")`.
    Step 2: Per Pitfall 4 — if auditor escalates the live workflow_dispatch row, correct it: Manual-Only.
    Step 3: Handle AskUserQuestion fallback per CLAUDE.md C1/C2/C3.
    Step 4: Verify outcome and record for Task 7.
    Step 5: Pause for user "approved" confirmation.
  </action>
  <how-to-verify>
    1. Frontmatter `nyquist_compliant` captured.
    2. Manual-Only row NOT counted as escalation.
    3. Outcome stored.
  </how-to-verify>
  <verify>
    <automated>bash -c 'val=$(grep -E "^nyquist_compliant:" .planning/phases/37-weekly-analytics-digest/37-VALIDATION.md | head -1); echo "Phase 37 post-skill: $val"; echo "$val" | grep -qE "nyquist_compliant:\s*(true|false)" || { echo "frontmatter malformed"; exit 1; }'</automated>
  </verify>
  <resume-signal>Type "approved" once Skill(gsd-validate-phase, "37") has returned AND outcome recorded.</resume-signal>
  <done>Skill invocation for Phase 37 complete; outcome recorded for Task 7.</done>
</task>

<task type="auto">
  <name>Task 7: Rewrite nyquist: block in .planning/v3.1-MILESTONE-AUDIT.md to reflect post-stamping state</name>
  <files>.planning/v3.1-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/v3.1-MILESTONE-AUDIT.md (frontmatter lines 41-45 — current `nyquist:` block; also Markdown section "Nyquist Coverage" lines 145-156 — the table to update)
    - All 5 phase VALIDATION.md files post-skill (`grep -E "^nyquist_compliant:" .planning/phases/{32-*,33-*,34-*,35-*,37-*}/*-VALIDATION.md` — capture each phase's stamped value)
    - Executor's running notes from Tasks 2-6 (per-phase outcome: GAPS_FILLED | PARTIAL | ESCALATE | MANUAL_ONLY_CLASSIFIED + gap-description)
  </read_first>
  <acceptance_criteria>
    - After this task, the audit's `nyquist:` block (frontmatter, around lines 41-45) reflects the actual post-stamping state. `compliant_phases:` lists exactly the phases whose VALIDATION.md frontmatter is now `nyquist_compliant: true` (e.g., `["32","33","34","35","36","37"]` if all stamped); `partial_phases:` lists any phase still at false; `missing_phases:` remains empty (all 6 phases have VALIDATION.md files); `overall:` is `complete` if compliant_phases.length == 6 else `partial`
    - Markdown "Nyquist Coverage" table (around line 145-156) updated row-by-row to match — each phase row shows the new `nyquist_compliant` value, `Status` = COMPLIANT or PARTIAL, and the "Suggested action" column reads `Phase 38 stamped 2026-05-29` for newly compliant phases (or the gap reason for still-partial phases)
    - The `tech_debt: - phase: cross-cutting` item `"5 of 6 phases carry draft VALIDATION.md (nyquist_compliant: false) — formal Nyquist coverage not stamped despite strong vitest suites"` is REMOVED if all 5 were stamped; OR REWRITTEN to list the remaining partial phases if some were not
    - If any phase returned PARTIAL/ESCALATE on a real gap (not Manual-Only), that gap is added as a NEW tech_debt entry under a new `- phase: <phase-slug>-nyquist-gap` block with a descriptive item bullet
    - Audit `audited:` timestamp (line 2) updated to current ISO timestamp
    - `gaps.integration` block UNTOUCHED (Plan 38-01 owns it)
    - `human_verification:` block UNTOUCHED (Plan 38-03 owns it)
    - File still parses as valid YAML+markdown
  </acceptance_criteria>
  <action>
    Step 1: Run `for d in .planning/phases/{32-human-uat-verification,33-re-run-validator,34-hybrid-triage-classifier,35-rich-issue-filer-+-quarantine-corpus,36-quarantine-ci-integration-+-pipeline-orchestrator,37-weekly-analytics-digest}; do printf "%s: %s\n" "$d" "$(grep -E '^nyquist_compliant:' "$d"/*-VALIDATION.md | head -1)"; done` and capture the actual post-stamping value for each phase. Build the new `compliant_phases` / `partial_phases` lists from this output.
    Step 2: Edit `.planning/v3.1-MILESTONE-AUDIT.md`. (a) Update `audited:` (line 2) to current ISO timestamp. (b) Replace the `nyquist:` block (lines 41-45) with the new state. Example post-stamping (all 5 newly compliant): the block becomes `nyquist:` with `compliant_phases: ["32", "33", "34", "35", "36", "37"]`, `partial_phases: []`, `missing_phases: []`, `overall: complete`. If some are still partial, list them explicitly with overall `partial`. Preserve the comment style of the original block.
    Step 3: Edit the markdown "## Nyquist Coverage" section (around line 145-156). For each row 32-37, update the `nyquist_compliant` column and `Status` column to the actual post-task value; update the "Suggested action" column to `Phase 38 stamped 2026-05-29 (commit <SHA-after-commit-step-5>)` for newly compliant phases. For still-partial phases write the specific gap (one short line). Update the "Overall: PARTIAL." paragraph immediately below the table to read `Overall: COMPLETE` (if all 6 compliant) or keep `Overall: PARTIAL` with the remaining count.
    Step 4: Edit `tech_debt: - phase: cross-cutting` items (lines 63-66 area). If all 5 newly compliant: REMOVE the bullet `"5 of 6 phases carry draft VALIDATION.md (nyquist_compliant: false) — formal Nyquist coverage not stamped despite strong vitest suites"`. KEEP the second bullet `"3 integration fragility warnings"` if not yet handled by Plan 38-01 (in this Wave 1 parallel arrangement, Plan 38-01 may run before or after; do NOT alter that line — it is owned by Plan 38-01 and its presence is independent). If some phases remain partial, REWRITE the bullet to `"<N> of 6 phases carry draft VALIDATION.md (nyquist_compliant: false): [<phase-list>] — remaining gap documented in 38-02-SUMMARY.md"`.
    Step 5: If any phase produced a NEW real gap (auditor returned PARTIAL/ESCALATE on a non-Manual-Only row), add a new top-level entry under `tech_debt:` with shape `- phase: <phase>-nyquist-gap` and `items:` containing a one-line description of the gap (e.g., 'Phase 34 TRIAGE-03 cluster pre-filter has no automated test for N=4 boundary (gap surfaced by gsd-nyquist-auditor 2026-05-29)').
    Step 6: Validate YAML coherence with the same harness Plan 38-01 Task 4 uses: `node -e "const fs=require('fs'); const t=fs.readFileSync('.planning/v3.1-MILESTONE-AUDIT.md','utf8'); const blocks=t.split('---\n'); if(blocks.length!==3) throw new Error('expected 1 frontmatter block, got '+(blocks.length-2)); const fm=blocks[1]; if(!/nyquist:\s*\n\s*compliant_phases:/m.test(fm)) throw new Error('nyquist block malformed'); console.log('AUDIT YAML coherent');"`.
    Step 7: Commit: stage `.planning/v3.1-MILESTONE-AUDIT.md` only. Commit message: `docs(38-02): close nyquist tech_debt — <N> of 5 partial phases stamped` (substituting actual count).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('.planning/v3.1-MILESTONE-AUDIT.md','utf8'); const blocks=t.split('---\n'); if(blocks.length!==3) throw new Error('YAML frontmatter malformed'); const fm=blocks[1]; if(!/nyquist:\s*\n\s*compliant_phases:/m.test(fm)) throw new Error('nyquist.compliant_phases not present'); if(!/overall:\s*(complete|partial)/.test(fm)) throw new Error('nyquist.overall missing'); console.log('AUDIT nyquist block coherent');"</automated>
  </verify>
  <done>Audit YAML `nyquist:` block reflects actual post-Skill state across all 6 phases; markdown "Nyquist Coverage" table mirrors the frontmatter; cross-cutting tech_debt closed or rewritten; any new real gaps surfaced as new tech_debt entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill invocation (gsd-validate-phase) ↔ draft VALIDATION.md | The skill reads + edits planning artifacts; sub-agent (gsd-nyquist-auditor) may generate new test files. All edits occur in the local working copy under `.planning/` and `tests/` — no network surface |
| AskUserQuestion interactive gate | Per CLAUDE.md C1/C2/C3, the tool result MUST contain the user's explicit choice. Empty results trigger fallback numbered-list prompt; fabricating an answer violates the project constraint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02-01 | Tampering | Skill overwriting an existing draft VALIDATION.md rather than patching | mitigate | Per RESEARCH Assumption A3 (VERIFIED: ~/.claude/get-shit-done/workflows/validate-phase.md lines 38-45) the skill enters State A when VALIDATION.md exists non-empty and AUDITS + FILLS rather than reconstructing. Pre-flight (Task 1) confirms `nyquist_compliant: false` on each file so State A is unambiguously triggered. |
| T-38-02-02 | Repudiation / Information Disclosure | Auditor incorrectly classifying Manual-Only rows as ESCALATE | mitigate | Per RESEARCH Pitfall 4, Tasks 2-6 each call out the Manual-Only sections to the executor; if the auditor escalates a Manual-Only row, the executor corrects in-loop (do not stamp false GAPS_FILLED; do not silently auto-resolve). |
| T-38-02-03 | Tampering | CLAUDE.md C1 violation: auto-picking AskUserQuestion answer | mitigate | Every checkpoint:human-action task explicitly references CLAUDE.md C1/C2/C3 and requires fallback numbered-list prompt + explicit user response if the tool result is empty/generic. |
| T-38-02-04 | Tampering | Concurrent edit of `.planning/v3.1-MILESTONE-AUDIT.md` with Plan 38-01 (same-wave parallel) | accept | Plans 38-01 and 38-02 edit DISJOINT YAML blocks (gaps.integration vs nyquist:) and DISJOINT markdown sections. Each plan's audit-edit task is the LAST task of its plan, so within a wave the orchestrator may serialize the two final commits. Acceptance criteria for both plans explicitly forbid touching the other plan's block. If a merge conflict occurs at commit time, the second plan rebases — no semantic conflict because the blocks don't overlap. |
| T-38-02-SC | Tampering | npm/pip/cargo installs | accept | Phase 38 installs ZERO new packages. The Skill invocation may cause the auditor sub-agent to GENERATE test files (within `tests/unit/` or `tests/e2e/`) but generated tests use existing vitest/Playwright infrastructure — no new dependency. |
</threat_model>

<verification>
After all 7 tasks complete:
1. Each of the 5 phase VALIDATION.md files has `nyquist_compliant: true` OR a documented PARTIAL/ESCALATE outcome in this plan's SUMMARY.md: `for d in 32-human-uat-verification 33-re-run-validator 34-hybrid-triage-classifier 35-rich-issue-filer-+-quarantine-corpus 37-weekly-analytics-digest; do grep -E "^nyquist_compliant:" .planning/phases/$d/*-VALIDATION.md; done`
2. Audit YAML `nyquist:` block matches actual file states (Task 7 verify).
3. Markdown "Nyquist Coverage" table mirrors frontmatter.
4. Cross-cutting tech_debt either closed or rewritten with remaining partial phases.
5. Any new real gaps recorded as new tech_debt entries.
6. Plan 38-01's `gaps.integration` block and Plan 38-03's `human_verification` block UNTOUCHED.
7. `npm run test:src` exits 0 (any generated tests are green; nothing broke).
</verification>

<success_criteria>
- 5 Skill invocations executed (one per phase 32, 33, 34, 35, 37) with outcome recorded
- Each phase that returned GAPS FILLED has its VALIDATION.md `nyquist_compliant: true`; each phase that returned PARTIAL/ESCALATE on a real gap has documented gap surfaced as new tech_debt
- `.planning/v3.1-MILESTONE-AUDIT.md` `nyquist:` block + "Nyquist Coverage" markdown table reflect actual state
- Cross-cutting tech_debt entry for nyquist coverage either removed (all stamped) or rewritten (remaining partials)
- No alterations to `gaps.integration` (Plan 38-01) or `human_verification` (Plan 38-03) blocks
</success_criteria>

<output>
Create `.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-02-SUMMARY.md` per template with:
- Per-phase outcome table: | Phase | Skill outcome | nyquist_compliant value | Tests generated | New gap surfaced |
- Audit-doc edit diff summary (before/after for nyquist: block)
- Any new tech_debt entries added (with rationale)
- Notes on any AskUserQuestion fallbacks invoked per CLAUDE.md C1/C2/C3
</output>
