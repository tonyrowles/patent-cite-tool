---
phase: 38-v3-1-cleanup-integration-warnings-nyquist-human-uat
plan: 03
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - .planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-UAT-EVIDENCE.md
  - .planning/v3.1-MILESTONE-AUDIT.md
autonomous: false
requirements:
  - UAT-01
  - UAT-02
  - UAT-03
  - ISSUE-01
  - ISSUE-02
  - ISSUE-03
  - ISSUE-04
  - QUAR-02
  - QUAR-03
  - QUAR-04
  - QUAR-05
  - ORCH-01
  - ORCH-02
  - ORCH-03
  - DIGEST-01
  - DIGEST-02
  - DIGEST-03
  - DIGEST-04
user_setup:
  - service: github
    why: "All UAT items require authenticated gh CLI to dispatch workflows + read run state + verify labels + read filed issues. The user must be logged into gh as a repo collaborator with write permission."
    env_vars: []
    dashboard_config:
      - task: "Confirm `gh auth status` is fresh (token valid) BEFORE first UAT task"
        location: "Local shell — `gh auth status` exits 0 and the configured account has write access to the patent-cite-tool repo"
      - task: "Confirm GitHub Discussions feature state for repo (DIGEST-03 publish branch)"
        location: "GitHub web — repo Settings > General > Features > Discussions toggle (determines whether UAT-37 lands as a Discussion or as an `e2e-digest`-labeled Issue)"
  - service: anthropic-max-5
    why: "UAT-32 (Phase 32 CR-04 mid-run phase-cap trip) requires `claude -p` invocations against the Max 5 subscription credit. This is the same path used in the original Phase 32 UAT (commit a47b… 2026-05-25)."
    env_vars: []
    dashboard_config:
      - task: "Confirm headless Claude credit available for ≤3 short iterations (well under the $80 warning threshold per STATE.md)"
        location: "Local — the existing spend ledger at the path used by Phase 32 UAT"
tags:
  - v3.1
  - human-uat
  - live-confirmation
  - tech-debt
must_haves:
  truths:
    - "5 dispatchable live UAT confirmations have been executed in sequence: UAT-32 (CR-04 phase-cap trip), UAT-35a (live filer --source triage), UAT-35b (quarantine-append idempotency + ready-for-promotion label), UAT-36a (nightly workflow_dispatch with llm_run_id), UAT-36b (npm run e2e:quarantine empty-corpus), UAT-37 (workflow_dispatch e2e-weekly-digest.yml)"
    - "Each UAT command's invocation was IMMEDIATELY followed by a `gh run list --limit 1 --json databaseId,status,createdAt` (or local equivalent verification) per RESEARCH Pitfall 7 — exit 0 alone is insufficient evidence; the run-list / file-existence / label-existence check is the authoritative signal"
    - "Phase 35 (c) gh-label-list is recorded as DONE (already confirmed by developer per `35-00-SUMMARY.md`) with no live re-run"
    - "Phase 37 live Monday-cron-tick is recorded as DEFERRED with rationale (workflow_dispatch surrogate is sufficient mechanism verification; clock cannot be advanced) per CONTEXT.md locked decision"
    - "Each UAT outcome is appended to `38-UAT-EVIDENCE.md` (single growing markdown doc, one section per item) with run URL, exit code, log excerpt, and issue URL or file path as appropriate; the audit YAML `human_verification:` block is updated to add `outcome: PASS|FAIL|DEFERRED, verified_at: <ISO>` per item"
    - "If any UAT FAILS, the failure is captured (stdout/stderr in 38-UAT-EVIDENCE.md + a follow-up GitHub issue or quick task), the audit YAML records `outcome: FAIL`, and the plan continues — does NOT block (per CONTEXT.md locked decision: 'failure handling: do NOT block Phase 38')"
  artifacts:
    - path: ".planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-UAT-EVIDENCE.md"
      provides: "Single evidence doc with one section per UAT item (UAT-32, UAT-35a, UAT-35b, UAT-36a, UAT-36b, UAT-37, UAT-35c-DONE, UAT-37-cron-DEFERRED)"
      min_lines: 60
      contains: "## UAT-32"
    - path: ".planning/v3.1-MILESTONE-AUDIT.md"
      provides: "Updated human_verification: block with outcome and verified_at per item"
      contains: "outcome:"
  key_links:
    - from: "Each UAT shell block"
      to: "38-UAT-EVIDENCE.md"
      via: ">> append redirection with section header"
      pattern: "^## UAT-"
    - from: "38-UAT-EVIDENCE.md per-section verified_at"
      to: ".planning/v3.1-MILESTONE-AUDIT.md human_verification: [].outcome"
      via: "explicit YAML edit in final task"
      pattern: "outcome:\\s*(PASS|FAIL|DEFERRED)"
    - from: "Every gh workflow run invocation"
      to: "gh run list --limit 1 --json databaseId,status,createdAt verification (RESEARCH Pitfall 7)"
      via: "immediate follow-up command in the same shell block"
      pattern: "gh run list --workflow"
---

<objective>
Execute the 5 dispatchable live-environment human-UAT confirmations deferred from prior v3.1 phases, capture evidence to a single growing markdown doc, mark the 1 already-done item DONE, mark the 1 cron-tick-deferred item DEFERRED, then update `.planning/v3.1-MILESTONE-AUDIT.md` `human_verification:` block with per-item outcome.

Purpose: Close the v3.1 `human_verification:` tech_debt — 7 outstanding live confirmations across phases 32, 35, 36, 37. These are NOT suspected failures; the underlying mechanism is fully verified by the existing 678-test vitest suite. The live runs are EXPECTED confirmations that the contract holds on real GitHub Actions + real `gh` CLI + real Max 5 subscription.

Output: 38-UAT-EVIDENCE.md (single committed evidence file under the phase dir) with 8 sections (6 live + 1 DONE + 1 DEFERRED); audit YAML updated to add `outcome:` + `verified_at:` per item. Per CONTEXT.md locked decision: failures do NOT block Phase 38 — they are captured + a follow-up issue/quick-task is opened, then the plan continues.

Depends on Plan 38-01 because UAT-36a (nightly workflow_dispatch) is most meaningful when the INT-FIX-03 quarantine-failure artifact-upload gating is already live — the dispatched run exercises the post-fix YAML.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v3.1-MILESTONE-AUDIT.md
@.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-CONTEXT.md
@.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-RESEARCH.md
@.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-VALIDATION.md
@.planning/phases/32-human-uat-verification/32-VERIFICATION.md
@.planning/phases/35-rich-issue-filer-+-quarantine-corpus/35-00-SUMMARY.md
@.planning/phases/36-quarantine-ci-integration-+-pipeline-orchestrator/36-HUMAN-UAT.md
@.planning/phases/37-weekly-analytics-digest/37-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Live commands + verification companions for each UAT item -->

UAT-32 (CR-04 phase-cap trip):
  Command: `npm run e2e:explore -- --max-iterations 2 --phase 32`
  (Or environment-variable equivalent if --phase flag not present — Phase 32 added PLAYWRIGHT_RUN_ID env var per STATE.md "Plan 32-04 resolveRunId() semantic mismatch")
  Expected outcome: exits with code 6 (the documented "phase cap tripped" exit code per Phase 32 CR-04)
  Source reference: e2e-explore.mjs lines 551-562 (override-accepted via AskUserQuestion 2026-05-25)
  Evidence to capture: exit code (must be 6), last ~30 lines of stdout showing cap-tripped message, llm-ledger location after run

UAT-35a (live --source triage filer):
  Command: `node scripts/e2e-report-issue.mjs --source triage --triage-report tests/e2e/fixtures/phase35-triage-report.json`
  (Per Assumption A7: fixture path verified via `ls tests/e2e/fixtures/` — `phase35-triage-report.json` exists)
  Expected outcome: GitHub issue created with body containing 4 sections (reproducer, verifier detail, LLM rationale, golden diff) + line-1 fingerprint comment + labels [category, e2e-nightly, triage]
  Evidence: issue URL via `gh issue list --label triage --limit 1 --json url,title,body,labels`; body excerpt showing the 4 section headers and line-1 fingerprint; full label set

UAT-35b (quarantine-append idempotency + ready-for-promotion):
  Command (3 iterations): `for i in 1 2 3; do node scripts/quarantine-append.mjs --input tests/e2e/fixtures/phase35-triage-report.json; done`
  Expected outcome: run 1 + 2 = entry inserted/updated; run 3 = source GitHub issue receives `quarantine:ready-for-promotion` label (auto-tagged at stable_runs ≥ 3 per QUAR-02 contract)
  Evidence: per-run exit code; after run 3 capture `gh issue view <source-issue> --json labels` showing the ready-for-promotion label; `cat tests/e2e/test-cases-quarantine.js` row count (should be 1 — idempotent upsert) and stable_runs field (should be 3)

UAT-36a (nightly workflow_dispatch):
  Command: `gh workflow run e2e-nightly.yml -f llm_run_id="${LLM_RUN_ID}"` where LLM_RUN_ID is sourced from a recent successful nightly artifact OR from the Phase 32 UAT preserved fixture (per CONTEXT.md "Specific Ideas")
  Per RESEARCH Pitfall 7 (CRITICAL): IMMEDIATELY follow with `gh run list --workflow=e2e-nightly.yml --limit 1 --json databaseId,status,createdAt,event` and assert the most-recent run is <60s old AND event="workflow_dispatch"
  Expected outcome: steps 2-5 (rerun → triage → issue-file → quarantine-append) execute; cron path (no input) unaffected on subsequent scheduled runs
  Evidence: run URL; jobs.status; jobs.conclusion; step-by-step view of the 5 gated steps; per Plan 38-01 INT-FIX-03 (this plan's depends_on): if the run hits a quarantine failure, the Upload E2E artifacts step should now upload diagnostic artifacts — verify via `gh run download <run-id> --name e2e-nightly-<run_id>` if the run failed quarantine

UAT-36b (e2e:quarantine local empty-corpus):
  Command: `npm run e2e:quarantine`
  Per RESEARCH Pitfall 5: BEFORE running, `cat tests/e2e/test-cases-quarantine.js | grep -c 'errorClass'` to verify corpus state. If non-zero (UAT-35b appended one entry), document the actual expectation ("exits 0 with N tests, all non-gating per QUAR-03 retries: 0") instead of insisting on 0 tests
  Expected outcome: exit 0; Playwright reports 0 tests (or N tests if corpus is non-empty post-35b) — non-gating per --pass-with-no-tests + --retries=0
  Evidence: exit code; Playwright stdout/stderr (last ~30 lines)

UAT-37 (workflow_dispatch digest):
  Command: `gh workflow run e2e-weekly-digest.yml`
  Per RESEARCH Pitfall 7: IMMEDIATELY follow with `gh run list --workflow=e2e-weekly-digest.yml --limit 1 --json databaseId,status,createdAt,event`
  Expected outcome: run completes; `reports/weekly-digest-YYYY-WNN.md` file committed to main with `[skip ci]` in commit message; digest published as GitHub Discussion (if Discussions enabled) OR as `e2e-digest`-labeled issue (fallback); per DIGEST-03 contract both paths are implemented and selected by resolvePublishMode
  Evidence: run URL; the committed `reports/weekly-digest-*.md` file path; either the Discussion URL OR the e2e-digest issue URL; per Plan 38-01 INT-FIX-02 (this plan's depends_on): the digest must complete WITHOUT throwing on SUMMARY_KEYS drift (the post-fix aggregateBySummaryKey path is now exercised on real data)

Already-DONE (skip live run):
  UAT-35c: `gh label list | grep -E 'triage|quarantine:ready-for-promotion'` confirms both labels exist with the documented colors (#6F42C1 + #FFA500). Already confirmed by developer during Plan 35-00 per audit. Mark DONE in evidence file with reference to 35-00-SUMMARY.md.

DEFERRED:
  UAT-37-monday-cron: Live Monday 07:00 UTC cron tick of e2e-weekly-digest.yml. Cannot be triggered manually as a cron event; clock cannot be advanced. workflow_dispatch surrogate (UAT-37 above) is sufficient mechanism verification. Mark DEFERRED in evidence file with rationale.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap 38-UAT-EVIDENCE.md and verify gh auth + Phase 38-01 commits in place</name>
  <files>.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-UAT-EVIDENCE.md</files>
  <read_first>
    - .planning/v3.1-MILESTONE-AUDIT.md (human_verification: block lines 26-40 — the 7 items to be closed; tracks which items map to which UAT- IDs in this plan)
    - .planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-CONTEXT.md (§"Human-UAT Execution" — locked decisions for scope, already-confirmed, deferred, result-recording, failure-handling)
    - .planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-RESEARCH.md (§"Pitfall 6: UAT result-recording race against audit YAML" lines 396-400; §"Pitfall 7: gh workflow run silent on auth failure" lines 403-406)
  </read_first>
  <acceptance_criteria>
    - File `.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-UAT-EVIDENCE.md` exists with a top-level title + 8 placeholder sections (`## UAT-32`, `## UAT-35a`, `## UAT-35b`, `## UAT-35c (DONE)`, `## UAT-36a`, `## UAT-36b`, `## UAT-37`, `## UAT-37-monday-cron (DEFERRED)`) each with `**status:** pending` initially
    - `gh auth status` exits 0 (token fresh — Pitfall 7 prerequisite)
    - The 3 INT-FIX commits from Plan 38-01 are present in git log: `git log --oneline | grep -cE "^[0-9a-f]+ fix\(38-01\): INT-FIX-(01|02|03)"` returns 3 (depends_on requirement met)
    - The INT-FIX-03 YAML change is on disk: `grep -E "steps\.quarantine\.outcome == 'failure'" .github/workflows/e2e-nightly.yml` exits 0 (UAT-36a will exercise the post-fix YAML)
  </acceptance_criteria>
  <action>
    Step 1: Verify Plan 38-01 dependency is satisfied. Run: `git log --oneline -20 | grep -E "fix\(38-01\): INT-FIX-"` — expect 3 matches. If fewer than 3, STOP and report which INT-FIX-NN commits are missing; Plan 38-03 cannot proceed.
    Step 2: Verify gh auth: `gh auth status 2>&1`. If exit non-zero, STOP and prompt the user to run `gh auth login` before continuing (per CLAUDE.md C1: do not assume; surface the gate).
    Step 3: Create `.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-UAT-EVIDENCE.md` with this skeleton (use Write tool, not heredoc):

      ```
      # Phase 38 — Human-UAT Live Confirmations Evidence

      **Plan:** 38-03
      **Created:** <current ISO timestamp>
      **Depends-on:** Plan 38-01 commits INT-FIX-01..INT-FIX-03 (verified present)

      ---

      ## UAT-32 — Phase 32 CR-04 mid-run phase-cap trip → exit code 6

      **status:** pending
      **requirement:** UAT-01 (live-confirmation portion)
      **audit human_verification entry:** phase 32, "CR-04 mid-run phase-cap trip routes to exit code 6"

      _(evidence captured in Task 2)_

      ---

      ## UAT-35a — Live `e2e-report-issue.mjs --source triage` against real triage-report.json

      **status:** pending
      **requirement:** ISSUE-01..04 (live-confirmation portion)
      **audit human_verification entry:** phase 35, "Live `e2e-report-issue.mjs --source triage` ... 4 sections + line-1 fingerprint + labels"

      _(evidence captured in Task 3)_

      ---

      ## UAT-35b — quarantine-append.mjs 3× same CONFIRMED finding → ready-for-promotion label on run 3

      **status:** pending
      **requirement:** QUAR-02 (live-confirmation portion)
      **audit human_verification entry:** phase 35, "quarantine-append.mjs 3x ... ready-for-promotion label on run 3"

      _(evidence captured in Task 4)_

      ---

      ## UAT-35c — gh label list shows triage + quarantine:ready-for-promotion — DONE

      **status:** DONE
      **requirement:** QUAR-05 (live-confirmation portion)
      **audit human_verification entry:** phase 35, "gh label list shows triage (#6F42C1) + quarantine:ready-for-promotion (#FFA500). [already confirmed by developer during Plan 00]"
      **rationale:** Already confirmed by developer during Plan 35-00 execution per `.planning/phases/35-rich-issue-filer-+-quarantine-corpus/35-00-SUMMARY.md`. No live re-run required per CONTEXT.md locked decision.

      _(no live invocation — historical confirmation)_

      ---

      ## UAT-36a — `gh workflow run e2e-nightly.yml -f llm_run_id=<id>` → steps 2-5 execute

      **status:** pending
      **requirement:** ORCH-01, ORCH-02, ORCH-03, QUAR-03, QUAR-04 (live-confirmation portion)
      **audit human_verification entry:** phase 36, "Dispatch e2e-nightly.yml with real llm_run_id → steps 2-5 execute; cron path (no input) byte-identical regression"
      **note:** Exercises the post-INT-FIX-03 Upload E2E artifacts step (Plan 38-01 commit present).

      _(evidence captured in Task 5)_

      ---

      ## UAT-36b — `npm run e2e:quarantine` local empty-corpus → exits 0, 0 tests

      **status:** pending
      **requirement:** QUAR-03, QUAR-04 (live-confirmation portion)
      **audit human_verification entry:** phase 36, "`npm run e2e:quarantine` local empty-corpus → exits 0, Playwright reports 0 tests"
      **note:** Per RESEARCH Pitfall 5, corpus may be non-empty if UAT-35b ran first (it does — Task 4 is sequenced before Task 6); adjust expectation per actual state.

      _(evidence captured in Task 6)_

      ---

      ## UAT-37 — `gh workflow run e2e-weekly-digest.yml` → reports/weekly-digest-YYYY-WNN.md committed + e2e-digest issue or Discussion

      **status:** pending
      **requirement:** DIGEST-01..04 (live-confirmation portion)
      **audit human_verification entry:** phase 37, "Live Monday-cron / manual workflow_dispatch of e2e-weekly-digest.yml → commits reports/weekly-digest-YYYY-WNN.md [skip ci] + files e2e-digest issue"
      **note:** Exercises the post-INT-FIX-02 aggregateBySummaryKey path (Plan 38-01 commit present).

      _(evidence captured in Task 7)_

      ---

      ## UAT-37-monday-cron — Live Monday 07:00 UTC cron tick — DEFERRED

      **status:** DEFERRED
      **requirement:** DIGEST-02 (live cron portion only — mechanism otherwise verified by UAT-37 workflow_dispatch)
      **audit human_verification entry:** subset of phase 37 entry above
      **rationale:** Cannot be triggered manually as a "cron" event; clock cannot be advanced. The workflow_dispatch surrogate (UAT-37) verifies all underlying mechanisms (cron-equivalent YAML, contents/discussions/issues:write permissions, [skip ci] commit, Discussion/issue publish branches). Per CONTEXT.md locked decision.

      _(no live invocation — formally deferred)_

      ---
      ```

    Step 4: Commit: `docs(38-03): bootstrap UAT-EVIDENCE skeleton (8 sections; UAT-35c DONE, UAT-37-cron DEFERRED)`. Stage only `.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-UAT-EVIDENCE.md`.
  </action>
  <verify>
    <automated>bash -c 'set -e; f=.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-UAT-EVIDENCE.md; test -f "$f"; sec=$(grep -cE "^## UAT-" "$f"); [ "$sec" -eq 8 ] || { echo "expected 8 sections, found $sec"; exit 1; }; gh auth status >/dev/null 2>&1 || { echo "gh auth not fresh"; exit 1; }; intfix=$(git log --oneline -20 | grep -cE "fix\(38-01\): INT-FIX-(01|02|03)"); [ "$intfix" -eq 3 ] || { echo "expected 3 INT-FIX commits from Plan 38-01, found $intfix"; exit 1; }; echo "bootstrap OK: 8 sections, gh auth fresh, INT-FIX-01/02/03 commits present"'</automated>
  </verify>
  <done>38-UAT-EVIDENCE.md created with 8 sections; UAT-35c marked DONE (historical) and UAT-37-monday-cron marked DEFERRED (locked decision); gh auth fresh; Plan 38-01 commits confirmed in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT-32 — Run `npm run e2e:explore` with low --max-iterations against Max 5 subscription → expect exit code 6 (phase-cap trip)</name>
  <files>.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-UAT-EVIDENCE.md</files>
  <read_first>
    - 38-UAT-EVIDENCE.md (current state — confirms section ## UAT-32 exists)
    - .planning/phases/32-human-uat-verification/32-VERIFICATION.md (CR-04 expected-outcome reference)
    - scripts/e2e-explore.mjs lines 540-580 (the CR-04 override path — provides the expected exit-6 message text for log-grep)
  </read_first>
  <what-built>The CR-04 override at e2e-explore.mjs:551-562 trips the phase cap mid-run, exits with code 6. This was accepted via AskUserQuestion on 2026-05-25 per audit; logic is sound by inspection. Task confirms the live exit code on real Max 5 subscription credit.</what-built>
  <action>
    1. Verify spend-ledger headroom before run: `cat <spend-ledger-path>` (path documented in Phase 32 SUMMARY). Confirm < $80 (warning threshold) and < $100 (hard cap) to leave room for ≤3 iterations of `claude -p`.
    2. Run the command (the executor MUST run this — not assume it):
       ```
       cd /home/fatduck/patent-cite-tool
       npm run e2e:explore -- --max-iterations 2 --phase 32 2>&1 | tee /tmp/uat-32.log
       echo "EXIT=$?" | tee -a /tmp/uat-32.log
       ```
       If --phase flag is not present (per STATE.md "Plan 32-04 resolveRunId() semantic mismatch — --run-id CLI flag deferred"), use the env-var alternative documented in Phase 32 SUMMARY (e.g., PLAYWRIGHT_RUN_ID=... npm run e2e:explore -- --max-iterations 2).
    3. Assert: `tail -5 /tmp/uat-32.log` shows `EXIT=6` (the documented phase-cap-trip exit code).
    4. Capture evidence: append to 38-UAT-EVIDENCE.md `## UAT-32` section:
       - Update `**status:**` from `pending` to `PASS` (or `FAIL` if exit ≠ 6)
       - Add `**verified_at:** <ISO timestamp>`
       - Add `**command:** npm run e2e:explore -- --max-iterations 2 --phase 32`
       - Add `**exit_code:** 6` (or actual)
       - Add a fenced ```` ```log ```` block with the last ~30 lines of /tmp/uat-32.log (the cap-tripped message + the runner's summary footer)
       - Add `**spend_ledger_delta_usd:** <pre vs post>` if measurable
    5. If exit ≠ 6: capture the full stdout/stderr, mark `FAIL`, and open a follow-up GitHub issue via `gh issue create --title "UAT-32 FAIL: phase-cap trip did not exit 6" --body "<paste log>" --label "e2e-uat-fail"` (per CONTEXT.md failure-handling locked decision: do NOT block Phase 38).
    6. Commit: `docs(38-03): UAT-32 PASS|FAIL — phase-cap trip exit code <code>`. Stage only the evidence doc.
  </action>
  <resume-signal>Type "approved" once UAT-32 has been run, exit code recorded, evidence appended, and committed. If FAILED, also type the follow-up issue URL.</resume-signal>
  <verify>
    <automated>bash -c 'tail -5 /tmp/uat-32.log 2>/dev/null | grep -q "EXIT=6" || { echo "UAT-32 evidence pending or exit ≠ 6"; exit 1; }; grep -qE "^\*\*status:\*\*\s+(PASS|FAIL)" .planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-UAT-EVIDENCE.md || { echo "UAT-32 section status not set"; exit 1; }'</automated>
  </verify>
  <done>UAT-32 ran live; exit code captured (PASS=6 / FAIL=other); evidence section in 38-UAT-EVIDENCE.md updated with status, verified_at, command, exit_code, log excerpt; committed to repo. On FAIL: follow-up issue URL recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT-35a — Run `e2e-report-issue.mjs --source triage` against fixture phase35-triage-report.json → expect issue with 4 sections + line-1 fingerprint + 3 labels</name>
  <files>.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-UAT-EVIDENCE.md</files>
  <read_first>
    - 38-UAT-EVIDENCE.md
    - tests/e2e/fixtures/phase35-triage-report.json (the fixture this UAT uses; verify it exists and contains at least one CONFIRMED finding)
    - tests/e2e/lib/issue-payload-builder.js (the 4-section body assembler — provides the expected body shape)
    - .planning/phases/35-rich-issue-filer-+-quarantine-corpus/35-03-SUMMARY.md (Plan 35-03 implemented --source triage)
  </read_first>
  <what-built>Phase 35 Plan 35-03 added `--source triage` to e2e-report-issue.mjs. Issue body has 4 sections (reproducer, verifier detail, LLM rationale, golden diff) within budgets + line-1 fingerprint comment + 3 labels [errorClass-category, e2e-nightly, triage]. UAT confirms the live filer hits real GitHub correctly.</what-built>
  <action>
    1. Verify fixture: `test -f tests/e2e/fixtures/phase35-triage-report.json && jq '.findings | length' tests/e2e/fixtures/phase35-triage-report.json` — expect ≥1.
    2. Capture pre-state: `gh issue list --label triage --state open --limit 5 --json number,title > /tmp/uat-35a-pre.json`.
    3. Run the command (executor MUST run):
       ```
       cd /home/fatduck/patent-cite-tool
       node scripts/e2e-report-issue.mjs --source triage --triage-report tests/e2e/fixtures/phase35-triage-report.json 2>&1 | tee /tmp/uat-35a.log
       echo "EXIT=$?" | tee -a /tmp/uat-35a.log
       ```
    4. Capture post-state: `gh issue list --label triage --state open --limit 5 --json number,title,url,labels,body > /tmp/uat-35a-post.json`. Diff pre vs post: `jq -r '.[].number' /tmp/uat-35a-pre.json | sort > /tmp/pre; jq -r '.[].number' /tmp/uat-35a-post.json | sort > /tmp/post; comm -13 /tmp/pre /tmp/post` — expect ≥1 new issue number (or 0 if dedup hit an existing v1/v2-fingerprint match — that is ALSO a PASS, per the ISSUE-03 dual-search contract).
    5. For each new issue: `gh issue view <number> --json body,labels`. Assert:
       - body contains the 4 section headers (look for the substrings established by Plan 35-01 — e.g., "Reproducer", "Verifier", "LLM rationale" or equivalent; verify against `tests/e2e/lib/issue-payload-builder.js`)
       - body line 1 contains the fingerprint comment (per ISSUE-04 contract — line-1 placement prevents 65,536-char body overflow)
       - labels include exactly one of the ERROR_CLASSES categories AND `e2e-nightly` AND `triage`
    6. Append evidence to 38-UAT-EVIDENCE.md `## UAT-35a` section:
       - `**status:**` PASS or FAIL
       - `**verified_at:** <ISO>`
       - `**command:** node scripts/e2e-report-issue.mjs --source triage --triage-report tests/e2e/fixtures/phase35-triage-report.json`
       - `**exit_code:** <code>`
       - `**issue_url:** <url>` (or "dedup hit — existing issue: <url>")
       - `**labels:** [<list>]`
       - `**body_line_1:** <first line, fingerprint comment>`
       - Fenced log block with last ~20 lines of /tmp/uat-35a.log
    7. On FAIL, file follow-up issue per the standard pattern; do NOT block.
    8. Commit: `docs(38-03): UAT-35a PASS|FAIL — live --source triage filer`.
  </action>
  <resume-signal>Type "approved" once UAT-35a evidence + commit complete. Include the created/deduped issue URL.</resume-signal>
  <verify>
    <automated>bash -c 'grep -qE "^\*\*issue_url:\*\*" .planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-UAT-EVIDENCE.md || { echo "UAT-35a issue_url not captured"; exit 1; }; echo "UAT-35a evidence ok"'</automated>
  </verify>
  <done>UAT-35a ran live; issue URL captured (or dedup-hit URL); 4-section body + line-1 fingerprint + 3 labels verified; evidence section in 38-UAT-EVIDENCE.md updated; committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT-35b — Run `quarantine-append.mjs` 3× same finding → expect quarantine:ready-for-promotion label on run 3</name>
  <files>.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-UAT-EVIDENCE.md</files>
  <read_first>
    - 38-UAT-EVIDENCE.md
    - scripts/quarantine-append.mjs (the script — confirm --input flag accepts the triage report)
    - tests/e2e/test-cases-quarantine.js (pre-state — the corpus this UAT mutates)
    - .planning/phases/35-rich-issue-filer-+-quarantine-corpus/35-04-SUMMARY.md (Plan 35-04 idempotent upsert + stable_runs≥3 auto-label)
  </read_first>
  <what-built>Plan 35-04 implemented `quarantine-append.mjs` with idempotent upsert; on run N where stable_runs reaches 3, the SOURCE GitHub issue receives the `quarantine:ready-for-promotion` label. UAT confirms the live label-add on real GitHub on iteration 3.</what-built>
  <action>
    1. Pre-state: `cat tests/e2e/test-cases-quarantine.js > /tmp/uat-35b-pre-corpus.js`. Identify the source-issue number from the triage fixture by reading `tests/e2e/fixtures/phase35-triage-report.json` → first finding → `source_issue.number` (or equivalent field per schema).
    2. Pre-label-state: `gh issue view <source-issue-number> --json labels -q '.labels[].name'` → save to /tmp/uat-35b-pre-labels.
    3. Run 3 iterations (executor MUST run):
       ```
       cd /home/fatduck/patent-cite-tool
       for i in 1 2 3; do
         echo "=== RUN $i ===" | tee -a /tmp/uat-35b.log
         node scripts/quarantine-append.mjs --input tests/e2e/fixtures/phase35-triage-report.json 2>&1 | tee -a /tmp/uat-35b.log
         echo "EXIT_RUN_$i=$?" | tee -a /tmp/uat-35b.log
       done
       ```
    4. Post-state assertions:
       - `cat tests/e2e/test-cases-quarantine.js` — find the entry matching the fixture's caseId. Confirm `stable_runs: 3` (or whatever the field name per Plan 35-04). Confirm the corpus has exactly ONE entry (idempotent — not three).
       - `gh issue view <source-issue-number> --json labels -q '.labels[].name'` → save to /tmp/uat-35b-post-labels. Assert `quarantine:ready-for-promotion` is in the post list but NOT in the pre list.
    5. Append evidence to 38-UAT-EVIDENCE.md `## UAT-35b`:
       - `**status:**` PASS or FAIL
       - `**verified_at:** <ISO>`
       - `**command:** for i in 1 2 3; do node scripts/quarantine-append.mjs --input tests/e2e/fixtures/phase35-triage-report.json; done`
       - `**run_1_exit:** <code>`, `**run_2_exit:** <code>`, `**run_3_exit:** <code>`
       - `**source_issue:** <url>`
       - `**pre_labels:** [<list>]`
       - `**post_labels:** [<list>]`
       - `**ready_for_promotion_added:** true|false`
       - `**corpus_entry_count:** 1` (idempotency check)
       - `**stable_runs:** 3`
       - Fenced log block with key excerpts of /tmp/uat-35b.log (run-3 idempotent-update message + label-add message)
    6. NOTE for Task 6 (UAT-36b): after this task, `tests/e2e/test-cases-quarantine.js` is NON-EMPTY (one entry). UAT-36b's expectation must be adjusted: `npm run e2e:quarantine` will run 1 test instead of 0 tests.
    7. On FAIL, follow-up issue + continue.
    8. Commit: `docs(38-03): UAT-35b PASS|FAIL — quarantine-append idempotency + ready-for-promotion auto-label`. NOTE: This commit will also include the modified `tests/e2e/test-cases-quarantine.js` (the corpus side-effect). Stage both files.
  </action>
  <resume-signal>Type "approved" once UAT-35b evidence + commit complete. Include `ready_for_promotion_added` and corpus_entry_count.</resume-signal>
  <verify>
    <automated>bash -c 'grep -qE "^\*\*ready_for_promotion_added:\*\*" .planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-UAT-EVIDENCE.md || { echo "UAT-35b ready_for_promotion_added not captured"; exit 1; }; echo "UAT-35b evidence ok"'</automated>
  </verify>
  <done>UAT-35b ran live 3×; idempotent upsert confirmed (corpus_entry_count=1); quarantine:ready-for-promotion label added to source issue on run 3 (or FAIL captured); evidence section updated; committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: UAT-36a — `gh workflow run e2e-nightly.yml -f llm_run_id=<id>` → steps 2-5 execute (post-INT-FIX-03 YAML)</name>
  <files>.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-UAT-EVIDENCE.md</files>
  <read_first>
    - 38-UAT-EVIDENCE.md
    - .github/workflows/e2e-nightly.yml (confirm INT-FIX-03 quarantine-failure upload condition is in place — this UAT exercises the post-fix YAML)
    - .planning/phases/36-quarantine-ci-integration-+-pipeline-orchestrator/36-04-SUMMARY.md (Plan 36-04 wired the 5 gated steps and timeout-budget)
    - .planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-RESEARCH.md (§"Pitfall 7: gh workflow run is silent on auth failure" — MUST follow with gh run list)
  </read_first>
  <what-built>Phase 36 Plan 36-04 wired e2e-nightly.yml so that when dispatched with `llm_run_id` input, 5 gated steps (download artifact → run-triage-pipeline → quarantine spec → quarantine failure filer → label-ensure) execute, while the no-input cron path is unaffected. UAT confirms live execution against real GitHub Actions; per Plan 38-01 INT-FIX-03 (depends_on) any quarantine failure now uploads diagnostic artifacts via the post-fix Upload step.</what-built>
  <action>
    1. Source a real `llm_run_id`. Options (in priority order, per CONTEXT.md "Specific Ideas"): (a) most recent nightly artifact: `gh run list --workflow=e2e-nightly.yml --limit 5 --json databaseId,artifacts -q '[.[] | select(.artifacts != null) | .databaseId] | .[0]'` AND `gh run download <id> --name llm-report` to confirm the artifact is present; (b) the Phase 32 UAT preserved llm-report if accessible (`ls tests/e2e/fixtures/phase35-llm-report.json` — that fixture exists and is a valid llm-report).
    2. If using fixture path (option b): `gh workflow run` cannot directly accept a local file — instead use option (a) and pass the artifact RUN_ID. If no recent nightly artifact exists, STOP and prompt the user: "No source llm-report available for UAT-36a. Options: 1) Trigger a non-llm nightly first (`gh workflow run e2e-nightly.yml`) and wait for completion, 2) Manually upload the fixture via UAT-32 pipeline (`npm run e2e:upload-llm-report`), 3) Mark UAT-36a DEFERRED with rationale." Per CLAUDE.md C1/C2/C3 — do NOT auto-pick.
    3. Once LLM_RUN_ID is sourced, dispatch (executor MUST run):
       ```
       LLM_RUN_ID=<the-run-id>
       cd /home/fatduck/patent-cite-tool
       gh workflow run e2e-nightly.yml -f llm_run_id="${LLM_RUN_ID}" 2>&1 | tee /tmp/uat-36a-dispatch.log
       echo "DISPATCH_EXIT=$?" | tee -a /tmp/uat-36a-dispatch.log
       ```
    4. RESEARCH Pitfall 7 — IMMEDIATELY verify the run was actually triggered:
       ```
       sleep 5
       gh run list --workflow=e2e-nightly.yml --limit 1 --json databaseId,status,createdAt,event 2>&1 | tee /tmp/uat-36a-run.json
       ```
       Parse the JSON: `event` must be "workflow_dispatch"; `createdAt` must be < 60 seconds old; `databaseId` is the run id to track.
    5. Watch the run to completion: `gh run watch <run-id> 2>&1 | tee -a /tmp/uat-36a-watch.log` (this may take 10-30 min depending on the corpus + queue).
    6. Inspect step-by-step: `gh run view <run-id> --json jobs -q '.jobs[].steps[] | {name, conclusion}'`. Assert (post-completion):
       - The 5 gated steps (download-llm-report, run-triage-pipeline, e2e-quarantine, quarantine-failure-filer, label-ensure) all show a non-skipped conclusion (success OR failure but NOT skipped)
       - The non-gated golden suite step (smoke, regression, etc.) is also present and ran
    7. Cron-path regression: `gh run list --workflow=e2e-nightly.yml --event=schedule --limit 3 --json conclusion` — confirm recent scheduled runs were not perturbed (this is observational; the byte-identical-regression guarantee is structural per Phase 36 design).
    8. Append evidence to 38-UAT-EVIDENCE.md `## UAT-36a`:
       - `**status:**` PASS or FAIL
       - `**verified_at:** <ISO>`
       - `**command:** gh workflow run e2e-nightly.yml -f llm_run_id=<id>`
       - `**llm_run_id_source:** <where the llm_run_id came from>`
       - `**dispatch_exit:** <code>`
       - `**triggered_run_id:** <id>`
       - `**triggered_run_url:** https://github.com/<owner>/<repo>/actions/runs/<id>`
       - `**event:** workflow_dispatch`
       - `**createdAt_within_60s:** true|false`
       - `**run_conclusion:** <success|failure|...>`
       - `**5_gated_steps_executed:** true|false`
       - Per-step conclusion table for the 5 gated steps
       - Per Plan 38-01 INT-FIX-03 verification: if the run hit a quarantine failure, was the Upload E2E artifacts step gated on quarantine outcome? Capture: `gh run view <id> --json jobs -q '.jobs[].steps[] | select(.name=="Upload E2E artifacts") | .conclusion'`
    9. On FAIL: file follow-up issue.
    10. Commit: `docs(38-03): UAT-36a PASS|FAIL — nightly workflow_dispatch with llm_run_id (post-INT-FIX-03)`.
  </action>
  <resume-signal>Type "approved" once UAT-36a evidence + commit complete. Include triggered_run_url AND the 5-gated-steps boolean.</resume-signal>
  <verify>
    <automated>bash -c 'grep -qE "^\*\*triggered_run_url:\*\*" .planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-UAT-EVIDENCE.md || { echo "UAT-36a triggered_run_url not captured"; exit 1; }; echo "UAT-36a evidence ok"'</automated>
  </verify>
  <done>UAT-36a ran live; gh workflow run dispatched with llm_run_id; gh run list verified the run was actually triggered (Pitfall 7); 5 gated steps executed; evidence section updated; committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: UAT-36b — `npm run e2e:quarantine` local → expect exit 0 (N tests per current corpus state)</name>
  <files>.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-UAT-EVIDENCE.md</files>
  <read_first>
    - 38-UAT-EVIDENCE.md
    - tests/e2e/test-cases-quarantine.js (current corpus state — modified by Task 4 UAT-35b which added one entry)
    - .planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-RESEARCH.md (§"Pitfall 5: UAT-36b empty-corpus expectation" — corpus may be non-empty)
    - tests/e2e/specs/quarantine.spec.js (confirm INT-FIX-01 import is in place from Plan 38-01)
  </read_first>
  <what-built>Phase 36 Plan 36-01 added `tests/e2e/specs/quarantine.spec.js` iterating TEST_CASES_QUARANTINE with `--retries=0 --pass-with-no-tests`. Empty corpus → 0 tests, exit 0. Non-empty corpus → N tests run non-gating. Per Plan 38-01 INT-FIX-01, the spec now imports QUARANTINE_REPORT_FILENAME from the script. UAT confirms local Playwright invocation works post-fix.</what-built>
  <action>
    1. Verify the spec source post-Plan 38-01 INT-FIX-01: `grep -E "import\s+\{[^}]*QUARANTINE_REPORT_FILENAME[^}]*\}\s+from\s+['\"][^'\"]*scripts/e2e-report-issue\.mjs['\"]" tests/e2e/specs/quarantine.spec.js` — expect exit 0.
    2. Check corpus state per Pitfall 5: `grep -c '^  {' tests/e2e/test-cases-quarantine.js` (or whatever pattern captures entry count per Plan 35-02 schema). Note: after Task 4 UAT-35b ran, corpus is NON-EMPTY with one entry — adjust the expected test count from 0 to 1.
    3. Run (executor MUST run):
       ```
       cd /home/fatduck/patent-cite-tool
       npm run e2e:quarantine 2>&1 | tee /tmp/uat-36b.log
       echo "EXIT=$?" | tee -a /tmp/uat-36b.log
       ```
       This invokes `playwright test --config tests/e2e/playwright.config.js specs/quarantine.spec.js --retries=0 --pass-with-no-tests` per package.json line 13.
    4. Assert:
       - Exit code 0 (regardless of test count — `--pass-with-no-tests` + `--retries=0` makes the spec non-gating per QUAR-03)
       - Playwright reports N tests where N matches the corpus entry count from step 2 (typically 1 after UAT-35b, or 0 if UAT-35b was skipped/failed)
       - No crash on import (proves INT-FIX-01 import is wired correctly)
       - Run time < 10 minutes (Pitfall 5 — >10s with N=0 or >10 min with N=1 indicates a regression)
    5. Append evidence to 38-UAT-EVIDENCE.md `## UAT-36b`:
       - `**status:**` PASS or FAIL
       - `**verified_at:** <ISO>`
       - `**command:** npm run e2e:quarantine`
       - `**exit_code:** <code>`
       - `**corpus_entry_count_pre:** <N>`
       - `**tests_run:** <N>`
       - `**playwright_output_excerpt:**` fenced log block
       - `**int_fix_01_import_resolved:** true|false` (verified in step 1)
    6. On FAIL: follow-up issue.
    7. Commit: `docs(38-03): UAT-36b PASS|FAIL — local e2e:quarantine (corpus=<N> tests)`.
  </action>
  <resume-signal>Type "approved" once UAT-36b evidence + commit complete.</resume-signal>
  <verify>
    <automated>bash -c 'grep -qE "^\*\*tests_run:\*\*" .planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-UAT-EVIDENCE.md || { echo "UAT-36b tests_run not captured"; exit 1; }; echo "UAT-36b evidence ok"'</automated>
  </verify>
  <done>UAT-36b ran live; npm run e2e:quarantine exit 0; corpus_entry_count + tests_run captured per Pitfall 5 (adjusted expectation); INT-FIX-01 import resolved; evidence section updated; committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: UAT-37 — `gh workflow run e2e-weekly-digest.yml` → commits reports/weekly-digest-YYYY-WNN.md + Discussion or e2e-digest issue (post-INT-FIX-02 aggregateBySummaryKey)</name>
  <files>.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-UAT-EVIDENCE.md</files>
  <read_first>
    - 38-UAT-EVIDENCE.md
    - .github/workflows/e2e-weekly-digest.yml (confirm `workflow_dispatch: {}` permission and `contents: write` / `discussions: write` / `issues: write`)
    - scripts/weekly-digest.mjs (post-Plan 38-01 INT-FIX-02 — aggregateBySummaryKey now feeds validateSummaryKeys against real data)
    - .planning/phases/37-weekly-analytics-digest/37-03-SUMMARY.md
    - .planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-RESEARCH.md (§"Pitfall 7" — IMMEDIATELY verify the run was triggered)
  </read_first>
  <what-built>Phase 37 Plan 37-03 added `e2e-weekly-digest.yml` Monday-cron + workflow_dispatch with contents/discussions/issues:write. The digest invokes `scripts/weekly-digest.mjs` which (post-Plan 38-01 INT-FIX-02) validates SUMMARY_KEYS against the real aggregateBySummaryKey output. Publishes to Discussion (if enabled) OR e2e-digest issue (fallback). Commits reports/weekly-digest-YYYY-WNN.md to main with [skip ci]. UAT confirms the full live flow post-fix.</what-built>
  <action>
    1. Pre-check Discussions feature state: `gh api repos/<owner>/<repo> -q '.has_discussions'` — record true|false (determines publish branch).
    2. Pre-state file: list current `reports/weekly-digest-*.md` files: `ls reports/weekly-digest-*.md 2>/dev/null > /tmp/uat-37-pre.txt`.
    3. Pre-state issue: `gh issue list --label e2e-digest --state all --limit 5 --json number,createdAt > /tmp/uat-37-pre-issues.json`.
    4. Dispatch (executor MUST run):
       ```
       cd /home/fatduck/patent-cite-tool
       gh workflow run e2e-weekly-digest.yml 2>&1 | tee /tmp/uat-37-dispatch.log
       echo "DISPATCH_EXIT=$?" | tee -a /tmp/uat-37-dispatch.log
       ```
    5. RESEARCH Pitfall 7 — IMMEDIATELY verify:
       ```
       sleep 5
       gh run list --workflow=e2e-weekly-digest.yml --limit 1 --json databaseId,status,createdAt,event 2>&1 | tee /tmp/uat-37-run.json
       ```
       Parse: event=workflow_dispatch; createdAt<60s; databaseId=<run-id>.
    6. Watch to completion: `gh run watch <run-id>` (~2-5 min).
    7. Post-state file: `git pull origin main && ls reports/weekly-digest-*.md > /tmp/uat-37-post.txt && diff /tmp/uat-37-pre.txt /tmp/uat-37-post.txt`. Expect ≥1 new file `reports/weekly-digest-YYYY-WNN.md` for the current ISO week.
    8. Post-state commit: `git log --oneline -5 reports/weekly-digest-*.md`. Confirm the commit message includes `[skip ci]` per DIGEST-03 contract.
    9. Publish-branch verification:
       - If Discussions enabled (step 1 = true): `gh api graphql -f query='query { repository(owner: "<owner>", name: "<repo>") { discussions(first: 5, orderBy: {field: CREATED_AT, direction: DESC}) { nodes { title, url, createdAt } } } }'` — expect a new discussion with title matching the digest pattern.
       - If Discussions disabled (step 1 = false): `gh issue list --label e2e-digest --state all --limit 5 --json number,url,createdAt > /tmp/uat-37-post-issues.json && diff /tmp/uat-37-pre-issues.json /tmp/uat-37-post-issues.json` — expect ≥1 new issue.
    10. Post-INT-FIX-02 verification: the run completed without throwing on SUMMARY_KEYS drift. Capture: `gh run view <run-id> --log | grep -E "(SUMMARY_KEY|validateSummaryKeys|aggregateBySummaryKey)" | head -10` — should show no error throws; if the post-fix path was exercised correctly, the validation passed silently.
    11. Append evidence to 38-UAT-EVIDENCE.md `## UAT-37`:
       - `**status:**` PASS or FAIL
       - `**verified_at:** <ISO>`
       - `**command:** gh workflow run e2e-weekly-digest.yml`
       - `**dispatch_exit:** <code>`
       - `**triggered_run_id:** <id>`
       - `**triggered_run_url:** <url>`
       - `**event:** workflow_dispatch`
       - `**discussions_enabled:** true|false`
       - `**publish_path:** discussion|e2e-digest-issue`
       - `**published_url:** <url>`
       - `**committed_file:** reports/weekly-digest-YYYY-WNN.md`
       - `**commit_skip_ci:** true|false`
       - `**int_fix_02_validation_passed:** true|false` (no throw on SUMMARY_KEYS in run log)
       - `**digest_line_count:** <N>` (must be ≤50 per DIGEST-01 contract — `wc -l reports/weekly-digest-*.md` on the new file)
       - Fenced log block with the new digest's first 20 lines
    12. On FAIL: follow-up issue.
    13. Commit: `docs(38-03): UAT-37 PASS|FAIL — weekly-digest workflow_dispatch (post-INT-FIX-02)`.
  </action>
  <resume-signal>Type "approved" once UAT-37 evidence + commit complete. Include the published URL and the committed file path.</resume-signal>
  <verify>
    <automated>bash -c 'grep -qE "^\*\*published_url:\*\*" .planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-UAT-EVIDENCE.md || { echo "UAT-37 published_url not captured"; exit 1; }; echo "UAT-37 evidence ok"'</automated>
  </verify>
  <done>UAT-37 ran live; gh workflow run dispatched; gh run list verified actual trigger (Pitfall 7); reports/weekly-digest-YYYY-WNN.md committed to main with [skip ci]; Discussion or e2e-digest issue published; INT-FIX-02 validation passed (no SUMMARY_KEYS throw); evidence section updated; committed.</done>
</task>

<task type="auto">
  <name>Task 8: Update .planning/v3.1-MILESTONE-AUDIT.md human_verification: block — add outcome + verified_at per item</name>
  <files>.planning/v3.1-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/v3.1-MILESTONE-AUDIT.md (frontmatter lines 26-40 — current human_verification: block with 7 items; markdown body section "Outstanding Human-Verification Items" lines 160-164)
    - 38-UAT-EVIDENCE.md (post-Tasks-2..7 state — each section now has **status:**, **verified_at:**, and supporting data)
  </read_first>
  <acceptance_criteria>
    - The 7 entries in `human_verification:` (frontmatter) each gain an `outcome: PASS|FAIL|DEFERRED` field AND a `verified_at: <ISO>` field — values sourced from 38-UAT-EVIDENCE.md sections
    - Phase 35 (c) `gh label list` item: `outcome: DONE`, `verified_at:` references the original Plan 35-00 SUMMARY date (NOT the current ISO — this is historical confirmation)
    - Phase 37 live Monday-cron-tick item: `outcome: DEFERRED`, `verified_at:` is current ISO, with a `rationale: <one-line>` field per CONTEXT.md locked decision
    - The other 5 items receive `outcome: PASS` or `outcome: FAIL` matching evidence file `**status:**`
    - Markdown body section "## Outstanding Human-Verification Items" (line 160) is updated: the paragraph noting "Seven live-environment confirmations remain" is replaced with a closing summary "All 7 human-verification items closed in Phase 38: <N> PASS, <M> FAIL (follow-up issues filed), 1 DONE (historical), 1 DEFERRED (rationale)."
    - If any FAIL: a new `tech_debt: - phase: <N>-uat-fail` entry is added with the follow-up issue URL captured during the FAIL handling
    - Audit `audited:` timestamp (line 2) updated
    - `gaps.integration` block UNTOUCHED (Plan 38-01 owns it)
    - `nyquist:` block UNTOUCHED (Plan 38-02 owns it)
    - File still parses as valid YAML+markdown
  </acceptance_criteria>
  <action>
    Step 1: Aggregate outcomes from 38-UAT-EVIDENCE.md. For each of the 7 audit `human_verification:` items, locate the matching evidence section and extract `**status:**` and `**verified_at:**`:
      - audit item phase 32 CR-04 → UAT-32 section
      - audit item phase 35 live filer → UAT-35a section
      - audit item phase 35 quarantine-append → UAT-35b section
      - audit item phase 35 gh label list → UAT-35c (DONE) section
      - audit item phase 36 nightly dispatch → UAT-36a section
      - audit item phase 36 e2e:quarantine empty corpus → UAT-36b section
      - audit item phase 37 weekly-digest → UAT-37 section AND UAT-37-monday-cron (DEFERRED) section
    Step 2: Edit `.planning/v3.1-MILESTONE-AUDIT.md` (Edit tool, region-scoped — DO NOT touch gaps.integration or nyquist:). (a) Update `audited:` timestamp (line 2) to current ISO. (b) For each item in `human_verification:` (frontmatter, lines 26-40), append two (or three for DEFERRED) keys keeping the existing list-item structure:
      ```
        - phase: 32
          item: "..."
          outcome: PASS
          verified_at: 2026-05-29T<HH:MM:SS>Z
      ```
      For the DEFERRED phase-37-cron portion: split the existing entry into TWO entries — one for the workflow_dispatch confirmation (outcome: PASS) and one for the live-cron-tick confirmation (outcome: DEFERRED, rationale: "Clock cannot be advanced; workflow_dispatch surrogate verifies underlying mechanism per CONTEXT.md locked decision."). The original single phase-37 audit item covers both — splitting keeps the YAML aligned with evidence file structure.
      For the phase-35 gh-label item: outcome: DONE, verified_at: <date-from-35-00-SUMMARY.md> (historical), no rationale needed.
    Step 3: Edit markdown body "## Outstanding Human-Verification Items" section (around line 160). REPLACE the paragraph "Seven live-environment confirmations remain..." with a CLOSED summary:
      ```
      ## Human-Verification Items — CLOSED in Phase 38

      All <N> human-verification items closed:
      - <P> PASS
      - <F> FAIL (follow-up issues: <list of URLs>)
      - 1 DONE (Phase 35 label list — historical confirmation per 35-00-SUMMARY.md)
      - 1 DEFERRED (Phase 37 live Monday-cron tick — workflow_dispatch surrogate verifies the underlying mechanism; clock cannot be advanced per CONTEXT.md locked decision)

      Evidence: `.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-UAT-EVIDENCE.md`
      ```
    Step 4: If any FAIL occurred, add a new top-level entry under `tech_debt:`:
      ```
      - phase: <phase>-uat-fail
        items:
          - "UAT-<id> FAIL — follow-up issue: <url>"
      ```
    Step 5: Validate YAML coherence:
      `node -e "const fs=require('fs'); const t=fs.readFileSync('.planning/v3.1-MILESTONE-AUDIT.md','utf8'); const blocks=t.split('---\n'); if(blocks.length!==3) throw new Error('frontmatter malformed'); const fm=blocks[1]; const outcomes=fm.match(/outcome:\s*(PASS|FAIL|DONE|DEFERRED)/g); if(!outcomes||outcomes.length<7) throw new Error('expected ≥7 outcome entries, got '+(outcomes?outcomes.length:0)); console.log('AUDIT human_verification block coherent: '+outcomes.length+' outcomes');"`
    Step 6: Commit: `docs(38-03): close human_verification tech_debt — <N> PASS, <M> FAIL, 1 DONE, 1 DEFERRED`. Stage only `.planning/v3.1-MILESTONE-AUDIT.md`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('.planning/v3.1-MILESTONE-AUDIT.md','utf8'); const blocks=t.split('---\n'); if(blocks.length!==3) throw new Error('frontmatter malformed'); const fm=blocks[1]; const outcomes=fm.match(/outcome:\s*(PASS|FAIL|DONE|DEFERRED)/g); if(!outcomes||outcomes.length<7) throw new Error('expected ≥7 outcome entries, got '+(outcomes?outcomes.length:0)); console.log('AUDIT human_verification: '+outcomes.length+' outcomes');"</automated>
  </verify>
  <done>Audit YAML `human_verification:` block has outcome + verified_at per entry (PASS/FAIL/DONE/DEFERRED) sourced from 38-UAT-EVIDENCE.md; markdown summary updated to CLOSED state; any FAILs surfaced as new tech_debt. gaps.integration and nyquist: blocks untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local shell ↔ GitHub Actions (via gh CLI) | gh CLI uses pre-existing user token; workflow_dispatch invocations execute on real GitHub runners; results read back over network |
| Local shell ↔ Anthropic API (via `claude -p`) | UAT-32 only; uses the Max 5 subscription credit; spend tracked by existing llm-ledger |
| GitHub repo write surface | UAT-35a creates issues; UAT-35b adds labels; UAT-37 commits reports/weekly-digest-*.md to main with [skip ci] |
| Audit YAML edit | Final task edits human_verification: block in place; concurrent with Plans 38-01 + 38-02 final edits to disjoint blocks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03-01 | Spoofing / Tampering | gh workflow run silent on auth failure (RESEARCH Pitfall 7) | mitigate | Every gh workflow run invocation in Tasks 5 + 7 is IMMEDIATELY followed by `gh run list --limit 1 --json databaseId,status,createdAt,event` and the createdAt-within-60s + event=workflow_dispatch assertions. Pre-flight (Task 1) verifies `gh auth status` is fresh. |
| T-38-03-02 | Information Disclosure | UAT log captures including issue body / iteration content | accept | Evidence file commits to the repo and is visible in git history. Content captured is fixture-derived (phase35-triage-report.json) or test output — no production PII surface. Sanitize log excerpts to redact any incidental tokens (none expected from gh CLI / vitest output). |
| T-38-03-03 | Tampering | Audit YAML race against Plans 38-01 + 38-02 final edits | accept | Plans 38-01, 38-02, 38-03 each edit DISJOINT YAML blocks (gaps.integration vs nyquist: vs human_verification:) and DISJOINT markdown sections. Plan 38-03 is in Wave 2 (depends on 38-01), so 38-01 has completed before this plan starts; 38-02 in Wave 1 may overlap — but the disjoint-block rule still applies. Final-task acceptance criteria explicitly forbid touching the other plans' blocks. If a merge conflict occurs at commit time, the second plan rebases — no semantic conflict. |
| T-38-03-04 | Repudiation | CLAUDE.md C1 violation — fabricating an AskUserQuestion answer (e.g. for Task 5 step 2 "no source llm-report" prompt) | mitigate | Task 5 explicitly invokes CLAUDE.md C1/C2/C3 — when the executor needs to pick between (a) trigger non-llm nightly first, (b) upload fixture, (c) defer — and AskUserQuestion returns empty/generic, the fallback numbered-list prompt is required. Auto-picking (Recommended) is prohibited. |
| T-38-03-05 | Tampering | UAT-35b mutates tests/e2e/test-cases-quarantine.js (real corpus side-effect) | accept | Per Plan 35-04 contract: this is the expected behavior of `quarantine-append.mjs`. The mutated corpus is committed in Task 4 alongside the evidence file. UAT-36b in Task 6 reads the post-UAT-35b state and adjusts expectation accordingly (per RESEARCH Pitfall 5). |
| T-38-03-06 | Elevation of Privilege | UAT-37 commits to main with [skip ci] | accept | Phase 37 Plan 37-03 explicitly configured `contents: write` on e2e-weekly-digest.yml; the [skip ci] commit is the intended publish mechanism per DIGEST-03 contract. UAT exercises the live path — no new privilege grant. |
| T-38-03-SC | Tampering | npm/pip/cargo installs | accept | Phase 38 installs ZERO new packages. UAT track uses only `gh` CLI, `node`, `npm run` invocations against existing scripts. |
</threat_model>

<verification>
After all 8 tasks complete:
1. 38-UAT-EVIDENCE.md exists with 8 sections, each carrying status (PASS|FAIL|DONE|DEFERRED) + verified_at: `grep -cE "^\*\*status:\*\*\s+(PASS|FAIL|DONE|DEFERRED)" 38-UAT-EVIDENCE.md` returns 8.
2. Audit YAML `human_verification:` block has ≥7 outcome entries (Task 8 verify).
3. Markdown "Outstanding Human-Verification Items" section updated to CLOSED summary.
4. Any FAILs surface as new tech_debt entries.
5. Plan 38-01's `gaps.integration` and Plan 38-02's `nyquist:` blocks UNTOUCHED.
6. Per CLAUDE.md C1/C2/C3: any AskUserQuestion fallback usage is recorded in 38-UAT-EVIDENCE.md per the relevant section.
7. `npm run test:src` exits 0 (no regression from corpus mutation or any incidental file changes).
</verification>

<success_criteria>
- 6 live UAT confirmations executed (UAT-32, -35a, -35b, -36a, -36b, -37) — each with command output, exit code, run URL or file path, and PASS/FAIL outcome recorded in 38-UAT-EVIDENCE.md
- 1 historical confirmation (UAT-35c) recorded as DONE with reference to 35-00-SUMMARY.md
- 1 cron-tick confirmation (UAT-37-monday-cron) recorded as DEFERRED with rationale per CONTEXT.md locked decision
- Every `gh workflow run` invocation followed by `gh run list --limit 1` verification per RESEARCH Pitfall 7
- `.planning/v3.1-MILESTONE-AUDIT.md` `human_verification:` block updated with outcome + verified_at per item
- Markdown body section updated to CLOSED state
- Any FAIL handled per CONTEXT.md locked decision: capture in evidence + open follow-up issue + continue (do NOT block)
- Plan 38-01's `gaps.integration` and Plan 38-02's `nyquist:` audit blocks untouched
</success_criteria>

<output>
Create `.planning/phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-03-SUMMARY.md` per the standard template with:
- Per-UAT outcome table: | UAT-ID | Command | Exit | Outcome | Evidence section | Run URL or Issue/file | Notes |
- Total: <P> PASS, <F> FAIL, 1 DONE, 1 DEFERRED
- Any follow-up issues opened (URLs)
- Any AskUserQuestion fallback usages (per CLAUDE.md C1/C2/C3) — when, what prompt was issued, what answer was received
- Audit-YAML edit diff summary (before/after for human_verification: block)
</output>
