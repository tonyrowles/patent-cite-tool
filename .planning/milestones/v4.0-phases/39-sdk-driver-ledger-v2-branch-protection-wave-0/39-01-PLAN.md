---
phase: 39-sdk-driver-ledger-v2-branch-protection-wave-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/lib/llm-ledger.js
  - tests/e2e/lib/llm-pricing.js
  - tests/unit/llm-ledger.test.js
autonomous: true
requirements:
  - LEDGER-01
  - LEDGER-02
  - LEDGER-03
tags:
  - ledger
  - cost-cap
  - vitest

must_haves:
  truths:
    - "appendLedgerEntry accepts additive transport ('subscription'|'sdk') and phase (string) fields without breaking any existing v3.1 callsite"
    - "combinedMonthlyTotal(ledger) returns the unified cross-transport monthly total against the same $80 warn / $100 hard cap"
    - "checkDayCap, checkIssueCap, checkPrCap block at exactly $10.00 / $1.00 / $2.00 respectively"
    - "claude-sonnet-4-6 in PRICING_BY_MODEL returns Sonnet rates ($3 input / $15 output per Mtok), not Opus default fallback"
    - "existing v3.1 ledger tests (cases 1–33) continue to pass without modification"
  artifacts:
    - path: "tests/e2e/lib/llm-ledger.js"
      provides: "transport+phase passthrough, combinedMonthlyTotal, combinedMonthlyTotalByTransport, currentIsoDay, dayTotal/issueTotal/prTotal, checkDayCap/checkIssueCap/checkPrCap, DAY_HARD_CAP_USD=10, ISSUE_HARD_CAP_USD=1, PR_HARD_CAP_USD=2"
      exports:
        - combinedMonthlyTotal
        - combinedMonthlyTotalByTransport
        - currentIsoDay
        - dayTotal
        - issueTotal
        - prTotal
        - checkDayCap
        - checkIssueCap
        - checkPrCap
        - DAY_HARD_CAP_USD
        - ISSUE_HARD_CAP_USD
        - PR_HARD_CAP_USD
    - path: "tests/e2e/lib/llm-pricing.js"
      provides: "claude-sonnet-4-6 entry in PRICING_BY_MODEL ($3 input / $15 output per Mtok)"
      contains: "claude-sonnet-4-6"
    - path: "tests/unit/llm-ledger.test.js"
      provides: "Phase 39 LEDGER-01/02/03 test coverage (transport field, combinedMonthlyTotal, sub-cap boundaries)"
      min_lines: 800
  key_links:
    - from: "tests/e2e/lib/llm-ledger.js"
      to: "tests/e2e/lib/llm-pricing.js"
      via: "existing PRICING_BY_MODEL import + re-export"
      pattern: "PRICING_BY_MODEL"
    - from: "tests/unit/llm-ledger.test.js"
      to: "tests/e2e/lib/llm-ledger.js"
      via: "named imports including new exports"
      pattern: "from '../e2e/lib/llm-ledger.js'"
---

<objective>
Extend `tests/e2e/lib/llm-ledger.js` with the v4.0 ledger-v2 schema (additive
`transport` + `phase` fields, per-day/per-issue/per-PR sub-caps,
`combinedMonthlyTotal` unified-cap reader) and add the `claude-sonnet-4-6`
pricing entry so the Phase 39 SDK driver (Plan 03) cannot fall through to
default Opus pricing on its first call.

Purpose: Lay the cost-runaway-defense foundation (PITFALLS Pitfall 2) before
the SDK driver lands. Ledger primitives are pure functions consumed by both
transports; Plan 03's `invokeAnthropicSdkWithLedger` imports them directly.

Output: 9 new exports in llm-ledger.js, 1 new pricing entry in llm-pricing.js,
~14 new Vitest cases extending the existing tests/unit/llm-ledger.test.js
(cases 34–47 per the Validation Architecture table in 39-RESEARCH.md).
</objective>

<execution_context>
@/home/fatduck/.claude/get-shit-done/workflows/execute-plan.md
@/home/fatduck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-sdk-driver-ledger-v2-branch-protection-wave-0/39-CONTEXT.md
@.planning/phases/39-sdk-driver-ledger-v2-branch-protection-wave-0/39-RESEARCH.md
@tests/e2e/lib/llm-ledger.js
@tests/e2e/lib/llm-pricing.js
@tests/unit/llm-ledger.test.js

<interfaces>
<!-- Existing v3.1 surface that Plan 01 extends additively. -->
<!-- Source: tests/e2e/lib/llm-ledger.js (direct inspection 2026-05-30). -->

From tests/e2e/lib/llm-ledger.js (existing v3.1 exports, DO NOT MODIFY shapes):
```js
export const LEDGER_PATH;                 // string — resolves to tests/e2e/.llm-spend-ledger.json
export const HARD_CAP_USD = 100;
export const WARN_THRESHOLD_USD = 80;
export const PHASE_HARD_CAP_USD = 10;
export const PHASE_WARN_THRESHOLD_USD = 8;
export function currentMonth(): string;   // "YYYY-MM" (UTC)
export function readLedger(ledgerPath?): { version: 1, months: Record<string, MonthBucket> };
export function monthlyTotal(ledger, month?): number;
export function phaseTotal(ledger, phase): number;                       // cross-month
export function checkSpendCap(ledger, month?): { status, monthly_total_usd, month, message };
export function checkPhaseSpendCap(ledger, phase): { status, phase_total_usd, phase, message };
export function appendLedgerEntry(ledgerPath, entry): void;              // spreads entry into iterations[]
```

Entry shape (existing — `appendLedgerEntry` jsdoc lines 292–317):
```
{ iso, model, cost_usd, tokens_in?, tokens_out?, iteration_n?, run_id?, phase?, source? }
```
Phase 39 ADDS optional `transport: 'subscription' | 'sdk'`, `issueId?: string`,
`prNumber?: number`, `cache_creation_tokens?: number`, `cache_read_tokens?: number`,
`error?: string` (free-form per A8 in 39-RESEARCH.md; back-compat preserved).

From tests/e2e/lib/llm-pricing.js (existing — extend the frozen map):
```js
export const PRICING_BY_MODEL = Object.freeze({
  'claude-opus-4-7[1m]':   Object.freeze({ input_per_mtok: 15, output_per_mtok: 75 }),
  'claude-opus-4-7':       Object.freeze({ input_per_mtok: 15, output_per_mtok: 75 }),
  'claude-sonnet-4-5':     Object.freeze({ input_per_mtok: 3,  output_per_mtok: 15 }),
  'default':               Object.freeze({ input_per_mtok: 15, output_per_mtok: 75 }),
});
export function fallbackCostUsd(modelId, inputTokens, outputTokens): number;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ledger-v2 sub-cap constants, helpers, and transport-field passthrough</name>
  <files>tests/e2e/lib/llm-ledger.js</files>
  <read_first>
    - tests/e2e/lib/llm-ledger.js (entire file — must preserve every existing export/jsdoc/comment; only APPEND new exports)
    - tests/e2e/lib/llm-driver.js lines 248–276 + 318–370 (canonical shape for cap-check + appendLedgerEntry that the new helpers mirror)
    - .planning/phases/39-sdk-driver-ledger-v2-branch-protection-wave-0/39-RESEARCH.md §"Pattern 2: Pure-Function Sub-Cap Helpers" and §"Pattern 3: Combined-Monthly-Total Helper"
    - .planning/phases/39-sdk-driver-ledger-v2-branch-protection-wave-0/39-CONTEXT.md §"Implementation Decisions" (cap values $10/$1/$2 LOCKED, ledger path LOCKED)
  </read_first>
  <behavior>
    - DAY_HARD_CAP_USD === 10, ISSUE_HARD_CAP_USD === 1, PR_HARD_CAP_USD === 2 (exact numeric constants, no warn threshold per LEDGER-03 — sub-caps are binary)
    - currentIsoDay() returns "YYYY-MM-DD" UTC prefix of new Date().toISOString()
    - dayTotal(ledger, isoDay = currentIsoDay()) sums cost_usd of iterations[] across ALL month buckets whose iso starts with isoDay; ignores non-finite cost_usd; returns +sum.toFixed(6)
    - issueTotal(ledger, issueId) sums cost_usd of iterations[] across ALL month buckets matching it.issueId === issueId; defensive filtering identical to phaseTotal
    - prTotal(ledger, prNumber) same shape as issueTotal but matches it.prNumber === prNumber (strict ===, allows numeric IDs)
    - checkDayCap(ledger, isoDay) returns { status: 'block', day_total_usd, iso_day, message } when total >= 10; { status: 'ok', day_total_usd, iso_day, message: '' } otherwise (NO warn state — sub-caps are binary per CONTEXT)
    - checkIssueCap(ledger, issueId) returns same shape with issue_total_usd + issue_id keys when total >= 1
    - checkPrCap(ledger, prNumber) returns same shape with pr_total_usd + pr_number keys when total >= 2
    - combinedMonthlyTotal(ledger, month = currentMonth()) === monthlyTotal(ledger, month) (thin wrapper signaling unified-cap intent)
    - combinedMonthlyTotalByTransport(ledger, month) returns { combined, by_transport: { subscription, sdk, unknown } } partitioning iterations[].transport (default 'subscription' for pre-Phase-39 entries lacking the field per A8)
    - appendLedgerEntry SHAPE UNCHANGED — already spreads entry verbatim per lines 308–311 jsdoc; the new transport/issueId/prNumber/cache_creation_tokens/cache_read_tokens/error fields pass through automatically with no code change required (LEDGER-01 is satisfied by the existing spread); add a comment near line 311 documenting the new optional fields
  </behavior>
  <action>
    APPEND new exports to `tests/e2e/lib/llm-ledger.js` AFTER the existing `checkPhaseSpendCap` (line 276) and BEFORE `appendLedgerEntry` (line 318). Per D-13/D-14/D-15 lock in CONTEXT and 39-RESEARCH.md Pattern 2/3:

    1. Three sub-cap constants (constants placed near the existing PHASE_*_CAP block at line 117–124):
       - `export const DAY_HARD_CAP_USD = 10;`  // per-day sub-cap (CONTEXT locked)
       - `export const ISSUE_HARD_CAP_USD = 1;` // per-issue sub-cap (CONTEXT locked)
       - `export const PR_HARD_CAP_USD = 2;`    // per-PR sub-cap (CONTEXT locked)

    2. `export function currentIsoDay()` returning `new Date().toISOString().slice(0, 10)` (UTC; mirrors `currentMonth()` at line 129).

    3. `export function dayTotal(ledger, isoDay = currentIsoDay())` — iterate ledger.months[*].iterations[]; sum `cost_usd` for entries where `typeof it.iso === 'string' && it.iso.startsWith(isoDay) && Number.isFinite(it.cost_usd)`; return `+sum.toFixed(6)`. Mirror the defensive shape of `phaseTotal` at lines 178–192.

    4. `export function issueTotal(ledger, issueId)` — same shape; filter on `it.issueId === issueId`.

    5. `export function prTotal(ledger, prNumber)` — same shape; filter on `it.prNumber === prNumber` (strict ===).

    6. `export function checkDayCap(ledger, isoDay = currentIsoDay())` — returns `{ status: 'block', day_total_usd: total, iso_day: isoDay, message: 'Day ${isoDay} LLM spend $${total.toFixed(2)} >= $${DAY_HARD_CAP_USD.toFixed(2)}. Refusing further invocations until next UTC day.' }` when `total >= DAY_HARD_CAP_USD`, else `{ status: 'ok', day_total_usd: total, iso_day: isoDay, message: '' }`. NO warn state (per LEDGER-03 — sub-caps are binary). Distinct key names (`day_total_usd`/`iso_day`) NOT `monthly_total_usd`/`month` so callers cannot mix the two views (mirrors discipline at line 240 jsdoc).

    7. `export function checkIssueCap(ledger, issueId)` — same shape with `issue_total_usd` + `issue_id` keys, `ISSUE_HARD_CAP_USD`, message names the issue.

    8. `export function checkPrCap(ledger, prNumber)` — same shape with `pr_total_usd` + `pr_number` keys, `PR_HARD_CAP_USD`, message names the PR.

    9. `export function combinedMonthlyTotal(ledger, month = currentMonth())` — thin wrapper: `return monthlyTotal(ledger, month);`. JSDoc explains "presently identical to monthlyTotal — wrapper exists to signal cap-check intent and reserve a hook for future per-transport breakdown" (per 39-RESEARCH.md Pattern 3).

    10. `export function combinedMonthlyTotalByTransport(ledger, month = currentMonth())` — partition iterations[] by `entry.transport` (default `'subscription'` for back-compat). Return shape exactly: `{ combined: bucket?.total_usd ?? 0, by_transport: { subscription: number, sdk: number, unknown: number } }`. Round each `by_transport[k]` to 6dp via `+x.toFixed(6)`. Treat `transport === 'sdk'` → sdk; `transport === 'subscription'` → subscription; any other (including absent) → if absent or === 'subscription' → subscription; else → unknown. The DEFAULT-for-absent-field choice is per A8 in 39-RESEARCH.md (back-compat with pre-Phase-39 entries — they were all subscription).

    11. Append a JSDoc note ABOVE `appendLedgerEntry` (lines 278–317) documenting the new optional fields the spread now propagates: `transport: 'subscription' | 'sdk'`, `issueId?: string` (e.g. 'issue-123'), `prNumber?: number`, `cache_creation_tokens?: number`, `cache_read_tokens?: number`, `error?: string`. NO code change to the function body — the existing spread at line 337 (`m.iterations.push(entry)`) already passes everything through (LEDGER-01 satisfied by inspection).

    Pure functions only — NO new imports of node:fs, node:path, node:child_process. Keep the file pure-function discipline intact.
  </action>
  <verify>
    <automated>
node -e "import('./tests/e2e/lib/llm-ledger.js').then(m => { const names = ['DAY_HARD_CAP_USD','ISSUE_HARD_CAP_USD','PR_HARD_CAP_USD','currentIsoDay','dayTotal','issueTotal','prTotal','checkDayCap','checkIssueCap','checkPrCap','combinedMonthlyTotal','combinedMonthlyTotalByTransport']; for (const n of names) { if (m[n] === undefined) { console.error('MISSING:', n); process.exit(1); } } if (m.DAY_HARD_CAP_USD !== 10 || m.ISSUE_HARD_CAP_USD !== 1 || m.PR_HARD_CAP_USD !== 2) { console.error('BAD CAP VALUES'); process.exit(1); } console.log('OK'); }).catch(e => { console.error(e); process.exit(1); });" \
  && grep -v '^#' tests/e2e/lib/llm-ledger.js | grep -c "DAY_HARD_CAP_USD = 10" \
  && grep -v '^#' tests/e2e/lib/llm-ledger.js | grep -c "ISSUE_HARD_CAP_USD = 1" \
  && grep -v '^#' tests/e2e/lib/llm-ledger.js | grep -c "PR_HARD_CAP_USD = 2"
    </automated>
  </verify>
  <done>
    All 12 new exports importable from tests/e2e/lib/llm-ledger.js with the exact numeric cap values $10/$1/$2; existing v3.1 exports untouched (no diff hunks deleting or modifying lines 1–276 of the original file); appendLedgerEntry body unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add claude-sonnet-4-6 entry to PRICING_BY_MODEL (Pitfall-2 calibration fix)</name>
  <files>tests/e2e/lib/llm-pricing.js</files>
  <read_first>
    - tests/e2e/lib/llm-pricing.js (lines 36–41 — the frozen map this task extends)
    - .planning/phases/39-sdk-driver-ledger-v2-branch-protection-wave-0/39-RESEARCH.md §"Pitfall 2: SDK pricing constants out-of-sync with model name in response" (A2 + Pitfall 2 explain why this MUST land in Phase 39, not deferred)
    - .planning/phases/39-sdk-driver-ledger-v2-branch-protection-wave-0/39-CONTEXT.md §"Locked Decisions" (default model `claude-sonnet-4-6` LOCKED; Sonnet rates $3 input / $15 output per Mtok per STACK.md)
  </read_first>
  <behavior>
    - `PRICING_BY_MODEL['claude-sonnet-4-6']` returns `{ input_per_mtok: 3, output_per_mtok: 15 }` (Sonnet rates, not default Opus fallback)
    - `fallbackCostUsd('claude-sonnet-4-6', 1_000_000, 1_000_000)` returns `+(3 + 15).toFixed(6) === 18` (exact Sonnet math, NOT 90 which would be the Opus default fallback)
    - existing entries (`claude-opus-4-7[1m]`, `claude-opus-4-7`, `claude-sonnet-4-5`, `default`) unchanged
    - Object.freeze on outer map preserved
  </behavior>
  <action>
    Edit `tests/e2e/lib/llm-pricing.js` to ADD one entry to `PRICING_BY_MODEL` (line 36–41). Insert AFTER the existing `'claude-sonnet-4-5'` line (line 39) and BEFORE the `'default'` line (line 40):

    `'claude-sonnet-4-6':     Object.freeze({ input_per_mtok: 3,  output_per_mtok: 15 }),`

    Do NOT modify the existing entries. Do NOT change the `default` entry (must remain Opus-priced as the over-counting safety per the existing jsdoc lines 30–32). Update the "Rates last updated:" comment at line 18 to `2026-05 (Opus 4.7, Sonnet 4.5, Sonnet 4.6 per CONTEXT lock 39-CONTEXT.md)`.

    Justification: per Pitfall 2 in 39-RESEARCH.md, without this entry the FIRST SDK call from Plan 03's `invokeAnthropicSdkWithLedger` would fall through to `PRICING_BY_MODEL.default` (Opus $15/$75) and over-count cost by 5×. The auto-fix workflow would then trip the day cap incorrectly. This is NOT the deferred per-model-extraction work (which is a structural refactor); it is a one-line addition to the existing frozen map.
  </action>
  <verify>
    <automated>
node -e "import('./tests/e2e/lib/llm-pricing.js').then(m => { const sonnet46 = m.PRICING_BY_MODEL['claude-sonnet-4-6']; if (!sonnet46) { console.error('MISSING claude-sonnet-4-6'); process.exit(1); } if (sonnet46.input_per_mtok !== 3 || sonnet46.output_per_mtok !== 15) { console.error('BAD RATES:', sonnet46); process.exit(1); } const cost = m.fallbackCostUsd('claude-sonnet-4-6', 1_000_000, 1_000_000); if (cost !== 18) { console.error('EXPECTED 18, GOT', cost); process.exit(1); } console.log('OK'); }).catch(e => { console.error(e); process.exit(1); });"
    </automated>
  </verify>
  <done>
    `PRICING_BY_MODEL['claude-sonnet-4-6']` returns `{ input_per_mtok: 3, output_per_mtok: 15 }`; `fallbackCostUsd('claude-sonnet-4-6', 1_000_000, 1_000_000) === 18` exactly; existing 4 entries unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend tests/unit/llm-ledger.test.js with Phase 39 sub-cap + transport + combined-total coverage</name>
  <files>tests/unit/llm-ledger.test.js</files>
  <read_first>
    - tests/unit/llm-ledger.test.js (entire file — must preserve every existing case 1–33; only APPEND new describe blocks)
    - tests/e2e/lib/llm-ledger.js (the new exports added by Task 1 — same file Task 1 modifies; the test must NOT duplicate behavior the implementation has not added)
    - .planning/phases/39-sdk-driver-ledger-v2-branch-protection-wave-0/39-RESEARCH.md §"Pattern 5: Vitest tmpDir-per-test for cap boundary tests" (canonical tmpDir/makeEntry pattern at lines 80–100 of the existing test file)
    - tests/e2e/lib/llm-pricing.js (Task 2 — to confirm sonnet-4-6 entry exists when the pricing-touchpoint test runs)
  </read_first>
  <behavior>
    - All existing cases (1–33) continue to pass with byte-for-byte identical assertions
    - 14 new cases added (cases 34–47) organized into 4 describe blocks
    - Tests use the existing tmpDir/ledgerPath beforeEach/afterEach pattern at lines 80–87 verbatim
    - Tests use the existing makeEntry() helper at line 89, extending with overrides for new fields
    - Every new test runs in < 50ms (pure in-memory ledger ops)
  </behavior>
  <action>
    APPEND to `tests/unit/llm-ledger.test.js` (do NOT modify any existing lines). Add 4 new top-level `describe` blocks AFTER the existing final closing `});` of the file, with these cases:

    1. Imports header — extend the existing import block (lines 62–75) to also import: `DAY_HARD_CAP_USD, ISSUE_HARD_CAP_USD, PR_HARD_CAP_USD, currentIsoDay, dayTotal, issueTotal, prTotal, checkDayCap, checkIssueCap, checkPrCap, combinedMonthlyTotal, combinedMonthlyTotalByTransport`. Do NOT touch existing imports.

    2. `describe('Phase 39 LEDGER-01: appendLedgerEntry transport + ledger-v2 fields (additive)')`:
       - Case 34: appendLedgerEntry with `transport: 'sdk'` persists the field on the iteration; readLedger then verifies `iterations[0].transport === 'sdk'`
       - Case 35: appendLedgerEntry with `transport: 'subscription'` persists; readLedger verifies
       - Case 36: appendLedgerEntry WITHOUT transport stores no `transport` property on the iteration (back-compat assertion: `'transport' in iterations[0]` === false)
       - Case 37: appendLedgerEntry with `issueId: 'issue-42'`, `prNumber: 7`, `cache_creation_tokens: 100`, `cache_read_tokens: 200`, `error: 'sdk timeout'` — every field round-trips through the spread

    3. `describe('Phase 39 LEDGER-02: combinedMonthlyTotal + combinedMonthlyTotalByTransport')`:
       - Case 38: empty ledger → `combinedMonthlyTotal` returns 0; `combinedMonthlyTotalByTransport` returns `{ combined: 0, by_transport: { subscription: 0, sdk: 0, unknown: 0 } }`
       - Case 39: append one subscription entry at $0.50 + one sdk entry at $0.75; `combinedMonthlyTotal` returns 1.25; `combinedMonthlyTotalByTransport.by_transport` returns `{ subscription: 0.5, sdk: 0.75, unknown: 0 }`
       - Case 40: append entry WITHOUT transport at $0.30 → partitioned into `subscription` bucket (back-compat default per A8); `combinedMonthlyTotal === 0.3`
       - Case 41: append entry with `transport: 'mystery'` → partitioned into `unknown` bucket

    4. `describe('Phase 39 LEDGER-03: checkDayCap binary boundary at $10.00')`:
       - Case 42: ok when day total $5.00; `result.status === 'ok'`, `result.day_total_usd === 5`
       - Case 43: block at exactly $10.00 inclusive; `result.status === 'block'`, `result.day_total_usd === 10`, `result.message` contains `'$10.00'`
       - Case 44: filters by isoDay — append at `'2026-05-29T23:59:00Z'` and `'2026-05-30T00:00:00Z'`, `checkDayCap(readLedger(ledgerPath), '2026-05-30').day_total_usd === 5.0`

    5. `describe('Phase 39 LEDGER-03: checkIssueCap and checkPrCap binary boundaries')`:
       - Case 45: checkIssueCap blocks at exactly $1.00 inclusive (issueId='issue-99'); return-shape uses `issue_total_usd` and `issue_id` (NOT `cost_usd`/`id`) per the discipline mirror at line 240 of llm-ledger.js
       - Case 46: checkIssueCap ignores entries from other issueIds (append issue-1 at $0.99, issue-2 at $0.99; checkIssueCap for issue-1 returns ok)
       - Case 47: checkPrCap blocks at exactly $2.00 inclusive (prNumber=123); return-shape uses `pr_total_usd` and `pr_number`

    For Cases 34–37 (transport field) use `makeEntry({ transport: 'sdk', ... })` etc. For Cases 42–47 use `makeEntry({ cost_usd, iso, issueId, prNumber })` — extend the existing `makeEntry` overrides argument; do NOT modify the helper itself.

    The test file already has a `combinedMonthlyTotal` reference NOWHERE so all new test names must be unique. Use the established naming `it('Test NN: <behavior>', () => {...})` matching the existing style.
  </action>
  <verify>
    <automated>
npx vitest run tests/unit/llm-ledger.test.js --reporter=basic 2>&1 | tail -20 \
  && npx vitest run tests/unit/llm-ledger.test.js -t "Phase 39 LEDGER-01" --reporter=basic 2>&1 | grep -E "✓.*Test 3[4-7]" | wc -l | grep -q "^4$" \
  && npx vitest run tests/unit/llm-ledger.test.js -t "Phase 39 LEDGER-02" --reporter=basic 2>&1 | grep -E "✓.*Test 4[01]|✓.*Test 3[89]" | wc -l | grep -q "^4$" \
  && npx vitest run tests/unit/llm-ledger.test.js -t "Phase 39 LEDGER-03" --reporter=basic 2>&1 | grep -E "✓.*Test 4[2-7]" | wc -l | grep -q "^6$"
    </automated>
  </verify>
  <done>
    All 14 new Phase 39 test cases pass (34–47); all 33 existing v3.1 cases still pass; total Vitest run exits 0; no edits to lines 1–end-of-existing-tests (only appended describe blocks).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → ledger | Untrusted entry fields (cost_usd, transport, phase, issueId, prNumber) cross into the spread; appendLedgerEntry trusts the caller (single-process developer use per file jsdoc line 23–26) |
| transport tag → cap math | A malicious transport tag could mis-bucket spend in combinedMonthlyTotalByTransport; combined total stays correct because it reads bucket.total_usd directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Denial of Service (budget) | Per-day / per-issue / per-PR cost runaway (Pitfall 2 — single-typo cron = 288×/day) | mitigate | Three new binary sub-caps (DAY $10 / ISSUE $1 / PR $2) at-or-above the threshold returns `status: 'block'`; Vitest cases 42/45/47 exercise inclusive boundary |
| T-39-02 | Information Integrity | First SDK call falls through to default Opus pricing → cost over-counted 5× → day cap fires spuriously | mitigate | Task 2 adds `claude-sonnet-4-6` to PRICING_BY_MODEL with verified $3/$15 rates; Vitest in Task 2's verify asserts the exact value |
| T-39-03 | Tampering | Caller passes `transport: 'invalid_value'` to appendLedgerEntry | accept | additive-only schema per CONTEXT (V5 input-validation partial scope); combinedMonthlyTotalByTransport buckets unknown values into `unknown` so they remain visible without corrupting the combined sum |
| T-39-04 | Information Disclosure | Existing v3.1 ledger entries leak through `combinedMonthlyTotalByTransport.by_transport.subscription` when committed in Plan 04 | accept | Per Open Questions §2 in 39-RESEARCH.md, committed ledger is fresh-start with only the bootstrap entry (Plan 04); v3.1 local history not migrated. Plan 04 enforces this. |
</threat_model>

<verification>
- `npx vitest run tests/unit/llm-ledger.test.js` exits 0
- `npm run lint` passes (no new imports introduced beyond what's already in llm-ledger.js / llm-pricing.js; ESLint guard on `@anthropic-ai/sdk` from Plan 03 not yet active)
- `grep -c "^export" tests/e2e/lib/llm-ledger.js` increases by exactly 12 over the pre-task baseline (3 constants + 1 currentIsoDay + 3 totals + 3 caps + 2 combined helpers)
- No deletions from tests/e2e/lib/llm-ledger.js or tests/e2e/lib/llm-pricing.js (additive-only)
</verification>

<success_criteria>
- LEDGER-01: appendLedgerEntry passes `transport` and `phase` fields through to iterations[] verbatim; existing v3.1 callers unaffected; Vitest cases 34–37 PASS
- LEDGER-02: `combinedMonthlyTotal(ledger)` returns unified cross-transport monthly total; `combinedMonthlyTotalByTransport(ledger)` returns the forensic-grep breakdown; both transports counted against the SAME $80/$100 thresholds (verified by Vitest cases 38–41)
- LEDGER-03: `checkDayCap`/`checkIssueCap`/`checkPrCap` refuse at $10/$1/$2 boundary inclusively; cases 42–47 PASS
- `claude-sonnet-4-6` resolves to Sonnet rates in PRICING_BY_MODEL (not default Opus); cost(1M in + 1M out) === $18 (Pitfall 2 mitigation lands in Phase 39)
- Pure-function discipline preserved (no new node:fs / node:child_process / node:path imports in llm-ledger.js)
</success_criteria>

<output>
Create `.planning/phases/39-sdk-driver-ledger-v2-branch-protection-wave-0/39-01-SUMMARY.md` when done. Document: (1) the 12 new exports, (2) the 14 new test cases, (3) confirmation that existing 33 v3.1 cases still pass byte-for-byte, (4) the pricing-map addition rationale (Pitfall 2).
</output>
