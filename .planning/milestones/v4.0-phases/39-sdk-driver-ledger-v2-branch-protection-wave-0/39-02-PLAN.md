---
phase: 39-sdk-driver-ledger-v2-branch-protection-wave-0
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/CODEOWNERS
  - docs/v40-repo-config.md
  - tests/unit/codeowners.test.js
autonomous: true
requirements:
  - CLEANUP-04
tags:
  - codeowners
  - branch-protection
  - repo-config

must_haves:
  truths:
    - ".github/CODEOWNERS exists at the canonical GitHub-recognised path and pins all 5 locked paths to @tonyrowles"
    - "docs/v40-repo-config.md documents the manual repo-settings checklist (Allow auto-merge OFF, branch protection ruleset on main with Do not allow bypassing ON and an empty required-status-check list slot reserved for Phase 41) with verification gh api commands captured for Phase 47's audit"
    - "tests/unit/codeowners.test.js statically asserts every one of the 5 locked path strings appears verbatim and @tonyrowles is the owner on each line"
  artifacts:
    - path: ".github/CODEOWNERS"
      provides: "Five locked path pins to @tonyrowles (src/, tests/, .github/workflows/, tests/golden/, tests/e2e/test-cases-quarantine.js)"
      contains: "@tonyrowles"
    - path: "docs/v40-repo-config.md"
      provides: "Manual repo-settings audit reference for CLEANUP-04 (initial setup) and Phase 47 re-audit"
      min_lines: 60
    - path: "tests/unit/codeowners.test.js"
      provides: "Static-grep assertion that the 5 locked CODEOWNERS lines exist and point to @tonyrowles"
      min_lines: 60
  key_links:
    - from: "tests/unit/codeowners.test.js"
      to: ".github/CODEOWNERS"
      via: "fs.readFileSync + line-by-line includes assertion"
      pattern: "CODEOWNERS"
    - from: "docs/v40-repo-config.md"
      to: "GitHub repo Settings (manual UI)"
      via: "gh api GET commands documented as the audit-time reads"
      pattern: "gh api GET /repos/"
---

<objective>
Land the v4.0 trust-invariant repo-config artifacts that live IN the repo:
`.github/CODEOWNERS` pinning the 5 locked paths to `@tonyrowles`, a manual-
settings audit-reference doc (`docs/v40-repo-config.md`), and a static-grep
Vitest test that the CODEOWNERS contents stay pinned across the milestone.

Purpose: Per CLEANUP-04, Phase 39 SETS UP branch-protection + CODEOWNERS;
Phase 47 RE-AUDITS the same artifacts via `gh api`. This plan ships every
file Phase 47 will assert on. The actual `Settings → Allow auto-merge: OFF`
and branch-protection-ruleset checkboxes are GitHub UI clicks (live in repo
Settings, NOT git-tracked) — they are the responsibility of Plan 04's
human-action checkpoint AFTER the in-repo artifacts land.

This plan is INDEPENDENT of Plans 01 and 03 (zero shared files) — runs in
Wave 1 with Plan 01.

Output: 1 CODEOWNERS file (5 pinned paths), 1 audit-reference doc with the
exact `gh api` snapshot commands, 1 new Vitest unit test (~7 assertions).
</objective>

<execution_context>
@/home/fatduck/.claude/get-shit-done/workflows/execute-plan.md
@/home/fatduck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-sdk-driver-ledger-v2-branch-protection-wave-0/39-CONTEXT.md
@.planning/phases/39-sdk-driver-ledger-v2-branch-protection-wave-0/39-RESEARCH.md
@tests/unit/llm-ledger.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/CODEOWNERS with five locked path pins</name>
  <files>.github/CODEOWNERS</files>
  <read_first>
    - .planning/phases/39-sdk-driver-ledger-v2-branch-protection-wave-0/39-CONTEXT.md §"Locked Decisions" (CODEOWNERS pins LOCKED: src/, tests/, .github/workflows/, tests/golden/, tests/e2e/test-cases-quarantine.js; owner @tonyrowles confirmed via gh api user --jq .login on 2026-05-30)
    - .planning/phases/39-sdk-driver-ledger-v2-branch-protection-wave-0/39-RESEARCH.md §"Pattern 6: CODEOWNERS file format" (canonical GitHub format, last-matching-rule wins, path ordering rule)
    - .planning/research/PITFALLS.md §"Pitfall 4" (CODEOWNERS username mismatch silently disables branch protection — verify exact login string)
  </read_first>
  <behavior>
    - File path is EXACTLY `.github/CODEOWNERS` (canonical GitHub location per A4 in 39-RESEARCH.md — NOT `CODEOWNERS` at repo root, NOT `docs/CODEOWNERS`)
    - 5 pinned paths in LAST-MATCH-WINS order (most-specific paths AFTER broader ones; per Pattern 6)
    - Every path line ends with `@tonyrowles` (verified GitHub login per CONTEXT)
    - File starts with a 4-line comment block referencing Phase 39 / Phase 47 / GitHub docs
  </behavior>
  <action>
    Create `.github/CODEOWNERS` with this EXACT content (do NOT alter the path strings, owner string, or ordering):

    ```
    # .github/CODEOWNERS
    # Phase 39 (CLEANUP-04 initial setup) — single-maintainer pins for v4.0 trust invariants.
    # Phase 47 (CLEANUP-04 audit) re-verifies these are present and unchanged via gh api.
    # Last-matching-rule semantics: more specific paths MUST appear AFTER broader ones.
    # https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-security/customizing-your-repository/about-code-owners

    /src/                                       @tonyrowles
    /tests/                                     @tonyrowles
    /.github/workflows/                         @tonyrowles
    /tests/golden/                              @tonyrowles
    /tests/e2e/test-cases-quarantine.js         @tonyrowles
    ```

    Rules:
    - Single owner per line; exactly `@tonyrowles` (verified via `gh api user --jq .login` per CONTEXT lock; PITFALLS.md Pitfall 4 documents that a mismatched login silently disables branch protection)
    - DO NOT use `@TR`, `@fatduck`, or any other alias — those are documentation/conversational references, NOT GitHub logins
    - Whitespace between path glob and owner is SPACES (CODEOWNERS spec accepts spaces or tabs; spaces match the convention in the example block in Pattern 6)
    - Paths are written from repo root with leading `/` (locks to repo-root anchors — GitHub treats `/foo` as repo-root-anchored, `foo` as a wildcard match anywhere)
    - Last-match-wins order: broader paths (`/tests/`) come BEFORE narrower paths (`/tests/golden/`, `/tests/e2e/test-cases-quarantine.js`) so the narrow rules take precedence on those specific paths

    If the file already exists (it does not, per `ls .github/`), preserve nothing — this is the canonical Phase 39 initial-setup file.
  </action>
  <verify>
    <automated>
test -f .github/CODEOWNERS \
  && grep -c "^/src/" .github/CODEOWNERS | grep -q "^1$" \
  && grep -c "^/tests/ " .github/CODEOWNERS | grep -q "^1$" \
  && grep -c "^/.github/workflows/" .github/CODEOWNERS | grep -q "^1$" \
  && grep -c "^/tests/golden/" .github/CODEOWNERS | grep -q "^1$" \
  && grep -c "^/tests/e2e/test-cases-quarantine.js" .github/CODEOWNERS | grep -q "^1$" \
  && grep -v '^#' .github/CODEOWNERS | grep -v '^$' | awk '{print $NF}' | sort -u | grep -q "^@tonyrowles$" \
  && ! grep -E "@TR|@fatduck" .github/CODEOWNERS
    </automated>
  </verify>
  <done>
    `.github/CODEOWNERS` exists with exactly 5 non-comment path-pin lines, every line ends with `@tonyrowles`, no `@TR` / `@fatduck` aliases present anywhere in the file; file path is canonical `.github/CODEOWNERS` (NOT root-level CODEOWNERS).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create docs/v40-repo-config.md (manual repo-settings audit reference)</name>
  <files>docs/v40-repo-config.md</files>
  <read_first>
    - .planning/phases/39-sdk-driver-ledger-v2-branch-protection-wave-0/39-CONTEXT.md §"Locked Decisions" (Allow auto-merge: OFF; Do not allow bypassing: ON with NO bypass list; branch protection scope: main only; required-status-check SLOT reserved empty for Phase 41)
    - .planning/phases/39-sdk-driver-ledger-v2-branch-protection-wave-0/39-RESEARCH.md §"Pitfall 5: Branch protection required status check" (slot must be EMPTY in Phase 39 or every PR is blocked) and §"Pitfall 6: Do not allow bypassing interacts with single-maintainer workflow" (operational impact accepted)
    - .planning/research/PITFALLS.md §"Pitfall 4: Auto-merge subverts the human-gated trust invariant" (the entire reason CLEANUP-04 exists)
    - .github/workflows/e2e-weekly-digest.yml (lines 98–110 — the `[skip ci]` atomic commit pattern that Phase 40+ will mirror for ledger commits; document the reference for Phase 40 here)
  </read_first>
  <behavior>
    - Document acts as the human-readable checklist for the manual UI clicks that ship in Plan 04's checkpoint AND the audit-time `gh api` reads Phase 47 will execute
    - Every UI setting that needs to be applied has a paired `gh api GET` verification command captured verbatim
    - Documents the LOCKED operational tradeoff: single-maintainer + Do not allow bypassing ON + NO bypass list (per CONTEXT)
    - Reserves the required-status-check slot as EMPTY in Phase 39; explicitly names `verifier-gate` as the Phase 41 placeholder so future planners know what to fill in
    - Documents the ANTHROPIC_API_KEY secret requirement for Phase 42+ (NOT required by Phase 39 — but documenting now is cheap per 39-RESEARCH.md Runtime State Inventory)
    - Documents the `[skip ci]` atomic commit pattern that Phase 40 will adopt for ledger commits (per LEDGER-04 — Phase 39 only flips the bit, Phase 40 is the consumer)
  </behavior>
  <action>
    Create `docs/v40-repo-config.md` with these sections (markdown headings exactly as listed). Use the exact wording and verification commands below — Phase 47's CLEANUP-04 audit will diff-check this doc.

    ```markdown
    # v4.0 Repo Configuration Reference

    **Created:** 2026-05-30 (Phase 39 CLEANUP-04 initial setup)
    **Re-audited:** Phase 47 CLEANUP-04 (compare against this doc + live `gh api` reads)
    **Owner:** @tonyrowles (verified via `gh api user --jq .login`)

    This doc captures the manual GitHub UI settings that v4.0's trust invariants depend on. The settings themselves live in GitHub Settings (NOT git-tracked); this file is the audit reference Phase 47 cross-checks.

    ## 1. Allow auto-merge: OFF (repo-level)

    **Why:** PITFALLS.md Pitfall 4 — auto-merge subverts the human-gated trust invariant for citation-accuracy code. All auto-fix PRs MUST require explicit human merge.

    **UI path:** Settings → General → Pull Requests → uncheck "Allow auto-merge"

    **Audit command (run AS the maintainer):**
    ```
    gh api GET /repos/tonyrowles/patent-cite-tool --jq '.allow_auto_merge'
    ```
    **Expected output:** `false`

    ## 2. Branch protection ruleset on `main`

    **Why:** PITFALLS.md Pitfall 4 (auto-merge bypass) + Pitfall 1/3 (verifier-gate skip prevention). Modern GitHub rulesets replace legacy branch protection rules (per 39-RESEARCH.md §State of the Art).

    **UI path:** Settings → Rules → Rulesets → New branch ruleset

    **Settings (locked per 39-CONTEXT.md):**

    | Setting | Value | Rationale |
    |---------|-------|-----------|
    | Target branches | `main` only | CONTEXT-locked scope |
    | Bypass list | EMPTY | `Do not allow bypassing: ON` — NO bypass list. Single-maintainer friction accepted (see §3). |
    | Require pull request before merging | ON | All changes go through PRs |
    | Required approvals | 1 (the maintainer) | Single-maintainer baseline |
    | Require review from Code Owners | ON | Triggers CODEOWNERS-pinned reviews on the 5 locked paths |
    | Required status checks | **EMPTY SLOT (reserved for Phase 41 `verifier-gate`)** | Per 39-RESEARCH.md Pitfall 5 — naming a non-existent check blocks EVERY PR including Phase 39's own |

    **Audit command (Phase 47):**
    ```
    gh api GET /repos/tonyrowles/patent-cite-tool/rulesets --jq '.[] | select(.target == "branch") | .name'
    gh api GET /repos/tonyrowles/patent-cite-tool/rulesets/<RULESET_ID> --jq '{enforcement, bypass_actors, conditions, rules}'
    ```

    **Expected on rule details:**
    - `enforcement: "active"`
    - `bypass_actors: []` (NO bypass list)
    - `rules[?(@.type=="pull_request")].parameters.require_code_owner_review: true`
    - `rules[?(@.type=="required_status_checks")].parameters.required_status_checks` — **empty array** in Phase 39; populated by Phase 41 to `[{ context: "verifier-gate", integration_id: <github-actions-id> }]`

    ## 3. Operational tradeoff: single-maintainer + Do not allow bypassing

    **Locked decision (39-CONTEXT.md):** `Do not allow bypassing: ON` with NO bypass list.

    **Implication for `@tonyrowles`:** PRs the maintainer opens against CODEOWNED files (which lack a second maintainer to approve) require a workaround. Two acceptable approaches:
    1. Branch + PR + self-approve via `gh pr merge --admin <n>` — requires admin bypass which is FORBIDDEN by this ruleset. Not acceptable.
    2. Open the PR, request review from a third party / collaborator on the bot account, merge after review. Acceptable for the rare manual-edit case.

    **Implication for auto-fix bot PRs:** The bot opens PRs as a distinct identity (github-actions[bot]); `@tonyrowles` reviews and merges them normally. This is the COMMON path — the trust invariant is preserved exactly because the bot cannot self-approve.

    ## 4. CODEOWNERS file

    **Location:** `.github/CODEOWNERS` (canonical GitHub-recognised path; A4 in 39-RESEARCH.md).

    **Pinned paths (verified by `tests/unit/codeowners.test.js`):**
    - `/src/` → `@tonyrowles`
    - `/tests/` → `@tonyrowles`
    - `/.github/workflows/` → `@tonyrowles`
    - `/tests/golden/` → `@tonyrowles`
    - `/tests/e2e/test-cases-quarantine.js` → `@tonyrowles`

    **Audit command:**
    ```
    cat .github/CODEOWNERS
    npx vitest run tests/unit/codeowners.test.js
    ```

    ## 5. ANTHROPIC_API_KEY secret (required by Phase 42+, NOT Phase 39)

    **Why documented now:** Phase 42 (`scripts/auto-fix.mjs`) is the first CI consumer of `invokeAnthropicSdkWithLedger` (Plan 03's SDK transport). The secret must exist before the first CI invocation; Phase 39 doesn't trigger that, but Phase 42 will fail loudly if the secret is absent.

    **UI path:** Settings → Secrets and variables → Actions → New repository secret
    - Name: `ANTHROPIC_API_KEY`
    - Value: API key from <https://console.anthropic.com/settings/keys>
    - Rotation: every 90 days (per PITFALLS.md security mistakes table)

    **Audit command (does NOT print the value — confirms presence):**
    ```
    gh api GET /repos/tonyrowles/patent-cite-tool/actions/secrets --jq '.secrets[].name' | grep -q '^ANTHROPIC_API_KEY$'
    ```

    ## 6. `[skip ci]` atomic commit pattern (Phase 40+ ledger commits)

    **Why here:** Phase 39 flips `tests/e2e/.llm-spend-ledger.json` from gitignored to committed (Plan 04). Phase 40+ workflows commit ledger updates atomically using the established pattern at `.github/workflows/e2e-weekly-digest.yml:98–110`.

    **Pattern (cite this exact block from `e2e-weekly-digest.yml`):**
    ```yaml
    - name: Commit ledger update
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git add tests/e2e/.llm-spend-ledger.json
        git diff --cached --quiet || git commit -m "chore(ledger): auto-fix #N spend [skip ci]"
        git push
    ```

    **`[skip ci]` is LOAD-BEARING** — without it, the bot push re-triggers `ci.yml` which re-runs the auto-fix workflow which re-files the ledger update. Infinite loop.

    ## 7. Phase 47 re-audit checklist

    Phase 47 (CLEANUP-04 re-audit) MUST verify:
    - [ ] `allow_auto_merge: false` via `gh api`
    - [ ] Ruleset on `main` exists with `enforcement: "active"`, `bypass_actors: []`, `require_code_owner_review: true`
    - [ ] Required status checks list NOW INCLUDES `verifier-gate` (populated by Phase 41)
    - [ ] `.github/CODEOWNERS` byte-for-byte matches §4 above (no drift)
    - [ ] `tests/unit/codeowners.test.js` PASSES
    - [ ] `ANTHROPIC_API_KEY` repo secret exists
    - [ ] `tests/e2e/.llm-spend-ledger.json` is git-tracked (NOT in `.gitignore`)
    ```

    Use the heading numbering exactly as shown so Phase 47's audit can grep section anchors.
  </action>
  <verify>
    <automated>
test -f docs/v40-repo-config.md \
  && wc -l docs/v40-repo-config.md | awk '{if ($1 < 60) exit 1}' \
  && grep -c "^## " docs/v40-repo-config.md | grep -q "^7$" \
  && grep -q "allow_auto_merge" docs/v40-repo-config.md \
  && grep -q "Do not allow bypassing" docs/v40-repo-config.md \
  && grep -q "verifier-gate" docs/v40-repo-config.md \
  && grep -q "ANTHROPIC_API_KEY" docs/v40-repo-config.md \
  && grep -q "skip ci" docs/v40-repo-config.md \
  && grep -q "@tonyrowles" docs/v40-repo-config.md
    </automated>
  </verify>
  <done>
    `docs/v40-repo-config.md` exists with all 7 numbered headings, references `allow_auto_merge` / `Do not allow bypassing` / `verifier-gate` / `ANTHROPIC_API_KEY` / `[skip ci]` / `@tonyrowles`; file ≥ 60 lines.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tests/unit/codeowners.test.js static-grep guard</name>
  <files>tests/unit/codeowners.test.js</files>
  <read_first>
    - .github/CODEOWNERS (the file Task 1 creates — assertions must match it byte-for-byte)
    - tests/unit/llm-ledger.test.js lines 56–87 (canonical Vitest imports + describe/it patterns to mirror)
    - .planning/phases/39-sdk-driver-ledger-v2-branch-protection-wave-0/39-RESEARCH.md §"Wave 0 Gaps" (codeowners.test.js is the NEW guard file this task creates)
    - .planning/research/PITFALLS.md §"Pitfall 4" (rationale: username mismatch silently disables protection — this test pins the exact login string forever)
  </read_first>
  <behavior>
    - Vitest test reads `.github/CODEOWNERS` from repo root (resolved via fileURLToPath + path.resolve, mirroring tests/unit/llm-ledger.test.js line 61)
    - Asserts file exists (fail-fast if Plan 02 Task 1 was skipped)
    - Asserts all 5 LOCKED paths appear verbatim as line PREFIXES (so trailing-whitespace formatting drift doesn't break the test, but path-string drift does)
    - Asserts every non-comment non-empty line ends with `@tonyrowles` (single owner, no co-owners introduced)
    - Asserts NO `@TR`, `@fatduck`, or other aliases anywhere in the file
    - Asserts the file is at `.github/CODEOWNERS` exactly (NOT root or docs/)
  </behavior>
  <action>
    Create `tests/unit/codeowners.test.js` with this structure (mirror the import block from tests/unit/llm-ledger.test.js verbatim — vitest + fs + path + fileURLToPath):

    ```js
    // tests/unit/codeowners.test.js
    //
    // Phase 39 (CLEANUP-04 initial setup) — static-grep guard that .github/CODEOWNERS
    // pins the 5 locked paths to @tonyrowles. Phase 47 CLEANUP-04 re-audit reads
    // this same file + executes this test; drift trips both gates.
    //
    // Rationale: PITFALLS.md Pitfall 4 — a CODEOWNERS owner mismatch silently
    // disables "Require review from Code Owners" branch protection. This test
    // pins the exact GitHub login string @tonyrowles (verified 2026-05-30 via
    // `gh api user --jq .login`).

    import { describe, it, expect } from 'vitest';
    import fs from 'node:fs';
    import path from 'node:path';
    import { fileURLToPath } from 'node:url';

    const __dirname = path.dirname(fileURLToPath(import.meta.url));
    const REPO_ROOT = path.resolve(__dirname, '..', '..');
    const CODEOWNERS_PATH = path.join(REPO_ROOT, '.github', 'CODEOWNERS');

    const LOCKED_PATHS = [
      '/src/',
      '/tests/',
      '/.github/workflows/',
      '/tests/golden/',
      '/tests/e2e/test-cases-quarantine.js',
    ];
    const LOCKED_OWNER = '@tonyrowles';
    const FORBIDDEN_ALIASES = ['@TR', '@fatduck'];

    describe('Phase 39 CLEANUP-04: .github/CODEOWNERS contents pinned', () => {
      it('Test 1: .github/CODEOWNERS file exists at canonical path', () => {
        expect(fs.existsSync(CODEOWNERS_PATH)).toBe(true);
      });

      it('Test 2: every locked path appears as a line prefix', () => {
        const text = fs.readFileSync(CODEOWNERS_PATH, 'utf8');
        const lines = text.split(/\r?\n/);
        for (const p of LOCKED_PATHS) {
          const found = lines.some((ln) => ln.startsWith(p));
          expect(found, `expected line beginning with "${p}" in CODEOWNERS`).toBe(true);
        }
      });

      it('Test 3: every locked path is followed by @tonyrowles (the verified login)', () => {
        const text = fs.readFileSync(CODEOWNERS_PATH, 'utf8');
        const lines = text.split(/\r?\n/);
        for (const p of LOCKED_PATHS) {
          const match = lines.find((ln) => ln.startsWith(p));
          expect(match, `path ${p} must be present`).toBeDefined();
          // owner is the LAST whitespace-separated token on the line
          const tokens = match.trim().split(/\s+/);
          expect(tokens[tokens.length - 1]).toBe(LOCKED_OWNER);
        }
      });

      it('Test 4: file contains exactly one owner across all path lines', () => {
        const text = fs.readFileSync(CODEOWNERS_PATH, 'utf8');
        const owners = text
          .split(/\r?\n/)
          .filter((ln) => ln && !ln.trim().startsWith('#') && ln.trim().length > 0)
          .map((ln) => ln.trim().split(/\s+/).slice(1))
          .flat();
        const unique = [...new Set(owners)];
        expect(unique).toEqual([LOCKED_OWNER]);
      });

      it('Test 5: forbidden aliases (@TR, @fatduck) are absent', () => {
        const text = fs.readFileSync(CODEOWNERS_PATH, 'utf8');
        for (const alias of FORBIDDEN_ALIASES) {
          expect(
            text.includes(alias),
            `${alias} is a documentation alias, NOT a GitHub login — see Pitfall 4`,
          ).toBe(false);
        }
      });

      it('Test 6: file is at .github/CODEOWNERS, NOT root or docs/', () => {
        expect(fs.existsSync(path.join(REPO_ROOT, 'CODEOWNERS'))).toBe(false);
        expect(fs.existsSync(path.join(REPO_ROOT, 'docs', 'CODEOWNERS'))).toBe(false);
      });

      it('Test 7: exactly 5 non-comment non-empty path lines', () => {
        const text = fs.readFileSync(CODEOWNERS_PATH, 'utf8');
        const pathLines = text
          .split(/\r?\n/)
          .filter((ln) => ln && !ln.trim().startsWith('#') && ln.trim().length > 0);
        expect(pathLines.length).toBe(5);
      });
    });
    ```

    Do NOT add web-ext-style globs, do NOT add `gh api` runtime calls (that's Phase 47's job; this test runs in pure Node without network). The grep against `@TR` and `@fatduck` is the load-bearing assertion preventing alias-drift on future edits.
  </action>
  <verify>
    <automated>
npx vitest run tests/unit/codeowners.test.js --reporter=basic 2>&1 | tail -15 \
  && npx vitest run tests/unit/codeowners.test.js --reporter=basic 2>&1 | grep -E "Test [1-7]" | wc -l | grep -q "^7$"
    </automated>
  </verify>
  <done>
    All 7 cases in `tests/unit/codeowners.test.js` pass; the test file imports only `vitest`, `node:fs`, `node:path`, `node:url` (no network or shell-out); test exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CODEOWNERS file → GitHub branch protection | An incorrect owner string silently disables the "Require review from Code Owners" check; the file is the trust-invariant anchor for v4.0 |
| docs/v40-repo-config.md → human auditor | Phase 47 reads this doc and the live `gh api` output; documentation drift means audit drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-05 | Spoofing | CODEOWNERS owner field (PITFALLS Pitfall 4 — wrong login string silently disables protection) | mitigate | CODEOWNERS uses exactly `@tonyrowles` (verified via `gh api user --jq .login` per CONTEXT); `tests/unit/codeowners.test.js` Test 3 pins the string forever; forbidden-alias grep (Test 5) prevents `@TR`/`@fatduck` drift |
| T-39-06 | Elevation of Privilege | Maintainer-bypass on branch protection (PITFALLS Pitfall 4) | mitigate | `docs/v40-repo-config.md` §2 documents `Do not allow bypassing: ON` with `bypass_actors: []`; Phase 47 §7 checklist re-audits via `gh api` |
| T-39-07 | Tampering | CODEOWNERS file edited to add/remove path or owner | mitigate | `tests/unit/codeowners.test.js` Test 2/4/7 pins exact path count + exact owner set; any drift fails Vitest |
| T-39-08 | Information Disclosure | Phase 39 docs leak ANTHROPIC_API_KEY value | accept | docs/v40-repo-config.md §5 documents the secret EXISTS but never the value; audit command uses `--jq '.secrets[].name'` which omits the value entirely |
</threat_model>

<verification>
- `test -f .github/CODEOWNERS` — file exists at canonical path
- `test -f docs/v40-repo-config.md` — audit-reference doc exists
- `npx vitest run tests/unit/codeowners.test.js` exits 0 (all 7 cases pass)
- `grep -v '^#' .github/CODEOWNERS | grep -v '^$' | wc -l` returns `5` (exactly 5 path lines)
- `grep -c '@tonyrowles' .github/CODEOWNERS` returns 5 (one per path; the comment block has none)
- `! grep -E '@TR|@fatduck' .github/CODEOWNERS` (no aliases present)
- `grep -c '^## ' docs/v40-repo-config.md` returns 7 (all 7 numbered sections present)
</verification>

<success_criteria>
- CLEANUP-04 initial setup: `.github/CODEOWNERS` pins the 5 locked paths to `@tonyrowles`; `docs/v40-repo-config.md` documents the manual UI settings + `gh api` audit commands for Phase 47; `tests/unit/codeowners.test.js` STATIC-PINS the contents (7 cases passing)
- The actual `Settings → Allow auto-merge: OFF` and branch-protection-ruleset UI clicks are NOT part of this plan — they ship in Plan 04's human-action checkpoint, AFTER the in-repo artifacts land and AFTER Plan 03's SDK driver / ESLint guard are in place. This plan ships every IN-REPO artifact Phase 47 CLEANUP-04 will audit.
</success_criteria>

<output>
Create `.planning/phases/39-sdk-driver-ledger-v2-branch-protection-wave-0/39-02-SUMMARY.md` when done. Document: (1) the exact owner string `@tonyrowles` pinned in CODEOWNERS, (2) the 5 locked paths, (3) Phase 47 audit reference commands captured in docs/v40-repo-config.md, (4) the 7 Vitest test cases.
</output>
