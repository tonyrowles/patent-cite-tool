---
phase: 41-verifier-gate-workflow-verify-single-case-mjs-cli-shim
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - docs/v40-verifier-gate-manual-test.md
  - tests/unit/v40-verifier-gate-doc.test.js
autonomous: true
requirements: [VFY-GATE-01]
tags: [verifier-gate, manual-test, documentation, vitest]

must_haves:
  truths:
    - "docs/v40-verifier-gate-manual-test.md documents the end-to-end smoke procedure for VFY-GATE success criterion 5 (a pushed auto-fix/test branch demonstrates the gate end-to-end with no LLM involvement)"
    - "The doc includes: (a) prerequisite checklist (Phase 41-01/02/03 merged), (b) a step-by-step procedure to create auto-fix/test branch with benign diff + affected_cases comment, (c) expected workflow run sequence (diff-guard → verifier-gate + regression-suite → ready-flip), (d) success signal (PR transitions draft → ready_for_review + comment posted), (e) failure-mode catalog with rejection comment examples for each gate"
    - "The doc includes a cleanup section (how to delete the test branch + PR + label after)"
    - "Vitest test asserts the doc exists + contains each of the 5 required sections (procedure, expected sequence, success signal, failure catalog, cleanup)"
  artifacts:
    - path: "docs/v40-verifier-gate-manual-test.md"
      provides: "End-to-end smoke test procedure for the verifier-gate workflow (VFY-GATE success criterion 5)"
      min_lines: 80
      contains: "auto-fix/test"
    - path: "tests/unit/v40-verifier-gate-doc.test.js"
      provides: "Vitest assertions pinning the 5 required sections of the manual-test doc"
      contains: "docs/v40-verifier-gate-manual-test.md"
  key_links:
    - from: "docs/v40-verifier-gate-manual-test.md"
      to: ".github/workflows/v40-verifier-gate.yml (Plan 41-03)"
      via: "documentation reference — manual test exercises the workflow"
      pattern: "v40-verifier-gate"
    - from: "docs/v40-verifier-gate-manual-test.md"
      to: ".planning/phases/47-v4-0-cleanup/ (Phase 47 CLEANUP-03 will execute the procedure as live HUMAN-UAT)"
      via: "procedure-reuse contract"
      pattern: "verifier-gate diff-guard rejecting a crafted bypass attempt"
---

<objective>
Ship `docs/v40-verifier-gate-manual-test.md` — a self-contained smoke-test procedure that a human (or future Phase 47 CLEANUP-03 HUMAN-UAT execution) can follow to verify the verifier-gate workflow end-to-end with NO LLM involvement — plus a tiny Vitest test that asserts the doc exists and contains its required sections.

Purpose: VFY-GATE success criterion 5 in ROADMAP.md requires "A pushed `auto-fix/test` branch can demonstrate the gate end-to-end with no LLM involvement (manual exercise)." Plan 41-03 ships the workflow; this plan documents how to exercise it manually. Phase 47 CLEANUP-03 will RUN this procedure as one of its 5 live HUMAN-UAT confirmations ("verifier-gate diff-guard rejecting a crafted bypass attempt"). Without this doc, Phase 47 has no procedure to follow.

The Vitest test prevents bit-rot: if a future plan modifies the workflow's expected sequence or success signal, the doc test fails on the next CI run, forcing the doc to be updated in lockstep.

Output: 1 documentation file (~100 LOC markdown) + 1 unit-test file (~30 LOC).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-verifier-gate-workflow-verify-single-case-mjs-cli-shim/41-CONTEXT.md
@.planning/phases/41-verifier-gate-workflow-verify-single-case-mjs-cli-shim/41-RESEARCH.md

<interfaces>
<!-- Doc references the workflow + CLI shims shipped by Plans 41-01, 41-02, 41-03 -->
<!-- But this plan does NOT depend on those plans at file-write time — the doc can be -->
<!-- authored in parallel with 41-03 since their files don't overlap. -->

Workflow under test (shipped by Plan 41-03):
- File: .github/workflows/v40-verifier-gate.yml
- Trigger: pull_request on auto-fix/* branches
- Jobs: diff-guard → verifier-gate + regression-suite (parallel) → ready-flip
- Helpers consumed: scripts/check-diff-guard.mjs, scripts/parse-affected-cases.mjs, scripts/verify-single-case.mjs

Existing docs style template:
- docs/v40-repo-config.md (Phase 39-04 documented branch protection + CODEOWNERS — the precedent for v40-* doc naming + section structure)

Existing TEST_CASES (used in the manual test's affected_cases comment):
- US11427642-spec-short-1 has baseline "1:26-27" (good first-case candidate; known-good per the integration test in Plan 41-02)

Test framework (existing):
- Vitest ^3.0.0 + tests/unit/*.test.js convention (matches Plan 41-01/02)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the manual-test documentation</name>
  <files>docs/v40-verifier-gate-manual-test.md</files>
  <read_first>
    - .planning/phases/41-verifier-gate-workflow-verify-single-case-mjs-cli-shim/41-CONTEXT.md (Specifics §"Manual end-to-end smoke (VFY-GATE-05)" — defines the procedure shape)
    - .planning/phases/41-verifier-gate-workflow-verify-single-case-mjs-cli-shim/41-RESEARCH.md (Architecture Patterns — for the expected workflow sequence + Pitfalls A-F for failure-mode examples)
    - docs/v40-repo-config.md (Phase 39-04 STYLE TEMPLATE for v40-* doc structure)
    - .planning/ROADMAP.md (Phase 41 Success Criterion 5 — the contract this doc satisfies)
  </read_first>
  <action>
    Create docs/v40-verifier-gate-manual-test.md with the following 5 required sections (each as an H2 heading with the exact wording the doc test pins):

    1. `## Prerequisites` — checklist:
       - [ ] Plans 41-01, 41-02, 41-03 merged to main (workflow + helpers + shim shipped)
       - [ ] Local clone has push access to origin
       - [ ] gh CLI authenticated (`gh auth status` returns green)
       - [ ] At least 1 TEST_CASES entry is known to pass verifyCitation (use 'US11427642-spec-short-1' with baseline "1:26-27" per Plan 41-02 integration test)

    2. `## Procedure` — numbered steps to create the test branch + PR:
       ```
       # Create a benign-diff branch from main
       git checkout main && git pull origin main
       git checkout -b auto-fix/test-manual-$(date +%s)

       # Add a benign src/ change (e.g., a comment-only edit to a non-load-bearing file)
       echo "// manual smoke test $(date)" >> src/popup/popup.js
       git add src/popup/popup.js
       git commit -m "test: manual verifier-gate smoke (delete after)"

       # Push branch
       git push -u origin HEAD

       # Open draft PR with the affected_cases HTML comment in the body
       gh pr create --draft --title "Manual verifier-gate smoke" --body $'Manual end-to-end smoke for VFY-GATE-05.\n\n<!-- affected_cases: US11427642-spec-short-1 -->\n\nDelete after verification.'
       ```

    3. `## Expected Workflow Sequence` — describe the 4-job execution order with timing expectations:
       - diff-guard (~1 min): runs git diff parse, checks size cap (passes — single-line change), runs check-diff-guard.mjs (passes — src/popup/popup.js not in forbidden bank), test-count invariant (passes — TEST_CASES unchanged)
       - verifier-gate + regression-suite (parallel, ~15-25 min): verifier-gate pins origin/main verifier, parses affected_cases (gets US11427642-spec-short-1), runs verify-single-case.mjs 3× consecutively (all Tier A/B expected); regression-suite runs 76-case Playwright spec (all pass expected since no real change)
       - ready-flip (~10s): gh pr ready flips draft → ready_for_review

    4. `## Success Signal` — what to observe:
       - GitHub PR badge transitions from "Draft" to "Ready for review"
       - All 4 workflow jobs show green checks in the Actions tab
       - PR has a new comment from github-actions bot: "Verifier-gate: all 3 affected-case runs Tier A/B + 76-case regression clean. Draft → ready-for-review."
       - PR does NOT have the human-review-required label

    5. `## Failure-Mode Catalog` — examples + expected rejection comments (covers each gate's failure path):
       - Size-cap rejection: modify many lines, expect "Diff-size cap exceeded — src/=XXX (limit 200)..." comment + human-review-required label + diff-guard job red
       - Diff-guard rejection: try to modify tests/test-cases.js, expect diff-guard job red with stderr "Diff-guard violations: tests/test-cases.js"
       - Verifier-gate failure: omit the affected_cases comment from PR body, expect verifier-gate job red with "PR body missing or empty <!-- affected_cases: ... -->" error
       - Regression failure: would require an actual broken src/ change; document but don't require execution in smoke
       - Already-ready idempotent re-run: re-trigger workflow on a ready PR via workflow_dispatch, expect ready-flip to skip with "PR already ready-for-review" log (per Pitfall D in 41-RESEARCH)

    Also add a section `## Cleanup` after the failure catalog:
       ```
       gh pr close <PR_NUMBER>
       git push origin --delete auto-fix/test-manual-<timestamp>
       # If human-review-required label was added during failure-mode testing:
       gh issue label remove --silent  # or via UI; the label persists on the closed PR which is fine
       ```

    Header doc-block at the top of the file MUST cite Phase 41 + VFY-GATE-05 success criterion + the Phase 47 CLEANUP-03 future reuse contract. Use the docs/v40-repo-config.md shape as the style template (H1 title + 1-paragraph purpose + sections per the order above).

    Commit with message: `docs(41-04): v40-verifier-gate manual smoke procedure`.
  </action>
  <verify>
    <automated>test -f docs/v40-verifier-gate-manual-test.md && grep -cE "^## (Prerequisites|Procedure|Expected Workflow Sequence|Success Signal|Failure-Mode Catalog|Cleanup)" docs/v40-verifier-gate-manual-test.md</automated>
  </verify>
  <done>Doc file exists; grep returns 6 (all 6 H2 headings present: Prerequisites, Procedure, Expected Workflow Sequence, Success Signal, Failure-Mode Catalog, Cleanup); commit landed.</done>
  <acceptance_criteria>
    - File exists at docs/v40-verifier-gate-manual-test.md
    - Contains all 6 H2 headings with EXACT wording: `## Prerequisites`, `## Procedure`, `## Expected Workflow Sequence`, `## Success Signal`, `## Failure-Mode Catalog`, `## Cleanup`
    - References the workflow file `.github/workflows/v40-verifier-gate.yml` explicitly
    - References at least 1 TEST_CASES id (e.g., US11427642-spec-short-1) in the procedure
    - Includes the literal HTML comment `<!-- affected_cases: US11427642-spec-short-1 -->` (so a future doc-reader can copy-paste)
    - Includes at least 4 failure-mode examples in the failure catalog (size-cap, diff-guard, missing affected_cases, idempotent re-run)
    - Header doc-block cites VFY-GATE success criterion 5 + Phase 47 CLEANUP-03 future reuse
    - File is ≥80 lines of markdown
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write Vitest test pinning the doc's required sections</name>
  <files>tests/unit/v40-verifier-gate-doc.test.js</files>
  <read_first>
    - tests/unit/check-deps-and-pr.test.js (Phase 40-02 STYLE TEMPLATE for simple grep tests)
    - docs/v40-verifier-gate-manual-test.md (the doc this test pins — created in Task 1)
  </read_first>
  <behavior>
    tests/unit/v40-verifier-gate-doc.test.js — minimum 6 it() blocks:
    - D1: docs/v40-verifier-gate-manual-test.md exists (fs.existsSync)
    - D2: doc contains `## Prerequisites` heading
    - D3: doc contains `## Procedure` heading AND mentions `auto-fix/test` branch name
    - D4: doc contains `## Expected Workflow Sequence` heading AND names all 4 jobs (diff-guard, verifier-gate, regression-suite, ready-flip)
    - D5: doc contains `## Success Signal` heading AND mentions "draft" → "ready" transition
    - D6: doc contains `## Failure-Mode Catalog` heading AND references human-review-required label
    - D7: doc references the workflow file path `.github/workflows/v40-verifier-gate.yml` exactly
    - D8: doc references at least 1 TEST_CASES id (regex `/US\d{7,}-/` matches at least once)
    - D9: doc includes the literal `<!-- affected_cases:` HTML comment (so a reader can copy-paste the procedure)
  </behavior>
  <action>Create tests/unit/v40-verifier-gate-doc.test.js. Use the Phase 40-02 STYLE TEMPLATE: ESM imports, fs.readFileSync(DOC_PATH, 'utf8') in beforeAll, describe + it blocks, toContain + toMatch assertions. Header doc-block cites Phase 41 + VFY-GATE-05 + the bit-rot prevention rationale. This task is TDD-AFTER (the doc was written in Task 1; this test pins it for future-proofing) so the test should pass GREEN immediately — that's intentional. Commit with message: `test(41-04): pin v40-verifier-gate manual-test doc structure`.</action>
  <verify>
    <automated>npx vitest run tests/unit/v40-verifier-gate-doc.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>Test file exists; vitest run shows all 6+ cases passing GREEN immediately (the doc was written in Task 1); `npm run test:src` (full suite) still green; commit landed.</done>
  <acceptance_criteria>
    - 6+ it() blocks covering each H2 section + key tokens (workflow path, TEST_CASES id, affected_cases comment, label name, draft→ready transition)
    - beforeAll reads docs/v40-verifier-gate-manual-test.md via fs.readFileSync
    - All assertions pass on the doc as written in Task 1 (test passes GREEN on first run — this is TDD-AFTER for bit-rot prevention, not for design)
    - No regressions on other Vitest suites
    - File header explains the bit-rot prevention purpose (future modifications to the workflow MUST update the doc; this test catches drift)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none load-bearing) | This plan ships a documentation file + a doc-existence test. No runtime code, no secrets, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04-T1 | Tampering | Doc gets out of sync with workflow over time (bit-rot) | mitigate | Vitest D-cases pin the workflow path + job names + key tokens; any workflow rename or signal-change forces a doc update in the same PR (test fails on CI otherwise) |
| T-41-04-I1 | Information Disclosure | Procedure references real PR numbers or branches | accept | Procedure uses `$(date +%s)` for branch names + `<PR_NUMBER>` placeholders — no leakage of real PR state. The procedure produces test branches/PRs that the cleanup section removes. |
| T-41-04-R1 | Repudiation | Manual smoke runner skips a step | accept | Manual procedure is human-executed; integrity rests on the human runner following the checklist. Phase 47 CLEANUP-03 will execute this as live HUMAN-UAT and stamp success/failure. |

ASVS Categories: V14 (Configuration documentation — manual-test procedure is configuration evidence for the auditor in Phase 47).

(No security_enforcement-applicable risks beyond documentation discipline. Phase 41 Layers 2 + 4 + 5 are all shipped in Plan 41-03; this plan documents how to verify them, doesn't add new defenses.)
</threat_model>

<verification>
- `test -f docs/v40-verifier-gate-manual-test.md` returns 0
- `grep -cE "^## " docs/v40-verifier-gate-manual-test.md` returns ≥6 (6 H2 sections + possibly a top-level H1)
- `grep -q "auto-fix/test" docs/v40-verifier-gate-manual-test.md && echo "OK"`
- `grep -q "<!-- affected_cases:" docs/v40-verifier-gate-manual-test.md && echo "OK"`
- `grep -q "US11427642" docs/v40-verifier-gate-manual-test.md && echo "OK"` (the test-case ID used in the procedure)
- `npx vitest run tests/unit/v40-verifier-gate-doc.test.js` exits 0 with all 6+ cases GREEN
- `npm run test:src` (full Vitest suite) passes — no regressions
</verification>

<success_criteria>
- docs/v40-verifier-gate-manual-test.md exists with all 6 required H2 sections
- Procedure is self-contained: a Phase 47 CLEANUP-03 human-UAT runner can execute it WITHOUT cross-referencing other docs
- Doc-existence Vitest test pins the structure for bit-rot prevention
- VFY-GATE-05 success criterion satisfied (procedure documented; Phase 47 will RUN it)
- Zero impact on Plan 41-03 (separate files; parallel-safe in Wave 2)
</success_criteria>

<output>
Create `.planning/phases/41-verifier-gate-workflow-verify-single-case-mjs-cli-shim/41-04-SUMMARY.md` when done. Include: tasks committed, doc LOC count, Vitest case count, self-check via the verification grep commands, and a "Next Phase Readiness" note that Phase 47 CLEANUP-03 has a clean procedure to execute as one of its 5 HUMAN-UAT confirmations.
</output>
