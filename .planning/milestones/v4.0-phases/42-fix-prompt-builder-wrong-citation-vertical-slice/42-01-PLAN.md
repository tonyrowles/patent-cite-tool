---
phase: 42-fix-prompt-builder-wrong-citation-vertical-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/lib/fix-prompt-builder.js
  - tests/e2e/lib/issue-payload-builder.js
  - tests/e2e/lib/llm-ledger.js
  - eslint.config.js
  - tests/unit/fix-prompt-builder.test.js
  - tests/unit/issue-payload-builder.test.js
  - tests/unit/llm-ledger.test.js
  - tests/unit/eslint-fix-prompt-builder-guard.test.js
autonomous: true
requirements: [PROMPT-01, PROMPT-02, PROMPT-03, PROMPT-04, AUTOFIX-05]
tags: [fix-prompt-builder, envelope, forbidden-delimiters, eslint-guard, count-fix-attempts]

must_haves:
  truths:
    - "buildFixPrompt({errorClass:'WRONG_CITATION', issueBody}) returns {ok:true, systemPrompt, userPrompt} where userPrompt is literally `<issue_body_untrusted>\\n{issueBody}\\n</issue_body_untrusted>`"
    - "buildFixPrompt({errorClass:'FLAKE'}) returns {ok:false, escalate:'re-quarantine'}"
    - "buildFixPrompt({errorClass:'LLM_API_ERROR'}) returns {ok:false, escalate:'retry'}"
    - "buildFixPrompt({errorClass:'PASS'}) returns {ok:false, escalate:'close-as-pass'}"
    - "PROMPT_SCAFFOLDS is Object.freeze()'d with EXACTLY one key (WRONG_CITATION)"
    - "issue-payload-builder.js escapes both <issue_body_untrusted> and </issue_body_untrusted> in LLM rationale + verifier reason + golden diff sections"
    - "ESLint fires no-restricted-imports error when fix-prompt-builder.js imports node:fs, node:child_process, node:path, or @anthropic-ai/sdk"
    - "countFixAttempts(ledger, fingerprint) returns the count of ledger iterations where phase==='42-auto-fix' AND fingerprint matches"
  artifacts:
    - path: "tests/e2e/lib/fix-prompt-builder.js"
      provides: "buildFixPrompt + PROMPT_SCAFFOLDS + ENVELOPE_OPEN/CLOSE + DIFF_FENCE_START/END exports"
      exports: ["buildFixPrompt", "PROMPT_SCAFFOLDS", "ENVELOPE_OPEN", "ENVELOPE_CLOSE", "DIFF_FENCE_START", "DIFF_FENCE_END"]
    - path: "tests/e2e/lib/issue-payload-builder.js"
      provides: "FORBIDDEN_DELIMITERS escape applied to LLM rationale + verifier reason + golden diff before truncate()"
      contains: "FORBIDDEN_DELIMITERS"
    - path: "tests/e2e/lib/llm-ledger.js"
      provides: "countFixAttempts(ledger, fingerprint) sibling of phaseTotal"
      exports: ["countFixAttempts"]
    - path: "eslint.config.js"
      provides: "New per-file block for fix-prompt-builder.js + augmented catch-all ignores"
      contains: "tests/e2e/lib/fix-prompt-builder.js"
    - path: "tests/unit/fix-prompt-builder.test.js"
      provides: "PROMPT-01 envelope assertion + PROMPT-03 registry shape + skip-class returns"
    - path: "tests/unit/eslint-fix-prompt-builder-guard.test.js"
      provides: "PROMPT-04 programmatic ESLint test for 4 forbidden imports (fs/cp/path/SDK)"
  key_links:
    - from: "tests/e2e/lib/fix-prompt-builder.js"
      to: "tests/e2e/lib/issue-payload-builder.js (FORBIDDEN_DELIMITERS)"
      via: "shared delimiter constants — payload-builder escapes them BEFORE prompt-builder wraps the body"
      pattern: "<issue_body_untrusted>"
    - from: "tests/e2e/lib/llm-ledger.js (countFixAttempts)"
      to: "scripts/auto-fix.mjs (Plan 42-02 consumer)"
      via: "named import — filters by phase==='42-auto-fix' + fingerprint"
      pattern: "phase === '42-auto-fix'"
    - from: "eslint.config.js per-file block"
      to: "eslint.config.js catch-all ignores list"
      via: "MUST augment in same commit — Pitfall 1/Pitfall 3 from 345cdcb"
      pattern: "tests/e2e/lib/fix-prompt-builder.js"
---

<objective>
Ship the pure-function `fix-prompt-builder.js` library (envelope wrapping + frozen PROMPT_SCAFFOLDS registry + skip-class short-circuits), extend `issue-payload-builder.js` with FORBIDDEN_DELIMITERS escape so v3.1-emitted issue bodies cannot pop the v4.0 envelope, add an ESLint purity guard for the new file (with the Pitfall 1/345cdcb catch-all-ignores augmentation in the SAME commit), and add `countFixAttempts(ledger, fingerprint)` to `llm-ledger.js` as a sibling of `phaseTotal` for Plan 42-02 to consume.

Purpose: Covers PROMPT-01..04 + the AUTOFIX-05 helper. Establishes the pure layer that Plan 42-02's dispatcher imports. Closes the v3.1→v4.0 issue-body injection seam (FORBIDDEN_DELIMITERS escape) BEFORE the dispatcher exists to send those bodies to the LLM.

Output: 1 new pure-function lib file, 2 extended pure-function lib files, 1 ESLint config edit (2 coordinated edits in same commit), 4 Vitest files (2 new + 2 extended) with full coverage of PROMPT-01..04 + the AUTOFIX-05 ledger helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-fix-prompt-builder-wrong-citation-vertical-slice/42-CONTEXT.md
@.planning/phases/42-fix-prompt-builder-wrong-citation-vertical-slice/42-RESEARCH.md
@.planning/phases/41-verifier-gate-workflow-verify-single-case-mjs-cli-shim/41-01-SUMMARY.md

# Source files this plan EXTENDS or MIRRORS
@tests/e2e/lib/issue-payload-builder.js
@tests/e2e/lib/llm-ledger.js
@scripts/check-diff-guard.mjs
@eslint.config.js
@tests/unit/eslint-sdk-guard.test.js
@tests/unit/issue-payload-builder.test.js
@tests/unit/llm-ledger.test.js
@tests/e2e/lib/error-codes.js

<interfaces>
<!-- Key contracts already in the codebase that Plan 42-01 must respect. -->
<!-- Executor should use these directly — no codebase exploration needed.   -->

From tests/e2e/lib/llm-ledger.js (Phase 39 — sibling pattern for countFixAttempts):
- export function phaseTotal(ledger, phase): number  // line 219
- export function appendLedgerEntry(ledgerPath, entry): void  // line 637
- Ledger shape: { version: 1, months: { '<YYYY-MM>': { invocations, total_usd, last_invocation_iso, iterations: [{iso, model, cost_usd, tokens_in, tokens_out, phase, transport, source, ...optional issueId, prNumber, fingerprint, ...}] } } }

From tests/e2e/lib/issue-payload-builder.js (Phase 35 — EXTEND in place):
- export const BUDGET_LLM_RATIONALE = 800
- export const BUDGET_VERIFIER_WINDOW = 600
- export const BUDGET_GOLDEN_DIFF = 400
- export const TRUNCATION_SUFFIX
- private fn truncate(text, budget): string
- private fn fenceCode(text): string
- private fn formatGoldenDiff(observed, golden): string
- export function buildIssuePayload({triageFinding, iteration, rerunEntry, goldenCitation, reproducerCmd, fingerprint}): {title, body, labels}
- LLM-derived inputs that need FORBIDDEN_DELIMITERS escape BEFORE truncate(): `rationale` (line 111), `reason` (line 110), the formatted `rawDiff` (line 157)

From tests/e2e/lib/error-codes.js (Phase 34):
- export const WRONG_CITATION, FLAKE, LLM_API_ERROR, PASS, LLM_HALLUCINATED_SELECTION, WORKER_FALLBACK_FAILED, GOOGLE_DOM_DRIFT, HARNESS_ERROR
- export const ERROR_CLASSES (frozen array of canonical class names — line 98)

From scripts/check-diff-guard.mjs (Phase 41 — Plan 42-02 imports; Plan 42-01 only documents):
- export const FORBIDDEN_PATHS: ReadonlyArray<RegExp>  (6 frozen regexes)
- export function checkDiffGuard(changedPaths): { ok: boolean, violations: string[] }

From eslint.config.js (current — line 197 catch-all):
- 4 per-file blocks BEFORE the catch-all (pdf-verifier, rerun-validator, triage-classifier+CLI, plus the catch-all itself at line 197)
- The catch-all (line 197) declares files: ['**/*.{js,mjs}'] with ignores: [5 paths listed at lines 199-205]
- CRITICAL Pitfall 1/Pitfall 3 (commit 345cdcb): each per-file block must INLINE the @anthropic-ai/sdk paths restriction; the catch-all's ignores list must be AUGMENTED with the new file's path
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Write failing Vitest cases for fix-prompt-builder + FORBIDDEN_DELIMITERS escape + countFixAttempts + ESLint purity guard</name>
  <files>tests/unit/fix-prompt-builder.test.js, tests/unit/issue-payload-builder.test.js, tests/unit/llm-ledger.test.js, tests/unit/eslint-fix-prompt-builder-guard.test.js</files>
  <read_first>
    - tests/unit/eslint-sdk-guard.test.js (the programmatic ESLint API pattern this task MIRRORS for the new guard test — same `new ESLint({overrideConfigFile: ...})` + `lintText(code, {filePath})` shape)
    - tests/unit/issue-payload-builder.test.js (existing 14 cases; APPEND new describe block for FORBIDDEN_DELIMITERS — do NOT modify existing cases)
    - tests/unit/llm-ledger.test.js (existing 54 cases; APPEND new describe block for countFixAttempts AFTER line 998 LEDGER-04 block — do NOT modify existing cases; reuse the existing tmpDir-per-test fixture pattern used in the Phase 39 LEDGER-01..03 blocks)
    - tests/e2e/lib/issue-payload-builder.js (the buildIssuePayload entry point this Task asserts behavior of, lines 95-183)
    - tests/e2e/lib/llm-ledger.js lines 200-243 (the phaseTotal export — countFixAttempts mirrors this defensive-filter shape exactly)
    - .planning/phases/42-fix-prompt-builder-wrong-citation-vertical-slice/42-RESEARCH.md §Code Examples Pattern 1, Pattern 2, Pattern 4, Pattern 5 (the inline code samples that fix-prompt-builder.js, the FORBIDDEN_DELIMITERS escape, countFixAttempts, and the ESLint guard test follow line-for-line)
  </read_first>
  <behavior>
    - tests/unit/fix-prompt-builder.test.js (NEW): 8+ Vitest cases:
      * envelope wraps a benign issue body exactly: `userPrompt === '<issue_body_untrusted>\n<body>\n</issue_body_untrusted>'`
      * envelope wraps when body contains newlines (multi-line case)
      * PROMPT_SCAFFOLDS is frozen (Object.isFrozen === true)
      * PROMPT_SCAFFOLDS has EXACTLY 1 key (`WRONG_CITATION`) — assert Object.keys().length === 1 (Phase 45 adds the other 4)
      * skip-class FLAKE → {ok:false, escalate:'re-quarantine'}
      * skip-class LLM_API_ERROR → {ok:false, escalate:'retry'}
      * skip-class PASS → {ok:false, escalate:'close-as-pass'}
      * unsupported errorClass (e.g., 'GOOGLE_DOM_DRIFT' which Phase 45 will add) → {ok:false, escalate: starts with 'unsupported-class:'}
      * WRONG_CITATION systemPrompt contains DIFF_FENCE_START + DIFF_FENCE_END literal strings (proves the output-format instruction landed)
      * WRONG_CITATION systemPrompt enumerates all 6 FORBIDDEN paths from the diff-guard bank (literal substrings: `tests/test-cases.js`, `tests/golden/baseline.json`, `tests/e2e/test-cases-quarantine.js`, `.github/workflows/v40-`, `tests/e2e/.llm-spend-ledger.json`, `.github/CODEOWNERS`)
    - tests/unit/issue-payload-builder.test.js (EXTEND with 1 new describe block "Phase 42 PROMPT-02: FORBIDDEN_DELIMITERS escape", 3-4 cases):
      * Crafted-payload rationale containing `</issue_body_untrusted>` is escaped in body output — body does NOT contain literal `</issue_body_untrusted>`
      * Crafted-payload verifier reason containing `<issue_body_untrusted>` is escaped
      * Benign content (e.g., "cite text at col 5") is unchanged — escape does not fire on innocent text (negative case per Pitfall 5)
      * FORBIDDEN_DELIMITERS export is Object.freeze()'d and contains exactly the 2 expected strings
    - tests/unit/llm-ledger.test.js (EXTEND with 1 new describe block "Phase 42 AUTOFIX-05: countFixAttempts helper", 5-6 cases):
      * Empty ledger → returns 0
      * Ledger with no `phase: '42-auto-fix'` entries → returns 0
      * Ledger with 3 matching fingerprint entries (across 1 month) → returns 3
      * Ledger with 2 matching entries in month A + 1 matching in month B (different ISO month buckets) → returns 3
      * Mixed: 5 entries with phase `42-auto-fix` but only 2 match fingerprint → returns 2
      * Defensive: null/undefined ledger → returns 0; non-string fingerprint → returns 0; empty fingerprint → returns 0
    - tests/unit/eslint-fix-prompt-builder-guard.test.js (NEW): 4 Vitest cases via programmatic ESLint API:
      * `import 'node:fs'` in fix-prompt-builder.js → message matches /fix-prompt-builder must be pure.*PROMPT-04/
      * `import 'node:child_process'` → same message
      * `import 'node:path'` → same message
      * `import '@anthropic-ai/sdk'` → message matches /Direct @anthropic-ai\/sdk imports forbidden/ (Pitfall 1 INLINE check — proves the per-file block ALSO carries the SDK restriction so the catch-all-clobber regression cannot recur)
    - ALL 4 test files MUST fail at this commit (RED gate) — the source files do not yet exist or do not yet have the new behavior. Verification command: `npx vitest run tests/unit/fix-prompt-builder.test.js tests/unit/issue-payload-builder.test.js tests/unit/llm-ledger.test.js tests/unit/eslint-fix-prompt-builder-guard.test.js` MUST exit non-zero with module-load failures or assertion failures.
  </behavior>
  <action>
    Write the 4 Vitest files per the behavior block. Use the existing test conventions:
    - Vitest `describe`/`it`/`expect` from 'vitest'; inline test data (no separate fixture files per Phase 41-01 convention)
    - For llm-ledger.test.js extension: APPEND a new describe block AFTER the existing LEDGER-04 block (line 994); use the existing tmpDir-per-test pattern from earlier describe blocks (mkdtempSync + writeFileSync seed + appendLedgerEntry on a tmpDir LEDGER_PATH); do NOT modify any of the 54 existing cases or any imports — Phase 39's import block already includes fs/path/os/spawnSync/fileURLToPath that this block reuses
    - For issue-payload-builder.test.js extension: APPEND a new describe block AFTER the existing tests; do NOT modify existing cases; reuse the `buildIssuePayload` import already at the top
    - For fix-prompt-builder.test.js: import the not-yet-existing module — the import failure IS the RED gate signal (matches Phase 41-01's "Direct-import RED gate" precedent — commit 47baad0)
    - For eslint-fix-prompt-builder-guard.test.js: MIRROR tests/unit/eslint-sdk-guard.test.js's import/setup shape verbatim (ESLint constructor + overrideConfigFile + lintText with filePath); the test file CAN exist before the ESLint config block — it fails because the ESLint config does not yet restrict fs/cp/path on fix-prompt-builder.js
    Commit with message: `test(42-01): RED — fix-prompt-builder envelope + FORBIDDEN_DELIMITERS escape + countFixAttempts + ESLint purity guard`
    DO NOT modify any source files in this task. The RED gate is a clean test-file-only commit.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/fix-prompt-builder.test.js tests/unit/issue-payload-builder.test.js tests/unit/llm-ledger.test.js tests/unit/eslint-fix-prompt-builder-guard.test.js 2>&1 | grep -vE '^#' | grep -E 'Error: Failed to load|Cannot find module|FAIL|failed' | head -5</automated>
  </verify>
  <done>
    4 test files exist; vitest run on the 4 files exits non-zero; git log shows commit prefixed `test(42-01): RED`; no source files modified in this commit (`git diff HEAD~1 --name-only` returns only test files); 54 pre-existing llm-ledger.test.js cases continue to pass (regression guard: `npx vitest run tests/unit/llm-ledger.test.js -t "Phase 31"` still passes if scoped tightly, but the new describe block fails — total file fail counts ≥6 new failing cases).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Ship fix-prompt-builder.js + FORBIDDEN_DELIMITERS escape in issue-payload-builder.js + countFixAttempts in llm-ledger.js + ESLint per-file block + catch-all ignores augmentation</name>
  <files>tests/e2e/lib/fix-prompt-builder.js, tests/e2e/lib/issue-payload-builder.js, tests/e2e/lib/llm-ledger.js, eslint.config.js</files>
  <read_first>
    - tests/e2e/lib/issue-payload-builder.js (full file 1-183 — the EXTEND target; the FORBIDDEN_DELIMITERS escape goes AFTER the existing exports section, lines 25-40 area; the helper is invoked inside buildIssuePayload BEFORE the truncate() calls on lines 129, 146, 158)
    - tests/e2e/lib/llm-ledger.js lines 200-243 (phaseTotal export — countFixAttempts MIRRORS this shape verbatim: defensive null-guard + month iteration + iteration filter + return count)
    - eslint.config.js full file (1-220) — the per-file block insertion point is BEFORE the catch-all (current line 197); the catch-all's ignores list at lines 199-205 must be AUGMENTED with `'tests/e2e/lib/fix-prompt-builder.js'` in the SAME commit per Pitfall 1/commit 345cdcb (each of the 4 existing per-file blocks also INLINES the @anthropic-ai/sdk paths restriction — the new block does too)
    - scripts/check-diff-guard.mjs lines 18-25 (the comment block enumerating the 6 LOCKED FORBIDDEN_PATHS — fix-prompt-builder.js's WRONG_CITATION_SYSTEM block MUST list these 6 paths LITERALLY as forbidden-paths instructions to the LLM)
    - .planning/phases/42-fix-prompt-builder-wrong-citation-vertical-slice/42-RESEARCH.md §Code Examples Pattern 1 (the WRONG_CITATION_SYSTEM template text — copy verbatim into fix-prompt-builder.js including the DIFF_FENCE_START/END constants), Pattern 2 (the escapeForbiddenDelimiters helper shape — splice escape pattern that converts `</issue_body_untrusted>` to `</issue_body_untrusted-DELIMITER-ESCAPED-PHASE-42>` — preserves human readability while breaking the literal token), Pattern 4 (countFixAttempts implementation — keep the 7-LOC body shape per the research; this is testable in isolation), Pattern 5 (eslint.config.js per-file block AND catch-all augmentation)
  </read_first>
  <action>
    Create `tests/e2e/lib/fix-prompt-builder.js` per RESEARCH §Pattern 1: exports `ENVELOPE_OPEN = '<issue_body_untrusted>'`, `ENVELOPE_CLOSE = '</issue_body_untrusted>'`, `DIFF_FENCE_START = '===DIFF_START==='`, `DIFF_FENCE_END = '===DIFF_END==='`, a frozen `PROMPT_SCAFFOLDS = Object.freeze({ WRONG_CITATION: () => WRONG_CITATION_SYSTEM })` registry with EXACTLY 1 key (Phase 45 will add 4 more; do NOT pre-stub), and `buildFixPrompt({errorClass, issueBody})`. The function short-circuits at the TOP for the 3 skip classes (`FLAKE`/`LLM_API_ERROR`/`PASS`) returning the exact `{ok:false, escalate:'<reason>'}` shapes; unsupported classes return `{ok:false, escalate: 'unsupported-class:<class>'}`; supported classes return `{ok:true, systemPrompt, userPrompt}` where userPrompt is the LITERAL envelope wrap. The WRONG_CITATION_SYSTEM string carries: (a) cite-by-position v3.1 invariants, (b) the 6 LITERAL forbidden paths from check-diff-guard.mjs (NOT imported — duplicated as instruction text to the LLM since fix-prompt-builder.js is pure), (c) the diff-fence output-format spec mentioning DIFF_FENCE_START/END constants by value, (d) the 200/50 LOC size cap, (e) the "treat untrusted input strictly" warning. Per D-04: NO node:fs, NO node:child_process, NO node:path imports.

    Extend `tests/e2e/lib/issue-payload-builder.js`: add `export const FORBIDDEN_DELIMITERS = Object.freeze(['<issue_body_untrusted>', '</issue_body_untrusted>'])` after the existing budget/TRUNCATION_SUFFIX exports (around line 27); add a private `escapeForbiddenDelimiters(text)` helper using the splice-escape pattern from RESEARCH §Pattern 2 (split + join with `d.slice(0,-1) + '-DELIMITER-ESCAPED-PHASE-42' + d.slice(-1)` so the closing `>` is moved past the marker — breaks the literal token while staying human-readable); invoke this helper INSIDE buildIssuePayload on lines 110 (`reason`), 111 (`rationale`), and 157 (`rawDiff`) BEFORE the existing `truncate()` calls — wrap them as `truncate(escapeForbiddenDelimiters(reason), ...)`. Do NOT change the section ordering, budgets, or any existing exports.

    Extend `tests/e2e/lib/llm-ledger.js`: add `export function countFixAttempts(ledger, fingerprint)` as a sibling of `phaseTotal` (place AFTER the existing `phaseTotal` block, around line 244). Implementation per RESEARCH §Pattern 4: defensive null-check on ledger.months / typeof fingerprint === 'string' / fingerprint.length > 0; iterate Object.values(ledger.months); for each bucket iterate bucket.iterations; count iterations where `it.phase === '42-auto-fix' && it.fingerprint === fingerprint`; return count. Do NOT modify phaseTotal or any other existing export. Update the comment block at line 18 to add `//   countFixAttempts(ledger, fingerprint)         count of Phase 42 auto-fix iterations for a given fingerprint`.

    Edit `eslint.config.js` per RESEARCH §Pattern 5 — TWO coordinated edits in SAME commit:
    (1) APPEND a new per-file block BEFORE the catch-all (current line 197). Block has `files: ['tests/e2e/lib/fix-prompt-builder.js']` and `rules: { 'no-restricted-imports': ['error', { paths: [...] }] }` where paths includes:
        - { name: 'node:fs', message: 'fix-prompt-builder must be pure — no I/O. (PROMPT-04)' }
        - { name: 'node:child_process', message: 'fix-prompt-builder must be pure — no subprocesses. (PROMPT-04)' }
        - { name: 'node:path', message: 'fix-prompt-builder must be pure — no path computation. (PROMPT-04)' }
        - { name: '@anthropic-ai/sdk', message: 'Import via invokeAnthropicSdkWithLedger from tests/e2e/lib/llm-driver.js. Direct @anthropic-ai/sdk imports forbidden — Phase 39 LEDGER-03 single-entry-point rule.' }  ← Pitfall 1 INLINE
    (2) MUST also add `'tests/e2e/lib/fix-prompt-builder.js'` to the catch-all's `ignores` array (currently 5 entries at lines 200-204; becomes 6). Do NOT change the existing 5 entries or the surrounding block shape — the eslint-sdk-guard.test.js Test 2 (loosened per commit 345cdcb) asserts the array CONTAINS the driver path, so adding a 6th entry is safe.

    Add a leading comment block to fix-prompt-builder.js documenting D-04 (purity invariant), PROMPT-01 (envelope), PROMPT-03 (registry), and PROMPT-04 (ESLint guard). All 4 Task-1 tests MUST PASS after this commit. All 54 pre-existing llm-ledger.test.js cases MUST continue to pass. All 14 pre-existing issue-payload-builder.test.js cases MUST continue to pass.

    Commit with message: `feat(42-01): fix-prompt-builder + FORBIDDEN_DELIMITERS escape + countFixAttempts + ESLint purity guard`
  </action>
  <verify>
    <automated>npx vitest run tests/unit/fix-prompt-builder.test.js tests/unit/issue-payload-builder.test.js tests/unit/llm-ledger.test.js tests/unit/eslint-fix-prompt-builder-guard.test.js 2>&1 | tail -5 | grep -E 'passed.*\(.*\)|Test Files.*passed'</automated>
  </verify>
  <done>
    All 4 test files PASS (≥6 new cases for fix-prompt-builder + ≥3 new for FORBIDDEN_DELIMITERS + ≥5 new for countFixAttempts + 4 for ESLint guard = ≥18 new green cases); `npm run test:src` exits 0 with NO regressions (~835+ cases passing); `npm run lint` exits 0; `git log --oneline -1` shows `feat(42-01): ...`; grep verification for the source files:
    - `grep -c "ENVELOPE_OPEN\|ENVELOPE_CLOSE\|PROMPT_SCAFFOLDS\|buildFixPrompt" tests/e2e/lib/fix-prompt-builder.js` returns ≥4
    - `grep -c "FORBIDDEN_DELIMITERS\|escapeForbiddenDelimiters" tests/e2e/lib/issue-payload-builder.js` returns ≥2
    - `grep -c "countFixAttempts" tests/e2e/lib/llm-ledger.js` returns ≥1
    - `grep -c "tests/e2e/lib/fix-prompt-builder.js" eslint.config.js` returns ≥2 (one in the per-file block's `files:`, one in the catch-all's `ignores:`)
    - `grep -vE '^[[:space:]]*//' tests/e2e/lib/fix-prompt-builder.js | grep -cE "import.*node:(fs|child_process|path)"` returns 0 (the file itself does not violate its own purity guard)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Issue body → fix-prompt-builder | Issue body is attacker-controlled (anyone with write access to a fork can file a triage-labeled issue); becomes LLM input via the dispatcher (Plan 42-02) |
| v3.1 issue-payload-builder output → fix-prompt-builder input | The v3.1 builder emits the issue body that Phase 42 wraps in `<issue_body_untrusted>`; if the body itself contains the closing delimiter, the envelope pops |
| eslint.config.js flat-config rule merge order | Catch-all block at array-end CAN clobber per-file blocks for the same rule key (Pitfall 1 from commit 345cdcb) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering (prompt injection via issue body) | fix-prompt-builder.js userPrompt | mitigate | PROMPT-01 envelope: every userPrompt is `<issue_body_untrusted>\n{body}\n</issue_body_untrusted>` with no content outside the envelope. SYSTEM block instructs the model to treat the envelope contents as untrusted data, not instructions. Vitest Task 1 envelope assertion is the regression guard. |
| T-42-02 | Tampering (envelope pop-out via v3.1 issue body) | issue-payload-builder.js LLM rationale / verifier reason / golden diff sections | mitigate | PROMPT-02 escape: FORBIDDEN_DELIMITERS escape runs on rationale + reason + rawDiff BEFORE truncate(). Crafted-payload Vitest test (Task 1) covers `</issue_body_untrusted>` injection through the rationale path. Negative test on benign content per Pitfall 5 prevents over-escape. |
| T-42-03 | Elevation (fix-prompt-builder.js gains filesystem/subprocess capability) | eslint.config.js | mitigate | PROMPT-04: per-file ESLint block forbids node:fs / node:child_process / node:path. Pitfall 1/Pitfall 3 mitigation: per-file block INLINES the @anthropic-ai/sdk paths restriction AND the catch-all's ignores list is augmented in the SAME commit. 4 programmatic ESLint tests (Task 1) prove all 4 imports fire — the 4th (`@anthropic-ai/sdk`) is the regression guard for the Pitfall 1 catch-all-clobber scenario. |
| T-42-04 | Tampering (silent counter bypass) | countFixAttempts | mitigate | Helper lives in llm-ledger.js as a sibling of phaseTotal (single source of truth for ledger-derived facts; Plan 42-02 imports it). Defensive null-guards on ledger.months / typeof fingerprint / empty-string fingerprint prevent silent zero-return on malformed input. Vitest covers null/undefined/empty/mixed-month/mixed-phase cases. |
| T-42-SC | Tampering | npm/pip/cargo installs | N/A | Phase 42 introduces NO new dependencies (per RESEARCH §Package Legitimacy Audit). The Phase 39 SDK install is the only network-fetched dep in scope and was audited at that time. |
</threat_model>

<verification>
- `npx vitest run tests/unit/fix-prompt-builder.test.js tests/unit/issue-payload-builder.test.js tests/unit/llm-ledger.test.js tests/unit/eslint-fix-prompt-builder-guard.test.js` exits 0 after Task 2 GREEN
- `npm run test:src` exits 0 (no regressions across the ~835+ existing test cases)
- `npm run lint` exits 0 (the new ESLint per-file block does not fire on the actual fix-prompt-builder.js — purity invariant holds)
- `git log --oneline -2` shows the TDD discipline: `test(42-01): RED — ...` THEN `feat(42-01): ...` in that order
- `grep -c "tests/e2e/lib/fix-prompt-builder.js" eslint.config.js` returns exactly 2 (per-file block files: + catch-all ignores:)
- `grep -vE '^[[:space:]]*//' tests/e2e/lib/fix-prompt-builder.js | grep -cE "^import.*node:(fs|child_process|path)"` returns 0
- `grep -c "^export function countFixAttempts" tests/e2e/lib/llm-ledger.js` returns 1
- `grep -c "^export const FORBIDDEN_DELIMITERS" tests/e2e/lib/issue-payload-builder.js` returns 1
</verification>

<success_criteria>
- PROMPT-01 satisfied: every generated WRONG_CITATION prompt has userPrompt wrapped in `<issue_body_untrusted>...</issue_body_untrusted>` exactly (Vitest pinned)
- PROMPT-02 satisfied: issue-payload-builder.js escapes both opening AND closing delimiter strings in rationale + verifier reason + golden diff sections (Vitest crafted-payload + negative-content cases pinned)
- PROMPT-03 satisfied: PROMPT_SCAFFOLDS is Object.freeze()'d with exactly 1 key (WRONG_CITATION); skip-class returns are the exact 3 shapes from CONTEXT (Vitest pinned)
- PROMPT-04 satisfied: ESLint fires on all 4 forbidden imports in fix-prompt-builder.js including @anthropic-ai/sdk (Vitest programmatic ESLint pinned); catch-all-clobber regression prevented by both the INLINE @anthropic-ai/sdk restriction in the per-file block AND the catch-all ignores augmentation in the same commit
- AUTOFIX-05 helper ready: countFixAttempts lives in llm-ledger.js as sibling of phaseTotal; filters by phase==='42-auto-fix' + fingerprint; defensive nulls handled (Vitest pinned); Plan 42-02 can `import { countFixAttempts } from '../tests/e2e/lib/llm-ledger.js'`
- TDD discipline: RED commit precedes GREEN commit; RED touches only test files; GREEN touches only source files (+ eslint.config.js)
- Pitfall 1/Pitfall 3 (345cdcb regression) actively prevented: both per-file inline + catch-all augmentation; 4th programmatic ESLint test is the explicit regression guard
- No regressions: all 54 pre-existing llm-ledger.test.js cases + 14 pre-existing issue-payload-builder.test.js cases + 817 full suite cases continue to pass
</success_criteria>

<output>
Create `.planning/phases/42-fix-prompt-builder-wrong-citation-vertical-slice/42-01-SUMMARY.md` documenting:
- The 2 TDD commits (RED + GREEN)
- Exact line counts added to each source file
- Confirmation that the Pitfall 1/345cdcb regression is actively guarded (both per-file inline + catch-all augmentation + 4th programmatic ESLint test)
- Confirmation that PROMPT_SCAFFOLDS has exactly 1 key (Phase 45 will add the other 4)
- Confirmation that all 4 PROMPT-* requirements + the AUTOFIX-05 helper are shipped
- Note that Plan 42-02 can now `import { buildFixPrompt, PROMPT_SCAFFOLDS, ENVELOPE_OPEN, ENVELOPE_CLOSE, DIFF_FENCE_START, DIFF_FENCE_END } from '../tests/e2e/lib/fix-prompt-builder.js'` and `import { countFixAttempts } from '../tests/e2e/lib/llm-ledger.js'`
</output>