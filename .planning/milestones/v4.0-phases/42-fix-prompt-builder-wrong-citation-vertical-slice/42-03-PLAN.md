---
phase: 42-fix-prompt-builder-wrong-citation-vertical-slice
plan: 03
type: execute
wave: 3
depends_on: [42-01, 42-02]
files_modified:
  - docs/v40-auto-fix-manual-demo.md
autonomous: false
requirements: [AUTOFIX-01, AUTOFIX-03, AUTOFIX-04, AUTOFIX-05]
tags: [manual-demo, end-to-end, human-uat, wrong-citation, issue-3, real-sdk-call, verifier-gate-advisory]
user_setup:
  - service: anthropic-api
    why: "The Phase 42 dispatcher's `--force-api` path makes a real SDK call against the Anthropic API; the manual demo proves the end-to-end loop with a real (not mocked) Sonnet 4.6 call against GitHub issue #3."
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> Settings -> API Keys (https://console.anthropic.com/settings/keys); paste a key starting with `sk-ant-` into the executor's local shell BEFORE running the dispatcher: `export ANTHROPIC_API_KEY=sk-ant-...`"
    dashboard_config:
      - task: "Confirm Phase 39 monthly + daily + per-issue + per-PR caps still allow ~$0.30 of headroom"
        location: "Inspect `tests/e2e/.llm-spend-ledger.json` for current month bucket totals; the dispatcher refuses the call if any sub-cap is exhausted"
  - service: github
    why: "Dispatcher uses `gh issue view`, `gh issue comment`, `gh label create`, `gh issue edit`, `gh pr create`; requires authenticated gh CLI in the executor's shell."
    env_vars:
      - name: "(none — gh uses its own keychain)"
        source: "Run `gh auth status` to confirm; if not authenticated run `gh auth login`"
    dashboard_config:
      - task: "Confirm issue #3 (US11427642-spec-short-1: WRONG_CITATION, fingerprint 139f821b3bb1) is still OPEN with the WRONG_CITATION label"
        location: "https://github.com/tonyrowles/patent-cite-tool/issues/3 — if closed or fingerprint changed, fall back to: `gh issue list --label WRONG_CITATION --state open --json number,title,labels --jq 'sort_by(.number)[0]'` and use the lowest-numbered open issue"

must_haves:
  truths:
    - "A single real invocation of `node scripts/auto-fix.mjs --issue 3 --force-api --no-push` (or its fallback target if #3 is gone) produces a clean ledger entry, applies a real LLM-emitted diff via git apply, and stages a local commit on branch `auto-fix/3-139f821b`"
    - "The manual `gh pr create --draft` invocation opens a PR whose body contains `<!-- affected_cases: US11427642-spec-short-1 -->` for the verifier-gate parser"
    - "Phase 41-01's verifier-gate workflow (`v40-verifier-gate.yml`) ADVISORILY runs against the PR (NOT yet binding as a required-status-check — Phase 47 wires that); the workflow either flips draft→ready with `auto-fix:verified` label OR comments with a rejection reason"
    - "docs/v40-auto-fix-manual-demo.md documents the exact procedure so a future executor (Phase 47 HUMAN-UAT) can re-run it"
    - "42-03-SUMMARY.md captures: (a) the SDK call ledger entry ID + iso + cost_usd, (b) the local branch URL after push (or the local sha + `git stash`-equivalent state if --no-push), (c) the PR URL, (d) the Phase 41 verifier-gate workflow-run URL, (e) the final draft-status / labels / ready-flip evidence"
  artifacts:
    - path: "docs/v40-auto-fix-manual-demo.md"
      provides: "Documented end-to-end demo procedure — re-usable by Phase 47 CLEANUP-03 HUMAN-UAT (a)"
      contains: "auto-fix/3-139f821b"
      min_lines: 60
    - path: ".planning/phases/42-fix-prompt-builder-wrong-citation-vertical-slice/42-03-SUMMARY.md"
      provides: "Captured evidence — ledger entry, branch URL, PR URL, workflow-run URL, label state"
  key_links:
    - from: "scripts/auto-fix.mjs (Plan 42-02)"
      to: "GitHub issue #3"
      via: "`gh issue view 3 --json body,labels,title,number,assignees`; fingerprint parsed from line 1 `<!-- fp: 139f821b3bb1 -->`"
      pattern: "139f821b"
    - from: "Branch auto-fix/3-139f821b"
      to: "v40-verifier-gate.yml (Phase 41-03)"
      via: "pull_request.opened on auto-fix/* head branch — workflow auto-triggers; parses affected_cases comment; runs 3× verifier + 76-case regression"
      pattern: "auto-fix/*"
    - from: "Phase 39 ledger committed file"
      to: "first real SDK call entry"
      via: "appendLedgerEntry inside invokeAnthropicSdkWithLedger — Pitfall 8 always-append discipline; entry has phase:'42-auto-fix', transport:'sdk', fingerprint, issueId:'issue-3', cost_usd > 0"
      pattern: "42-auto-fix"
---

<objective>
Demonstrate the full Phase 42 vertical slice end-to-end on a real GitHub issue (#3 by default; lowest-fingerprint open WRONG_CITATION issue as fallback): real SDK call → real diff → real branch → real PR → real verifier-gate run. Document the procedure in `docs/v40-auto-fix-manual-demo.md` for Phase 47 HUMAN-UAT (a) reuse. Capture all evidence (ledger entry, branch URL, PR URL, workflow-run URL, ready-flip state) in the SUMMARY.

Purpose: This is the "does it actually work?" gate for the entire v4.0 milestone — every primitive that v4.0 ships (Phase 39 SDK driver + caps, Phase 41 verifier-gate, Phase 42 prompt-builder + dispatcher) is exercised in a single user-driven invocation. A green outcome here unblocks Phase 43 (workflow lift) with empirical confidence; a red outcome flags whatever primitive failed BEFORE Phase 43 invests in workflow plumbing.

Output: 1 documentation file (the procedure) + a SUMMARY capturing the captured evidence. NO new production code. The plan is autonomous: false because the SDK call costs real money and the PR opens against the maintainer's live repo — only the maintainer should execute it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-fix-prompt-builder-wrong-citation-vertical-slice/42-CONTEXT.md
@.planning/phases/42-fix-prompt-builder-wrong-citation-vertical-slice/42-RESEARCH.md
@.planning/phases/42-fix-prompt-builder-wrong-citation-vertical-slice/42-01-SUMMARY.md
@.planning/phases/42-fix-prompt-builder-wrong-citation-vertical-slice/42-02-SUMMARY.md
@.planning/phases/41-verifier-gate-workflow-verify-single-case-mjs-cli-shim/41-04-SUMMARY.md
@scripts/auto-fix.mjs

<interfaces>
<!-- The dispatcher's CLI contract (Plan 42-02 output) and the live demo target -->

Dispatcher CLI:
  node scripts/auto-fix.mjs --issue <n> [--transport sdk] [--force-api] [--dry-run] [--no-push]
  Exit codes: 0=success/skip; 1=apply-check/diff-guard/malformed/sdk-error; 2=arg/contract; 3=cap

Live demo target (verified at research time 2026-05-31):
  - Repo: github.com/tonyrowles/patent-cite-tool
  - Issue: #3
  - Title: [e2e-nightly] US11427642-spec-short-1: WRONG_CITATION
  - Fingerprint: 139f821b3bb1 (full); 139f821b (fp8)
  - Expected branch name: auto-fix/3-139f821b
  - Expected affected case-id: US11427642-spec-short-1
  - Expected PR body comment: <!-- affected_cases: US11427642-spec-short-1 -->
  - Fallback if issue #3 is closed: `gh issue list --label WRONG_CITATION --state open --json number,title,labels --jq 'sort_by(.number)[0]'`

Phase 41 verifier-gate (already shipped):
  - Workflow: .github/workflows/v40-verifier-gate.yml
  - Trigger: pull_request.opened/synchronize/reopened with head ref matching auto-fix/*
  - Jobs: diff-guard → (verifier-gate, regression-suite) → ready-flip
  - On pass: adds label `auto-fix:verified`; flips PR draft→ready
  - On fail: stays draft; comments with rejection reason
  - Phase 47 will bind verifier-gate as a required-status-check on the ruleset; Phase 42 demo runs it ADVISORILY only

Cost expectation:
  - Sonnet 4.6: ~5k input tokens + ~1k output tokens per WRONG_CITATION fix
  - Projected cost: $0.05-$0.15 per demo invocation
  - First invocation: full cost (no cache hit)
  - Second invocation within 1h on the same SYSTEM block: ~30% cheaper IF Pitfall 6 fix from Plan 42-02 works correctly (array-form system block enables cache_control)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/v40-auto-fix-manual-demo.md (procedure document)</name>
  <files>docs/v40-auto-fix-manual-demo.md</files>
  <read_first>
    - scripts/auto-fix.mjs (Plan 42-02 output — the CLI shape this doc describes)
    - .planning/phases/41-verifier-gate-workflow-verify-single-case-mjs-cli-shim/41-04-SUMMARY.md (the Phase 41 verifier-gate manual-test doc — this is the structural template; mirror its section layout)
    - .planning/phases/42-fix-prompt-builder-wrong-citation-vertical-slice/42-RESEARCH.md §Pattern 7 (the verified gh issue view 3 JSON shape — copy the body/labels block verbatim as the "Expected pre-state" reference), §System Architecture Diagram (the 18-step dispatcher sequence — the doc references which steps map to which executor actions), Pitfall 4 (decoupled dispatcher/verifier semantics — document the "exit 0 from dispatcher does NOT mean verifier-gate passed" caveat), §Open Questions Q3 (concurrent-invocation caveat — local-only acceptance documented in the doc)
  </read_first>
  <action>
    Create `docs/v40-auto-fix-manual-demo.md` (~60-120 LOC) with these sections in order:

    1. **Purpose & When To Use** (1 paragraph): briefly state the doc is the Phase 42 manual demo procedure — to be run once by the maintainer to validate the end-to-end loop before Phase 43 workflows it, and re-run by Phase 47 CLEANUP-03 HUMAN-UAT (a) for milestone close.

    2. **Prereqs Checklist** (bulleted): authenticated `gh` CLI (`gh auth status` returns OK); `ANTHROPIC_API_KEY` exported (`echo $ANTHROPIC_API_KEY | head -c 7` should show `sk-ant-`); clean working tree on a checkout of `main` (`git status` empty, `git rev-parse --abbrev-ref HEAD` = `main`); Phase 39 monthly + daily + per-issue + per-PR sub-caps have ~$0.30 headroom (`node -e "const {readLedger,combinedMonthlyTotal,DAY_HARD_CAP_USD,ISSUE_HARD_CAP_USD} = await import('./tests/e2e/lib/llm-ledger.js'); const l=readLedger(); console.log('monthly:', combinedMonthlyTotal(l).toFixed(2));"`); npm dependencies installed (`npm ci --no-audit --no-fund` if `node_modules/` is absent).

    3. **Target Selection** (numbered): (a) verify issue #3 is OPEN via `gh issue view 3 --json state,labels,title --jq '{state,labels:[.labels[].name],title}'` — confirm state=OPEN, labels include `WRONG_CITATION`, title matches `[e2e-nightly] US11427642-spec-short-1: WRONG_CITATION`; (b) if #3 is closed/missing, fall back to `gh issue list --label WRONG_CITATION --state open --json number,title,body --jq 'sort_by(.number)[0]'` and capture the chosen `number`, `title`, body's `<!-- fp: ... -->` line as the demo target; (c) confirm the fingerprint via `gh issue view <n> --json body --jq '.body' | head -1` returns `<!-- fp: <12-hex> -->`.

    4. **Dry-Run Validation** (2 commands): (a) `node scripts/auto-fix.mjs --issue 3 --dry-run` — expected: prints the rendered systemPrompt (200/50 LOC cap; forbidden paths; envelope notice) + userPrompt (envelope-wrapped issue body); no ledger entry written (confirm via `git diff tests/e2e/.llm-spend-ledger.json` empty); no SDK call (confirm by no monthly delta); exit 0. (b) inspect the printed userPrompt — confirm it starts with `<issue_body_untrusted>` and ends with `</issue_body_untrusted>` (Plan 42-01 envelope assertion).

    5. **Real Invocation** (1 command, 1 capture): `node scripts/auto-fix.mjs --issue 3 --force-api --no-push 2>&1 | tee /tmp/auto-fix-demo-$(date +%s).log` — expected: SDK call landed; ledger entry appended; diff applied; branch `auto-fix/3-139f821b` checked out; commit staged locally (NOT pushed — `--no-push` set so the maintainer can inspect first); stdout includes "branch staged locally; push manually with: git push -u origin auto-fix/3-139f821b" + the suggested gh pr create command. Capture the log file path.

    6. **Branch Inspection** (3 commands): `git log --oneline -1` (shows the commit on the branch); `git diff main..auto-fix/3-139f821b` (shows the LLM-emitted diff); `git diff --stat main..auto-fix/3-139f821b` (confirms diff size is within the 200 LOC src / 50 LOC tests cap). If diff is reasonable, proceed; if pathological, abort and re-run with --dry-run to iterate on prompt; document the abort in the SUMMARY.

    7. **Push + PR Creation** (2 commands): `git push -u origin auto-fix/3-139f821b`; then `gh pr create --draft --base main --head auto-fix/3-139f821b --title 'auto-fix: WRONG_CITATION for US11427642-spec-short-1' --body "$(cat <<EOF
    <!-- affected_cases: US11427642-spec-short-1 -->

    Phase 42 manual demo of the v4.0 auto-fix vertical slice.

    - Issue: #3
    - Fingerprint: 139f821b3bb1
    - fix_attempts: 1
    - Ledger entry: <paste-iso-from-step-5-log>

    /cc @tonyrowles
    EOF
    )"` — capture the printed PR URL.

    8. **Verifier-Gate Observation** (1 command + 1 wait): `gh pr view <pr-number> --json statusCheckRollup,isDraft,labels` — confirm verifier-gate workflow is queued/in-progress within 30s; `gh run watch <workflow-run-id>` — observe to completion. On pass: PR auto-flips draft→ready + gains `auto-fix:verified` label. On fail: PR stays draft + gains a rejection-reason comment. Document the outcome.

    9. **Cleanup** (optional, 2 commands): if the demo PR is being closed without merging (typical for a v4.0 demo — the actual fix flow merges through Phase 43): `gh pr close <pr-number> --delete-branch` cleans up both the PR and the remote branch; `git checkout main && git branch -D auto-fix/3-139f821b` cleans up local state.

    10. **Caveats** (3 bullets): (a) Pitfall 4 — dispatcher exit 0 does NOT mean verifier-gate will pass (the gate runs asynchronously on the PR-side); the manual demo's outcome is the gate's verdict, not the dispatcher's. (b) RESEARCH Open Q3 — concurrent dispatcher invocations from two terminals are NOT prevented at the Phase 42 dispatcher level (Phase 43 workflow concurrency group is the proper defense); the demo must be single-terminal-only. (c) The demo costs ~$0.05-$0.15 against the Anthropic API; the Phase 39 caps will refuse if any sub-cap is exhausted (monthly $100 / day $10 / per-issue $1 / per-PR $2).

    Commit message: `docs(42-03): manual demo procedure for v4.0 auto-fix vertical slice`. NO source-file modifications in this task.
  </action>
  <verify>
    <automated>test -f docs/v40-auto-fix-manual-demo.md && wc -l docs/v40-auto-fix-manual-demo.md | awk '{ if ($1 >= 60) print "OK"; else print "FAIL: doc too short ("$1" lines)" }' && grep -cE "auto-fix/3-139f821b|node scripts/auto-fix.mjs|gh pr create|--dry-run|--force-api|--no-push|ANTHROPIC_API_KEY|verifier-gate" docs/v40-auto-fix-manual-demo.md | awk '{ if ($1 >= 8) print "OK references"; else print "FAIL: missing key references" }'</automated>
  </verify>
  <done>
    docs/v40-auto-fix-manual-demo.md exists with ≥60 LOC; contains ≥8 references to the load-bearing CLI flags / target / workflow names; structural template mirrors Phase 41-04's `docs/v40-verifier-gate-manual-test.md`; commit landed on the worktree branch (NOT yet merged to main — the maintainer will rebase if cleanup is needed).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: HUMAN-VERIFY — Maintainer executes the demo procedure and pastes captures</name>
  <files>(none — checkpoint task; no file modifications by Claude; the maintainer produces evidence captures that the orchestrator commits into 42-03-SUMMARY.md after the resume-signal)</files>
  <action>
    THIS IS A CHECKPOINT TASK — Claude does NOT execute the action. The maintainer executes the procedure in `docs/v40-auto-fix-manual-demo.md` (Task 1 output) end-to-end, captures the 5 evidence pieces enumerated in <how-to-verify> below, and pastes them in the resume-signal. The orchestrator commits the captures into `.planning/phases/42-fix-prompt-builder-wrong-citation-vertical-slice/42-03-SUMMARY.md` after the maintainer approves.

    This CANNOT be automated by Claude because:
    - It consumes the maintainer's Anthropic API budget ($0.05-$0.15 per invocation)
    - It opens a real PR against the public repo — the maintainer's discretion on title/body wording matters
    - It triggers the Phase 41 verifier-gate workflow against the live PR
    - The outcome (verifier-gate pass/fail + ready-flip evidence) is the load-bearing signal for the entire v4.0 vertical slice
  </action>
  <verify>
    <automated>echo "MISSING — Wave 3 manual demo: evidence captured in 42-03-SUMMARY.md after maintainer resume-signal; no automated verification possible (real SDK call + real PR + live workflow run)"</automated>
  </verify>
  <done>
    Maintainer has typed "approved" in the checkpoint UI AND pasted all 5 evidence captures listed in <how-to-verify> below; the orchestrator has committed `42-03-SUMMARY.md` containing the captures (ledger entry JSON, branch URL, PR URL, verifier-gate workflow-run URL, final PR state with isDraft + labels + check conclusions). Acceptable outcomes: (a) gate PASS (isDraft=false + `auto-fix:verified` label), or (b) gate FAIL (isDraft=true + rejection comment — proves the gate caught a bad fix; iteration is the next step but Phase 42 is still considered complete since the wiring is empirically validated).
  </done>
  <what-built>
    Plan 42-01 shipped the pure prompt-builder + escape + ESLint guard + countFixAttempts helper.
    Plan 42-02 shipped the dispatcher + the systemBlocks driver extension.
    Plan 42-03 Task 1 shipped the demo procedure doc (`docs/v40-auto-fix-manual-demo.md`).

    This checkpoint is the actual end-to-end demo: a real Anthropic SDK call against GitHub issue #3 producing a real auto-fix branch + PR. The dispatcher's logic was unit-tested with mocks in Plan 42-02; this checkpoint replaces the mocks with the live services (Anthropic + GitHub + git) and confirms the wiring holds end-to-end.
  </what-built>
  <how-to-verify>
    Execute the procedure documented in `docs/v40-auto-fix-manual-demo.md` (Plan 42-03 Task 1) STEP BY STEP. Capture the following 5 pieces of evidence and paste them in the resume-signal:

    **Step 1 — Prereq check captures (1 line each):**
    - Output of `gh auth status | head -3`
    - Output of `echo $ANTHROPIC_API_KEY | head -c 7` (should print `sk-ant-` followed by nothing — confirming the key is set without leaking the secret)
    - Output of `git status --short && git rev-parse --abbrev-ref HEAD`

    **Step 2 — Target confirmation (full JSON, ≤10 lines):**
    - Output of `gh issue view 3 --json state,labels,title,body --jq '{state,labels:[.labels[].name],title,fp:(.body|split("\n")[0])}'`
    - If issue #3 is gone, paste the fallback target's identifier + title + fingerprint and note "FALLBACK TARGET: #<n>" prominently.

    **Step 3 — Dry-run evidence (last 20 lines of the dry-run output):**
    - Output of `node scripts/auto-fix.mjs --issue 3 --dry-run 2>&1 | tail -20`
    - Confirm: (a) "(dry-run: SDK not invoked..." line present, (b) systemPrompt mentions the 6 FORBIDDEN paths, (c) userPrompt is envelope-wrapped (starts with `<issue_body_untrusted>`), (d) exit 0.

    **Step 4 — Real invocation evidence (5 lines):**
    - Output of `node scripts/auto-fix.mjs --issue 3 --force-api --no-push 2>&1 | tail -10` — confirm the success line + "branch staged locally" message + suggested gh pr create command
    - Exit code: `echo $?` (should be 0)
    - Ledger entry: `node -e "const {readLedger}=await import('./tests/e2e/lib/llm-ledger.js'); const l=readLedger(); const m=Object.values(l.months).pop(); const last=m.iterations[m.iterations.length-1]; console.log(JSON.stringify(last,null,2));"` — confirm the entry has `phase:'42-auto-fix', transport:'sdk', fingerprint:'139f821b3bb1', issueId:'issue-3', cost_usd > 0, model:'claude-sonnet-4-6'`

    **Step 5 — Branch + PR + verifier-gate evidence (4 captures):**
    - Branch URL after push: `git push -u origin auto-fix/3-139f821b` then `gh browse --branch auto-fix/3-139f821b --no-browser` (or just paste the resulting `git push` output's "remote: " URL)
    - PR creation: paste output of `gh pr create --draft --base main --head auto-fix/3-139f821b --title 'auto-fix: WRONG_CITATION for US11427642-spec-short-1' --body '<paste-body-from-doc-step-7>'` — capture the printed PR URL
    - Verifier-gate workflow run URL: `gh pr view <pr-num> --json statusCheckRollup --jq '.statusCheckRollup[]|select(.name|contains("verifier-gate")).detailsUrl'`
    - Final state after the gate completes: `gh pr view <pr-num> --json isDraft,labels,statusCheckRollup --jq '{isDraft,labels:[.labels[].name],check:[.statusCheckRollup[]|{name,conclusion}]}'` — confirm either (a) isDraft=false + labels includes `auto-fix:verified` (PASS), or (b) isDraft=true + a rejection comment (FAIL — document the reason).

    If the verifier-gate FAILS: that is also a valid Phase 42 outcome — it proves the gate caught a bad fix. Document the failure mode in the resume-signal; the Phase 47 HUMAN-UAT will re-run with whatever fixes the iteration produces.

    If any prereq fails or a step errors before Step 5: paste the error output, hit "needs changes" in the checkpoint UI, and describe what failed so the orchestrator can dispatch a follow-up plan.
  </how-to-verify>
  <resume-signal>
    Type "approved" with the 5 captures pasted in the message — orchestrator will commit them to `42-03-SUMMARY.md`. Alternatively type "needs changes — <description>" if the demo failed in a way that requires re-planning.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ANTHROPIC_API_KEY env var → maintainer's local shell | Real billable credential; must not be logged or committed |
| Live GitHub PR body → public repo | PR body content visible to the world; must not contain secrets or PII |
| Verifier-gate workflow → main branch artifacts | Workflow is ADVISORY in Phase 42 (Phase 47 binds it as required-status-check); a bad gate output here cannot block main directly, but it can mislead the maintainer if not interpreted correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-11 | Information Disclosure | Maintainer's ANTHROPIC_API_KEY in shell | mitigate | The procedure's Step 1 prereq uses `head -c 7` to confirm the key is set without printing more than `sk-ant-`; the dispatcher itself never logs the key (it's consumed by @anthropic-ai/sdk client constructor); the demo log redirected to /tmp does NOT contain the key (the SDK request body sent to Anthropic does, but that's the intended channel). |
| T-42-12 | DoS (cost runaway in the demo itself) | Multiple back-to-back demo invocations | mitigate | --no-push flag lets the maintainer inspect the branch BEFORE pushing — if the diff is pathological, abort without invoking another SDK call. The AUTOFIX-04 git ls-remote idempotency prevents a re-run from invoking the SDK once a branch exists. Phase 39 per-issue $1 cap means at most ~7-20 demo invocations before the cap blocks. |
| T-42-13 | Tampering (Pitfall 4 — exit 0 misinterpreted as gate-pass) | Maintainer's interpretation of dispatcher output | mitigate | The procedure doc Step 8 explicitly separates "dispatcher exit 0" from "verifier-gate verdict"; the resume-signal MUST capture the gate's final state (isDraft + labels) — not just the dispatcher's exit code. Pitfall 4 documented in the doc's Caveats section. |
| T-42-14 | Information Disclosure (PII / secrets in PR body) | gh pr create --body | mitigate | The procedure doc Step 7 uses a HEREDOC body with explicit fields (affected_cases comment, fingerprint, fix_attempts, ledger entry ID) — no free-form interpolation of issue body content; the body cannot accidentally include the issue's verifier-window text or rationale. |
| T-42-SC | Tampering | npm/pip/cargo installs | N/A | This plan introduces NO dependencies and NO code; it only adds a docs file + executes existing primitives. |
</threat_model>

<verification>
- `test -f docs/v40-auto-fix-manual-demo.md` returns 0; `wc -l docs/v40-auto-fix-manual-demo.md` ≥60
- `grep -c "auto-fix/3-139f821b" docs/v40-auto-fix-manual-demo.md` returns ≥2 (target + branch references)
- `grep -c "ANTHROPIC_API_KEY\|sk-ant-" docs/v40-auto-fix-manual-demo.md` returns ≥1 (prereq mentioned)
- `grep -c "Pitfall 4\|exit 0 does NOT mean" docs/v40-auto-fix-manual-demo.md` returns ≥1 (caveat documented)
- Checkpoint resume-signal contains all 5 evidence captures (prereqs / target / dry-run / real invocation / branch+PR+gate); 42-03-SUMMARY.md commits the captures verbatim
- The maintainer's ANTHROPIC_API_KEY does NOT appear in 42-03-SUMMARY.md (only the `sk-ant-` prefix proof)
- The ledger entry captured in Step 4 has `phase:'42-auto-fix', transport:'sdk', fingerprint:'139f821b3bb1', issueId:'issue-3', cost_usd > 0`
- The final PR state from Step 5 is one of: (a) isDraft=false + labels include `auto-fix:verified` (success path — verifier-gate flipped to ready), or (b) isDraft=true + rejection comment (gate-caught-bad-fix path — also valid; documents the iteration loop)
</verification>

<success_criteria>
- Procedure doc exists, ≥60 LOC, covers all 10 sections from the action block
- Maintainer-executed demo produced ALL 5 evidence captures and posted them via the resume-signal
- 42-03-SUMMARY.md committed with the 5 captures, the demo log path, the SDK cost figure, and the final verifier-gate verdict (pass or fail — both are valid Phase 42 outcomes; only an INFRA failure (dispatcher crash, gate unreachable) is a blocker)
- All 8 PROMPT-01..04 + AUTOFIX-01/03/04/05 requirements are now empirically validated (not just unit-tested)
- The Phase 42 vertical slice is provably end-to-end: GitHub issue → dispatcher → SDK call → diff → branch → PR → verifier-gate
- Pitfall 4 caveat clearly communicated: dispatcher exit 0 ≠ verifier-gate pass; the gate's verdict is the load-bearing signal
- Concurrent-invocation acceptance (CONTEXT Q3) documented in the SUMMARY: Phase 42 is single-terminal-only; Phase 43 workflow concurrency group is the proper defense
- Phase 43 unblocked: empirical confidence that the dispatcher + verifier-gate handshake works → safe to invest in the workflow-side automation (peter-evans/create-pull-request@v8 + issues.labeled trigger + concurrency group)
</success_criteria>

<output>
Create `.planning/phases/42-fix-prompt-builder-wrong-citation-vertical-slice/42-03-SUMMARY.md` with:
- Captured prereq evidence (gh auth status excerpt, ANTHROPIC_API_KEY presence proof, clean-tree confirmation)
- Captured target confirmation (issue #3 state/labels/title/fingerprint OR fallback target identifier)
- Captured dry-run output (last 20 lines)
- Captured real-invocation output (last 10 lines) + exit code + the appended ledger entry JSON
- Captured branch URL, PR URL, verifier-gate workflow-run URL
- Final PR state (isDraft + labels + check conclusions) — the load-bearing outcome
- SDK cost figure (cost_usd from the ledger entry)
- Pitfall 4 caveat block: "dispatcher exit 0 ≠ gate pass; the gate's verdict is the load-bearing signal; this demo's gate verdict was <PASS|FAIL>"
- Concurrent-invocation acceptance note (CONTEXT Q3 / RESEARCH Open Q3)
- Phase 43 readiness statement: green or red, with the next-steps recommendation
</output>