---
phase: 46-gsd-fix-issue-local-ux-ledger-v2-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/auto-fix.mjs
  - tests/e2e/lib/llm-driver.js
  - tests/unit/auto-fix.test.js
  - package.json
autonomous: true
requirements:
  - AUTOFIX-06

must_haves:
  truths:
    - "Running `npm run fix-issue -- --issue <n>` invokes auto-fix.mjs with --transport subscription"
    - "Subscription transport routes to invokeClaudePWithLedger (NOT invokeAnthropicSdkWithLedger)"
    - "Subscription mode does NOT push to origin without an explicit --push flag"
    - "Explicit --push pushes the branch under both transports"
    - "Explicit --no-push wins over --push when both are set"
    - "sdk transport with neither flag pushes (Phase 42 default — unchanged)"
    - "Ledger entries written by invokeClaudePWithLedger carry transport: 'subscription' (self-describing for forensic greps)"
    - "Dispatcher rejects unrecognized --transport values with exit 2 and a list of valid transports"
    - "CI guard at llm-driver.js:387-393 is preserved (Pitfall 8 invariant — DO NOT TOUCH)"
  artifacts:
    - path: "scripts/auto-fix.mjs"
      provides: "Dispatcher with subscription transport routing + --push flag"
      contains: "VALID_TRANSPORTS"
    - path: "tests/e2e/lib/llm-driver.js"
      provides: "invokeClaudePWithLedger one-line transport tag patch"
      contains: "transport: 'subscription'"
    - path: "tests/unit/auto-fix.test.js"
      provides: "Vitest coverage of subscription dispatch + --push truth table"
      contains: "subscription"
    - path: "package.json"
      provides: "scripts.fix-issue entry"
      contains: "fix-issue"
  key_links:
    - from: "package.json scripts.fix-issue"
      to: "scripts/auto-fix.mjs --transport subscription"
      via: "npm run fix-issue"
      pattern: "node scripts/auto-fix\\.mjs --transport subscription"
    - from: "scripts/auto-fix.mjs Step 10 transport branch"
      to: "tests/e2e/lib/llm-driver.js invokeClaudePWithLedger"
      via: "import + call when transport==='subscription'"
      pattern: "invokeClaudePWithLedger\\("
    - from: "tests/e2e/lib/llm-driver.js Step 5 (appendLedgerEntry)"
      to: "tests/e2e/.llm-spend-ledger.json entries"
      via: "appendLedgerEntry call with transport field"
      pattern: "transport: ['\"]subscription['\"]"
---

<objective>
Patch the Phase 42 dispatcher to route `--transport subscription` through `invokeClaudePWithLedger`, add a `--push` flag with inverted default for subscription mode, register `npm run fix-issue` in `package.json`, and patch `invokeClaudePWithLedger` to self-tag ledger entries with `transport: 'subscription'`.

Purpose: AUTOFIX-06 — close Phase 42's deferred subscription-transport seam so developers can iterate against their Max-5 subscription without burning API spend. The committed-ledger forensic trail becomes self-describing (no inference-by-absence) once entries carry an explicit transport tag.

Output: Patched dispatcher (lines 402-408 rejection guard REPLACED with allow-list; Step 10 branched; Step 17 push gated by truth table), one-line driver patch, extended Vitest suite, and the `fix-issue` script entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-gsd-fix-issue-local-ux-ledger-v2-dashboard/46-CONTEXT.md
@.planning/phases/46-gsd-fix-issue-local-ux-ledger-v2-dashboard/46-RESEARCH.md

@scripts/auto-fix.mjs
@tests/e2e/lib/llm-driver.js
@tests/unit/auto-fix.test.js
@package.json

<interfaces>
<!-- Key contracts the executor needs. Pulled from verified-by-read sources. -->
<!-- DO NOT re-explore the codebase — these are load-bearing. -->

From tests/e2e/lib/llm-driver.js (invokeClaudePWithLedger):

```
export async function invokeClaudePWithLedger({
  systemPrompt,    // string — REQUIRED (NOT systemBlocks; no cache_control)
  userPrompt,      // string — REQUIRED
  timeoutMs,       // default LLM_TIMEOUT_MS (60_000)
  phase,           // string — flows to appendLedgerEntry .phase field
  source,          // string — free-form forensic tag
} = {});

// Returns (verified from JSDoc at llm-driver.js:370-376):
//   {ok:true,  llmText, modelId, costUsd, rawJson}
//   {ok:false, ciGate:true, message}
//   {ok:false, capBlocked:true, monthly, phaseCap}
//   {ok:false, errorReason, llmText:null, modelId, costUsd, rawJson}
//
// CI guard: refuses outside CI (OPPOSITE of SDK). Lines 387-393 are LOAD-BEARING
// — Pitfall 8 of PITFALLS.md. DO NOT MODIFY THE GUARD; only the appendLedgerEntry
// call at lines 419-427 receives the one-line transport tag patch.
```

From scripts/auto-fix.mjs (Phase 42 dispatcher — current Step 1 reject):

```
// Lines 402-408 — CURRENT REJECTION (TO BE REPLACED in this plan):
if (transport !== 'sdk') {
  process.stderr.write(
    `[auto-fix] transport '${transport}' not supported in Phase 42 ` +
      '(subscription is Phase 46); use --transport sdk\n',
  );
  return 2;
}
```

From scripts/auto-fix.mjs CLI shim (lines 767-779):

```
parsed = parseArgs({
  options: {
    issue: { type: 'string' },
    transport: { type: 'string', default: 'sdk' },
    'force-api': { type: 'boolean', default: false },
    'dry-run': { type: 'boolean', default: false },
    'no-push': { type: 'boolean', default: false },
    help: { type: 'boolean', default: false },
  },
  strict: true,
  allowPositionals: false,
});
```

Push truth table (locked in 46-RESEARCH.md Pattern 3):

| transport    | --push | --no-push | Push? |
|--------------|--------|-----------|-------|
| sdk          | —      | —         | YES   |
| sdk          | YES    | —         | YES   |
| sdk          | —      | YES       | NO    |
| sdk          | YES    | YES       | NO    |
| subscription | —      | —         | NO    |
| subscription | YES    | —         | YES   |
| subscription | —      | YES       | NO    |
| subscription | YES    | YES       | NO    |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Patch invokeClaudePWithLedger transport tag + dispatcher subscription routing</name>
  <files>
    tests/e2e/lib/llm-driver.js
    scripts/auto-fix.mjs
  </files>

  <read_first>
    - tests/e2e/lib/llm-driver.js lines 378-447 (full `invokeClaudePWithLedger`) — verify the appendLedgerEntry call site at lines 419-427 BEFORE patching. The CI guard at lines 387-393 MUST remain untouched.
    - scripts/auto-fix.mjs lines 80-95 (PHASE/TRANSPORT/MODEL constants).
    - scripts/auto-fix.mjs lines 380-410 (runDispatcher signature + Step 1 rejection guard) — these 7 lines (402-408) are REPLACED, not merely edited.
    - scripts/auto-fix.mjs lines 53-66 (existing imports from `tests/e2e/lib/llm-driver.js` — `invokeAnthropicSdkWithLedger` is imported; ADD `invokeClaudePWithLedger` to that import).
    - scripts/auto-fix.mjs lines 577-619 (Step 9-10 systemBlocks build + SDK call + sdkResult error mapper).
    - scripts/auto-fix.mjs lines 720-741 (Step 16 commit + Step 17 push branch).
    - 46-RESEARCH.md Pattern 1 (Step 1 patch), Pattern 2 (Step 10 dispatch), Pattern 3 (push truth table), Assumption A1 (recommend the one-line driver patch).
  </read_first>

  <behavior>
    - Test 1: `runDispatcher({issue: 7, transport: 'subscription', ...})` invokes the mocked `invokeClaudePWithLedger` (NOT `invokeAnthropicSdkWithLedger`) and passes `{systemPrompt, userPrompt, phase: '46-fix-issue', source: 'fix-issue-cli'}`.
    - Test 2: `runDispatcher({transport: 'sdk', ...})` invokes `invokeAnthropicSdkWithLedger` byte-identically to Phase 42 (regression guard — `systemBlocks`, `model`, `phase: '42-auto-fix'`, `issueId`, `forceApi` all preserved).
    - Test 3: `runDispatcher({transport: 'subscription', push: false, noPush: false, ...})` does NOT call `git push` (Step 17 short-circuits, exit 0).
    - Test 4: `runDispatcher({transport: 'subscription', push: true, ...})` DOES call `git push -u origin <branchName>`.
    - Test 5: `runDispatcher({transport: 'subscription', push: true, noPush: true, ...})` does NOT push (`--no-push` wins; matches truth-table row 8).
    - Test 6: `runDispatcher({transport: 'sdk', push: true, noPush: true, ...})` does NOT push (truth-table row 4).
    - Test 7: `runDispatcher({transport: 'sdk', push: false, noPush: false, ...})` DOES push (Phase 42 default — regression guard for truth-table row 1).
    - Test 8: `runDispatcher({transport: 'banana', ...})` returns exit 2 and stderr matches `/unrecognized .* expected one of: sdk, subscription/`.
    - Test 9: `invokeClaudePWithLedger({phase: '46-fix-issue', ...})` (mock `invokeClaudeP` to return a parseable JSON response) appends a ledger entry whose `.transport === 'subscription'`. Read ledger via `readLedger(LEDGER_PATH)` with the test's `E2E_LEDGER_PATH_OVERRIDE`; assert `entries[last].transport === 'subscription'`.
    - All tests follow the existing `tests/unit/auto-fix.test.js` mock pattern (vi.mock for `tests/e2e/lib/llm-driver.js` + `tests/e2e/lib/llm-ledger.js`, plus `vi.spyOn(child_process, 'execFileSync')`).
  </behavior>

  <action>
    Apply three patches in one task (atomic — all three are load-bearing for AUTOFIX-06):

    **A. tests/e2e/lib/llm-driver.js — one-line transport tag (per Assumption A1)**
    - Locate the `appendLedgerEntry` call inside `invokeClaudePWithLedger` (lines 419-427).
    - Add `transport: 'subscription',` to the entry object (place it adjacent to `phase` for symmetry with the SDK path).
    - DO NOT modify the CI guard at lines 387-393. DO NOT change the function signature. DO NOT touch `invokeAnthropicSdkWithLedger`. This is the smallest possible patch consistent with Assumption A1's recommendation (per locked open Q1).

    **B. scripts/auto-fix.mjs — dispatcher rewiring**

    1. Add `invokeClaudePWithLedger` to the existing import from `'../tests/e2e/lib/llm-driver.js'` (lines 53-58). Resulting import names: `invokeAnthropicSdkWithLedger, invokeClaudePWithLedger`.
    2. Add two module-level constants near line 80-82:
       ```
       const PHASE_46 = '46-fix-issue';
       const SOURCE_FIX_ISSUE = 'fix-issue-cli';
       ```
    3. Update the JSDoc on `runDispatcher` (lines 382-389) to add the new params: `transport` (now accepts 'sdk' OR 'subscription'); `push` (boolean, default false; inverted-default semantics resolved against `noPush` and `transport` per truth table).
    4. Add `push = false` to the destructured opts (line 390-396).
    5. REPLACE lines 402-408 with the allow-list guard from RESEARCH Pattern 1:
       ```
       const VALID_TRANSPORTS = new Set(['sdk', 'subscription']);
       if (!VALID_TRANSPORTS.has(transport)) {
         process.stderr.write(
           `[auto-fix] unrecognized --transport '${transport}'; ` +
             `expected one of: ${[...VALID_TRANSPORTS].join(', ')}\n`,
         );
         return 2;
       }
       ```
    6. At Step 10 (lines 587-594), replace the unconditional `invokeAnthropicSdkWithLedger` call with the branched dispatch from RESEARCH Pattern 2 — `if (transport === 'subscription') { sdkResult = await invokeClaudePWithLedger({systemPrompt, userPrompt, phase: PHASE_46, source: SOURCE_FIX_ISSUE}); } else { ...existing sdk call unchanged... }`. The existing error mapper at lines 596-619 already handles `ciGate`/`capBlocked`/`errorReason` — leave it untouched (both wrappers share that contract per RESEARCH Pattern 2's "Important divergence" note).
    7. At Step 17 (lines 729-741), replace `if (noPush) { ... } else { git push ... }` with the truth-table resolution from RESEARCH Pattern 3:
       ```
       const shouldPush = (() => {
         if (noPush) return false;
         if (push) return true;
         return transport === 'sdk';
       })();
       if (!shouldPush) {
         process.stdout.write(
           `[auto-fix] branch staged locally; push manually with: ` +
           `git push -u origin ${branchName} (or rerun with --push)\n`,
         );
       } else {
         execFileSync('git', ['push', '-u', 'origin', branchName], { stdio: ['ignore', 'inherit', 'inherit'] });
       }
       ```
       Wrap the `execFileSync` in a try/catch matching the existing error path (lines 735-740) — same `[auto-fix] git push failed: <msg>; exit 1` semantics.
    8. Update the CLI shim (lines 767-779) `parseArgs` options block to add `push: { type: 'boolean', default: false }`. Pass `push: parsed.values.push` to `runDispatcher` (line 815 area).
    9. Update the `--help` usage string (lines 786-794) to document `--push` (opt-in for subscription) and the inverted-default semantics in one sentence. Document that `--transport subscription` is now accepted.
    10. Update the file header comment (lines 24-26) to reflect `--transport sdk|subscription` and the `--push` flag. Update the Exit-code 0 line (line 29) to mention "subscription branch staged locally (--push not set)".

    **C. tests/unit/auto-fix.test.js — Vitest extensions**
    - Add 9 new test cases per the `<behavior>` block. Use the existing test file's mock harness; do NOT introduce new test runners or harness files.
    - For Test 9 specifically (driver transport-tag patch), exercise `invokeClaudePWithLedger` directly with a mocked `invokeClaudeP` (the inner subprocess wrapper) so the test does not require a live `claude` CLI. Use `E2E_LEDGER_PATH_OVERRIDE` to point at a per-test ledger temp file (the existing test file already establishes the override pattern — replicate it).
    - Group the new tests under a `describe('Phase 46 — subscription transport routing + --push', ...)` block at the bottom of the file so they don't interleave with Phase 42 / 45 blocks.

    Per D-(CONTEXT)locked choice: smallest one-line patch to `invokeClaudePWithLedger` (Assumption A1's recommendation in RESEARCH) — preserves CI guard verbatim.
  </action>

  <verify>
    <automated>cd /home/fatduck/patent-cite-tool &amp;&amp; npx vitest run tests/unit/auto-fix.test.js --reporter=verbose 2>&amp;1 | tail -80</automated>
  </verify>

  <done>
    - tests/e2e/lib/llm-driver.js:419-427 contains `transport: 'subscription'` in the appendLedgerEntry call (verified by grep).
    - llm-driver.js:387-393 CI guard is byte-identical to pre-Phase-46 state (verified by `git diff -- tests/e2e/lib/llm-driver.js | grep -E '^[+-]' | grep -v '^[+-]\s*transport' | wc -l` returning 0 minus diff headers).
    - scripts/auto-fix.mjs imports both `invokeAnthropicSdkWithLedger` and `invokeClaudePWithLedger`.
    - scripts/auto-fix.mjs contains `VALID_TRANSPORTS = new Set(['sdk', 'subscription'])`.
    - scripts/auto-fix.mjs Step 10 has the branched dispatch (grep `transport === 'subscription'` returns ≥1 hit inside `runDispatcher`).
    - scripts/auto-fix.mjs Step 17 has the `shouldPush` IIFE.
    - parseArgs options includes `push: { type: 'boolean', default: false }`.
    - All 9 new Vitest cases pass; existing Phase 42 / 45 cases still pass (regression guard).
    - Exit code from `npx vitest run tests/unit/auto-fix.test.js` is 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register npm run fix-issue scripts entry</name>
  <files>
    package.json
  </files>

  <read_first>
    - package.json lines 8-35 (current `scripts` block — verify alphabetical ordering convention BEFORE editing; `check-deps-and-pr` sits between `build:*` and `dev`).
    - 46-RESEARCH.md "Code Examples" → "`npm run fix-issue` scripts entry" (locked invocation string).
    - 46-CONTEXT.md Locked Decisions — "Scripts entry: `"fix-issue": "node scripts/auto-fix.mjs --transport subscription"`".
  </read_first>

  <action>
    Add a single scripts entry to `package.json`:
    ```
    "fix-issue": "node scripts/auto-fix.mjs --transport subscription",
    ```
    Place it in alphabetical order within the existing `scripts` block (between `e2e:weekly-digest` and `lint` — `fix` sorts after `e2e:*` and before `generate-icons`/`lint`).

    Do NOT add the `ledger-dashboard` entry — that belongs to Plan 46-02 (avoids file-edit contention; package.json is the only file touched by both plans, so 46-02 will Read-then-Edit after 46-01 lands in the same wave).

    NOTE on wave-1 file overlap: both plans modify `package.json`. Per the planner's parallel-execution rules, ANY shared file across same-wave plans must be reconciled. **Override resolution:** package.json edits in 46-01 and 46-02 touch DIFFERENT keys (`fix-issue` vs `ledger-dashboard`), both inside the `scripts` object — non-conflicting additive lines. Plan 46-02 explicitly notes this and writes its line at a different sort position. If executed in parallel by separate worktree agents, the merge is a clean three-way merge of two single-line additions. If executed sequentially in one worktree, the second executor must Re-Read `package.json` before its Edit (standard execute-plan rule).

    Do NOT modify `devDependencies`. No new npm deps (CONTEXT lock).
  </action>

  <verify>
    <automated>cd /home/fatduck/patent-cite-tool &amp;&amp; node -e "const p = require('./package.json'); if (p.scripts['fix-issue'] !== 'node scripts/auto-fix.mjs --transport subscription') { console.error('fix-issue script missing or wrong: ' + p.scripts['fix-issue']); process.exit(1); } console.log('OK'); "</automated>
  </verify>

  <done>
    - `package.json` `scripts.fix-issue` exactly equals `"node scripts/auto-fix.mjs --transport subscription"` (string match, no extra args).
    - No other `scripts` entries deleted or modified (`git diff -- package.json` shows only one added line).
    - `npm install` is NOT required (no devDependencies changed); `package-lock.json` is unmodified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer CLI → auto-fix dispatcher | Operator-supplied `--issue <n>` and `--push` flag values cross into the dispatcher; pre-Phase-46 hygiene (CWE-94: arg arrays only, no shell strings) is preserved. |
| Dispatcher → invokeClaudePWithLedger | Phase 39 subscription wrapper enforces its own CI guard (`llm-driver.js:387-393`); Phase 46 only routes, never re-implements. |
| invokeClaudePWithLedger → committed ledger | Ledger entries now self-tag transport; the appendLedgerEntry primitive is unchanged (Phase 39 atomic-write contract preserved). |
| Dispatcher Step 17 → origin remote | The `git push` invocation is gated by the `shouldPush` IIFE; subscription-default no-push prevents accidental remote writes during local iteration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01-01 | Tampering | Dispatcher Step 17 push branch | mitigate | `shouldPush` IIFE defaults to NO-PUSH for subscription transport; explicit `--push` opt-in required. Truth table verified by Vitest cases 3-7. |
| T-46-01-02 | Information Disclosure | invokeClaudePWithLedger CI guard | mitigate | Lines 387-393 left BYTE-IDENTICAL — Pitfall 8 invariant #1. Task-1 `<done>` block contains a static-diff assertion that no non-transport line of llm-driver.js changes outside the appendLedgerEntry call. |
| T-46-01-03 | Information Disclosure | Subscription auth bleeding to remote | accept | The CI guard in invokeClaudePWithLedger prevents CI-side invocation; pushing a locally-iterated branch is operator-intended when `--push` is set. No additional control needed. |
| T-46-01-04 | Tampering | Dispatcher arg parsing | mitigate | parseArgs `strict: true` (unchanged) rejects typo'd flags; the allow-list guard catches `--transport <unknown>` with exit 2. Vitest case 8 verifies. |
| T-46-01-05 | Repudiation | Ledger transport audit trail | mitigate | One-line patch to `invokeClaudePWithLedger` makes subscription entries self-describing — eliminates inference-by-absence. Vitest case 9 asserts the tag is present. |
| T-46-01-SC | Tampering | npm/pip/cargo installs | accept | NO new npm packages in this plan. RESEARCH.md "Package Legitimacy Audit" explicitly states "ZERO new npm packages" — no legitimacy gate required. |
</threat_model>

<verification>
- Vitest suite passes: `npx vitest run tests/unit/auto-fix.test.js`
- Static diff guard on llm-driver.js: only the `transport: 'subscription'` line changes (CI guard at lines 387-393 untouched).
- Grep guard on dispatcher: `grep -c "VALID_TRANSPORTS" scripts/auto-fix.mjs` returns ≥1; `grep -c "shouldPush" scripts/auto-fix.mjs` returns ≥1.
- `node -e` smoke-check on package.json (Task 2 verify command).
- `npm run lint` passes (ESLint config — including the Phase 42 ESLint catch-all-ignores augmentation — remains satisfied).
- Manual smoke (not gating; will be exercised live in Phase 47 HUMAN-UAT a): `npm run fix-issue -- --help` prints the updated usage string.
</verification>

<success_criteria>
- All `must_haves.truths` are observably true via the Vitest suite.
- AUTOFIX-06 is closed:
  - `npm run fix-issue <n>` (via `--`) invokes `auto-fix.mjs --transport subscription` — verified by Task 2 node smoke + the dispatch-mock Vitest cases.
  - Subscription mode refuses to push without `--push` — verified by Vitest case 3.
  - `package.json scripts.fix-issue` is declared — verified by Task 2 node smoke.
- The Pitfall 8 invariant (`invokeClaudePWithLedger` CI guard) is preserved.
- Phase 42 regression guard: existing dispatcher behavior for `--transport sdk` is byte-identical (Vitest case 7 + existing Phase 42 / 45 cases still green).
</success_criteria>

<output>
Create `.planning/phases/46-gsd-fix-issue-local-ux-ledger-v2-dashboard/46-01-SUMMARY.md` when done. Include:
- Patch diff summary (driver one-line, dispatcher 7-region patch, package.json one-line).
- Vitest case count before/after (existing N → N+9).
- The Pitfall 8 invariant audit (one-sentence "verified byte-identical at lines 387-393").
- Any deviation from the locked truth table (none expected).
</output>
