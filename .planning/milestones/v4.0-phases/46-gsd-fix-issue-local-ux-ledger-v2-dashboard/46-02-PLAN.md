---
phase: 46-gsd-fix-issue-local-ux-ledger-v2-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-ledger-dashboard.mjs
  - docs/v40-ledger-dashboard.md
  - docs/v40-ledger-privacy-audit.md
  - .github/workflows/v40-cost-ledger-snapshot.yml
  - tests/unit/build-ledger-dashboard.test.js
  - tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js
  - package.json
autonomous: true
requirements:
  - AUTOFIX-06

must_haves:
  truths:
    - "Running `npm run ledger-dashboard` regenerates docs/v40-ledger-dashboard.md from the committed ledger"
    - "The dashboard markdown is byte-stable across two calls with the same ledger input (deterministic)"
    - "The dashboard `Generated:` timestamp is derived from the ledger's most recent `iso` (NOT `new Date()`)"
    - "The dashboard surfaces per-transport, per-day, per-phase spend against the $80 warn / $100 hard caps"
    - "The Phase 40 daily snapshot workflow regenerates the dashboard BEFORE the [skip ci] commit step, committing both files atomically"
    - "The privacy audit document records the 6 regex patterns and the zero-hit result against the current committed ledger"
    - "scripts/build-ledger-dashboard.mjs does NOT import appendLedgerEntry (read-only against the ledger)"
  artifacts:
    - path: "scripts/build-ledger-dashboard.mjs"
      provides: "Deterministic markdown dashboard generator"
      contains: "buildDashboardMarkdown"
    - path: "docs/v40-ledger-dashboard.md"
      provides: "Committed bootstrap dashboard for current ledger state"
      contains: "By Transport"
    - path: "docs/v40-ledger-privacy-audit.md"
      provides: "Privacy audit verdict with 6 regex patterns + zero-hit result"
      contains: "PASS"
    - path: ".github/workflows/v40-cost-ledger-snapshot.yml"
      provides: "Phase 40 workflow extended with dashboard regen step"
      contains: "build-ledger-dashboard.mjs"
    - path: "tests/unit/build-ledger-dashboard.test.js"
      provides: "Vitest coverage of markdown shape + determinism + privacy regex sweep"
      contains: "deterministic"
    - path: "tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js"
      provides: "Extended YAML contract test asserting the regen step exists before [skip ci]"
      contains: "build-ledger-dashboard"
    - path: "package.json"
      provides: "scripts.ledger-dashboard entry"
      contains: "ledger-dashboard"
  key_links:
    - from: "scripts/build-ledger-dashboard.mjs"
      to: "tests/e2e/lib/llm-ledger.js"
      via: "named imports (readLedger, combinedMonthlyTotalByTransport, phaseTotal, dayTotal, currentMonth, HARD_CAP_USD, WARN_THRESHOLD_USD, LEDGER_PATH)"
      pattern: "from ['\"]\\.\\./tests/e2e/lib/llm-ledger\\.js['\"]"
    - from: ".github/workflows/v40-cost-ledger-snapshot.yml regen step"
      to: "scripts/build-ledger-dashboard.mjs"
      via: "shell `node scripts/build-ledger-dashboard.mjs`"
      pattern: "node scripts/build-ledger-dashboard\\.mjs"
    - from: "Workflow [skip ci] commit step"
      to: "docs/v40-ledger-dashboard.md + tests/e2e/.llm-spend-ledger.json"
      via: "git add A B then git commit"
      pattern: "git add tests/e2e/\\.llm-spend-ledger\\.json docs/v40-ledger-dashboard\\.md"
---

<objective>
Ship the Ledger v2 dashboard generator (`scripts/build-ledger-dashboard.mjs`), commit a bootstrap dashboard at `docs/v40-ledger-dashboard.md` reflecting current ledger state, extend the Phase 40 daily-snapshot workflow to regenerate the dashboard atomically with the snapshot commit, document the privacy-audit verdict in `docs/v40-ledger-privacy-audit.md`, and register `npm run ledger-dashboard`.

Purpose: AUTOFIX-06 (dashboard half) — surface combined-transport spend in a readable GitHub-web-UI format and prove forensically (regex-sweep audit) that the committed ledger carries no secrets/PII. Closes Pitfall 2 (cost runaway) via visibility; closes the committed-ledger privacy concern raised in STATE.md.

Output: One new script, one bootstrap markdown, one audit markdown, one YAML extension, two test additions, one package.json scripts line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-gsd-fix-issue-local-ux-ledger-v2-dashboard/46-CONTEXT.md
@.planning/phases/46-gsd-fix-issue-local-ux-ledger-v2-dashboard/46-RESEARCH.md

@tests/e2e/lib/llm-ledger.js
@tests/e2e/.llm-spend-ledger.json
@.github/workflows/v40-cost-ledger-snapshot.yml
@tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js
@package.json

<interfaces>
<!-- Ledger primitives Phase 46 consumes (verified-by-read). DO NOT re-implement. -->

From tests/e2e/lib/llm-ledger.js (Phase 39 exports — verified at the line ranges noted in RESEARCH Sources):

```
export function readLedger(ledgerPath: string): Ledger;             // honors E2E_LEDGER_PATH_OVERRIDE
export function currentMonth(): string;                              // 'YYYY-MM' UTC
export function currentIsoDay(): string;                             // 'YYYY-MM-DD' UTC
export function monthlyTotal(ledger, month): number;
export function dayTotal(ledger, isoDay): number;
export function phaseTotal(ledger, phase): number;                   // cross-month
export function combinedMonthlyTotalByTransport(ledger, month):
  { sdk: number, subscription: number };                             // missing transport => 'subscription'
export const HARD_CAP_USD: number;            // $100
export const WARN_THRESHOLD_USD: number;      // $80
export const PHASE_HARD_CAP_USD: number;      // $10
export const PHASE_WARN_THRESHOLD_USD: number;// $8
export const LEDGER_PATH: string;             // default; can be overridden by E2E_LEDGER_PATH_OVERRIDE
export function appendLedgerEntry(...);       // ← FORBIDDEN to import in this plan
```

Ledger entry shape (relevant fields):

```
{
  iso: 'YYYY-MM-DDTHH:MM:SS.sssZ',
  model: string,
  cost_usd: number,
  tokens_in?: number,
  tokens_out?: number,
  phase: string,
  transport?: 'sdk' | 'subscription',   // missing => treated as 'subscription' by back-compat default
  source?: string,
  ...other forensic fields
}
```

From .github/workflows/v40-cost-ledger-snapshot.yml (Phase 40, verified):

```yaml
# Three steps in the snapshot job:
- uses: actions/checkout@v4
- uses: actions/setup-node@v4 with node-version: 22
- name: Capture snapshot summary for commit message   # sets $SNAPSHOT_DATE / $INVOCATIONS / $SPEND_USD
- name: Commit daily ledger snapshot                  # git add ledger; [skip ci] commit; push
```

Phase 46 inserts a new step BETWEEN "Capture snapshot summary" and "Commit daily ledger snapshot" — the regen step writes docs/v40-ledger-dashboard.md from the just-read ledger. The commit step's `git add` is extended to add both files.

Dashboard markdown layout (locked in 46-RESEARCH.md Pattern 4):

```
# v4.0 LLM Spend Ledger Dashboard

**Generated:** <ledger.last_invocation_iso>
**Source:** tests/e2e/.llm-spend-ledger.json
**Month:** YYYY-MM  |  **Total:** $X.XX  |  **Warn @ $80.00**  |  **Hard cap @ $100.00**  |  Status: <ok|warn|block>

## By Transport (current month)
| Transport | Spend (USD) | Share |
| ...       | $X.XX       | NN.N% |
| **Total** | **$X.XX**   | **100%** |

## By Day (current month, UTC)
| Day | Invocations | Spend (USD) |
| YYYY-MM-DD | N | $X.XX |

## By Phase (cross-month)
| Phase | Spend (USD) | Per-Phase Cap | Status |
| <phase> | $X.XX | $10.00 | <ok|warn (≥$8)|block> |
```

Privacy audit regex bank (locked in 46-RESEARCH.md Code Examples — these EXACT 6 patterns):

```
anthropicApiKey:   /sk-ant-[A-Za-z0-9-]{20,}/g
genericApiKey:     /sk-[A-Za-z0-9]{20,}/g
emailRfc5322:      /[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}/g
localUserPath:     /\/home\/[^/\s"]+\//g
windowsUserPath:   /[A-Z]:\\Users\\[^\\\s"]+\\/g
ipv4Octet:         /\b(?:\d{1,3}\.){3}\d{1,3}\b/g
```

Expected verdict against current committed ledger: PASS (zero hits across all 6).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write scripts/build-ledger-dashboard.mjs + Vitest coverage</name>
  <files>
    scripts/build-ledger-dashboard.mjs
    tests/unit/build-ledger-dashboard.test.js
    package.json
  </files>

  <read_first>
    - tests/e2e/lib/llm-ledger.js (Phase 39) — full file. Pay attention to:
      • `readLedger` signature and `E2E_LEDGER_PATH_OVERRIDE` handling.
      • `combinedMonthlyTotalByTransport` — the back-compat default treats missing `transport` as `'subscription'`.
      • Atomic write pattern (`appendLedgerEntry`'s temp+rename at the file's bottom) — `build-ledger-dashboard.mjs` MUST mirror this pattern when writing the markdown.
      • Constants: `HARD_CAP_USD`, `WARN_THRESHOLD_USD`, `PHASE_HARD_CAP_USD`, `PHASE_WARN_THRESHOLD_USD`, `LEDGER_PATH`.
    - tests/e2e/.llm-spend-ledger.json — current state (1 entry, May 2026, sdk transport, phase '39-bootstrap'). The bootstrap dashboard generated in Task 2 reflects this.
    - 46-RESEARCH.md Pattern 4 (markdown layout — 20-line concrete example) and Pattern 5 (script shape + determinism requirements 1-6).
    - 46-RESEARCH.md "Don't Hand-Roll" table — every ledger math primitive is already exported; the dashboard script is pure composition + emission.
    - 46-RESEARCH.md Pitfall 2 (timestamp drift) — `Generated:` derives from ledger's most recent `iso`, NOT `new Date()`.
    - tests/unit/auto-fix.test.js (existing patterns — for mock harness shape) — use the same Vitest conventions.
    - 46-RESEARCH.md Validation Architecture → Phase Requirements → Test Map (cases for `deterministic output`, `table shape`, `Generated derived from ledger`, `privacy regex sweep`).
  </read_first>

  <behavior>
    - Test 1 (deterministic output): `buildDashboardMarkdown(fixtureLedger)` returns the SAME string on two calls. Mock `currentMonth()` and `currentIsoDay()` if necessary so the test is timezone-stable.
    - Test 2 (table shape — by transport): Given a fixture ledger with 3 entries (`{transport:'sdk', cost:1.00}`, `{transport:'subscription', cost:0.50}`, `{transport: undefined, cost:0.25}` — back-compat → subscription), the "By Transport" table shows sdk=$1.00, subscription=$0.75, Total=$1.75; shares 57.1% / 42.9% / 100%.
    - Test 3 (table shape — by day): Given fixture entries on `2026-05-29` and `2026-05-30`, day rows are sorted ASCENDING by ISO day; invocation counts and spends match.
    - Test 4 (table shape — by phase): Phase rows sorted by spend DESC; ties broken by phase name ASC. Each row shows the per-phase cap status (`ok` / `warn (≥$8)` / `block` per `PHASE_WARN_THRESHOLD_USD` / `PHASE_HARD_CAP_USD`).
    - Test 5 (Generated derived from ledger): The `Generated:` line uses the MAX `iso` from current-month entries; if the current month is empty, falls back to the global max iso; if the ledger is empty, the line reads `**Generated:** (no ledger entries)`. NEVER uses `new Date()` — assert by mocking the global Date constructor in the test (or by comparing against a frozen fixture).
    - Test 6 (currency formatting): All USD values formatted as `$N.NN` (exactly 2 decimals via `.toFixed(2)`); all percentages formatted as `N.N%` (one decimal, round half up).
    - Test 7 (trailing newline): Output ends with exactly one `\n` (POSIX convention).
    - Test 8 (cap status header): Month total < $80 → status `ok`; ≥$80 and <$100 → `warn`; ≥$100 → `block`. Three sub-cases.
    - Test 9 (privacy regex sweep — built-in to the test file, even though the script itself does NOT run the sweep): Read `tests/e2e/.llm-spend-ledger.json`, apply all 6 regex patterns from the locked bank, assert ZERO matches. (This test pins the audit's PASS verdict and would fail loudly if a future commit accidentally introduces sensitive content. The audit DOC is committed in Task 3; this test is the automated guard.)
    - Test 10 (forbidden import — appendLedgerEntry NEVER imported): Static-grep `scripts/build-ledger-dashboard.mjs` source for `appendLedgerEntry` — must return zero matches. (Read-only invariant from RESEARCH Anti-Patterns.)
    - Test 11 (atomic write): When `writeAtomic(path, content)` is called, the final file at `path` contains `content`; if the test simulates a failure between temp-write and rename, no partial file lands at `path`. (Mirrors Phase 39's `appendLedgerEntry` invariant.)
  </behavior>

  <action>
    **A. Create `scripts/build-ledger-dashboard.mjs`** per RESEARCH Pattern 5:

    1. Header comment: phase tag (`Phase 46 Plan 02 (AUTOFIX-06)`), purpose, determinism requirements (the 6 from RESEARCH Pattern 5), and the load-bearing note that `appendLedgerEntry` MUST NOT be imported.
    2. Imports (named — exact list per the `<interfaces>` block): `readLedger`, `currentMonth`, `currentIsoDay`, `monthlyTotal`, `dayTotal`, `phaseTotal`, `combinedMonthlyTotalByTransport`, `HARD_CAP_USD`, `WARN_THRESHOLD_USD`, `PHASE_HARD_CAP_USD`, `PHASE_WARN_THRESHOLD_USD`, `LEDGER_PATH`. From `'../tests/e2e/lib/llm-ledger.js'`. Plus `parseArgs` from `node:util`, `fs` and `path` from node builtins.
    3. Export a pure function `buildDashboardMarkdown(ledger, { month = currentMonth() } = {})` that returns the complete markdown string. Structure:
       - Header line `# v4.0 LLM Spend Ledger Dashboard`.
       - Generated line — derive ISO from ledger entries' MAX `iso`; fallback ladder per Test 5.
       - Source line — `tests/e2e/.llm-spend-ledger.json`.
       - Month header line with total, caps, and status (`ok|warn|block` per Test 8 thresholds).
       - Empty blank line.
       - `## By Transport (current month)` — table with sdk / subscription / **Total** rows. Use `combinedMonthlyTotalByTransport` (back-compat default treats missing transport as subscription).
       - `## By Day (current month, UTC)` — iterate `ledger.months[month]?.iterations || []`, group by `iso.slice(0, 10)`, sort ASCENDING. Use `dayTotal(ledger, isoDay)` per day.
       - `## By Phase (cross-month)` — collect unique phase values from ALL months' iterations (Set-dedup), compute `phaseTotal(ledger, phase)` for each, sort DESC by spend then ASC by phase name. Status column per Test 4.
       - Trailing newline.
    4. Helper `formatUsd(n)` → `'$' + n.toFixed(2)`.
    5. Helper `formatPct(n)` → `n.toFixed(1) + '%'`.
    6. Helper `writeAtomic(outPath, content)` — temp+rename pattern from `llm-ledger.js:723-737`. Use `fs.writeFileSync(temp, content)` then `fs.renameSync(temp, outPath)` with EXDEV fallback (try/catch: on EXDEV, `fs.copyFileSync` + `fs.unlinkSync`).
    7. CLI shim at the bottom: `if (import.meta.url === \`file://${process.argv[1]}\`)` block. parseArgs options: `output: { type: 'string', default: 'docs/v40-ledger-dashboard.md' }`, `help: { type: 'boolean', default: false }`. On `--help`, print usage and exit 0. Otherwise: `readLedger(LEDGER_PATH)` (honors `E2E_LEDGER_PATH_OVERRIDE`), build markdown, write atomically to the resolved output path. Stdout: `[build-ledger-dashboard] wrote <path>`.
    8. ZERO `appendLedgerEntry` import. ZERO `new Date()` usage anywhere (except inside string parsers/comparators that operate on existing ISO strings — i.e., `Date.parse(iso)` is allowed for sort-key comparisons).

    **B. Add `scripts.ledger-dashboard` to `package.json`:**
    ```
    "ledger-dashboard": "node scripts/build-ledger-dashboard.mjs"
    ```
    Place it in alphabetical order (between `lint` and `test`). Note Plan 46-01 also edits `package.json` (`fix-issue` entry) — different key at a different sort position, so additive edits are conflict-free in either parallel or sequential execution. If parallel agents collide, three-way merge resolves cleanly (each side adds one line in a different region of the `scripts` object).

    **C. Create `tests/unit/build-ledger-dashboard.test.js`:**
    - Import `buildDashboardMarkdown` from the new script.
    - 11 test cases per the `<behavior>` block.
    - Fixture ledgers built inline as JS objects (no separate fixture files); each test owns its own minimal fixture.
    - Test 10 (forbidden import) reads the source file as text and asserts `!source.includes('appendLedgerEntry')`.
    - Use Vitest's `describe('build-ledger-dashboard', ...)` block; nest sub-describes for `markdown shape`, `determinism`, `privacy regex sweep`.
  </action>

  <verify>
    <automated>cd /home/fatduck/patent-cite-tool &amp;&amp; npx vitest run tests/unit/build-ledger-dashboard.test.js --reporter=verbose 2>&amp;1 | tail -60</automated>
  </verify>

  <acceptance_criteria>
    - `scripts/build-ledger-dashboard.mjs` exists and exports `buildDashboardMarkdown`.
    - `node scripts/build-ledger-dashboard.mjs --help` exits 0 with a usage string.
    - All 11 Vitest cases pass.
    - `grep -c "appendLedgerEntry" scripts/build-ledger-dashboard.mjs` returns 0 (forbidden-import invariant).
    - `grep -c "new Date(" scripts/build-ledger-dashboard.mjs` returns 0 (Pitfall 2 — Generated derived from ledger only).
    - `package.json scripts['ledger-dashboard']` equals `'node scripts/build-ledger-dashboard.mjs'`.
  </acceptance_criteria>

  <done>
    Same as acceptance_criteria. Plus: `npm run lint` still passes (the new script lives under `scripts/` — the existing `npm run lint` invocation explicitly lists individual `scripts/*.mjs` files; verify whether `build-ledger-dashboard.mjs` needs to be appended to that lint list. If it does, append it. If the lint config picks it up via glob, no change needed.)
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate bootstrap docs/v40-ledger-dashboard.md + privacy audit doc</name>
  <files>
    docs/v40-ledger-dashboard.md
    docs/v40-ledger-privacy-audit.md
  </files>

  <read_first>
    - tests/e2e/.llm-spend-ledger.json — current committed state. The bootstrap dashboard reflects this 1-entry ledger.
    - 46-RESEARCH.md Pattern 4 (markdown layout) — the bootstrap output must match this structure exactly.
    - 46-RESEARCH.md "Code Examples → Privacy audit regex patterns" + the "Example findings against the CURRENT committed ledger" table (zero hits across all 6 patterns).
    - 46-CONTEXT.md decisions on the audit scope (API keys, model IDs, emails, paths, IPs).
    - docs/v40-repo-config.md (Phase 39 precedent for committed audit docs) — use the same heading depth + voice.
  </read_first>

  <action>
    **A. Generate `docs/v40-ledger-dashboard.md`:**
    Run `node scripts/build-ledger-dashboard.mjs --output docs/v40-ledger-dashboard.md` AFTER Task 1 lands the script. Then commit the generated markdown verbatim. Do not hand-edit the output — that defeats the determinism invariant.

    If the generator output for the current 1-entry ledger comes out empty/sparse (e.g., zero by-transport breakdown because only the bootstrap entry exists), the dashboard will still render: the bootstrap entry is `transport: 'sdk', phase: '39-bootstrap', cost: $0.00` per RESEARCH Runtime State Inventory.

    **B. Write `docs/v40-ledger-privacy-audit.md`:**
    Mirror `docs/v40-repo-config.md`'s voice. Structure:
    1. Title + date.
    2. **Scope:** what is audited (committed `tests/e2e/.llm-spend-ledger.json`) and why (the file is committed-and-versioned per LEDGER-04; future contributors must be able to verify it carries no secrets/PII).
    3. **Method:** the 6 regex patterns from the locked bank (table — copy verbatim from `<interfaces>` block above with `name | regex | targets` columns).
    4. **Verdict (current ledger snapshot):** PASS — table with all 6 patterns and the per-pattern hit count (zero across all 6). Cite the date the audit was last run (commit date of this PR).
    5. **Redaction policy:** PER CONTEXT — no redaction required while the audit verdict is PASS. If a future regex hit occurs:
       - Anthropic API keys (`sk-ant-*`): MANDATORY redaction at `appendLedgerEntry`. Open a security advisory.
       - Generic `sk-*` keys: MANDATORY redaction.
       - Email addresses: MANDATORY redaction.
       - Local user paths (`/home/<user>/...`, `C:\Users\<user>\...`): MANDATORY redaction.
       - IPv4 octets: case-by-case — public DNS resolver IPs (`8.8.8.8`, `1.1.1.1`) are not sensitive; private RFC1918 ranges or operator-machine IPs warrant redaction.
       - Model IDs: NOT considered sensitive (Anthropic publishes them publicly) — no redaction.
    6. **Continuous enforcement:** the privacy regex sweep is wired into `tests/unit/build-ledger-dashboard.test.js` Test 9; any future commit that introduces a regex hit fails CI. The dashboard generator never writes the ledger (Test 10 enforces this).
    7. **Reaudit triggers:** any change to `tests/e2e/lib/llm-driver.js` that touches `appendLedgerEntry` calls; any new SDK error field added to the ledger entry shape; any expansion of the `source` or `phase` taxonomy.

    Do NOT include redaction code in this plan — it ships only if the verdict flips to a hit (deferred per CONTEXT recommendation).
  </action>

  <verify>
    <automated>cd /home/fatduck/patent-cite-tool &amp;&amp; node scripts/build-ledger-dashboard.mjs --output /tmp/dash-verify.md &amp;&amp; diff /tmp/dash-verify.md docs/v40-ledger-dashboard.md &amp;&amp; test -s docs/v40-ledger-privacy-audit.md &amp;&amp; grep -q "PASS" docs/v40-ledger-privacy-audit.md &amp;&amp; echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `docs/v40-ledger-dashboard.md` exists; re-running the generator produces a byte-identical file (no diff against the committed copy).
    - `docs/v40-ledger-privacy-audit.md` exists; contains the literal token "PASS" in the verdict section.
    - The audit doc lists ALL 6 regex patterns from the locked bank.
    - The audit doc's redaction policy explicitly states "no redaction required" for the current ledger state.
  </acceptance_criteria>

  <done>
    Same as acceptance_criteria. Plus: the committed bootstrap dashboard has the structure of RESEARCH Pattern 4 (Generated/Source/Month header + three section headings).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend Phase 40 workflow + YAML contract test</name>
  <files>
    .github/workflows/v40-cost-ledger-snapshot.yml
    tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js
  </files>

  <read_first>
    - .github/workflows/v40-cost-ledger-snapshot.yml — entire file (only 83 lines). Identify the insertion point: AFTER the "Capture snapshot summary for commit message" step (ends ~line 68), BEFORE the "Commit daily ledger snapshot" step (starts ~line 70).
    - tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js — existing Phase 40 YAML contract tests (S1-S12 per the ROADMAP entry); understand the existing assertion harness shape before extending.
    - 46-RESEARCH.md Code Examples → "Phase 40 workflow extension — exact diff" — apply the locked diff verbatim modulo whitespace.
    - 46-RESEARCH.md Open Question 2 — locked: leave the `[skip ci]` commit message UNCHANGED. Only the `git add` line gains `docs/v40-ledger-dashboard.md`.
  </read_first>

  <behavior>
    - Test S13 (or next sequential `S<N>` per the existing file's numbering): The workflow contains a step named `"Regenerate ledger dashboard"` (or a name string-matching `/Regenerate.*dashboard/i`) that runs `node scripts/build-ledger-dashboard.mjs --output docs/v40-ledger-dashboard.md`.
    - Test S14: The regen step appears BEFORE the "Commit daily ledger snapshot" step (by index in `steps[]`).
    - Test S15: The "Commit daily ledger snapshot" step's `git add` line includes BOTH `tests/e2e/.llm-spend-ledger.json` AND `docs/v40-ledger-dashboard.md`.
    - Test S16: The `[skip ci]` commit-message format is UNCHANGED (`/\[skip ci\] ledger snapshot/` still present); LOAD-BEARING per RESEARCH Open Q2.
    - Test S17: The workflow's `permissions` block still contains ONLY `contents: write` (no expansion to `issues:` or `pull-requests:` — least-privilege per Phase 40's T-40-01-05 threat). Regression guard.
    - All existing S1-S12 tests still pass (cron schedule, concurrency group, idempotent guard, no E2E_LEDGER_PATH_OVERRIDE injection, etc.).
  </behavior>

  <action>
    **A. Patch `.github/workflows/v40-cost-ledger-snapshot.yml`** per RESEARCH "exact diff":

    1. Insert a new step BETWEEN "Capture snapshot summary for commit message" (ends ~line 68) and "Commit daily ledger snapshot" (starts ~line 70):
       ```yaml
       - name: Regenerate ledger dashboard
         # Phase 46 Plan 02 — regenerate docs/v40-ledger-dashboard.md from
         # the just-snapshotted ledger so both files commit atomically in
         # the next step. Read-only against the ledger; deterministic
         # output for an unchanged ledger (the Generated: field is derived
         # from the ledger's max iso, not new Date() — RESEARCH Pitfall 2).
         run: |
           node scripts/build-ledger-dashboard.mjs --output docs/v40-ledger-dashboard.md
       ```
    2. In the "Commit daily ledger snapshot" step's shell block, change the `git add` line:
       - FROM: `git add tests/e2e/.llm-spend-ledger.json`
       - TO:   `git add tests/e2e/.llm-spend-ledger.json docs/v40-ledger-dashboard.md`
    3. Do NOT modify the commit message (locked per RESEARCH Open Q2).
    4. Do NOT modify the cron schedule, concurrency group, permissions, or any other block.
    5. Do NOT introduce `E2E_LEDGER_PATH_OVERRIDE` anywhere in the workflow (Phase 40 invariant — the snapshot must operate against the canonical `LEDGER_PATH`).

    **B. Extend `tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js`** with 5 new assertions (S13-S17 per the `<behavior>` block):
    - Add the new tests inside the existing `describe` block (or a new sibling `describe('Phase 46 dashboard regen extension', ...)` block — match the file's existing conventions).
    - Use the same YAML-parser harness the file already establishes (do NOT introduce a new YAML lib; reuse whatever Phase 40 picked).
    - S13/S14/S15 search the `steps[]` array for the regen step and verify ordering + `git add` content.
    - S16 asserts the commit message is unchanged.
    - S17 asserts `permissions` is still `{contents: 'write'}` only.
  </action>

  <verify>
    <automated>cd /home/fatduck/patent-cite-tool &amp;&amp; npx vitest run tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js --reporter=verbose 2>&amp;1 | tail -50</automated>
  </verify>

  <acceptance_criteria>
    - Workflow YAML parses cleanly (`node -e "require('yaml').parse(require('fs').readFileSync('.github/workflows/v40-cost-ledger-snapshot.yml','utf8'))"` exits 0 — or whichever parser the existing tests use).
    - The "Regenerate ledger dashboard" step is present and ordered BEFORE the commit step.
    - `git add` includes both files.
    - All existing S1-S12 tests AND all new S13-S17 tests pass.
    - Permissions block unchanged.
  </acceptance_criteria>

  <done>
    Same as acceptance_criteria. The full YAML contract test passes (`vitest run` exit 0 against this single test file).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow runtime → committed dashboard | The GitHub Actions runner regenerates `docs/v40-ledger-dashboard.md` and commits it under the GITHUB_TOKEN — least-privilege `contents: write` only. |
| Dashboard generator → committed ledger | Read-only — `build-ledger-dashboard.mjs` MUST NOT import `appendLedgerEntry`; static-grep test enforces. |
| Committed ledger → git history | The privacy audit doc verifies (and the Vitest privacy-regex sweep continuously re-verifies) that no secrets/PII land in the committed file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-02-01 | Tampering | Dashboard generator writing the ledger | mitigate | Task 1 Test 10 static-greps the source for `appendLedgerEntry` — must return zero matches. Forbidden import enforced by test, not just convention. |
| T-46-02-02 | Tampering | Workflow `git add` accidentally staging extra files | mitigate | Task 3 Test S15 asserts the exact two-file `git add` line; any drift (e.g., `git add docs/*`) fails the YAML contract test. |
| T-46-02-03 | Repudiation / Tampering | Dashboard markdown drifting from ledger truth | mitigate | Determinism (Pitfall 2): `Generated:` derived from ledger's max iso; Task 1 Test 1 asserts byte-stable output for unchanged input. Daily `[skip ci]` commits will be empty (and thus skipped via `git diff --cached --quiet`) when the ledger is unchanged — the workflow's idempotent guard already exists from Phase 40. |
| T-46-02-04 | Information Disclosure | Committed ledger leaks API key / PII | mitigate | Privacy audit doc records PASS verdict; Task 1 Test 9 (privacy regex sweep against the committed ledger) is a continuous CI guard — any future leak fails the unit suite. |
| T-46-02-05 | Elevation of Privilege | Workflow permissions creep | mitigate | Task 3 Test S17 pins `permissions: {contents: write}` only; expansion to `issues:` or `pull-requests:` is rejected. Mirrors Phase 40 T-40-01-05. |
| T-46-02-06 | Tampering | New script imports an unintended ledger writer | mitigate | The named-imports list in Task 1 (interfaces block) is exhaustive; ESLint will flag unused imports, and Test 10 grep enforces absence of `appendLedgerEntry`. |
| T-46-02-SC | Tampering | npm/pip/cargo installs | accept | NO new npm packages in this plan. RESEARCH Package Legitimacy Audit explicit. |
</threat_model>

<verification>
- `npx vitest run tests/unit/build-ledger-dashboard.test.js` — all 11 cases pass.
- `npx vitest run tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js` — S1-S12 + S13-S17 all pass.
- `node scripts/build-ledger-dashboard.mjs --output /tmp/x.md && diff /tmp/x.md docs/v40-ledger-dashboard.md` — byte-identical (no drift).
- `grep -c "PASS" docs/v40-ledger-privacy-audit.md` — ≥1.
- `grep -c "appendLedgerEntry" scripts/build-ledger-dashboard.mjs` — 0 (read-only invariant).
- `grep -c "new Date(" scripts/build-ledger-dashboard.mjs` — 0 (Pitfall 2 invariant).
- `node -e "const p = require('./package.json'); if (p.scripts['ledger-dashboard'] !== 'node scripts/build-ledger-dashboard.mjs') { process.exit(1); }"` — exit 0.
- Workflow YAML still parses; commit message unchanged.
</verification>

<success_criteria>
- All `must_haves.truths` are observable:
  - Dashboard generated by `npm run ledger-dashboard`.
  - Output byte-stable for unchanged ledger.
  - `Generated:` derived from ledger (NOT `new Date()`).
  - Three tables present (transport / day / phase) with correct sums + cap-status indicators.
  - Phase 40 workflow runs regen BEFORE [skip ci] commit; both files committed atomically.
  - Privacy audit doc commits the PASS verdict with 6-pattern table.
  - Static-grep tests pin the read-only invariant and the no-`new Date()` invariant.
- AUTOFIX-06 dashboard half closed (the wrapper half lives in Plan 46-01).
- Pitfall 2 (cost runaway) defense visible: monthly total + cap status surfaced in the dashboard's header line.
</success_criteria>

<output>
Create `.planning/phases/46-gsd-fix-issue-local-ux-ledger-v2-dashboard/46-02-SUMMARY.md` when done. Include:
- New script LOC; new dashboard LOC; audit doc structure.
- Vitest case additions (11 + 5).
- Workflow patch diff (one new step + one extended `git add`).
- Confirmation that ALL 6 privacy regex patterns scored zero against the committed ledger (cite the SHA or commit date of the audited ledger).
- Confirmation that the `appendLedgerEntry` forbidden-import test passes.
</output>
