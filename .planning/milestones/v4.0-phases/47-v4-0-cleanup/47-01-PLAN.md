---
phase: 47
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/v4-touchpoints.test.js
  - tests/unit/package-lock-pinned.test.js
  - tests/e2e/.llm-spend-ledger.json
  - tests/e2e/scripts/e2e-weekly-digest.test.js
autonomous: true
requirements:
  - CLEANUP-01
user_setup: []
tags:
  - v4.0
  - cleanup
  - integration-audit
  - int-fix
must_haves:
  truths:
    - "5 v3.1→v4.0 ARCHITECTURE §4 touchpoints (TP-01..TP-05) are pinned by a vitest regression test that fails on a one-sided rename of any producer symbol / line / branch-namer / shim import / triple-gate string"
    - "tests/e2e/.llm-spend-ledger.json is back to the Phase 39 seed-only shape: exactly one month, exactly one bootstrap iteration, total_usd 0 — Test 48 at tests/unit/llm-ledger.test.js (months.length toBe(1)) returns GREEN"
    - "tests/e2e/scripts/e2e-weekly-digest.test.js no longer hardcodes the '2026-05' ledger month key — the test's renderCostLine fixture writes to `[new Date().toISOString().slice(0,7)]` so it never calendar-rolls"
    - "package-lock.json pins @anthropic-ai/sdk to EXACT 0.100.1 (no caret); a vitest static-grep test fails if a future npm install re-introduces ^0.100.1 or bumps to 0.100.2+"
    - "Exactly 3 atomic INT-FIX commits land in the order INT-FIX-LEDGER → INT-FIX-CAL → INT-FIX-LOCK, each matching `fix(47-cleanup): INT-FIX-<TAG> — <one-line>`, mirroring Phase 38 INT-FIX-01..03"
  artifacts:
    - path: "tests/unit/v4-touchpoints.test.js"
      provides: "5 touchpoint regression tests (TP-01 triage label, TP-02 fingerprint→branch namer, TP-03 invokeClaudePWithLedger subscription, TP-04 verifyCitation CLI shim, TP-05 _skipCiGuard triple-gate)"
      min_lines: 80
      contains: "TP-01"
    - path: "tests/unit/package-lock-pinned.test.js"
      provides: "INT-FIX-LOCK static-grep: package-lock.json + package.json pin @anthropic-ai/sdk EXACT 0.100.1"
      min_lines: 20
      contains: "@anthropic-ai/sdk"
    - path: "tests/e2e/.llm-spend-ledger.json"
      provides: "Seed-only committed ledger — Phase 39 LEDGER-04 contract restored (months={<seed-month>}, invocations=1, total_usd=0)"
      contains: "39-bootstrap"
    - path: "tests/e2e/scripts/e2e-weekly-digest.test.js"
      provides: "INT-FIX-CAL: line 389 ledger-month key replaced with dynamic [new Date().toISOString().slice(0,7)] derivation"
      contains: "new Date().toISOString().slice(0, 7)"
  key_links:
    - from: "tests/unit/v4-touchpoints.test.js TP-03"
      to: "tests/e2e/lib/llm-driver.js (export invokeClaudePWithLedger at L378)"
      via: "dynamic import + file-as-text grep"
      pattern: "invokeClaudePWithLedger"
    - from: "tests/unit/v4-touchpoints.test.js TP-04"
      to: "scripts/verify-single-case.mjs import + tests/e2e/lib/pdf-verifier.js verifyCitation export"
      via: "dynamic import + import-statement grep"
      pattern: "import\\s+\\{[^}]*verifyCitation"
    - from: "tests/unit/v4-touchpoints.test.js TP-05"
      to: "scripts/auto-fix-promote.mjs assertTripleGate at L67"
      via: "file-as-text grep for 3 gate strings AND _skipCiGuard:true"
      pattern: "_skipCiGuard:\\s*true"
    - from: "tests/unit/package-lock-pinned.test.js"
      to: "package-lock.json + package.json"
      via: "fs.readFileSync + regex"
      pattern: "\"@anthropic-ai\\\\/sdk\":\\s*\"0\\\\.100\\\\.1\""
---

<objective>
Close CLEANUP-01: pin all 5 v3.1→v4.0 ARCHITECTURE §4 touchpoint contracts with vitest regression tests in a single new file `tests/unit/v4-touchpoints.test.js`, then resolve the 3 pre-existing test regressions catalogued in 47-RESEARCH.md as atomic INT-FIX-* commits (LEDGER, CAL, LOCK) — matching Phase 38 Plan 01's pattern.

Purpose: Eliminate four silent-failure classes — (a) one-sided rename of any of the 5 v3.1→v4.0 touchpoint contracts producing silent zero-fan-out OR shim-stops-importing-from-canonical-source, (b) leaked Opus calls in the committed ledger violating Phase 39 LEDGER-04 seed-only contract, (c) calendar-rollover flake in `e2e-weekly-digest.test.js` hardcoded `'2026-05'` ledger key, (d) `npm install` silently un-pinning `@anthropic-ai/sdk` via `^0.100.1` caret re-creep.

Output: 5 touchpoint regression tests in a single file + 1 new INT-FIX-LOCK test file + 1 committed-ledger reset + 1 single-line dynamic-month edit. Four commits in the order TP-bundle (touchpoints) → INT-FIX-LEDGER → INT-FIX-CAL → INT-FIX-LOCK.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/47-v4-0-cleanup/47-CONTEXT.md
@.planning/phases/47-v4-0-cleanup/47-RESEARCH.md
@.planning/phases/47-v4-0-cleanup/47-VALIDATION.md
@.planning/milestones/v3.1-phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Producer + consumer symbols / lines that the 5 touchpoint tests assert. -->
<!-- All file:line references verified at planning time (2026-06-01) against tree state. -->

Touchpoint 1 — triage label (TP-01):
  Producer: tests/e2e/lib/issue-payload-builder.js L261:
    const labels = [category, 'e2e-nightly', 'triage'];
  Consumer: .github/workflows/v40-auto-fix.yml L43 `on:`, downstream filter on label.name == 'triage'

Touchpoint 2 — fingerprint() → branch-namer (TP-02):
  Producer: scripts/e2e-report-issue.mjs L78:
    export function fingerprint(caseId, errorClass, topOfStackHash) — sha256 substring(0,12), 12-hex lowercase
  Consumer: scripts/auto-fix.mjs L454-455:
    const fp8 = fingerprint.slice(0, 8);
    const branchName = `auto-fix/${issue}-${fp8}`;
  Body-line-1 comment shape (per buildIssueBody at scripts/e2e-report-issue.mjs L266):
    `<!-- fingerprint: ${fp} -->` (NOT `<!-- fp: ... -->` — that's the D-02 v2 marker used in dual-search only).

Touchpoint 3 — invokeClaudePWithLedger (TP-03):
  Producer: tests/e2e/lib/llm-driver.js L378:
    export async function invokeClaudePWithLedger({systemPrompt, userPrompt, timeoutMs, phase, source})
    L391: refuses with `invokeClaudePWithLedger refused: subscription-local invariant (CI detected)` when process.env.CI / GITHUB_ACTIONS
  Consumer: scripts/auto-fix.mjs L617-618:
    if (transport === 'subscription') {
      sdkResult = await invokeClaudePWithLedger({...});
    }
  Return-shape keys used after the call: sdkResult.ok, sdkResult.ciGate, sdkResult.capBlocked

Touchpoint 4 — verifyCitation → verify-single-case shim (TP-04):
  Producer: tests/e2e/lib/pdf-verifier.js — `export async function verifyCitation({patentId, selectedText, observedCitation})`
  Consumer: scripts/verify-single-case.mjs L39:
    import { verifyCitation } from '../tests/e2e/lib/pdf-verifier.js';
  Contract: shim MUST import from the canonical path; MUST NOT re-implement verifyCitation locally.

Touchpoint 5 — runPromote + _skipCiGuard triple-gate (TP-05):
  Producer: scripts/promote-from-quarantine.mjs L115 — runPromote({id, confirm:true, _skipCiGuard?}); CI refusal downstream
  Consumer: scripts/auto-fix-promote.mjs L67-80 — assertTripleGate({prLabels, merged, sourceIssueLabels}):
    Leg 1: prLabels.includes('auto-fix:verified')   — throws TRIPLE_GATE_FAILED if missing
    Leg 2: merged === true                            — throws TRIPLE_GATE_FAILED if missing
    Leg 3: sourceIssueLabels.includes('triage')       — throws TRIPLE_GATE_FAILED if missing
  Downstream call site passes _skipCiGuard:true ONLY after assertTripleGate succeeds.
  Existing behavioural test at tests/unit/auto-fix-promote-gate.test.js already covers rejection paths.

INT-FIX-LEDGER current dirty state (tests/e2e/.llm-spend-ledger.json):
  months: { '2026-05': {seed only — invocations:1, total_usd:0}, '2026-06': {LEAKED — 3 iterations, all carrying iteration_n + run_id + phase:null} }
  Seed entry shape (preserve verbatim):
    {
      "iso": "2026-05-31T16:03:31.594Z",
      "model": "claude-sonnet-4-6",
      "cost_usd": 0,
      "tokens_in": 0,
      "tokens_out": 0,
      "phase": "39-bootstrap",
      "transport": "sdk",
      "source": "phase-39-flip"
    }
  Bucket frontmatter to preserve:
    "invocations": 1, "total_usd": 0, "last_invocation_iso": "2026-05-31T16:03:31.595Z"
  Root cause (per 47-RESEARCH.md §"INT-FIX-LEDGER root cause"):
    Leaked entries' iteration_n + run_id + phase:null signature is unique to scripts/e2e-explore.mjs L262/L313 — direct local `npm run e2e:explore` writes leaked into the canonical LEDGER_PATH because E2E_LEDGER_PATH_OVERRIDE was NOT set.
    DO NOT relax Test 48 (tests/unit/llm-ledger.test.js around L1010-1020). Reset the file; document root cause in the commit body. Test 48 assertion stays: `expect(months.length).toBe(1)` and `expect(bucket.invocations).toBe(1)` and `expect(bucket.total_usd).toBe(0)`.

INT-FIX-CAL current failing line:
  tests/e2e/scripts/e2e-weekly-digest.test.js L389:
    '2026-05': { invocations: 5, total_usd: 12.5, last_invocation_iso: null, iterations: [] },
  renderCostLine reads currentMonth() (system clock); now June 2026 → month key '2026-06' → seeded bucket missed → returns $0.00 not $12.50.
  Fix (1-line, per Pitfall 2): replace the literal '2026-05' key with a computed key
    [new Date().toISOString().slice(0, 7)]: { invocations: 5, total_usd: 12.5, last_invocation_iso: null, iterations: [] },
  WARNING (Pitfall 2): do NOT substitute the file's `PIN_NOW` constant's month — PIN_NOW pins calendar facts for week-label tests; for this ledger-bucket-key test, the right pattern is "current month at TEST-RUN time" via dynamic Date.

INT-FIX-LOCK shape:
  package.json L39: "@anthropic-ai/sdk": "0.100.1",  (exact, no caret) — VERIFIED
  package-lock.json L9 (packages.""."devDependencies" block):  "@anthropic-ai/sdk": "0.100.1",  — VERIFIED
  package-lock.json L19-21 (packages."node_modules/@anthropic-ai/sdk" block):
    "version": "0.100.1",
    "resolved": "https://registry.npmjs.org/@anthropic-ai/sdk/-/sdk-0.100.1.tgz",
  Test pattern (Pattern 3 in 47-RESEARCH.md):
    expect(lock).toMatch(/"@anthropic-ai\/sdk":\s*"0\.100\.1"/);
    expect(lock).not.toMatch(/"@anthropic-ai\/sdk":\s*"\^/);
    expect(lock).toMatch(/"node_modules\/@anthropic-ai\/sdk":\s*\{[^}]*"version":\s*"0\.100\.1"/s);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TP-bundle — Create tests/unit/v4-touchpoints.test.js with 5 touchpoint regression tests (TP-01..TP-05)</name>
  <files>tests/unit/v4-touchpoints.test.js</files>
  <read_first>
    - .planning/phases/47-v4-0-cleanup/47-RESEARCH.md (§"Integration Touchpoint Catalog" lines 338-516 — all 5 producer/consumer file:line tables + proposed test shapes; §"Pattern 1: Touchpoint contract regression test" lines 254-282; §"Pattern 2: Behavioural triple-gate test" lines 284-287)
    - .planning/research/ARCHITECTURE.md §4 (5 touchpoints + `_skipCiGuard` triple-gate spec — canonical source of truth referenced by 47-CONTEXT.md and 47-RESEARCH.md)
    - tests/e2e/lib/issue-payload-builder.js (full file — confirm line 261 labels array shape verbatim)
    - tests/e2e/lib/llm-driver.js (lines 370-400 — invokeClaudePWithLedger signature + L391 CI refusal text; lines 500-550 — invokeAnthropicSdkWithLedger signature for shape-comparison only)
    - tests/e2e/lib/pdf-verifier.js (lines 820-840 — verifyCitation export signature)
    - scripts/e2e-report-issue.mjs (lines 51, 78, 236-270 — QUARANTINE_REPORT_FILENAME export, fingerprint() signature, buildIssueBody L266 `<!-- fingerprint: ${fp} -->` line-1-no-but-tail-of-body shape)
    - scripts/auto-fix.mjs (lines 60-100 — import block including invokeClaudePWithLedger; lines 400-470 — branch-namer + fp8 slice; lines 610-625 — transport==='subscription' branch; lines 625-700 — sdkResult.ok/ciGate/capBlocked switch)
    - scripts/verify-single-case.mjs (lines 1-50 — confirm L39 import of verifyCitation from '../tests/e2e/lib/pdf-verifier.js')
    - scripts/promote-from-quarantine.mjs (lines 110-145 — runPromote export + _skipCiGuard parameter)
    - scripts/auto-fix-promote.mjs (lines 60-90 — assertTripleGate function, three legs, three error messages)
    - .github/workflows/v40-auto-fix.yml (lines 40-110 — on: trigger block, label filter)
    - tests/unit/auto-fix-promote-gate.test.js (full file — confirm it exists; TP-05 delegates rejection-path coverage to it)
    - .planning/milestones/v3.1-phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-01-PLAN.md (Task 1 + Task 3 — verbatim templates for file-as-text grep tests + step-window YAML scoping pattern)
    - tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js (lines 1-100 — verbatim template for file-as-text grep + step-window-scoped YAML assertion pattern reused by TP-01 consumer check)
  </read_first>
  <behavior>
    - TP-01-triage-label-filter test 1: `tests/e2e/lib/issue-payload-builder.js` source matches /labels\s*=\s*\[[^\]]*['"]triage['"][^\]]*\]/ (producer pins literal 'triage')
    - TP-01-triage-label-filter test 2: `.github/workflows/v40-auto-fix.yml` source contains `types: [labeled]` (the issues trigger filter) AND contains the literal string `'triage'` (used in the label filter logic)
    - TP-02-fingerprint-branch test 1: dynamic import of scripts/e2e-report-issue.mjs returns a `fingerprint` function; calling it with `('case-id', 'WRONG_CITATION', '')` returns a string matching /^[0-9a-f]{12}$/
    - TP-02-fingerprint-branch test 2: `scripts/auto-fix.mjs` source contains the branch-namer pattern `auto-fix/${issue}-${fp8}` (regex: /auto-fix\/\$\{issue\}-\$\{fp8\}/) AND contains `fingerprint.slice(0, 8)`
    - TP-02-fingerprint-branch test 3: `scripts/e2e-report-issue.mjs` source contains the hidden HTML comment template `<!-- fingerprint: ${fp} -->` (used by auto-fix.mjs body-grep dedup)
    - TP-03-subscription-ledger test 1: dynamic import of `tests/e2e/lib/llm-driver.js` exports `invokeClaudePWithLedger` as a function
    - TP-03-subscription-ledger test 2: `scripts/auto-fix.mjs` source contains both `transport === 'subscription'` AND `await invokeClaudePWithLedger(` within an explicit branch (file-as-text grep — does not need full AST)
    - TP-03-subscription-ledger test 3: `scripts/auto-fix.mjs` source contains all three return-shape keys post-call: `sdkResult.ok`, `sdkResult.ciGate`, `sdkResult.capBlocked` (proves the SDK + subscription transports share the return shape)
    - TP-04-verify-single-case-shim test 1: dynamic import of `tests/e2e/lib/pdf-verifier.js` exports `verifyCitation` as a function
    - TP-04-verify-single-case-shim test 2: `scripts/verify-single-case.mjs` source contains an import-statement matching /import\s+\{[^}]*verifyCitation[^}]*\}\s+from\s+['"][^'"]*tests\/e2e\/lib\/pdf-verifier\.js['"]/
    - TP-04-verify-single-case-shim test 3: `scripts/verify-single-case.mjs` source does NOT contain a local re-implementation `function verifyCitation(` or `async function verifyCitation(` (guards against the consumer drifting from single source of truth)
    - TP-05-skipciguard-triple-gate test 1: `scripts/promote-from-quarantine.mjs` source contains an export of `runPromote` AND mentions `_skipCiGuard` (parameter is plumbed through)
    - TP-05-skipciguard-triple-gate test 2: `scripts/auto-fix-promote.mjs` source contains all three triple-gate literal strings: `'auto-fix:verified'`, the `merged === true` predicate (or equivalent `merged !== true` check), and `'triage'`; AND contains `_skipCiGuard: true`
    - TP-05-skipciguard-triple-gate test 3: behavioural rejection paths are exercised by `tests/unit/auto-fix-promote-gate.test.js` — assert that file exists (path-existence check, since the behavioural tests are owned by Phase 44, this touchpoint test just asserts continued presence)
    - All 12 it() blocks (3 across TP-01, 3 across TP-02, 3 across TP-03, 3 across TP-04 — TP-05 has 3 = 15 total it() blocks) MUST pass when run against the post-fix tree
  </behavior>
  <action>
    Step 1 (RED): Create `tests/unit/v4-touchpoints.test.js` (new file). At the top import: `import { describe, it, expect } from 'vitest';` + `import fs from 'node:fs';` + `import path from 'node:path';` + `import { fileURLToPath } from 'node:url';`. Resolve REPO_ROOT relative to __dirname (the test lives at tests/unit/ — REPO_ROOT is `path.resolve(__dirname, '..', '..')`). Add a top-level `describe('Phase 47 CLEANUP-01: 5 v3.1→v4.0 ARCHITECTURE §4 touchpoint contracts', ...)` block. Inside add 5 nested describe() blocks named exactly `TP-01-triage-label-filter`, `TP-02-fingerprint-branch`, `TP-03-subscription-ledger`, `TP-04-verify-single-case-shim`, `TP-05-skipciguard-triple-gate` so the per-task VALIDATION.md grep names match.
    Step 2 (still RED): Inside `TP-01-triage-label-filter`, add `it('producer (issue-payload-builder.js) labels array ends with literal "triage"', () => {...})` reading `tests/e2e/lib/issue-payload-builder.js` as text and asserting the regex match per Behavior bullet 1. Add `it('consumer (v40-auto-fix.yml) trigger uses issues.labeled with triage filter', () => {...})` reading `.github/workflows/v40-auto-fix.yml` and asserting both `types: [labeled]` and the literal string `'triage'` appear. Use the step-window grep pattern verbatim from `tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js` Y2 if scoping is needed; flat string-contains assertions are sufficient given the workflow's small surface.
    Step 3 (still RED): Inside `TP-02-fingerprint-branch`, add: (a) `it('fingerprint() returns 12-hex lowercase')` — `const mod = await import('../../scripts/e2e-report-issue.mjs'); expect(typeof mod.fingerprint).toBe('function'); expect(mod.fingerprint('test-case', 'WRONG_CITATION', '')).toMatch(/^[0-9a-f]{12}$/);`. (b) `it('auto-fix.mjs branch-namer uses fp.slice(0,8) of fingerprint')` — read scripts/auto-fix.mjs as text, assert `/auto-fix\/\$\{issue\}-\$\{fp8\}/` matches AND `/fingerprint\.slice\(0,\s*8\)/` matches. (c) `it('issue-body fingerprint comment template uses canonical <!-- fingerprint: ${fp} --> form')` — read scripts/e2e-report-issue.mjs as text, assert the substring `<!-- fingerprint: ${fp} -->` appears (this is the dedup-grep target downstream consumers rely on).
    Step 4 (still RED): Inside `TP-03-subscription-ledger`, add: (a) `it('llm-driver.js exports invokeClaudePWithLedger as a function')` — `const mod = await import('../../tests/e2e/lib/llm-driver.js'); expect(typeof mod.invokeClaudePWithLedger).toBe('function');`. (b) `it('auto-fix.mjs subscription branch awaits invokeClaudePWithLedger')` — read scripts/auto-fix.mjs as text, assert `/transport\s*===\s*['"]subscription['"]/` matches AND `/await\s+invokeClaudePWithLedger\(/` matches. (c) `it('subscription transport return shape uses .ok / .ciGate / .capBlocked keys')` — read scripts/auto-fix.mjs, assert each of `sdkResult.ok`, `sdkResult.ciGate`, `sdkResult.capBlocked` appears as a substring.
    Step 5 (still RED): Inside `TP-04-verify-single-case-shim`, add: (a) `it('pdf-verifier.js exports verifyCitation as a function')` — `const mod = await import('../../tests/e2e/lib/pdf-verifier.js'); expect(typeof mod.verifyCitation).toBe('function');`. (b) `it('verify-single-case.mjs imports verifyCitation from canonical pdf-verifier.js path')` — assert the import-statement regex `/import\s+\{[^}]*verifyCitation[^}]*\}\s+from\s+['"][^'"]*tests\/e2e\/lib\/pdf-verifier\.js['"]/` matches scripts/verify-single-case.mjs source. (c) `it('verify-single-case.mjs has no local re-implementation of verifyCitation')` — assert the regex `/(?:export\s+)?(?:async\s+)?function\s+verifyCitation\s*\(/` does NOT match the file source.
    Step 6 (still RED): Inside `TP-05-skipciguard-triple-gate`, add: (a) `it('promote-from-quarantine.mjs exports runPromote and threads _skipCiGuard')` — read scripts/promote-from-quarantine.mjs as text, assert `/export\s+(async\s+)?function\s+runPromote/` matches AND substring `_skipCiGuard` appears. (b) `it('auto-fix-promote.mjs passes _skipCiGuard:true after asserting all 3 gate strings')` — read scripts/auto-fix-promote.mjs, assert each substring: `'auto-fix:verified'`, the regex `/(merged\s*===?\s*true|merged\s*!==?\s*true)/`, `'triage'`, AND `/_skipCiGuard:\s*true/`. (c) `it('behavioural rejection paths are covered by tests/unit/auto-fix-promote-gate.test.js (path exists)')` — `expect(fs.existsSync(path.join(REPO_ROOT, 'tests/unit/auto-fix-promote-gate.test.js'))).toBe(true);`.
    Step 7 (GREEN — already): Run `npx vitest run tests/unit/v4-touchpoints.test.js --reporter=dot`. ALL 15 it() blocks should PASS immediately because the contracts already hold in the post-Phase-46 tree (the tests pin existing behaviour — no production code change needed for Task 1). If any it() FAILS, the failure points to a real touchpoint drift — STOP and surface to user per CLAUDE.md C1 before adjusting test or source; do not weaken the assertion to make it pass.
    Step 8 (COMMIT): Stage exactly `tests/unit/v4-touchpoints.test.js`. Commit message: `test(47-cleanup): pin 5 v4.0 ARCHITECTURE §4 touchpoint contracts (TP-01..TP-05)`. Single atomic commit.
  </action>
  <acceptance_criteria>
    - `test -f tests/unit/v4-touchpoints.test.js` exits 0
    - `grep -cE "^\s+describe\\(['\"]TP-0[1-5]-" tests/unit/v4-touchpoints.test.js` returns 5 (5 nested describe blocks, one per touchpoint)
    - `grep -cE "^\s+it\\(" tests/unit/v4-touchpoints.test.js` returns 15 (3 it() blocks per touchpoint × 5 = 15)
    - `npx vitest run tests/unit/v4-touchpoints.test.js --reporter=dot` exits 0 (all 15 tests pass against current tree)
    - `npx vitest run tests/unit/v4-touchpoints.test.js -t "TP-01-triage-label-filter" --reporter=dot` exits 0
    - `npx vitest run tests/unit/v4-touchpoints.test.js -t "TP-02-fingerprint-branch" --reporter=dot` exits 0
    - `npx vitest run tests/unit/v4-touchpoints.test.js -t "TP-03-subscription-ledger" --reporter=dot` exits 0
    - `npx vitest run tests/unit/v4-touchpoints.test.js -t "TP-04-verify-single-case-shim" --reporter=dot` exits 0
    - `npx vitest run tests/unit/v4-touchpoints.test.js -t "TP-05-skipciguard-triple-gate" --reporter=dot` exits 0
    - `git log --oneline -1` shows commit subject matching `^[0-9a-f]+ test\(47-cleanup\): pin 5 v4\.0 ARCHITECTURE §4 touchpoint contracts`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/v4-touchpoints.test.js --reporter=dot</automated>
  </verify>
  <done>5 touchpoint regression tests pinned in tests/unit/v4-touchpoints.test.js (15 it() blocks across 5 nested describe blocks named TP-01..TP-05); all pass against the post-Phase-46 tree; single atomic test-commit landed; future rename of any producer/consumer in the touchpoint chain trips the test rather than a silent runtime drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: INT-FIX-LEDGER — Reset tests/e2e/.llm-spend-ledger.json to Phase 39 seed-only shape (delete leaked 2026-06 bucket)</name>
  <files>tests/e2e/.llm-spend-ledger.json</files>
  <read_first>
    - tests/e2e/.llm-spend-ledger.json (current dirty state — months={'2026-05': seed-only, '2026-06': 3 LEAKED iterations})
    - tests/unit/llm-ledger.test.js (lines 998-1034 — Test 48 contract: months.length toBe(1), invocations toBe(1), total_usd toBe(0), iterations[0] is the Phase 39 seed)
    - tests/e2e/lib/llm-ledger.js (lines 75-100 — LEDGER_PATH override resolver; root-cause documentation reference)
    - scripts/e2e-explore.mjs (lines 260-320 — appendLedgerEntry calls that carry iteration_n + run_id + phase=null; this is the writer signature that leaked)
    - .planning/phases/47-v4-0-cleanup/47-RESEARCH.md (§"INT-FIX-LEDGER root cause and fix shape" lines 519-580; §"Pitfall 1: relaxing Test 48 instead of fixing root cause" lines 1149-1153)
  </read_first>
  <behavior>
    - Before fix: `npx vitest run tests/unit/llm-ledger.test.js -t "Test 48" --reporter=dot` exits non-zero (months.length === 2 not 1)
    - After fix: same command exits 0
    - After fix: `tests/e2e/.llm-spend-ledger.json` has exactly one key under `months` and that bucket has exactly one iteration matching the Phase 39 seed (model=claude-sonnet-4-6, phase='39-bootstrap', transport='sdk', source='phase-39-flip', cost_usd=0, tokens_in=0, tokens_out=0)
    - After fix: the file contains NO `iteration_n` field, NO `run_id` field, NO `"phase": null` substring (defensive — these are the leak-signature markers)
  </behavior>
  <action>
    Step 1 (RED, verify failure): `npx vitest run tests/unit/llm-ledger.test.js -t "Test 48" --reporter=dot` — confirm it currently FAILS at `expect(months.length).toBe(1)` (current state has both '2026-05' and '2026-06' keys). This step verifies the contract is being enforced; do NOT proceed if Test 48 already passes — the leak may have already been cleaned by an out-of-band edit.
    Step 2 (GREEN): Use the Write tool to overwrite `tests/e2e/.llm-spend-ledger.json` with exactly this content (preserve verbatim — these values are the Phase 39 bootstrap seed and are pinned by Test 48):
      Line 1: `{`
      Line 2: `  "version": 1,`
      Line 3: `  "months": {`
      Line 4: `    "2026-05": {`
      Line 5: `      "invocations": 1,`
      Line 6: `      "total_usd": 0,`
      Line 7: `      "last_invocation_iso": "2026-05-31T16:03:31.595Z",`
      Line 8: `      "iterations": [`
      Line 9: `        {`
      Line 10: `          "iso": "2026-05-31T16:03:31.594Z",`
      Line 11: `          "model": "claude-sonnet-4-6",`
      Line 12: `          "cost_usd": 0,`
      Line 13: `          "tokens_in": 0,`
      Line 14: `          "tokens_out": 0,`
      Line 15: `          "phase": "39-bootstrap",`
      Line 16: `          "transport": "sdk",`
      Line 17: `          "source": "phase-39-flip"`
      Line 18: `        }`
      Line 19: `      ]`
      Line 20: `    }`
      Line 21: `  }`
      Line 22: `}`
      (Trailing newline after `}`.) The bucket key '2026-05' is preserved verbatim — Test 48 reads whichever single key exists, so this matches Test 48's `const months = Object.keys(j.months); expect(months.length).toBe(1);` then `const bucket = j.months[months[0]];` without assuming a specific calendar string.
    Step 3 (GREEN verify): Run `npx vitest run tests/unit/llm-ledger.test.js -t "Test 48" --reporter=dot` — confirm it now exits 0. Also run `grep -c "iteration_n\|\"run_id\"\|\"phase\":\s*null" tests/e2e/.llm-spend-ledger.json` — expect 0 matches (no leak-signature markers remain).
    Step 4 (COMMIT): Stage exactly `tests/e2e/.llm-spend-ledger.json`. Commit message (use heredoc to preserve the multi-line body):
      Subject: `fix(47-cleanup): INT-FIX-LEDGER — reset committed ledger to Phase 39 seed-only`
      Body: `Leaked entries traced to local 'npm run e2e:explore' iterations (iteration_n + run_id + phase:null writer signature is unique to scripts/e2e-explore.mjs:262/313). Direct local invocation without E2E_LEDGER_PATH_OVERRIDE wrote 3 Opus calls into the canonical LEDGER_PATH between 2026-06-01 16:00:03 and 16:00:40 UTC. Reset honors the Phase 39 LEDGER-04 seed-only contract; Test 48 at tests/unit/llm-ledger.test.js (months.length toBe(1)) is restored to GREEN. DEV NOTE: local 'npm run e2e:explore' must set E2E_LEDGER_PATH_OVERRIDE=$(mktemp -d)/ledger.json before iteration to avoid polluting the committed ledger.`
      Single atomic commit.
  </action>
  <acceptance_criteria>
    - `node -e "const j=JSON.parse(require('fs').readFileSync('tests/e2e/.llm-spend-ledger.json','utf8')); const m=Object.keys(j.months); if(m.length!==1) throw new Error('months.length='+m.length); const b=j.months[m[0]]; if(b.invocations!==1) throw new Error('invocations='+b.invocations); if(b.total_usd!==0) throw new Error('total_usd='+b.total_usd); if(b.iterations.length!==1) throw new Error('iterations.length='+b.iterations.length); const it=b.iterations[0]; if(it.phase!=='39-bootstrap') throw new Error('phase='+it.phase); if(it.transport!=='sdk') throw new Error('transport='+it.transport); if(it.source!=='phase-39-flip') throw new Error('source='+it.source); console.log('ledger seed-only OK');"` exits 0
    - `grep -c "iteration_n\|\"run_id\"\|\"phase\":\s*null" tests/e2e/.llm-spend-ledger.json` returns 0
    - `npx vitest run tests/unit/llm-ledger.test.js -t "Test 48" --reporter=dot` exits 0
    - `git log --oneline -1` shows commit subject matching `^[0-9a-f]+ fix\(47-cleanup\): INT-FIX-LEDGER —`
    - `git log -1 --format=%b` includes the substring "iteration_n + run_id + phase:null" (root-cause documented in commit body)
    - `git log -1 --format=%b` includes "E2E_LEDGER_PATH_OVERRIDE" (dev-note documented in commit body)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/llm-ledger.test.js -t "Test 48" --reporter=dot</automated>
  </verify>
  <done>Committed ledger reset to Phase 39 seed-only shape; Test 48 GREEN; leak-signature markers (iteration_n, run_id, phase:null) absent from the file; root cause + dev-note documented in commit body; single atomic INT-FIX-LEDGER commit landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: INT-FIX-CAL — Replace hardcoded '2026-05' ledger month key at tests/e2e/scripts/e2e-weekly-digest.test.js:389 with dynamic [new Date().toISOString().slice(0,7)] derivation</name>
  <files>tests/e2e/scripts/e2e-weekly-digest.test.js</files>
  <read_first>
    - tests/e2e/scripts/e2e-weekly-digest.test.js (lines 380-400 — the failing renderCostLine test; line 389 contains the hardcoded '2026-05' literal; lines 60-75 — PIN_NOW fixture-time constant for context (do NOT use for this fix per Pitfall 2))
    - scripts/weekly-digest.mjs (around line 224 — renderCostLine reads currentMonth() = `new Date().toISOString().slice(0, 7)`; this is the consumer that explains why dynamic-key is correct)
    - .planning/phases/47-v4-0-cleanup/47-RESEARCH.md (§"INT-FIX-CAL — line + fix shape" lines 583-619; §"Pitfall 2: using PIN_NOW month instead of dynamic month" lines 1155-1159)
  </read_first>
  <behavior>
    - Before fix: `npx vitest run tests/e2e/scripts/e2e-weekly-digest.test.js -t "cost data unavailable when ledger absent" --reporter=dot` exits non-zero (test seeded '2026-05' bucket but renderCostLine reads currentMonth() = '2026-06' → returns $0.00 not $12.50)
    - After fix: same command exits 0; renderCostLine returns the seeded $12.50 against the dynamic-current-month bucket
    - After fix: the literal string `'2026-05'` no longer appears in the ledgerData object literal at L389
    - After fix: the file contains the substring `[new Date().toISOString().slice(0, 7)]` exactly once at the location of the ledgerData seed
  </behavior>
  <action>
    Step 1 (RED, verify failure): `npx vitest run tests/e2e/scripts/e2e-weekly-digest.test.js -t "returns" --reporter=dot 2>&1 | tail -20` — confirm at least one assertion fails with `expected '$0.00 / $100 (0%)' to contain '12.50'` (the calendar-rollover symptom).
    Step 2 (GREEN): Use the Edit tool on `tests/e2e/scripts/e2e-weekly-digest.test.js` at line 389. Locate this exact line:
      `        '2026-05': { invocations: 5, total_usd: 12.5, last_invocation_iso: null, iterations: [] },`
      Replace with this exact line (preserve the 8-space indent + trailing comma):
      `        [new Date().toISOString().slice(0, 7)]: { invocations: 5, total_usd: 12.5, last_invocation_iso: null, iterations: [] },`
      The change is one line: a string-literal key `'2026-05'` becomes a computed-property key `[new Date().toISOString().slice(0, 7)]` so the seeded bucket key matches whatever month renderCostLine reads from `currentMonth()` at test-run time. Per Pitfall 2 — do NOT substitute the file's `PIN_NOW` constant (PIN_NOW pins calendar facts for week-label tests; for ledger-bucket-key tests the right pattern is "current month at TEST-RUN time" via dynamic Date).
    Step 3 (GREEN verify): `npx vitest run tests/e2e/scripts/e2e-weekly-digest.test.js -t "returns" --reporter=dot` — confirm it exits 0. Also run the full file once: `npx vitest run tests/e2e/scripts/e2e-weekly-digest.test.js --reporter=dot` — confirm no other tests in the file regressed (e.g., PIN_NOW-based week-label tests still pass).
    Step 4 (COMMIT): Stage exactly `tests/e2e/scripts/e2e-weekly-digest.test.js`. Commit message: `fix(47-cleanup): INT-FIX-CAL — replace hardcoded '2026-05' ledger key with dynamic month derivation`. Body: `tests/e2e/scripts/e2e-weekly-digest.test.js:389 hardcoded the ledger month key '2026-05'; renderCostLine reads currentMonth() = new Date().toISOString().slice(0,7), so the test silently rolled over each calendar month after 2026-05 (now June 2026 → $0.00 not $12.50). One-line fix replaces the literal key with a computed [new Date().toISOString().slice(0,7)] property so the seeded bucket matches whatever month renderCostLine reads at test-run time. PIN_NOW deliberately NOT used (PIN_NOW is the right pattern for week-label tests pinning calendar facts; ledger-bucket-key needs dynamic current-month per Pitfall 2).`
  </action>
  <acceptance_criteria>
    - `grep -c "'2026-05':" tests/e2e/scripts/e2e-weekly-digest.test.js` returns 0 (the hardcoded ledger-key literal is gone; OTHER '2026-05-25' date strings used by PIN_NOW are unaffected because they don't end in `':`)
    - `grep -c "new Date().toISOString().slice(0, 7)" tests/e2e/scripts/e2e-weekly-digest.test.js` returns at least 1 (the dynamic-month computed-property key was added)
    - `npx vitest run tests/e2e/scripts/e2e-weekly-digest.test.js --reporter=dot` exits 0 (the targeted test passes AND no other tests in the file regressed)
    - `git log --oneline -1` shows commit subject matching `^[0-9a-f]+ fix\(47-cleanup\): INT-FIX-CAL —`
    - `git log -1 --format=%b` includes "PIN_NOW" (Pitfall 2 rationale documented in commit body)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/e2e/scripts/e2e-weekly-digest.test.js --reporter=dot</automated>
  </verify>
  <done>Calendar-rollover flake at tests/e2e/scripts/e2e-weekly-digest.test.js:389 fixed via 1-line dynamic-month-key derivation; targeted test + full file GREEN; PIN_NOW-based tests unaffected; rationale (Pitfall 2 anti-pattern) documented in commit body; single atomic INT-FIX-CAL commit landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: INT-FIX-LOCK — Add tests/unit/package-lock-pinned.test.js asserting @anthropic-ai/sdk EXACT 0.100.1 pin (no caret) in package.json AND package-lock.json</name>
  <files>tests/unit/package-lock-pinned.test.js</files>
  <read_first>
    - package.json (line 39 — `"@anthropic-ai/sdk": "0.100.1",` exact pin in devDependencies)
    - package-lock.json (lines 7-25 — exact pin in `packages.""."devDependencies"` block at L9 AND in `packages."node_modules/@anthropic-ai/sdk"` block at L19-21 with version 0.100.1 + resolved URL)
    - tests/unit/llm-ledger.test.js (lines 1036-1055 — Test 49 .gitignore static-grep pattern; verbatim template for the new file-as-text grep test)
    - .planning/phases/47-v4-0-cleanup/47-RESEARCH.md (§"INT-FIX-LOCK — grep pattern" lines 622-641; §"Pattern 3: Static-grep on package-lock.json" lines 288-318)
  </read_first>
  <behavior>
    - New file `tests/unit/package-lock-pinned.test.js` exists with at least 4 it() blocks
    - it('package.json pins @anthropic-ai/sdk to EXACT 0.100.1 — no caret') reads package.json, asserts `/"@anthropic-ai\/sdk":\s*"0\.100\.1"/` matches AND `/"@anthropic-ai\/sdk":\s*"\^/` does NOT match
    - it('package-lock.json devDependencies block pins @anthropic-ai/sdk EXACT 0.100.1') reads package-lock.json, asserts `/"@anthropic-ai\/sdk":\s*"0\.100\.1"/` matches AND `/"@anthropic-ai\/sdk":\s*"\^/` does NOT match
    - it('package-lock.json node_modules entry resolves @anthropic-ai/sdk to version 0.100.1') reads package-lock.json, asserts `/"node_modules\/@anthropic-ai\/sdk":\s*\{[^}]*"version":\s*"0\.100\.1"/s` matches
    - it('package-lock.json @anthropic-ai/sdk resolved URL points to 0.100.1 tarball') reads package-lock.json, asserts the substring `sdk-0.100.1.tgz` appears (catches drift even when version string is renamed via semver bump)
    - All 4 it() blocks pass against current tree
  </behavior>
  <action>
    Step 1 (RED, verify file does not exist): `test ! -f tests/unit/package-lock-pinned.test.js && echo "ok"` — confirm the file does NOT yet exist (this is a NEW test file; nothing to fail before creation).
    Step 2 (GREEN): Create `tests/unit/package-lock-pinned.test.js` (new file). Top of file:
      `import { describe, it, expect } from 'vitest';`
      `import fs from 'node:fs';`
      `import path from 'node:path';`
      `import { fileURLToPath } from 'node:url';`
      `const __dirname = path.dirname(fileURLToPath(import.meta.url));`
      `const REPO_ROOT = path.resolve(__dirname, '..', '..');`
      Then a top-level `describe('INT-FIX-LOCK: @anthropic-ai/sdk EXACT 0.100.1 pin survives in package.json + package-lock.json', () => {...})` containing 4 it() blocks matching the Behavior bullets verbatim. Within each it(), read the file via `fs.readFileSync(path.join(REPO_ROOT, '<file>'), 'utf8')` and apply the regex assertions per 47-RESEARCH.md Pattern 3 (lines 288-318). For the resolved-URL assertion (it block 4), use `expect(lock).toContain('sdk-0.100.1.tgz')` (the substring match catches future bumps).
    Step 3 (GREEN verify): `npx vitest run tests/unit/package-lock-pinned.test.js --reporter=dot` — confirm all 4 it() blocks PASS against the current tree (package.json + package-lock.json both already pin 0.100.1 exactly — VERIFIED at planning time).
    Step 4 (COMMIT): Stage exactly `tests/unit/package-lock-pinned.test.js`. Commit message: `fix(47-cleanup): INT-FIX-LOCK — static-grep test pins @anthropic-ai/sdk EXACT 0.100.1 in package-lock.json`. Body: `Defensive static-grep on package-lock.json + package.json asserts the @anthropic-ai/sdk exact pin (0.100.1, no caret) survives across npm install / merge-conflict resolution / future caret re-creep. Layered defense beyond CI npm ci: catches regression vectors that survive npm ci (manual lockfile edit, conflict resolution that reverts to ^, dependabot-style auto-update). Resolved-URL substring (sdk-0.100.1.tgz) catches version drift even when caret check passes.`
  </action>
  <acceptance_criteria>
    - `test -f tests/unit/package-lock-pinned.test.js` exits 0
    - `grep -cE "^\s+it\\(" tests/unit/package-lock-pinned.test.js` returns at least 4
    - `npx vitest run tests/unit/package-lock-pinned.test.js --reporter=dot` exits 0 (all 4 tests pass)
    - `git log --oneline -1` shows commit subject matching `^[0-9a-f]+ fix\(47-cleanup\): INT-FIX-LOCK —`
    - `git log -1 --format=%b` mentions "npm ci" (layered-defense rationale documented)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/package-lock-pinned.test.js --reporter=dot</automated>
  </verify>
  <done>Static-grep test pins @anthropic-ai/sdk EXACT 0.100.1 in both package.json and package-lock.json (4 assertions including resolved-URL substring); test GREEN; rationale (npm ci layered-defense) documented in commit body; single atomic INT-FIX-LOCK commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| v3.1 primitive exports ↔ v4.0 consumers | Each of the 5 ARCHITECTURE §4 touchpoints crosses a phase-version boundary; the producer (v3.1) and consumer (v4.0) live in different files and must agree on a literal string / function signature / branch-name template / import path / triple-gate predicate |
| Committed ledger ↔ local dev iteration | tests/e2e/.llm-spend-ledger.json is committed-but-versioned; local `npm run e2e:explore` runs (no override) write to the same canonical path that CI commits to, creating a leak path |
| package-lock.json ↔ npm install resolution | A future `npm install` against a manually-edited `^0.100.1` would silently re-resolve the lockfile to a different version; CI npm ci catches package.json drift but not all leak vectors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01-01 | Tampering | One-sided rename of producer (e.g., `'triage'` label becomes `'auto-triage'` in issue-payload-builder.js but workflow filter unchanged) → silent zero-fan-out | mitigate | TP-01 vitest grep asserts the literal `'triage'` on BOTH sides (producer at L261 AND workflow trigger). Future rename trips the test rather than silently halting the auto-fix pipeline. |
| T-47-01-02 | Tampering | Refactor extracts a local `verifyCitation` into scripts/verify-single-case.mjs, drifting from canonical pdf-verifier.js → divergent semantics | mitigate | TP-04 vitest asserts the canonical import path (`tests/e2e/lib/pdf-verifier.js`) AND that no local `function verifyCitation(` re-implementation exists. Drift surfaces immediately at test time. |
| T-47-01-03 | Elevation of Privilege | A future refactor moves the triple-gate check out of auto-fix-promote.mjs (or removes a leg) → `_skipCiGuard:true` reaches runPromote without the human-gate invariant | mitigate | TP-05 vitest asserts all 3 gate strings (`'auto-fix:verified'`, `merged === true`, `'triage'`) AND `_skipCiGuard:true` appear in scripts/auto-fix-promote.mjs source. Behavioural rejection paths remain covered by the existing tests/unit/auto-fix-promote-gate.test.js (Phase 44). Combined defense — contract + behaviour. |
| T-47-01-04 | Information Disclosure | Continued leak of private spend pattern (Opus call costs, run-IDs, hour-precision timestamps) into the committed git history via direct `npm run e2e:explore` runs without override | mitigate | INT-FIX-LEDGER resets the committed ledger to seed-only; commit body documents the leak path (e2e-explore.mjs writer signature) so future maintainers know to set E2E_LEDGER_PATH_OVERRIDE for local iteration. Test 48 stands as the canary. |
| T-47-01-05 | Tampering | Calendar-rollover flake makes the test suite silently RED each month — masks real regressions in renderCostLine downstream | mitigate | INT-FIX-CAL replaces the hardcoded '2026-05' ledger month key with dynamic Date derivation. Test now passes regardless of calendar month at test-run time. |
| T-47-01-06 | Tampering | `npm install` against a manually-edited `^0.100.1` in package.json silently bumps @anthropic-ai/sdk to 0.100.2+ (breaking SDK invariants e.g. Phase 39 ESLint guard, cache_control handling) | mitigate | INT-FIX-LOCK vitest static-grep on BOTH package.json AND package-lock.json (4 assertions including caret-negative + resolved-URL substring). Catches drift that survives `npm ci` (which only catches package.json mismatch with lockfile, not manual lockfile edits). |
| T-47-01-SC | Tampering | npm/pip/cargo installs | accept | Phase 47 installs ZERO new packages per 47-CONTEXT.md hard rule (v4.0 zero-new-dep). 47-RESEARCH.md §"Package Legitimacy Audit" (line 149) is empty by construction. |
</threat_model>

<verification>
After all 4 tasks complete:
1. All 4 plan-owned tests pass: `npx vitest run tests/unit/v4-touchpoints.test.js tests/unit/package-lock-pinned.test.js tests/unit/llm-ledger.test.js tests/e2e/scripts/e2e-weekly-digest.test.js --reporter=dot` exits 0.
2. Full unit suite still green (no regression to other 1000+ tests): `npm run test:src` exits 0.
3. Lint clean: `npm run lint` exits 0.
4. Git log shows exactly 4 new commits in order — TP bundle → INT-FIX-LEDGER → INT-FIX-CAL → INT-FIX-LOCK — `git log --oneline -4 | grep -cE "^[0-9a-f]+ (test|fix)\(47-cleanup\):"` returns 4.
5. Committed ledger seed-only check: `node -e "const j=JSON.parse(require('fs').readFileSync('tests/e2e/.llm-spend-ledger.json','utf8')); if(Object.keys(j.months).length!==1) throw new Error('multi-month'); console.log('ok')"` exits 0.
</verification>

<success_criteria>
- 5 v3.1→v4.0 ARCHITECTURE §4 touchpoints pinned by 15 it() blocks in tests/unit/v4-touchpoints.test.js (named TP-01..TP-05)
- 3 INT-FIX commits land (LEDGER → CAL → LOCK) in that order, each matching `fix(47-cleanup): INT-FIX-<TAG> —`, each with root-cause documented in commit body
- tests/e2e/.llm-spend-ledger.json restored to Phase 39 seed-only shape; Test 48 GREEN
- tests/e2e/scripts/e2e-weekly-digest.test.js no longer hardcodes any ledger month key; targeted test GREEN
- tests/unit/package-lock-pinned.test.js pins @anthropic-ai/sdk EXACT 0.100.1 in both package.json and package-lock.json
- `npm run test:src && npm run lint` exits 0
</success_criteria>

<output>
Create `.planning/phases/47-v4-0-cleanup/47-01-SUMMARY.md` per the standard template, recording: the 4 commit SHAs, regression-test file paths created, ledger before/after summary (months count, total iterations dropped), Test 48 before/after status, list of 5 TP-* test names with PASS counts, INT-FIX-LOCK assertion count.
</output>
