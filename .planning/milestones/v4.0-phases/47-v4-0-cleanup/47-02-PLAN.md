---
phase: 47
plan: 02
type: execute
wave: 3
depends_on:
  - 47-01
files_modified:
  - .planning/phases/39-sdk-driver-+-ledger-v2-+-branch-protection-wave-0/39-VALIDATION.md
  - .planning/phases/40-deps-update-+-cost-ledger-snapshot-workflows/40-VALIDATION.md
  - .planning/phases/41-verifier-gate-workflow-+-verify-single-case-mjs-cli-shim/41-VALIDATION.md
  - .planning/phases/42-fix-prompt-builder-+-wrong-citation-vertical-slice/42-VALIDATION.md
  - .planning/phases/43-v40-auto-fix-yml-workflow-+-draft-pr-creation/43-VALIDATION.md
  - .planning/phases/44-v40-auto-promote-yml-+-triple-gate-skipciguard/44-VALIDATION.md
  - .planning/phases/45-per-error-class-expansion-+-flake-5-state-machine/45-VALIDATION.md
  - .planning/phases/46-gsd-fix-issue-local-ux-+-ledger-v2-dashboard/46-VALIDATION.md
autonomous: false
requirements:
  - CLEANUP-02
user_setup: []
tags:
  - v4.0
  - cleanup
  - nyquist
  - retroactive
must_haves:
  truths:
    - "Each of the 8 v4.0 phases (39, 40, 41, 42, 43, 44, 45, 46) has had `Skill(gsd-validate-phase, <N>)` invoked against State B (cold) — no draft VALIDATION.md previously existed; the skill reconstructs from PLAN.md + SUMMARY.md + VERIFICATION.md inputs and stamps in one pass"
    - "Every phase whose auditor returns GAPS FILLED is stamped `nyquist_compliant: true` in its newly-created VALIDATION.md frontmatter"
    - "Any phase whose auditor returns PARTIAL or ESCALATE on a REAL gap (not Manual-Only) has its gap documented in this plan's SUMMARY and surfaced as new tech_debt for 47-04's v4.0-MILESTONE-AUDIT.md authoring step"
    - "Manual-Only entries (UAT items requires-push for phases 43/44/45) are NOT treated as auditor escalations (per Pitfall 6 in 47-RESEARCH.md) — they are PRE-CLASSIFIED COVERED-MANUAL via the `human_verification:` block in v4.0-MILESTONE-AUDIT.md to be bootstrapped in Plan 47-04"
    - "After all 8 stamping attempts, a per-phase outcome table is committed to 47-02-SUMMARY.md (phase / skill outcome / nyquist_compliant value / tests referenced / new gap surfaced)"
  artifacts:
    - path: ".planning/phases/39-sdk-driver-+-ledger-v2-+-branch-protection-wave-0/39-VALIDATION.md"
      provides: "Phase 39 VALIDATION.md (newly created by skill in State B); frontmatter nyquist_compliant: true OR documented gap"
      contains: "nyquist_compliant:"
    - path: ".planning/phases/40-deps-update-+-cost-ledger-snapshot-workflows/40-VALIDATION.md"
      provides: "Phase 40 VALIDATION.md (newly created); stamped or gap-documented"
      contains: "nyquist_compliant:"
    - path: ".planning/phases/41-verifier-gate-workflow-+-verify-single-case-mjs-cli-shim/41-VALIDATION.md"
      provides: "Phase 41 VALIDATION.md (newly created); stamped or gap-documented"
      contains: "nyquist_compliant:"
    - path: ".planning/phases/42-fix-prompt-builder-+-wrong-citation-vertical-slice/42-VALIDATION.md"
      provides: "Phase 42 VALIDATION.md (newly created); stamped or gap-documented; UAT-42-demo Manual-Only honored per Pitfall 6"
      contains: "nyquist_compliant:"
    - path: ".planning/phases/43-v40-auto-fix-yml-workflow-+-draft-pr-creation/43-VALIDATION.md"
      provides: "Phase 43 VALIDATION.md (newly created); stamped or gap-documented; UAT-43-a Manual-Only honored"
      contains: "nyquist_compliant:"
    - path: ".planning/phases/44-v40-auto-promote-yml-+-triple-gate-skipciguard/44-VALIDATION.md"
      provides: "Phase 44 VALIDATION.md (newly created); stamped or gap-documented"
      contains: "nyquist_compliant:"
    - path: ".planning/phases/45-per-error-class-expansion-+-flake-5-state-machine/45-VALIDATION.md"
      provides: "Phase 45 VALIDATION.md (newly created); stamped or gap-documented; UAT-47-c Manual-Only honored"
      contains: "nyquist_compliant:"
    - path: ".planning/phases/46-gsd-fix-issue-local-ux-+-ledger-v2-dashboard/46-VALIDATION.md"
      provides: "Phase 46 VALIDATION.md (newly created); stamped or gap-documented"
      contains: "nyquist_compliant:"
    - path: ".planning/phases/47-v4-0-cleanup/47-02-SUMMARY.md"
      provides: "Per-phase outcome table + any new tech_debt surfaced + AskUserQuestion fallback notes if any"
  key_links:
    - from: "Skill invocation Skill(gsd-validate-phase, <N>)"
      to: "<phase>/PLAN.md + SUMMARY.md + VERIFICATION.md inputs"
      via: "State B (cold reconstruct) — no draft VALIDATION.md exists; skill builds per-task verification map from upstream inputs and stamps in one pass"
      pattern: "Skill\\(gsd-validate-phase"
    - from: "Per-phase outcome (GAPS_FILLED / PARTIAL / ESCALATE / MANUAL_ONLY_CLASSIFIED)"
      to: "47-02-SUMMARY.md per-phase outcome table"
      via: "executor running notes captured per Skill invocation, written to SUMMARY at Task 10"
      pattern: "GAPS_FILLED|PARTIAL|ESCALATE"
    - from: "Any newly-surfaced real gap"
      to: ".planning/v4.0-MILESTONE-AUDIT.md tech_debt: block (authored by Plan 47-04)"
      via: "running notes preserved across plans for 47-04 to consume"
      pattern: "tech_debt:"
---

<objective>
Drive all 8 v4.0 phases (39, 40, 41, 42, 43, 44, 45, 46) to formal Nyquist coverage via 8 explicit `Skill(gsd-validate-phase, "<N>")` invocations (cold-stamp State B — no draft VALIDATION.md exists for any phase per the handoff). Each invocation reconstructs the per-task verification map from PLAN.md + SUMMARY.md + VERIFICATION.md inputs and stamps `nyquist_compliant: true` if all auditable rows have automated commands. Outcomes are tabulated in 47-02-SUMMARY.md for downstream consumption by Plan 47-04 (v4.0-MILESTONE-AUDIT.md bootstrap).

Purpose: Close the v4.0 formal-coverage gap. Test suites are strong (1000+ vitest tests across the v4.0 phases per handoff); this plan stamps cold-state VALIDATION.md files to the standard established by Phase 38 Plan 02 (which cold-stamped 5 v3.1 phases) and Phase 36 (the one COMPLIANT v3.1 phase template).

Output: 8 newly-created `*-VALIDATION.md` files + 1 SUMMARY.md tabulating per-phase outcomes. Per CONTEXT.md locked decision: gaps documented inline; do NOT block Phase 47 — unresolved Nyquist gaps surface as new tech_debt for 47-04 to record in v4.0-MILESTONE-AUDIT.md.

Depends on Plan 47-01 because the auditor reads the post-INT-FIX-* test state; running Nyquist stamping after touchpoint tests + INT-FIX commits land ensures the auditor sees the clean state, not the pre-cleanup dirty state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/workflows/validate-phase.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/skills/gsd-validate-phase/SKILL.md
@$HOME/.claude/agents/gsd-nyquist-auditor.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-v4-0-cleanup/47-CONTEXT.md
@.planning/phases/47-v4-0-cleanup/47-RESEARCH.md
@.planning/phases/47-v4-0-cleanup/47-VALIDATION.md
@.planning/milestones/v3.1-phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-02-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Skill invocation contract + State B semantics for cold-stamp -->

Skill invocation (canonical form, per Phase 38 Plan 02 precedent):
  Skill(skill="gsd-validate-phase", args="<phase-number>")

Skill State semantics (per ~/.claude/get-shit-done/workflows/validate-phase.md):
  - State A (audit): VALIDATION.md exists with `status: draft` or `status: ready` — skill audits + fills gaps without overwriting existing rows
  - State B (cold reconstruct): VALIDATION.md does NOT exist OR is empty — skill reconstructs from PLAN.md + SUMMARY.md + VERIFICATION.md inputs and stamps in one pass
  - All 8 v4.0 phases (39-46) are at State B per handoff (zero draft VALIDATION.md files exist)
  - Sub-agent (gsd-nyquist-auditor) returns one of:
      * GAPS FILLED — all auditable rows have automated commands; stamps `nyquist_compliant: true`
      * PARTIAL — some rows have no command and no manual-only justification; surfaces to user for decision
      * ESCALATE — irrecoverable (test framework broken, conflicting decisions); surfaces to user

Per-phase phase-dir paths (verified via `ls .planning/phases/`):
  .planning/phases/39-sdk-driver-+-ledger-v2-+-branch-protection-wave-0/
  .planning/phases/40-deps-update-+-cost-ledger-snapshot-workflows/
  .planning/phases/41-verifier-gate-workflow-+-verify-single-case-mjs-cli-shim/
  .planning/phases/42-fix-prompt-builder-+-wrong-citation-vertical-slice/
  .planning/phases/43-v40-auto-fix-yml-workflow-+-draft-pr-creation/
  .planning/phases/44-v40-auto-promote-yml-+-triple-gate-skipciguard/
  .planning/phases/45-per-error-class-expansion-+-flake-5-state-machine/
  .planning/phases/46-gsd-fix-issue-local-ux-+-ledger-v2-dashboard/
  (At plan-execute time, run `ls .planning/phases/ | grep -E '^4[0-6]-|^39-'` to confirm exact directory names; the skill resolves the phase by number, not slug, so the slug variations are tolerable.)

Recommended invocation order (per 47-RESEARCH.md §"Nyquist Stamping Protocol" lines 661-669 — low-risk → high-risk → largest surface last):
  1. Phase 39 (SDK driver + ledger v2; foundational, ~70 tests, well-bounded)
  2. Phase 40 (deps-update + ledger snapshot; workflow conventions, ~75 tests)
  3. Phase 41 (verifier-gate 4-job workflow + CLI shim; ~60 tests)
  4. Phase 43 (v40-auto-fix.yml + PR-body builder; 28 tests, smallest surface)
  5. Phase 44 (v40-auto-promote.yml + triple-gate; ~30 tests, load-bearing invariant)
  6. Phase 45 (4 ERROR_CLASS scaffolds + 5-state classifier; ~56 tests)
  7. Phase 46 (npm run fix-issue + ledger v2 dashboard; ~70 tests)
  8. Phase 42 (fix-prompt-builder + auto-fix dispatcher; ~122 tests — LARGEST surface, save for last so prior stamps inform style)

Manual-Only entries to PRE-CLASSIFY (per 47-RESEARCH.md Pitfall 6 + 47-CONTEXT.md "UAT items DEFERRED requires-push"):
  - Phase 42: live end-to-end auto-fix demo on issue #3 (UAT-47-a) — DEFERRED requires-push; COVERED-MANUAL
  - Phase 43: live workflow-driven auto-fix demo — DEFERRED requires-push; COVERED-MANUAL
  - Phase 44: live merge → auto-promote flow — DEFERRED requires-push; COVERED-MANUAL
  - Phase 45: UAT-47-c FLAKE escalation — RUN-NOW LOCAL (executed in Plan 47-03, not here); COVERED-MANUAL for Phase 45's local UAT items
  - Phase 40: live dep-PR pre-flight gate (UAT-47-b) — DEFERRED requires-push; COVERED-MANUAL
  - Phase 46: live ledger snapshot commit (UAT-47-d) — DEFERRED requires-push; COVERED-MANUAL
  - Phase 41: live diff-guard reject (UAT-47-e) — DEFERRED requires-push; COVERED-MANUAL

CLAUDE.md C1/C2/C3 enforcement:
  If the auditor sub-agent emits an AskUserQuestion call and the tool result is empty/generic (does not name the user's selection), the task MUST fall back to a plain-text numbered list of options and wait for an explicit user response. Do NOT auto-pick the "(Recommended)" option or fabricate an answer.

Outcome tracking (passed to Task 10 → SUMMARY.md → consumed by Plan 47-04):
  Per phase, capture: skill_outcome ∈ {GAPS_FILLED, PARTIAL, ESCALATE, MANUAL_ONLY_CLASSIFIED}, nyquist_compliant value stamped (true/false), tests_referenced count (rounded), new_gap_one_line (or "none").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — confirm 8 v4.0 phase directories exist; confirm no VALIDATION.md drafts present (State B cold-stamp expected for all 8)</name>
  <files>(read-only)</files>
  <read_first>
    - .planning/phases/47-v4-0-cleanup/47-RESEARCH.md (§"Nyquist Stamping Protocol" lines 643-684; §"Pitfall 6: Nyquist auditor escalates on Manual-Only items" lines 1180-1184)
    - .planning/phases/47-v4-0-cleanup/47-CONTEXT.md (§"CLEANUP-02: Nyquist Coverage Stamping" lines 60-65 — cold-stamp approach locked; gap handling locked)
    - .planning/milestones/v3.1-phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-02-PLAN.md (Tasks 1-2 — verbatim template for pre-flight + per-phase skill invocation)
  </read_first>
  <acceptance_criteria>
    - `ls .planning/phases/ | grep -cE "^(39|40|41|42|43|44|45|46)-"` returns 8 (all 8 phase directories present)
    - `find .planning/phases -path "*/47-v4-0-cleanup" -prune -o -name "*-VALIDATION.md" -print 2>/dev/null | grep -E "/(39|40|41|42|43|44|45|46)-" | wc -l` returns 0 (zero draft VALIDATION.md files; all 8 phases are cold-stamp State B)
    - For each of the 8 phases, a one-line note is captured in the executor's working memory: directory path, PLAN.md count (≥ 1 confirms the skill has reconstructive input), SUMMARY.md count (≥ 1 confirms post-execution narrative exists), VERIFICATION.md presence (true/false — verifier may not have run yet for all phases)
    - Per Pitfall 6 — the Manual-Only list (Phase 42/43/44/45 live demos, Phase 40 dep-PR gate, Phase 41 diff-guard reject, Phase 46 ledger snapshot) is reproduced into the executor's notes so each Skill invocation can pre-classify those rows COVERED-MANUAL before the auditor sees them
  </acceptance_criteria>
  <action>
    Step 1: Confirm directory count. Run `ls .planning/phases/ | grep -E "^(39|40|41|42|43|44|45|46)-"`. Capture each directory name verbatim (the slugs may differ slightly from the frontmatter; the skill resolves by phase number so the slug variations are tolerable).
    Step 2: Confirm zero draft VALIDATION.md files exist. Run `find .planning/phases -path "*/47-v4-0-cleanup" -prune -o -name "*-VALIDATION.md" -print 2>/dev/null | grep -E "/(39|40|41|42|43|44|45|46)-"`. Expect zero lines of output. If non-zero, STOP and surface the discrepancy: the handoff said zero drafts; non-zero means out-of-band edits and the skill may enter State A instead of State B for those phases.
    Step 3: For each of the 8 phases, count input artifacts: `for n in 39 40 41 42 43 44 45 46; do d=$(ls -d .planning/phases/${n}-* 2>/dev/null | head -1); echo "$n: dir=$d, plans=$(ls $d/${n}-*-PLAN.md 2>/dev/null | wc -l), summaries=$(ls $d/${n}-*-SUMMARY.md 2>/dev/null | wc -l), verification=$(test -f $d/${n}-VERIFICATION.md && echo yes || echo no)"; done`. Capture in working notes.
    Step 4: Build the pre-classified Manual-Only list (from interfaces block above): record which row IDs in each phase's expected per-task map are COVERED-MANUAL (e.g., Phase 42 UAT-42-demo / Phase 45 FLAKE-02 live escalation row / etc.). This list is the input to Pitfall 6 mitigation in each subsequent task.
  </action>
  <verify>
    <automated>bash -c 'count=$(ls .planning/phases/ | grep -cE "^(39|40|41|42|43|44|45|46)-"); [ "$count" -eq 8 ] || { echo "expected 8 v4.0 phase dirs, found $count"; exit 1; }; drafts=$(find .planning/phases -path "*/47-v4-0-cleanup" -prune -o -name "*-VALIDATION.md" -print 2>/dev/null | grep -cE "/(39|40|41|42|43|44|45|46)-"); [ "$drafts" -eq 0 ] || { echo "expected 0 draft VALIDATION.md, found $drafts"; exit 1; }; echo "pre-flight OK: 8 v4.0 phase dirs, 0 drafts (cold-stamp State B for all)"'</automated>
  </verify>
  <done>All 8 v4.0 phase directories confirmed present; zero draft VALIDATION.md files (cold-stamp State B for all); per-phase input-artifact counts captured; Manual-Only pre-classification list assembled for Pitfall 6 mitigation in Tasks 2-9.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Invoke Skill(gsd-validate-phase, "39") — Phase 39 SDK Driver + Ledger v2 + Branch Protection Wave-0</name>
  <files>.planning/phases/39-sdk-driver-+-ledger-v2-+-branch-protection-wave-0/39-VALIDATION.md</files>
  <read_first>
    - .planning/phases/39-sdk-driver-+-ledger-v2-+-branch-protection-wave-0/ (full directory listing — confirm PLAN/SUMMARY artifacts present)
    - $HOME/.claude/skills/gsd-validate-phase/SKILL.md
    - $HOME/.claude/get-shit-done/workflows/validate-phase.md
    - $HOME/.claude/agents/gsd-nyquist-auditor.md
  </read_first>
  <what-built>Cold-stamp State B: 39-VALIDATION.md will be CREATED by the skill from PLAN + SUMMARY + VERIFICATION inputs (no draft exists). Phase 39 owns LEDGER-01..04 + CLEANUP-04 initial setup; ~70 tests in suite per RESEARCH.</what-built>
  <acceptance_criteria>
    - `ls .planning/phases/39-*/39-VALIDATION.md 2>/dev/null | head -1` outputs a non-empty path (file created by skill in State B cold-reconstruct)
    - `grep -E "^nyquist_compliant:\s*(true|false)" .planning/phases/39-*/39-VALIDATION.md` exits 0 (frontmatter field present with explicit true OR false value)
    - Executor's running notes record for Task 10 consumption: phase=39, skill_outcome ∈ {GAPS_FILLED|PARTIAL|ESCALATE|MANUAL_ONLY_CLASSIFIED}, nyquist_compliant value (true|false), tests_referenced count (rounded), one-line gap description (or literal "none")
    - If AskUserQuestion fired and tool result was empty per CLAUDE.md C1/C2/C3, the user's explicit numbered-list fallback response is captured in the notes (not auto-picked "(Recommended)")
    - If auditor escalated a CLEANUP-04 live `gh api` row, it was corrected in-loop with the note "CLEANUP-04 live audit OWNED BY Plan 47-04 — do not generate test"
  </acceptance_criteria>
  <action>
    Step 1: Invoke the skill — `Skill(skill="gsd-validate-phase", args="39")`. The skill reconstructs 39-VALIDATION.md from phase inputs, spawns gsd-nyquist-auditor sub-agent, returns one of: (a) GAPS FILLED — frontmatter `nyquist_compliant: true`, Validation Audit table appended; (b) PARTIAL / ESCALATE — surfaces a question OR gap report.
    Step 2: Per 47-RESEARCH.md Pitfall 6 — Phase 39 has no requires-push Manual-Only items (all CLEANUP-04 work is local audit). If the auditor escalates the live `gh api` audit row, correct in-loop: "CLEANUP-04 live audit is OWNED BY Plan 47-04 (Phase 47); do not generate a test for it here."
    Step 3: Per CLAUDE.md C1/C2/C3 — if the skill emits AskUserQuestion and the tool result is empty/generic, fall back to a plain-text numbered list of options and wait for explicit user response. Do NOT auto-select "(Recommended)" or fabricate.
    Step 4: After skill returns, record outcome for Task 10: skill_outcome ∈ {GAPS_FILLED | PARTIAL | ESCALATE | MANUAL_ONLY_CLASSIFIED}, nyquist_compliant value stamped, tests_referenced count, one-line gap description if not GAPS_FILLED.
    Step 5: Pause for user "approved" confirmation before proceeding to Task 3.
  </action>
  <verify>
    <automated>bash -c 'f=$(ls .planning/phases/39-*/39-VALIDATION.md 2>/dev/null | head -1); test -n "$f" || { echo "Phase 39 VALIDATION.md not created by skill"; exit 1; }; val=$(grep -E "^nyquist_compliant:" "$f" | head -1); echo "Phase 39 post-skill: $val"; echo "$val" | grep -qE "nyquist_compliant:\s*(true|false)" || { echo "frontmatter malformed"; exit 1; }'</automated>
  </verify>
  <resume-signal>Type "approved" once Skill(gsd-validate-phase, "39") has returned AND outcome (GAPS_FILLED / PARTIAL / ESCALATE / MANUAL_ONLY_CLASSIFIED) has been recorded for Task 10. If AskUserQuestion fired and result was empty, the numbered-list fallback response must be received before proceeding.</resume-signal>
  <done>Skill invocation for Phase 39 complete; 39-VALIDATION.md created with frontmatter reflecting outcome; per-phase outcome recorded in running notes for Task 10.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Invoke Skill(gsd-validate-phase, "40") — Phase 40 Deps-Update + Cost-Ledger-Snapshot Workflows</name>
  <files>.planning/phases/40-deps-update-+-cost-ledger-snapshot-workflows/40-VALIDATION.md</files>
  <read_first>
    - .planning/phases/40-deps-update-+-cost-ledger-snapshot-workflows/ (full directory listing)
    - $HOME/.claude/skills/gsd-validate-phase/SKILL.md
  </read_first>
  <what-built>Cold-stamp State B: 40-VALIDATION.md created by skill. Phase 40 owns DEPS-01..04; ~75 tests. UAT-47-b (live dep-PR pre-flight gate) is COVERED-MANUAL (requires-push, deferred to Plan 47-03 runbook stub).</what-built>
  <acceptance_criteria>
    - `ls .planning/phases/40-*/40-VALIDATION.md 2>/dev/null | head -1` outputs a non-empty path
    - `grep -E "^nyquist_compliant:\s*(true|false)" .planning/phases/40-*/40-VALIDATION.md` exits 0
    - Executor's notes record for Task 10: phase=40, skill_outcome, nyquist_compliant value, tests_referenced count, gap description (or "none")
    - UAT-47-b live dep-PR pre-flight gate row pre-classified COVERED-MANUAL per Pitfall 6; if auditor escalated, correction applied in-loop
    - AskUserQuestion fallback captured if fired (CLAUDE.md C1/C2/C3)
  </acceptance_criteria>
  <action>
    Step 1: Invoke `Skill(skill="gsd-validate-phase", args="40")`.
    Step 2: Per Pitfall 6 — if the auditor escalates a "live dep-PR" row, correct in-loop: "UAT-47-b is COVERED-MANUAL (requires-push DEFERRED) per Plan 47-03 runbook. Do not generate test."
    Step 3: Handle AskUserQuestion fallback per CLAUDE.md C1/C2/C3.
    Step 4: Verify outcome and record for Task 10.
    Step 5: Pause for user "approved".
  </action>
  <verify>
    <automated>bash -c 'f=$(ls .planning/phases/40-*/40-VALIDATION.md 2>/dev/null | head -1); test -n "$f" || { echo "Phase 40 VALIDATION.md not created"; exit 1; }; val=$(grep -E "^nyquist_compliant:" "$f" | head -1); echo "Phase 40 post-skill: $val"; echo "$val" | grep -qE "nyquist_compliant:\s*(true|false)" || { echo "frontmatter malformed"; exit 1; }'</automated>
  </verify>
  <resume-signal>Type "approved" once Skill(gsd-validate-phase, "40") has returned AND outcome recorded for Task 10.</resume-signal>
  <done>Skill invocation for Phase 40 complete; outcome recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Invoke Skill(gsd-validate-phase, "41") — Phase 41 Verifier-Gate Workflow + verify-single-case CLI shim</name>
  <files>.planning/phases/41-verifier-gate-workflow-+-verify-single-case-mjs-cli-shim/41-VALIDATION.md</files>
  <read_first>
    - .planning/phases/41-verifier-gate-workflow-+-verify-single-case-mjs-cli-shim/
    - $HOME/.claude/skills/gsd-validate-phase/SKILL.md
  </read_first>
  <what-built>Cold-stamp State B: 41-VALIDATION.md created by skill. Phase 41 owns VFY-GATE-01..04; ~60 tests. UAT-47-e (live diff-guard reject) is COVERED-MANUAL (requires-push DEFERRED).</what-built>
  <acceptance_criteria>
    - `ls .planning/phases/41-*/41-VALIDATION.md 2>/dev/null | head -1` outputs a non-empty path
    - `grep -E "^nyquist_compliant:\s*(true|false)" .planning/phases/41-*/41-VALIDATION.md` exits 0
    - Executor's notes record for Task 10: phase=41, skill_outcome, nyquist_compliant value, tests_referenced count, gap description (or "none")
    - UAT-47-e live diff-guard reject row pre-classified COVERED-MANUAL per Pitfall 6; if auditor escalated, correction applied in-loop
    - AskUserQuestion fallback captured if fired (CLAUDE.md C1/C2/C3)
  </acceptance_criteria>
  <action>
    Step 1: Invoke `Skill(skill="gsd-validate-phase", args="41")`.
    Step 2: Per Pitfall 6 — if auditor escalates the "live verifier-gate crafted bypass" row, correct in-loop: COVERED-MANUAL per UAT-47-e Plan 47-03 runbook.
    Step 3: Handle AskUserQuestion fallback per CLAUDE.md C1/C2/C3.
    Step 4: Verify outcome and record for Task 10.
    Step 5: Pause for user "approved".
  </action>
  <verify>
    <automated>bash -c 'f=$(ls .planning/phases/41-*/41-VALIDATION.md 2>/dev/null | head -1); test -n "$f" || { echo "Phase 41 VALIDATION.md not created"; exit 1; }; val=$(grep -E "^nyquist_compliant:" "$f" | head -1); echo "Phase 41 post-skill: $val"; echo "$val" | grep -qE "nyquist_compliant:\s*(true|false)" || { echo "frontmatter malformed"; exit 1; }'</automated>
  </verify>
  <resume-signal>Type "approved" once Skill(gsd-validate-phase, "41") has returned AND outcome recorded for Task 10.</resume-signal>
  <done>Skill invocation for Phase 41 complete; outcome recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Invoke Skill(gsd-validate-phase, "43") — Phase 43 v40-auto-fix.yml Workflow + Draft PR (smallest surface; run before 42)</name>
  <files>.planning/phases/43-v40-auto-fix-yml-workflow-+-draft-pr-creation/43-VALIDATION.md</files>
  <read_first>
    - .planning/phases/43-v40-auto-fix-yml-workflow-+-draft-pr-creation/
    - $HOME/.claude/skills/gsd-validate-phase/SKILL.md
  </read_first>
  <what-built>Cold-stamp State B: 43-VALIDATION.md created by skill. Phase 43 owns AUTOFIX-02; ~28 tests (smallest surface — run early per RESEARCH-recommended order). UAT-43-a (live workflow-driven auto-fix demo) is COVERED-MANUAL (requires-push DEFERRED to Plan 47-03 UAT-47-a runbook).</what-built>
  <acceptance_criteria>
    - `ls .planning/phases/43-*/43-VALIDATION.md 2>/dev/null | head -1` outputs a non-empty path
    - `grep -E "^nyquist_compliant:\s*(true|false)" .planning/phases/43-*/43-VALIDATION.md` exits 0
    - Executor's notes record for Task 10: phase=43, skill_outcome, nyquist_compliant value, tests_referenced count, gap description (or "none")
    - UAT-43-a live CI auto-fix demo row pre-classified COVERED-MANUAL per Pitfall 6 (inheriting Phase 42 UAT-47-a runbook); if auditor escalated, correction applied in-loop
    - AskUserQuestion fallback captured if fired (CLAUDE.md C1/C2/C3)
  </acceptance_criteria>
  <action>
    Step 1: Invoke `Skill(skill="gsd-validate-phase", args="43")`.
    Step 2: Per Pitfall 6 — UAT-43-a / live CI auto-fix demo on issue #3 is COVERED-MANUAL per Plan 47-03 runbook UAT-47-a (inheriting Phase 42's deferred demo). Correct any auditor escalation in-loop.
    Step 3: Handle AskUserQuestion fallback per CLAUDE.md C1/C2/C3.
    Step 4: Verify outcome and record for Task 10.
    Step 5: Pause for user "approved".
  </action>
  <verify>
    <automated>bash -c 'f=$(ls .planning/phases/43-*/43-VALIDATION.md 2>/dev/null | head -1); test -n "$f" || { echo "Phase 43 VALIDATION.md not created"; exit 1; }; val=$(grep -E "^nyquist_compliant:" "$f" | head -1); echo "Phase 43 post-skill: $val"; echo "$val" | grep -qE "nyquist_compliant:\s*(true|false)" || { echo "frontmatter malformed"; exit 1; }'</automated>
  </verify>
  <resume-signal>Type "approved" once Skill(gsd-validate-phase, "43") has returned AND outcome recorded for Task 10.</resume-signal>
  <done>Skill invocation for Phase 43 complete; outcome recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: Invoke Skill(gsd-validate-phase, "44") — Phase 44 v40-auto-promote.yml + Triple-Gate _skipCiGuard</name>
  <files>.planning/phases/44-v40-auto-promote-yml-+-triple-gate-skipciguard/44-VALIDATION.md</files>
  <read_first>
    - .planning/phases/44-v40-auto-promote-yml-+-triple-gate-skipciguard/
    - $HOME/.claude/skills/gsd-validate-phase/SKILL.md
  </read_first>
  <what-built>Cold-stamp State B: 44-VALIDATION.md created by skill. Phase 44 owns PROMOTE-01..04; ~30 tests; carries the load-bearing triple-gate trust invariant (tests/unit/auto-fix-promote-gate.test.js exists and was just re-pinned by Plan 47-01 TP-05). Live merge → auto-promote flow is COVERED-MANUAL (requires-push DEFERRED — no UAT-47 slot dedicated; embedded in UAT-47-a end-to-end run).</what-built>
  <acceptance_criteria>
    - `ls .planning/phases/44-*/44-VALIDATION.md 2>/dev/null | head -1` outputs a non-empty path
    - `grep -E "^nyquist_compliant:\s*(true|false)" .planning/phases/44-*/44-VALIDATION.md` exits 0
    - Executor's notes record for Task 10: phase=44, skill_outcome, nyquist_compliant value, tests_referenced count, gap description (or "none")
    - Live merge→promote flow row pre-classified COVERED-MANUAL (embedded in UAT-47-a per Pitfall 6); if auditor escalated, correction applied in-loop
    - AskUserQuestion fallback captured if fired (CLAUDE.md C1/C2/C3)
  </acceptance_criteria>
  <action>
    Step 1: Invoke `Skill(skill="gsd-validate-phase", args="44")`.
    Step 2: Per Pitfall 6 — live merge→promote flow is part of UAT-47-a (end-to-end auto-fix flow), DEFERRED requires-push. Correct any auditor escalation in-loop.
    Step 3: Handle AskUserQuestion fallback per CLAUDE.md C1/C2/C3.
    Step 4: Verify outcome and record for Task 10.
    Step 5: Pause for user "approved".
  </action>
  <verify>
    <automated>bash -c 'f=$(ls .planning/phases/44-*/44-VALIDATION.md 2>/dev/null | head -1); test -n "$f" || { echo "Phase 44 VALIDATION.md not created"; exit 1; }; val=$(grep -E "^nyquist_compliant:" "$f" | head -1); echo "Phase 44 post-skill: $val"; echo "$val" | grep -qE "nyquist_compliant:\s*(true|false)" || { echo "frontmatter malformed"; exit 1; }'</automated>
  </verify>
  <resume-signal>Type "approved" once Skill(gsd-validate-phase, "44") has returned AND outcome recorded for Task 10.</resume-signal>
  <done>Skill invocation for Phase 44 complete; outcome recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 7: Invoke Skill(gsd-validate-phase, "45") — Phase 45 Per-ERROR_CLASS Expansion + FLAKE 5-State Machine</name>
  <files>.planning/phases/45-per-error-class-expansion-+-flake-5-state-machine/45-VALIDATION.md</files>
  <read_first>
    - .planning/phases/45-per-error-class-expansion-+-flake-5-state-machine/
    - $HOME/.claude/skills/gsd-validate-phase/SKILL.md
  </read_first>
  <what-built>Cold-stamp State B: 45-VALIDATION.md created by skill. Phase 45 owns FLAKE-01..03 + PROMPT-03 extension; ~56 tests. UAT-47-c (FLAKE escalation suppressing re-files) is RUN-NOW LOCAL — but EXECUTED IN PLAN 47-03, not here. Mark as COVERED-MANUAL for Phase 45's local UAT row; Plan 47-03 owns the live execution.</what-built>
  <acceptance_criteria>
    - `ls .planning/phases/45-*/45-VALIDATION.md 2>/dev/null | head -1` outputs a non-empty path
    - `grep -E "^nyquist_compliant:\s*(true|false)" .planning/phases/45-*/45-VALIDATION.md` exits 0
    - Executor's notes record for Task 10: phase=45, skill_outcome, nyquist_compliant value, tests_referenced count, gap description (or "none")
    - UAT-47-c live classifier / quarantine-append row pre-classified COVERED-MANUAL (Plan 47-03 owns execution per Pitfall 6); if auditor escalated, correction applied in-loop
    - AskUserQuestion fallback captured if fired (CLAUDE.md C1/C2/C3)
  </acceptance_criteria>
  <action>
    Step 1: Invoke `Skill(skill="gsd-validate-phase", args="45")`.
    Step 2: Per Pitfall 6 — the FLAKE escalation live test is OWNED BY Plan 47-03 (UAT-47-c). If the auditor escalates a "live classifier" or "live quarantine-append" row, correct in-loop: "COVERED-MANUAL — Plan 47-03 owns the live UAT execution."
    Step 3: Handle AskUserQuestion fallback per CLAUDE.md C1/C2/C3.
    Step 4: Verify outcome and record for Task 10.
    Step 5: Pause for user "approved".
  </action>
  <verify>
    <automated>bash -c 'f=$(ls .planning/phases/45-*/45-VALIDATION.md 2>/dev/null | head -1); test -n "$f" || { echo "Phase 45 VALIDATION.md not created"; exit 1; }; val=$(grep -E "^nyquist_compliant:" "$f" | head -1); echo "Phase 45 post-skill: $val"; echo "$val" | grep -qE "nyquist_compliant:\s*(true|false)" || { echo "frontmatter malformed"; exit 1; }'</automated>
  </verify>
  <resume-signal>Type "approved" once Skill(gsd-validate-phase, "45") has returned AND outcome recorded for Task 10.</resume-signal>
  <done>Skill invocation for Phase 45 complete; outcome recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 8: Invoke Skill(gsd-validate-phase, "46") — Phase 46 /gsd:fix-issue Local UX + Ledger v2 Dashboard</name>
  <files>.planning/phases/46-gsd-fix-issue-local-ux-+-ledger-v2-dashboard/46-VALIDATION.md</files>
  <read_first>
    - .planning/phases/46-gsd-fix-issue-local-ux-+-ledger-v2-dashboard/
    - $HOME/.claude/skills/gsd-validate-phase/SKILL.md
  </read_first>
  <what-built>Cold-stamp State B: 46-VALIDATION.md created by skill. Phase 46 owns AUTOFIX-06; ~70 tests including ledger dashboard generator + privacy audit. UAT-47-d (live ledger snapshot workflow commit) is COVERED-MANUAL (requires-push DEFERRED to Plan 47-03 runbook stub).</what-built>
  <acceptance_criteria>
    - `ls .planning/phases/46-*/46-VALIDATION.md 2>/dev/null | head -1` outputs a non-empty path
    - `grep -E "^nyquist_compliant:\s*(true|false)" .planning/phases/46-*/46-VALIDATION.md` exits 0
    - Executor's notes record for Task 10: phase=46, skill_outcome, nyquist_compliant value, tests_referenced count, gap description (or "none")
    - UAT-47-d live ledger snapshot row pre-classified COVERED-MANUAL per Pitfall 6; if auditor escalated, correction applied in-loop
    - AskUserQuestion fallback captured if fired (CLAUDE.md C1/C2/C3)
  </acceptance_criteria>
  <action>
    Step 1: Invoke `Skill(skill="gsd-validate-phase", args="46")`.
    Step 2: Per Pitfall 6 — UAT-47-d live ledger snapshot is COVERED-MANUAL per Plan 47-03 runbook. Correct any escalation in-loop.
    Step 3: Handle AskUserQuestion fallback per CLAUDE.md C1/C2/C3.
    Step 4: Verify outcome and record for Task 10.
    Step 5: Pause for user "approved".
  </action>
  <verify>
    <automated>bash -c 'f=$(ls .planning/phases/46-*/46-VALIDATION.md 2>/dev/null | head -1); test -n "$f" || { echo "Phase 46 VALIDATION.md not created"; exit 1; }; val=$(grep -E "^nyquist_compliant:" "$f" | head -1); echo "Phase 46 post-skill: $val"; echo "$val" | grep -qE "nyquist_compliant:\s*(true|false)" || { echo "frontmatter malformed"; exit 1; }'</automated>
  </verify>
  <resume-signal>Type "approved" once Skill(gsd-validate-phase, "46") has returned AND outcome recorded for Task 10.</resume-signal>
  <done>Skill invocation for Phase 46 complete; outcome recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 9: Invoke Skill(gsd-validate-phase, "42") — Phase 42 fix-prompt-builder + WRONG_CITATION Vertical Slice (LARGEST surface; run LAST)</name>
  <files>.planning/phases/42-fix-prompt-builder-+-wrong-citation-vertical-slice/42-VALIDATION.md</files>
  <read_first>
    - .planning/phases/42-fix-prompt-builder-+-wrong-citation-vertical-slice/
    - $HOME/.claude/skills/gsd-validate-phase/SKILL.md
  </read_first>
  <what-built>Cold-stamp State B: 42-VALIDATION.md created by skill. Phase 42 owns PROMPT-01..04 + AUTOFIX-01/03/04/05; ~122 tests (largest surface — save for LAST per RESEARCH-recommended order so the prior 7 stamps inform style). UAT-47-a (Phase 42's deferred live end-to-end demo on issue #3 US11427642-spec-short-1 fingerprint 139f821b3bb1 branch auto-fix/3-139f821b) is COVERED-MANUAL (requires-push DEFERRED to Plan 47-03 runbook stub).</what-built>
  <acceptance_criteria>
    - `ls .planning/phases/42-*/42-VALIDATION.md 2>/dev/null | head -1` outputs a non-empty path
    - `grep -E "^nyquist_compliant:\s*(true|false)" .planning/phases/42-*/42-VALIDATION.md` exits 0
    - Executor's notes record for Task 10: phase=42, skill_outcome, nyquist_compliant value, tests_referenced count, gap description (or "none")
    - UAT-42-demo (issue #3 US11427642-spec-short-1 fp 139f821b3bb1) row pre-classified COVERED-MANUAL per Pitfall 6 (Phase 42 ROADMAP explicitly notes "demo deferred to Phase 47 CLEANUP-03 HUMAN-UAT (a)"); if auditor escalated, correction applied in-loop
    - AskUserQuestion fallback captured if fired (CLAUDE.md C1/C2/C3)
  </acceptance_criteria>
  <action>
    Step 1: Invoke `Skill(skill="gsd-validate-phase", args="42")`.
    Step 2: Per Pitfall 6 — UAT-42-demo (issue #3 live end-to-end) is COVERED-MANUAL per Plan 47-03 UAT-47-a runbook. Phase 42 ROADMAP explicitly notes "(demo deferred to Phase 47 CLEANUP-03 HUMAN-UAT (a))". Correct any auditor escalation in-loop.
    Step 3: Handle AskUserQuestion fallback per CLAUDE.md C1/C2/C3.
    Step 4: Verify outcome and record for Task 10.
    Step 5: Pause for user "approved".
  </action>
  <verify>
    <automated>bash -c 'f=$(ls .planning/phases/42-*/42-VALIDATION.md 2>/dev/null | head -1); test -n "$f" || { echo "Phase 42 VALIDATION.md not created"; exit 1; }; val=$(grep -E "^nyquist_compliant:" "$f" | head -1); echo "Phase 42 post-skill: $val"; echo "$val" | grep -qE "nyquist_compliant:\s*(true|false)" || { echo "frontmatter malformed"; exit 1; }'</automated>
  </verify>
  <resume-signal>Type "approved" once Skill(gsd-validate-phase, "42") has returned AND outcome recorded for Task 10.</resume-signal>
  <done>Skill invocation for Phase 42 complete; outcome recorded.</done>
</task>

<task type="auto">
  <name>Task 10: Write 47-02-SUMMARY.md with per-phase outcome table + new tech_debt list (consumed by Plan 47-04 audit bootstrap)</name>
  <files>.planning/phases/47-v4-0-cleanup/47-02-SUMMARY.md</files>
  <read_first>
    - All 8 phase VALIDATION.md files post-skill: `for d in .planning/phases/{39,40,41,42,43,44,45,46}-*; do echo "$d:"; grep -E "^nyquist_compliant:" "$d"/*-VALIDATION.md; done`
    - Executor's running notes captured per Tasks 2-9 (per-phase: skill_outcome, nyquist_compliant value, tests_referenced count, new_gap_one_line)
    - $HOME/.claude/get-shit-done/templates/summary.md (the standard SUMMARY template — adapt frontmatter + sections)
  </read_first>
  <acceptance_criteria>
    - File `.planning/phases/47-v4-0-cleanup/47-02-SUMMARY.md` exists with: frontmatter (phase: 47, plan: 02, status: done), top-level heading `# Phase 47 Plan 02 — Bulk Nyquist Stamping (8 phases) — SUMMARY`, sections in this order: `## Per-Phase Outcome Table` / `## New Tech-Debt for v4.0-MILESTONE-AUDIT.md` / `## Manual-Only Classifications Applied` / `## AskUserQuestion Fallbacks (CLAUDE.md C1/C2/C3)`
    - Per-Phase Outcome Table has 8 rows (one per phase 39, 40, 41, 42, 43, 44, 45, 46) with columns: `Phase | Skill outcome | nyquist_compliant | tests_referenced | New gap surfaced`
    - For each phase whose `nyquist_compliant` is `true`, the row shows `GAPS_FILLED | true | <N> | none`
    - For any phase whose `nyquist_compliant` is `false`, the row shows the gap description and an entry exists in `## New Tech-Debt for v4.0-MILESTONE-AUDIT.md`
    - `## Manual-Only Classifications Applied` lists each row that was pre-classified COVERED-MANUAL (per Pitfall 6) and which UAT-47-* slot it maps to in Plan 47-03 runbooks
    - `## AskUserQuestion Fallbacks` records any task where the auditor's interactive gate misfired and the numbered-list fallback was used per CLAUDE.md C1/C2/C3 (empty section if none fired)
    - Commit message: `docs(47-02): bulk Nyquist stamping outcomes (<N> of 8 v4.0 phases COMPLIANT)` substituting actual count
  </acceptance_criteria>
  <action>
    Step 1: Re-read the actual post-skill values: `for n in 39 40 41 42 43 44 45 46; do d=$(ls -d .planning/phases/${n}-* 2>/dev/null | head -1); val=$(grep -E "^nyquist_compliant:" "$d"/${n}-VALIDATION.md 2>/dev/null | head -1); printf "%s: %s\n" "Phase $n" "$val"; done`. Compile the actual count of COMPLIANT (true) vs PARTIAL (false).
    Step 2: Use the Write tool to create `.planning/phases/47-v4-0-cleanup/47-02-SUMMARY.md` with the structure mandated by acceptance criteria. Top of file is YAML frontmatter:
      `---`
      `phase: 47`
      `plan: 02`
      `status: done`
      `created: <current ISO timestamp>`
      `nyquist_compliant_count: <N>`
      `partial_count: <8-N>`
      `---`
      Then markdown body with the 4 sections above. The Per-Phase Outcome Table uses the values captured per Task. The New Tech-Debt section enumerates any PARTIAL phase with a single-line description for Plan 47-04 to copy into v4.0-MILESTONE-AUDIT.md `tech_debt:` block. The Manual-Only Classifications section reproduces the pre-classification list from Task 1 with the actual auditor outcome (did the auditor escalate? was it corrected in-loop?). The AskUserQuestion Fallbacks section captures any CLAUDE.md C1/C2/C3 fallback events.
    Step 3: Commit: stage exactly `.planning/phases/47-v4-0-cleanup/47-02-SUMMARY.md`. Commit message: `docs(47-02): bulk Nyquist stamping outcomes (<N> of 8 v4.0 phases COMPLIANT)` substituting actual N.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/phases/47-v4-0-cleanup/47-02-SUMMARY.md; test -f "$f" || { echo "47-02-SUMMARY.md missing"; exit 1; }; rows=$(grep -cE "^\| Phase (39|40|41|42|43|44|45|46) " "$f"); [ "$rows" -eq 8 ] || { echo "expected 8 phase rows, found $rows"; exit 1; }; sections=$(grep -cE "^## (Per-Phase Outcome Table|New Tech-Debt|Manual-Only Classifications|AskUserQuestion Fallbacks)" "$f"); [ "$sections" -eq 4 ] || { echo "expected 4 sections, found $sections"; exit 1; }; echo "47-02-SUMMARY.md coherent: 8 phase rows, 4 sections"'</automated>
  </verify>
  <done>47-02-SUMMARY.md created with per-phase outcome table (8 rows), new tech_debt list (for Plan 47-04 to consume), Manual-Only classifications applied per Pitfall 6, and AskUserQuestion fallback events recorded per CLAUDE.md C1/C2/C3; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill invocation (gsd-validate-phase) ↔ State B reconstruct | Skill reads PLAN.md + SUMMARY.md + VERIFICATION.md inputs and CREATES a new VALIDATION.md from scratch; sub-agent (gsd-nyquist-auditor) may generate new test files. All edits in local working copy under `.planning/` and `tests/` — no network surface. |
| AskUserQuestion interactive gate | Per CLAUDE.md C1/C2/C3, tool result MUST contain the user's explicit choice. Empty results trigger numbered-list fallback; fabricating an answer violates the project constraint. |
| Manual-Only pre-classification → auditor escalation | Pitfall 6: phases 42/43/44/45/40/41/46 each have requires-push UAT rows; the auditor's gap classifier may flag them as ESCALATE. Pre-classification must be applied in-loop to prevent false-COMPLIANT stamps OR auto-escalation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-02-01 | Tampering | Skill creating a VALIDATION.md that misclassifies Manual-Only UAT rows as actionable gaps → false PARTIAL stamp | mitigate | Per Pitfall 6 + 47-CONTEXT.md, each task (2-9) reproduces the per-phase Manual-Only list before Skill invocation; if auditor escalates a pre-classified row, executor corrects in-loop ("COVERED-MANUAL per Plan 47-03 runbook UAT-47-X"). No false GAPS_FILLED; no false ESCALATE. |
| T-47-02-02 | Tampering | CLAUDE.md C1 violation: auto-picking AskUserQuestion answer for cold-stamp State B questions | mitigate | Every checkpoint:human-action task references CLAUDE.md C1/C2/C3 and requires the fallback numbered-list + explicit user response if tool result is empty. Task 10 records every fallback event in the SUMMARY's AskUserQuestion section. |
| T-47-02-03 | Tampering | Phase whose draft VALIDATION.md gets silently overwritten when it shouldn't | accept | Per pre-flight (Task 1), zero draft VALIDATION.md files exist for the 8 v4.0 phases — all 8 are unambiguously State B (cold). If pre-flight discovers a draft, Task 1 STOPS and surfaces; the skill cannot overwrite without entering State A. |
| T-47-02-SC | Tampering | npm/pip/cargo installs | accept | Phase 47 installs ZERO new packages. Skill MAY trigger gsd-nyquist-auditor to generate new test files within `tests/unit/` or `tests/e2e/` but generated tests use existing vitest/Playwright infrastructure — no new dependency. |
</threat_model>

<verification>
After all 10 tasks complete:
1. Each of the 8 v4.0 phases has a VALIDATION.md file: `for n in 39 40 41 42 43 44 45 46; do test -f .planning/phases/${n}-*/${n}-VALIDATION.md || echo "MISSING: $n"; done` produces zero MISSING lines.
2. Each VALIDATION.md has `nyquist_compliant:` set to either `true` or `false` (no missing frontmatter field): `grep -lE "^nyquist_compliant:\s*(true|false)" .planning/phases/{39,40,41,42,43,44,45,46}-*/*-VALIDATION.md | wc -l` returns 8.
3. 47-02-SUMMARY.md has 8 phase rows + 4 required sections (Task 10 verify).
4. `npm run test:src` exits 0 — any generated tests are green; nothing broke.
5. Plan 47-01's commits (TP-bundle, INT-FIX-LEDGER, INT-FIX-CAL, INT-FIX-LOCK) are still in git log (no regression).
</verification>

<success_criteria>
- 8 Skill invocations executed (one per v4.0 phase 39, 40, 41, 42, 43, 44, 45, 46) with outcome recorded per phase
- Each phase that returned GAPS_FILLED has its VALIDATION.md `nyquist_compliant: true`; each phase that returned PARTIAL/ESCALATE on a real gap has its gap documented as new tech_debt for Plan 47-04 consumption
- 47-02-SUMMARY.md per-phase outcome table reflects actual post-skill state (8 rows)
- Manual-Only rows (requires-push UATs across phases 40, 41, 42, 43, 44, 45, 46) pre-classified COVERED-MANUAL per Pitfall 6 — none escalated as real gaps
- AskUserQuestion fallback events captured per CLAUDE.md C1/C2/C3 (or "none" if no interactive gate fired)
- Committed to repo as `docs(47-02): bulk Nyquist stamping outcomes (<N> of 8 v4.0 phases COMPLIANT)`
</success_criteria>

<output>
Create `.planning/phases/47-v4-0-cleanup/47-02-SUMMARY.md` per the standard template (Task 10 already authors it as part of the plan). Verification: file exists with 8 phase rows + 4 required sections + frontmatter nyquist_compliant_count + partial_count.
</output>
