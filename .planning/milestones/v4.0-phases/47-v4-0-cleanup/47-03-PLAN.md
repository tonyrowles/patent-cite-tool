---
phase: 47
plan: 03
type: execute
wave: 4
depends_on:
  - 47-02
files_modified:
  - .planning/phases/47-v4-0-cleanup/47-UAT-EVIDENCE.md
  - .planning/phases/47-v4-0-cleanup/47-UAT-DEFERRED.md
  - tests/unit/uat-deferred-runbook.test.js
autonomous: false
requirements:
  - CLEANUP-03
user_setup:
  - service: github
    why: "UAT-47-c FLAKE escalation may attempt to open a flake-investigation issue via `gh issue create` (best-effort, non-blocking per scripts/auto-fix.mjs:325-329 — failure is logged but does not block). Authenticated `gh` CLI required."
    env_vars: []
    dashboard_config:
      - task: "Confirm `gh auth status` exits 0 BEFORE Task 2 invokes the FLAKE escalation runner — the runner may emit a `gh issue create flake-investigation` call best-effort"
        location: "Local shell"
tags:
  - v4.0
  - cleanup
  - human-uat
  - flake-escalation
must_haves:
  truths:
    - "UAT-47-c (FLAKE escalation suppressing re-files) has been executed locally per the 47-RESEARCH.md §UAT-47-c runbook strategy — outcome is PASS or FAIL recorded in 47-UAT-EVIDENCE.md with an ISO timestamp + executor command line + last ~20 lines of /tmp/uat-47c.log"
    - "47-UAT-DEFERRED.md exists and contains exactly 4 runbook stubs (UAT-47-a, UAT-47-b, UAT-47-d, UAT-47-e) — each stub has 4 required fields: dispatch command, expected outcome, success heuristic, rollback (per 47-CONTEXT.md locked decision)"
    - "A new vitest test tests/unit/uat-deferred-runbook.test.js asserts the 4 stubs are present in 47-UAT-DEFERRED.md and each stub has all 4 required field headers — protects against accidental stub deletion or field rename"
    - "Phase 47 does NOT block on UAT-47-c FAILURE per CONTEXT.md locked decision — on FAIL, a follow-up GitHub issue is filed via `gh issue create` (best-effort) and the failure becomes new tech_debt for v4.1; the plan continues to Task 3 (runbook stub authoring)"
    - "47-UAT-EVIDENCE.md records UAT-47-c outcome (the only locally-executable UAT) AND adds DEFERRED status references to the 4 deferred items (pointing at 47-UAT-DEFERRED.md sections); the audit YAML `human_verification:` block in Plan 47-04's v4.0-MILESTONE-AUDIT.md consumes both files"
  artifacts:
    - path: ".planning/phases/47-v4-0-cleanup/47-UAT-EVIDENCE.md"
      provides: "UAT-47-c PASS/FAIL evidence + DEFERRED references to the 4 deferred items"
      min_lines: 40
      contains: "UAT-47-c"
    - path: ".planning/phases/47-v4-0-cleanup/47-UAT-DEFERRED.md"
      provides: "4 deferred-UAT runbook stubs (UAT-47-a, UAT-47-b, UAT-47-d, UAT-47-e); each with dispatch command, expected outcome, success heuristic, rollback"
      min_lines: 80
      contains: "UAT-47-a"
    - path: "tests/unit/uat-deferred-runbook.test.js"
      provides: "Vitest static-grep test: asserts 4 stub presence + 4 required field headers per stub in 47-UAT-DEFERRED.md"
      min_lines: 30
      contains: "47-UAT-DEFERRED.md"
  key_links:
    - from: "Task 2 UAT-47-c execution"
      to: "47-UAT-EVIDENCE.md ## UAT-47-c section"
      via: "append-only markdown write with ISO timestamp + log excerpt"
      pattern: "## UAT-47-c"
    - from: "Task 3 47-UAT-DEFERRED.md authoring"
      to: "tests/unit/uat-deferred-runbook.test.js assertions"
      via: "static-grep against the deferred doc's section headers and field labels"
      pattern: "UAT-47-(a|b|d|e)"
    - from: "47-UAT-EVIDENCE.md + 47-UAT-DEFERRED.md"
      to: ".planning/v4.0-MILESTONE-AUDIT.md human_verification: block (authored by Plan 47-04)"
      via: "Plan 47-04 reads both docs to populate 5 human_verification entries"
      pattern: "human_verification:"
---

<objective>
Execute the single locally-runnable HUMAN-UAT (UAT-47-c FLAKE escalation) per 47-RESEARCH.md runbook, then author runbook stubs for the 4 DEFERRED requires-push UATs (UAT-47-a, UAT-47-b, UAT-47-d, UAT-47-e) so the post-push operator can dispatch them without re-discovering setup. Add a vitest static-grep test pinning stub presence + required-field structure in `47-UAT-DEFERRED.md`. Outcomes feed Plan 47-04's v4.0-MILESTONE-AUDIT.md `human_verification:` block (5 items total — 1 PASS/FAIL + 4 DEFERRED).

Purpose: Close CLEANUP-03 locally. Per CONTEXT.md locked decision, 4 of 5 UATs require pushed v4.0 workflows (no `origin` deploy yet); only UAT-47-c can be exercised today against local fixtures + the FLAKE 5-state classifier shipped in Phase 45.

Output: 47-UAT-EVIDENCE.md (UAT-47-c outcome) + 47-UAT-DEFERRED.md (4 runbook stubs verbatim from 47-RESEARCH.md UAT-47-{a,b,d,e}) + tests/unit/uat-deferred-runbook.test.js (stub-presence guard). 3 commits in order: UAT-47-c evidence → runbook-stub doc → runbook-stub vitest.

Depends on Plan 47-02 because the cold-stamped VALIDATION.md files surfaced any nyquist tech-debt that's relevant to UAT classification; running UAT-47-c after Nyquist stamping ensures the test suite is in known-clean state when the live run executes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-v4-0-cleanup/47-CONTEXT.md
@.planning/phases/47-v4-0-cleanup/47-RESEARCH.md
@.planning/phases/47-v4-0-cleanup/47-VALIDATION.md
@.planning/milestones/v3.1-phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-03-PLAN.md
@CLAUDE.md

<interfaces>
<!-- UAT-47-c execution surface + DEFERRED runbook authoritative source -->

UAT-47-c FLAKE escalation — RUN-NOW LOCAL (per 47-RESEARCH.md §"UAT-47-c" lines 750-797, Strategy A recommended):
  Dispatch (Strategy A — synthetic suppressions/ring-buffer fixture):
    mkdir -p /tmp/uat-47c
    cat > /tmp/uat-47c/suppressions.json <<EOF
    {"suppressions":{}}
    EOF
    cat > /tmp/uat-47c/ring-buffer.json <<EOF
    {"cases":{"synthetic-flake-case":{"outcomes":[
      {"verdict":"FLAKE","at":"2026-05-20T00:00:00Z","fingerprint":"aabbccdd1122"},
      {"verdict":"FLAKE","at":"2026-05-25T00:00:00Z","fingerprint":"aabbccdd1122"},
      {"verdict":"FLAKE","at":"2026-05-30T00:00:00Z","fingerprint":"aabbccdd1122"}
    ],"flakeHistory":[
      "2026-05-20T00:00:00Z","2026-05-25T00:00:00Z","2026-05-30T00:00:00Z"
    ]}}}
    EOF
    node scripts/quarantine-append.mjs --escalate-stable-runs-reset 1 --case synthetic-flake-case 2>&1 | tee /tmp/uat-47c.log
    echo "EXIT=$?" | tee -a /tmp/uat-47c.log

  Expected outcome (per FLAKE-02 contract from Phase 45):
    - quarantine-append.mjs exits 0 (the --escalate-stable-runs-reset path is a stable-runs counter reset, not a classifier dispatch)
    - log contains "[quarantine-append]" lines confirming the reset for case 'synthetic-flake-case'
    - if the classifier path is wired (via dispatchFlakeState at scripts/auto-fix.mjs:252), expect "FLAKE_ESCALATION" decision in stderr (best-effort `gh label create flake-investigation` may follow)
    - Strategy B fallback (if Strategy A produces no classifier-side evidence): write a small driver that calls classifyRerunOutcomes from tests/e2e/lib/triage-classifier.js directly with the 3-FLAKE-in-14d ring-buffer state — expected return: state=FLAKE_ESCALATION

  Success heuristic:
    - tail -10 /tmp/uat-47c.log contains "[quarantine-append]" success line for the case reset OR "FLAKE_ESCALATION" decision
    - subsequent invocation with same fingerprint within 30 days returns FLAKE_SUPPRESSED (this requires writing the suppression file first — Strategy A above seeds an empty suppressions.json; suppression-takes-effect verification is optional for UAT scope)

  Failure handling (per CONTEXT.md locked decision):
    - If UAT-47-c FAILS, capture full stdout/stderr in 47-UAT-EVIDENCE.md; open follow-up GitHub issue via `gh issue create --title "UAT-47-c FAIL: FLAKE escalation did not trip" --body "<paste log>" --label "v4.0-uat-fail"`; the plan continues — does NOT block Phase 47 close
    - Failure becomes new tech_debt entry in v4.0-MILESTONE-AUDIT.md (authored by Plan 47-04)
    - Per CLAUDE.md C1/C2/C3: if Strategy A produces ambiguous evidence and the executor needs to choose Strategy B, surface the decision via numbered-list fallback — do NOT auto-pick

4 DEFERRED runbook stubs (verbatim from 47-RESEARCH.md §"HUMAN-UAT Runbook Stubs"):
  UAT-47-a: end-to-end auto-fix flow on issue #3 US11427642-spec-short-1 fp 139f821b3bb1 branch auto-fix/3-139f821b (lines 692-720)
  UAT-47-b: dep-PR pre-flight gate blocking on regression (lines 723-746)
  UAT-47-d: ledger snapshot workflow commit (lines 801-823)
  UAT-47-e: verifier-gate diff-guard rejecting crafted bypass (lines 827-857)

Each stub structure (mandatory 4 fields per CONTEXT.md):
  ## UAT-47-<id> — <one-line summary>
  **Status:** DEFERRED (requires-push)
  ### Dispatch command (post-push)
    ```bash
    <verbatim commands from 47-RESEARCH.md>
    ```
  ### Expected outcome
    <bullet list>
  ### Success heuristic
    <bullet list with specific commands like `gh pr checks <pr-n>`>
  ### Rollback
    <commands to undo the test>

Vitest static-grep test contract:
  - File reads .planning/phases/47-v4-0-cleanup/47-UAT-DEFERRED.md as text
  - Asserts each of {UAT-47-a, UAT-47-b, UAT-47-d, UAT-47-e} appears as an `## ` section heading
  - Asserts each section contains all 4 required field headers (`### Dispatch command`, `### Expected outcome`, `### Success heuristic`, `### Rollback`)
  - Total 4 + (4×4) = 20 assertions minimum
  - Pattern: file-as-text grep with section-window scoping (find startIdx of `## UAT-47-<id>`, slice until next `## UAT-` or EOF, assert window contains each required header)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap 47-UAT-EVIDENCE.md skeleton (5 sections: UAT-47-c PENDING + 4 DEFERRED references); confirm gh auth fresh</name>
  <files>.planning/phases/47-v4-0-cleanup/47-UAT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/47-v4-0-cleanup/47-CONTEXT.md (§"CLEANUP-03: Human-UAT Execution" lines 67-71 — locked decisions: 1 locally-runnable + 4 deferred; result recording shape; failure handling)
    - .planning/phases/47-v4-0-cleanup/47-RESEARCH.md (§"Summary of UAT status" table at lines 860-869; §"HUMAN-UAT Runbook Stubs" sections lines 686-857)
    - .planning/milestones/v3.1-phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-03-PLAN.md (Task 1 — verbatim template for bootstrap + gh-auth-fresh pre-check)
  </read_first>
  <acceptance_criteria>
    - File `.planning/phases/47-v4-0-cleanup/47-UAT-EVIDENCE.md` exists with a top-level title + 5 section headers (`## UAT-47-c`, `## UAT-47-a (DEFERRED)`, `## UAT-47-b (DEFERRED)`, `## UAT-47-d (DEFERRED)`, `## UAT-47-e (DEFERRED)`) — UAT-47-c marked `**status:** pending`; the 4 deferred sections marked `**status:** DEFERRED — requires-push` and pointing at the corresponding section of 47-UAT-DEFERRED.md (to be authored in Task 3)
    - `gh auth status` exits 0 (token fresh — UAT-47-c may best-effort invoke `gh issue create flake-investigation`)
    - Phase 47-01 commits are present in git log: `git log --oneline -20 | grep -cE "(test|fix)\(47-cleanup\):"` returns at least 4 (4 commits from Plan 47-01: TP-bundle + INT-FIX-LEDGER + INT-FIX-CAL + INT-FIX-LOCK)
    - Phase 47-02 commit is present: `git log --oneline -20 | grep -cE "docs\(47-02\):"` returns at least 1
  </acceptance_criteria>
  <action>
    Step 1: Verify Plan 47-01 commits present: `git log --oneline -20 | grep -cE "(test|fix)\(47-cleanup\):"` — expect ≥ 4. If fewer, STOP and report missing INT-FIX-NN commits; Plan 47-03 cannot proceed (Plan 47-02 SUMMARY assumed a green test suite from 47-01).
    Step 2: Verify Plan 47-02 commit present: `git log --oneline -20 | grep -cE "docs\(47-02\):"` — expect ≥ 1.
    Step 3: Verify gh auth: `gh auth status 2>&1`. If exit non-zero, STOP and prompt user per CLAUDE.md C1: "gh auth required for UAT-47-c best-effort `gh issue create flake-investigation`; run `gh auth login`." Do NOT proceed without explicit user instruction.
    Step 4: Use the Write tool to create `.planning/phases/47-v4-0-cleanup/47-UAT-EVIDENCE.md` with this skeleton structure (5 sections; preserve exact heading text):
      Top title: `# Phase 47 — Human-UAT Live Confirmations Evidence`
      Subtitle line: `**Plan:** 47-03`
      Subtitle line: `**Created:** <current ISO timestamp>`
      Subtitle line: `**Depends-on:** Plan 47-01 commits + Plan 47-02 SUMMARY (verified present)`
      Horizontal rule.
      Section 1 heading: `## UAT-47-c — FLAKE escalation suppresses re-files (N=3 in 14 days; 30-day cooldown)` — body: `**status:** pending` / `**requirement:** CLEANUP-03 (c)` / `**audit human_verification entry:** phase 47, "FLAKE escalation suppresses re-files"` / `_(evidence captured in Task 2)_`
      Section 2 heading: `## UAT-47-a (DEFERRED) — End-to-end auto-fix flow against real triage-labeled fork issue` — body: `**status:** DEFERRED — requires-push` / `**runbook:** .planning/phases/47-v4-0-cleanup/47-UAT-DEFERRED.md §UAT-47-a` / `**inherits:** Phase 42 deferred demo on issue #3 (US11427642-spec-short-1, fp 139f821b3bb1, branch auto-fix/3-139f821b)`
      Section 3 heading: `## UAT-47-b (DEFERRED) — Dep-PR pre-flight gate blocking on regression` — body: `**status:** DEFERRED — requires-push` / `**runbook:** .planning/phases/47-v4-0-cleanup/47-UAT-DEFERRED.md §UAT-47-b`
      Section 4 heading: `## UAT-47-d (DEFERRED) — Ledger snapshot workflow committing daily snapshot` — body: `**status:** DEFERRED — requires-push` / `**runbook:** .planning/phases/47-v4-0-cleanup/47-UAT-DEFERRED.md §UAT-47-d`
      Section 5 heading: `## UAT-47-e (DEFERRED) — Verifier-gate diff-guard rejecting crafted bypass` — body: `**status:** DEFERRED — requires-push` / `**runbook:** .planning/phases/47-v4-0-cleanup/47-UAT-DEFERRED.md §UAT-47-e`
    Step 5: Commit: stage `.planning/phases/47-v4-0-cleanup/47-UAT-EVIDENCE.md`. Commit message: `docs(47-03): bootstrap UAT-EVIDENCE skeleton (1 live + 4 DEFERRED references)`.
  </action>
  <verify>
    <automated>bash -c 'set -e; f=.planning/phases/47-v4-0-cleanup/47-UAT-EVIDENCE.md; test -f "$f"; sec=$(grep -cE "^## UAT-47-[abcde]" "$f"); [ "$sec" -eq 5 ] || { echo "expected 5 UAT sections, found $sec"; exit 1; }; gh auth status >/dev/null 2>&1 || { echo "gh auth not fresh"; exit 1; }; p01=$(git log --oneline -20 | grep -cE "(test|fix)\(47-cleanup\):"); [ "$p01" -ge 4 ] || { echo "expected ≥4 Plan 47-01 commits, found $p01"; exit 1; }; p02=$(git log --oneline -20 | grep -cE "docs\(47-02\):"); [ "$p02" -ge 1 ] || { echo "expected ≥1 Plan 47-02 commit, found $p02"; exit 1; }; echo "bootstrap OK: 5 sections, gh auth fresh, Plan 47-01 + 47-02 commits present"'</automated>
  </verify>
  <done>47-UAT-EVIDENCE.md created with 5 sections (1 live UAT-47-c PENDING + 4 DEFERRED references pointing to forthcoming 47-UAT-DEFERRED.md); gh auth fresh; Plan 47-01 and 47-02 commits confirmed; committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT-47-c — Run `scripts/quarantine-append.mjs --escalate-stable-runs-reset 1 --case synthetic-flake-case` against Strategy A synthetic fixture → expect FLAKE escalation behaviour per FLAKE-02 contract</name>
  <files>.planning/phases/47-v4-0-cleanup/47-UAT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/47-v4-0-cleanup/47-RESEARCH.md (§"UAT-47-c" lines 750-797 — Strategy A synthetic fixture + Strategy B test-driver alternative; §"Pitfall 8: Live UAT (c) FLAKE classifier requires fingerprint stability" lines 1191-1195)
    - scripts/quarantine-append.mjs (full file — confirm --escalate-stable-runs-reset 1 + --case <id> CLI surface at L57-130; understand what the script writes vs reads)
    - scripts/auto-fix.mjs (lines 222-340 — dispatchFlakeState helper at L252; understand whether quarantine-append alone trips FLAKE_ESCALATION or whether dispatchFlakeState must be exercised separately)
    - tests/e2e/lib/triage-classifier.js (classifyRerunOutcomes export + 5-state machine; Strategy B fallback path)
    - 47-UAT-EVIDENCE.md (current state — UAT-47-c section is `**status:** pending`)
  </read_first>
  <what-built>Phase 45 Plan 02 added classifyRerunOutcomes (5-state machine) + ring-buffer + suppressions wiring; Plan 45-03 added dispatchFlakeState dispatching at scripts/auto-fix.mjs:252 and the --escalate-stable-runs-reset 1 --case <id> flag at scripts/quarantine-append.mjs. UAT-47-c confirms either: (a) the quarantine-append reset call exits 0 (mechanism verification — required); OR (b) Strategy B exercises classifyRerunOutcomes directly with a 3-FLAKE-in-14d ring buffer → returns state=FLAKE_ESCALATION (behaviour verification — preferred if Strategy A is ambiguous).</what-built>
  <acceptance_criteria>
    - `/tmp/uat-47c.log` exists and contains a line matching `^EXIT=` (UAT execution captured exit code)
    - In .planning/phases/47-v4-0-cleanup/47-UAT-EVIDENCE.md, the `## UAT-47-c` section's `**status:**` line is PASS or FAIL (not `pending`)
    - Section also contains `**verified_at:**`, `**strategy_used:**` (A, A+B, or B), `**command:**`, `**exit_code:**`, `**fingerprint_used:** aabbccdd1122`, and a fenced log block with the last ~20 lines of /tmp/uat-47c.log
    - On FAIL: `**followup_issue_url:**` field populated with the gh issue URL filed via `gh issue create --label v4.0-uat-fail`
    - Per Pitfall 8: all synthetic ring-buffer outcomes used fingerprint `aabbccdd1122` (per-fingerprint stability invariant maintained)
    - Per CLAUDE.md C1/C2/C3: if Strategy A was ambiguous and Strategy B was invoked via numbered-list fallback, the user's explicit choice is recorded (not auto-picked)
    - `git log --oneline -1` shows commit subject matching `^[0-9a-f]+ docs\(47-03\): UAT-47-c (PASS|FAIL) — FLAKE escalation`
  </acceptance_criteria>
  <action>
    Step 1: Strategy A setup (the default per 47-RESEARCH.md). Create synthetic fixture directory + files (the exact commands per 47-RESEARCH.md §UAT-47-c):
      ```
      mkdir -p /tmp/uat-47c
      cat > /tmp/uat-47c/suppressions.json <<'EOF'
      {"suppressions":{}}
      EOF
      cat > /tmp/uat-47c/ring-buffer.json <<'EOF'
      {"cases":{"synthetic-flake-case":{"outcomes":[
        {"verdict":"FLAKE","at":"2026-05-20T00:00:00Z","fingerprint":"aabbccdd1122"},
        {"verdict":"FLAKE","at":"2026-05-25T00:00:00Z","fingerprint":"aabbccdd1122"},
        {"verdict":"FLAKE","at":"2026-05-30T00:00:00Z","fingerprint":"aabbccdd1122"}
      ],"flakeHistory":[
        "2026-05-20T00:00:00Z","2026-05-25T00:00:00Z","2026-05-30T00:00:00Z"
      ]}}}
      EOF
      ```
      Pitfall 8 reminder: ALL 3 outcomes use the SAME fingerprint `aabbccdd1122` — N=3-in-14-days is per-fingerprint.
    Step 2: Strategy A invocation:
      ```
      cd /home/fatduck/patent-cite-tool
      node scripts/quarantine-append.mjs --escalate-stable-runs-reset 1 --case synthetic-flake-case 2>&1 | tee /tmp/uat-47c.log
      echo "EXIT=$?" | tee -a /tmp/uat-47c.log
      ```
    Step 3: Inspect /tmp/uat-47c.log. Two outcomes are acceptable PASS:
      (a) `EXIT=0` AND log contains a `[quarantine-append]` confirmation line for the case reset (the script wrote a stable_runs reset — mechanism verified).
      (b) `EXIT=0` AND log additionally contains "FLAKE_ESCALATION" or "flake-investigation" (the classifier was exercised inline).
      If `EXIT != 0` OR neither (a) nor (b) appears, fall back to Strategy B per Step 4.
    Step 4 (Strategy B — only if Strategy A is ambiguous or fails): Write a small inline test driver to exercise classifyRerunOutcomes directly. Use a one-shot `node -e` invocation:
      ```
      node -e "
        import('./tests/e2e/lib/triage-classifier.js').then((m) => {
          const ring = [
            {verdict:'FLAKE', at:'2026-05-20T00:00:00Z', fingerprint:'aabbccdd1122'},
            {verdict:'FLAKE', at:'2026-05-25T00:00:00Z', fingerprint:'aabbccdd1122'},
            {verdict:'FLAKE', at:'2026-05-30T00:00:00Z', fingerprint:'aabbccdd1122'},
          ];
          const decision = m.classifyRerunOutcomes({outcomes: ring, now: new Date('2026-06-01T00:00:00Z')});
          console.log(JSON.stringify(decision));
          process.exit(decision && decision.state === 'FLAKE_ESCALATION' ? 0 : 2);
        }).catch((e) => { console.error(e); process.exit(3); });
      " 2>&1 | tee -a /tmp/uat-47c.log
      echo "STRATEGY_B_EXIT=$?" | tee -a /tmp/uat-47c.log
      ```
      Per CLAUDE.md C1/C2/C3 — if Strategy A is ambiguous, do NOT auto-pick Strategy B; surface the choice via numbered-list fallback: "Strategy A produced ambiguous evidence. Options: 1) Use Strategy B (classifyRerunOutcomes direct invocation); 2) Accept Strategy A as PASS based on EXIT=0 alone; 3) Mark UAT-47-c as FAIL and open follow-up." Wait for explicit user response.
    Step 5: Use the Edit tool to update `.planning/phases/47-v4-0-cleanup/47-UAT-EVIDENCE.md` `## UAT-47-c` section. Replace `**status:** pending` with `**status:** PASS` (or `FAIL` if neither strategy succeeded). Append these lines under the status:
      - `**verified_at:** <ISO timestamp>`
      - `**strategy_used:** A` (or `A+B` if B was needed; or `B` if A was skipped)
      - `**command:** node scripts/quarantine-append.mjs --escalate-stable-runs-reset 1 --case synthetic-flake-case`
      - `**exit_code:** <code>`
      - `**fingerprint_used:** aabbccdd1122` (the per-fingerprint stability invariant per Pitfall 8)
      - `**outcome_evidence:** <one-line summary — e.g., "EXIT=0 with classifier-side FLAKE_ESCALATION line at log L7" OR "EXIT=0 with quarantine-append reset confirmation only">`
      - Then a fenced ```` ```log ```` block containing the last ~20 lines of /tmp/uat-47c.log
    Step 6: On FAIL: capture full log in evidence section, file follow-up issue: `gh issue create --title "UAT-47-c FAIL: FLAKE escalation did not trip on synthetic fixture" --body "Strategy A exit code: <code>; Strategy B exit code: <code>. See log excerpt in 47-UAT-EVIDENCE.md." --label "v4.0-uat-fail"`. Record the resulting issue URL in the evidence section as `**followup_issue_url:** <url>`. Do NOT block Phase 47 — per CONTEXT.md failure becomes new tech_debt.
    Step 7: Commit: stage `.planning/phases/47-v4-0-cleanup/47-UAT-EVIDENCE.md` only. Commit message: `docs(47-03): UAT-47-c <PASS|FAIL> — FLAKE escalation (Strategy <A|A+B|B>, exit <code>)`.
  </action>
  <how-to-verify>
    1. /tmp/uat-47c.log exists with EXIT line; PASS criteria captured in evidence section.
    2. 47-UAT-EVIDENCE.md UAT-47-c section status is `PASS` or `FAIL` (not `pending`).
    3. On FAIL: follow-up issue URL recorded in the evidence section.
    4. Per CLAUDE.md C1/C2/C3: if Strategy B was invoked via a fallback prompt, the user's explicit choice is captured (no auto-select).
  </how-to-verify>
  <verify>
    <automated>bash -c 'f=.planning/phases/47-v4-0-cleanup/47-UAT-EVIDENCE.md; status=$(awk "/^## UAT-47-c /,/^## UAT-/" "$f" | grep -E "^\*\*status:\*\*" | head -1); echo "UAT-47-c $status"; echo "$status" | grep -qE "(PASS|FAIL)" || { echo "UAT-47-c status not set to PASS or FAIL"; exit 1; }; test -f /tmp/uat-47c.log || { echo "/tmp/uat-47c.log missing"; exit 1; }; grep -qE "^EXIT=" /tmp/uat-47c.log || { echo "EXIT line missing in /tmp/uat-47c.log"; exit 1; }; echo "UAT-47-c evidence captured"'</automated>
  </verify>
  <resume-signal>Type "approved" once UAT-47-c has been run (Strategy A and/or B), exit code captured, evidence appended to 47-UAT-EVIDENCE.md, and commit landed. If FAIL, also include the follow-up GitHub issue URL.</resume-signal>
  <done>UAT-47-c ran live; outcome PASS or FAIL captured with strategy used + exit code + fingerprint + log excerpt; evidence section in 47-UAT-EVIDENCE.md updated; committed. On FAIL: follow-up issue filed, plan continues (no block per CONTEXT.md).</done>
</task>

<task type="auto">
  <name>Task 3: Author 47-UAT-DEFERRED.md with 4 runbook stubs (UAT-47-a, UAT-47-b, UAT-47-d, UAT-47-e) — each with 4 required fields verbatim from 47-RESEARCH.md</name>
  <files>.planning/phases/47-v4-0-cleanup/47-UAT-DEFERRED.md</files>
  <read_first>
    - .planning/phases/47-v4-0-cleanup/47-RESEARCH.md (§"HUMAN-UAT Runbook Stubs" full — lines 686-869; specifically UAT-47-a lines 692-720, UAT-47-b lines 723-746, UAT-47-d lines 801-823, UAT-47-e lines 827-857; the dispatch / expected / heuristic / rollback bash blocks must be transcribed verbatim — do NOT re-invent)
    - .planning/phases/47-v4-0-cleanup/47-CONTEXT.md (§"CLEANUP-03 — Deferred (requires-push)" lines 28-31 — 4 items + result-recording structure)
  </read_first>
  <acceptance_criteria>
    - File `.planning/phases/47-v4-0-cleanup/47-UAT-DEFERRED.md` exists with: top-level title `# Phase 47 — Deferred HUMAN-UAT Runbook Stubs (requires-push)`; preamble paragraph naming the rationale (post-push operator dispatches these after v4.0 ships to origin); then 4 sections in this order: `## UAT-47-a — End-to-end auto-fix flow against real triage-labeled fork issue`, `## UAT-47-b — Dep-PR pre-flight gate blocking on regression`, `## UAT-47-d — Ledger snapshot workflow committing daily snapshot`, `## UAT-47-e — Verifier-gate diff-guard rejecting crafted bypass`
    - Each of the 4 sections contains exactly these 4 sub-headers in this order: `### Dispatch command (post-push)` / `### Expected outcome` / `### Success heuristic` / `### Rollback`
    - Each `### Dispatch command (post-push)` contains a fenced ```` ```bash ```` block with the verbatim commands from 47-RESEARCH.md UAT-47-{a,b,d,e}
    - UAT-47-a section preserves the specific issue context: "Issue #3 US11427642-spec-short-1, fingerprint 139f821b3bb1, branch auto-fix/3-139f821b" (the canonical inherited Phase 42 demo target per CONTEXT.md "Specific Ideas")
    - Total file ≥ 80 lines (4 sections × ~20 lines each minimum)
    - Commit message: `docs(47-03): author 4 DEFERRED UAT runbook stubs (47-UAT-DEFERRED.md)`
  </acceptance_criteria>
  <action>
    Step 1: Use the Write tool to create `.planning/phases/47-v4-0-cleanup/47-UAT-DEFERRED.md`. Top of file:
      `# Phase 47 — Deferred HUMAN-UAT Runbook Stubs (requires-push)`
      blank line
      `**Plan:** 47-03`
      `**Created:** <current ISO timestamp>`
      `**Status:** 4 runbooks (UAT-47-a, UAT-47-b, UAT-47-d, UAT-47-e) — all DEFERRED requires-push per CONTEXT.md`
      blank line
      Preamble paragraph: explain that these 4 UATs cannot execute today because the v4.0 workflows (`v40-auto-fix.yml`, `v40-deps-update.yml`, `v40-cost-ledger-snapshot.yml`, `v40-verifier-gate.yml`) have not yet been pushed to `origin/main`. Once v4.0 ships (separate readiness gate, out of Phase 47 scope), the post-push operator dispatches each runbook in turn and records outcomes in the v4.0-MILESTONE-AUDIT.md `human_verification:` block.
      Horizontal rule `---`.
    Step 2: For UAT-47-a — write the section per the verbatim runbook in 47-RESEARCH.md lines 692-720. Section heading: `## UAT-47-a — End-to-end auto-fix flow against real triage-labeled fork issue`. Body: `**Status:** DEFERRED (requires-push)` / `**Inherits:** Phase 42's deferred demo on issue #3 US11427642-spec-short-1, fingerprint 139f821b3bb1, branch auto-fix/3-139f821b` then the 4 sub-headers with fenced bash blocks copied verbatim from 47-RESEARCH.md (dispatch: gh workflow list / gh issue edit / gh run watch; expected: workflow fires within 30s + draft PR + affected_cases HTML comment + ledger commit; success heuristic: gh pr list --head auto-fix/3-139f821b --json + body grep affected_cases; rollback: gh pr close + gh issue edit --remove-label triage).
    Step 3: For UAT-47-b — section heading: `## UAT-47-b — Dep-PR pre-flight gate blocking on regression`. Body: `**Status:** DEFERRED (requires-push)`. Then the 4 sub-headers with fenced bash blocks per 47-RESEARCH.md lines 723-746 (dispatch: gh workflow run v40-deps-update.yml + sleep + gh pr list + gh run watch; expected: deps-update-gate runs + on synthetic regression FAILS + PR marked human-review-required; success heuristic: gh pr checks shows FAILURE + comment; rollback: gh pr close --delete-branch).
    Step 4: For UAT-47-d — section heading: `## UAT-47-d — Ledger snapshot workflow committing daily snapshot`. Body: `**Status:** DEFERRED (requires-push)`. Then the 4 sub-headers per 47-RESEARCH.md lines 801-823 (dispatch: gh workflow run v40-cost-ledger-snapshot.yml + sleep + gh run list; expected: workflow completes + commit message matches chore(ledger-snapshot): YYYY-MM-DD [skip ci] + atomic update of tests/e2e/.llm-spend-ledger.json per LEDGER-04; success heuristic: git pull origin main + git log --oneline -1 shows [skip ci] + git diff HEAD~1 HEAD shows only ledger updates; rollback: git revert with ruleset relaxation caveat).
    Step 5: For UAT-47-e — section heading: `## UAT-47-e — Verifier-gate diff-guard rejecting crafted bypass`. Body: `**Status:** DEFERRED (requires-push)`. Then the 4 sub-headers per 47-RESEARCH.md lines 827-857 (dispatch: git checkout -b auto-fix/test-craftedbypass-9999-deadbeef + echo append to tests/golden/baseline.json + git commit + git push + gh pr create --draft + gh pr checks; expected: diff-guard FAILS + PR comment names tests/golden/baseline.json + label human-review-required; success heuristic: gh pr checks shows diff-guard FAILURE + gh pr view --json labels contains human-review-required + gh pr view --json comments mentions tests/golden/baseline.json; rollback: gh pr close --delete-branch + revert test fork commit).
    Step 6: Commit: stage `.planning/phases/47-v4-0-cleanup/47-UAT-DEFERRED.md`. Commit message: `docs(47-03): author 4 DEFERRED UAT runbook stubs (47-UAT-DEFERRED.md)`.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/phases/47-v4-0-cleanup/47-UAT-DEFERRED.md; test -f "$f" || { echo "47-UAT-DEFERRED.md missing"; exit 1; }; sec=$(grep -cE "^## UAT-47-[abde] —" "$f"); [ "$sec" -eq 4 ] || { echo "expected 4 UAT sections, found $sec"; exit 1; }; dispatch=$(grep -cE "^### Dispatch command \(post-push\)" "$f"); [ "$dispatch" -eq 4 ] || { echo "expected 4 dispatch headers, found $dispatch"; exit 1; }; outcome=$(grep -cE "^### Expected outcome" "$f"); [ "$outcome" -eq 4 ] || { echo "expected 4 outcome headers, found $outcome"; exit 1; }; heuristic=$(grep -cE "^### Success heuristic" "$f"); [ "$heuristic" -eq 4 ] || { echo "expected 4 heuristic headers, found $heuristic"; exit 1; }; rollback=$(grep -cE "^### Rollback" "$f"); [ "$rollback" -eq 4 ] || { echo "expected 4 rollback headers, found $rollback"; exit 1; }; grep -q "139f821b3bb1" "$f" || { echo "UAT-47-a inherited Phase 42 fingerprint missing"; exit 1; }; lines=$(wc -l < "$f"); [ "$lines" -ge 80 ] || { echo "expected ≥80 lines, found $lines"; exit 1; }; echo "47-UAT-DEFERRED.md coherent: 4 sections × 4 sub-headers, fingerprint pinned, $lines lines"'</automated>
  </verify>
  <done>47-UAT-DEFERRED.md authored with 4 runbook stubs (UAT-47-a/b/d/e); each stub has all 4 required fields (Dispatch / Expected / Success heuristic / Rollback) with verbatim bash blocks from 47-RESEARCH.md; UAT-47-a preserves the Phase 42 inherited target (issue #3, fp 139f821b3bb1); committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create tests/unit/uat-deferred-runbook.test.js — vitest static-grep asserting 4 stub presence + 4 required field headers per stub</name>
  <files>tests/unit/uat-deferred-runbook.test.js</files>
  <read_first>
    - .planning/phases/47-v4-0-cleanup/47-UAT-DEFERRED.md (the file this test asserts — confirm 4 sections + 4 sub-headers per section from Task 3)
    - tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js (lines 1-100 — verbatim template for file-as-text grep + step-window scoping pattern; reusable for stub-window scoping in this test)
    - .planning/phases/47-v4-0-cleanup/47-RESEARCH.md (§"Pattern 1: Touchpoint contract regression test" lines 254-282 — file-as-text grep template; this test follows the same pattern but targets the deferred doc)
  </read_first>
  <behavior>
    - it('47-UAT-DEFERRED.md exists') — `expect(fs.existsSync(DEFERRED_PATH)).toBe(true)`
    - it('contains 4 UAT-47 section headings: a, b, d, e') — 4 sub-its, one per stub, asserting the regex `/^## UAT-47-<id> —/m` matches
    - it('each of 4 sections contains "### Dispatch command (post-push)"') — for each stub, slice the section window (startIdx of `## UAT-47-<id>` to next `## UAT-` or EOF), assert the substring appears
    - it('each of 4 sections contains "### Expected outcome"') — same window-slice + substring
    - it('each of 4 sections contains "### Success heuristic"') — same
    - it('each of 4 sections contains "### Rollback"') — same
    - it('UAT-47-a preserves the inherited Phase 42 demo target fingerprint 139f821b3bb1') — `expect(src).toContain('139f821b3bb1')`
    - Total ≥ 20 assertions (1 file-exists + 4 sections + 4 dispatch + 4 expected + 4 heuristic + 4 rollback + 1 fingerprint = 22)
    - All assertions pass against the post-Task-3 47-UAT-DEFERRED.md
  </behavior>
  <action>
    Step 1 (RED, verify): The test file doesn't yet exist — `test ! -f tests/unit/uat-deferred-runbook.test.js && echo "ok"`.
    Step 2 (GREEN): Create `tests/unit/uat-deferred-runbook.test.js` (new file). Top of file:
      `import { describe, it, expect } from 'vitest';`
      `import fs from 'node:fs';`
      `import path from 'node:path';`
      `import { fileURLToPath } from 'node:url';`
      `const __dirname = path.dirname(fileURLToPath(import.meta.url));`
      `const REPO_ROOT = path.resolve(__dirname, '..', '..');`
      `const DEFERRED_PATH = path.join(REPO_ROOT, '.planning/phases/47-v4-0-cleanup/47-UAT-DEFERRED.md');`
      Then a top-level `describe('Phase 47 CLEANUP-03: 47-UAT-DEFERRED.md runbook stubs are present and complete', () => {...})`.
      Inside: `it('47-UAT-DEFERRED.md exists', () => { expect(fs.existsSync(DEFERRED_PATH)).toBe(true); });`.
      Then load source once: `const src = fs.readFileSync(DEFERRED_PATH, 'utf8');`.
      Then a `for (const stub of ['a', 'b', 'd', 'e']) { describe('UAT-47-' + stub + ' stub', () => { ... }); }` block. Inside each iteration:
        `it('section heading present', () => { expect(src).toMatch(new RegExp('^## UAT-47-' + stub + ' —', 'm')); });`
        `it('### Dispatch command (post-push) present in stub window', () => { const startIdx = src.indexOf('## UAT-47-' + stub + ' —'); expect(startIdx).toBeGreaterThan(-1); let endIdx = src.indexOf('\n## UAT-47-', startIdx + 1); if (endIdx === -1) endIdx = src.length; const window = src.slice(startIdx, endIdx); expect(window).toContain('### Dispatch command (post-push)'); });`
        `it('### Expected outcome present in stub window', () => { /* same window scoping */ expect(window).toContain('### Expected outcome'); });`
        `it('### Success heuristic present in stub window', () => { /* same */ expect(window).toContain('### Success heuristic'); });`
        `it('### Rollback present in stub window', () => { /* same */ expect(window).toContain('### Rollback'); });`
      Outside the per-stub loop, one more it() block:
        `it('UAT-47-a preserves Phase 42 inherited demo fingerprint 139f821b3bb1', () => { expect(src).toContain('139f821b3bb1'); });`
    Step 3 (GREEN verify): `npx vitest run tests/unit/uat-deferred-runbook.test.js --reporter=dot` — confirm all assertions pass against the post-Task-3 47-UAT-DEFERRED.md. Expected count: 1 file-exists + 5 (per-stub) × 4 stubs + 1 fingerprint = 22 it() blocks PASS.
    Step 4 (COMMIT): Stage exactly `tests/unit/uat-deferred-runbook.test.js`. Commit message: `test(47-03): pin 47-UAT-DEFERRED.md stub presence + required-field structure (4 stubs × 4 fields + fingerprint pin)`.
  </action>
  <acceptance_criteria>
    - `test -f tests/unit/uat-deferred-runbook.test.js` exits 0
    - `grep -cE "^\s+it\\(" tests/unit/uat-deferred-runbook.test.js` returns at least 22 (file-exists + 5×4 + fingerprint)
    - `npx vitest run tests/unit/uat-deferred-runbook.test.js --reporter=dot` exits 0 (all assertions pass)
    - `git log --oneline -1` shows commit subject matching `^[0-9a-f]+ test\(47-03\):`
    - `npm run test:src` still exits 0 (no regression to other tests)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/uat-deferred-runbook.test.js --reporter=dot</automated>
  </verify>
  <done>tests/unit/uat-deferred-runbook.test.js created with ≥22 assertions pinning 4 stub presence + 4 required field headers per stub + Phase 42 inherited fingerprint; all assertions GREEN; single atomic test-commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT-47-c local invocation ↔ FLAKE 5-state classifier | Strategy A invokes scripts/quarantine-append.mjs directly against a synthetic fixture; Strategy B invokes classifyRerunOutcomes from the test fixture. Both run locally; only Strategy A may best-effort hit `gh issue create flake-investigation` (best-effort, non-blocking) |
| Deferred runbook content ↔ post-push operator dispatch | 47-UAT-DEFERRED.md is the canonical handoff doc; if dispatch commands drift from reality (e.g., workflow renamed), the post-push operator wastes a cycle. Static-grep test guards stub structure but cannot validate command currency. |
| Phase 47 close criteria ↔ UAT-47-c FAILURE | Per CONTEXT.md locked decision: FAILURE does NOT block; opens follow-up issue + new tech_debt for v4.1. The plan continues to Tasks 3 + 4. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-03-01 | Repudiation | UAT-47-c outcome captured in 47-UAT-EVIDENCE.md but no log preservation | mitigate | Task 2 mandates `/tmp/uat-47c.log` capture + last-20-lines fenced block in the evidence section + the EXIT code; reproducibility hash (synthetic fingerprint aabbccdd1122) is preserved in the evidence. |
| T-47-03-02 | Tampering | Future commit accidentally deletes a runbook stub from 47-UAT-DEFERRED.md (e.g., during a milestone audit cleanup) → post-push operator has no instructions | mitigate | Task 4 vitest test asserts 4 stub presence + 4 required field headers per stub; deletion trips the test at next `npm run test:src` run. |
| T-47-03-03 | Tampering | UAT-47-c Strategy B fallback auto-selected without user consent → CLAUDE.md C1 violation | mitigate | Task 2 explicitly references CLAUDE.md C1/C2/C3 and requires the numbered-list fallback if Strategy A is ambiguous; do NOT auto-pick Strategy B. |
| T-47-03-04 | Information Disclosure | Best-effort `gh issue create flake-investigation` from Strategy A creates a noisy real issue on the production repo | accept | Per scripts/auto-fix.mjs:325-329 the gh issue creation is best-effort and only fires if the classifier reaches the dispatch path. If a noisy issue is created, the rollback note in 47-UAT-EVIDENCE.md instructs the operator to `gh issue close <n> --reason completed --comment "UAT-47-c synthetic test artefact"`. |
| T-47-03-SC | Tampering | npm/pip/cargo installs | accept | Phase 47 installs ZERO new packages. UAT-47-c uses existing scripts/quarantine-append.mjs + tests/e2e/lib/triage-classifier.js + Node 22 built-ins. |
</threat_model>

<verification>
After all 4 tasks complete:
1. 47-UAT-EVIDENCE.md exists with 5 sections; UAT-47-c status is PASS or FAIL (not pending); the 4 DEFERRED sections point at 47-UAT-DEFERRED.md.
2. 47-UAT-DEFERRED.md exists with 4 sections × 4 sub-headers each (Task 3 verify).
3. tests/unit/uat-deferred-runbook.test.js exists; `npx vitest run tests/unit/uat-deferred-runbook.test.js --reporter=dot` exits 0.
4. `npm run test:src` exits 0 (full unit suite green including the new uat-deferred-runbook + the Plan 47-01 v4-touchpoints + package-lock-pinned + Test 48 + INT-FIX-CAL tests).
5. Git log shows 3 new commits from Plan 47-03: `git log --oneline -5 | grep -cE "(docs|test)\(47-03\):"` returns at least 3 (UAT-47-c evidence + UAT-DEFERRED doc + uat-deferred-runbook test).
</verification>

<success_criteria>
- UAT-47-c executed locally per Strategy A (with Strategy B fallback if needed per CLAUDE.md C1); outcome PASS or FAIL recorded in 47-UAT-EVIDENCE.md with strategy + exit code + fingerprint + log excerpt
- 47-UAT-DEFERRED.md authored with 4 stubs (UAT-47-a/b/d/e); each stub has all 4 required fields verbatim from 47-RESEARCH.md; UAT-47-a preserves the inherited Phase 42 demo target
- tests/unit/uat-deferred-runbook.test.js pins stub presence + structure with ≥22 it() blocks; GREEN against post-Task-3 doc
- On UAT-47-c FAILURE: follow-up issue filed, new tech_debt added (recorded for Plan 47-04 to copy into v4.0-MILESTONE-AUDIT.md); plan continues without blocking
- 3 commits land in order: UAT-47-c evidence → UAT-DEFERRED doc → uat-deferred-runbook test
</success_criteria>

<output>
Create `.planning/phases/47-v4-0-cleanup/47-03-SUMMARY.md` per the standard template recording: UAT-47-c outcome (PASS/FAIL + strategy + exit code + fingerprint + log excerpt + follow-up issue URL if FAIL), 4 DEFERRED runbook stubs authored (paths + line counts), vitest assertion count (22+), commit SHAs.
</output>
