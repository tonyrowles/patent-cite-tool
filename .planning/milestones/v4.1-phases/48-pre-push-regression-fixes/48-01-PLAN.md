---
phase: 48-pre-push-regression-fixes
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/e2e/lib/llm-driver.js
  - tests/e2e/.llm-spend-ledger.json
  - tests/e2e/scripts/e2e-weekly-digest.test.js
  - scripts/weekly-digest.mjs
autonomous: true
requirements:
  - PRE-01
  - PRE-02
  - PRE-03
  - PRE-04
must_haves:
  truths:
    - "`npm test` exits 0 locally with zero failures (the full script: build + test:src + test:chrome + test:firefox + lint + test:lint — ROADMAP §Phase 48 SC-1)"
    - "`tests/e2e/.llm-spend-ledger.json` contains exactly 1 month bucket (the `2026-05` Phase-39 bootstrap sentinel) — restores the Phase-39 seed shape PRE-01 requires"
    - "Calling `invokeAnthropicSdkWithLedger({forceApi:true,...})` outside CI without `E2E_LEDGER_PATH_OVERRIDE` throws the locked PRE-02 contract error before any ledger write — Step 0 guard per D-03 fires at the first line of the function body; plain `Error` with verbatim message per D-02; reuses the relocated `inCi` definition (no new escape hatch per D-04)"
    - "Per D-01, NO regression unit test is added to pin the PRE-02 guard — surgical commit; Test 48 catches downstream pollution and the contract-error text is self-documenting"
    - "`package-lock.json` and `package.json` both retain EXACT `\"@anthropic-ai/sdk\": \"0.100.1\"` (no caret); the Phase 47 4-assertion static-grep test passes unchanged"
    - "`tests/e2e/scripts/e2e-weekly-digest.test.js` calendar-rollover fix uses epoch-relative fixture dates derived from a single `PIN_NOW_ISO = '2026-05-25T00:00:00Z'` anchor per D-05 — no hardcoded `'2026-05'` month-key, no `new Date().toISOString().slice(0,7)` band-aid, no hardcoded `'2026-W22'` literal inside assertion sites (D-07 replacements)"
    - "Per D-06, `const daysAgo = (n) => new Date(Date.parse(PIN_NOW_ISO) - n * 86400000).toISOString()` is defined inline next to the anchor in `e2e-weekly-digest.test.js`; every fixture ISO literal (`'2026-05-22T...'`, `'2026-05-19T...'`, `'2026-05-10T...'`) is replaced by a `daysAgo(N)` call so the call-site name encodes the semantics"
    - "Per D-08 and its D-08-AMEND (2026-06-02): no new helper module is added — all new helpers stay inline in `e2e-weekly-digest.test.js`; no `process.env.TEST_PIN_NOW` or other env-overridable hatch. The amendment permits the single additive `now: Date` parameter on the existing `renderCostLine` export in `scripts/weekly-digest.mjs` because it is not a new module — only a backward-compatible signature extension threading the anchor through `monthlyTotal(ledger, month)`"
    - "Plan structure follows D-09 (single plan file `48-01-PLAN.md` covering all 4 PRE-* fixes as 4 sequential tasks) and D-10 (four atomic commits — three guaranteed for PRE-02/01/03 plus optional PRE-04 in Branch B drift-repair — each with subject `fix(48-pre-push): PRE-<N> — <one-line>` mirroring Phase 47's `fix(<phase>-<scope>): <TAG> — <one-line>` convention)"
    - "Per D-11, the four atomic commits land in MANDATORY order: PRE-02 (Guard) → PRE-01 (Reset) → PRE-03 (Epoch) → PRE-04 (Lockfile verify-or-repair); each task body and acceptance criterion verifies prior commits via `git log` before proceeding"
  artifacts:
    - path: "tests/e2e/lib/llm-driver.js"
      provides: "Step 0 guard at the top of `invokeAnthropicSdkWithLedger` (lines 506-545 region); throws the locked contract-error message when `forceApi:true && !process.env.CI && !process.env.E2E_LEDGER_PATH_OVERRIDE`"
      contains: "invokeAnthropicSdkWithLedger: forceApi:true blocked outside CI without E2E_LEDGER_PATH_OVERRIDE"
    - path: "tests/e2e/.llm-spend-ledger.json"
      provides: "Phase-39 single-bootstrap-entry ledger; `.months` has exactly 1 key (`2026-05`)"
      contains: "39-bootstrap"
    - path: "tests/e2e/scripts/e2e-weekly-digest.test.js"
      provides: "Epoch-relative fixture pattern: top-of-file `PIN_NOW_ISO` const + `daysAgo(n)` helper; all assertion-site week labels and inline fixture dates derive from the anchor"
      contains: "PIN_NOW_ISO = '2026-05-25T00:00:00Z'"
    - path: "scripts/weekly-digest.mjs"
      provides: "`renderCostLine` accepts an optional `now: Date` parameter that threads through `monthlyTotal(ledger, month)` so the cost-line month-key is derivable from the test's pinned anchor instead of live `new Date()`"
      contains: "renderCostLine({ ledgerPath, now"
    - path: "tests/unit/package-lock-pinned.test.js"
      provides: "Phase 47 INT-FIX-LOCK 4-assertion static-grep — verified still present and passing; no change unless drift detected"
      contains: '"@anthropic-ai/sdk":\\s*"0\\.100\\.1"'
    - path: "package.json"
      provides: "`@anthropic-ai/sdk` EXACT pinned to `0.100.1` (no caret/tilde)"
      contains: '"@anthropic-ai/sdk": "0.100.1"'
    - path: "package-lock.json"
      provides: "`@anthropic-ai/sdk` EXACT pinned to `0.100.1` with resolved-URL substring `sdk-0.100.1.tgz`"
      contains: '"@anthropic-ai/sdk": "0.100.1"'
  key_links:
    - from: "tests/e2e/lib/llm-driver.js (invokeAnthropicSdkWithLedger Step 0)"
      to: "process.env.CI && process.env.E2E_LEDGER_PATH_OVERRIDE"
      via: "first-line env-check before any forceApi branch"
      pattern: "process\\.env\\.E2E_LEDGER_PATH_OVERRIDE.*forceApi|forceApi.*process\\.env\\.E2E_LEDGER_PATH_OVERRIDE"
    - from: "tests/e2e/scripts/e2e-weekly-digest.test.js (PIN_NOW_ISO)"
      to: "scripts/weekly-digest.mjs (renderCostLine now param)"
      via: "passing `now: PIN_NOW()` into the renderCostLine call so its month-key derivation aligns with the test's pinned anchor"
      pattern: "renderCostLine\\(\\{[^}]*now"
    - from: "tests/unit/llm-ledger.test.js:1012 (Test 48 schema-plus-state contract)"
      to: "tests/e2e/.llm-spend-ledger.json"
      via: "static read; assertion is `Object.keys(j.months).length === 1` plus per-field bootstrap shape (phase='39-bootstrap', sonnet, cost_usd=0)"
      pattern: "39-bootstrap"
---

<objective>
Phase 48 restores `npm test` to exit 0 locally so the v4.0-integration PR (Phase 49) can pass CI green. Four surgical fixes land as four atomic commits in a mandatory order (guard → reset → epoch → lockfile) per D-11:

1. **PRE-02** — Step 0 guard in `invokeAnthropicSdkWithLedger` prevents future committed-ledger pollution from local `forceApi:true` SDK calls. **Lands first** so any local SDK invocation between subsequent commits cannot re-leak the ledger.
2. **PRE-01** — Resets `tests/e2e/.llm-spend-ledger.json` to its Phase-39 single-bootstrap-entry state, removing the 4 leaked `2026-06` `claude-opus-4-7[1m]` entries (~$0.451). Returns Test 48 to GREEN.
3. **PRE-03** — Refactors `tests/e2e/scripts/e2e-weekly-digest.test.js` to a single `PIN_NOW_ISO = '2026-05-25T00:00:00Z'` anchor + `daysAgo(n)` derivation pattern. Supersedes Phase 47's INT-FIX-CAL band-aid that used live `new Date()` for the ledger month key. Threads the anchor into `scripts/weekly-digest.mjs` `renderCostLine` via a new optional `now` parameter.
4. **PRE-04** — Verifies the Phase 47 lockfile EXACT-pin defense (`tests/unit/package-lock-pinned.test.js`) still passes against current `package.json` + `package-lock.json`. Per Discretion bullet 4 in CONTEXT.md, this is verify-first: pre-flight checks show all four assertions GREEN and `@anthropic-ai/sdk@0.100.1` exact (no caret) in both files, so this task is verify-only and records the result in the plan SUMMARY without a code commit. If drift is detected at execution time the task adds repair steps.

Purpose: Test 48 ledger leak, calendar-rollover flake, and lockfile pin must all be GREEN before CI can pass on the v4.0-integration push. Without these fixes, the Wave-1 push is blocked.

Output: Three atomic commits (PRE-02, PRE-01, PRE-03) matching the locked `fix(48-pre-push): PRE-<N> — <one-line>` pattern, plus a PRE-04 verify-only SUMMARY entry (no commit if no drift). `npm test` exits 0 with zero failures. Ledger is single-bootstrap-entry clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-pre-push-regression-fixes/48-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/milestones/v4.0-phases/47-v4-0-cleanup/47-CONTEXT.md
@.planning/milestones/v4.0-phases/47-v4-0-cleanup/47-01-SUMMARY.md

<interfaces>
<!-- Key signatures and literals the executor needs. Extracted from the codebase
     so no exploration is required. All line numbers verified at plan time. -->

From `tests/e2e/lib/llm-driver.js` (function body opens at line 506; first existing
`forceApi` branch is at line 538):

  export async function invokeAnthropicSdkWithLedger({
    systemBlocks,
    messages,
    model,
    maxTokens,
    forceApi = false,
    // ... other named params ...
  }) {
    // ... existing systemBlocks validation (lines ~520-533) ...
    if (!inCi && !forceApi) {
      // existing local-no-CI no-op branch (line 538)
    }
    // PRE-02 inserts Step 0 guard BEFORE the line 538 branch (D-03).

From `tests/e2e/lib/llm-ledger.js`:
  export function currentMonth() {
    return new Date().toISOString().slice(0, 7);   // line 161-163
  }
  export function monthlyTotal(ledger, month = currentMonth()) {  // line 199 — accepts optional month override
    return ledger?.months?.[month]?.total_usd ?? 0;
  }

From `scripts/weekly-digest.mjs` (signature at line 224; production call site at line 435):
  export function renderCostLine({ ledgerPath } = {}) {
    const effectivePath = ledgerPath ?? LEDGER_PATH;
    if (!fs.existsSync(effectivePath)) return 'cost data unavailable';
    const spent = monthlyTotal(readLedger(effectivePath));  // ← live currentMonth() default
    return `$${spent.toFixed(2)} / $${HARD_CAP_USD} (${Math.round((spent / HARD_CAP_USD) * 100)}%)`;
  }

From `tests/e2e/scripts/e2e-weekly-digest.test.js`:
  Line 64:  const PIN_NOW = () => new Date('2026-05-25T00:00:00Z');
  Line 248: weekLabel: '2026-W22',               (assertion-site literal — refactor)
  Line 272: const md = renderDigest({ weekLabel: '2026-W22', agg, costLine, now: PIN_NOW() });
  Line 329: { number: 999, created_at: '2026-05-24T00:00:00Z', labels: [...] }   (inline fixture — refactor to daysAgo(1))
  Line 389: [new Date().toISOString().slice(0, 7)]: { invocations: 5, total_usd: 12.5, ... }
           ← Phase-47 INT-FIX-CAL band-aid; PRE-03 replaces with PIN_NOW_ISO.slice(0, 7)
           ← Paired with passing `now: PIN_NOW()` into the renderCostLine call two lines below.
  Lines 130/134/137-139: `isoWeekLabel(new Date('2026-01-01T00:00:00Z')).toBe('2026-W01')` etc.
           ← these are SELF-TESTS of the isoWeekLabel helper using hardcoded calendar
              facts; they DO NOT get refactored to daysAgo() (would lose the property
              being tested). Leave them alone.

From `tests/unit/llm-ledger.test.js:1012` (Test 48 — NOT MODIFIED; goes green after PRE-01):
    const months = Object.keys(j.months);
    expect(months.length).toBe(1);
    const bucket = j.months[months[0]];
    expect(bucket.invocations).toBe(1);
    expect(bucket.total_usd).toBe(0);
    expect(bucket.iterations.length).toBe(1);
    const it = bucket.iterations[0];
    expect(it.phase).toBe('39-bootstrap');
    expect(it.transport).toBe('sdk');
    expect(it.cost_usd).toBe(0);
    expect(it.source).toBe('phase-39-flip');
    expect(it.model).toBe('claude-sonnet-4-6');

Current ledger state (`git show HEAD:tests/e2e/.llm-spend-ledger.json` — confirmed
at plan time): 2 month buckets. `2026-05` has the 1-entry bootstrap to preserve.
`2026-06` has 4 leaked `claude-opus-4-7[1m]` entries with `phase: null` totalling
$0.451461 — this entire bucket gets deleted.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PRE-02 — Step 0 guard in invokeAnthropicSdkWithLedger</name>
  <files>tests/e2e/lib/llm-driver.js</files>
  <read_first>
    - tests/e2e/lib/llm-driver.js (full file; the function body opens at line 506 and the first existing forceApi branch is at line 538 — Step 0 guard goes BETWEEN line 506 and line 538, after the existing systemBlocks validation but BEFORE any forceApi-conditional logic per D-03)
    - .planning/phases/48-pre-push-regression-fixes/48-CONTEXT.md (D-01, D-02, D-03, D-04 — guard contract; D-02 error message is LOCKED verbatim)
    - .planning/research/PITFALLS.md (Pitfall 5 — root-cause analysis of why the leak happens: LEDGER_PATH IIFE resolves at module-load if E2E_LEDGER_PATH_OVERRIDE is unset, so any forceApi:true call without the override lands in the committed file; this guard closes that vector)
    - .planning/milestones/v4.0-phases/47-v4-0-cleanup/47-CONTEXT.md (CLEANUP-01 commit-message pattern `fix(<phase>-<scope>): <TAG> — <one-line>`; Phase 48 mirrors as `fix(48-pre-push): PRE-<N> — <one-line>` per D-10)
  </read_first>
  <action>
    Per D-03, insert a Step 0 guard at the FIRST line of the `invokeAnthropicSdkWithLedger` function body in `tests/e2e/lib/llm-driver.js` — BEFORE the existing systemBlocks validation that begins around line 520 and BEFORE the `if (!inCi && !forceApi)` branch at line 538. Read `process.env.CI` (any truthy value) and `process.env.E2E_LEDGER_PATH_OVERRIDE` (any non-empty string). When `forceApi === true && !process.env.CI && !process.env.E2E_LEDGER_PATH_OVERRIDE`, throw a plain `Error` (D-02 — no custom error class) with the EXACT verbatim message: `invokeAnthropicSdkWithLedger: forceApi:true blocked outside CI without E2E_LEDGER_PATH_OVERRIDE. Set E2E_LEDGER_PATH_OVERRIDE=<tmpfile> to redirect ledger writes, or run inside CI. Prevents committed-ledger pollution.`

    The string must match this byte-for-byte — it is locked by D-02 and verified by acceptance grep. No paraphrasing. No additional newlines, no leading/trailing whitespace inside the quotes, no string concatenation that would change byte output.

    Truthiness model per D-03 / D-04 — PINNED to match the existing `inCi` definition at line 538: the guard MUST use the SAME CI-check expression as the existing function. Concretely: relocate the existing `const inCi = process.env.CI === 'true' || process.env.GITHUB_ACTIONS === 'true';` line from line 538 to the FIRST line of the function body (above the Step 0 guard), and reuse `inCi` in the guard condition. Result: one CI-check definition for the entire function. Do NOT use `Boolean(process.env.CI)` (which would accept `CI=0`, `CI=false`, `CI=anything-non-empty` as truthy and ignore `GITHUB_ACTIONS` — divergent semantics from line 538). Treat `process.env.E2E_LEDGER_PATH_OVERRIDE` as "set" when it is a non-empty string. The final guard expression: `if (forceApi === true && !inCi && !process.env.E2E_LEDGER_PATH_OVERRIDE) throw new Error('...');`. The semantics: guard fires ONLY when `forceApi === true` AND we are outside CI AND the override env-var is empty/unset. Per D-04 there is no other escape hatch — the daily snapshot workflow runs in CI so `process.env.CI === 'true'` and the guard does not block it; local dev that legitimately wants to write the committed ledger sets `E2E_LEDGER_PATH_OVERRIDE` to a tmpdir.

    Per D-01, no regression unit test pins this guard — the surgical commit is the guard alone. Test 48 catches downstream pollution; the contract-error message text is self-documenting.

    Commit shape (D-10, D-11): this is the FIRST commit of the four. Stage ONLY the change in `tests/e2e/lib/llm-driver.js`. Commit message subject: `fix(48-pre-push): PRE-02 — block forceApi:true ledger writes outside CI without E2E_LEDGER_PATH_OVERRIDE` (executor may refine the one-line at discretion per Discretion bullet 1 in CONTEXT.md, but must keep the `fix(48-pre-push): PRE-02 — ` prefix). Body: 1-2 sentences naming the root cause (LEDGER_PATH IIFE resolves to committed path when override is unset; any local `forceApi:true` then writes there) and the fix mechanism (Step 0 throw before any ledger code path). Mirror the body shape of `fix(47-cleanup): INT-FIX-LEDGER — ...` from Phase 47 — terse, root-cause + mechanism.
  </action>
  <verify>
    <automated>grep -F "invokeAnthropicSdkWithLedger: forceApi:true blocked outside CI without E2E_LEDGER_PATH_OVERRIDE" tests/e2e/lib/llm-driver.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "invokeAnthropicSdkWithLedger: forceApi:true blocked outside CI" tests/e2e/lib/llm-driver.js` exits 0
    - `grep -F "Prevents committed-ledger pollution." tests/e2e/lib/llm-driver.js` exits 0 (locked tail of D-02 error message)
    - CI-check semantics consistency: `inCi` is defined exactly ONCE in `invokeAnthropicSdkWithLedger` and uses the strict expression `process.env.CI === 'true' || process.env.GITHUB_ACTIONS === 'true'` — verify by `grep -cF "process.env.CI === 'true' || process.env.GITHUB_ACTIONS === 'true'" tests/e2e/lib/llm-driver.js` returning `1` (the original line 538 definition was relocated, not duplicated)
    - No `Boolean(process.env.CI)` introduced anywhere in the file: `grep -F "Boolean(process.env.CI)" tests/e2e/lib/llm-driver.js` exits 1
    - The guard uses `inCi`, not a fresh CI check: `grep -nE "if\s*\(\s*forceApi\s*===\s*true\s*&&\s*!\s*inCi\s*&&\s*!\s*process\.env\.E2E_LEDGER_PATH_OVERRIDE" tests/e2e/lib/llm-driver.js` exits 0
    - Live throw probe (outside CI, override unset): the command below exits non-zero AND stderr contains the locked PRE-02 error string (`Prevents committed-ledger pollution.`):
      `env -u CI -u E2E_LEDGER_PATH_OVERRIDE node --input-type=module -e "import('./tests/e2e/lib/llm-driver.js').then(m => m.invokeAnthropicSdkWithLedger({forceApi:true,systemBlocks:[{type:'text',text:'x'}],messages:[{role:'user',content:'x'}],model:'claude-sonnet-4-6',maxTokens:1}).catch(e => { console.error(e.message); process.exit(2); }))"`
    - Same probe with `CI=true` set: guard does NOT fire (the call may fail later for other reasons such as auth/network — but NOT with the PRE-02 error string in stderr)
    - Same probe with `E2E_LEDGER_PATH_OVERRIDE=/tmp/test-ledger.json` set: guard does NOT fire
    - Existing vitest coverage for llm-driver (if any tests reference this module) still passes — no regressions
    - Exactly one new commit lands with subject prefix `fix(48-pre-push): PRE-02 — ` and changes ONLY `tests/e2e/lib/llm-driver.js`: `git log -1 --name-only --pretty=format:%s` shows the subject and only this file
  </acceptance_criteria>
  <done>
    Step 0 guard is at the top of `invokeAnthropicSdkWithLedger` body, throws the locked D-02 error verbatim when `forceApi:true && !CI && !E2E_LEDGER_PATH_OVERRIDE`, does NOT fire when CI is set, does NOT fire when the override is set. The change lands as a single atomic commit `fix(48-pre-push): PRE-02 — ...` modifying only `tests/e2e/lib/llm-driver.js`. Per D-11 this is the first of four commits — no other PRE-* task starts until this commit lands.
  </done>
</task>

<task type="auto">
  <name>Task 2: PRE-01 — Reset committed ledger to Phase-39 single-bootstrap entry</name>
  <files>tests/e2e/.llm-spend-ledger.json</files>
  <read_first>
    - tests/e2e/.llm-spend-ledger.json (current committed state — verify against `git show HEAD:tests/e2e/.llm-spend-ledger.json` immediately before editing; current state at plan time has 2 month buckets — the `2026-05` Phase-39 bootstrap to PRESERVE, and the `2026-06` leak to DELETE entirely)
    - tests/unit/llm-ledger.test.js lines 999-1025 (Test 48 — the schema-plus-state contract that this reset must satisfy; assertion details in the `<interfaces>` block above; this test is NOT MODIFIED — locked by CONTEXT.md Out of scope)
    - .planning/phases/48-pre-push-regression-fixes/48-CONTEXT.md (planner_constraints item 7 — PRE-01 reset shape: single `2026-05` bucket with one invocation; `phase='39-bootstrap'`, `model='claude-sonnet-4-6'`, `cost_usd:0`, `tokens_in:0`, `tokens_out:0`, `transport:'sdk'`, `source:'phase-39-flip'`)
    - .planning/milestones/v4.0-phases/47-v4-0-cleanup/47-01-SUMMARY.md (Phase 47 INT-FIX-LEDGER summary — same fix, same shape; this is the reference for the JSON structure to restore)
    - .planning/research/PITFALLS.md Pitfall 5 (root-cause: the leaked `claude-opus-4-7[1m]` entries with `phase=null, transport=null` came from a GSD executor that imported `invokeAnthropicSdkWithLedger` without setting `E2E_LEDGER_PATH_OVERRIDE`; PRE-02 closes the recurrence vector, PRE-01 restores the clean state)
  </read_first>
  <action>
    PRE-02 (Task 1) MUST be in HEAD before this task starts so the guard is active for any subsequent local SDK call that might fire during work. Verify with `git log -1 --pretty=format:%s` showing the `fix(48-pre-push): PRE-02 — ` subject.

    Overwrite `tests/e2e/.llm-spend-ledger.json` to contain exactly the Phase-39 bootstrap shape: top-level `version: 1`, a single `months` key (`2026-05`), one invocation. The preserved entry already exists at index 0 of the current `months["2026-05"].iterations` array — keep its exact ISO timestamp (`2026-05-31T16:03:31.594Z` per current HEAD; do NOT regenerate the timestamp — Test 48 asserts the bootstrap fields, but preserving the original ISO maintains audit-trail continuity with Phase 39's seed commit).

    Exact JSON shape (mirrors Phase 47 INT-FIX-LEDGER restoration):

    - `version: 1`
    - `months["2026-05"].invocations: 1` (the count field — integer, NOT the iterations array)
    - `months["2026-05"].total_usd: 0`
    - `months["2026-05"].last_invocation_iso: "2026-05-31T16:03:31.595Z"` (preserve current value to avoid spurious diff vs Phase 39 seed)
    - `months["2026-05"].iterations: [{ iso: "2026-05-31T16:03:31.594Z", model: "claude-sonnet-4-6", cost_usd: 0, tokens_in: 0, tokens_out: 0, phase: "39-bootstrap", transport: "sdk", source: "phase-39-flip" }]`

    DELETE the entire `months["2026-06"]` key — all four leaked `claude-opus-4-7[1m]` entries with `phase: null` go away. Do NOT keep any of them. Do NOT add a `phase` value to them to "salvage" them — they bypassed the wrapper and have no legitimate provenance.

    JSON formatting: match the existing file's formatting (2-space indent, trailing newline, no leading BOM). Verify with `jq` after the write that the file parses and `jq '.months | keys'` returns exactly `["2026-05"]`.

    Per CONTEXT.md Out of scope: do NOT modify `tests/unit/llm-ledger.test.js:1012` (Test 48). The test stays unchanged and goes green automatically once the ledger matches the bootstrap shape.

    Commit shape (D-10, D-11): this is the SECOND commit. Stage ONLY `tests/e2e/.llm-spend-ledger.json`. Subject: `fix(48-pre-push): PRE-01 — reset committed ledger to Phase-39 single-bootstrap entry` (executor may refine the one-line per Discretion bullet 1 but must keep the `fix(48-pre-push): PRE-01 — ` prefix). Body 1-2 sentences: root cause (`2026-06` bucket contained 4 `phase=null, transport=null` opus 4.7 entries from a local executor run without `E2E_LEDGER_PATH_OVERRIDE` set; total $0.451) and fix mechanism (delete the leaked bucket; PRE-02 guard now prevents recurrence). Reference Test 48 returning to GREEN as the verification anchor.
  </action>
  <verify>
    <automated>jq -e '(.months | keys | length) == 1 and (.months["2026-05"] != null) and (.months["2026-06"] == null) and (.months["2026-05"].invocations == 1) and (.months["2026-05"].total_usd == 0) and (.months["2026-05"].iterations | length) == 1 and (.months["2026-05"].iterations[0].phase == "39-bootstrap") and (.months["2026-05"].iterations[0].transport == "sdk") and (.months["2026-05"].iterations[0].source == "phase-39-flip") and (.months["2026-05"].iterations[0].model == "claude-sonnet-4-6") and (.months["2026-05"].iterations[0].cost_usd == 0)' tests/e2e/.llm-spend-ledger.json</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.months | keys | length' tests/e2e/.llm-spend-ledger.json` returns `1`
    - `jq '.months["2026-05"].iterations | length' tests/e2e/.llm-spend-ledger.json` returns `1`
    - `jq '.months["2026-05"].invocations' tests/e2e/.llm-spend-ledger.json` returns `1`
    - `jq '.months["2026-05"].total_usd' tests/e2e/.llm-spend-ledger.json` returns `0`
    - `jq -r '.months["2026-05"].iterations[0].phase' tests/e2e/.llm-spend-ledger.json` returns `39-bootstrap`
    - `jq -r '.months["2026-05"].iterations[0].transport' tests/e2e/.llm-spend-ledger.json` returns `sdk`
    - `jq -r '.months["2026-05"].iterations[0].source' tests/e2e/.llm-spend-ledger.json` returns `phase-39-flip`
    - `jq -r '.months["2026-05"].iterations[0].model' tests/e2e/.llm-spend-ledger.json` returns `claude-sonnet-4-6`
    - `jq '.months["2026-06"]' tests/e2e/.llm-spend-ledger.json` returns `null`
    - No entry in the file has `phase: null` or `transport: null`: `jq '[.months[].iterations[] | select(.phase == null or .transport == null)] | length' tests/e2e/.llm-spend-ledger.json` returns `0`
    - Test 48 passes: `npx vitest run tests/unit/llm-ledger.test.js` exits 0
    - Exactly one new commit lands with subject prefix `fix(48-pre-push): PRE-01 — ` and changes ONLY `tests/e2e/.llm-spend-ledger.json`: `git log -1 --name-only --pretty=format:%s` shows the subject and only this file
    - Commit order check: `git log --oneline -2 --pretty=format:%s` shows PRE-01 at HEAD and PRE-02 at HEAD~1 (in that order per D-11)
  </acceptance_criteria>
  <done>
    Ledger is restored to single-bootstrap state matching the Phase-39 seed shape; Test 48 returns to GREEN; the leaked `2026-06` bucket is gone; no `phase: null` or `transport: null` entries remain. Commit `fix(48-pre-push): PRE-01 — ...` is in HEAD and `fix(48-pre-push): PRE-02 — ...` is at HEAD~1 per D-11 ordering.
  </done>
</task>

<task type="auto">
  <name>Task 3: PRE-03 — Epoch-relative fixture refactor in e2e-weekly-digest.test.js</name>
  <files>tests/e2e/scripts/e2e-weekly-digest.test.js, scripts/weekly-digest.mjs</files>
  <read_first>
    - tests/e2e/scripts/e2e-weekly-digest.test.js (full file — at minimum lines 1-100, 240-280, 320-340, 380-410, and the regions referenced in the `<interfaces>` block above; understand the existing PIN_NOW factory, the inline assertion-site literals, and how the test currently passes its `now` parameter into `aggregate`, `renderDigest`, and `runDigest`)
    - scripts/weekly-digest.mjs (lines 210-260 — the `renderCostLine` and `renderDigest` exports; the executor must add an optional `now: Date` parameter to `renderCostLine` and thread it into `monthlyTotal(ledger, month)` so the cost-line month-key derivation aligns with the test's pinned anchor instead of live `new Date()`. This is the only production-code change required by PRE-03; CONTEXT.md does not forbid it and D-08's "no new helper module" applies to the test file, not to threading an optional param into an existing function)
    - tests/e2e/lib/llm-ledger.js lines 161-200 (`currentMonth()` at line 161-163 and `monthlyTotal(ledger, month = currentMonth())` at line 199 — the latter already accepts an optional month override, so threading `now` through `renderCostLine` requires only converting Date to `'YYYY-MM'` string at the call site)
    - .planning/phases/48-pre-push-regression-fixes/48-CONTEXT.md (D-05, D-06, D-07, D-08 — the locked refactor pattern; planner_constraints item 5 — anchor value LOCKED to `'2026-05-25T00:00:00Z'` so the existing `'2026-W22'` ISO-week semantics are preserved)
    - .planning/research/PITFALLS.md Pitfall 6 (why the current Phase-47 INT-FIX-CAL band-aid is wrong: dynamic `new Date().toISOString().slice(0,7)` makes the test non-deterministic across calendar boundaries; PRE-03 replaces it with `PIN_NOW_ISO.slice(0,7)`)
    - .planning/milestones/v4.0-phases/47-v4-0-cleanup/47-01-SUMMARY.md lines 88-95 (Phase 47 INT-FIX-CAL committed `cf9ec46` — what it did and why this phase supersedes it with the epoch-relative anchor)
  </read_first>
  <action>
    PRE-01 and PRE-02 must be in HEAD before this task starts (per D-11 ordering). Verify with `git log -2 --pretty=format:%s` showing the two prior PRE-* subjects.

    The refactor has three parts: (a) introduce the anchor and helper in the test file; (b) thread a `now` parameter through `renderCostLine` so the live-clock month-key band-aid can be removed; (c) replace the inline assertion-site and fixture literals derived from the anchor.

    Part A — Test file anchor and helper (D-05, D-06, D-08). At the top of `tests/e2e/scripts/e2e-weekly-digest.test.js`, near the existing line 64 `PIN_NOW` definition, replace the factory with the locked pattern:

      `const PIN_NOW_ISO = '2026-05-25T00:00:00Z';`
      `const PIN_NOW = () => new Date(PIN_NOW_ISO);`
      `const daysAgo = (n) => new Date(Date.parse(PIN_NOW_ISO) - n * 86400000).toISOString();`

    The `PIN_NOW_ISO` value is LOCKED by planner_constraints item 5 — exact byte string `'2026-05-25T00:00:00Z'`. Per D-08, no new helper module — the helper lives inline above. Per Discretion bullet 2 the executor may place `daysAgo` above or below `PIN_NOW_ISO` (stylistic). Per Discretion bullet 3 the executor may keep `PIN_NOW = () => new Date(PIN_NOW_ISO)` (factory returning fresh instances — safer for downstream consumers that may mutate) OR `const PIN_NOW_DATE = new Date(PIN_NOW_ISO)` if a survey of all call sites confirms none mutate the Date. The factory form is the safer default; recommend keeping it unless a call-site grep proves mutation is impossible.

    Part B — Thread `now` into `renderCostLine` (`scripts/weekly-digest.mjs`). Change the signature at line 224 from `renderCostLine({ ledgerPath } = {})` to `renderCostLine({ ledgerPath, now } = {})`. Inside the function, when `now` is provided, derive the month key from it: `const month = now ? now.toISOString().slice(0, 7) : undefined;` and pass it to `monthlyTotal`: `monthlyTotal(readLedger(effectivePath), month)`. When `now` is undefined, `monthlyTotal` defaults to `currentMonth()` (live clock) — preserves backward compatibility for the production call at line 435 which does NOT pass `now`. This is the only production-code change in this task; it is additive (optional param) and does not alter existing call-site behavior.

    Part C — Refactor test file literals. Apply these changes:

    - Line 248 (`weekLabel: '2026-W22'` inside the assertion object passed to `renderDigest({...})` in the second test of the "five aggregations present" block): replace with `weekLabel: isoWeekLabel(PIN_NOW())` so the week label is derived from the anchor. `isoWeekLabel` is already imported at line 46.
    - Line 272 (`renderDigest({ weekLabel: '2026-W22', agg, costLine, now: PIN_NOW() })`): same replacement — `weekLabel: isoWeekLabel(PIN_NOW())`.
    - Line 329 (`{ number: 999, created_at: '2026-05-24T00:00:00Z', ... }` inside the CR-02 fixture array): replace `'2026-05-24T00:00:00Z'` with `daysAgo(1)`. The semantic is "1 day before pinned now" — verify by computing: `Date.parse('2026-05-25T00:00:00Z') - 86400000 = 2026-05-24T00:00:00Z` matches.
    - Line 389 (`[new Date().toISOString().slice(0, 7)]: { invocations: 5, total_usd: 12.5, ... }` — the Phase-47 INT-FIX-CAL band-aid month-key): replace with `[PIN_NOW_ISO.slice(0, 7)]` per D-07. The same `it()` block must also update the `renderCostLine({ ledgerPath: tmpLedger })` call site (a few lines below at line ~393) to pass `now: PIN_NOW()`, so the assertion-side month-key (`'2026-05'`) matches the seeded ledger bucket key (`'2026-05'`). Without threading `now`, the renderCostLine would lookup `currentMonth()` from the live clock and the assertion would only pass in May 2026.

    DO NOT MODIFY lines 130/134/137-139 (the `isoWeekLabel` self-tests): those assertions use hardcoded calendar facts like `'2026-01-01T00:00:00Z' -> '2026-W01'` and `'2026-05-25T00:00:00Z' -> '2026-W22'` to test the helper's correctness. Refactoring them to use `daysAgo()` would destroy the property being tested. Leave them byte-unchanged.

    DO NOT MODIFY the external fixture `tests/e2e/fixtures/phase37-digest-issues.json`. The quarantine issue fixtures (numbers 104/105/107/109) referenced in the comment at lines 24-25 live in that JSON file with absolute May 2026 dates that are ALREADY in-window for `PIN_NOW = 2026-05-25` (issue 104 = 2026-05-22, issue 105 = 2026-05-19, issue 107 = 2026-05-10 OUT-of-window, issue 109 = 2026-05-24 IN-window per the comment block). The fixture is already calendar-rollover-proof IF the test uses the pinned anchor for its `now` — which it already does for `aggregate({...now: PIN_NOW()})`. No fixture edits needed.

    DO NOT add `process.env.TEST_PIN_NOW` or any env-overridable hatch (D-08 forbids this).

    Verify after edits: `grep -nE "'2026-(05|06)-[0-9]" tests/e2e/scripts/e2e-weekly-digest.test.js` should now return ONLY the unchanged self-tests at lines ~130-139 (the `isoWeekLabel` self-tests using `'2026-01-01...'`, `'2027-01-01...'`, `'2026-05-25...'`) — no other absolute May/June date literals remain in the test file. The week-label literal `'2026-W22'` should appear ONLY in the line 139 self-test (`expect(isoWeekLabel(...)).toBe('2026-W22')`), not at assertion sites in the runtime tests.

    Commit shape (D-10, D-11): this is the THIRD commit. Stage `tests/e2e/scripts/e2e-weekly-digest.test.js` AND `scripts/weekly-digest.mjs`. Subject: `fix(48-pre-push): PRE-03 — anchor digest test to PIN_NOW_ISO with daysAgo derivation` (executor may refine the one-line per Discretion bullet 1 but must keep the `fix(48-pre-push): PRE-03 — ` prefix). Body 1-2 sentences: root cause (Phase 47's INT-FIX-CAL band-aid used live `new Date()` for the ledger month-key, making the test non-deterministic across calendar boundaries) and fix mechanism (single `PIN_NOW_ISO` anchor + `daysAgo(n)` helper drive all derived dates; `renderCostLine` gains an optional `now` param so the cost-line lookup aligns with the anchor).
  </action>
  <verify>
    <automated>grep -F "PIN_NOW_ISO = '2026-05-25T00:00:00Z'" tests/e2e/scripts/e2e-weekly-digest.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "PIN_NOW_ISO = '2026-05-25T00:00:00Z'" tests/e2e/scripts/e2e-weekly-digest.test.js` exits 0
    - `grep -nF "daysAgo" tests/e2e/scripts/e2e-weekly-digest.test.js` shows both the definition AND at least one call site (`grep -c` would be greater than or equal to 2 lines)
    - `grep -nE "renderCostLine\(\{[^}]*now" scripts/weekly-digest.mjs` exits 0 (signature accepts `now`)
    - No band-aid remains as a ledger month-key: `grep -nE "^\s*\[new Date\(\)\.toISOString\(\)\.slice\(0, *7\)\]" tests/e2e/scripts/e2e-weekly-digest.test.js` exits 1 (no matches)
    - No hardcoded `'2026-W22'` at assertion sites — only in the isoWeekLabel self-test at line ~139:
      `grep -nF "'2026-W22'" tests/e2e/scripts/e2e-weekly-digest.test.js` returns at most 1 line, and that line is inside an `it('2026-05-25 (Monday) -> expected ISO week', ...)` block testing the helper
    - No inline fixture date `'2026-05-24T00:00:00Z'` remains at line 329; replaced with `daysAgo(1)`:
      `grep -nF "created_at: '2026-05-" tests/e2e/scripts/e2e-weekly-digest.test.js` exits 1
    - The full e2e-weekly-digest test file passes: `npx vitest run tests/e2e/scripts/e2e-weekly-digest.test.js` exits 0
    - The full project unit test directory passes: `npx vitest run tests/unit tests/e2e/scripts` exits 0 with zero failures (catches accidental breakage in `renderCostLine`'s new signature affecting other tests)
    - Exactly one new commit lands with subject prefix `fix(48-pre-push): PRE-03 — ` and changes ONLY `tests/e2e/scripts/e2e-weekly-digest.test.js` and `scripts/weekly-digest.mjs`: `git log -1 --name-only --pretty=format:%s` confirms
    - Commit order check: `git log --oneline -3 --pretty=format:%s` shows PRE-03 at HEAD, PRE-01 at HEAD~1, PRE-02 at HEAD~2 (D-11 sequence)
  </acceptance_criteria>
  <done>
    The test file is anchored to a single `PIN_NOW_ISO = '2026-05-25T00:00:00Z'` constant with a `daysAgo(n)` helper; all assertion-site week labels and inline fixture dates derive from the anchor; `renderCostLine` accepts an optional `now: Date` parameter that threads through `monthlyTotal(ledger, month)`. The Phase 47 INT-FIX-CAL band-aid (live `new Date().toISOString().slice(0,7)`) is gone. The full unit + e2e-scripts test directory passes. Commit `fix(48-pre-push): PRE-03 — ...` is in HEAD per D-11 ordering.
  </done>
</task>

<task type="auto">
  <name>Task 4: PRE-04 — Verify lockfile EXACT pin (verify-first, no commit if green)</name>
  <files>tests/unit/package-lock-pinned.test.js, package.json, package-lock.json</files>
  <read_first>
    - tests/unit/package-lock-pinned.test.js (Phase 47 INT-FIX-LOCK artifact — 4 it() blocks; assertions are static-grep regexes over `package.json` and `package-lock.json`; the 4th assertion checks the resolved-URL substring `sdk-0.100.1.tgz` per `47-REVIEW.md` lines 32-51)
    - package.json line 39 (current state: `"@anthropic-ai/sdk": "0.100.1"` — verified at plan time)
    - package-lock.json (current state: `"@anthropic-ai/sdk": "0.100.1"` in both the devDependencies block and the `node_modules/@anthropic-ai/sdk` entry — verified at plan time)
    - .planning/phases/48-pre-push-regression-fixes/48-CONTEXT.md (Discretion bullet 4 — PRE-04 is verify-first: planner has already confirmed all 4 assertions GREEN; executor re-runs verification at task time and either records the PASS in SUMMARY with no commit, or — if drift is detected — adds repair steps)
    - .planning/milestones/v4.0-phases/47-v4-0-cleanup/47-REVIEW.md lines 32-51 (canonical Phase 47 INT-FIX-LOCK structure for repair-path reference)
  </read_first>
  <action>
    PRE-02, PRE-01, PRE-03 must all be in HEAD before this task starts. Verify with `git log -3 --pretty=format:%s` showing the three prior PRE-* subjects in D-11 order.

    PRE-04 is verify-first per Discretion bullet 4. At plan time, the planner ran these pre-flight checks and ALL GREEN:
    - `tests/unit/package-lock-pinned.test.js` exists (`git log --oneline -- tests/unit/package-lock-pinned.test.js` shows Phase 47 commit `33a65f3`)
    - `grep '"@anthropic-ai/sdk"' package.json` shows `"@anthropic-ai/sdk": "0.100.1"` (no caret, no tilde)
    - `grep '"@anthropic-ai/sdk"' package-lock.json` shows `"@anthropic-ai/sdk": "0.100.1"` in the devDependencies block
    - `npx vitest run tests/unit/package-lock-pinned.test.js` exits 0 with all 4 assertions passing

    Executor protocol:

    Step 1 — Re-run all four pre-flight checks at task execution time (the working tree may have drifted since planning). Capture the results.

    Step 2 — Decision branch:

      Branch A (NO DRIFT — all 4 checks GREEN): Record the verification result in the plan SUMMARY (`48-01-SUMMARY.md`) under a `## PRE-04 verify-only result` section listing the four green checks. Do NOT create a code commit. Do NOT modify any of the three files. This is the "no-op verify" path explicitly authorized by Discretion bullet 4 in CONTEXT.md ("commit may be skipped or a no-op-doc commit per Discretion bullet 4").

      Branch B (DRIFT DETECTED — any check FAILS): Construct minimal repair steps to restore the EXACT 0.100.1 pin and the static-grep test. Specifically:
        - If `package.json` shows a caret or different version: edit `package.json` line 39 to `"@anthropic-ai/sdk": "0.100.1"` (exact, no caret), then run `npm install --save-exact @anthropic-ai/sdk@0.100.1` to regenerate `package-lock.json` without re-introducing the caret.
        - If `package-lock.json` is the only drifted file: run `rm -rf node_modules package-lock.json && npm install --save-exact @anthropic-ai/sdk@0.100.1` to regenerate cleanly.
        - If `tests/unit/package-lock-pinned.test.js` has been deleted or modified to weaken assertions: restore from Phase 47 commit `33a65f3` via `git checkout 33a65f3 -- tests/unit/package-lock-pinned.test.js`.
      Then commit ONLY the repaired files. Subject: `fix(48-pre-push): PRE-04 — restore @anthropic-ai/sdk EXACT 0.100.1 pin in lockfile` with body 1-2 sentences naming what drifted and how it was restored.

    Per CONTEXT.md Out of scope: do NOT bump `@anthropic-ai/sdk` past `0.100.1` under any circumstance. The exact pin holds through v4.1.

    Per D-10, four atomic commits is the maximum — but Branch A explicitly authorizes three commits + verify-only entry. The PRE-04 SUMMARY entry is sufficient; no fourth commit is required.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/package-lock-pinned.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/package-lock-pinned.test.js` exits 0 with 4 passed / 0 failed
    - `grep -F '"@anthropic-ai/sdk": "0.100.1"' package.json` exits 0
    - `grep -F '"@anthropic-ai/sdk":' package.json` returns a line containing `"0.100.1"` with NO caret (`^`) and NO tilde (`~`): `grep -E '"@anthropic-ai/sdk":\s*"[\^~]' package.json` exits 1 (no match)
    - `grep -F '"@anthropic-ai/sdk": "0.100.1"' package-lock.json` exits 0
    - `grep -F 'sdk-0.100.1.tgz' package-lock.json` exits 0 (resolved-URL substring per Phase 47 INT-FIX-LOCK 4th assertion)
    - `grep -E '"@anthropic-ai/sdk":\s*"[\^~]' package-lock.json` exits 1 (no caret/tilde anywhere in lockfile for this package)
    - `tests/unit/package-lock-pinned.test.js` still exists and has not been weakened: `grep -c "expect(" tests/unit/package-lock-pinned.test.js` returns at least 4 (4 assertions intact)
    - Branch A path: `48-01-SUMMARY.md` contains a `## PRE-04 verify-only result` section listing the four checks as green; NO new commit beyond the three from PRE-02/PRE-01/PRE-03
    - Branch B path (only if drift detected): exactly one new commit lands with subject prefix `fix(48-pre-push): PRE-04 — ` and changes only the affected file(s) — `package.json`, `package-lock.json`, and/or `tests/unit/package-lock-pinned.test.js`
    - Phase-wide must-have (final gate per ROADMAP Phase 48 SC-1): `npm test` exits 0 with ZERO failures. This runs the full `package.json` test script: `npm run build && npm run test:src && npm run test:chrome && npm run test:firefox && npm run lint && npm run test:lint`. A faster inner-loop smoke check during execution may use `npx vitest run tests/unit tests/e2e/scripts` (subset; misses build/chrome/firefox/lint), but the phase CANNOT close until `npm test` exits 0.
  </acceptance_criteria>
  <done>
    Either (Branch A) all four pre-flight checks pass and the result is recorded in `48-01-SUMMARY.md` with no new commit, OR (Branch B) drift was detected and a single `fix(48-pre-push): PRE-04 — ...` commit restores the EXACT 0.100.1 pin and the 4-assertion test passes. Phase-wide `npm test` exits 0 with zero failures (full script: build + test:src + test:chrome + test:firefox + lint + test:lint). The four PRE-* requirements are all satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local-dev shell -> committed ledger (`tests/e2e/.llm-spend-ledger.json`) | The leaked entries arrived via this boundary: a local GSD executor imported `invokeAnthropicSdkWithLedger` without setting `E2E_LEDGER_PATH_OVERRIDE`, the LEDGER_PATH IIFE resolved to the committed file, and `forceApi:true` writes landed there. PRE-02 is the structural defense at this boundary. |
| test fixtures -> production code (`renderCostLine` `now` param) | PRE-03 adds an additive optional parameter; the boundary remains the function signature. No untrusted input crosses — `now` is always a Date constructed in the test from a hardcoded ISO constant. |
| committed repo state -> CI (Phase 49 push) | If any of the four PRE-* fixes are incomplete, CI fails on the integration PR and the push is blocked. The boundary is verified by the phase-wide `npm test` exit-0 must-have. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering | `tests/e2e/.llm-spend-ledger.json` (committed) | mitigate | PRE-02 Step 0 guard blocks the only known leak path (`forceApi:true` outside CI without `E2E_LEDGER_PATH_OVERRIDE`); PRE-01 restores the clean state; combined this closes the recurrence vector observed in v4.0 and now. |
| T-48-02 | Information Disclosure | Ledger entries with `phase=null, transport=null` | mitigate | PRE-01 deletes the 4 leaked entries that bypassed the wrapper; Test 48 asserts the single-bootstrap shape and goes green; the wrapper's required-field guards (existing `appendLedgerEntry` schema in Phase 39) prevent future malformed entries from being written. |
| T-48-03 | Repudiation | Audit trail integrity (legal-filing tool) | accept | Forward-only commits per BOOKS-01 convention; no `git commit --amend` on historical commits; the ledger reset is itself committed as a forward commit so the leak window is recorded in git history. The leak's existence and clean-up are auditable; the deletion does not erase the leak's prior commit. |
| T-48-04 | Denial of Service | CI never goes green on integration PR | mitigate | Phase-wide `npm test` exit-0 must-have is the verification anchor; PRE-01/02/03 collectively restore green; PRE-04 verifies the lockfile pin is intact so `npm install` in CI does not reintroduce drift. |
| T-48-05 | Tampering | Test contract relaxation (Test 48, package-lock-pinned) | accept | CONTEXT.md "Out of scope" explicitly locks these test assertions; the plan forbids any task that modifies `tests/unit/llm-ledger.test.js:1012` or weakens the 4 `expect()` blocks in `tests/unit/package-lock-pinned.test.js`. Pitfall 5 (contract relaxation) is the documented anti-pattern. |
| T-48-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase. PRE-04 only re-verifies the existing EXACT pin on `@anthropic-ai/sdk@0.100.1`; in the unlikely drift-repair branch, the repair uses `npm install --save-exact @anthropic-ai/sdk@0.100.1` against the already-known package — no new package legitimacy gate needed. RESEARCH.md has no `## Package Legitimacy Audit` for this phase because no new packages are introduced. |
</threat_model>

<verification>
Phase-wide verification (run after all four tasks complete):

- `npm test` exits 0 with zero failures (full script: build + test:src + test:chrome + test:firefox + lint + test:lint — ROADMAP §Phase 48 SC-1). `npx vitest run tests/unit tests/e2e/scripts` is a faster inner-loop subset but NOT the final gate.
- `jq '.months | keys | length' tests/e2e/.llm-spend-ledger.json` returns `1` (success criterion 2)
- The PRE-02 contract-error string is present in `tests/e2e/lib/llm-driver.js` (success criterion 3) and fires on the live probe described in Task 1 acceptance
- `grep -F '"@anthropic-ai/sdk": "0.100.1"' package-lock.json` exits 0 and `grep -E '"@anthropic-ai/sdk":\s*"[\^~]' package-lock.json` exits 1 (success criterion 4)
- `grep -F "PIN_NOW_ISO = '2026-05-25T00:00:00Z'" tests/e2e/scripts/e2e-weekly-digest.test.js` exits 0 AND `grep -nE "^\s*\[new Date\(\)\.toISOString\(\)\.slice\(0, *7\)\]" tests/e2e/scripts/e2e-weekly-digest.test.js` exits 1 (success criterion 5)
- Commit order check: `git log --oneline -4 --pretty=format:%s` shows (top-to-bottom, most-recent-first): optional PRE-04 (Branch B only), PRE-03, PRE-01, PRE-02 — verifying D-11 sequence

Commit-message contract check:
- Every commit subject from this phase matches `^fix\(48-pre-push\): PRE-0[1-4] — `: `git log --pretty=format:%s -- $(git log --pretty=format:%H -10) | grep -cE '^fix\(48-pre-push\): PRE-0[1-4] — '` returns the count of PRE-* commits actually landed (3 in Branch A, 4 in Branch B)

Audit checks (paranoia gate):
- No new files created outside `files_modified`: `git diff HEAD~3..HEAD --name-only` (or `HEAD~4..HEAD` in Branch B) returns only paths from `files_modified` plus possibly `tests/unit/package-lock-pinned.test.js` if Branch B restored it
- Test 48 assertion at `tests/unit/llm-ledger.test.js:1012` is byte-unchanged from HEAD before phase start: `git diff $(git log --pretty=format:%H --until='2026-06-02 18:00' -1) HEAD -- tests/unit/llm-ledger.test.js` returns empty
- `assertTripleGate` body in `scripts/auto-fix-promote.mjs` is byte-unchanged: `git diff $(git log --pretty=format:%H --until='2026-06-02 18:00' -1) HEAD -- scripts/auto-fix-promote.mjs` returns empty
</verification>

<success_criteria>
Phase 48 is complete when ALL of the following hold:

1. `npm test` exits 0 with zero failures (full script: `npm run build && npm run test:src && npm run test:chrome && npm run test:firefox && npm run lint && npm run test:lint` — mirrors ROADMAP success criterion 1 verbatim)
2. `tests/e2e/.llm-spend-ledger.json` contains exactly 1 month bucket (`2026-05`) with one bootstrap iteration; `jq '.months | keys | length'` returns `1`; no `phase: null` or `transport: null` entries (ROADMAP criterion 2)
3. `invokeAnthropicSdkWithLedger({forceApi:true,...})` outside CI throws the locked PRE-02 contract error when `E2E_LEDGER_PATH_OVERRIDE` is unset; verified by the live probe in Task 1 acceptance (ROADMAP criterion 3)
4. `package-lock.json` retains EXACT `"@anthropic-ai/sdk": "0.100.1"` with no caret; the 4-assertion `tests/unit/package-lock-pinned.test.js` passes (ROADMAP criterion 4)
5. Calendar-rollover fix uses epoch-relative fixture dates derived from a single `PIN_NOW_ISO = '2026-05-25T00:00:00Z'` constant in `tests/e2e/scripts/e2e-weekly-digest.test.js` — no hardcoded `'2026-05'` month-key band-aid, no hardcoded `'2026-W22'` literal at assertion sites (ROADMAP criterion 5)
6. Three or four atomic commits land in D-11 order (PRE-02 -> PRE-01 -> PRE-03 -> optional PRE-04 in Branch B only), each matching `^fix\(48-pre-push\): PRE-0[1-4] — `
7. No file outside `files_modified` is modified (plus `tests/unit/package-lock-pinned.test.js` only if Branch B drift-repair fires)
8. `tests/unit/llm-ledger.test.js:1012` is byte-unchanged (Test 48 locked); `scripts/auto-fix-promote.mjs` is byte-unchanged (assertTripleGate locked); `tests/unit/package-lock-pinned.test.js` is byte-unchanged in Branch A
9. Plan SUMMARY (`48-01-SUMMARY.md`) records the PRE-04 verification result (verify-only entry in Branch A; restoration steps in Branch B)
</success_criteria>

<output>
Create `.planning/phases/48-pre-push-regression-fixes/48-01-SUMMARY.md` when done. SUMMARY must include:

- One section per PRE-* requirement (`## PRE-02 — guard installed`, `## PRE-01 — ledger reset`, `## PRE-03 — epoch fixtures`, `## PRE-04 — verify-only result` or `## PRE-04 — lockfile restored`)
- For PRE-02/01/03: commit SHA, commit subject, files changed, key verification command output
- For PRE-04 Branch A: the four pre-flight check results (all green); explicit note that no commit was created per Discretion bullet 4
- For PRE-04 Branch B: commit SHA, restoration steps, drift evidence
- Phase-wide gate: `npm test` final exit code and pass/fail counts (capture the full script output, including build + chrome + firefox + lint results)
- D-11 commit-order verification: `git log --oneline -3` or `-4` output showing the locked sequence
</output>
