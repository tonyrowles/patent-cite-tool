---
phase: 51.1-required-check-trigger-hotfix
plan: 01
type: execute
wave: 1
depends_on: [50-01, 51-01]
files_modified:
  - .github/workflows/v40-verifier-gate.yml
  - .github/workflows/v40-deps-update.yml
  - .planning/scratch/trigger-hotfix-probe.txt
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-ruleset.json
  - .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-v40-verifier-gate.yml
  - .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-v40-deps-update.yml
  - .planning/phases/51.1-required-check-trigger-hotfix/evidence/bypass-readd-payload.json
  - .planning/phases/51.1-required-check-trigger-hotfix/evidence/post-bypass-readd-ruleset.json
  - .planning/phases/51.1-required-check-trigger-hotfix/evidence/yaml-diff.patch
  - .planning/phases/51.1-required-check-trigger-hotfix/evidence/direct-push-log.txt
  - .planning/phases/51.1-required-check-trigger-hotfix/evidence/verification-pr-checks.json
  - .planning/phases/51.1-required-check-trigger-hotfix/evidence/verification-pr-state.json
  - .planning/phases/51.1-required-check-trigger-hotfix/evidence/bypass-remove-payload.json
  - .planning/phases/51.1-required-check-trigger-hotfix/evidence/final-ruleset.json
  - .planning/phases/51.1-required-check-trigger-hotfix/evidence/INDEX.md
  - .planning/phases/51.1-required-check-trigger-hotfix/51.1-01-SUMMARY.md
autonomous: false
requirements_addressed: [REGRESSION-51-01]
tags: [github-state, ruleset, workflow-trigger, hotfix, break-glass, security-hardening]
user_setup: []

must_haves:
  truths:
    - "SC-1 / D-01 / D-03: .github/workflows/v40-verifier-gate.yml `on.pull_request` no longer carries the BASE-ref filter `branches: ['auto-fix/*']`; the workflow fires on every PR into main. A scope-decision step (text verbatim per D-03) is the first step of each of the three PR-gate jobs (`verifier-gate`, `regression-suite`, `ready-flip`) and subsequent steps within those jobs are guarded by `if: steps.scope.outputs.active == 'true' || github.event_name != 'pull_request'`. The `diff-guard` job is intentionally unguarded (universal LOCKED-path check)."
    - "SC-2 / D-02 / D-04: .github/workflows/v40-deps-update.yml `on:` block gains a `pull_request: { types: [opened, synchronize, reopened] }` trigger as a sibling of the existing `schedule:` and `workflow_dispatch:` keys. The `deps-update-gate` job receives the same scope-decision step (head-ref prefix `v40-deps-update/*`) at the top of its `steps:` block; subsequent steps are guarded. The `dep-scan` job is unchanged (PR-creator, not PR-gate)."
    - "SC-3 / D-07 / D-15: A verification test PR opened on branch `test-only/trigger-hotfix-verify-${ts}` (probe file `.planning/scratch/trigger-hotfix-probe.txt`) shows BOTH `verifier-gate` AND `deps-update-gate` contexts present in `gh pr checks --json name` output (whether they pass or fail is secondary; what matters is they FIRE). The PR is CLOSED + branch deleted immediately after evidence capture. Captured to `evidence/verification-pr-checks.json` + `verification-pr-state.json`."
    - "SC-4 / D-07: The verification PR is CLOSED (not merged); the throwaway branch is deleted from origin; the probe file never appears on main; no LOCKED-path mutation is committed (mirrors UAT-47-e / GATE-03 convention)."
    - "SC-5 / D-05 / D-08: Ruleset 17086676 ends Phase 51.1 with `bypass_actors=[]` and `current_user_can_bypass=\"never\"`, matching Phase 50 closure baseline (`final-ruleset.json`). The bypass actor (id=254599900) is added by Task 2 via lighter PUT body `{\"bypass_actors\":[{\"actor_id\":254599900,\"actor_type\":\"User\",\"bypass_mode\":\"always\"}]}` and removed by Task 6 via lighter PUT body `{\"bypass_actors\":[]}` (D-08 — NO `rules` key, jq-guarded; Phase 50 5-rule set preserved untouched)."
    - "D-09 / D-10: Existing Vitest D11/D12 from Phase 50 (`tests/unit/v40-verifier-gate-doc.test.js`) remain valid after the YAML edits because the jobid declarations (`  verifier-gate:` at indent 2, `  deps-update-gate:` at indent 2) are unchanged. If `npx vitest run tests/unit/v40-verifier-gate-doc.test.js` reports drift, Task 3 STOPS and the rename is rejected (D-10)."
    - "D-11 / D-12 / D-06: A single plan (this file) with 7 sequential tasks in the LOCKED order (baselines → break-glass add → YAML edits → direct push to main [D-06: no PR — would Catch-22; single combined commit covers both YAML edits + any Vitest test updates] → verification PR → break-glass remove → SUMMARY+STATE+ROADMAP); each completed task lands as one atomic `chore(51.1): <one-line>` commit (D-12)."
    - "D-13 / D-14: Tasks 2, 4, 6 are `type: checkpoint:human-verify gate=blocking` (each LIVE ruleset / main mutation requires explicit operator approval). Tasks 1, 3, 5, 7 are `type: auto`."
    - "D-15: Per-task evidence files are produced in `.planning/phases/51.1-required-check-trigger-hotfix/evidence/` matching the 11-file manifest (pre-hotfix-ruleset.json, pre-hotfix-v40-verifier-gate.yml, pre-hotfix-v40-deps-update.yml, bypass-readd-payload.json, post-bypass-readd-ruleset.json, yaml-diff.patch, direct-push-log.txt, verification-pr-checks.json, verification-pr-state.json, bypass-remove-payload.json, final-ruleset.json) plus an INDEX.md role-map and the 51.1-01-SUMMARY.md closure narrative."
    - "D-16: STATE.md §Pending Todos Phase 56 entry is AMENDED (not appended) by Task 7 — the sub-item `verifier-gate trigger patch` is removed while the other Phase 56 sub-items (`ledger-commit refactor`, `fixture-mutator authoring`, `4-UAT re-sweep`) are preserved verbatim. ROADMAP.md Phase 51.1 row checkbox flips `[ ]` → `[x]` with completion date."
  artifacts:
    - path: ".github/workflows/v40-verifier-gate.yml"
      provides: "Trigger fixed: BASE-ref filter removed; scope-decision step prepended to 3 PR-gate jobs; subsequent steps guarded"
      contains: "Scope decision (auto-fix/* PRs only; fast-path SUCCESS otherwise)"
    - path: ".github/workflows/v40-deps-update.yml"
      provides: "pull_request trigger added; scope-decision step prepended to deps-update-gate; subsequent steps guarded"
      contains: "Scope decision"
    - path: ".planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-ruleset.json"
      provides: "Live ruleset state BEFORE break-glass — must match Phase 50 final-ruleset.json baseline (5 rules, 0 bypass actors)"
    - path: ".planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-v40-verifier-gate.yml"
      provides: "Frozen snapshot of verifier-gate.yml BEFORE Task 3 edits — diff reference for yaml-diff.patch"
    - path: ".planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-v40-deps-update.yml"
      provides: "Frozen snapshot of deps-update.yml BEFORE Task 3 edits"
    - path: ".planning/phases/51.1-required-check-trigger-hotfix/evidence/bypass-readd-payload.json"
      provides: "Task 2 PUT body — re-adds operator bypass actor for hotfix window"
      contains: "254599900"
    - path: ".planning/phases/51.1-required-check-trigger-hotfix/evidence/post-bypass-readd-ruleset.json"
      provides: "Post-Task-2 ruleset GET — 1 bypass actor, 5 rules preserved, current_user_can_bypass=always"
    - path: ".planning/phases/51.1-required-check-trigger-hotfix/evidence/yaml-diff.patch"
      provides: "`git diff HEAD~1 HEAD -- .github/workflows/v40-*.yml` after Task 3 commit — readable diff of the trigger fix"
    - path: ".planning/phases/51.1-required-check-trigger-hotfix/evidence/direct-push-log.txt"
      provides: "Task 4 record: pre-push HEAD SHA, `git push origin main` stdout/stderr, post-push `git ls-remote origin main` confirming the SHA landed"
    - path: ".planning/phases/51.1-required-check-trigger-hotfix/evidence/verification-pr-checks.json"
      provides: "Task 5 `gh pr checks --json name,state,bucket` output showing BOTH verifier-gate AND deps-update-gate contexts fire on the verification PR"
      contains: "verifier-gate"
    - path: ".planning/phases/51.1-required-check-trigger-hotfix/evidence/verification-pr-state.json"
      provides: "Task 5 `gh pr view --json state,mergedAt,headRefName` showing state=CLOSED, mergedAt=null, branch=test-only/trigger-hotfix-verify-*"
    - path: ".planning/phases/51.1-required-check-trigger-hotfix/evidence/bypass-remove-payload.json"
      provides: "Task 6 PUT body — lighter `{\"bypass_actors\":[]}` shape, no `rules` key (jq-guarded)"
    - path: ".planning/phases/51.1-required-check-trigger-hotfix/evidence/final-ruleset.json"
      provides: "Closing snapshot — must match Phase 50 final-ruleset.json byte-for-byte on rules+bypass_actors+current_user_can_bypass keys"
      contains: "current_user_can_bypass"
    - path: ".planning/phases/51.1-required-check-trigger-hotfix/evidence/INDEX.md"
      provides: "Per-evidence-file role map (Phase 50 convention)"
    - path: ".planning/phases/51.1-required-check-trigger-hotfix/51.1-01-SUMMARY.md"
      provides: "Phase closure narrative + 5-SC mapping + deviations + Phase 56 amendment"
  key_links:
    - from: ".github/workflows/v40-verifier-gate.yml on.pull_request"
      to: "GitHub Actions check-runs on PRs into main"
      via: "removed BASE-ref filter `branches:['auto-fix/*']` → workflow now triggers on every PR; scope-decision fast-paths non-auto-fix PRs to SUCCESS"
      pattern: "scope-decision step + steps.scope.outputs.active gate"
    - from: ".github/workflows/v40-deps-update.yml on"
      to: "GitHub Actions check-runs on PRs into main"
      via: "added `pull_request:` trigger as sibling of schedule/workflow_dispatch"
      pattern: "pull_request:\\s*\\n\\s*types: \\[opened, synchronize, reopened\\]"
    - from: "evidence/bypass-readd-payload.json"
      to: "Ruleset 17086676 (live GitHub state — Task 2 LIVE mutation)"
      via: "gh api -X PUT --input (no -X PATCH)"
      pattern: "PUT /repos/tonyrowles/patent-cite-tool/rulesets/17086676"
    - from: "evidence/bypass-remove-payload.json"
      to: "Ruleset 17086676 (live GitHub state — Task 6 LIVE mutation)"
      via: "gh api -X PUT --input with lighter body (no rules key)"
      pattern: "jq -e 'has(\"rules\") | not'"
    - from: "Task 4 local commit (YAML edits)"
      to: "origin/main"
      via: "git push origin main (no PR — bypass actor permits direct push)"
      pattern: "git ls-remote origin main"
    - from: "Task 5 verification PR"
      to: "evidence/verification-pr-checks.json"
      via: "12-min poll loop on `gh pr checks --json name` until both verifier-gate and deps-update-gate names appear"
      pattern: "verifier-gate|deps-update-gate"
    - from: "tests/unit/v40-verifier-gate-doc.test.js D11/D12 (Phase 50)"
      to: "post-edit verifier-gate.yml + deps-update.yml jobid declarations"
      via: "regex /^\\s{2}verifier-gate:\\s*$/m and /^\\s{2}deps-update-gate:\\s*$/m"
      pattern: "  verifier-gate:|  deps-update-gate:"
    - from: ".planning/STATE.md §Pending Todos Phase 56 line"
      to: "(amended in place)"
      via: "Task 7 sed/Edit replacing the verifier-gate sub-item out, preserving ledger-commit + fixture-mutator + 4-UAT re-sweep"
      pattern: "Phase 56 \\(v4.2 backlog\\)"
---

<objective>
Close REGRESSION-51-01 by making `.github/workflows/v40-verifier-gate.yml` and `.github/workflows/v40-deps-update.yml` actually fire on PRs into `main`, while preserving Phase 50's `bypass_actors=[]` end-state on ruleset 17086676. The fix uses approach α (per CONTEXT D-01): remove the BASE-ref filter on verifier-gate.yml, add a `pull_request` trigger to deps-update.yml, and prepend a verbatim scope-decision step (D-03) to each PR-gate job so non-matching head refs fast-path SUCCESS while `auto-fix/*` / `v40-deps-update/*` head refs still receive the heavy gate. Phase 50's break-glass §7 runbook is exercised end-to-end for the first time: bypass actor re-added (Task 2), fix committed and pushed directly to main (Tasks 3–4), verification PR proves both checks now fire (Task 5), bypass actor removed (Task 6), closure (Task 7) — net Phase 50 SC-2 end-state preserved.

Purpose: Unjams every PR into `main` (REGRESSION-51-01 was structurally blocking Phases 52–55) without weakening Phase 50's trust enforcement.

Output: Two YAML edits on origin/main, one verification PR (CLOSED + branch deleted), 11 per-task evidence files + INDEX, one closure SUMMARY, STATE.md Phase 56 amendment, ROADMAP.md Phase 51.1 closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51.1-required-check-trigger-hotfix/51.1-CONTEXT.md
@.planning/phases/51-live-readiness-uats/51-VERIFICATION.md
@.planning/phases/51-live-readiness-uats/51-01-SUMMARY.md
@.planning/phases/50-cleanup-04-readiness-gate/50-01-PLAN.md
@.planning/phases/50-cleanup-04-readiness-gate/evidence/final-ruleset.json
@docs/v40-repo-config.md
@.github/workflows/v40-verifier-gate.yml
@.github/workflows/v40-deps-update.yml
@tests/unit/v40-verifier-gate-doc.test.js

<interfaces>
<!-- Live GitHub state at plan time (Phase 50 closure baseline, verified 2026-06-03 in evidence/final-ruleset.json): -->
<!-- Ruleset 17086676: 5 rules (deletion, non_fast_forward, required_linear_history, pull_request, required_status_checks) + 0 bypass actors + enforcement=active + current_user_can_bypass=never. -->
<!-- required_status_checks contains contexts: verifier-gate + deps-update-gate, both integration_id=15368. -->
<!-- Operator bypass actor id (Phase 50 §7 break-glass payload): 254599900 (User, bypass_mode=always). -->

<!-- Workflow YAML jobid declarations (lines verified against committed files at plan time): -->
<!--   - .github/workflows/v40-verifier-gate.yml:181 → `  verifier-gate:` (2-space indent, no `name:` override) — KEEP UNCHANGED through Task 3 -->
<!--   - .github/workflows/v40-deps-update.yml:164 → `  deps-update-gate:` (2-space indent, no `name:` override) — KEEP UNCHANGED through Task 3 -->

<!-- Trigger blocks that Task 3 mutates: -->
<!-- v40-verifier-gate.yml lines 44-47 current shape: -->
<!--   on: -->
<!--     pull_request: -->
<!--       types: [opened, synchronize, reopened] -->
<!--       branches: ['auto-fix/*']           # <-- line 47 — DELETE this line -->
<!-- -->
<!-- v40-deps-update.yml lines 47-50 current shape: -->
<!--   on: -->
<!--     schedule: -->
<!--       - cron: '0 9 * * 1' -->
<!--     workflow_dispatch: {} -->
<!-- Task 3 inserts a `pull_request:` block as the FIRST key under `on:` (before `schedule:`). -->

<!-- Scope-decision step text (D-03 LOCKED VERBATIM — must be reproduced byte-for-byte in YAML): -->
<!-- ```yaml -->
<!-- - name: Scope decision (auto-fix/* PRs only; fast-path SUCCESS otherwise) -->
<!--   id: scope -->
<!--   if: github.event_name == 'pull_request' -->
<!--   run: | -->
<!--     if [[ "${{ github.head_ref }}" == auto-fix/* ]]; then -->
<!--       echo "active=true" >> "$GITHUB_OUTPUT" -->
<!--     else -->
<!--       echo "active=false" >> "$GITHUB_OUTPUT" -->
<!--       echo "Fast-path SUCCESS — non-auto-fix PR (head=${{ github.head_ref }})" -->
<!--     fi -->
<!-- ``` -->
<!-- For deps-update-gate, the head-ref check changes to `v40-deps-update/*` and the log line to "non-v40-deps-update PR" (D-04). -->

<!-- Gate clause for subsequent steps in each guarded job: -->
<!--   if: steps.scope.outputs.active == 'true' || github.event_name != 'pull_request' -->
<!-- (the OR-clause preserves backward-compat for workflow_dispatch invocations even though verifier-gate currently only has pull_request — guard against future on: additions). -->

<!-- REST verb (Phase 50 PITFALLS): All ruleset mutations use `gh api -X PUT` (NOT -X PATCH). PATCH returns HTTP 404. -->
<!-- Lighter PUT bodies (Phase 50 Pattern 2): the `bypass_actors` PUTs in Tasks 2 and 6 OMIT the `rules` key so the live 5 rules are preserved untouched. jq guard: `jq -e 'has("rules") | not' < payload.json`. -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → GitHub ruleset 17086676 | gh api -X PUT mutates required_status_checks + bypass_actors |
| operator CLI → origin/main | direct `git push origin main` while bypass actor is present (Task 4 only) |
| auto-fix/* PR → verifier-gate workflow | scope-decision step activates heavy gate; non-matching head refs fast-path SUCCESS |
| dependency PR (v40-deps-update/*) → deps-update-gate workflow | same scope-decision pattern |

## STRIDE Threat Register

| Threat ID    | Category     | Component                                                                | Severity | Disposition | Mitigation Plan                                                                                                                                                                                                                              |
|--------------|--------------|--------------------------------------------------------------------------|----------|-------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| T-51.1-01    | Tampering    | `.github/workflows/v40-verifier-gate.yml` + `v40-deps-update.yml`        | HIGH     | mitigate    | Task 3 removes the BASE-ref filter and adds the `pull_request` trigger so both required check providers fire on PRs into main; Task 5 verification PR captures `gh pr checks --json name` showing both contexts present → REGRESSION-51-01 closed. |
| T-51.1-02    | Elevation of Privilege | Operator bypass actor (id 254599900) on ruleset 17086676           | MEDIUM   | mitigate    | Time-box: actor is added in Task 2 and removed in Task 6 within the same operator session. Only Task 4 (direct push) and Task 5 (verification PR) execute within the window. Task 6 verifies `current_user_can_bypass=never` post-removal.    |
| T-51.1-03    | Repudiation  | Phase 50 SC-2 (`bypass_actors=[]`) regression                            | MEDIUM   | mitigate    | Task 6 captures `final-ruleset.json` and asserts byte-equality with Phase 50's `final-ruleset.json` on the keys `bypass_actors` and `current_user_can_bypass`. Lighter PUT body (`{"bypass_actors":[]}`) is jq-guarded against `rules` key.  |
| T-51.1-04    | Tampering    | Ruleset 17086676 `required_status_checks` rule (Phase 50 SC-1)           | MEDIUM   | mitigate    | Lighter PUT bodies in Tasks 2 and 6 omit `rules` key (Phase 50 Pattern 2 partial-body merge). Task 6 post-state assertion confirms 5 rules preserved + both contexts present + integration_id=15368 on both.                                  |
| T-51.1-05    | Tampering    | `tests/unit/v40-verifier-gate-doc.test.js` D11/D12 jobid pins (Phase 50) | MEDIUM   | mitigate    | Task 3 sub-step asserts `npx vitest run tests/unit/v40-verifier-gate-doc.test.js` exits 0 AFTER the YAML edits. The jobid lines `  verifier-gate:` and `  deps-update-gate:` MUST remain byte-unchanged. Per D-10, a drift signal triggers a STOP-not-relax decision. |
| T-51.1-06    | Repudiation  | Direct push to main (Catch-22 escape)                                    | MEDIUM   | mitigate    | Task 4 captures pre-push `git rev-parse HEAD`, push stdout/stderr, and post-push `git ls-remote origin main` to `evidence/direct-push-log.txt`. The push is operator-gated (checkpoint:human-verify). No --force; no rebase.                  |

Severity rationale: T-51.1-01 is HIGH because every PR into main is currently blocked (operationally LIVE since Phase 50 closure at 2026-06-03T21:34:18Z). T-51.1-02…06 are MEDIUM because they are bounded (operator session, defined revert path, captured baselines).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 01: Capture pre-hotfix baselines (ruleset + 2 YAML files)</name>
  <files>
    .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-ruleset.json
    .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-v40-verifier-gate.yml
    .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-v40-deps-update.yml
  </files>
  <read_first>
    .planning/phases/51.1-required-check-trigger-hotfix/51.1-CONTEXT.md (D-04, D-15 baseline artifact list)
    .planning/phases/50-cleanup-04-readiness-gate/evidence/final-ruleset.json (Phase 50 closure baseline — what pre-hotfix-ruleset.json MUST equal on rules+bypass_actors+current_user_can_bypass keys)
    .github/workflows/v40-verifier-gate.yml (full file — the snapshot being frozen)
    .github/workflows/v40-deps-update.yml (full file — the snapshot being frozen)
  </read_first>
  <action>
    Three sub-steps in ONE atomic commit. Commit message: `chore(51.1): pre-hotfix baselines (ruleset + 2 YAML snapshots)` (D-12).

    Sub-step 1a — Confirm the evidence directory exists and the working tree is clean:
      mkdir -p .planning/phases/51.1-required-check-trigger-hotfix/evidence
      [[ -z "$(git status --porcelain)" ]] || { echo "FAIL: working tree dirty — stash or commit before proceeding"; exit 1; }
      git rev-parse --abbrev-ref HEAD | grep -qx main || { echo "FAIL: not on main branch — checkout main first"; exit 1; }

    Sub-step 1b — Capture live ruleset baseline (read-only GET):
      gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 \
        > .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-ruleset.json
      # Local sanity assertions BEFORE proceeding — MUST match Phase 50 closure (final-ruleset.json):
      PRE=.planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-ruleset.json
      jq -e '.rules | length == 5'                       "$PRE" || { echo "FAIL: rules count drifted from Phase 50 baseline of 5"; exit 1; }
      jq -e '.bypass_actors | length == 0'               "$PRE" || { echo "FAIL: bypass_actors drifted from Phase 50 closure (expected [])"; exit 1; }
      jq -e '.current_user_can_bypass == "never"'        "$PRE" || { echo "FAIL: current_user_can_bypass drifted (expected never)"; exit 1; }
      jq -e '.enforcement == "active"'                   "$PRE" || { echo "FAIL: enforcement not active"; exit 1; }
      jq -e '[.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context] | sort == ["deps-update-gate","verifier-gate"]' "$PRE" \
        || { echo "FAIL: required contexts drifted from verifier-gate + deps-update-gate"; exit 1; }
      jq -e '.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].integration_id | . == 15368' "$PRE" \
        || { echo "FAIL: integration_id drifted from 15368 (Phase 50 SC-1)"; exit 1; }
      # If ANY assertion fails, STOP. The ruleset has drifted since Phase 50 closure and a planning-level investigation is required before Phase 51.1 can safely proceed.

    Sub-step 1c — Freeze YAML snapshots verbatim (these are the diff reference for Task 3's yaml-diff.patch):
      cp .github/workflows/v40-verifier-gate.yml \
         .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-v40-verifier-gate.yml
      cp .github/workflows/v40-deps-update.yml \
         .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-v40-deps-update.yml
      # Sanity check: frozen snapshots contain the known broken patterns:
      grep -qE "^\s+branches: \['auto-fix/\*'\]" .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-v40-verifier-gate.yml \
        || { echo "FAIL: pre-hotfix verifier-gate.yml snapshot missing the broken branches: filter — file may have been edited already; ABORT"; exit 1; }
      grep -q "pull_request:" .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-v40-deps-update.yml \
        && { echo "FAIL: pre-hotfix deps-update.yml already has pull_request trigger — file may have been edited already; ABORT"; exit 1; }
      grep -q "schedule:" .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-v40-deps-update.yml \
        || { echo "FAIL: pre-hotfix deps-update.yml missing the expected schedule: key; aborting"; exit 1; }

    Sub-step 1d — Commit baselines atomically:
      git add .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-ruleset.json \
              .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-v40-verifier-gate.yml \
              .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-v40-deps-update.yml
      git commit -m "chore(51.1): pre-hotfix baselines (ruleset + 2 YAML snapshots)"

    Notes:
      - No live mutation in this task (read-only GET + filesystem writes + local commit). autonomous=true.
      - This commit lands on the LOCAL main branch only; it is NOT pushed yet. Task 4's `git push origin main` will land Task 3's edits PLUS this Task 1 commit PLUS any other intervening commits in one push.
      - The baselines are the source of truth for `evidence/yaml-diff.patch` (Task 3) and the Task 6 byte-equality assertion against Phase 50's final-ruleset.json.
  </action>
  <verify>
    <automated>
      jq -e '.rules | length == 5 and (.bypass_actors | length == 0) and .current_user_can_bypass == "never" and .enforcement == "active"' .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-ruleset.json &&
      jq -e '[.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context] | sort == ["deps-update-gate","verifier-gate"]' .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-ruleset.json &&
      test -s .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-v40-verifier-gate.yml &&
      test -s .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-v40-deps-update.yml &&
      grep -qE "branches: \['auto-fix/\*'\]" .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-v40-verifier-gate.yml &&
      ! grep -q "pull_request:" .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-v40-deps-update.yml &&
      git log -1 --pretty=%s | grep -qE '^chore\(51\.1\): pre-hotfix baselines'
    </automated>
  </verify>
  <acceptance_criteria>
    - `evidence/pre-hotfix-ruleset.json` exists; jq asserts 5 rules, 0 bypass actors, current_user_can_bypass="never", enforcement="active".
    - Required status checks contexts sort to exactly `["deps-update-gate","verifier-gate"]`; integration_id=15368 on both.
    - `evidence/pre-hotfix-v40-verifier-gate.yml` is non-empty and contains the broken `branches: ['auto-fix/*']` line (proves we froze the BEFORE state).
    - `evidence/pre-hotfix-v40-deps-update.yml` is non-empty and does NOT contain the string `pull_request:` (proves we froze the BEFORE state).
    - Three files ship in ONE commit whose subject matches `^chore\(51\.1\): pre-hotfix baselines`.
    - Working tree clean after commit (`git status --porcelain` empty).
    - Branch is `main` and the commit is local-only (not pushed yet — Task 4 owns the push).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 02: Break-glass — PUT bypass actor BACK on ruleset 17086676 (re-open bypass window)</name>
  <files>
    .planning/phases/51.1-required-check-trigger-hotfix/evidence/bypass-readd-payload.json
    .planning/phases/51.1-required-check-trigger-hotfix/evidence/post-bypass-readd-ruleset.json
  </files>
  <read_first>
    .planning/phases/51.1-required-check-trigger-hotfix/51.1-CONTEXT.md (D-05 — break-glass actor id, D-13 — checkpoint placement)
    docs/v40-repo-config.md §7(a) (Re-add the operator bypass actor — the runbook this task is exercising)
    .planning/phases/50-cleanup-04-readiness-gate/50-01-PLAN.md Task 04 (Phase 50 PUT pattern — lighter body, no rules key, idempotent semantics)
    .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-ruleset.json (Task 1 baseline — proves pre-state == Phase 50 closure)
  </read_first>
  <action>
    AUTOMATED WORK FIRST (the PUT executes, evidence is captured, post-state assertions run). THEN the operator-approval checkpoint fires before Task 03 proceeds. Commit message at task end: `chore(51.1): break-glass — re-add operator bypass actor (hotfix window OPEN)` (D-12).

    Sub-step 2a — Construct the lighter PUT body verbatim per docs §7(a) (D-05):
      PAYLOAD=.planning/phases/51.1-required-check-trigger-hotfix/evidence/bypass-readd-payload.json
      cat > "$PAYLOAD" <<'JSON'
      {"bypass_actors":[{"actor_id":254599900,"actor_type":"User","bypass_mode":"always"}]}
      JSON
      # Hard guard — the lighter body MUST NOT contain a `rules` key (Phase 50 Pattern 2 partial-body merge — `rules` key omitted preserves the live 5 rules):
      jq -e 'has("rules") | not' "$PAYLOAD" \
        || { echo "FAIL: bypass-readd-payload.json contains rules key — would clobber the 5 rules"; exit 1; }
      jq -e '.bypass_actors | length == 1 and .bypass_actors[0].actor_id == 254599900 and .bypass_actors[0].actor_type == "User" and .bypass_actors[0].bypass_mode == "always"' "$PAYLOAD" \
        || { echo "FAIL: bypass-readd-payload.json malformed — expected single actor 254599900 / User / always"; exit 1; }

    Sub-step 2b — Apply the PUT (LIVE MUTATION — first ruleset audit-log entry of Phase 51.1):
      gh api -X PUT /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --input "$PAYLOAD"
      # Capture post-state IMMEDIATELY for evidence + assertions:
      gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 \
        > .planning/phases/51.1-required-check-trigger-hotfix/evidence/post-bypass-readd-ruleset.json

    Sub-step 2c — Hard post-state assertions (any failure → STOP, do NOT proceed to checkpoint; operator must investigate):
      POST=.planning/phases/51.1-required-check-trigger-hotfix/evidence/post-bypass-readd-ruleset.json
      jq -e '.bypass_actors | length == 1'                          "$POST" || { echo "FAIL: bypass_actors length != 1 after PUT"; exit 1; }
      jq -e '.bypass_actors[0].actor_id == 254599900'               "$POST" || { echo "FAIL: bypass actor_id wrong"; exit 1; }
      jq -e '.bypass_actors[0].actor_type == "User"'                "$POST" || { echo "FAIL: bypass actor_type wrong"; exit 1; }
      jq -e '.bypass_actors[0].bypass_mode == "always"'             "$POST" || { echo "FAIL: bypass_mode wrong"; exit 1; }
      jq -e '.current_user_can_bypass == "always"'                  "$POST" || { echo "FAIL: current_user_can_bypass not flipped to always (expected for the operator)"; exit 1; }
      # Rule preservation — the 5 Phase 50 rules MUST still be intact (Pattern 2 partial-body merge):
      jq -e '.rules | length == 5'                                  "$POST" || { echo "FAIL: rules count drifted from 5 — Pattern 2 merge failed"; exit 1; }
      jq -e '[.rules[].type] | sort == ["deletion","non_fast_forward","pull_request","required_linear_history","required_status_checks"]' "$POST" \
        || { echo "FAIL: rule type set drifted"; exit 1; }
      jq -e '[.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context] | sort == ["deps-update-gate","verifier-gate"]' "$POST" \
        || { echo "FAIL: required contexts drifted"; exit 1; }
      jq -e '.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].integration_id | . == 15368' "$POST" \
        || { echo "FAIL: integration_id drifted from 15368"; exit 1; }

    Sub-step 2d — Stage evidence and commit:
      git add .planning/phases/51.1-required-check-trigger-hotfix/evidence/bypass-readd-payload.json \
              .planning/phases/51.1-required-check-trigger-hotfix/evidence/post-bypass-readd-ruleset.json
      git commit -m "chore(51.1): break-glass — re-add operator bypass actor (hotfix window OPEN)"

    THEN the operator-approval checkpoint fires (Task 4 — the actual `git push origin main` — happens later; Task 3 is autonomous YAML edits but the operator should still confirm the bypass window is intentional before any further work proceeds).
  </action>
  <what-built>
    Ruleset 17086676 now carries 1 bypass actor (operator self, id 254599900, bypass_mode=always). The 5 Phase 50 rules and required_status_checks contexts are preserved untouched (Pattern 2 partial-body merge — lighter PUT, no `rules` key). The break-glass §7(a) runbook has been exercised end-to-end for the first time in production. Evidence: `bypass-readd-payload.json` (the PUT body) + `post-bypass-readd-ruleset.json` (the GET that proves the post-state) + the atomic `chore(51.1):` commit.
  </what-built>
  <how-to-verify>
    1. Confirm the LIVE ruleset state (definitive truth — independent of the captured JSON):
       gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --jq '{rules_count: (.rules | length), bypass_count: (.bypass_actors | length), actor_id: (.bypass_actors[0]?.actor_id // null), mode: (.bypass_actors[0]?.bypass_mode // null), can_bypass: .current_user_can_bypass, contexts: [.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context]}'
       Expected (exact): `{"rules_count":5,"bypass_count":1,"actor_id":254599900,"mode":"always","can_bypass":"always","contexts":["verifier-gate","deps-update-gate"]}` (contexts order may vary; sort to confirm).
    2. Confirm the lighter PUT body did NOT clobber rules (Pattern 2 test):
       gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --jq '[.rules[].type] | sort'
       Expected: `["deletion","non_fast_forward","pull_request","required_linear_history","required_status_checks"]`
    3. Inspect git log:
       git log -1 --pretty='%H %s' --stat
       Expected: ONE commit modifying both `evidence/bypass-readd-payload.json` AND `evidence/post-bypass-readd-ruleset.json`.
    4. If anything looks wrong, IMMEDIATELY run the §7(b) remove command to restore Phase 50 closure state BEFORE approving:
         gh api -X PUT /repos/tonyrowles/patent-cite-tool/rulesets/17086676 \
           --input <(echo '{"bypass_actors":[]}')
       Then reply "rolled back, abort phase" instead of "approved".
  </how-to-verify>
  <resume-signal>Type "approved" once the live ruleset shows bypass_actors length=1 with actor 254599900 and current_user_can_bypass=always, the 5 rules are preserved, and both required contexts remain pinned to integration_id=15368. Type "rolled back" if you executed §7(b) to abort. Type "dry-run" if you want to review the planned Task 3 + Task 4 commands before opening the bypass window further. Type a description of the issue otherwise.</resume-signal>
  <acceptance_criteria>
    - `evidence/bypass-readd-payload.json` exists; `jq -e 'has("rules") | not'` passes (D-08 lighter-body guard).
    - `evidence/bypass-readd-payload.json` contains exactly one bypass actor with actor_id=254599900, actor_type="User", bypass_mode="always".
    - `evidence/post-bypass-readd-ruleset.json` shows: `.bypass_actors | length == 1`, `.bypass_actors[0].actor_id == 254599900`, `.bypass_actors[0].bypass_mode == "always"`, `.current_user_can_bypass == "always"`.
    - The 5 Phase 50 rules are preserved: `.rules | length == 5` and the type set matches `["deletion","non_fast_forward","pull_request","required_linear_history","required_status_checks"]`.
    - Required contexts `verifier-gate` + `deps-update-gate` remain present with `integration_id == 15368` on both.
    - One atomic commit subject matches `^chore\(51\.1\): break-glass — re-add operator bypass actor`.
    - Operator has confirmed via "approved" before Task 03 starts.
    - No `-X PATCH` appears in any executed `gh api` command (only `-X PUT`).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 03: Apply YAML edits (verifier-gate.yml BASE-filter removal + scope-decision; deps-update.yml pull_request trigger + scope-decision)</name>
  <files>
    .github/workflows/v40-verifier-gate.yml
    .github/workflows/v40-deps-update.yml
    .planning/phases/51.1-required-check-trigger-hotfix/evidence/yaml-diff.patch
  </files>
  <read_first>
    .planning/phases/51.1-required-check-trigger-hotfix/51.1-CONTEXT.md (D-01, D-02, D-03, D-04, D-09, D-10 — full LITERAL edit specification)
    .github/workflows/v40-verifier-gate.yml (current full file — lines 44-47 `on:` block; line 66 `diff-guard:` job; line 181 `verifier-gate:` job; line 307 `regression-suite:` job; line 384 `ready-flip:` job)
    .github/workflows/v40-deps-update.yml (current full file — lines 47-50 `on:` block; line 69 `dep-scan:` job; line 164 `deps-update-gate:` job)
    .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-v40-verifier-gate.yml (Task 1 frozen snapshot — Task 3 diffs against this)
    .planning/phases/51.1-required-check-trigger-hotfix/evidence/pre-hotfix-v40-deps-update.yml (Task 1 frozen snapshot)
    tests/unit/v40-verifier-gate-doc.test.js (D11/D12 jobid pins — Task 3 sub-step asserts vitest still passes after the edits)
  </read_first>
  <action>
    Six sub-steps in ONE atomic commit. Commit message: `chore(51.1): fix verifier-gate + deps-update-gate triggers (BASE-filter removed; pull_request added; scope-decision fast-path)` (D-12).

    The literal edit specifications below are LOCKED — they reproduce CONTEXT D-01/D-02/D-03/D-04 byte-for-byte. Do NOT paraphrase. The scope-decision step text is reproduced verbatim from D-03 (and D-04 for the deps-update variant).

    Sub-step 3a — Edit `.github/workflows/v40-verifier-gate.yml`:

      EDIT 1 (D-01): DELETE line 47 (`    branches: ['auto-fix/*']`). The `on.pull_request:` block goes from:

        on:
          pull_request:
            types: [opened, synchronize, reopened]
            branches: ['auto-fix/*']

      To:

        on:
          pull_request:
            types: [opened, synchronize, reopened]

      Use the Edit tool with `old_string` being the 4-line current block including `    branches: ['auto-fix/*']` and `new_string` being the 3-line block without that line. The blank line that previously followed line 47 (separating `on:` from `permissions:`) is preserved.

      EDIT 2 (D-03): INSERT the scope-decision step at the TOP of each of THREE jobs' `steps:` block. The three jobs are `verifier-gate:` (line 181), `regression-suite:` (line 307), and `ready-flip:` (line 384). The `diff-guard:` job (line 66) is INTENTIONALLY NOT touched (it is the universal LOCKED-path check per CONTEXT discretion area).

      The step text (LOCKED VERBATIM per D-03) for verifier-gate / regression-suite / ready-flip:

        - name: Scope decision (auto-fix/* PRs only; fast-path SUCCESS otherwise)
          id: scope
          if: github.event_name == 'pull_request'
          run: |
            if [[ "${{ github.head_ref }}" == auto-fix/* ]]; then
              echo "active=true" >> "$GITHUB_OUTPUT"
            else
              echo "active=false" >> "$GITHUB_OUTPUT"
              echo "Fast-path SUCCESS — non-auto-fix PR (head=${{ github.head_ref }})"
            fi

      Indentation: the step is inserted as the FIRST item under each job's `steps:` block, with 6-space indentation on the `- name:` line (matching existing step indentation in those jobs). Verify by reading the file at each insertion site (look for the existing `- uses: actions/checkout@v4` step that currently sits at the top of each job — the scope-decision step is inserted IMMEDIATELY BEFORE it).

      EDIT 3 (D-03): GATE every subsequent step in each of the THREE guarded jobs with the OR-clause:

        if: steps.scope.outputs.active == 'true' || github.event_name != 'pull_request'

      Rules for applying the gate clause:
        - For steps that currently have NO `if:` key: insert the gate clause as a new top-level key on the step (between `- name:` and `run:` or `uses:`, at the same indentation as `name:` / `run:` siblings — typically 8 spaces under each step's `-`).
        - For steps that already have an `if:` key (e.g., `if: failure()` on the regression-comment step at line 360; `if: steps.playwright-cache.outputs.cache-hit != 'true'` on the Install Playwright Chromium step): COMBINE with `&&`. The combined form is `if: (steps.scope.outputs.active == 'true' || github.event_name != 'pull_request') && (<existing condition>)`. Always parenthesize both sides for unambiguous boolean precedence.
        - Do NOT gate the scope-decision step itself (it already has `if: github.event_name == 'pull_request'`).
        - Do NOT gate steps in the `diff-guard:` job (NOT in scope per D-03).

      EDIT 4 (HYGIENE): Do NOT rename any `jobs.<jobid>:` line. The jobids `diff-guard:`, `verifier-gate:`, `regression-suite:`, `ready-flip:` MUST remain byte-unchanged (Phase 50 D11 pins `verifier-gate:`).

    Sub-step 3b — Edit `.github/workflows/v40-deps-update.yml`:

      EDIT 5 (D-02): INSERT a `pull_request:` trigger as the FIRST key under `on:`, BEFORE the existing `schedule:` key. The `on:` block goes from:

        on:
          schedule:
            - cron: '0 9 * * 1'   # Monday 09:00 UTC — 40-CONTEXT locked decision #2
          workflow_dispatch: {}    # Manual trigger for testing

      To:

        on:
          pull_request:
            types: [opened, synchronize, reopened]
          schedule:
            - cron: '0 9 * * 1'   # Monday 09:00 UTC — 40-CONTEXT locked decision #2
          workflow_dispatch: {}    # Manual trigger for testing

      Use the Edit tool with `old_string` matching `on:\n  schedule:` and `new_string` adding the two-line `pull_request:` block before `schedule:`. Comments on the schedule and workflow_dispatch lines are preserved byte-for-byte.

      EDIT 6 (D-04): INSERT the scope-decision step at the TOP of the `deps-update-gate:` job's `steps:` block (line 164 → first `- uses: actions/checkout@v4` step). The dep-scan job (line 69) is NOT touched (it is the PR-creator, not a PR-gate).

      The step text (LOCKED VERBATIM per D-04 — head-ref check uses `v40-deps-update/*` prefix instead of `auto-fix/*`, and the log line says "non-v40-deps-update PR"):

        - name: Scope decision (v40-deps-update/* PRs only; fast-path SUCCESS otherwise)
          id: scope
          if: github.event_name == 'pull_request'
          run: |
            if [[ "${{ github.head_ref }}" == v40-deps-update/* ]]; then
              echo "active=true" >> "$GITHUB_OUTPUT"
            else
              echo "active=false" >> "$GITHUB_OUTPUT"
              echo "Fast-path SUCCESS — non-v40-deps-update PR (head=${{ github.head_ref }})"
            fi

      EDIT 7 (D-04): GATE every subsequent step in `deps-update-gate:` with the same OR-clause as Sub-step 3a EDIT 3:

        if: steps.scope.outputs.active == 'true' || github.event_name != 'pull_request'

      Combine with existing `if:` keys (Install Playwright Chromium has `if: steps.playwright-cache.outputs.cache-hit != 'true'`; Run E2E regression has `if: steps.smoke.outcome == 'success'`) using the parenthesized `&&` pattern from Sub-step 3a.

      EDIT 8 (HYGIENE): Do NOT rename any `jobs.<jobid>:` line. The jobids `dep-scan:` and `deps-update-gate:` MUST remain byte-unchanged (Phase 50 D12 pins `deps-update-gate:`).

    Sub-step 3c — Verify Vitest pins still pass (D-09 — drift detection):
      npx vitest run tests/unit/v40-verifier-gate-doc.test.js
      # Expected: all 12 tests pass (D1-D10 doc tests + D11 verifier-gate jobid pin + D12 deps-update-gate jobid pin).
      # If D11 or D12 fails, STOP (D-10) — the YAML edit accidentally renamed a jobid; do NOT relax the contract; revert and re-edit.

    Sub-step 3d — Local YAML sanity assertions (defense-in-depth):
      # verifier-gate.yml MUST no longer contain the BASE-ref filter:
      ! grep -qE "branches: \['auto-fix/\*'\]" .github/workflows/v40-verifier-gate.yml \
        || { echo "FAIL: branches: ['auto-fix/*'] still present"; exit 1; }
      # verifier-gate.yml MUST contain exactly 3 scope-decision steps (verifier-gate + regression-suite + ready-flip; diff-guard NOT touched):
      [[ "$(grep -c 'Scope decision (auto-fix/\* PRs only' .github/workflows/v40-verifier-gate.yml)" == "3" ]] \
        || { echo "FAIL: expected 3 verifier-style scope-decision steps in verifier-gate.yml; got $(grep -c 'Scope decision (auto-fix' .github/workflows/v40-verifier-gate.yml)"; exit 1; }
      # diff-guard job MUST NOT contain a scope-decision step (universal LOCKED-path check stays unguarded):
      awk '/^  diff-guard:/,/^  verifier-gate:/' .github/workflows/v40-verifier-gate.yml | grep -q 'Scope decision' \
        && { echo "FAIL: diff-guard job accidentally received a scope-decision step"; exit 1; } || true
      # deps-update.yml MUST contain the new pull_request trigger:
      grep -qE "^\s+pull_request:\s*$" .github/workflows/v40-deps-update.yml \
        || { echo "FAIL: pull_request: trigger missing from deps-update.yml on: block"; exit 1; }
      grep -qE "types: \[opened, synchronize, reopened\]" .github/workflows/v40-deps-update.yml \
        || { echo "FAIL: pull_request.types missing from deps-update.yml"; exit 1; }
      # deps-update.yml MUST contain exactly 1 deps-update-style scope-decision step (deps-update-gate only; dep-scan NOT touched):
      [[ "$(grep -c 'Scope decision (v40-deps-update/\* PRs only' .github/workflows/v40-deps-update.yml)" == "1" ]] \
        || { echo "FAIL: expected 1 deps-update-style scope-decision step in deps-update.yml; got $(grep -c 'Scope decision (v40-deps-update' .github/workflows/v40-deps-update.yml)"; exit 1; }
      # dep-scan job MUST NOT contain a scope-decision step:
      awk '/^  dep-scan:/,/^  deps-update-gate:/' .github/workflows/v40-deps-update.yml | grep -q 'Scope decision' \
        && { echo "FAIL: dep-scan job accidentally received a scope-decision step"; exit 1; } || true
      # YAML lint (best-effort — if `yamllint` is available, run it; if not, skip):
      command -v yamllint >/dev/null 2>&1 && yamllint -d '{extends: relaxed, rules: {line-length: disable}}' \
        .github/workflows/v40-verifier-gate.yml .github/workflows/v40-deps-update.yml || true
      # node-yaml structural parse (always available — Node is in the dev environment):
      node -e 'const yaml=require("js-yaml"); const fs=require("fs"); ["v40-verifier-gate.yml","v40-deps-update.yml"].forEach(f=>{const p=`.github/workflows/${f}`; const d=yaml.load(fs.readFileSync(p,"utf8")); if(!d.jobs) throw new Error(`${f} has no jobs key`); console.log(`${f} parses: jobs=${Object.keys(d.jobs).join(",")}`);});' \
        || { echo "FAIL: YAML structural parse failed — check syntax"; exit 1; }

    Sub-step 3e — Commit BOTH YAML files atomically:
      git add .github/workflows/v40-verifier-gate.yml .github/workflows/v40-deps-update.yml
      git commit -m "chore(51.1): fix verifier-gate + deps-update-gate triggers (BASE-filter removed; pull_request added; scope-decision fast-path)"

    Sub-step 3f — Snapshot the diff to evidence/yaml-diff.patch (D-15 artifact):
      git diff HEAD~1 HEAD -- .github/workflows/v40-verifier-gate.yml .github/workflows/v40-deps-update.yml \
        > .planning/phases/51.1-required-check-trigger-hotfix/evidence/yaml-diff.patch
      [[ -s .planning/phases/51.1-required-check-trigger-hotfix/evidence/yaml-diff.patch ]] \
        || { echo "FAIL: yaml-diff.patch empty — Task 3 made no changes?"; exit 1; }
      git add .planning/phases/51.1-required-check-trigger-hotfix/evidence/yaml-diff.patch
      git commit --amend --no-edit
      # The amend folds the diff snapshot into the Task 3 commit so Task 3 is still ONE atomic commit (D-12) containing: 2 YAML edits + the diff evidence file.

    Notes:
      - No live ruleset mutation; no push yet. autonomous=true.
      - The amend in 3f keeps Task 3 atomic (one commit, three files). If amend is disallowed by policy, the diff snapshot can be a separate commit, but the LOCKED D-12 cadence is "one atomic commit per task" — amend is the closest honoring of that intent.
      - If sub-step 3d's verifier-gate sub-job scope-decision count is wrong (e.g., 2 instead of 3, or one accidentally landed in diff-guard), STOP, revert the file edits, and re-apply with corrected Edit tool calls.
  </action>
  <verify>
    <automated>
      ! grep -qE "branches: \['auto-fix/\*'\]" .github/workflows/v40-verifier-gate.yml &&
      [[ "$(grep -c 'Scope decision (auto-fix/\* PRs only' .github/workflows/v40-verifier-gate.yml)" == "3" ]] &&
      ! (awk '/^  diff-guard:/,/^  verifier-gate:/' .github/workflows/v40-verifier-gate.yml | grep -q 'Scope decision') &&
      grep -qE "^\s+pull_request:\s*$" .github/workflows/v40-deps-update.yml &&
      grep -qE "types: \[opened, synchronize, reopened\]" .github/workflows/v40-deps-update.yml &&
      [[ "$(grep -c 'Scope decision (v40-deps-update/\* PRs only' .github/workflows/v40-deps-update.yml)" == "1" ]] &&
      ! (awk '/^  dep-scan:/,/^  deps-update-gate:/' .github/workflows/v40-deps-update.yml | grep -q 'Scope decision') &&
      grep -qE "^  verifier-gate:\s*$" .github/workflows/v40-verifier-gate.yml &&
      grep -qE "^  deps-update-gate:\s*$" .github/workflows/v40-deps-update.yml &&
      test -s .planning/phases/51.1-required-check-trigger-hotfix/evidence/yaml-diff.patch &&
      npx vitest run tests/unit/v40-verifier-gate-doc.test.js 2>&1 | grep -qE 'Test Files\s+1 passed|✓\s+D11|✓\s+D12' &&
      git log -1 --pretty=%s | grep -qE '^chore\(51\.1\): fix verifier-gate \+ deps-update-gate triggers'
    </automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/v40-verifier-gate.yml` no longer contains `branches: ['auto-fix/*']` anywhere in the file.
    - `.github/workflows/v40-verifier-gate.yml` contains EXACTLY 3 occurrences of the literal string `Scope decision (auto-fix/* PRs only` (one in each of verifier-gate, regression-suite, ready-flip jobs).
    - `.github/workflows/v40-verifier-gate.yml` `diff-guard:` job contains ZERO occurrences of `Scope decision` (D-03 discretion: diff-guard is the universal check).
    - `.github/workflows/v40-deps-update.yml` `on:` block now contains a `pull_request:` key with `types: [opened, synchronize, reopened]`.
    - `.github/workflows/v40-deps-update.yml` contains EXACTLY 1 occurrence of `Scope decision (v40-deps-update/* PRs only` (in deps-update-gate job only).
    - `.github/workflows/v40-deps-update.yml` `dep-scan:` job contains ZERO occurrences of `Scope decision`.
    - Both YAML files still declare the protected jobids: `^  verifier-gate:\s*$` and `^  deps-update-gate:\s*$` (D-09 — Phase 50 D11/D12 jobid pins remain valid).
    - `npx vitest run tests/unit/v40-verifier-gate-doc.test.js` exits 0 with 12 tests passing (D11 + D12 pin tests green).
    - YAML structural parse (Node `js-yaml`) succeeds for both files (no syntax errors).
    - `evidence/yaml-diff.patch` is non-empty and contains the deletion of the `branches:` line + the insertion of `pull_request:` + scope-decision step text.
    - ONE atomic commit (after amend) whose subject matches `^chore\(51\.1\): fix verifier-gate \+ deps-update-gate triggers`. The commit modifies three files: both YAML files + the diff patch.
    - Working tree clean (`git status --porcelain` empty).
    - Commit is local-only (not pushed — Task 4 owns the push).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 04: Direct push to origin/main (Catch-22 escape — bypass actor permits)</name>
  <files>
    .planning/phases/51.1-required-check-trigger-hotfix/evidence/direct-push-log.txt
  </files>
  <read_first>
    .planning/phases/51.1-required-check-trigger-hotfix/51.1-CONTEXT.md (D-06 — single combined push covering YAML edits; D-13 — operator approval for first direct push to main since Phase 49)
    .planning/phases/51.1-required-check-trigger-hotfix/evidence/post-bypass-readd-ruleset.json (Task 2 result — confirms bypass window is OPEN)
    .planning/phases/51.1-required-check-trigger-hotfix/evidence/yaml-diff.patch (Task 3 result — proves what is being pushed)
  </read_first>
  <action>
    AUTOMATED PRE-PUSH ASSERTIONS run first; then the operator-approval checkpoint pauses BEFORE the actual `git push`; then post-push assertions and evidence capture. Commit message at task end: `chore(51.1): direct-push log (origin/main now carries the YAML fix)` (D-12).

    Sub-step 4a — Pre-push assertions (local state must match expectations):
      LOG=.planning/phases/51.1-required-check-trigger-hotfix/evidence/direct-push-log.txt
      mkdir -p "$(dirname "$LOG")"
      {
        echo "=== Task 4 direct-push log $(date -u +%FT%TZ) ==="
        echo ""
        echo "--- Pre-push local state ---"
        echo "Branch: $(git rev-parse --abbrev-ref HEAD)"
        echo "HEAD SHA: $(git rev-parse HEAD)"
        echo "HEAD subject: $(git log -1 --pretty=%s)"
        echo "Last 5 commits:"
        git log --oneline -5
        echo ""
        echo "Remote SHA: $(git ls-remote origin main | awk '{print $1}')"
        echo ""
        echo "Commits to push (origin/main..HEAD):"
        git log --oneline origin/main..HEAD
      } > "$LOG"
      # Hard assertions BEFORE asking operator approval:
      [[ "$(git rev-parse --abbrev-ref HEAD)" == "main" ]] || { echo "FAIL: not on main branch"; exit 1; }
      [[ -z "$(git status --porcelain)" ]]                || { echo "FAIL: working tree dirty"; exit 1; }
      # The HEAD commit MUST be the Task 3 YAML-edits commit:
      git log -1 --pretty=%s | grep -qE '^chore\(51\.1\): fix verifier-gate \+ deps-update-gate triggers' \
        || { echo "FAIL: HEAD commit is not the Task 3 YAML-edits commit; abort"; exit 1; }
      # origin/main..HEAD MUST contain at least the Task 1 baselines commit, the Task 2 bypass-readd commit, and the Task 3 YAML-edits commit:
      COMMITS_TO_PUSH=$(git log --oneline origin/main..HEAD | wc -l)
      [[ "$COMMITS_TO_PUSH" -ge 3 ]] || { echo "FAIL: expected >=3 commits to push (Tasks 1, 2, 3); got $COMMITS_TO_PUSH"; exit 1; }
      git log --oneline origin/main..HEAD | grep -qE 'chore\(51\.1\): pre-hotfix baselines'        || { echo "FAIL: missing Task 1 commit in push window"; exit 1; }
      git log --oneline origin/main..HEAD | grep -qE 'chore\(51\.1\): break-glass — re-add'        || { echo "FAIL: missing Task 2 commit in push window"; exit 1; }
      git log --oneline origin/main..HEAD | grep -qE 'chore\(51\.1\): fix verifier-gate'           || { echo "FAIL: missing Task 3 commit in push window"; exit 1; }
      # Confirm bypass window is OPEN (defensive — operator may have closed it manually):
      CURRENT_BYPASS=$(gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --jq '.bypass_actors | length')
      [[ "$CURRENT_BYPASS" == "1" ]] || { echo "FAIL: ruleset bypass_actors=$CURRENT_BYPASS (expected 1 — Task 2's bypass window appears closed); cannot push without bypass"; exit 1; }

    --- OPERATOR APPROVAL CHECKPOINT FIRES HERE (before the actual push) ---

    Sub-step 4b — Push directly to origin/main (LIVE MUTATION — first direct push to main since Phase 49 merge commit c0bb37d):
      PRE_PUSH_SHA=$(git rev-parse HEAD)
      {
        echo ""
        echo "--- Push attempt $(date -u +%FT%TZ) ---"
        echo "Command: git push origin main"
      } >> "$LOG"
      git push origin main 2>&1 | tee -a "$LOG"

    Sub-step 4c — Post-push assertions (remote MUST equal local HEAD):
      REMOTE_SHA=$(git ls-remote origin main | awk '{print $1}')
      {
        echo ""
        echo "--- Post-push verification $(date -u +%FT%TZ) ---"
        echo "Pre-push HEAD SHA: $PRE_PUSH_SHA"
        echo "Post-push origin/main SHA: $REMOTE_SHA"
        echo "Match: $([[ "$PRE_PUSH_SHA" == "$REMOTE_SHA" ]] && echo YES || echo NO)"
      } >> "$LOG"
      [[ "$PRE_PUSH_SHA" == "$REMOTE_SHA" ]] \
        || { echo "FAIL: post-push origin/main SHA does not match pre-push HEAD"; exit 1; }
      # Fetch to sync local origin/main reference:
      git fetch origin main
      [[ "$(git rev-parse origin/main)" == "$PRE_PUSH_SHA" ]] \
        || { echo "FAIL: local origin/main ref does not match post-push SHA after fetch"; exit 1; }

    Sub-step 4d — Commit the push log (one more atomic commit; this lands locally and will be pushed in a subsequent push during Task 5 or Task 6):
      git add .planning/phases/51.1-required-check-trigger-hotfix/evidence/direct-push-log.txt
      git commit -m "chore(51.1): direct-push log (origin/main now carries the YAML fix)"
      # Push the log commit immediately so origin/main is the authoritative source for Task 5's verification PR:
      git push origin main 2>&1 | tee -a "$LOG"
      # Re-assert the new HEAD landed:
      NEW_HEAD=$(git rev-parse HEAD)
      NEW_REMOTE=$(git ls-remote origin main | awk '{print $1}')
      [[ "$NEW_HEAD" == "$NEW_REMOTE" ]] || { echo "FAIL: log-commit push failed"; exit 1; }

    Notes:
      - The bypass window is still OPEN at end-of-task (closed by Task 6).
      - The push uses `git push origin main` with NO flags (no --force, no --no-verify). The bypass actor permits the push to a rule-protected branch; ordinary fast-forward semantics apply.
      - If the push is rejected (e.g., remote drifted), STOP. Do NOT --force. Investigate the remote state, fetch, and either rebase or abort the phase (Task 6 to close the bypass + STATE rollback).
  </action>
  <what-built>
    The YAML trigger fix is now live on origin/main. Task 3's combined commit (verifier-gate.yml BASE-filter removal + scope-decision steps; deps-update.yml pull_request trigger + scope-decision) is the new origin/main HEAD (along with the Task 1 baselines, Task 2 bypass-readd evidence, and Task 4 push-log). Phase 51.1 has now structurally repaired REGRESSION-51-01 at the source — the verification PR in Task 5 is the empirical confirmation.
  </what-built>
  <how-to-verify>
    1. Confirm origin/main HEAD on the GitHub side carries the Task 3 + Task 4 commits:
       gh api /repos/tonyrowles/patent-cite-tool/branches/main --jq '{name, commit_sha: .commit.sha, commit_message: .commit.commit.message}'
       Expected: `commit_message` starts with `chore(51.1): direct-push log` (Task 4d's amend-via-followup-commit) and the parent is the Task 3 YAML-edits commit.
    2. Spot-check the YAML edits landed on origin:
       gh api /repos/tonyrowles/patent-cite-tool/contents/.github/workflows/v40-verifier-gate.yml --jq '.content' | base64 -d | grep -c "Scope decision (auto-fix/\* PRs only"
       Expected: `3`
       gh api /repos/tonyrowles/patent-cite-tool/contents/.github/workflows/v40-deps-update.yml --jq '.content' | base64 -d | grep -c "pull_request:"
       Expected: at least `1`
    3. Confirm bypass window is STILL OPEN (Task 6 closes it):
       gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --jq '.bypass_actors | length'
       Expected: `1`
    4. Inspect the captured log:
       cat .planning/phases/51.1-required-check-trigger-hotfix/evidence/direct-push-log.txt
       Look for: pre-push HEAD SHA, push stdout/stderr (should show no warnings about bypass), post-push origin/main SHA matching pre-push HEAD.
    5. If anything looks wrong (push rejected, wrong SHA on remote, push log shows protection-block error), IMMEDIATELY:
       a. Run §7(b) to close the bypass window:
            gh api -X PUT /repos/tonyrowles/patent-cite-tool/rulesets/17086676 \
              --input <(echo '{"bypass_actors":[]}')
       b. Reply "rolled back, abort phase" so Task 6 runs explicitly to confirm the bypass closure + Task 7 documents the deviation.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `git push origin main` after reviewing sub-step 4a's pre-push state output (branch=main, HEAD subject starts with `chore(51.1): fix verifier-gate`, 3+ commits to push, bypass window confirmed OPEN). Type "dry-run" if you want to see what `git push --dry-run origin main` would do before approving the real push. Type "rolled back" if you closed the bypass window manually and want to abort the phase. Type a description of the issue otherwise.</resume-signal>
  <acceptance_criteria>
    - Pre-push assertion log shows: branch=main, working tree clean, HEAD subject matches `chore(51.1): fix verifier-gate + deps-update-gate triggers`, >=3 commits in `origin/main..HEAD`, bypass_actors length=1.
    - `evidence/direct-push-log.txt` contains the literal pre-push HEAD SHA AND the literal post-push `git ls-remote origin main` SHA, and the two SHAs match (one of these MUST be the Task 3 YAML-edits commit).
    - After Sub-step 4d, `git rev-parse origin/main == git rev-parse HEAD` (locally and on remote).
    - `gh api /repos/tonyrowles/patent-cite-tool/contents/.github/workflows/v40-verifier-gate.yml` (decoded) contains the scope-decision step text and does NOT contain `branches: ['auto-fix/*']`.
    - `gh api /repos/tonyrowles/patent-cite-tool/contents/.github/workflows/v40-deps-update.yml` (decoded) contains `pull_request:` under `on:` and the deps-update-gate scope-decision step.
    - Bypass actor is STILL PRESENT on the ruleset (`.bypass_actors | length == 1`) — Task 6 owns the removal.
    - The Task 4 log commit `chore(51.1): direct-push log` is the new origin/main HEAD; no --force was used.
    - Operator has confirmed via "approved" before the push executed.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 05: Verification test PR — prove BOTH checks fire (open, 12-min poll, capture, CLOSE + delete-branch)</name>
  <files>
    .planning/scratch/trigger-hotfix-probe.txt
    .planning/phases/51.1-required-check-trigger-hotfix/evidence/verification-pr-checks.json
    .planning/phases/51.1-required-check-trigger-hotfix/evidence/verification-pr-state.json
  </files>
  <read_first>
    .planning/phases/51.1-required-check-trigger-hotfix/51.1-CONTEXT.md (D-07 — verification PR shape, branch naming, probe file path, body purpose, close-after-evidence convention)
    .planning/phases/51.1-required-check-trigger-hotfix/51.1-CONTEXT.md §specifics (probe file content format: single line `trigger-hotfix probe ${ISO_TIMESTAMP}`)
    .planning/phases/51.1-required-check-trigger-hotfix/evidence/direct-push-log.txt (Task 4 confirms origin/main carries the fix)
    .github/CODEOWNERS (sanity check: `.planning/` non-CODEOWNED — same as Phase 50 GATE-03 convention)
  </read_first>
  <action>
    All-automated task; ONE atomic commit at the end. Commit message: `chore(51.1): verification PR captures both required checks firing (PR CLOSED + branch deleted)` (D-12).

    Sub-step 5a — Pre-flight: confirm origin/main has the fix and `.planning/scratch/` is non-CODEOWNED:
      git fetch origin main
      git checkout main
      git reset --hard origin/main
      # The Task 4d push-log commit MUST be the HEAD:
      git log -1 --pretty=%s | grep -qE '^chore\(51\.1\): direct-push log' \
        || { echo "FAIL: origin/main HEAD is not Task 4d push-log commit"; exit 1; }
      # CODEOWNERS check:
      grep -E '^\s*\.?/?planning' .github/CODEOWNERS && { echo "FAIL: .planning/ became CODEOWNED — aborting"; exit 1; } || true

    Sub-step 5b — Create test branch + probe file (D-07):
      ISO_TS=$(date -u +%Y%m%dT%H%M%SZ)
      BRANCH="test-only/trigger-hotfix-verify-${ISO_TS}"
      mkdir -p .planning/scratch
      echo "trigger-hotfix probe ${ISO_TS}" > .planning/scratch/trigger-hotfix-probe.txt
      git checkout -b "$BRANCH"
      git add .planning/scratch/trigger-hotfix-probe.txt
      git commit -m "chore(51.1): trigger-hotfix probe ${ISO_TS} (verification PR — will be CLOSED not merged)"
      git push -u origin "$BRANCH"

    Sub-step 5c — Open the verification PR (--draft); body explains purpose per CONTEXT discretion:
      PR_URL=$(gh pr create --base main --head "$BRANCH" --draft \
        --title "test-only: Phase 51.1 trigger-hotfix verification ${ISO_TS} (DO NOT MERGE)" \
        --body $'Phase 51.1 verification PR. Confirms that after the trigger hotfix (chore(51.1) commits on origin/main), BOTH required status check contexts (verifier-gate, deps-update-gate) now FIRE on a PR into main.\n\nExpected behaviour:\n- The verifier-gate workflow triggers (no more BASE-ref filter); the scope-decision step in verifier-gate/regression-suite/ready-flip jobs fast-paths to SUCCESS because the head ref (test-only/trigger-hotfix-verify-*) is NOT auto-fix/*.\n- The deps-update-gate workflow triggers (newly-added pull_request trigger); the scope-decision step fast-paths to SUCCESS because the head ref is NOT v40-deps-update/*.\n- diff-guard runs unguarded (universal LOCKED-path check); should report SUCCESS for the .planning/scratch/ probe path.\n\nThis PR will be CLOSED + branch deleted immediately after evidence capture. Do not approve, do not merge. See .planning/phases/51.1-required-check-trigger-hotfix/51.1-01-PLAN.md Task 5.')
      PR_NUM=$(echo "$PR_URL" | grep -oE '[0-9]+$')
      [[ -n "$PR_NUM" ]] || { echo "FAIL: could not extract PR number from gh pr create output"; exit 1; }
      echo "Verification PR #$PR_NUM opened on $BRANCH"

    Sub-step 5d — 12-min poll loop watching `gh pr checks --json name,state,bucket` until BOTH `verifier-gate` AND `deps-update-gate` names appear in the output (regardless of state/bucket — D-07 success criterion is that the checks FIRE, not necessarily PASS):
      CHECKS=.planning/phases/51.1-required-check-trigger-hotfix/evidence/verification-pr-checks.json
      # Poll: 72 iterations * 10s = 720s = 12 minutes. Workflow startup latency can be 30-90s; cold runners can add 1-2 min.
      for i in $(seq 1 72); do
        gh pr checks "$PR_NUM" --json name,state,bucket > "$CHECKS" 2>/dev/null || echo '[]' > "$CHECKS"
        # Check whether BOTH contexts appear by name:
        HAS_VERIFIER=$(jq -e '[.[] | .name?] | index("verifier-gate")' "$CHECKS" 2>/dev/null && echo yes || echo no)
        HAS_DEPS=$(jq -e '[.[] | .name?] | index("deps-update-gate")' "$CHECKS" 2>/dev/null && echo yes || echo no)
        if [[ "$HAS_VERIFIER" == "yes" ]] && [[ "$HAS_DEPS" == "yes" ]]; then
          echo "Poll $i/72: BOTH contexts present — capturing evidence."
          break
        fi
        echo "Poll $i/72: verifier-gate=$HAS_VERIFIER, deps-update-gate=$HAS_DEPS — sleeping 10s..."
        sleep 10
      done
      # Final hard assertions (one of these MUST hold):
      jq -e '[.[] | .name?] | index("verifier-gate") != null' "$CHECKS" \
        || { echo "FAIL: verifier-gate context never appeared in 12 minutes"; cat "$CHECKS"; gh pr close "$PR_NUM" --delete-branch 2>/dev/null || true; exit 1; }
      jq -e '[.[] | .name?] | index("deps-update-gate") != null' "$CHECKS" \
        || { echo "FAIL: deps-update-gate context never appeared in 12 minutes"; cat "$CHECKS"; gh pr close "$PR_NUM" --delete-branch 2>/dev/null || true; exit 1; }
      # Both fired — re-capture once more for the definitive evidence file:
      gh pr checks "$PR_NUM" --json name,state,bucket > "$CHECKS"

    Sub-step 5e — Capture PR state evidence:
      STATE=.planning/phases/51.1-required-check-trigger-hotfix/evidence/verification-pr-state.json
      gh pr view "$PR_NUM" --json state,mergedAt,headRefName,baseRefName,isDraft,number,url > "$STATE"
      jq -e '.headRefName | startswith("test-only/trigger-hotfix-verify-")' "$STATE" \
        || { echo "FAIL: verification PR head branch does not match test-only/trigger-hotfix-verify-* pattern"; exit 1; }
      jq -e '.baseRefName == "main"' "$STATE" \
        || { echo "FAIL: verification PR base is not main"; exit 1; }

    Sub-step 5f — CLOSE PR + delete branch (D-07 — UAT-47-e / GATE-03 convention):
      gh pr close "$PR_NUM" --delete-branch \
        --comment "Phase 51.1 trigger-hotfix verification complete: BOTH verifier-gate AND deps-update-gate contexts appeared in gh pr checks (see .planning/phases/51.1-required-check-trigger-hotfix/evidence/verification-pr-checks.json). Closing per Phase 51.1 plan D-07 (UAT-47-e / GATE-03 close-after-evidence convention)."
      git checkout main
      git branch -D "$BRANCH" 2>/dev/null || true
      git fetch --prune origin

      # Confirm the branch is gone from origin:
      git ls-remote origin "$BRANCH" | grep -q . \
        && { echo "FAIL: $BRANCH still present on origin after close --delete-branch"; exit 1; } || true

      # Re-capture state to confirm CLOSED:
      gh pr view "$PR_NUM" --json state,mergedAt,headRefName,baseRefName,isDraft,number,url > "$STATE"
      jq -e '.state == "CLOSED"' "$STATE"     || { echo "FAIL: PR state is not CLOSED"; exit 1; }
      jq -e '.mergedAt == null'  "$STATE"     || { echo "FAIL: PR was MERGED (should have been CLOSED)"; exit 1; }

    Sub-step 5g — Confirm the probe file never landed on main (UAT-47-e hygiene rule):
      git log --all --diff-filter=A -- .planning/scratch/trigger-hotfix-probe.txt | grep -v "$BRANCH" | grep -q . \
        && { echo "FAIL: probe file appears on a branch other than the deleted test branch"; exit 1; } || true
      # On the local main checkout the file should not exist (it only ever lived on the deleted branch):
      [[ ! -f .planning/scratch/trigger-hotfix-probe.txt ]] || { echo "FAIL: probe file present on main checkout"; exit 1; }

    Sub-step 5h — Stage evidence and commit:
      git add .planning/phases/51.1-required-check-trigger-hotfix/evidence/verification-pr-checks.json \
              .planning/phases/51.1-required-check-trigger-hotfix/evidence/verification-pr-state.json
      git commit -m "chore(51.1): verification PR captures both required checks firing (PR CLOSED + branch deleted)"
      git push origin main

    Notes:
      - The probe file `.planning/scratch/trigger-hotfix-probe.txt` only ever exists on the verification branch; it is never on main (and the branch is deleted).
      - The 12-min poll covers cold-runner and queueing latency; GitHub Actions PR-triggered workflows typically register check-runs within 30-90s but can take longer under load.
      - We assert on `name` presence only (not `state`/`bucket` value) because the success criterion D-07 is "both checks FIRE on a PR into main" — fast-path SUCCESS (likely, since the test branch is neither `auto-fix/*` nor `v40-deps-update/*`) and FAILURE both prove the trigger fires.
      - If the poll times out, the evidence file is still captured (last poll result), the PR is closed with the failure documented, and the task exits non-zero so the operator can investigate before Task 6 closes the bypass window.
  </action>
  <verify>
    <automated>
      jq -e '[.[] | .name?] | index("verifier-gate") != null and index("deps-update-gate") != null' .planning/phases/51.1-required-check-trigger-hotfix/evidence/verification-pr-checks.json &&
      jq -e '.state == "CLOSED" and .mergedAt == null and (.headRefName | startswith("test-only/trigger-hotfix-verify-")) and .baseRefName == "main"' .planning/phases/51.1-required-check-trigger-hotfix/evidence/verification-pr-state.json &&
      [[ ! -f .planning/scratch/trigger-hotfix-probe.txt ]] &&
      git log -1 --pretty=%s | grep -qE '^chore\(51\.1\): verification PR captures'
    </automated>
  </verify>
  <acceptance_criteria>
    - `evidence/verification-pr-checks.json` is a non-empty JSON array; `jq` confirms BOTH `verifier-gate` AND `deps-update-gate` appear by `.name`.
    - `evidence/verification-pr-state.json` shows `state="CLOSED"`, `mergedAt=null`, `headRefName` starts with `test-only/trigger-hotfix-verify-`, `baseRefName="main"`.
    - The verification PR branch is deleted from origin (`git ls-remote origin test-only/trigger-hotfix-verify-*` returns empty).
    - The probe file `.planning/scratch/trigger-hotfix-probe.txt` does NOT exist on `main` and only ever existed on the deleted branch.
    - ONE atomic commit subject matches `^chore\(51\.1\): verification PR captures both required checks firing`.
    - The commit lands on origin/main via `git push origin main` (bypass window is still OPEN so the push is permitted; Task 6 closes the window next).
    - SC-3 (ROADMAP "BOTH `verifier-gate` AND `deps-update-gate` contexts present in `gh pr checks` output") is demonstrably satisfied.
    - SC-4 (ROADMAP "verification PR is CLOSED + branch deleted immediately after evidence capture") is demonstrably satisfied.
    - REGRESSION-51-01 is empirically closed (the trigger fires; the required checks now have providers).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 06: Remove bypass actor — restore Phase 50 SC-2 end-state (bypass_actors=[])</name>
  <files>
    .planning/phases/51.1-required-check-trigger-hotfix/evidence/bypass-remove-payload.json
    .planning/phases/51.1-required-check-trigger-hotfix/evidence/final-ruleset.json
  </files>
  <read_first>
    .planning/phases/51.1-required-check-trigger-hotfix/51.1-CONTEXT.md (D-08 — bypass removal closes the hotfix window; final ruleset MUST match Phase 50 closure baseline)
    docs/v40-repo-config.md §7(b) (Remove the bypass actor — the runbook this task is exercising)
    .planning/phases/50-cleanup-04-readiness-gate/evidence/final-ruleset.json (Phase 50 closure baseline — Task 6's final-ruleset.json MUST byte-equal this on rules+bypass_actors+current_user_can_bypass keys)
    .planning/phases/51.1-required-check-trigger-hotfix/evidence/post-bypass-readd-ruleset.json (Task 2 result — confirms what we are reverting)
  </read_first>
  <action>
    AUTOMATED WORK FIRST (PUT executes, evidence captured, byte-equality assertions against Phase 50 baseline). THEN the operator-approval checkpoint fires (the operator confirms Phase 50 SC-2 end-state was restored). Commit message at task end: `chore(51.1): break-glass — remove operator bypass actor (hotfix window CLOSED; Phase 50 SC-2 restored)` (D-12).

    Sub-step 6a — Construct the lighter PUT body verbatim per docs §7(b) (D-08):
      PAYLOAD=.planning/phases/51.1-required-check-trigger-hotfix/evidence/bypass-remove-payload.json
      cat > "$PAYLOAD" <<'JSON'
      {"bypass_actors":[]}
      JSON
      # Hard guard — lighter body MUST NOT contain a `rules` key (D-08 — preserves the live 5 rules via Pattern 2 partial-body merge):
      jq -e 'has("rules") | not' "$PAYLOAD" \
        || { echo "FAIL: bypass-remove-payload.json contains rules key — would clobber the 5 rules"; exit 1; }
      jq -e '.bypass_actors == []' "$PAYLOAD" \
        || { echo "FAIL: bypass-remove-payload.json bypass_actors is not exactly []"; exit 1; }
      # Belt-and-suspenders: payload top-level keys MUST be exactly {bypass_actors}:
      [[ "$(jq -r 'keys_unsorted | join(",")' "$PAYLOAD")" == "bypass_actors" ]] \
        || { echo "FAIL: bypass-remove-payload.json has extra top-level keys"; exit 1; }

    Sub-step 6b — Apply the PUT (LIVE MUTATION — second ruleset audit-log entry of Phase 51.1):
      gh api -X PUT /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --input "$PAYLOAD"
      gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 \
        > .planning/phases/51.1-required-check-trigger-hotfix/evidence/final-ruleset.json

    Sub-step 6c — Hard post-state assertions (end-state MUST match Phase 50 closure):
      FINAL=.planning/phases/51.1-required-check-trigger-hotfix/evidence/final-ruleset.json
      P50_BASELINE=.planning/phases/50-cleanup-04-readiness-gate/evidence/final-ruleset.json
      # Phase 50 SC-2 end-state assertions:
      jq -e '.bypass_actors | length == 0'             "$FINAL" || { echo "FAIL: bypass_actors not empty after PUT"; exit 1; }
      jq -e '.current_user_can_bypass == "never"'      "$FINAL" || { echo "FAIL: current_user_can_bypass != never after PUT"; exit 1; }
      jq -e '.enforcement == "active"'                 "$FINAL" || { echo "FAIL: enforcement not active"; exit 1; }
      # Phase 50 SC-1 preservation:
      jq -e '.rules | length == 5'                     "$FINAL" || { echo "FAIL: rules count != 5 after PUT — Pattern 2 partial-body merge failed"; exit 1; }
      jq -e '[.rules[].type] | sort == ["deletion","non_fast_forward","pull_request","required_linear_history","required_status_checks"]' "$FINAL" \
        || { echo "FAIL: rule type set drifted"; exit 1; }
      jq -e '[.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context] | sort == ["deps-update-gate","verifier-gate"]' "$FINAL" \
        || { echo "FAIL: required contexts drifted"; exit 1; }
      jq -e '.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].integration_id | . == 15368' "$FINAL" \
        || { echo "FAIL: integration_id drifted from 15368 (Phase 50 SC-1)"; exit 1; }
      # Byte-equality against Phase 50 closure baseline on the LOAD-BEARING keys:
      P50_RULES=$(jq -cS '.rules' "$P50_BASELINE")
      NEW_RULES=$(jq -cS '.rules' "$FINAL")
      [[ "$P50_RULES" == "$NEW_RULES" ]] \
        || { echo "FAIL: .rules drifted from Phase 50 final-ruleset baseline"; diff <(echo "$P50_RULES" | jq .) <(echo "$NEW_RULES" | jq .); exit 1; }
      P50_BYPASS=$(jq -cS '.bypass_actors' "$P50_BASELINE")
      NEW_BYPASS=$(jq -cS '.bypass_actors' "$FINAL")
      [[ "$P50_BYPASS" == "$NEW_BYPASS" ]] \
        || { echo "FAIL: .bypass_actors drifted from Phase 50 baseline"; exit 1; }
      P50_CANBYPASS=$(jq -r '.current_user_can_bypass' "$P50_BASELINE")
      NEW_CANBYPASS=$(jq -r '.current_user_can_bypass' "$FINAL")
      [[ "$P50_CANBYPASS" == "$NEW_CANBYPASS" ]] \
        || { echo "FAIL: .current_user_can_bypass drifted ($P50_CANBYPASS vs $NEW_CANBYPASS)"; exit 1; }

    Sub-step 6d — Stage evidence and commit:
      git add .planning/phases/51.1-required-check-trigger-hotfix/evidence/bypass-remove-payload.json \
              .planning/phases/51.1-required-check-trigger-hotfix/evidence/final-ruleset.json
      git commit -m "chore(51.1): break-glass — remove operator bypass actor (hotfix window CLOSED; Phase 50 SC-2 restored)"
      # Push this commit so origin/main reflects the closed-bypass-window state:
      git push origin main

    THEN the operator-approval checkpoint fires (operator confirms the final ruleset GET shows bypass_actors=[] and the Phase 50 SC-2 invariant is restored).
  </action>
  <what-built>
    The hotfix window is CLOSED. Ruleset 17086676 is restored to Phase 50 closure baseline: `bypass_actors=[]`, `current_user_can_bypass="never"`, 5 rules preserved, verifier-gate + deps-update-gate required contexts pinned to integration_id=15368. Phase 50 SC-1 and SC-2 are both preserved. The lighter PUT body `{"bypass_actors":[]}` was jq-guarded against `rules` key inclusion (D-08). Evidence: `bypass-remove-payload.json` + `final-ruleset.json` + the atomic `chore(51.1):` commit pushed to origin/main.
  </what-built>
  <how-to-verify>
    1. Confirm the LIVE ruleset is at Phase 50 closure end-state:
       gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --jq '{rules_count: (.rules | length), bypass_count: (.bypass_actors | length), can_bypass: .current_user_can_bypass, enforcement, contexts: ([.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context] | sort), iids: ([.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].integration_id] | unique)}'
       Expected (exact): `{"rules_count":5,"bypass_count":0,"can_bypass":"never","enforcement":"active","contexts":["deps-update-gate","verifier-gate"],"iids":[15368]}`
    2. Confirm byte-equality against Phase 50 baseline:
       diff <(jq -cS '{rules, bypass_actors, current_user_can_bypass}' .planning/phases/50-cleanup-04-readiness-gate/evidence/final-ruleset.json) \
            <(jq -cS '{rules, bypass_actors, current_user_can_bypass}' .planning/phases/51.1-required-check-trigger-hotfix/evidence/final-ruleset.json)
       Expected: empty (no diff).
    3. Confirm a manual probe push to main is now BLOCKED (defensive check, optional):
       Attempting any direct push to main from an unprivileged context should be rejected — the operator does not need to actually attempt this; the byte-equality check above is the definitive signal.
    4. Inspect git log:
       git log -1 --pretty='%H %s' --stat
       Expected: ONE commit modifying `evidence/bypass-remove-payload.json` AND `evidence/final-ruleset.json`.
    5. If anything looks wrong, the §7(a) re-add command is documented in `docs/v40-repo-config.md` — DO NOT execute it unless explicitly intending to re-open the window for additional work. Reply with a description of the issue instead of "approved".
  </how-to-verify>
  <resume-signal>Type "approved" once the live ruleset shows bypass_actors=[], current_user_can_bypass=never, 5 rules preserved with both required contexts pinned to integration_id=15368, AND the byte-equality diff against Phase 50 baseline shows empty. Type "dry-run" if you want to inspect `evidence/final-ruleset.json` before declaring the phase closed. Type a description of the issue otherwise.</resume-signal>
  <acceptance_criteria>
    - `evidence/bypass-remove-payload.json` exists; `jq -e 'has("rules") | not'` passes (D-08 lighter-body guard); content is exactly `{"bypass_actors":[]}`; top-level keys are exactly `["bypass_actors"]`.
    - `evidence/final-ruleset.json` shows: `.bypass_actors | length == 0`, `.current_user_can_bypass == "never"`, `.enforcement == "active"`, `.rules | length == 5`.
    - Required contexts `verifier-gate` + `deps-update-gate` are present with integration_id=15368 on both (Phase 50 SC-1 preserved).
    - `jq -cS '{rules,bypass_actors,current_user_can_bypass}'` of the final ruleset BYTE-EQUALS the same projection of `.planning/phases/50-cleanup-04-readiness-gate/evidence/final-ruleset.json` (Phase 50 closure baseline).
    - ONE atomic commit subject matches `^chore\(51\.1\): break-glass — remove operator bypass actor`.
    - The commit is pushed to origin/main; `git rev-parse origin/main == git rev-parse HEAD`.
    - Operator has confirmed via "approved" before Task 07 proceeds.
    - No `-X PATCH` appears in any executed `gh api` command (only `-X PUT`).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 07: SUMMARY + STATE pending-todo AMENDMENT + ROADMAP closure</name>
  <files>
    .planning/phases/51.1-required-check-trigger-hotfix/evidence/INDEX.md
    .planning/phases/51.1-required-check-trigger-hotfix/51.1-01-SUMMARY.md
    .planning/STATE.md
    .planning/ROADMAP.md
  </files>
  <read_first>
    .planning/phases/51.1-required-check-trigger-hotfix/51.1-CONTEXT.md (D-15 — INDEX role-map convention, D-16 — exact STATE.md amendment shape)
    .planning/phases/51.1-required-check-trigger-hotfix/evidence/ (all 11 evidence files from Tasks 1-6 — INDEX.md is the role-map for these)
    .planning/STATE.md (Pending Todos table — the line to amend; verify current literal text before editing)
    .planning/ROADMAP.md (Phase 51.1 row + Progress table — find Phase 51.1 checkbox and flip it)
    .planning/phases/50-cleanup-04-readiness-gate/evidence/INDEX.md (Phase 50 INDEX format — Phase 51.1 INDEX mirrors)
    .planning/phases/51-live-readiness-uats/51-01-SUMMARY.md (Phase 51 SUMMARY format — Phase 51.1 SUMMARY mirrors with 5 SCs + deviations + provides + sign-off)
  </read_first>
  <action>
    Four sub-steps in ONE atomic commit. Commit message: `chore(51.1): SUMMARY + STATE Phase 56 amendment + ROADMAP closure` (D-12).

    Sub-step 7a — Write `evidence/INDEX.md` (per-file role map, Phase 50 convention):

      Use the Write tool to create `.planning/phases/51.1-required-check-trigger-hotfix/evidence/INDEX.md` with content listing all 11 evidence files plus the INDEX itself, one line per file with a single-sentence role description. Format:

        # Phase 51.1 Evidence Index
        Per-file role map for the 12 artifacts in this directory.

        ## Task 1 — Pre-hotfix baselines
        - `pre-hotfix-ruleset.json` — Live ruleset GET before Phase 51.1 mutations; must match Phase 50 final-ruleset.json (5 rules / 0 bypass actors / current_user_can_bypass=never).
        - `pre-hotfix-v40-verifier-gate.yml` — Frozen snapshot of v40-verifier-gate.yml before Task 3 edits; contains the broken `branches: ['auto-fix/*']` BASE-ref filter that REGRESSION-51-01 identifies.
        - `pre-hotfix-v40-deps-update.yml` — Frozen snapshot of v40-deps-update.yml before Task 3 edits; does NOT contain a `pull_request:` trigger (the second half of REGRESSION-51-01).

        ## Task 2 — Break-glass: re-add bypass actor
        - `bypass-readd-payload.json` — Lighter PUT body re-adding operator (id=254599900) with bypass_mode=always; jq-guarded against `rules` key.
        - `post-bypass-readd-ruleset.json` — Live ruleset GET after Task 2 PUT; bypass_actors length=1, 5 rules preserved.

        ## Task 3 — YAML edits
        - `yaml-diff.patch` — `git diff HEAD~1 HEAD` of both YAML files showing: BASE-ref filter removed from verifier-gate.yml; pull_request trigger added to deps-update.yml; 4 scope-decision steps inserted (3 in verifier-gate jobs, 1 in deps-update-gate); subsequent steps gated.

        ## Task 4 — Direct push to main
        - `direct-push-log.txt` — Pre-push HEAD SHA + push stdout/stderr + post-push `git ls-remote origin main` SHA; SHAs match (no --force).

        ## Task 5 — Verification PR
        - `verification-pr-checks.json` — `gh pr checks --json name,state,bucket` capture showing BOTH `verifier-gate` AND `deps-update-gate` contexts fire on a PR into main (REGRESSION-51-01 closed empirically).
        - `verification-pr-state.json` — `gh pr view --json state,mergedAt,headRefName,baseRefName,isDraft,number,url` showing state=CLOSED, mergedAt=null, headRefName=test-only/trigger-hotfix-verify-* (D-07 close-after-evidence).

        ## Task 6 — Break-glass: remove bypass actor
        - `bypass-remove-payload.json` — Lighter PUT body `{"bypass_actors":[]}`; jq-guarded against `rules` key (D-08).
        - `final-ruleset.json` — Live ruleset GET after Task 6 PUT; byte-equals Phase 50 final-ruleset.json on `rules` + `bypass_actors` + `current_user_can_bypass` (Phase 50 SC-2 restored).

        ## Task 7 — Closure
        - `INDEX.md` — This file.

    Sub-step 7b — Write `51.1-01-SUMMARY.md` (closure narrative, Phase 50/51 convention):

      Use the Write tool to create `.planning/phases/51.1-required-check-trigger-hotfix/51.1-01-SUMMARY.md` with frontmatter (phase, plan, status, completed_at, requirements_addressed=[REGRESSION-51-01], evidence_dir, deviations, provides, next_phase=52) and body containing:

        - Outcome Matrix (5-SC mapping — SC-1 through SC-5 from ROADMAP, each marked PASS with evidence file reference)
        - "What was built" table (the artifacts produced — both YAML edits + 12 evidence files + STATE amendment + ROADMAP update)
        - "Live-infrastructure mutations performed" table (ruleset PUT #1 at Task 2, direct push at Task 4, ruleset PUT #2 at Task 6, verification PR opened+closed at Task 5)
        - "Discoveries" section (e.g., D-09 vitest pins remained green proving the jobid lines were untouched; any sub-step deviations noted)
        - "Phase 56 amendment" section (the line that was removed from STATE Pending Todos; the remaining sub-items)
        - "Deviations from plan" section (frontmatter deviations: list narrated with prose)
        - "Sign-off" — Phase 51.1 closes 5/5 SCs PASS, REGRESSION-51-01 empirically closed (verification PR captured both checks firing), Phase 50 SC-1+SC-2 preserved (final-ruleset.json byte-equals baseline), the PR pipeline is unjammed.

    Sub-step 7c — Amend STATE.md Phase 56 Pending Todos line (D-16 — surgical edit; preserve other sub-items):

      Read the current Pending Todos line. Per the current STATE.md content:

        BEFORE (literal text in STATE.md `### Pending Todos`):
          - Phase 56 (v4.2 backlog): refactor v40-cost-ledger-snapshot.yml + v40-auto-fix.yml ledger-commit-to-main pattern (UAT-47-d structurally blocked by Phase 50 ruleset; UAT-47-a's ledger commit also affected — both need PR-then-merge or branch-redirect)

      D-16 mandates removing the "verifier-gate trigger patch" sub-item but the existing line does NOT literally contain those words — the Phase 51 SUMMARY enumerated 4 follow-ups under "Phase 56 enqueueing" but STATE.md captured only the ledger-commit one. D-16's intent is to refine STATE.md to:
        a) Reflect that Phase 51.1 has closed the verifier-gate trigger patch sub-item (so it does NOT need to be added retroactively); AND
        b) Preserve the ledger-commit refactor + fixture-mutator authoring + 4-UAT re-sweep sub-items as the remaining Phase 56 backlog.

      Therefore Task 7c replaces the single Pending Todos line with an explicit multi-item list that ENUMERATES the surviving Phase 56 work. Use the Edit tool with:

        old_string (literal, single line):
          - Phase 56 (v4.2 backlog): refactor v40-cost-ledger-snapshot.yml + v40-auto-fix.yml ledger-commit-to-main pattern (UAT-47-d structurally blocked by Phase 50 ruleset; UAT-47-a's ledger commit also affected — both need PR-then-merge or branch-redirect)

        new_string (literal, multi-line):
          - Phase 56 (v4.2 backlog) — surviving sub-items after Phase 51.1 closed REGRESSION-51-01:
            - ledger-commit refactor: refactor v40-cost-ledger-snapshot.yml + v40-auto-fix.yml ledger-commit-to-main pattern (UAT-47-d structurally blocked by Phase 50 ruleset; UAT-47-a's ledger commit also affected — both need PR-then-merge or branch-redirect)
            - fixture-mutator authoring: author tests/e2e/uat-helpers/regression-fixture-mutator.sh (required to execute UAT-47-b)
            - 4-UAT re-sweep: re-run UAT-47-a + UAT-47-b + UAT-47-d + UAT-47-e against the patched workflow triggers (Phase 51.1 verified the triggers fire structurally; the UATs verify the gates produce the correct outcomes)
            - (REMOVED in Phase 51.1: verifier-gate trigger patch — landed via .github/workflows/v40-verifier-gate.yml + v40-deps-update.yml edits on 2026-06-XX; see .planning/phases/51.1-required-check-trigger-hotfix/51.1-01-SUMMARY.md)

      Also update STATE.md frontmatter `last_updated` to the Task 7 ISO timestamp and `last_activity` to "2026-06-XX -- Phase 51.1 closed (REGRESSION-51-01 resolved; Phase 50 SC-2 preserved)". Increment `progress.total_phases` from 8 to 9, `completed_phases` from 4 to 5, `total_plans` from 4 to 5, `completed_plans` from 4 to 5; recompute `percent` (now 5/9 = 56). Update Current Position to "Phase 51.1 — COMPLETE; Phase 52 ready; Wave-2 fully unblocked".

      Add a new line under `### Decisions` summarizing Phase 51.1 closure (mirror the Phase 50 / Phase 51 closure entries):
        - [Phase 51.1]: Closure (2026-06-XX): REGRESSION-51-01 resolved — v40-verifier-gate.yml BASE-ref filter removed + v40-deps-update.yml pull_request trigger added + scope-decision fast-path step added to 4 PR-gate jobs (verifier-gate/regression-suite/ready-flip in verifier-gate.yml; deps-update-gate in deps-update.yml); diff-guard + dep-scan jobs unguarded by design. Phase 50 SC-1+SC-2 preserved (final-ruleset.json byte-equals baseline). Break-glass §7 runbook live-tested end-to-end. Verification PR captured BOTH required contexts firing then CLOSED+branch-deleted (test-only/trigger-hotfix-verify-*). 7 atomic chore(51.1) commits.

    Sub-step 7d — Update ROADMAP.md Phase 51.1 closure:

      Use the Edit tool to:
        1. Flip the Phase 51.1 row in the Progress table from `Not started` (or blank) to `Complete   ` with completion date `2026-06-XX`. If a Phase 51.1 row does not yet exist in the Progress table, insert one before the Phase 52 row.
        2. Update the Phase 51.1 Phase Details block: `**Plans**: TBD` → `**Plans**: 1 plan` and add the Plans block:
            Plans:
            - [x] 51.1-01-PLAN.md — 7 sequential tasks: baselines → break-glass add → YAML edits → direct push → verification PR → break-glass remove → SUMMARY+STATE+ROADMAP

    Sub-step 7e — Stage and commit:
      git add .planning/phases/51.1-required-check-trigger-hotfix/evidence/INDEX.md \
              .planning/phases/51.1-required-check-trigger-hotfix/51.1-01-SUMMARY.md \
              .planning/STATE.md \
              .planning/ROADMAP.md
      git commit -m "chore(51.1): SUMMARY + STATE Phase 56 amendment + ROADMAP closure"
      git push origin main

    Notes:
      - Task 7 is the only Task 7 sub-step that mutates STATE.md and ROADMAP.md; previous tasks left those files untouched.
      - The STATE Pending Todos amendment in Sub-step 7c is the LOAD-BEARING D-16 requirement; if the current STATE.md literal text differs from the BEFORE string above (e.g., a typo was fixed after Phase 51 closure), the operator should re-read STATE.md and adjust the Edit `old_string` to the actual current literal text before running this task.
      - After Task 7, the working tree is clean and origin/main carries 7 atomic `chore(51.1):` commits + 0 ruleset bypass actors + the structural REGRESSION-51-01 fix.
  </action>
  <verify>
    <automated>
      test -s .planning/phases/51.1-required-check-trigger-hotfix/evidence/INDEX.md &&
      grep -c '^- `' .planning/phases/51.1-required-check-trigger-hotfix/evidence/INDEX.md | grep -qE '^(11|12)$' &&
      test -s .planning/phases/51.1-required-check-trigger-hotfix/51.1-01-SUMMARY.md &&
      grep -q 'REGRESSION-51-01' .planning/phases/51.1-required-check-trigger-hotfix/51.1-01-SUMMARY.md &&
      grep -q 'Phase 56' .planning/STATE.md &&
      grep -q 'verifier-gate trigger patch' .planning/STATE.md &&
      grep -q 'fixture-mutator authoring' .planning/STATE.md &&
      grep -q 'ledger-commit refactor' .planning/STATE.md &&
      grep -q '4-UAT re-sweep' .planning/STATE.md &&
      grep -qE '^\s*- \[x\] \*\*Phase 51\.1' .planning/ROADMAP.md &&
      git log -1 --pretty=%s | grep -qE '^chore\(51\.1\): SUMMARY \+ STATE Phase 56 amendment \+ ROADMAP closure' &&
      [[ "$(git rev-parse HEAD)" == "$(git ls-remote origin main | awk '{print $1}')" ]]
    </automated>
  </verify>
  <acceptance_criteria>
    - `evidence/INDEX.md` exists and lists all 11 evidence files (Task 1-6 outputs) by name with single-sentence role descriptions.
    - `51.1-01-SUMMARY.md` exists with frontmatter `status: complete`, `requirements_addressed: [REGRESSION-51-01]`, 5-SC outcome matrix, deviations list, Phase 56 amendment section, sign-off.
    - `STATE.md` Pending Todos contains the surviving Phase 56 sub-items: `ledger-commit refactor`, `fixture-mutator authoring`, `4-UAT re-sweep`. The "verifier-gate trigger patch" sub-item is explicitly marked REMOVED via the parenthetical note (D-16 amendment shape).
    - `STATE.md` frontmatter `progress.completed_phases` incremented; `last_updated` advanced.
    - `STATE.md` §Decisions has a new `[Phase 51.1]: Closure` entry.
    - `ROADMAP.md` Phase 51.1 row in the Progress table is marked `Complete` with completion date; the checkbox `- [x] **Phase 51.1` is present in the Phases section; the Phase Details block shows `**Plans**: 1 plan` and the plan checkbox is `[x]`.
    - ONE atomic commit subject matches `^chore\(51\.1\): SUMMARY \+ STATE Phase 56 amendment \+ ROADMAP closure`.
    - The commit is pushed to origin/main; `git rev-parse origin/main == git rev-parse HEAD`.
    - Working tree clean.
    - Phase 51.1 closes with 7 atomic `chore(51.1)` commits visible in `git log --oneline | grep -c 'chore(51.1)'` (== 7).
  </acceptance_criteria>
</task>

</tasks>

<verification>
End-of-phase verification (post Task 7 commit, before declaring closure):

1. Ruleset 17086676 state (must match Phase 50 closure baseline byte-for-byte on key fields):
   gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --jq '{rules_count: (.rules | length), bypass_count: (.bypass_actors | length), can_bypass: .current_user_can_bypass, enforcement, contexts: ([.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context] | sort), iids: ([.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].integration_id] | unique)}'
   Expected: `{"rules_count":5,"bypass_count":0,"can_bypass":"never","enforcement":"active","contexts":["deps-update-gate","verifier-gate"],"iids":[15368]}`.

2. Byte-equality with Phase 50 baseline on load-bearing keys:
   diff <(jq -cS '{rules,bypass_actors,current_user_can_bypass}' .planning/phases/50-cleanup-04-readiness-gate/evidence/final-ruleset.json) \
        <(jq -cS '{rules,bypass_actors,current_user_can_bypass}' .planning/phases/51.1-required-check-trigger-hotfix/evidence/final-ruleset.json)
   Expected: empty (no diff).

3. YAML edits landed on origin/main:
   gh api /repos/tonyrowles/patent-cite-tool/contents/.github/workflows/v40-verifier-gate.yml --jq '.content' | base64 -d | grep -c "Scope decision (auto-fix"
   Expected: 3
   gh api /repos/tonyrowles/patent-cite-tool/contents/.github/workflows/v40-deps-update.yml --jq '.content' | base64 -d | grep -c "pull_request:"
   Expected: >= 1

4. Verification PR closed + branch deleted:
   git ls-remote origin 'test-only/trigger-hotfix-verify-*'
   Expected: empty.

5. Vitest jobid pins still green (Phase 50 D11/D12 preserved):
   npx vitest run tests/unit/v40-verifier-gate-doc.test.js
   Expected: 12 tests passing.

6. 7 atomic chore(51.1) commits in origin/main:
   git log origin/main --oneline | grep -c 'chore(51.1)'
   Expected: 7.

7. STATE.md / ROADMAP.md reflect Phase 51.1 closure:
   grep -q 'Phase 51.1' .planning/STATE.md && grep -q 'verifier-gate trigger patch' .planning/STATE.md
   grep -qE '\[x\] \*\*Phase 51\.1' .planning/ROADMAP.md
   Expected: both pass.
</verification>

<success_criteria>
Phase 51.1 closes when ALL 5 ROADMAP SCs are PASS:

- **SC-1:** `.github/workflows/v40-verifier-gate.yml` `pull_request:` trigger fires on PRs whose base is `main` (the BASE-ref filter `branches: ['auto-fix/*']` is removed). Verified by: `! grep -qE "branches: \['auto-fix/\*'\]"` on the file at origin/main AND the verification PR's `verification-pr-checks.json` shows the `verifier-gate` context fired.
- **SC-2:** `.github/workflows/v40-deps-update.yml` gains a `pull_request:` trigger so `deps-update-gate` runs as a check provider on dep-update PRs. Verified by: `grep -qE "^\s+pull_request:\s*$"` on the file at origin/main AND the verification PR's `verification-pr-checks.json` shows the `deps-update-gate` context fired.
- **SC-3:** A verification test PR opened against `main` after the fix lands shows BOTH `verifier-gate` AND `deps-update-gate` contexts present in `gh pr checks` output. Verified by: `jq -e '[.[]|.name?]|index("verifier-gate")!=null and index("deps-update-gate")!=null'` on `evidence/verification-pr-checks.json`.
- **SC-4:** The verification PR is CLOSED + branch deleted immediately after evidence capture; no live LOCKED-path touch. Verified by: `evidence/verification-pr-state.json` shows `state="CLOSED"` and `mergedAt=null`; `git ls-remote origin 'test-only/trigger-hotfix-verify-*'` is empty.
- **SC-5:** Ruleset 17086676 ends Phase 51.1 with `bypass_actors=[]` (Phase 50 SC-2 preserved). Verified by: `jq -e '.bypass_actors|length==0 and .current_user_can_bypass=="never"' evidence/final-ruleset.json` AND the byte-equality diff against Phase 50 baseline returns empty.

Additionally:
- REGRESSION-51-01 is empirically closed (every PR into main now has providers for both required status checks).
- Phase 50 SC-1 preserved (`required_status_checks` rule still pins verifier-gate + deps-update-gate to integration_id=15368).
- Phase 50 D11/D12 Vitest pins still pass (the jobid lines `  verifier-gate:` and `  deps-update-gate:` are unchanged).
- STATE.md Pending Todos Phase 56 amendment is in place; ROADMAP.md Phase 51.1 row is checked.
</success_criteria>

<output>
Create `.planning/phases/51.1-required-check-trigger-hotfix/51.1-01-SUMMARY.md` when Task 7 completes. The SUMMARY frontmatter MUST include:
  - status: complete
  - completed_at: <ISO timestamp>
  - requirements_addressed: [REGRESSION-51-01]
  - evidence_dir: evidence/
  - deviations: [<list any task-level inline-fixes>]
  - provides:
      regression_51_01_closed: true
      phase_50_sc1_preserved: true
      phase_50_sc2_preserved: true
      verifier_gate_fires_on_pr_main: true
      deps_update_gate_fires_on_pr_main: true
      bypass_window_open_duration_minutes: <Task 2 to Task 6 elapsed minutes>
  - next_phase: 52
</output>

## PLANNING COMPLETE
