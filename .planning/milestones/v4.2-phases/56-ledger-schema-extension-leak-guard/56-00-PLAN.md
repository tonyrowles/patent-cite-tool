---
phase: 56-ledger-schema-extension-leak-guard
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: true
requirements: []
tags: [ledger, leak-guard, vitest, wave-0-inspection]

must_haves:
  truths:
    - "Wave 0 confirms tests/setup/chrome-stub.js does NOT touch process.env.CI (Research §A1 assumption converted from [ASSUMED] to VERIFIED before Wave 1 begins)"
    - "Wave 0 confirms the existing vi.mock('../e2e/lib/llm-ledger.js', ...) hoisting pattern at tests/unit/auto-fix.test.js:62-67 still works under the currently-pinned Vitest version (`CI=true npx vitest run tests/unit/auto-fix.test.js` exits 0 against unmodified source)"
    - "Wave 1 (plans 01, 02) is unblocked to proceed with the LEDGER-04 test seam pattern as designed in RESEARCH §4"
  artifacts: []
  key_links:
    - from: ".planning/phases/56-ledger-schema-extension-leak-guard/56-RESEARCH.md §A1"
      to: "tests/setup/chrome-stub.js"
      via: "grep -nE 'process\\.env\\.CI' tests/setup/chrome-stub.js"
      pattern: "should return zero matches (NOT setting CI confirms hermetic test env)"
    - from: ".planning/phases/56-ledger-schema-extension-leak-guard/56-RESEARCH.md §A2"
      to: "tests/unit/auto-fix.test.js"
      via: "CI=true npx vitest run tests/unit/auto-fix.test.js"
      pattern: "exit 0 baseline (Wave 1 LEDGER-04 test extends this pattern)"
---

<objective>
Wave 0 verification gate. Convert the two RESEARCH §Assumptions Log [ASSUMED] claims into verified facts BEFORE Wave 1 plans (which build on them) execute. This is an inspection-only plan — no source code is modified, no commits are produced (or, if anything is committed, it is purely informational logging to the SUMMARY artifact).

Purpose: Research §A1 asserts that `tests/setup/chrome-stub.js` does NOT set/unset `process.env.CI`. Research §A2 asserts that `vi.mock('../e2e/lib/llm-ledger.js', ...)` hoisting still works in the pinned Vitest. Both underwrite Wave 1's `LEDGER-04` integration test design (which sets `process.env.CI = 'true'` inside the test and depends on the vi.mock factory at `tests/unit/auto-fix.test.js:62-67`). If either assumption is false, Wave 1's design must adapt before code lands.

Output: A short verification result captured in `.planning/phases/56-ledger-schema-extension-leak-guard/56-00-SUMMARY.md`. Wave 1 proceeds only if both checks pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/56-ledger-schema-extension-leak-guard/56-CONTEXT.md
@.planning/phases/56-ledger-schema-extension-leak-guard/56-RESEARCH.md
@.planning/phases/56-ledger-schema-extension-leak-guard/56-VALIDATION.md

<interfaces>
<!-- Read-only inspection targets — Wave 0 confirms invariants the Wave 1 plans rely on. -->

From tests/setup/chrome-stub.js (current contents, head of file):
```
import { vi } from 'vitest';
const chromeMock = { runtime: {...}, storage: {...} };
vi.stubGlobal('chrome', chromeMock);
```
Note: Only the chrome browser-globals stub. No `process.env.CI` reference at the time of planning. Wave 0 must reverify in case the file is edited between planning and execution.

From tests/unit/auto-fix.test.js:62-67 (vi.mock factory — Wave 1 LEDGER-04 test depends on this pattern):
```
vi.mock('../e2e/lib/llm-ledger.js', () => ({
  readLedger: vi.fn(() => ({ version: 1, months: {} })),
  appendLedgerEntry: vi.fn(),
  countFixAttempts: vi.fn(() => 0),
  LEDGER_PATH: '/tmp/test-ledger.json',
}));
```

From vitest.config.js (RESEARCH §7 — confirms hermetic env mutation is safe inside Wave 1's LEDGER-04 test):
```
fileParallelism: false,
globals: true,
setupFiles: ['./tests/setup/chrome-stub.js'],
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 56-00-W0a: Inspect tests/setup/chrome-stub.js for process.env.CI touches</name>
  <files>(none modified — read-only inspection)</files>
  <read_first>
    - tests/setup/chrome-stub.js (entire file — small)
    - vitest.config.js (confirm setupFiles points only at chrome-stub.js)
    - .planning/phases/56-ledger-schema-extension-leak-guard/56-RESEARCH.md §7 + §Assumptions Log A1
  </read_first>
  <action>
    Run two greps against `tests/setup/chrome-stub.js` to convert RESEARCH §A1 from [ASSUMED] to [VERIFIED]:
      Command 1: `grep -nE 'process\.env\.CI' tests/setup/chrome-stub.js || echo "OK: CI not touched"`
      Command 2: `grep -cE 'process\.env\.CI' tests/setup/chrome-stub.js` (must equal 0)

    If grep returns ANY match: STOP. Record the finding in 56-00-SUMMARY.md "Wave 0 result" section, flag it as a Wave 1 design impact (the LEDGER-04 test must add explicit `delete process.env.CI; process.env.CI = 'true'; try {...} finally { delete process.env.CI; }` in a `beforeEach`/`afterEach` rather than just inside the single test body), and return `## WAVE-0 BLOCKED` to the orchestrator so Wave 1 plan 01 can be revised before execution.

    If grep returns zero matches: RESEARCH §A1 converts from [ASSUMED] → [VERIFIED]. Note this in 56-00-SUMMARY.md. Wave 1 proceeds with the design as written in RESEARCH §4 (single-test env mutation, no beforeEach/afterEach restructure needed).

    Do NOT modify chrome-stub.js, vitest.config.js, or any other file in this task.
  </action>
  <verify>
    <automated>grep -cE 'process\.env\.CI' tests/setup/chrome-stub.js</automated>
    <expect>Output is exactly `0` (zero). Any non-zero count means the assumption A1 is FALSE and Wave 1 must adapt.</expect>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'process\.env\.CI' tests/setup/chrome-stub.js` returns exactly `0`
    - 56-00-SUMMARY.md records the grep output verbatim
    - 56-00-SUMMARY.md explicitly notes either "RESEARCH §A1 VERIFIED — Wave 1 proceeds as designed" OR "RESEARCH §A1 FALSIFIED — Wave 1 plan 01 must add beforeEach/afterEach CI env reset"
  </acceptance_criteria>
  <done>56-00-SUMMARY.md contains the chrome-stub.js grep result and an explicit Wave-1-proceeds-or-blocks decision line.</done>
</task>

<task type="auto">
  <name>Task 56-00-W0b: Confirm Vitest vi.mock hoisting still works (baseline test run)</name>
  <files>(none modified — baseline test execution)</files>
  <read_first>
    - tests/unit/auto-fix.test.js (lines 1-110 — confirm the vi.mock factory at lines 62-67 still references `'../e2e/lib/llm-ledger.js'` and exports `appendLedgerEntry: vi.fn()`)
    - vitest.config.js (entire file — small)
    - .planning/phases/56-ledger-schema-extension-leak-guard/56-RESEARCH.md §4 + §Assumptions Log A2
  </read_first>
  <action>
    Run the existing auto-fix test suite under CI=true to confirm the mock-hoisting pattern is still functional in the pinned Vitest version. This is the exact pattern Wave 1's LEDGER-04 test (in plan 01) will extend.

    Command: `CI=true npx vitest run tests/unit/auto-fix.test.js 2>&1 | tee /tmp/wave0-mock-hoist-baseline.log`

    Inspect the result:
      - Exit code 0 AND no test cases fail → A2 VERIFIED. Wave 1 proceeds.
      - Exit code non-zero OR any test failure → A2 FALSIFIED. Stop, record failure details (test names + line numbers), return `## WAVE-0 BLOCKED` to the orchestrator. RESEARCH §4 notes this would mean "all 20 existing tests in auto-fix.test.js break, not just LEDGER-04" — investigation belongs in a follow-up before plan 01 lands.

    Record the test count (number of `it` blocks executed) and the pass count in 56-00-SUMMARY.md as the Wave-1 baseline. Wave 1 plan 01's verify step references this baseline (plan 01 expects the same count + the new LEDGER-04 test, so the post-plan-01 count must be baseline+1).

    Do NOT modify any source or test file in this task.
  </action>
  <verify>
    <automated>CI=true npx vitest run tests/unit/auto-fix.test.js</automated>
    <expect>Exit code 0; zero failures; record the test count for Wave 1 baseline.</expect>
  </verify>
  <acceptance_criteria>
    - `CI=true npx vitest run tests/unit/auto-fix.test.js` exits 0
    - 56-00-SUMMARY.md records the test count (passing N of N) verbatim
    - 56-00-SUMMARY.md explicitly notes "RESEARCH §A2 VERIFIED — vi.mock factory at tests/unit/auto-fix.test.js:62-67 hoists correctly; Wave 1 plan 01 LEDGER-04 test design is safe to ship"
  </acceptance_criteria>
  <done>56-00-SUMMARY.md contains the baseline `vitest run` output (test counts + exit code) and an explicit Wave-1-proceeds-or-blocks decision line for A2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed in Wave 0) | Wave 0 is inspection-only. No env-var write, no source mutation, no commit produced (other than the summary artifact). Threat surface is read-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-02 | Tampering (cap bypass — referenced from RESEARCH security domain) | tests/setup/chrome-stub.js | mitigate (verify-only) | Wave 0 inspection confirms chrome-stub.js does NOT set CI=true; otherwise every local test silently satisfies safeAppendLedger's CI guard, masking the LEDGER-02 protection. Wave 1 plan 01's LEDGER-04 test design depends on this verification. |
| T-56-04 | Tampering (data integrity — partial-wiring) | tests/unit/auto-fix.test.js vi.mock seam | mitigate (verify-only) | Wave 0 baseline test run confirms the vi.mock factory at lines 62-67 still hoists; Wave 1 plan 01 LEDGER-04 integration test extends this exact seam to catch partial errorClass wiring (Pitfall 3). Without Wave 0, a silently-broken mock pattern could cause LEDGER-04 to pass without actually exercising errorClass. |

(T-56-01 and T-56-03 are addressed by Wave 1 plans 01 and 02 respectively; they have no Wave 0 surface.)
</threat_model>

<verification>
- Both Wave 0 inspections produce explicit VERIFIED/FALSIFIED markers in 56-00-SUMMARY.md
- Wave 1 plans 01 and 02 are unblocked (the summary explicitly says "Wave 1: proceed")
- No file outside `.planning/phases/56-.../56-00-SUMMARY.md` is modified
- No commit is produced (per `commit_docs` policy, the SUMMARY may be committed in a `docs(56)` commit; that is the only acceptable Wave 0 commit)
</verification>

<success_criteria>
- 56-00-SUMMARY.md exists in the phase directory
- The summary contains the verbatim grep output for chrome-stub.js
- The summary contains the verbatim vitest baseline output (or at minimum the exit code and test count)
- The summary contains the line `Wave 1: proceed` if both checks passed, OR `Wave 1: BLOCKED — see notes` if either failed
- No production code (`scripts/`, `tests/e2e/lib/`) is touched in this plan
</success_criteria>

<output>
Create `.planning/phases/56-ledger-schema-extension-leak-guard/56-00-SUMMARY.md` when done.
</output>
