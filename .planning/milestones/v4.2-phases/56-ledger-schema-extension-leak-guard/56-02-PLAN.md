---
phase: 56-ledger-schema-extension-leak-guard
plan: 02
type: execute
wave: 1
depends_on: [56-00]
files_modified:
  - tests/unit/llm-ledger.test.js
autonomous: true
requirements: [LEDGER-03]
tags: [ledger, test-48, vitest, cardinality-relax]

must_haves:
  truths:
    - "Test 48 in tests/unit/llm-ledger.test.js passes on a working copy where the committed tests/e2e/.llm-spend-ledger.json contains MORE than one entry per month bucket (i.e., after one or more live auto-fix runs append to the bootstrap month)"
    - "Test 48's per-entry shape assertions (phase='39-bootstrap', transport='sdk', cost_usd=0, source='phase-39-flip', model='claude-sonnet-4-6') still execute against the bootstrap entry (now selected by filter rather than by index 0 of the bucket's iterations array)"
    - "No other Test in tests/unit/llm-ledger.test.js is modified; the 60 sibling tests in the file remain byte-identical"
    - "tests/e2e/lib/llm-ledger.js is NOT modified (Pitfall E — touching the LEDGER_PATH IIFE breaks Test 32/33/49 and the WR-05 boundary)"
  artifacts:
    - path: "tests/unit/llm-ledger.test.js"
      provides: "Test 48 relaxed to '≥1 entry with phase=\"39-bootstrap\"' across all month buckets; per-entry shape checks preserved against the filter-selected bootstrap entry"
      contains: "Test 48"
  key_links:
    - from: "tests/unit/llm-ledger.test.js Test 48 body"
      to: "tests/e2e/.llm-spend-ledger.json on-disk content"
      via: "fs.readFileSync + JSON.parse + Object.values(j.months).flatMap(m => m.iterations ?? []).filter(e => e?.phase === '39-bootstrap')"
      pattern: "phase === '39-bootstrap'"
---

<objective>
Relax Test 48's cardinality assertions so the regression suite passes on a working copy that has accumulated additional iterations from live auto-fix runs on origin/main. Preserve the per-entry shape invariants on the bootstrap entry (which remains forever the first iteration written at Phase 39 flip, source='phase-39-flip').

Purpose: Test 48 currently asserts "exactly 1 month bucket, exactly 1 invocation, exactly 1 iteration, total_usd === 0" against `tests/e2e/.llm-spend-ledger.json`. The first live auto-fix run that lands a `source: 'auto-fix-api'` entry breaks all four assertions simultaneously (added invocation, added iteration, possibly new month bucket, non-zero total_usd). This breaks `npm test` on origin/main, blocking the v4.2 milestone DoD (UAT-47-a). RESEARCH §3 + CONTEXT.md §decisions lock the rewrite mechanics: filter `iterations` across all months for `phase === '39-bootstrap'`, assert `length >= 1`, and run the existing per-entry shape checks on `bootstraps[0]` (which remains the seed entry forever — appends push to the END of the iterations array, RESEARCH §3 "Why preserve per-entry assertions").

Output: Test 48's body rewritten in place (~10 lines changed). Single atomic commit recommended: `fix(56): relax Test 48 cardinality for live auto-fix runs (LEDGER-03)`. Disjoint from plan 01's files — fully parallelizable in Wave 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-ledger-schema-extension-leak-guard/56-CONTEXT.md
@.planning/phases/56-ledger-schema-extension-leak-guard/56-RESEARCH.md
@.planning/phases/56-ledger-schema-extension-leak-guard/56-VALIDATION.md
@.planning/research/PITFALLS.md
@.planning/phases/56-ledger-schema-extension-leak-guard/56-00-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs in-context. Do NOT explore the codebase — use what's here. -->

From tests/unit/llm-ledger.test.js — Test 48 location and current body (verbatim, RESEARCH §3, verified by direct inspection on 2026-06-04):

Test 48 lives within `describe('Phase 39 LEDGER-04: committed ledger flip', () => { ... })` at lines ~997-1023.

Current it() body (line ~999 through ~1023):
  it('Test 48: committed tests/e2e/.llm-spend-ledger.json is valid v1 with bootstrap entry', () => {
    const REAL_LEDGER_PATH = path.resolve(
      path.dirname(fileURLToPath(import.meta.url)),
      '..',
      'e2e',
      '.llm-spend-ledger.json',
    );
    expect(fs.existsSync(REAL_LEDGER_PATH)).toBe(true);
    const text = fs.readFileSync(REAL_LEDGER_PATH, 'utf8');
    const j = JSON.parse(text);
    expect(j.version).toBe(1);
    // Exactly one bootstrap entry — fresh-start per CONTEXT seed policy.
    const months = Object.keys(j.months);
    expect(months.length).toBe(1);             // ← Cardinality assertion 1 — RELAX
    const bucket = j.months[months[0]];
    expect(bucket.invocations).toBe(1);        // ← Cardinality assertion 2 — RELAX
    expect(bucket.total_usd).toBe(0);          // ← Cardinality assertion 3 — RELAX
    expect(bucket.iterations.length).toBe(1);  // ← Cardinality assertion 4 — RELAX
    const it = bucket.iterations[0];
    expect(it.phase).toBe('39-bootstrap');     // ← Per-entry shape — PRESERVE (rename to boot)
    expect(it.transport).toBe('sdk');          // ← Per-entry shape — PRESERVE
    expect(it.cost_usd).toBe(0);               // ← Per-entry shape — PRESERVE
    expect(it.source).toBe('phase-39-flip');   // ← Per-entry shape — PRESERVE
    expect(it.model).toBe('claude-sonnet-4-6');// ← Per-entry shape — PRESERVE
  });

The describe-block name is `'Phase 39 LEDGER-04: committed ledger flip'` (matches the existing label structure; do NOT rename it — the LEDGER-04 substring in that describe name is a Phase 39 artifact, NOT this phase's LEDGER-04 plan 01 test).

The next test in the file (Test 49) at line ~1024 begins:
  it('Test 49: .gitignore does NOT contain tests/e2e/.llm-spend-ledger.json (LEDGER-04 commitment)', ...
This is the boundary of Test 48; the rewrite must end before this `it` block.

The bootstrap entry on disk (RESEARCH §3 verification, lines 1018-1022 shape):
  - phase: '39-bootstrap'
  - transport: 'sdk'
  - cost_usd: 0
  - source: 'phase-39-flip'
  - model: 'claude-sonnet-4-6'
  - committed timestamp: 2026-05-31T16:03:31.594Z (in month bucket '2026-05')

Subsequent live auto-fix runs append to the end of an iterations array — never the front. Months sort lexicographically by UTC date; '2026-05' < '2026-06' < ... — so `Object.values(j.months).flatMap(m => m.iterations ?? []).filter(e => e?.phase === '39-bootstrap')[0]` deterministically resolves to the bootstrap seed entry forever (RESEARCH §3 "Risk note: Risk = 0").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 56-02-01: Relax Test 48 cardinality + preserve per-entry shape (LEDGER-03)</name>
  <files>tests/unit/llm-ledger.test.js</files>
  <read_first>
    - tests/unit/llm-ledger.test.js (entire file — required so executor sees the `describe('Phase 39 LEDGER-04: committed ledger flip', ...)` block boundary, the exact line numbers of Test 48's it() body, and the surrounding Tests 47, 49, 50 which must remain byte-unchanged)
    - tests/e2e/.llm-spend-ledger.json (current on-disk state — confirm a bootstrap entry with phase='39-bootstrap' exists; the test must pass against THIS file's current content as well as future content with additional entries)
    - .planning/phases/56-ledger-schema-extension-leak-guard/56-RESEARCH.md §3 (Test 48 Rewrite Mechanics — the exact source-text rewrite is quoted; follow it)
    - .planning/phases/56-ledger-schema-extension-leak-guard/56-CONTEXT.md (decisions block — locked: "Assertion relaxed from 'exactly 1 bootstrap entry' to '≥1 entry with phase=39-bootstrap'")
    - .planning/research/PITFALLS.md Pitfall E (LOAD-BEARING — do NOT touch the LEDGER_PATH IIFE in llm-ledger.js)
    - .planning/phases/56-ledger-schema-extension-leak-guard/56-00-SUMMARY.md (Wave 0 verification — must explicitly say "Wave 1: proceed"; if it does not, halt and notify orchestrator)
  </read_first>
  <behavior>
    - Test 1 (the existing Test 48): after the rewrite, the test PASSES against the current committed ledger (1 month bucket, 1 iteration matching '39-bootstrap'). This is the regression baseline — the rewrite must not introduce a behavioral break.
    - Test 2 (the live-runs forward-compatibility, expressed via the rewrite, not a separate test): if `tests/e2e/.llm-spend-ledger.json` accumulates additional entries in `2026-06` or later months with various `phase` values (e.g., `'42-auto-fix'`), Test 48 STILL PASSES because it filters by `e?.phase === '39-bootstrap'` and asserts only `length >= 1`. The five per-entry shape assertions still run against the filter-result's `[0]`, which remains the bootstrap seed entry (lexicographically-first month, first-pushed iteration).
    - Test 3 (negative — Pitfall E sanity): tests/e2e/lib/llm-ledger.js is NOT modified. `git diff -- tests/e2e/lib/llm-ledger.js` is empty.
  </behavior>
  <action>
    Edit Test 48's it() body in tests/unit/llm-ledger.test.js at lines ~999-1023 in place. The rewrite follows RESEARCH §3 "Recommended minimal-diff rewrite" verbatim.

    Specifically:

    1. Update the test name string (the first argument to `it(...)`) from `'Test 48: committed tests/e2e/.llm-spend-ledger.json is valid v1 with bootstrap entry'` to `'Test 48: committed tests/e2e/.llm-spend-ledger.json is valid v1 with ≥1 bootstrap entry'` (the `≥1` substring documents the relaxation; this also makes the `-t 'Test 48'` filter from VALIDATION.md per-task map row `56-02-01` continue to match).

    2. Inside the it() body, lines containing:
         expect(j.version).toBe(1);
         expect(fs.existsSync(REAL_LEDGER_PATH)).toBe(true);
         expect(text)... (the JSON.parse call and the path.resolve/fileURLToPath header lines)
       remain BYTE-UNCHANGED. Only the cardinality-and-bootstrap section is rewritten.

    3. DELETE the comment line `// Exactly one bootstrap entry — fresh-start per CONTEXT seed policy.` and the next four lines (the four cardinality assertions):
         const months = Object.keys(j.months);
         expect(months.length).toBe(1);
         const bucket = j.months[months[0]];
         expect(bucket.invocations).toBe(1);
         expect(bucket.total_usd).toBe(0);
         expect(bucket.iterations.length).toBe(1);
         const it = bucket.iterations[0];

    4. INSERT a replacement block that does:
         // Phase 56 LEDGER-03: relaxed from "exactly 1 bootstrap entry" to
         // "≥1 entry with phase='39-bootstrap'" because live auto-fix runs on
         // origin/main append additional entries (post-Phase 56 errorClass-wired
         // entries land in this file after every CI run). The per-entry shape
         // check on the bootstrap entry below is unchanged.
         const allIterations = Object.values(j.months).flatMap((m) => m.iterations ?? []);
         const bootstraps = allIterations.filter((e) => e?.phase === '39-bootstrap');
         expect(bootstraps.length).toBeGreaterThanOrEqual(1);
         const boot = bootstraps[0];

    5. UPDATE the 5 per-entry shape assertions to use `boot` instead of `it` (the rename also avoids the visual collision with Vitest's `it` test-runner identifier — RESEARCH §3 calls this out as a clarity improvement):
         expect(boot.phase).toBe('39-bootstrap');
         expect(boot.transport).toBe('sdk');
         expect(boot.cost_usd).toBe(0);
         expect(boot.source).toBe('phase-39-flip');
         expect(boot.model).toBe('claude-sonnet-4-6');

    Do NOT touch any other test in the file. Tests 32, 33, 34-37, 47, 49, 50, and the remaining 56-61 sibling it() blocks are byte-unchanged.

    Do NOT modify `tests/e2e/lib/llm-ledger.js` (Pitfall E — touching the LEDGER_PATH IIFE breaks Tests 32, 33, 49, and the WR-05 boundary; ZERO lines may change in that file).

    Do NOT modify `tests/e2e/.llm-spend-ledger.json` (the on-disk committed ledger is updated only by live CI runs; this phase does not seed it).

    Do NOT add new fields to the per-entry shape assertions (e.g., do not add `expect(boot.errorClass).toBe(...)` — the bootstrap entry pre-dates LEDGER-01 wiring and lacks the field; adding such an assertion would fail).
  </behavior>
  <action>
    Apply the precise edit described in `<behavior>` above. Use the Edit tool (NOT Write — the file is large and only ~10 lines change). The exact target lines for the edit are inside the `describe('Phase 39 LEDGER-04: committed ledger flip', () => { ... })` block, between the existing `it(...)` opening and closing, at the lines containing the four cardinality assertions and the `const it = bucket.iterations[0];` binding.

    Verification sub-steps the executor should run in order:
      1. After the edit, `grep -n "Test 48" tests/unit/llm-ledger.test.js` should show the new test name line (with `≥1 bootstrap entry`).
      2. `grep -c 'toBeGreaterThanOrEqual(1)' tests/unit/llm-ledger.test.js` should show ≥ 1 occurrence (the new relaxation).
      3. `grep -c 'expect(bucket.invocations).toBe(1)' tests/unit/llm-ledger.test.js` should equal 0 (the old cardinality assertion is gone).
      4. `grep -c "expect(months.length).toBe(1)" tests/unit/llm-ledger.test.js` should equal 0 (the old month-cardinality assertion is gone).
      5. `git diff -- tests/e2e/lib/llm-ledger.js` should be empty (Pitfall E invariant).
      6. The test still passes: `CI=true npx vitest run tests/unit/llm-ledger.test.js -t 'Test 48'` exits 0.
      7. The whole file still passes: `CI=true npx vitest run tests/unit/llm-ledger.test.js` exits 0 — confirms no neighbor test was disturbed.
  </action>
  <verify>
    <automated>
echo "=== gate 1: new relaxation present ===" && \
  grep -c 'toBeGreaterThanOrEqual(1)' tests/unit/llm-ledger.test.js && \
  echo "=== gate 2: old cardinality gone (expected 0) ===" && \
  grep -c 'expect(bucket.invocations).toBe(1)' tests/unit/llm-ledger.test.js && \
  echo "=== gate 3: old month-cardinality gone (expected 0) ===" && \
  grep -cE 'expect\(months\.length\)\.toBe\(1\)' tests/unit/llm-ledger.test.js && \
  echo "=== gate 4: bootstraps filter present ===" && \
  grep -c "filter((e) => e?.phase === '39-bootstrap')" tests/unit/llm-ledger.test.js && \
  echo "=== gate 5: per-entry shape on boot[0] preserved ===" && \
  grep -c "expect(boot.phase).toBe('39-bootstrap')" tests/unit/llm-ledger.test.js && \
  echo "=== gate 6: Pitfall E — llm-ledger.js byte-unchanged ===" && \
  test -z "$(git diff -- tests/e2e/lib/llm-ledger.js)" && echo OK || { echo "FAIL: llm-ledger.js was modified"; exit 1; } && \
  echo "=== gate 7: Test 48 passes ===" && \
  CI=true npx vitest run tests/unit/llm-ledger.test.js -t 'Test 48' && \
  echo "=== gate 8: full file still passes (no sibling-test disturbance) ===" && \
  CI=true npx vitest run tests/unit/llm-ledger.test.js
    </automated>
    <expect>
      gate 1: ≥ 1
      gate 2: exactly 0
      gate 3: exactly 0
      gate 4: ≥ 1
      gate 5: exactly 1
      gate 6: prints OK
      gate 7: exit 0, Test 48 passes
      gate 8: exit 0, all 56+ existing tests in the file pass (no neighbor disturbed)
    </expect>
  </verify>
  <acceptance_criteria>
    - `grep -c 'toBeGreaterThanOrEqual(1)' tests/unit/llm-ledger.test.js` ≥ 1
    - `grep -c 'expect(bucket.invocations).toBe(1)' tests/unit/llm-ledger.test.js` = 0
    - `grep -cE 'expect\(months\.length\)\.toBe\(1\)' tests/unit/llm-ledger.test.js` = 0
    - `grep -c "filter((e) => e?.phase === '39-bootstrap')" tests/unit/llm-ledger.test.js` ≥ 1
    - `grep -c "expect(boot.phase).toBe('39-bootstrap')" tests/unit/llm-ledger.test.js` = 1
    - `grep -c "expect(boot.transport).toBe('sdk')" tests/unit/llm-ledger.test.js` = 1
    - `grep -c "expect(boot.cost_usd).toBe(0)" tests/unit/llm-ledger.test.js` = 1
    - `grep -c "expect(boot.source).toBe('phase-39-flip')" tests/unit/llm-ledger.test.js` = 1
    - `grep -c "expect(boot.model).toBe('claude-sonnet-4-6')" tests/unit/llm-ledger.test.js` = 1
    - `git diff -- tests/e2e/lib/llm-ledger.js` produces no output (Pitfall E)
    - `git diff -- tests/e2e/.llm-spend-ledger.json` produces no output (the on-disk ledger is not touched)
    - `CI=true npx vitest run tests/unit/llm-ledger.test.js -t 'Test 48'` exits 0
    - `CI=true npx vitest run tests/unit/llm-ledger.test.js` exits 0 (full file; all 56+ tests green; no sibling test was disturbed)
    - The diff for tests/unit/llm-ledger.test.js touches ONLY lines inside Test 48's it() body and its name string (verifiable by reviewing the diff hunks — Tests 47 and 49 are unchanged)
  </acceptance_criteria>
  <done>Test 48 body is rewritten to filter for `phase === '39-bootstrap'` across all months and assert `bootstraps.length >= 1`; the five per-entry shape checks run against `boot = bootstraps[0]`; all gates pass; tests/e2e/lib/llm-ledger.js is byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vitest test runner → committed tests/e2e/.llm-spend-ledger.json (read-only) | Test 48 reads the on-disk committed ledger and asserts schema/seed invariants. The relaxation does not change the read direction (still read-only); it only relaxes the assertion shape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-03 | Denial of Service (operational — test regression on live ledger) | tests/unit/llm-ledger.test.js Test 48 | mitigate | Cardinality assertions relaxed (Task 56-02-01) so `npm test` exits 0 on a working copy that has had live auto-fix runs. The relaxation is a precise change: cardinality lower-bound only; per-entry shape on the bootstrap entry preserved verbatim. No information disclosure, no tampering surface introduced. |
| T-56-04 | Tampering (data integrity — bootstrap shape regression) | tests/unit/llm-ledger.test.js Test 48 per-entry shape assertions | mitigate | The five per-entry shape assertions (phase, transport, cost_usd, source, model) are preserved verbatim against `boot = bootstraps[0]`. The bootstrap entry remains forever the first-pushed iteration (RESEARCH §3 "Risk note: Risk = 0"); any future tampering with the seed entry's shape immediately fails Test 48. |
| T-56-SC | Tampering | npm package supply chain | accept | Plan 02 installs ZERO new packages. No legitimacy audit needed. |
</threat_model>

<verification>
- `CI=true npx vitest run tests/unit/llm-ledger.test.js -t 'Test 48'` exits 0
- `CI=true npx vitest run tests/unit/llm-ledger.test.js` exits 0 (all 56+ existing tests, including Tests 32, 33, 47, 49, 50, still pass)
- `git diff -- tests/e2e/lib/llm-ledger.js` is empty (Pitfall E)
- `git diff -- tests/e2e/.llm-spend-ledger.json` is empty (the on-disk ledger is not touched by this plan)
- The diff for tests/unit/llm-ledger.test.js touches ONLY lines inside the Test 48 it() block (the rewrite is precise; no neighbor tests should appear in the diff hunks)
</verification>

<success_criteria>
- LEDGER-03: Test 48's cardinality assertions are relaxed to `bootstraps.length >= 1`. The per-entry shape assertions (phase='39-bootstrap', transport='sdk', cost_usd=0, source='phase-39-flip', model='claude-sonnet-4-6') still execute on the filter-selected bootstrap entry. `npm test` exits 0 on a working copy where the committed ledger has accumulated additional iterations.
- Pitfall E invariant: tests/e2e/lib/llm-ledger.js is byte-unchanged.
- All 56+ existing tests in tests/unit/llm-ledger.test.js still pass; only Test 48's body changed.
- Plan 02 is disjoint from plan 01's files — both can execute in parallel in Wave 1.
</success_criteria>

<output>
Create `.planning/phases/56-ledger-schema-extension-leak-guard/56-02-SUMMARY.md` when done.
</output>
