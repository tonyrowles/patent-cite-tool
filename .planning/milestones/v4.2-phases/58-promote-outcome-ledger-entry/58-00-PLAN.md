---
phase: 58-promote-outcome-ledger-entry
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: true
requirements: [PROMOTE-04]  # baseline sha256/byte-count capture underpins the PROMOTE-04 byte-unchanged invariant pinned in Plan 01
must_haves:
  truths:
    - "PHASE_58_BASELINE ref captured at git HEAD (0442cff4bb7d062a892d027ebf0b9fb9b07e0352 per pre-flight; planner-recorded; executor re-confirms)"
    - "assertTripleGate body lines 89-103 of scripts/auto-fix-promote.mjs hash sha256=5311c1d5364b1b8558d44029432bfb0d5164c19fcc38b9b2d6fcd539c2e0c40f"
    - "_skipCiGuard:\\s*true non-comment occurrence count in scripts/auto-fix-promote.mjs equals 1 (line 434)"
    - "tests/unit/auto-fix-promote-gate.test.js test count equals 26 across 6 describe blocks"
    - "tests/e2e/lib/llm-ledger.js sha256 captured for the byte-unchanged invariant"
  artifacts:
    - path: ".planning/phases/58-promote-outcome-ledger-entry/58-BASELINE.txt"
      provides: "Recorded baseline ref + hashes used by Wave 1 byte-unchanged assertions"
      contains: "PHASE_58_BASELINE, assertTripleGate sha256, llm-ledger.js sha256"
  key_links: []
---

<objective>
Wave 0 — capture the pre-Phase-58 baseline that Wave 1's byte-unchanged
assertions and Vitest pins reference. No source code or test code is modified
in this plan. The four anchored facts (HEAD ref, assertTripleGate body sha256,
llm-ledger.js sha256, _skipCiGuard non-comment count) are recorded into
58-BASELINE.txt so they survive context resets and can be cited verbatim in
Wave 1 commit messages and Vitest expected-string blocks.

Purpose: Establish a single, durable, machine-verifiable snapshot that Wave 1
references. Without this snapshot, the assertTripleGate body-pin assertion has
no anchor and any silent drift between research-time and execute-time would go
undetected.

Output: 58-BASELINE.txt at the phase directory root with the four anchored
values. Optional sanity halt if any value diverges from RESEARCH §3/§9 (in
which case the planner's locked constants are stale and the phase must abort).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-promote-outcome-ledger-entry/58-CONTEXT.md
@.planning/phases/58-promote-outcome-ledger-entry/58-RESEARCH.md
@.planning/phases/58-promote-outcome-ledger-entry/58-VALIDATION.md
@scripts/auto-fix-promote.mjs
@tests/unit/auto-fix-promote-gate.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 0.1: Capture PHASE_58_BASELINE + verify locked constants</name>
  <files>.planning/phases/58-promote-outcome-ledger-entry/58-BASELINE.txt</files>
  <read_first>
    scripts/auto-fix-promote.mjs (lines 89-103 — assertTripleGate body span per RESEARCH §3);
    scripts/auto-fix-promote.mjs (line 434 — sole non-comment _skipCiGuard:true literal per RESEARCH §9);
    tests/unit/auto-fix-promote-gate.test.js (full file — 26-test baseline per RESEARCH §5);
    tests/e2e/lib/llm-ledger.js (full file — Phase 56 byte-unchanged invariant);
    .planning/phases/58-promote-outcome-ledger-entry/58-RESEARCH.md (§3 sha256 5311c1d5364b1b8558d44029432bfb0d5164c19fcc38b9b2d6fcd539c2e0c40f, §5 26 tests, §9 grep-count == 1)
  </read_first>
  <action>
Capture the pre-Phase-58 baseline as 58-BASELINE.txt in the phase directory.
Record verbatim (one fact per line, KEY=VALUE form, no trailing whitespace):

1. PHASE_58_BASELINE — run `git rev-parse HEAD` from the repo root; record the 40-char sha as `PHASE_58_BASELINE=<sha>`.
2. ASSERT_TRIPLE_GATE_BODY_SHA256 — run `awk 'NR>=89 && NR<=103' scripts/auto-fix-promote.mjs | sha256sum | awk '{print $1}'`; record as `ASSERT_TRIPLE_GATE_BODY_SHA256=<hex>`. MUST equal `5311c1d5364b1b8558d44029432bfb0d5164c19fcc38b9b2d6fcd539c2e0c40f` per RESEARCH §3. If it differs, HALT — Phase 53 baseline has drifted and the planner's locked verbatim string in Wave 1 is wrong.
3. ASSERT_TRIPLE_GATE_BODY_BYTES — run `awk 'NR>=89 && NR<=103' scripts/auto-fix-promote.mjs | wc -c`; record. MUST equal `815` per RESEARCH §3.
4. SKIP_CI_GUARD_NONCOMMENT_COUNT — run `grep -nE '_skipCiGuard:\\s*true' scripts/auto-fix-promote.mjs | grep -vE '^[0-9]+:\\s*//' | wc -l`; record. MUST equal `1` per RESEARCH §9.
5. LLM_LEDGER_SHA256 — run `sha256sum tests/e2e/lib/llm-ledger.js | awk '{print $1}'`; record. Used by Wave 1 verification-gate (file byte-unchanged invariant).
6. PROMOTE_GATE_TEST_COUNT — run `grep -cE "^\\s*it\\(" tests/unit/auto-fix-promote-gate.test.js`; record. MUST equal `26` per RESEARCH §5.

Write the six KEY=VALUE lines to `.planning/phases/58-promote-outcome-ledger-entry/58-BASELINE.txt`.

If ANY of the four hard-pinned values (ASSERT_TRIPLE_GATE_BODY_SHA256, ASSERT_TRIPLE_GATE_BODY_BYTES, SKIP_CI_GUARD_NONCOMMENT_COUNT, PROMOTE_GATE_TEST_COUNT) diverges from its required value above, do NOT write the file. Instead, surface the divergence with the actual measured value and the required value, and HALT the phase — Wave 1 plans reference these as locked constants and would generate incorrect commits otherwise.
  </action>
  <verify>
    <automated>test -f .planning/phases/58-promote-outcome-ledger-entry/58-BASELINE.txt && grep -q '^ASSERT_TRIPLE_GATE_BODY_SHA256=5311c1d5364b1b8558d44029432bfb0d5164c19fcc38b9b2d6fcd539c2e0c40f$' .planning/phases/58-promote-outcome-ledger-entry/58-BASELINE.txt && grep -q '^SKIP_CI_GUARD_NONCOMMENT_COUNT=1$' .planning/phases/58-promote-outcome-ledger-entry/58-BASELINE.txt && grep -q '^PROMOTE_GATE_TEST_COUNT=26$' .planning/phases/58-promote-outcome-ledger-entry/58-BASELINE.txt && grep -q '^ASSERT_TRIPLE_GATE_BODY_BYTES=815$' .planning/phases/58-promote-outcome-ledger-entry/58-BASELINE.txt</automated>
  </verify>
  <acceptance_criteria>
58-BASELINE.txt exists with six KEY=VALUE lines. ASSERT_TRIPLE_GATE_BODY_SHA256 equals 5311c1d5364b1b8558d44029432bfb0d5164c19fcc38b9b2d6fcd539c2e0c40f. ASSERT_TRIPLE_GATE_BODY_BYTES equals 815. SKIP_CI_GUARD_NONCOMMENT_COUNT equals 1. PROMOTE_GATE_TEST_COUNT equals 26. PHASE_58_BASELINE and LLM_LEDGER_SHA256 are captured (specific values not asserted here — Wave 1 references them by name).
  </acceptance_criteria>
  <done>
58-BASELINE.txt is committed-ready. Wave 1 plans can reference the four locked constants as verified-against-live-tree facts. No source code or test code was modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner → executor | Locked constants from RESEARCH must match live tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-05 | Tampering | assertTripleGate body integrity | mitigate | Baseline hash captured BEFORE any Wave 1 edit; Wave 1 Vitest pin (Plan 01) references this hash as the expected value. Drift between planner-locked sha256 (5311c1d5364b1b8558d44029432bfb0d5164c19fcc38b9b2d6fcd539c2e0c40f) and live tree halts the phase. |
| T-58-06 | Tampering | _skipCiGuard non-comment count | mitigate | Baseline count == 1 captured; Wave 1 Vitest pin (Plan 01) re-verifies after edits. |
</threat_model>

<verification>
- 58-BASELINE.txt exists at `.planning/phases/58-promote-outcome-ledger-entry/58-BASELINE.txt`
- Six KEY=VALUE lines present
- Four hard-pinned values match RESEARCH §3/§5/§9 exactly
- No source code or test code modified (zero files in files_modified)
</verification>

<success_criteria>
- ASSERT_TRIPLE_GATE_BODY_SHA256 confirmed at 5311c1d5364b1b8558d44029432bfb0d5164c19fcc38b9b2d6fcd539c2e0c40f
- SKIP_CI_GUARD_NONCOMMENT_COUNT confirmed at 1
- PROMOTE_GATE_TEST_COUNT confirmed at 26
- Wave 1 (Plan 01) can begin with locked constants validated against live tree
</success_criteria>

<output>
Create `.planning/phases/58-promote-outcome-ledger-entry/58-00-SUMMARY.md` with the captured baseline values and a one-line confirmation that the four hard-pinned constants match RESEARCH.
</output>
