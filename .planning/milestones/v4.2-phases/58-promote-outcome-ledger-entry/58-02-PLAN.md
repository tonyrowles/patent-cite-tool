---
phase: 58-promote-outcome-ledger-entry
plan: 02
type: execute
wave: 2
depends_on: ["58-01"]
files_modified:
  - .github/workflows/v40-auto-promote.yml
  - tests/e2e/scripts/v40-auto-promote-yaml.test.js
autonomous: true
requirements: [PROMOTE-02, PROMOTE-03]
must_haves:
  truths:
    - "v40-auto-promote.yml pre-resolves fingerprint from the SOURCE ISSUE body via gh issue view --json body before invoking auto-fix-promote.mjs"
    - "v40-auto-promote.yml pre-resolves errorClass from the SOURCE ISSUE labels (via gh issue view --json labels) before invoking auto-fix-promote.mjs"
    - "v40-auto-promote.yml pre-resolves model from the upstream auto-fix ledger entry by jq-lookup keyed on the resolved fingerprint (matches the source: 'auto-fix-api' entry written by Phase 54+56) before invoking auto-fix-promote.mjs"
    - "All three pre-resolved values are passed via the new --fingerprint, --error-class, AND --model argv flags on the existing node invocation"
    - "If fingerprint cannot be resolved (no <!-- fp: ... --> marker in issue body), the workflow step fails with a clear error message — does NOT silently default to empty"
    - "If errorClass cannot be resolved (no recognized ERROR_CLASS label on the source issue), the workflow step fails with a clear error message — does NOT silently default to empty"
    - "If model cannot be resolved (no matching upstream auto-fix ledger entry for the fingerprint), the workflow step HARD-FAILS with a clear error message — does NOT silently default to sonnet (would break a-b-winner.mjs per-arm attribution for the opus arm)"
    - "Existing argv flags and their order are byte-unchanged; --fingerprint, --error-class, and --model are added as new lines, not edits to existing lines"
    - "v40-auto-fix.yml is NOT modified (LOAD-BEARING anti-feature per Pitfall 1 — direct-to-main ledger commit stays byte-unchanged)"
    - "Vitest YAML contract test in tests/e2e/scripts/v40-auto-promote-yaml.test.js gains assertions pinning the three new pre-resolution steps AND the three new argv lines (--fingerprint, --error-class, --model)"
  artifacts:
    - path: ".github/workflows/v40-auto-promote.yml"
      provides: "Workflow-side fingerprint + errorClass + model pre-resolution and argv threading to scripts/auto-fix-promote.mjs"
      contains: "gh issue view ${SOURCE_ISSUE} --json body, jq -r .months[].iterations[] | select(.fingerprint == $FP and .source == 'auto-fix-api') | .model, --fingerprint, --error-class, --model"
    - path: "tests/e2e/scripts/v40-auto-promote-yaml.test.js"
      provides: "Vitest YAML contract assertion pinning the new pre-resolution step names and the three new argv flags"
      contains: "Pre-resolve source-issue fingerprint, Pre-resolve source-issue errorClass, Pre-resolve upstream-ledger model, --fingerprint, --error-class, --model"
  key_links:
    - from: "v40-auto-promote.yml pre-resolution step"
      to: "scripts/auto-fix-promote.mjs main() outcome entries"
      via: "argv --fingerprint $FP, --error-class $EC, and --model $MODEL"
      pattern: "--fingerprint \\\".*\\\""
    - from: "v40-auto-promote.yml model pre-resolution step"
      to: "tests/e2e/.llm-spend-ledger.json upstream auto-fix entry"
      via: "jq query keyed on fingerprint match + source == 'auto-fix-api' filter; extracts .model field"
      pattern: "jq -r .*select.*fingerprint.*source.*auto-fix-api"
---

<objective>
Wire the workflow-side pre-resolution of fingerprint, errorClass, AND model
into .github/workflows/v40-auto-promote.yml, then thread all three values into
the existing per-case invocation of scripts/auto-fix-promote.mjs via the new
--fingerprint, --error-class, AND --model argv flags introduced by Plan 01.
Also add a Vitest YAML contract assertion in
tests/e2e/scripts/v40-auto-promote-yaml.test.js pinning the new step names and
argv flags.

Purpose: Plan 01 added the script-side acceptance of --fingerprint,
--error-class, and --model. Without the workflow passing all three, outcome
entries written in production would have fingerprint:null / errorClass:null /
model:null — and a-b-winner.mjs's isAttributable filter (lines 178-189) would
drop them all. The model field specifically: a hardcoded sonnet (the verbatim
literal that Plan 01's pre-revision draft used) would mean the opus arm
receives zero outcome entries and stays in indefinite abstention. Model must
be sourced from the UPSTREAM auto-fix ledger entry (Phase 54 wired `model` on
each auto-fix-api entry; Phase 56 wired `fingerprint` on the same), which we
look up by jq query keyed on the resolved fingerprint. The milestone DoD
("a-b-winner can exit abstention automatically once entries accumulate")
requires per-arm attribution. Plan 02 closes the workflow gap for all three
values.

Output: Single atomic commit modifying TWO files:
  1. .github/workflows/v40-auto-promote.yml — adds three pre-resolution steps
     (fingerprint, errorClass, model) and three new argv lines on the
     existing node invocation.
  2. tests/e2e/scripts/v40-auto-promote-yaml.test.js — adds a contract
     assertion pinning the three new step names and the three new argv flags.

v40-auto-fix.yml stays byte-unchanged (LOAD-BEARING anti-feature; not
refactored — see REQUIREMENTS.md COMMIT-04 and Pitfall 1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-promote-outcome-ledger-entry/58-CONTEXT.md
@.planning/phases/58-promote-outcome-ledger-entry/58-RESEARCH.md
@.planning/phases/58-promote-outcome-ledger-entry/58-VALIDATION.md
@.planning/phases/58-promote-outcome-ledger-entry/58-01-SUMMARY.md
@.github/workflows/v40-auto-promote.yml
@scripts/auto-fix-promote.mjs

<interfaces>
<!-- Locked contracts from Plan 01 + RESEARCH; executor uses verbatim. -->

From scripts/auto-fix-promote.mjs (post-Plan 01):
- parseArgv now accepts `--fingerprint <12-hex>` and `--error-class <ERROR_CLASS>`
- Both default to null when absent
- Both validated: fingerprint via `/^[0-9a-f]{12}$/`, errorClass via `/^[A-Z_][A-Z0-9_]*$/`; malformed exits 2
- Both threaded into PROMOTE-02 and PROMOTE-03 outcome entries verbatim

From .github/workflows/v40-auto-promote.yml (current state):
- Step "Parse PR body + labels + source issue" (lines 97-168): produces steps.parse.outputs.{pr_labels, case_ids, source_issue, partial_passing_cases}
- Step "Resolve source-issue labels" (lines 170-178): produces steps.source.outputs.source_issue_labels (CSV)
- Step "Triple-gate + runPromote (per affected case)" (lines 180-211): the for-loop node invocation that this plan extends with two new argv lines
- env in the parse step: PR_NUMBER from github.event.pull_request.number
- env in the promote step (lines 191-197): PR_NUMBER, PR_LABELS, SOURCE_ISSUE, SOURCE_ISSUE_LABELS, CASE_IDS, PARTIAL_PASSING_CASES

From RESEARCH §2 (fingerprint pre-resolution recipe):
- gh issue view ${SOURCE_ISSUE} --json body --jq '.body' | grep -oE '<!-- fp: [0-9a-f]{12} -->' | head -1 | sed -E 's/<!-- fp: ([0-9a-f]{12}) -->/\\1/'
- Hard-error if FP is empty: `echo "::error::Could not extract fingerprint from issue #${SOURCE_ISSUE}"; exit 1`

From RESEARCH §1 + Pitfall 1 (errorClass pre-resolution):
- gh issue view ${SOURCE_ISSUE} --json labels --jq '[.labels[].name]'
- Then filter labels against the known ERROR_CLASSES set: WRONG_CITATION, LLM_HALLUCINATED_SELECTION, WORKER_FALLBACK_FAILED, GOOGLE_DOM_DRIFT, HARNESS_ERROR (the 5 existing scaffolds per REQUIREMENTS.md Out of Scope "ERROR_CLASS coverage")
- Hard-error if no matching label: workflow exits 1 with clear message

From RESEARCH §A1 + 2026-06-05 second scope expansion (model pre-resolution):
- The upstream auto-fix entry in tests/e2e/.llm-spend-ledger.json carries `model` per Phase 54 wiring AND `fingerprint` per Phase 56 wiring.
- Ledger shape: `{ "months": { "<YYYY-MM>": { "iterations": [ { "model": "...", "fingerprint": "...", "source": "auto-fix-api", ... } ] } } }`
- Lookup pattern: `jq -r --arg fp "$FINGERPRINT" '[.months[].iterations[] | select(.fingerprint == $fp and .source == "auto-fix-api")] | last | .model // empty' tests/e2e/.llm-spend-ledger.json`
  - `[.months[].iterations[]]` flattens all months' iterations
  - `select(...)` filters to entries matching the resolved fingerprint AND `source == "auto-fix-api"` (Phase 56's upstream auto-fix entry, not earlier phases)
  - `| last` takes the most recent entry if multiple match (defensive against fingerprint reuse across attempts)
  - `| .model // empty` extracts the model or empty if nothing matched
- Hard-error if jq output is empty: `echo "::error::Could not resolve model from upstream auto-fix ledger entry for fingerprint=$FINGERPRINT"; exit 1`. NO silent default to sonnet.
- The pre-resolution MUST run AFTER the fingerprint pre-resolution step (depends on $FP).

From v40-auto-promote.yml runner context:
- tests/e2e/.llm-spend-ledger.json is committed to origin/main by v40-cost-ledger-snapshot.yml (Phase 57); the workflow checkout (default actions/checkout@v4) brings it into the runner workspace.
- jq is pre-installed on ubuntu-latest runners.

LOAD-BEARING anti-features (per CONTEXT scope_constraints):
- v40-auto-fix.yml: NOT modified (Pitfall 1, COMMIT-04 byte-unchanged invariant)
- No new npm dependencies
- No widening of any other workflow's permissions

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Pre-resolve fingerprint + errorClass + model in v40-auto-promote.yml; thread all three into argv</name>
  <files>.github/workflows/v40-auto-promote.yml</files>
  <read_first>
    .github/workflows/v40-auto-promote.yml (full file — lines 97-211 are the active edit region; lines 170-178 source-label resolution; lines 180-211 promote step that gains the three new argv lines);
    scripts/auto-fix-promote.mjs (parseArgv post-Plan-01 — confirm new flag names match what the workflow passes: --fingerprint, --error-class, --model);
    tests/e2e/.llm-spend-ledger.json (top-of-file shape — `.months[<YYYY-MM>].iterations[]` array carries upstream auto-fix entries with fingerprint + model + source:'auto-fix-api');
    .planning/phases/58-promote-outcome-ledger-entry/58-RESEARCH.md §2 (fingerprint pre-resolution recipe), §1 + Pitfall 1 (errorClass requirement and resolution path), §A1 + 2026-06-05 scope expansion (model pre-resolution via jq);
    REQUIREMENTS.md (line "Out of Scope" — ERROR_CLASSES list of 5: WRONG_CITATION, LLM_HALLUCINATED_SELECTION, WORKER_FALLBACK_FAILED, GOOGLE_DOM_DRIFT, HARNESS_ERROR; PROMOTE-02/03 entry shapes now include `model`)
  </read_first>
  <action>
Edit `.github/workflows/v40-auto-promote.yml`:

(a) Insert a NEW step IMMEDIATELY AFTER the existing step "Resolve source-issue labels" (currently lines 170-178; ends at the `echo "source_issue_labels=$SOURCE_LABELS_CSV" >> "$GITHUB_OUTPUT"` close). The new step name: `Pre-resolve source-issue fingerprint`. Step id: `fp`.

The step uses env var SOURCE_ISSUE from `${{ steps.parse.outputs.source_issue }}`. Inside `run: |`:
- assign `FP=$(gh issue view "$SOURCE_ISSUE" --json body --jq '.body' | grep -oE '<!-- fp: [0-9a-f]{12} -->' | head -1 | sed -E 's/<!-- fp: ([0-9a-f]{12}) -->/\\1/')`
- if `-z "$FP"`: echo `::error::Could not extract fingerprint from issue #$SOURCE_ISSUE (expected <!-- fp: ([0-9a-f]{12}) --> marker in issue body line 1)`; exit 1
- echo `fingerprint=$FP` >> "$GITHUB_OUTPUT"

(b) Insert a SECOND new step IMMEDIATELY AFTER the fingerprint step. Name: `Pre-resolve source-issue errorClass`. Step id: `ec`.

The step uses env SOURCE_ISSUE from `${{ steps.parse.outputs.source_issue }}` and SOURCE_ISSUE_LABELS from `${{ steps.source.outputs.source_issue_labels }}`. Inside `run: |`:
- declare a shell variable with the 5 known ERROR_CLASSES: `KNOWN_CLASSES="WRONG_CITATION LLM_HALLUCINATED_SELECTION WORKER_FALLBACK_FAILED GOOGLE_DOM_DRIFT HARNESS_ERROR"`
- iterate the CSV: `EC=""; for L in ${SOURCE_ISSUE_LABELS//,/ }; do for C in $KNOWN_CLASSES; do if [ "$L" = "$C" ]; then EC="$C"; break 2; fi; done; done`
- if `-z "$EC"`: echo `::error::Could not extract errorClass label from issue #$SOURCE_ISSUE (expected one of: $KNOWN_CLASSES; got labels: $SOURCE_ISSUE_LABELS)`; exit 1
- echo `error_class=$EC` >> "$GITHUB_OUTPUT"

(b.1) Insert a THIRD new step IMMEDIATELY AFTER the errorClass step. Name: `Pre-resolve upstream-ledger model`. Step id: `ml`.

The step uses env FINGERPRINT from `${{ steps.fp.outputs.fingerprint }}`. Inside `run: |`:
- Add a one-line comment block BEFORE the jq call documenting the design (verbatim): `# Look up the upstream auto-fix ledger entry's model by fingerprint match.` + `# Phase 54 wired model on each source:'auto-fix-api' entry; Phase 56 wired` + `# fingerprint on the same. We match on both to defend against fingerprint` + `# reuse across phases. Hard-fail if no match — silent default to sonnet` + `# would break a-b-winner.mjs per-arm attribution for the opus arm` + `# (a-b-winner.mjs:178-189). See RESEARCH §A1 + 2026-06-05 scope expansion.`
- assign `MODEL=$(jq -r --arg fp "$FINGERPRINT" '[.months[].iterations[] | select(.fingerprint == $fp and .source == "auto-fix-api")] | last | .model // empty' tests/e2e/.llm-spend-ledger.json)`
- if `-z "$MODEL"`: echo `::error::Could not resolve model from upstream auto-fix ledger entry for fingerprint=$FINGERPRINT (no .months[].iterations[] entry with source=auto-fix-api and matching fingerprint in tests/e2e/.llm-spend-ledger.json — workflow refuses to silent-default to sonnet because that would break per-arm attribution for the opus arm; see RESEARCH §A1)`; exit 1
- echo `model=$MODEL` >> "$GITHUB_OUTPUT"

(c) Modify the existing step "Triple-gate + runPromote (per affected case)" (currently lines 180-211). Two changes:

(c.1) Extend the env block (currently lines 191-197) by adding THREE new env entries AFTER the existing `PARTIAL_PASSING_CASES:` line:
    `FINGERPRINT: ${{ steps.fp.outputs.fingerprint }}`
    `ERROR_CLASS: ${{ steps.ec.outputs.error_class }}`
    `MODEL: ${{ steps.ml.outputs.model }}`

(c.2) Extend the per-case node invocation (currently lines 201-210 inside the for-loop) by adding THREE new argv-pair lines immediately AFTER the existing `--passing-cases "$PARTIAL_PASSING_CASES" \\` line and BEFORE the closing `done`. Preserve the existing line-continuation backslash convention:
    `--fingerprint "$FINGERPRINT" \\`
    `--error-class "$ERROR_CLASS" \\`
    `--model "$MODEL"`
    Note: the last argv flag does NOT take a trailing backslash (it terminates the multi-line node command). Reorder so the existing `--passing-cases` keeps its backslash, `--fingerprint` keeps its backslash, `--error-class` keeps its backslash, and `--model` is the new last flag.

(d) Do NOT touch any other step. Specifically:
    - Do NOT modify the "Parse PR body + labels + source issue" step.
    - Do NOT modify the "Resolve source-issue labels" step body.
    - Do NOT modify the "Build auto-promote PR body" step.
    - Do NOT modify the "Open auto-promote PR" step.
    - Do NOT modify the "Close source issue" step.
    - Do NOT modify the "Re-check on main HEAD (post-merge verifier)" step.
    - Do NOT modify the "File regression issue on verifier failure" step.
    - Do NOT modify permissions, concurrency, on:, or env at the workflow level.

(e) Do NOT touch .github/workflows/v40-auto-fix.yml (LOAD-BEARING anti-feature; Pitfall 1; COMMIT-04 byte-unchanged invariant from Phase 57).

(f) Commit (single atomic) with message:
    `feat(58): pre-resolve fingerprint + errorClass in v40-auto-promote.yml`
    Body:
    ```
    Three new pre-resolution steps after "Resolve source-issue labels":

    - "Pre-resolve source-issue fingerprint": gh issue view --json body
      | grep -oE '<!-- fp: [0-9a-f]{12} -->' | sed; hard-fails if absent.
    - "Pre-resolve source-issue errorClass": filters source-issue labels
      against the 5 known ERROR_CLASSES (WRONG_CITATION,
      LLM_HALLUCINATED_SELECTION, WORKER_FALLBACK_FAILED,
      GOOGLE_DOM_DRIFT, HARNESS_ERROR); hard-fails if no match.
    - "Pre-resolve upstream-ledger model": jq lookup against
      tests/e2e/.llm-spend-ledger.json by fingerprint match +
      source=='auto-fix-api' filter; extracts the upstream auto-fix
      entry's model (Phase 54 wired this); hard-fails if no matching
      entry. NO silent default to sonnet — that would break per-arm
      attribution for the opus arm in a-b-winner.mjs:isAttributable.

    All three values threaded into the existing per-case node invocation
    of scripts/auto-fix-promote.mjs via new --fingerprint, --error-class,
    and --model argv flags (acceptance added in Plan 01's atomic commit
    feat(58): wire promote outcome ledger entries).

    Required so a-b-winner.mjs's isAttributable filter
    (a-b-winner.mjs:178-189) does not drop outcome entries written by
    scripts/auto-fix-promote.mjs — fingerprint, errorClass, AND model
    are all REQUIRED non-empty strings for the filter to accept the
    entry (model specifically: must startsWith one of the two arm
    prefixes; the upstream-ledger lookup guarantees this).

    Vitest YAML contract assertion in
    tests/e2e/scripts/v40-auto-promote-yaml.test.js pins the three new
    step names and the three new argv lines (regression gate against
    silent drift).

    LOAD-BEARING anti-feature preserved: .github/workflows/v40-auto-fix.yml
    is NOT modified (Pitfall 1; COMMIT-04 byte-unchanged invariant).

    Files: .github/workflows/v40-auto-promote.yml,
           tests/e2e/scripts/v40-auto-promote-yaml.test.js.

    Trailers per project convention.
    ```
    Stage ONLY the two files in `files_modified`. Verify staging matches `git diff --cached --name-only` returns exactly two paths (sorted): `.github/workflows/v40-auto-promote.yml` and `tests/e2e/scripts/v40-auto-promote-yaml.test.js`. Defer the actual commit to AFTER Task 2.2 has added the Vitest assertions — both tasks land in the SAME single atomic commit per the Phase 58 atomic-commit discipline.
  </action>
  <verify>
    <automated>grep -c -- "--fingerprint" .github/workflows/v40-auto-promote.yml | grep -q '^[1-9]' && grep -c -- "--error-class" .github/workflows/v40-auto-promote.yml | grep -q '^[1-9]' && grep -c -- "--model" .github/workflows/v40-auto-promote.yml | grep -q '^[1-9]' && grep -q "Pre-resolve source-issue fingerprint" .github/workflows/v40-auto-promote.yml && grep -q "Pre-resolve source-issue errorClass" .github/workflows/v40-auto-promote.yml && grep -q "Pre-resolve upstream-ledger model" .github/workflows/v40-auto-promote.yml && grep -q 'jq -r --arg fp' .github/workflows/v40-auto-promote.yml</automated>
  </verify>
  <acceptance_criteria>
1. `grep -c -- "--fingerprint" .github/workflows/v40-auto-promote.yml` returns ≥ 1.
2. `grep -c -- "--error-class" .github/workflows/v40-auto-promote.yml` returns ≥ 1.
3. `grep -c -- "--model" .github/workflows/v40-auto-promote.yml` returns ≥ 1 (model argv plumbed; per RESEARCH §A1 scope expansion).
4. `grep -q "Pre-resolve source-issue fingerprint" .github/workflows/v40-auto-promote.yml` succeeds (step exists).
5. `grep -q "Pre-resolve source-issue errorClass" .github/workflows/v40-auto-promote.yml` succeeds (step exists).
6. `grep -q "Pre-resolve upstream-ledger model" .github/workflows/v40-auto-promote.yml` succeeds (step exists).
7. `grep -q "gh issue view .* --json body" .github/workflows/v40-auto-promote.yml` succeeds (fingerprint pre-resolution uses gh issue view body).
8. `grep -q 'WRONG_CITATION LLM_HALLUCINATED_SELECTION WORKER_FALLBACK_FAILED GOOGLE_DOM_DRIFT HARNESS_ERROR' .github/workflows/v40-auto-promote.yml` succeeds (5 known ERROR_CLASSES listed).
9. `grep -q 'jq -r --arg fp' .github/workflows/v40-auto-promote.yml` succeeds (model pre-resolution uses jq parameterized by fingerprint).
10. `grep -q 'select(.fingerprint == .fp and .source == "auto-fix-api")' .github/workflows/v40-auto-promote.yml` succeeds (jq filter matches both fingerprint AND source per RESEARCH §A1 defensive-against-reuse design).
11. Commit not yet created at end of Task 2.1 — defer until Task 2.2 has added the Vitest contract assertions. Acceptance criteria 11–14 below apply AFTER the Task 2.2 commit:
    - 11. `git diff HEAD~1 HEAD --name-only | sort | tr '\n' ' '` returns `.github/workflows/v40-auto-promote.yml tests/e2e/scripts/v40-auto-promote-yaml.test.js ` (exactly two files).
    - 12. `git diff HEAD~1 HEAD -- .github/workflows/v40-auto-fix.yml | wc -c` returns 0 (LOAD-BEARING anti-feature preserved).
    - 13. `git diff HEAD~1 HEAD -- scripts/auto-fix-promote.mjs | wc -c` returns 0 (Plan 01 commit was prior; this plan does not touch the script).
    - 14. `git diff HEAD~1 HEAD -- tests/e2e/lib/llm-ledger.js | wc -c` returns 0 (Phase 56 byte-unchanged invariant).
    - 15. `grep -c 'git push origin main' .github/workflows/v40-auto-fix.yml` STILL returns 1 (COMMIT-04 Phase 57 invariant; not touched here but verified for sanity).
  </acceptance_criteria>
  <done>
v40-auto-promote.yml pre-resolves all three of fingerprint (from source-issue body HTML comment), errorClass (from source-issue labels filtered against the 5 known ERROR_CLASSES), AND model (from upstream auto-fix ledger entry via jq lookup keyed on fingerprint + source=='auto-fix-api') before invoking scripts/auto-fix-promote.mjs. All three values are passed via the new --fingerprint, --error-class, AND --model argv flags Plan 01 wired. Commit is DEFERRED to the end of Task 2.2 so the workflow edit + the Vitest contract assertion land in the SAME atomic commit. v40-auto-fix.yml untouched. The full chain (workflow → script → ledger) now produces outcome entries with errorClass + model populated per-arm; once 20+ accumulate per ERROR_CLASS per model arm, a-b-winner.mjs exits abstention automatically for BOTH arms.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.2: Add Vitest YAML contract assertion for --model plumbing + commit atomically with Task 2.1</name>
  <files>tests/e2e/scripts/v40-auto-promote-yaml.test.js</files>
  <read_first>
    tests/e2e/scripts/v40-auto-promote-yaml.test.js (full file — lines 1-50 imports + beforeAll YAML load; lines 53-209 the single describe block 'v40-auto-promote.yml contract (Phase 44)' with grep/regex assertions on the YAML text);
    .github/workflows/v40-auto-promote.yml (post-Task-2.1 state — the three new step names and the --model argv line to be pinned);
    .planning/phases/58-promote-outcome-ledger-entry/58-RESEARCH.md §A1 + 2026-06-05 scope expansion (model pre-resolution rationale and jq pattern)
  </read_first>
  <behavior>
    - Test 1 (PHASE-58-Y1): yaml contains the step name `Pre-resolve source-issue fingerprint` (regex match).
    - Test 2 (PHASE-58-Y2): yaml contains the step name `Pre-resolve source-issue errorClass`.
    - Test 3 (PHASE-58-Y3): yaml contains the step name `Pre-resolve upstream-ledger model`.
    - Test 4 (PHASE-58-Y4): yaml contains `--fingerprint "$FINGERPRINT"` argv line (regex `/--fingerprint\s+"\$FINGERPRINT"/`).
    - Test 5 (PHASE-58-Y5): yaml contains `--error-class "$ERROR_CLASS"` argv line.
    - Test 6 (PHASE-58-Y6): yaml contains `--model "$MODEL"` argv line — pins the model argv plumbing (regression gate against revert to hardcoded sonnet on the script side).
    - Test 7 (PHASE-58-Y7): yaml contains the jq lookup pattern `jq -r --arg fp` (proves the model pre-resolution step uses parameterized jq, NOT a vulnerable string-interpolated form).
    - Test 8 (PHASE-58-Y8): yaml contains the filter expression `select(.fingerprint == $fp and .source == "auto-fix-api")` (pins the defensive AND-of-two-fields match per RESEARCH §A1).
    - Test 9 (PHASE-58-Y9): yaml does NOT contain a silent-default-to-sonnet fallback pattern — assert `/MODEL=\$\{MODEL:-claude-sonnet-4-6\}/` does NOT appear; assert `/MODEL="claude-sonnet-4-6"/` (literal hardcode) does NOT appear. This is the anti-pattern guard against future silent-default regressions.
    - Test 10 (PHASE-58-Y10): yaml model pre-resolution step contains a hard-fail clause — regex match `/::error::Could not resolve model from upstream auto-fix ledger entry/` AND `exit 1` within the same step block.
  </behavior>
  <action>
Edit `tests/e2e/scripts/v40-auto-promote-yaml.test.js`:

(a) Append a NEW describe block at the END of the file (after the existing `'v40-auto-promote.yml contract (Phase 44)'` describe — currently ends near line 209). Name verbatim:
    `describe('v40-auto-promote.yml Phase 58 contract — fingerprint + errorClass + model plumbing', () => { ... })`

(b) Inside the new describe block, place ten `it` cases corresponding to the ten behavior tests above. Each test uses the existing `yaml` variable bound by `beforeAll`. Pattern (per existing file's convention — grep/regex/.toContain/.toMatch on the raw YAML text):

  - it 'PHASE-58-Y1 — Pre-resolve source-issue fingerprint step exists' → `expect(yaml).toContain('Pre-resolve source-issue fingerprint');`
  - it 'PHASE-58-Y2 — Pre-resolve source-issue errorClass step exists' → `expect(yaml).toContain('Pre-resolve source-issue errorClass');`
  - it 'PHASE-58-Y3 — Pre-resolve upstream-ledger model step exists' → `expect(yaml).toContain('Pre-resolve upstream-ledger model');`
  - it 'PHASE-58-Y4 — --fingerprint argv plumbed' → `expect(yaml).toMatch(/--fingerprint\s+"\$FINGERPRINT"/);`
  - it 'PHASE-58-Y5 — --error-class argv plumbed' → `expect(yaml).toMatch(/--error-class\s+"\$ERROR_CLASS"/);`
  - it 'PHASE-58-Y6 — --model argv plumbed (PIN: must not regress to hardcoded sonnet on script side)' → `expect(yaml).toMatch(/--model\s+"\$MODEL"/);`
  - it 'PHASE-58-Y7 — model lookup uses parameterized jq (jq -r --arg fp)' → `expect(yaml).toContain('jq -r --arg fp');`
  - it 'PHASE-58-Y8 — jq filter matches fingerprint AND source==auto-fix-api (defensive against fingerprint reuse)' → `expect(yaml).toContain('select(.fingerprint == $fp and .source == "auto-fix-api")');`
  - it 'PHASE-58-Y9 — no silent-default-to-sonnet fallback (anti-pattern guard)' → `expect(yaml).not.toMatch(/MODEL=\$\{MODEL:-claude-sonnet-4-6\}/); expect(yaml).not.toMatch(/MODEL="claude-sonnet-4-6"/);`
  - it 'PHASE-58-Y10 — model pre-resolution hard-fails on no match (clear ::error:: + exit 1)' → `expect(yaml).toMatch(/::error::Could not resolve model from upstream auto-fix ledger entry/); expect(yaml).toMatch(/Pre-resolve upstream-ledger model[\s\S]*?exit 1/);` (the second regex ensures `exit 1` appears WITHIN the same step block after the step name — uses `[\s\S]*?` non-greedy to bound the search).

(c) Run the new tests locally: `CI=true npx vitest run tests/e2e/scripts/v40-auto-promote-yaml.test.js -t "Phase 58 contract"`. Confirm all 10 pass. Also run the FULL file: `CI=true npx vitest run tests/e2e/scripts/v40-auto-promote-yaml.test.js` and confirm the existing Phase 44 cases (22 tests) STILL pass — Phase 58 additions are additive and namespace-clean (verified by RESEARCH §5 namespace-collision analysis applied here too: no existing test name starts with 'PHASE-58' or 'Pre-resolve upstream-ledger').

(d) Stage `tests/e2e/scripts/v40-auto-promote-yaml.test.js` alongside `.github/workflows/v40-auto-promote.yml` (staged by Task 2.1). Confirm `git diff --cached --name-only | sort` returns exactly:
    ```
    .github/workflows/v40-auto-promote.yml
    tests/e2e/scripts/v40-auto-promote-yaml.test.js
    ```

(e) CREATE THE SINGLE ATOMIC COMMIT now using the verbatim commit message from Task 2.1 directive (f) (title + body). Use a heredoc. This commit covers BOTH files (workflow + Vitest contract) — single atomic per Phase 58 atomic-commit discipline.

(f) Final verification AFTER commit: confirm `git diff HEAD~1 HEAD -- scripts/auto-fix-promote.mjs | wc -c` returns 0 (Plan 01 commit was prior — this commit does NOT touch the script), and `git diff HEAD~1 HEAD -- tests/e2e/lib/llm-ledger.js | wc -c` returns 0 (Phase 56 byte-unchanged invariant), and `git diff HEAD~1 HEAD -- .github/workflows/v40-auto-fix.yml | wc -c` returns 0 (Pitfall 1 LOAD-BEARING anti-feature).
  </action>
  <verify>
    <automated>CI=true npx vitest run tests/e2e/scripts/v40-auto-promote-yaml.test.js -t "Phase 58 contract" && git diff HEAD~1 HEAD --name-only | sort | tr '\n' ' ' | grep -q '^\.github/workflows/v40-auto-promote\.yml tests/e2e/scripts/v40-auto-promote-yaml\.test\.js $' && test -z "$(git diff HEAD~1 HEAD -- .github/workflows/v40-auto-fix.yml)"</automated>
  </verify>
  <acceptance_criteria>
1. New describe block `'v40-auto-promote.yml Phase 58 contract — fingerprint + errorClass + model plumbing'` exists in tests/e2e/scripts/v40-auto-promote-yaml.test.js with exactly 10 `it` cases (PHASE-58-Y1..Y10).
2. `CI=true npx vitest run tests/e2e/scripts/v40-auto-promote-yaml.test.js` exits 0 (all 22 Phase 44 cases + 10 Phase 58 cases pass = 32 tests in the file).
3. PHASE-58-Y6 (`--model "$MODEL"`) passes — proves the workflow plumbs the model argv (regression gate against any future silent revert to hardcoded sonnet).
4. PHASE-58-Y9 (no silent-default) passes — proves neither `MODEL=${MODEL:-claude-sonnet-4-6}` nor `MODEL="claude-sonnet-4-6"` appears in the YAML.
5. Single atomic commit on HEAD touching EXACTLY two files: `.github/workflows/v40-auto-promote.yml` AND `tests/e2e/scripts/v40-auto-promote-yaml.test.js`. `git diff HEAD~1 HEAD --name-only | sort` returns exactly those two paths.
6. `git diff HEAD~1 HEAD -- scripts/auto-fix-promote.mjs | wc -c` returns 0.
7. `git diff HEAD~1 HEAD -- tests/e2e/lib/llm-ledger.js | wc -c` returns 0 (Phase 56 byte-unchanged invariant).
8. `git diff HEAD~1 HEAD -- .github/workflows/v40-auto-fix.yml | wc -c` returns 0 (Pitfall 1 LOAD-BEARING anti-feature).
  </acceptance_criteria>
  <done>
Vitest YAML contract assertion lands in the SAME single atomic commit as the workflow YAML edit. PHASE-58-Y6 + PHASE-58-Y9 together form the regression gate against silent revert to hardcoded sonnet on either side (script via Plan 01's grep gates, workflow via this plan's Vitest contract). End-to-end --model plumbing is now pinned by executable assertions at three layers: parseArgv (Plan 01 PA4/PA5), entry shape (Plan 01 O1 opus + O2 sonnet), and workflow YAML (Plan 02 PHASE-58-Y1..Y10).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions ← attacker-controlled issue body | issue body content reaches the fingerprint pre-resolution step; protected by sed-extracted strict regex (12-hex) and downstream argv validation in the script (Plan 01's `/^[0-9a-f]{12}$/` check) |
| GitHub Actions ← source-issue labels | label list reaches the errorClass pre-resolution step; filtered against a fixed shell-string whitelist of 5 known ERROR_CLASSES — no untrusted label can produce a non-whitelisted EC value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-04 | A-B Winner Abstention | outcome entries reach the ledger with errorClass AND per-arm model populated (a-b-winner.mjs:isAttributable requires both) | mitigate | Two pre-resolution steps: (a) errorClass filtered against the 5 known ERROR_CLASSES; (b) model resolved via jq lookup against the upstream auto-fix ledger entry (Phase 54 wired model + Phase 56 wired fingerprint on the same entry — we join by fingerprint match + source=='auto-fix-api'). Both steps hard-fail with clear ::error:: messages if no match. Combined with Plan 01's argv validation (errorClass naming regex; model startsWith one of two arm prefixes), only valid values reach the entry shape. Vitest contract pins all three argv lines in tests/e2e/scripts/v40-auto-promote-yaml.test.js. |
| T-58-A1 | Tampering (issue body injection) | malicious issue body contains crafted `<!-- fp: ... -->` to inject shell metacharacters | mitigate | sed regex `s/<!-- fp: ([0-9a-f]{12}) -->/\\1/` extracts ONLY 12-hex; downstream `/^[0-9a-f]{12}$/` in the script re-validates. Defense in depth — both layers reject metacharacters. |
| T-58-A2 | Tampering (label injection) | malicious issue label like `WRONG_CITATION; rm -rf /` | mitigate | Shell `[ "$L" = "$C" ]` string-equality match against fixed whitelist — no globbing, no command substitution, no eval. Label values reaching the ledger are guaranteed to be one of the 5 fixed strings. |
| T-58-A3 | Tampering (jq injection) | malicious fingerprint argv value containing jq metacharacters reaches the model pre-resolution step | mitigate | jq invoked with `--arg fp "$FINGERPRINT"` (parameterized argument, NOT string interpolation into the filter expression). jq treats `$fp` as a string variable — no jq-expression evaluation against the fingerprint value. Pin via Vitest assertion PHASE-58-Y7. Defense in depth: Plan 01's argv validation (`/^[0-9a-f]{12}$/`) already restricts fingerprint to 12-hex characters before any downstream use. |
| T-58-A4 | Tampering (silent sonnet default) | future workflow edit silently defaults model to sonnet on lookup failure, hiding opus-arm attribution loss | mitigate | Vitest assertion PHASE-58-Y9 hard-asserts that neither `MODEL=${MODEL:-claude-sonnet-4-6}` nor `MODEL="claude-sonnet-4-6"` patterns appear in the workflow YAML. Any future PR that re-introduces silent default trips this assertion. PHASE-58-Y10 additionally pins the hard-fail clause with `::error::` + `exit 1`. |
| T-58-SC | Tampering (Supply Chain) | npm/pip/cargo installs | accept | Zero new dependencies (workflow uses only `gh` which is pre-installed in the runner image and pinned by ubuntu-latest version) |
</threat_model>

<verification>
- Single atomic commit modifies EXACTLY two files: .github/workflows/v40-auto-promote.yml AND tests/e2e/scripts/v40-auto-promote-yaml.test.js
- v40-auto-fix.yml byte-unchanged (LOAD-BEARING; Pitfall 1; COMMIT-04)
- tests/e2e/lib/llm-ledger.js byte-unchanged (Phase 56)
- scripts/auto-fix-promote.mjs byte-unchanged (Plan 01 commit already done; this plan touches only the workflow + the Vitest YAML contract)
- THREE new pre-resolution steps named verbatim: "Pre-resolve source-issue fingerprint", "Pre-resolve source-issue errorClass", AND "Pre-resolve upstream-ledger model"
- --fingerprint, --error-class, AND --model argv flags appear in the per-case node invocation
- 5 known ERROR_CLASSES whitelist literally in the workflow YAML
- jq -r --arg fp lookup pattern in the model pre-resolution step (defense-in-depth against jq injection via parameterized argument)
- No silent-default-to-sonnet pattern anywhere in the workflow YAML (pinned by PHASE-58-Y9)
- Vitest YAML contract file gains the new describe block with 10 PHASE-58-Y1..Y10 cases, all green
</verification>

<success_criteria>
- All three pre-resolution steps run during a real auto-promote workflow execution
- Fingerprint step exits 1 with clear ::error:: message if no `<!-- fp: ... -->` marker on source issue
- ErrorClass step exits 1 with clear ::error:: message if no recognized ERROR_CLASS label on source issue
- Model step exits 1 with clear ::error:: message if no upstream auto-fix ledger entry matches the resolved fingerprint (NO silent default to sonnet)
- The downstream node invocation receives all three flags; Plan 01's argv validation regex (incl. model startsWith one of two arm prefixes) accepts the values
- The full chain end-to-end (Phase 59 SWEEP-03 territory) produces ledger entries that a-b-winner.mjs's isAttributable filter accepts for BOTH arms (sonnet and opus)
- Vitest YAML contract PHASE-58-Y6 + PHASE-58-Y9 + PHASE-58-Y10 together form a regression gate against any future silent revert to hardcoded sonnet
</success_criteria>

<output>
Create `.planning/phases/58-promote-outcome-ledger-entry/58-02-SUMMARY.md` with:
- Commit SHA of feat(58) workflow commit (single atomic; covers both workflow YAML edit AND Vitest YAML contract)
- Confirmation of Task 2.1 acceptance criteria (1-10 pre-commit grep checks; 11-15 post-commit diff checks)
- Confirmation of Task 2.2 acceptance criteria (Vitest PHASE-58-Y1..Y10 all green; commit touches exactly two files)
- Confirmation that v40-auto-fix.yml is byte-unchanged
- Confirmation that scripts/auto-fix-promote.mjs is byte-unchanged (Plan 01 territory)
- Note any deviations from the plan
- Statement of Phase 58 readiness for Phase 59 SWEEP-03 evidence (both arms now attributable per a-b-winner.mjs:isAttributable)
</output>
