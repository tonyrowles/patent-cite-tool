---
phase: 61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/scripts/inject-defect.mjs
  - tests/e2e/scripts/e2e-inject-defect.test.js
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-03]
tags: [mutator, diagnostic-injection, vitest, deterministic-fixture]

must_haves:
  truths:
    - "buildBody(...errorClass='GOOGLE_DOM_DRIFT'...) returns a body containing at least one verbatim selector from selection.js / navigation.js vocabulary (one of: 'patent-result', 'section[itemprop=\"claims\"]', 'main', 'article')"
    - "buildBody(...errorClass='WRONG_CITATION'...) returns a body containing the Phase 35 Verifier Disagreement template-parity literals ('### Verifier Disagreement', 'Expected citation', 'Observed citation', 'Verifier tier:', 'Rerun verdict:')"
    - "buildBody(...) is byte-identical across two calls with the same {fp, caseId, seed, errorClass} arguments — no Math.random / Date.now / crypto.randomBytes leakage"
    - "SOURCE_TAG literal 'fixture-mutator-uat-47b' appears verbatim in every buildBody output regardless of errorClass (MUTATOR-04 co-design preserved)"
    - "Existing 5 ERROR_CLASSES entries in inject-defect.mjs:64-70 retain pre-extension body shape for 3 untouched classes (LLM_HALLUCINATED_SELECTION, WORKER_FALLBACK_FAILED, HARNESS_ERROR)"
  artifacts:
    - path: "tests/e2e/scripts/inject-defect.mjs"
      provides: "buildBody extended with per-errorClass diagnostic block; SOURCE_TAG and ERROR_CLASSES Set unchanged"
      contains: "buildDiagnosticBlock"
    - path: "tests/e2e/scripts/e2e-inject-defect.test.js"
      provides: "7+ new Vitest cases covering DIAG-01 selector verbatim, DIAG-02 template parity, DIAG-03 determinism + SOURCE_TAG preservation"
      contains: "DIAG-03"
  key_links:
    - from: "tests/e2e/scripts/inject-defect.mjs:buildBody"
      to: "tests/e2e/lib/selection.js:170-172 + tests/e2e/lib/navigation.js:34"
      via: "verbatim string match in emitted body"
      pattern: "patent-result|section\\[itemprop=\"claims\"\\]|main|article"
    - from: "tests/e2e/scripts/inject-defect.mjs:buildBody"
      to: "tests/e2e/lib/issue-payload-builder.js:208-216"
      via: "literal headers in WRONG_CITATION block"
      pattern: "### Verifier Disagreement"
    - from: "tests/e2e/scripts/inject-defect.mjs:75 SOURCE_TAG"
      to: "scripts/quarantine-append.mjs:239 isFixtureMutator filter"
      via: "verbatim 'fixture-mutator-uat-47b' literal in body"
      pattern: "fixture-mutator-uat-47b"
---

<objective>
Extend `tests/e2e/scripts/inject-defect.mjs:buildBody` (line 277) with per-errorClass seeded diagnostic blocks for `GOOGLE_DOM_DRIFT` (verbatim Google-Patents selector vocabulary from `selection.js`/`navigation.js`) and `WRONG_CITATION` (Phase 35 Verifier Disagreement template parity from `issue-payload-builder.js:208-216`). Add 7+ Vitest assertions in `tests/e2e/scripts/e2e-inject-defect.test.js` pinning DIAG-01 selector verbatim match, DIAG-02 template parity, DIAG-03 determinism + SOURCE_TAG preservation across all errorClasses.

Purpose: Synthetic mutator-injected issues acquire enough realistic diagnostic content that the auto-fix loop's `GOOGLE_DOM_DRIFT_CONTRACT` prompt scaffold (`tests/e2e/lib/fix-prompt-builder.js:252-268`) receives a parseable DOM snippet, eliminating the v4.2 SWEEP-03 `apply-check-failed` failure mode where Claude could not propose a fix without diagnostic data.

Output: Extended `buildBody` (additive: existing 3 untouched classes retain prior body shape; 2 extended classes gain a diagnostic block) + extended test file with new Vitest describe block ("DIAG-03 deterministic diagnostic body") containing inline-snapshot byte-pins + selector regex assertion + template-parity contains-assertions + SOURCE_TAG preservation across-errorClass loop.

Per CONTEXT D-DIAG-03-PATH and RESEARCH SUMMARY correction: the DIAG-03 test extension lives at `tests/e2e/scripts/e2e-inject-defect.test.js` (NOT `tests/unit/e2e-inject-defect.test.js` — that path does not exist).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep/61-CONTEXT.md
@.planning/phases/61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep/61-RESEARCH.md

<!-- READ-ONLY references — verbatim-match source files -->
@tests/e2e/scripts/inject-defect.mjs
@tests/e2e/scripts/e2e-inject-defect.test.js
@tests/e2e/lib/selection.js
@tests/e2e/lib/navigation.js
@tests/e2e/lib/issue-payload-builder.js

<interfaces>
<!-- Key contracts extracted from the codebase. Executor uses these directly — NO codebase exploration needed. -->

From tests/e2e/scripts/inject-defect.mjs:64-75 (VERIFIED on disk 2026-06-09):
```javascript
export const ERROR_CLASSES = new Set([
  'WRONG_CITATION',
  'LLM_HALLUCINATED_SELECTION',
  'WORKER_FALLBACK_FAILED',
  'GOOGLE_DOM_DRIFT',
  'HARNESS_ERROR',
]);

export const SOURCE_TAG = 'fixture-mutator-uat-47b';
```

From tests/e2e/scripts/inject-defect.mjs:277-298 (CURRENT buildBody — EXTENSION TARGET):
```javascript
export function buildBody({ fp, caseId, seed, errorClass }) {
  return [
    `<!-- fp: ${fp} -->`,
    '',
    '### Reproducer',
    `case-id: ${caseId}`,
    `seed: ${seed}`,
    `error-class: ${errorClass}`,
    '',
    '### Synthetic Defect',
    '',
    'This issue was created by `tests/e2e/scripts/inject-defect.mjs` as a',
    'UAT-47-b synthetic exercise of the auto-fix loop. The case is NOT a real',
    'pipeline regression; it exists only to drive a controlled end-to-end',
    'auto-fix → auto-promote → ledger run on origin/main.',
    '',
    `Source: ${SOURCE_TAG}`,
    '',
    'Cleanup runbook: see `.planning/phases/59-fixture-mutator-4-uat-re-sweep/56-MUTATOR-CLEANUP.md`.',
    '',
  ].join('\n');
}
```

From tests/e2e/lib/selection.js:167-175 (VERIFIED — DIAG-01 verbatim-match source):
```javascript
const CONTAINERS = [
  'section#description',
  'section#claims',
  'section[itemprop="description"]',
  'section[itemprop="claims"]',
  'patent-result',
  'main',
  'body',
];
```

From tests/e2e/lib/navigation.js:34 (VERIFIED — DIAG-01 verbatim-match source):
```javascript
await page.waitForSelector('main, article, patent-result', {
```

From tests/e2e/lib/issue-payload-builder.js:208-216 (VERIFIED — DIAG-02 template-parity source):
```javascript
const verifierSection = [
  '### Verifier Disagreement',
  '',
  `Expected citation (golden): \`${safeGolden ?? 'n/a'}\``,
  `Observed citation: \`${safeCitation}\``,
  fenceCode(truncatedReason),
  `Verifier tier: ${tierUsed}`,
  rerunLine,
].join('\n');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend buildBody with per-errorClass diagnostic block (DIAG-01 + DIAG-02)</name>
  <files>tests/e2e/scripts/inject-defect.mjs</files>
  <read_first>
    - tests/e2e/scripts/inject-defect.mjs (lines 1-100 for header/imports/ERROR_CLASSES/SOURCE_TAG; lines 270-300 for existing buildBody — EXTENSION TARGET)
    - tests/e2e/lib/selection.js (lines 165-180 for canonical CONTAINERS selector vocabulary — DIAG-01 verbatim-match source)
    - tests/e2e/lib/navigation.js (lines 28-40 for 'main, article, patent-result' waitForSelector — DIAG-01 verbatim-match source)
    - tests/e2e/lib/issue-payload-builder.js (lines 200-220 for Verifier Disagreement section literals — DIAG-02 template-parity source)
    - scripts/quarantine-append.mjs (lines 235-245 for isFixtureMutator filter regex — MUTATOR-04 co-design invariant being preserved, NOT modified)
  </read_first>
  <behavior>
    - For errorClass='GOOGLE_DOM_DRIFT': buildBody output contains an HTML/fenced block including at least one of the literal substrings 'patent-result', 'section[itemprop="claims"]', 'main', 'article' (verbatim, from selection.js:170-172 / navigation.js:34 vocabulary, per D-DIAG-01)
    - For errorClass='WRONG_CITATION': buildBody output contains the literal headers '### Verifier Disagreement', 'Expected citation', 'Observed citation', 'Verifier tier:', 'Rerun verdict:' (template parity with issue-payload-builder.js:208-216, per D-DIAG-02)
    - For errorClass in {'LLM_HALLUCINATED_SELECTION', 'WORKER_FALLBACK_FAILED', 'HARNESS_ERROR'}: buildBody output retains pre-extension shape (default branch — no diagnostic block; existing 7+ tests I1-I9 in e2e-inject-defect.test.js stay green)
    - For ANY errorClass: line 1 of buildBody output is verbatim `<!-- fp: ${fp} -->` (v2 marker preserved); body contains literal string 'fixture-mutator-uat-47b' (SOURCE_TAG preserved); body contains literal `Source: ${SOURCE_TAG}` line
    - Same {fp, caseId, seed, errorClass} inputs always produce byte-identical output (pure function — no Date.now, no Math.random, no crypto.randomBytes inside buildBody or its helper)
  </behavior>
  <action>
    Add a NEW pure helper `buildDiagnosticBlock(errorClass, seed)` to `tests/e2e/scripts/inject-defect.mjs` immediately above `buildBody` (before line 271). The helper returns an array of strings: for `GOOGLE_DOM_DRIFT` an array containing a fenced HTML snippet with verbatim selectors `<main>`, `<article>`, `<patent-result>`, `<section itemprop="claims">` (at minimum one of these MUST be present verbatim per DIAG-01 — recommend including all four nested for richest scaffold context); for `WRONG_CITATION` an array containing the verbatim headers `### Verifier Disagreement`, `Expected citation (golden): \`1:34-46\``, `Observed citation: \`2:12-24\``, a fenced reason line, `Verifier tier: A`, `Rerun verdict: CONFIRMED (3/3)` (template parity with issue-payload-builder.js:208-216 per DIAG-02); for any other errorClass an empty array `[]` (preserves existing body shape for non-extended classes).

    Modify `buildBody` at line 277 to: (a) keep line 1 marker `<!-- fp: ${fp} -->` and `### Reproducer` header lines (lines 279-285) unchanged; (b) call `buildDiagnosticBlock(errorClass, seed)` and splice its return value via `...` spread INTO the array between the `### Reproducer` block and the `### Synthetic Defect` block; (c) keep `Source: ${SOURCE_TAG}` line and cleanup runbook line unchanged. The function remains pure (no I/O, no time/random sources) — determinism comes from input args only.

    Constraints (NON-NEGOTIABLE, per D-MUTATOR-04 and trust invariants):
    - DO NOT modify the `SOURCE_TAG` literal at line 75 ('fixture-mutator-uat-47b' must remain byte-identical)
    - DO NOT modify the `ERROR_CLASSES` Set at lines 64-70 (additive-only allowlist — no entries added or removed in this task; v4.3 capability expansion that adds new classes is Phase 65 scope, not Phase 61)
    - DO NOT modify the `<!-- fp: ${fp} -->` v2 marker shape on line 1 of buildBody output (existing test I6 at e2e-inject-defect.test.js asserts this regex)
    - DO NOT introduce Math.random, Date.now, crypto.randomBytes, or any external file read inside buildBody or buildDiagnosticBlock — purity is the determinism guarantee
    - The seed argument is currently used in `seed: ${seed}` line at line 283; do not change that reference. If buildDiagnosticBlock uses `seed`, it MUST use it as a deterministic-string substitution (e.g., a `seed-hint: ${seed}` tail line for the GOOGLE_DOM_DRIFT block), NOT as a seed for any RNG.

    Implementation hint: Use the array-spread pattern shown in the existing buildBody. The helper returns string[]; buildBody splices it via `...buildDiagnosticBlock(errorClass, seed)` between the reproducer block and the synthetic-defect block. Empty array for non-extended classes spreads to nothing — body shape unchanged.
  </action>
  <verify>
    <automated>npx vitest run tests/e2e/scripts/e2e-inject-defect.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fixture-mutator-uat-47b" tests/e2e/scripts/inject-defect.mjs` returns exactly 1 (SOURCE_TAG literal preserved)
    - `grep -n "buildDiagnosticBlock" tests/e2e/scripts/inject-defect.mjs` returns at least 2 hits (declaration + call site in buildBody)
    - `grep -c "ERROR_CLASSES = new Set" tests/e2e/scripts/inject-defect.mjs` returns exactly 1 and includes the original 5 entries unchanged
    - Running `node -e "import('./tests/e2e/scripts/inject-defect.mjs').then(m => { const a = m.buildBody({fp:'aaaaaaaaaaaa',caseId:'tc',seed:'s1',errorClass:'GOOGLE_DOM_DRIFT'}); const b = m.buildBody({fp:'aaaaaaaaaaaa',caseId:'tc',seed:'s1',errorClass:'GOOGLE_DOM_DRIFT'}); console.log(a === b); })"` prints `true` (determinism)
    - Running same command with errorClass:'GOOGLE_DOM_DRIFT' produces output where `/patent-result|section\[itemprop="claims"\]|main|article/` matches (verbatim selector match)
    - Running with errorClass:'WRONG_CITATION' produces output containing all of: '### Verifier Disagreement', 'Expected citation', 'Observed citation', 'Verifier tier:', 'Rerun verdict:'
    - Existing tests I1-I9 in tests/e2e/scripts/e2e-inject-defect.test.js still pass (verified via the vitest run command in `<verify>`)
    - No new top-level imports added to inject-defect.mjs beyond what existed (zero-new-deps invariant preserved)
  </acceptance_criteria>
  <done>buildBody emits per-errorClass diagnostic block for GOOGLE_DOM_DRIFT (verbatim Google-Patents selector) + WRONG_CITATION (Phase 35 Verifier Disagreement template parity); all pre-existing inject-defect tests stay green; SOURCE_TAG + ERROR_CLASSES + v2 marker byte-unchanged</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend e2e-inject-defect.test.js with DIAG-01/02/03 Vitest assertions</name>
  <files>tests/e2e/scripts/e2e-inject-defect.test.js</files>
  <read_first>
    - tests/e2e/scripts/e2e-inject-defect.test.js (full file — 323 lines — for existing describe/it pattern and import style; identifies extension point at end of file or alongside existing buildBody describe block)
    - tests/e2e/scripts/inject-defect.mjs (post-Task-1 state — verify the extended buildBody is importable and helper is reachable via buildBody output rather than direct buildDiagnosticBlock import)
    - tests/e2e/lib/selection.js (lines 165-180 — selector vocabulary the regex assertion targets)
    - tests/e2e/lib/issue-payload-builder.js (lines 200-220 — Verifier Disagreement headers the contains-assertions target)
  </read_first>
  <behavior>
    - Test: `DIAG-03 GOOGLE_DOM_DRIFT verbatim selector` — calls buildBody with errorClass='GOOGLE_DOM_DRIFT' and asserts the output matches the regex `/patent-result|section\[itemprop="claims"\]|main|article/` (verbatim selector match from selection.js:170-172 + navigation.js:34 vocabulary)
    - Test: `DIAG-03 WRONG_CITATION template parity` — calls buildBody with errorClass='WRONG_CITATION' and asserts the output contains ALL of the literal headers: '### Verifier Disagreement', 'Expected citation', 'Observed citation', 'Verifier tier:', 'Rerun verdict:'
    - Test: `DIAG-03 determinism — GOOGLE_DOM_DRIFT` — calls buildBody twice with identical {fp:'aaaaaaaaaaaa', caseId:'tc', seed:'s1', errorClass:'GOOGLE_DOM_DRIFT'} and asserts `bodyA === bodyB` (byte-identical)
    - Test: `DIAG-03 determinism — WRONG_CITATION` — same shape, errorClass='WRONG_CITATION', different fp+seed; asserts byte-identical across two calls
    - Test: `DIAG-03 SOURCE_TAG literal preserved across all errorClasses` — iterates over ['GOOGLE_DOM_DRIFT', 'WRONG_CITATION', 'WORKER_FALLBACK_FAILED'], calls buildBody for each, asserts every body contains the literal 'fixture-mutator-uat-47b' (MUTATOR-04 co-design invariant)
    - Test: `DIAG-03 v2 marker line 1 invariant` — calls buildBody with any errorClass and asserts `body.split('\n')[0]` matches `/^<!-- fp: [0-9a-f]{12} -->$/` (line 1 marker preserved; complements existing test I6)
  </behavior>
  <action>
    Append a new `describe('inject-defect.mjs — DIAG-03 deterministic diagnostic body', () => { ... })` block to `tests/e2e/scripts/e2e-inject-defect.test.js` at the end of the file (after the last existing closing `});`). Use the same import pattern already in the file (the file already imports from `./inject-defect.mjs` — extend the existing import to include `buildBody` if not already imported). Inside the describe block, add 6 `it(...)` cases implementing the assertions enumerated in `<behavior>` above.

    For the selector-verbatim test (DIAG-03 GOOGLE_DOM_DRIFT): use the regex literal `/patent-result|section\[itemprop="claims"\]|main|article/` and `expect(body).toMatch(...)`. This is the load-bearing DIAG-01 anti-mutator-drift pin per PITFALLS Pitfall 2.

    For the template-parity test (DIAG-03 WRONG_CITATION): use 5 successive `expect(body).toContain('### Verifier Disagreement')` / `expect(body).toContain('Expected citation')` / `expect(body).toContain('Observed citation')` / `expect(body).toContain('Verifier tier:')` / `expect(body).toContain('Rerun verdict:')` calls (template-parity check per DIAG-02 — co-design with issue-payload-builder.js:208-216).

    For determinism tests: declare `const args = { fp:'aaaaaaaaaaaa', caseId:'tc', seed:'s1', errorClass:'GOOGLE_DOM_DRIFT' };` then `expect(buildBody(args)).toBe(buildBody(args))`. Repeat for WRONG_CITATION with seed 's2'.

    For SOURCE_TAG preservation: write `for (const errorClass of ['GOOGLE_DOM_DRIFT', 'WRONG_CITATION', 'WORKER_FALLBACK_FAILED']) { const body = buildBody({fp:'cccccccccccc', caseId:'tc', seed:'sx', errorClass}); expect(body).toContain('fixture-mutator-uat-47b'); }`.

    For v2 marker test: `expect(body.split('\n')[0]).toMatch(/^<!-- fp: [0-9a-f]{12} -->$/);` with any errorClass.

    Constraints:
    - DO NOT delete or modify any existing `it(...)` cases or `describe(...)` blocks
    - DO NOT add `toMatchInlineSnapshot()` calls (the RESEARCH §pattern-2 examples show snapshot pins; the locked DIAG-03 strategy per VALIDATION.md is contains/regex/byte-identical assertions only — snapshot pins are deferred to keep this plan tight; if researcher snippets included `toMatchInlineSnapshot()` they are illustrative not required)
    - DO NOT add new top-level imports beyond what is needed (likely no additions if `buildBody` is already imported; otherwise add to the existing import statement only)
    - Use `describe`/`it`/`expect` already imported from 'vitest' at the top of the file
  </action>
  <verify>
    <automated>npx vitest run tests/e2e/scripts/e2e-inject-defect.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The vitest run for `tests/e2e/scripts/e2e-inject-defect.test.js` exits 0 (all tests pass — both pre-existing and new)
    - `grep -c "DIAG-03" tests/e2e/scripts/e2e-inject-defect.test.js` returns at least 6 (matches the 6 new `it(...)` cases)
    - `grep -c "fixture-mutator-uat-47b" tests/e2e/scripts/e2e-inject-defect.test.js` returns at least 1 (SOURCE_TAG preservation test present)
    - `grep -c "Verifier Disagreement" tests/e2e/scripts/e2e-inject-defect.test.js` returns at least 1 (DIAG-02 template-parity test present)
    - `grep -E "patent-result|section\\[itemprop" tests/e2e/scripts/e2e-inject-defect.test.js` returns at least 1 hit (DIAG-01 selector-verbatim test present)
    - Running `npx vitest run tests/e2e/scripts/e2e-inject-defect.test.js -t "determinism"` reports >= 2 tests passed (GOOGLE_DOM_DRIFT + WRONG_CITATION determinism)
    - Running `npx vitest run tests/e2e/scripts/e2e-inject-defect.test.js -t "GOOGLE_DOM_DRIFT verbatim selector"` reports 1 test passed
    - Running `npx vitest run tests/e2e/scripts/e2e-inject-defect.test.js -t "WRONG_CITATION template parity"` reports 1 test passed
  </acceptance_criteria>
  <done>6 new DIAG-03 Vitest cases pin DIAG-01 selector-verbatim match, DIAG-02 template parity, byte-identical determinism, SOURCE_TAG preservation, v2 marker invariant; npx vitest run on this file is green</done>
</task>

</tasks>

<verification>
After both tasks:

1. `npx vitest run tests/e2e/scripts/e2e-inject-defect.test.js` exits 0 — all pre-existing I1-I9 tests stay green; 6 new DIAG-03 tests pass.
2. Source-grep guards:
   - `grep -c "fixture-mutator-uat-47b" tests/e2e/scripts/inject-defect.mjs` == 1
   - `grep -c "ERROR_CLASSES = new Set" tests/e2e/scripts/inject-defect.mjs` == 1
   - `grep -c "buildDiagnosticBlock" tests/e2e/scripts/inject-defect.mjs` >= 2
3. Trust invariants verified by adjacent grep (MUTATOR-04 co-design preservation): `grep -c "&& !isFixtureMutator" scripts/quarantine-append.mjs` == 1 (unchanged — file not touched in this plan).
4. NO changes outside `tests/e2e/scripts/inject-defect.mjs` and `tests/e2e/scripts/e2e-inject-defect.test.js` in this plan. The `assertTripleGate` and `appendLedgerEntry` bodies are untouched by this plan's surface.
</verification>

<success_criteria>
- DIAG-01: `buildBody(...,errorClass:'GOOGLE_DOM_DRIFT')` output contains at least one verbatim Google-Patents selector from `selection.js`/`navigation.js` vocabulary, verified by Vitest regex assertion
- DIAG-02: `buildBody(...,errorClass:'WRONG_CITATION')` output contains all 5 Phase 35 Verifier Disagreement template literals, verified by Vitest contains-assertions
- DIAG-03: byte-identical output across two calls with identical inputs (same-seed determinism); SOURCE_TAG `'fixture-mutator-uat-47b'` literal preserved verbatim across all errorClasses
- Existing 7+ inject-defect tests (I1-I9) remain green (no regression)
- buildBody remains a pure function (no time/random/file-system sources introduced)
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mutator → gh issue body | Pure-function string composition; output flows verbatim into `gh issue create --body-file -` (stdin-fed to avoid shell-quote hazards on backticks/fences/HTML comments per inject-defect.mjs:303). No untrusted external input crosses this boundary in this plan. |
| mutator → quarantine-append.mjs filter | Co-design invariant: `quarantine-append.mjs:239 && !isFixtureMutator` reads body for SOURCE_TAG literal. Boundary preserved by keeping SOURCE_TAG byte-identical. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01-01 | Tampering | tests/e2e/scripts/inject-defect.mjs:buildBody | mitigate | Determinism Vitest pin (same-seed → byte-identical output) detects any non-pure mutation (Math.random/Date.now leakage) introduced by future edits |
| T-61-01-02 | Tampering | SOURCE_TAG literal at inject-defect.mjs:75 | mitigate | Source-grep gate `grep -c "fixture-mutator-uat-47b" tests/e2e/scripts/inject-defect.mjs == 1` plus Vitest SOURCE_TAG-preservation iteration test catches accidental edit. Co-design partner `scripts/quarantine-append.mjs:239` regex relies on this literal — drift here cascades to MUTATOR-04 production-path suppression failure. |
| T-61-01-03 | Spoofing | mutator-emitted DOM snippet vs real Google Patents page | mitigate | Vitest selector-verbatim regex pins synthetic snippet to `selection.js`/`navigation.js` vocabulary, preventing the Pitfall-2 failure mode where LLM proposes a fix for a fictional DOM that the verifier-gate rejects against real Google Patents (76-case regression goes 0-PASS) |
| T-61-01-04 | Information Disclosure | Verifier Disagreement block in WRONG_CITATION body | accept | The fixture-injected expected/observed citations (e.g., `1:34-46` / `2:12-24`) are synthetic literals, not real production data — no PII or production-content leak. Phase 42 PROMPT-02 envelope-pop defenses in issue-payload-builder.js apply only to real payloads; synthetic mutator path is fully controlled. |
| T-61-01-SC | Tampering | npm install / new dependency | mitigate | Zero new npm packages introduced (verified by reviewer); no install tasks; Package Legitimacy Audit not applicable per RESEARCH.md (none table) |
</threat_model>

<output>
Create `.planning/phases/61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep/61-01-SUMMARY.md` when done.
</output>
