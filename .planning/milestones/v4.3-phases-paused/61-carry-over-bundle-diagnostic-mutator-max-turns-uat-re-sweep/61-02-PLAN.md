---
phase: 61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/lib/llm-driver.js
  - tests/unit/llm-driver.test.js
  - tests/unit/llm-driver-cost-bound.test.js
  - tests/fixtures/ledger-cost-bound.jsonl
autonomous: true
requirements: [TURNS-01, TURNS-02, TURNS-03]
tags: [llm-driver, max-turns, tools-palette, cost-bound, vitest-fixture]

must_haves:
  truths:
    - "tests/e2e/lib/llm-driver.js:invokeClaudeP spawns the claude subprocess with argv literal containing '--max-turns', '5', '--tools', 'Read,Glob,Grep', '--max-budget-usd', '0.50' in that order, AND the trailing '--system-prompt', systemPrompt, userPrompt positional arguments unchanged"
    - "The argv literal does NOT contain the strings 'Edit', 'Bash', 'Write', 'WebFetch', '--allowed-tools', '--allowedTools' anywhere (these are permission-grant or forbidden-tool literals that would silently re-enable the v4.0 trust-invariant violation per Pitfall 1)"
    - "SDK transport (invokeAnthropicSdkWithLedger / messages.create) argv is UNCHANGED — single-turn by API design; an inline source comment at the subscription site documents this asymmetry"
    - "tests/unit/llm-driver.test.js Test 23 (line 389) is updated in place: the expected argv toEqual now includes --max-turns 5 + --tools Read,Glob,Grep + --max-budget-usd 0.50, plus explicit `.not.toContain('Edit')` / `.not.toContain('Bash')` / `.not.toContain('Write')` / `.not.toContain('WebFetch')` / `.not.toContain('--allowed-tools')` / `.not.toContain('--allowedTools')` assertions"
    - "tests/unit/llm-driver-cost-bound.test.js exists as a NEW file; loads tests/fixtures/ledger-cost-bound.jsonl (5 entries); asserts mean of cost_usd field is < 0.30 AND > 0.20; asserts each individual entry's cost_usd < $1 (per-issue cap); asserts --max-budget-usd argv value 0.50 is < ISSUE_HARD_CAP_USD 1.00"
    - "tests/fixtures/ledger-cost-bound.jsonl exists with exactly 5 JSON lines; each line is a valid JSON object with iso/cost_usd/transport/source/issueId fields; mean(cost_usd) ∈ [$0.20, $0.29]"
  artifacts:
    - path: "tests/e2e/lib/llm-driver.js"
      provides: "Subscription transport argv updated to --max-turns 5 + --tools Read,Glob,Grep + --max-budget-usd 0.50; inline comment at lines 80-84 documents both Pitfall 1+2 trust invariants and SDK single-turn asymmetry"
      contains: "--tools"
    - path: "tests/unit/llm-driver.test.js"
      provides: "Test 23 (line 389) updated: positive argv-equality assertion + 6 negative .not.toContain assertions for forbidden palette tokens"
      contains: "--max-turns', '5'"
    - path: "tests/unit/llm-driver-cost-bound.test.js"
      provides: "NEW Vitest file: TURNS-03 cost-bound regression — mean-per-call < $0.30 across 5 smoke-issue fixture entries"
      min_lines: 30
    - path: "tests/fixtures/ledger-cost-bound.jsonl"
      provides: "NEW 5-entry deterministic fixture (one JSON object per line) for cost-bound regression"
      min_lines: 5
  key_links:
    - from: "tests/e2e/lib/llm-driver.js:91-97 argv literal"
      to: "tests/unit/llm-driver.test.js:389 Test 23 expected-equality assertion"
      via: "byte-identical argv string match"
      pattern: "--max-turns', '5', '--tools', 'Read,Glob,Grep'"
    - from: "tests/unit/llm-driver-cost-bound.test.js"
      to: "tests/fixtures/ledger-cost-bound.jsonl"
      via: "fs.readFileSync + JSON.parse per line"
      pattern: "ledger-cost-bound.jsonl"
---

<objective>
Update `tests/e2e/lib/llm-driver.js:invokeClaudeP` argv literal (lines 91-97) to swap `'--max-turns', '1'` for `'--max-turns', '5'` and inject `'--tools', 'Read,Glob,Grep'` plus `'--max-budget-usd', '0.50'` (subscription transport ONLY — SDK transport unchanged by API design). Extend `tests/unit/llm-driver.test.js` Test 23 (line 389) in place to assert the new argv AND explicit `.not.toContain` exclusions for `Edit`/`Bash`/`Write`/`WebFetch`/`--allowed-tools`/`--allowedTools`. Create NEW file `tests/unit/llm-driver-cost-bound.test.js` (TURNS-03 cost-bound regression with `tests/fixtures/ledger-cost-bound.jsonl` 5-entry fixture asserting mean per-call < $0.30).

Purpose: Phase 61 atomic bundle Pitfall 1 + 2 + 9 mitigation. Multi-turn `--max-turns 5` lets Claude read source files via Read/Glob/Grep tools (needed for the v4.2 SWEEP-03 `error_max_turns` failure to clear), while `--tools` palette restriction (NOT `--allowedTools` permission grant per RESEARCH STACK § flag-name correctness, verified against `claude --help` v2.1.169) and `--max-budget-usd 0.50` defense-in-depth keep cost-discipline gates closed. The Vitest pins detect any future PR that re-enables `Edit`/`Bash` "for debugging" before it can ship.

Output: Modified `llm-driver.js` subscription-transport argv literal + extended Test 23 + new `llm-driver-cost-bound.test.js` + new `ledger-cost-bound.jsonl` fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep/61-CONTEXT.md
@.planning/phases/61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep/61-RESEARCH.md

<!-- READ-ONLY references — files extending/testing -->
@tests/e2e/lib/llm-driver.js
@tests/unit/llm-driver.test.js

<interfaces>
<!-- Verified extraction from disk 2026-06-09 -->

From tests/e2e/lib/llm-driver.js:80-97 (CURRENT — EXTENSION TARGET):
```javascript
 * Args (Pitfalls 1, 2 — DO NOT change):
 *   ['-p', '--output-format', 'json', '--max-turns', '1',
 *    '--system-prompt', sysP, userP]
 *
 * @param {{ systemPrompt: string, userPrompt: string, timeoutMs?: number }} opts
 * @returns {Promise<{ timedOut: boolean, stdout: string, stderr: string, code: number|null }>}
 */
export async function invokeClaudeP({ systemPrompt, userPrompt, timeoutMs = LLM_TIMEOUT_MS } = {}) {
  return new Promise((resolve) => {
    const args = [
      '-p',
      '--output-format', 'json',
      '--max-turns', '1',
      '--system-prompt', systemPrompt,
      userPrompt,
    ];
    // Threat T-31-4 mitigation: blank ANTHROPIC_API_KEY even if developer set it.
    const env = { ...process.env, ANTHROPIC_API_KEY: '' };

    const child = spawn('claude', args, { stdio: ['ignore', 'pipe', 'pipe'], env });
```

From tests/unit/llm-driver.test.js:389-408 (CURRENT — EXTENSION TARGET):
```javascript
  it('23. args are EXACTLY ["-p","--output-format","json","--max-turns","1","--system-prompt",sysP,userP] — no --bare, no --json-schema', async () => {
    const promise = invokeClaudeP({
      systemPrompt: 'mysys',
      userPrompt: 'myuser',
      timeoutMs: 5_000,
    });
    setTimeout(() => mockChild.emit('close', 0), 5);
    await promise;
    expect(spawnCalls.length).toBe(1);
    const { args } = spawnCalls[0];
    expect(args).toEqual([
      '-p',
      '--output-format', 'json',
      '--max-turns', '1',
      '--system-prompt', 'mysys',
      'myuser',
    ]);
    expect(args).not.toContain('--bare');
    expect(args).not.toContain('--json-schema');
  });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update llm-driver.js subscription argv + extend Test 23 (TURNS-01 + TURNS-02)</name>
  <files>tests/e2e/lib/llm-driver.js, tests/unit/llm-driver.test.js</files>
  <read_first>
    - tests/e2e/lib/llm-driver.js (entire file: header comment at lines 80-84 documenting Pitfalls 1+2; argv literal at lines 91-97; surrounding spawn call at lines 99-101; SDK path `invokeAnthropicSdkWithLedger` at lines ~506-647 — verify it remains unchanged after this task)
    - tests/unit/llm-driver.test.js (lines 1-50 for vi.mock('node:child_process') setup pattern; lines 380-410 for Test 22 + Test 23 — extension target)
    - .planning/phases/61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep/61-CONTEXT.md (Argv Update section — locked BEFORE/AFTER argv shape)
  </read_first>
  <behavior>
    - invokeClaudeP spawns `claude` with argv literal: `['-p', '--output-format', 'json', '--max-turns', '5', '--tools', 'Read,Glob,Grep', '--max-budget-usd', '0.50', '--system-prompt', systemPrompt, userPrompt]` — exactly this order, exactly these strings
    - The argv literal does NOT contain `'Edit'`, `'Bash'`, `'Write'`, `'WebFetch'`, `'--allowed-tools'`, `'--allowedTools'` — verified by `.not.toContain` assertions in Test 23
    - Header comment at llm-driver.js lines 80-84 is updated to document (a) the new argv shape, (b) the `--tools` palette restriction vs `--allowedTools` permission grant distinction (citing Pitfall 1), (c) the SDK-transport asymmetry (single-turn by API design — `messages.create` is one request → one response, no agent loop)
    - SDK transport function `invokeAnthropicSdkWithLedger` at lines ~506-647 is byte-unchanged (verify via `git diff` post-task)
    - Test 23 in `tests/unit/llm-driver.test.js:389` is updated in place: `it(...)` title reflects new argv shape; `expect(args).toEqual([...])` lists the new 11-element argv; 6 new `.not.toContain` assertions added (Edit/Bash/Write/WebFetch/--allowed-tools/--allowedTools); existing `.not.toContain('--bare')` and `.not.toContain('--json-schema')` assertions preserved
  </behavior>
  <action>
    File 1 — `tests/e2e/lib/llm-driver.js`:

    (a) Update the header doc-comment block at lines 80-84. Replace `* Args (Pitfalls 1, 2 — DO NOT change):` and the two following lines with a multi-line comment block that says:
      - The new argv shape verbatim: `['-p', '--output-format', 'json', '--max-turns', '5', '--tools', 'Read,Glob,Grep', '--max-budget-usd', '0.50', '--system-prompt', sysP, userP]`
      - "DO NOT change without ALSO updating --tools palette restriction. --tools RESTRICTS the available tools (NOT --allowedTools which only grants permission); per Pitfall 1 the auto-fix loop's diff-via-fences contract REQUIRES that Edit/Bash/Write be unavailable in the subprocess."
      - "SDK transport (invokeAnthropicSdkWithLedger) is single-turn by API design — messages.create is one request → one response, no agent loop. The --max-turns 5 asymmetry between subscription and SDK is intentional."

    (b) Replace the argv literal at lines 91-97 with the new 11-element form (per CONTEXT.md locked AFTER value):
    ```
        const args = [
          '-p',
          '--output-format', 'json',
          '--max-turns', '5',
          '--tools', 'Read,Glob,Grep',
          '--max-budget-usd', '0.50',
          '--system-prompt', systemPrompt,
          userPrompt,
        ];
    ```

    (c) Add a 2-line inline comment IMMEDIATELY above the `const args = [` line documenting "--tools restricts palette (NOT --allowedTools permission-grant); --max-budget-usd is verified against claude CLI v2.1.169 --help." (so a future reader sees the rationale at the call site).

    (d) Confirm: do NOT touch `invokeAnthropicSdkWithLedger` or any other function in this file. The diff for `llm-driver.js` should be confined to lines 80-100 (header comment + argv literal + adjacent inline comment).

    File 2 — `tests/unit/llm-driver.test.js`:

    (a) Update Test 23's `it(...)` title at line 389 to: `'23. args contain --max-turns 5 + --tools Read,Glob,Grep + --max-budget-usd 0.50; exclude Edit/Bash/Write/WebFetch/--allowed-tools/--allowedTools (Pitfall 1 trust invariant)'`

    (b) Update the `expect(args).toEqual([...])` block (currently at lines 399-405) to:
    ```
        expect(args).toEqual([
          '-p',
          '--output-format', 'json',
          '--max-turns', '5',
          '--tools', 'Read,Glob,Grep',
          '--max-budget-usd', '0.50',
          '--system-prompt', 'mysys',
          'myuser',
        ]);
    ```

    (c) AFTER the existing two `.not.toContain('--bare')` / `.not.toContain('--json-schema')` lines (currently at lines 406-407), append 6 new `.not.toContain` assertions:
    ```
        // TURNS-02 trust-invariant exclusions (Pitfall 1)
        expect(args).not.toContain('Edit');
        expect(args).not.toContain('Bash');
        expect(args).not.toContain('Write');
        expect(args).not.toContain('WebFetch');
        expect(args).not.toContain('--allowed-tools');
        expect(args).not.toContain('--allowedTools');
    ```

    (d) Do NOT modify any other test (Test 1-22, Test 24+) in this file.

    Constraints:
    - The SDK transport function `invokeAnthropicSdkWithLedger` (lines ~506-647 in llm-driver.js) MUST remain byte-unchanged
    - The `'-p', '--output-format', 'json'` prefix and `'--system-prompt', systemPrompt, userPrompt` suffix MUST remain in the same positions
    - The ANTHROPIC_API_KEY blanking at line 99 MUST remain unchanged (T-31-4 mitigation preserved)
    - DO NOT introduce any new top-level imports in llm-driver.js
    - DO NOT introduce any new top-level imports in llm-driver.test.js
    - The number of `.not.toContain` assertions in Test 23 after this task is exactly 8 (2 pre-existing + 6 new)
  </action>
  <verify>
    <automated>npx vitest run tests/unit/llm-driver.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'--max-turns', '5'" tests/e2e/lib/llm-driver.js` >= 1 (new argv value present)
    - `grep -c "'--max-turns', '1'" tests/e2e/lib/llm-driver.js` == 0 (old argv value removed)
    - `grep -c "'--tools', 'Read,Glob,Grep'" tests/e2e/lib/llm-driver.js` == 1 (new palette-restriction flag present exactly once)
    - `grep -c "'--max-budget-usd', '0.50'" tests/e2e/lib/llm-driver.js` == 1 (defense-in-depth flag present exactly once)
    - `grep -c "'--allowed-tools'\|'--allowedTools'\|'Edit'\|'Bash'\|'Write'\|'WebFetch'" tests/e2e/lib/llm-driver.js` == 0 (forbidden tokens absent from llm-driver.js source)
    - `grep -c "invokeAnthropicSdkWithLedger" tests/e2e/lib/llm-driver.js` >= 1 (SDK function name preserved — verifies SDK path not deleted)
    - `npx vitest run tests/unit/llm-driver.test.js -t "Test 23"` reports 1 test passed (extended Test 23 green)
    - `npx vitest run tests/unit/llm-driver.test.js` exits 0 (all 23+ tests still green)
    - `grep -cE "\\.not\\.toContain\\('(Edit|Bash|Write|WebFetch|--allowed-tools|--allowedTools)'\\)" tests/unit/llm-driver.test.js` == 6 (exactly 6 new exclusion assertions present)
    - `grep -c "ANTHROPIC_API_KEY: ''" tests/e2e/lib/llm-driver.js` >= 1 (T-31-4 mitigation preserved)
  </acceptance_criteria>
  <done>llm-driver.js subscription argv updated to 4-flag form; Test 23 in place with new toEqual + 6 .not.toContain exclusions; SDK transport byte-unchanged; npx vitest run on llm-driver.test.js exits 0</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TURNS-03 cost-bound fixture + Vitest regression file</name>
  <files>tests/unit/llm-driver-cost-bound.test.js, tests/fixtures/ledger-cost-bound.jsonl</files>
  <read_first>
    - tests/unit/llm-driver.test.js (lines 1-30 for the import-from-vitest pattern and the file-header style — pattern to mirror in the new file)
    - tests/unit/llm-ledger.test.js (any existing test in this file showing the fs.readFileSync + JSON.parse pattern for fixture loading — mirror that pattern)
    - tests/fixtures/ (verify directory exists; sibling fixture file present? — use as parent path)
    - .planning/STATE.md (lines 33-46 Budget section — mean-per-call cap of $0.30 is the threshold being asserted)
  </read_first>
  <behavior>
    - tests/fixtures/ledger-cost-bound.jsonl exists with exactly 5 lines; each line is a valid JSON object with at minimum the fields {iso, cost_usd, transport, source, issueId}; the mean of cost_usd across the 5 entries is between $0.20 and $0.29 (well below $0.30 mean-per-call cap, above $0.20 sanity floor)
    - Each individual cost_usd value is < $1 (below per-issue cap)
    - tests/unit/llm-driver-cost-bound.test.js loads the fixture via `fs.readFileSync` + per-line `JSON.parse`, computes mean, asserts:
      - 5 entries loaded
      - mean(cost_usd) < 0.30 (TURNS-03 cost-bound regression assertion)
      - mean(cost_usd) > 0.20 (sanity floor — prevents "all entries set to $0 gaming")
      - per-entry cost_usd < 1.00 (per-issue cap)
      - --max-budget-usd argv value 0.50 < ISSUE_HARD_CAP_USD 1.00 (defense-in-depth tuning sanity check; values declared inline in the test)
    - Each entry's `transport` value is `'subscription'` (the argv update is subscription-only); `source` is `'auto-fix-api'` (matches the dispatcher's source-tag convention)
  </behavior>
  <action>
    File 1 — `tests/fixtures/ledger-cost-bound.jsonl` (NEW file, 5 lines, one JSON object per line):

    Create the file with EXACTLY these 5 lines (each is a complete JSON object on a single line — `.jsonl` format). The cost_usd values sum to $1.20 (mean = $0.24, between $0.20 and $0.29). Recommended literal content (operator may adjust iso timestamps within today's date but cost_usd values are LOCKED to preserve the mean-bound test):
    ```
    {"iso":"2026-06-09T10:00:00Z","cost_usd":0.15,"transport":"subscription","source":"auto-fix-api","issueId":"101","model":"claude-sonnet-4-6"}
    {"iso":"2026-06-09T10:05:00Z","cost_usd":0.22,"transport":"subscription","source":"auto-fix-api","issueId":"102","model":"claude-sonnet-4-6"}
    {"iso":"2026-06-09T10:10:00Z","cost_usd":0.28,"transport":"subscription","source":"auto-fix-api","issueId":"103","model":"claude-sonnet-4-6"}
    {"iso":"2026-06-09T10:15:00Z","cost_usd":0.27,"transport":"subscription","source":"auto-fix-api","issueId":"104","model":"claude-sonnet-4-6"}
    {"iso":"2026-06-09T10:20:00Z","cost_usd":0.28,"transport":"subscription","source":"auto-fix-api","issueId":"105","model":"claude-sonnet-4-6"}
    ```

    File 2 — `tests/unit/llm-driver-cost-bound.test.js` (NEW file):

    Create the file with the following structure:
    - Import `describe`, `it`, `expect` from `'vitest'`
    - Import `readFileSync` from `'node:fs'`
    - Import `dirname`, `resolve` from `'node:path'`
    - Import `fileURLToPath` from `'node:url'` (needed to resolve fixture path relative to test file in ESM)
    - Compute `__dirname` via `dirname(fileURLToPath(import.meta.url))`
    - Compute fixture path: `resolve(__dirname, '..', 'fixtures', 'ledger-cost-bound.jsonl')`
    - Load fixture: `const raw = readFileSync(fixturePath, 'utf8'); const entries = raw.trim().split('\n').map(line => JSON.parse(line));`
    - Wrap tests in `describe('TURNS-03 — --max-turns 5 cost-bound regression', () => { ... })`
    - Test 1: `it('loads exactly 5 fixture entries', ...)` — `expect(entries).toHaveLength(5);`
    - Test 2: `it('mean per-call spend < $0.30 across 5 smoke-issue entries (TURNS-03 cap)', ...)` — compute `const meanPerCall = entries.reduce((acc, e) => acc + e.cost_usd, 0) / entries.length;`, then `expect(meanPerCall).toBeLessThan(0.30); expect(meanPerCall).toBeGreaterThan(0.20);`
    - Test 3: `it('individual entry never exceeds per-issue cap $1', ...)` — for-of loop with `expect(e.cost_usd).toBeLessThan(1.00);`
    - Test 4: `it('every entry tagged transport:subscription + source:auto-fix-api (forensic-ledger schema integrity)', ...)` — for-of loop with `expect(e.transport).toBe('subscription'); expect(e.source).toBe('auto-fix-api');`
    - Test 5: `it('argv --max-budget-usd 0.50 is below ISSUE_HARD_CAP_USD 1.00 (defense-in-depth tuning)', ...)` — declare `const MAX_BUDGET_USD_ARGV = 0.50; const ISSUE_HARD_CAP_USD = 1.00;` inline, then `expect(MAX_BUDGET_USD_ARGV).toBeLessThan(ISSUE_HARD_CAP_USD);`

    Constraints:
    - NO live API calls — fixture-only test (per RESEARCH §TURNS-03 cost-bound approach)
    - NO new npm dependencies — `node:fs` and `node:path` are Node 22 built-ins
    - The fixture path resolution must work under `npx vitest run` from project root (Vitest 3.x runs tests with cwd = repo root by default; the path is computed relative to the test file via `import.meta.url` for robustness)
    - No mocking of `node:fs` or `node:child_process` (this file does NOT exercise the spawn pattern; it reads the static fixture)
    - The fixture file uses LF line endings (Unix-standard); each line is a complete JSON object with NO trailing comma
  </action>
  <verify>
    <automated>npx vitest run tests/unit/llm-driver-cost-bound.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l tests/fixtures/ledger-cost-bound.jsonl` reports exactly 5 (5 entries, one per line)
    - `node -e "const lines = require('node:fs').readFileSync('tests/fixtures/ledger-cost-bound.jsonl','utf8').trim().split('\\n'); const entries = lines.map(l => JSON.parse(l)); const mean = entries.reduce((a,e) => a+e.cost_usd, 0)/entries.length; console.log(mean);"` prints a number where 0.20 < value < 0.30
    - `jq -r '.transport' tests/fixtures/ledger-cost-bound.jsonl | sort -u` outputs only `subscription` (every entry has transport:'subscription')
    - `jq -r '.source' tests/fixtures/ledger-cost-bound.jsonl | sort -u` outputs only `auto-fix-api` (every entry has source:'auto-fix-api')
    - `npx vitest run tests/unit/llm-driver-cost-bound.test.js` exits 0 with 5 tests passed
    - `grep -c "toBeLessThan(0.30)" tests/unit/llm-driver-cost-bound.test.js` >= 1 (TURNS-03 cap assertion present)
    - `grep -c "toBeGreaterThan(0.20)" tests/unit/llm-driver-cost-bound.test.js` >= 1 (sanity-floor assertion present)
    - `grep -c "from 'vitest'" tests/unit/llm-driver-cost-bound.test.js` == 1 (single Vitest import — no other test framework references)
    - `grep -c "@anthropic-ai/sdk" tests/unit/llm-driver-cost-bound.test.js` == 0 (no SDK import — fixture-only test)
  </acceptance_criteria>
  <done>tests/fixtures/ledger-cost-bound.jsonl (5 entries, mean cost_usd ≈ $0.24) + tests/unit/llm-driver-cost-bound.test.js (5 Vitest cases) committed; npx vitest run on the new file exits 0; per-call mean asserted < $0.30 per BUDG-01 / TURNS-03</done>
</task>

</tasks>

<verification>
After both tasks:

1. `npx vitest run tests/unit/llm-driver.test.js tests/unit/llm-driver-cost-bound.test.js` exits 0 — Test 23 + new cost-bound file both green.
2. `grep -c "'--max-turns', '5'" tests/e2e/lib/llm-driver.js` == 1; `grep -c "'--max-turns', '1'" tests/e2e/lib/llm-driver.js` == 0.
3. `grep -cE "'(Edit|Bash|Write|WebFetch|--allowed-tools|--allowedTools)'" tests/e2e/lib/llm-driver.js` == 0 (no forbidden palette tokens in source).
4. `wc -l tests/fixtures/ledger-cost-bound.jsonl` == 5; mean cost_usd ∈ [0.20, 0.29] (verified by node one-liner).
5. SDK transport unchanged: `grep -n "messages.create" tests/e2e/lib/llm-driver.js` returns the original line numbers; `invokeAnthropicSdkWithLedger` body unchanged.
6. Trust invariant preservation:
   - ANTHROPIC_API_KEY blanking at llm-driver.js:99 unchanged (T-31-4 mitigation)
   - SDK ESLint single-entry-point preserved (no new SDK imports added)
   - `appendLedgerEntry` body unchanged (file not touched in this plan)
</verification>

<success_criteria>
- TURNS-01: llm-driver.js subscription argv literal contains `'--max-turns', '5', '--tools', 'Read,Glob,Grep', '--max-budget-usd', '0.50'` in that order; SDK transport function byte-unchanged
- TURNS-02: Test 23 in tests/unit/llm-driver.test.js asserts new toEqual AND 6 new `.not.toContain` exclusions for Edit/Bash/Write/WebFetch/--allowed-tools/--allowedTools
- TURNS-03: tests/unit/llm-driver-cost-bound.test.js fixture-loads tests/fixtures/ledger-cost-bound.jsonl (5 entries), asserts mean cost_usd < $0.30 AND > $0.20 (sanity floor), per-entry < $1, --max-budget-usd 0.50 < ISSUE_HARD_CAP_USD 1.00
- npx vitest run on llm-driver.test.js AND llm-driver-cost-bound.test.js both exit 0
- Zero new npm dependencies introduced
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatcher → claude subprocess | Subscription transport spawn at `tests/e2e/lib/llm-driver.js:101` (`spawn('claude', args, ...)`). Trust gate: Claude's tool palette must NOT include Edit/Bash/Write so the dispatcher's parseFencedDiff → checkDiffGuard → git apply chain remains the sole code-mutation path. Argv literal IS the gate. |
| test fixture → cost-bound regression | tests/fixtures/ledger-cost-bound.jsonl is a static, version-controlled fixture (NOT loaded over network). cost_usd values are byte-identical across runs — no time/network/PII flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-02-01 | Elevation of Privilege | claude subprocess tool palette (--max-turns 5) | mitigate | --tools 'Read,Glob,Grep' RESTRICTS palette (verified --help v2.1.169); 6 .not.toContain assertions in Test 23 detect any future PR re-introducing Edit/Bash/Write/WebFetch tokens; --allowed-tools / --allowedTools also excluded (these are permission-grants, NOT palette restrictions per RESEARCH STACK convergence) |
| T-61-02-02 | Tampering | argv literal at llm-driver.js:91-97 | mitigate | Test 23 toEqual is an exact-byte-sequence assertion — any drift (extra space, reordered flag, removed --tools) fails loudly. The 6 exclusion `.not.toContain` provide defense-in-depth against the most likely mutation vectors. |
| T-61-02-03 | Denial of Service (cost) | cost runaway via --max-turns 5 × multi-issue UAT | mitigate | --max-budget-usd 0.50 argv flag (verified real on claude CLI v2.1.169) enforces per-call hard ceiling; TURNS-03 fixture pin asserts mean per-call < $0.30 across 5 entries; per-issue ISSUE_HARD_CAP_USD=$1 and per-PR $2 existing caps preserved (no changes to llm-ledger.js in this plan) |
| T-61-02-04 | Spoofing | fixture entries pretending to be ledger entries | accept | The fixture is explicitly under tests/fixtures/ — out-of-scope for the live ledger reader; reader-side `isAttributable` filters in scripts/a-b-winner.mjs only consume `tests/e2e/.llm-spend-ledger.json`, not this fixture. No risk of fixture-as-ledger-entry confusion. |
| T-61-02-05 | Information Disclosure | model name in fixture | accept | Fixture uses synthetic 'claude-sonnet-4-6' model literal — same as production routing default — no sensitive model name disclosure |
| T-61-02-SC | Tampering | npm install / new dependency | mitigate | Zero new npm packages; node:fs + node:path are Node 22 built-ins; no install tasks in this plan. Package Legitimacy Audit not applicable per RESEARCH.md. |
</threat_model>

<output>
Create `.planning/phases/61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep/61-02-SUMMARY.md` when done.
</output>
