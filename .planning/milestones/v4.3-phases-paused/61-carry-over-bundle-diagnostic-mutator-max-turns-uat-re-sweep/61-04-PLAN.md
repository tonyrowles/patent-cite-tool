---
phase: 61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep
plan: 04
type: execute
wave: 2
depends_on: [61-03]
files_modified:
  - .planning/sweep-03-04-pass-evidence.yaml
autonomous: false
requirements: [UAT-01]
tags: [uat-runbook, sweep-03, gh-cli, live-api-spend, evidence-sentinel, post-merge]

user_setup:
  - service: github
    why: "gh CLI orchestrates issue creation, workflow runs, PR review, and merge for the synthetic GOOGLE_DOM_DRIFT live UAT loop"
    env_vars:
      - name: GH_TOKEN
        source: "Either gh auth login (preferred) OR a Personal Access Token with repo + workflow scopes from https://github.com/settings/tokens"
    dashboard_config: []
  - service: anthropic
    why: "Auto-fix loop spends real API budget on the live UAT (subscription credit pool OR ANTHROPIC_API_KEY-backed SDK transport — operator's call per CONTEXT.md Discretion item)"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Only if SDK transport selected; otherwise the subscription transport uses local Claude CLI auth (claude /login). Source: https://console.anthropic.com/settings/keys"
    dashboard_config: []

must_haves:
  truths:
    - "ONE synthetic GOOGLE_DOM_DRIFT issue created on origin via inject-defect.mjs --error-class GOOGLE_DOM_DRIFT, with a known seed value recorded for evidence reproducibility"
    - "v40-auto-fix.yml workflow fires on the issue label; auto-fix dispatcher invokes Claude via either subscription or SDK transport; produces a unified-diff response"
    - "Auto-fix PR opens, passes verifier-gate (auto-fix:verified label applied), merges to origin/main via standard PR review path (NOT --admin, per BYPASS-03)"
    - "Promote pipeline (v40-auto-promote.yml + scripts/auto-fix-promote.mjs) writes an outcome ledger entry visible in tests/e2e/.llm-spend-ledger.json with: errorClass='GOOGLE_DOM_DRIFT', outcome='pass', source='auto-fix-promoted', transport='sdk' or 'subscription' (recorded as observed)"
    - ".planning/sweep-03-04-pass-evidence.yaml exists (created if absent) with a uat_01 row containing: passed_at_iso, errorClass='GOOGLE_DOM_DRIFT', outcome='pass', source='auto-fix-promoted', transport, issueId, prNumber, ledgerEntryIso (the iso field of the matching ledger entry for cross-reference)"
  artifacts:
    - path: ".planning/sweep-03-04-pass-evidence.yaml"
      provides: "Phase 68 precondition sentinel — Phase 68 cleanup script refuses to run unless this file exists with non-empty passed_at_iso fields per Pitfall 8 mitigation"
      contains: "uat_01:"
  key_links:
    - from: ".planning/sweep-03-04-pass-evidence.yaml uat_01 row"
      to: "tests/e2e/.llm-spend-ledger.json outcome entry"
      via: "ledgerEntryIso field cross-reference"
      pattern: "errorClass.*GOOGLE_DOM_DRIFT.*outcome.*pass"
    - from: "uat_01 evidence row"
      to: "GitHub issue + PR pair"
      via: "issueId + prNumber gh API resolvable URLs"
      pattern: "issueId.*prNumber"
---

<objective>
Execute the live UAT-01 runbook (SWEEP-03 / UAT-47-a) after the Phase 61 atomic commit (Plan 03) has landed on origin/main. The runbook drives a synthetic GOOGLE_DOM_DRIFT issue through the entire auto-fix loop: inject → v40-auto-fix.yml workflow → Claude (subscription or SDK transport) → unified-diff PR → verifier-gate PASS → human-review merge → v40-auto-promote.yml → outcome ledger entry. Capture the PASS evidence into `.planning/sweep-03-04-pass-evidence.yaml` as the `uat_01` row — this file becomes the Phase 68 precondition sentinel.

Purpose: Closes the v4.2 architectural deferral (UAT-47-a STILL-DEFERRED through Phase 51 + 59 — see STATE.md Deferred Items table). Live evidence that errorClass + outcome + source + transport ledger fields flow end-to-end through the auto-fix loop on a real synthetic, validating the Phase 61 atomic bundle's behavioral claim.

Output: One closed PR on origin/main (the auto-fix PR that fixed the synthetic GOOGLE_DOM_DRIFT issue), one ledger entry with full forensic provenance, one row appended to `.planning/sweep-03-04-pass-evidence.yaml`.

This plan is NOT autonomous — it requires gh CLI authentication, live API spend (subscription credits OR API key), and human merge approval (per BYPASS-03 runbook: no `--admin` bypass on auto-fix/* branches).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep/61-CONTEXT.md
@.planning/phases/61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep/61-RESEARCH.md
@.planning/phases/61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep/61-03-SUMMARY.md

<!-- READ-ONLY references — runbook source files -->
@tests/e2e/scripts/inject-defect.mjs
@.github/workflows/v40-auto-fix.yml
@scripts/auto-fix-promote.mjs

<interfaces>
<!-- The evidence sentinel schema (NEW file; created in this plan) -->

.planning/sweep-03-04-pass-evidence.yaml schema:
```yaml
schema_version: 1
created_at_iso: "<ISO 8601 timestamp of file creation>"

uat_01:
  passed_at_iso: "<ISO 8601 timestamp of evidence capture>"
  errorClass: "GOOGLE_DOM_DRIFT"
  outcome: "pass"
  source: "auto-fix-promoted"
  transport: "sdk" | "subscription"  # whichever the auto-fix loop chose at run-time
  issueId: <integer GitHub issue number>
  prNumber: <integer GitHub PR number>
  ledgerEntryIso: "<ISO 8601 timestamp of the matching outcome entry in tests/e2e/.llm-spend-ledger.json>"
  seed: "<the --seed value passed to inject-defect.mjs>"
  notes: "<free-text operator observations, e.g., 'PR #N merged at HH:MM; verifier-gate green on first attempt'>"
```

uat_02 row (Plan 05) will be appended later with the same shape — this plan defines the schema and creates the file.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Pre-flight — confirm Phase 61 atomic commit on origin/main + gh CLI auth + spend posture</name>
  <what-built>The Phase 61 atomic commit (Plan 03 output) must be live on origin/main BEFORE this runbook starts. The gh CLI must be authenticated, and the operator must choose a spend posture for the live API call (subscription credits via local claude CLI OR ANTHROPIC_API_KEY for SDK transport).</what-built>
  <how-to-verify>
    1. Verify Phase 61 commit is on origin/main:
       ```
       git fetch origin
       git log origin/main -1 --format='%s %h'
       ```
       Expected: subject starts with `feat(61): atomic carry-over bundle — DIAG + TURNS + BUDG`. If not, STOP and push first (`git push origin main`).

    2. Verify gh CLI auth + repo scope:
       ```
       gh auth status
       ```
       Expected: shows authentication to github.com with `repo` and `workflow` scopes. If not, run `gh auth login` (or set GH_TOKEN env var).

    3. Decide spend posture (operator picks per CONTEXT.md Discretion item):
       - OPTION A (subscription, ~$0 cost): use local `claude` CLI auth (subscription credits). The auto-fix workflow auto-detects this via env vars; no setup needed beyond `claude /login` having been run.
       - OPTION B (SDK transport, ~$0.20-0.50/call cost): ensure `ANTHROPIC_API_KEY` is set in GitHub repo secrets (`gh secret list --repo $OWNER/$REPO` must show ANTHROPIC_API_KEY).

    4. Verify no in-flight auto-fix PR is pending (avoid concurrent state confusion):
       ```
       gh pr list --label 'auto-fix:candidate' --state open
       ```
       Expected: empty list. If not, wait for in-flight PR to merge or close before starting UAT-01.

    5. Verify `.planning/sweep-03-04-pass-evidence.yaml` does NOT already contain a `uat_01:` row from a prior failed attempt:
       ```
       grep -c "^uat_01:" .planning/sweep-03-04-pass-evidence.yaml 2>/dev/null || echo "0"
       ```
       Expected: `0`. If `1`, the file already has prior evidence — operator decides whether to append a new row (e.g., uat_01_retry_a) or replace.

    Report: post-flight check status as a numbered list back to the operator before proceeding to Task 2.
  </how-to-verify>
  <resume-signal>Type "approved" once: (a) Phase 61 commit is on origin/main, (b) gh CLI is authenticated, (c) spend posture is chosen and credentials available, (d) no in-flight auto-fix PR, (e) no prior uat_01 row. Or describe any issue encountered.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Inject synthetic GOOGLE_DOM_DRIFT issue + observe full auto-fix loop</name>
  <what-built>The Phase 61 atomic bundle is now on origin/main. inject-defect.mjs is the updated mutator with the GOOGLE_DOM_DRIFT diagnostic block (DIAG-01). The llm-driver.js subscription transport is on --max-turns 5 --tools Read,Glob,Grep (TURNS-01). This task executes the live loop end-to-end and observes each gate.</what-built>
  <how-to-verify>
    1. Inject the synthetic issue (operator picks a stable seed for reproducibility — recommended: an ISO date like `47a-2026-06-09`):
       ```
       node tests/e2e/scripts/inject-defect.mjs --error-class GOOGLE_DOM_DRIFT --seed 47a-2026-06-09
       ```
       Capture stdout: the script prints the new issue URL + number. Record `ISSUE_ID`.

    2. Verify issue body contains the new diagnostic block (DIAG-01 verbatim selector + SOURCE_TAG preserved):
       ```
       gh issue view $ISSUE_ID --json body --jq '.body' | grep -E "patent-result|section\\[itemprop=\"claims\"\\]|main|article"
       gh issue view $ISSUE_ID --json body --jq '.body' | grep "fixture-mutator-uat-47b"
       ```
       Expected: both grep return matches. If either is empty, STOP — Phase 61 Plan 01 did not ship the DIAG-01 extension correctly.

    3. Watch for v40-auto-fix.yml workflow trigger:
       ```
       gh run list --workflow=v40-auto-fix.yml --limit 5
       ```
       Expected: a new run started within ~60s of issue creation. Record `RUN_ID`. Track to completion: `gh run watch $RUN_ID`.

    4. After the auto-fix workflow completes (success path), an auto-fix PR is opened. Identify it:
       ```
       gh pr list --label 'auto-fix:candidate' --state open --json number,title,headRefName
       ```
       Record `PR_NUMBER` and the branch name (expected pattern `auto-fix/<issue>-fp<hex>`).

    5. Watch verifier-gate workflow on the PR:
       ```
       gh pr checks $PR_NUMBER --watch
       ```
       Expected: `verifier-gate` check succeeds; `auto-fix:verified` label is applied automatically. If the gate fails, capture the workflow logs (`gh run view <gate-run-id> --log`) for diagnostic — this is the SWEEP-03 evidence we need to either confirm PASS (proceed to merge) or document FAIL (retry with different seed; do NOT proceed to Plan 05 until UAT-01 is PASS).

    6. Merge the PR via standard review path (NOT --admin per BYPASS-03 runbook):
       ```
       gh pr review $PR_NUMBER --approve
       gh pr merge $PR_NUMBER --squash --delete-branch
       ```
       (Or use the GitHub web UI — operator's call. The non-negotiable constraint is NO `--admin` flag.)

    7. Watch v40-auto-promote.yml workflow fire on the merge:
       ```
       gh run list --workflow=v40-auto-promote.yml --limit 3
       gh run watch <new-promote-run-id>
       ```
       Expected: promote workflow runs assertTripleGate → runPromote → appendLedgerEntry path.

    8. Capture the ledger entry written by promote. Pull the updated ledger from origin/main:
       ```
       git fetch origin main
       git show origin/main:tests/e2e/.llm-spend-ledger.json | jq '.iterations[-5:]'
       ```
       Inspect the last 5 entries. Find the entry matching this UAT: `source: 'auto-fix-promoted'`, `outcome: 'pass'`, `errorClass: 'GOOGLE_DOM_DRIFT'`. Record `LEDGER_ENTRY_ISO` (the entry's `iso` field) AND `TRANSPORT` (`sdk` or `subscription`).

       If no matching entry exists, STOP — promote path did not write the expected outcome entry. Investigate before proceeding to Task 3.

    Report all captured values back: ISSUE_ID, RUN_ID, PR_NUMBER, LEDGER_ENTRY_ISO, TRANSPORT, plus the seed used and any observations (e.g., "verifier-gate green on first attempt; PR merged at 14:23 UTC").
  </how-to-verify>
  <resume-signal>Type "uat-01-pass" once you have all 5 captured values and the ledger entry confirmed in tests/e2e/.llm-spend-ledger.json on origin/main. Or type "uat-01-fail" with a description of which gate failed (then this plan halts and the operator decides retry strategy or escalates to Phase 61 gap-closure).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Append uat_01 row to .planning/sweep-03-04-pass-evidence.yaml + commit</name>
  <files>.planning/sweep-03-04-pass-evidence.yaml</files>
  <read_first>
    - .planning/sweep-03-04-pass-evidence.yaml (if it exists from a prior Phase 61 run — verify schema_version + created_at_iso; otherwise create new with the schema in `<interfaces>` above)
    - The captured values reported by the operator at Task 2 close (ISSUE_ID, PR_NUMBER, LEDGER_ENTRY_ISO, TRANSPORT, seed, notes)
  </read_first>
  <action>
    Step 1 — If `.planning/sweep-03-04-pass-evidence.yaml` does NOT exist, CREATE it with the schema header:
    ```
    schema_version: 1
    created_at_iso: "<current ISO 8601 timestamp at file creation, e.g., 2026-06-09T14:30:00Z>"
    ```

    Step 2 — Append a `uat_01:` block AFTER the header (and after any existing rows). Use the values captured by the operator at Task 2 close. The exact YAML to append (substituting captured values for ALL placeholders):
    ```
    
    uat_01:
      passed_at_iso: "<ISO 8601 of evidence capture — i.e., now>"
      errorClass: "GOOGLE_DOM_DRIFT"
      outcome: "pass"
      source: "auto-fix-promoted"
      transport: "<sdk or subscription as observed>"
      issueId: <ISSUE_ID integer>
      prNumber: <PR_NUMBER integer>
      ledgerEntryIso: "<LEDGER_ENTRY_ISO captured at Task 2 step 8>"
      seed: "<seed used at Task 2 step 1>"
      notes: "<free-text observations: e.g., 'verifier-gate green first attempt; PR merged HH:MM UTC; transport=sdk via API key in repo secrets'>"
    ```

    Step 3 — Verify YAML well-formedness:
    ```
    node -e "const yaml = require('node:fs').readFileSync('.planning/sweep-03-04-pass-evidence.yaml','utf8'); console.log(yaml.length, 'bytes;', (yaml.match(/^uat_\\d+:/gm) || []).length, 'uat rows present');"
    ```

    Step 4 — Stage and commit:
    ```
    git add .planning/sweep-03-04-pass-evidence.yaml
    git commit -m "$(cat <<'EOF'
    docs(61): capture UAT-01 PASS evidence (SWEEP-03 GOOGLE_DOM_DRIFT)

    Live UAT-01 (SWEEP-03 / UAT-47-a) PASS on origin/main — closes v4.2
    architectural deferral. Synthetic GOOGLE_DOM_DRIFT issue flowed end-to-end
    through inject-defect → v40-auto-fix → verifier-gate → merge → promote
    with errorClass + outcome + source + transport ledger entry captured.

    Evidence sentinel .planning/sweep-03-04-pass-evidence.yaml created/extended
    as Phase 68 precondition (uat_cleanup.mjs refuses to run without it).

    Refs: UAT-01

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```

    Constraints:
    - Use real captured values — DO NOT use placeholders or fabricated values. If any captured value is missing, STOP and re-run Task 2 step 8 to retrieve it from origin/main ledger.
    - The YAML row keys must match the schema in `<interfaces>` byte-for-byte (Phase 68's cleanup script will parse this file by key)
    - DO NOT push in this task — operator decides push timing (likely batched with Plan 05's uat_02 row if both pass on the same day)
  </action>
  <verify>
    <automated>test -f .planning/sweep-03-04-pass-evidence.yaml && grep -c "^uat_01:" .planning/sweep-03-04-pass-evidence.yaml | grep -q "^1$" && grep -A 10 "^uat_01:" .planning/sweep-03-04-pass-evidence.yaml | grep -qE "passed_at_iso:.*[0-9]" && grep -A 10 "^uat_01:" .planning/sweep-03-04-pass-evidence.yaml | grep -qE "outcome:.*pass" && grep -A 10 "^uat_01:" .planning/sweep-03-04-pass-evidence.yaml | grep -qE "errorClass:.*GOOGLE_DOM_DRIFT" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/sweep-03-04-pass-evidence.yaml` exists with `schema_version: 1` header
    - `grep -c "^uat_01:" .planning/sweep-03-04-pass-evidence.yaml` returns exactly 1
    - The uat_01 block contains non-empty values for all 9 required keys (passed_at_iso, errorClass, outcome, source, transport, issueId, prNumber, ledgerEntryIso, seed)
    - `errorClass: "GOOGLE_DOM_DRIFT"` literal present
    - `outcome: "pass"` literal present
    - `source: "auto-fix-promoted"` literal present
    - `transport` is exactly one of `"sdk"` or `"subscription"` (no other value accepted)
    - issueId and prNumber are positive integers
    - `git log -1 --format='%s'` matches `docs(61): capture UAT-01 PASS evidence (SWEEP-03 GOOGLE_DOM_DRIFT)`
    - The commit touches ONLY `.planning/sweep-03-04-pass-evidence.yaml` (no FORBIDDEN_PATHS or unrelated files)
  </acceptance_criteria>
  <done>uat_01 PASS row appended to .planning/sweep-03-04-pass-evidence.yaml with all 9 required keys populated from live observation; committed locally; Phase 68 precondition sentinel now has the first of two required rows (uat_02 follows in Plan 05)</done>
</task>

</tasks>

<verification>
After all 3 tasks:
1. One GitHub issue (`triage` + `GOOGLE_DOM_DRIFT` labels) closed via auto-fix PR merge to origin/main.
2. One auto-fix PR closed with `auto-fix:verified` label applied by verifier-gate (NOT `--admin` bypassed).
3. One ledger entry in `tests/e2e/.llm-spend-ledger.json` on origin/main with `errorClass: 'GOOGLE_DOM_DRIFT'` + `outcome: 'pass'` + `source: 'auto-fix-promoted'` + `transport: 'sdk'|'subscription'`.
4. `.planning/sweep-03-04-pass-evidence.yaml` exists with one `uat_01:` block populated from real captured values.
5. A local commit `docs(61): capture UAT-01 PASS evidence` exists with the sentinel file change only.
6. Trust invariants preserved (no FORBIDDEN_PATHS edits during the live loop — verifier-gate would have rejected anyway).
</verification>

<success_criteria>
- UAT-01 (SWEEP-03 / UAT-47-a): PROVEN on origin/main with full ledger forensics (errorClass + outcome + source + transport)
- v4.2 architectural deferral closed (UAT-47-a STILL-DEFERRED → PASS captured)
- Phase 68 precondition sentinel exists with uat_01 row (uat_02 follows in Plan 05)
- BYPASS-03 discipline observed (no `--admin` bypass)
- Live spend recorded in committed ledger (probed against BUDG-01 cap in this phase's VERIFICATION.md footer)
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → gh CLI → GitHub API | Authenticated by GH_TOKEN or gh auth login; trust gate via repo scope; no token leakage in evidence sentinel (only issue/PR numbers + ISO timestamps recorded) |
| auto-fix workflow → Anthropic API | If SDK transport: ANTHROPIC_API_KEY from GitHub repo secrets crosses into the runner; never written to evidence sentinel (transport label only) |
| origin/main ledger → evidence sentinel | Cross-referenced via ledgerEntryIso field; evidence sentinel is the authoritative Phase 68 precondition record |
| merge gate → outcome ledger entry | assertTripleGate (verified-label + merged + triage-sourced) protects against `--admin` bypass contamination per BYPASS-03; the runbook explicitly forbids `--admin` here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-04-01 | Tampering | --admin bypass of verifier-gate | mitigate | Task 2 step 6 explicitly forbids `gh pr merge --admin`; BYPASS-03 runbook documented in STATE.md ## Bypass Conventions; if operator bypasses, Phase 62 audit-bypass-merges.mjs surfaces the bypass post-hoc |
| T-61-04-02 | Information Disclosure | API key leakage into evidence sentinel | mitigate | Evidence sentinel records `transport: "sdk"|"subscription"` label only; no API key or token fields; Task 3 acceptance criteria enforce schema purity |
| T-61-04-03 | Denial of Service | runaway API spend during live UAT | mitigate | TURNS-03 cost-bound regression + --max-budget-usd 0.50 per-call cap + ISSUE_HARD_CAP_USD $1 per-issue cap; expected spend ~$0.05-0.50 for one issue; pre-flight check at Task 1 step 3 confirms operator awareness |
| T-61-04-04 | Spoofing | synthetic issue mistaken for real triage backlog | mitigate | DIAG-01 SOURCE_TAG 'fixture-mutator-uat-47b' literal in body + DIAG-02 fixture-mutator title prefix; Task 2 step 2 verifies SOURCE_TAG present before proceeding; Phase 68 triple-tagged filter (title + body + label) closes this synthetic when ready |
| T-61-04-05 | Repudiation | who merged the auto-fix PR | accept | GitHub records merger identity in PR merge event; not recorded in evidence sentinel (operator identity is repo-scoped) |
| T-61-04-SC | Tampering | npm install / new dependency | mitigate | No install tasks in this plan; gh CLI assumed pre-installed (operator setup); pre-flight check confirms gh auth status. Package Legitimacy Audit not applicable per RESEARCH.md. |
</threat_model>

<output>
Create `.planning/phases/61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep/61-04-SUMMARY.md` when done. Include in the summary: captured ISSUE_ID, PR_NUMBER, LEDGER_ENTRY_ISO, TRANSPORT, seed, observed cost (operator best-estimate from ledger entry's cost_usd field), and a status flag indicating uat_01 evidence captured. Plan 05 can run in parallel with this plan after Task 1 (pre-flight) passes — operator may interleave depending on subscription credit / API key availability.
</output>
