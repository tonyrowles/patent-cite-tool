---
phase: 64-heuristic-first-triage-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/lib/triage-classifier.js
  - tests/unit/triage-classifier.test.js
  - tests/e2e/specs/fault-injection.spec.js
autonomous: true
requirements: [TRIAGE-01, TRIAGE-02, TRIAGE-03, TRIAGE-04]

must_haves:
  truths:
    - "runTriage resolves EXTENSION_NOT_LOADED heuristically (no LLM call) when iter.classification === 'EXTENSION_NOT_LOADED' OR iter.error_reason matches the locked regex"
    - "runTriage resolves GOOGLE_DOM_DRIFT heuristically ONLY when BOTH the Phase 61 mutator marker /<!-- fp: [0-9a-f]{12} -->/ AND a verbatim DOM-drift selector are present in iter.issue_body — real DOM drift (no mutator marker) STILL falls through to the LLM cluster pre-filter"
    - "runTriage resolves WORKER_FALLBACK_FAILED heuristically when iter.fault_injection_status?.worker_fallback_failed === true OR iter.classification === 'WORKER_FALLBACK_FAILED' (graceful degradation when the field is absent on legacy iter shape)"
    - "The set of distinct ERROR_CLASSES the classifier heuristic-resolves grows from exactly 7 (pre-Phase-64 baseline) to exactly 10 (post-Phase-64) — asserted by a single Vitest coverage assertion"
    - "The VERIFIER_STRONG_AGREEMENT arrow function body at triage-classifier.js:43-44 is BYTE-UNCHANGED across this commit (Phase 34 D-02 / Pitfall 2 mitigation preserved); Tier C verdicts NEVER short-circuit to heuristic"
    - "Rule 2 body (CONFIRMED + strong agreement, current lines 447-471) is BYTE-UNCHANGED across this commit"
    - "Cluster pre-filter sample-size invariant: a fixture of 10 same-category ambiguous findings (e.g. NO_CITATION_PRODUCED — a class NOT covered by any new rule) STILL produces exactly 1 grouped invokeLlm call (cluster path) — call count NOT decreased vs v4.2 baseline"
    - "All pre-existing triage-classifier.test.js tests stay green; no test deletions, only additions"
  artifacts:
    - path: "tests/e2e/lib/triage-classifier.js"
      provides: "3 new heuristic rules inserted between current line 501 (Rule 3 close) and current line 503 (ambiguous.push(iter) — Rule 4 head)"
      contains: "iter.triage_confidence = 'heuristic'"
    - path: "tests/e2e/lib/triage-classifier.js"
      provides: "3 new heuristic rules inserted between current line 501 (Rule 3 close) and current line 503 (ambiguous.push(iter) — Rule 4 head)"
      contains: "iter.triage_confidence = 'heuristic_mutator_aware'"
    - path: "tests/e2e/lib/triage-classifier.js"
      provides: "3 new heuristic rules inserted between current line 501 (Rule 3 close) and current line 503 (ambiguous.push(iter) — Rule 4 head)"
      contains: "iter.triage_confidence = 'heuristic_fault_injection'"
    - path: "tests/unit/triage-classifier.test.js"
      provides: "Vitest cases for each new rule + mutator-aware vs real-drift split + Tier-C-no-mask guard + cluster sample-size invariant + 7→10 coverage assertion + VERIFIER_STRONG_AGREEMENT body byte-stability + Rule 2 body byte-stability"
      contains: "T_TIER_C_NO_MASK"
    - path: "tests/unit/triage-classifier.test.js"
      provides: "Coverage growth assertion"
      contains: "HEURISTIC_RESOLVABLE_CLASSES"
  key_links:
    - from: "tests/e2e/lib/triage-classifier.js — new Rule 5 (EXTENSION_NOT_LOADED)"
      to: "tests/e2e/lib/error-codes.js:51 EXTENSION_NOT_LOADED constant"
      via: "string literal match — no import added (existing iter.classification is already the string)"
      pattern: "iter\\.classification === 'EXTENSION_NOT_LOADED'"
    - from: "tests/e2e/lib/triage-classifier.js — new Rule 6 (GOOGLE_DOM_DRIFT mutator-aware)"
      to: "Phase 61 mutator marker shape <!-- fp: <12-hex> -->"
      via: "anchored regex on iter.issue_body"
      pattern: "<!-- fp: \\[0-9a-f\\]\\{12\\} -->"
    - from: "tests/e2e/lib/triage-classifier.js — new Rule 7 (WORKER_FALLBACK_FAILED)"
      to: "tests/e2e/specs/fault-injection.spec.js — fault_injection_status field emission"
      via: "additive iter shape field consumption (graceful degradation when absent)"
      pattern: "iter\\.fault_injection_status\\?\\.worker_fallback_failed === true"
---

<objective>
Extend `tests/e2e/lib/triage-classifier.js:runTriage` D-03 rule chain with 3 new heuristic rules — EXTENSION_NOT_LOADED, GOOGLE_DOM_DRIFT (mutator-aware), and WORKER_FALLBACK_FAILED — pushing heuristic-resolvable ERROR_CLASS coverage from 7 to 10. The Phase 34 D-02 Tier-C-masking guard (`VERIFIER_STRONG_AGREEMENT` body at line 43, Rule 2 body at lines 447-471) is preserved byte-for-byte; the cluster pre-filter sample-size invariant holds; producer-site co-design extends the fault-injection spec to emit `fault_injection_status` so the heuristic has data to consume in CI.

Purpose: Reduce the number of ambiguous iterations routed to the LLM second-pass (cost discipline) without weakening the Pitfall 2 mitigation that keeps Tier C disagreements escalating to LLM judgment. New rules are inserted between Rule 3 close (current line 501) and Rule 4 head (current line 503 — the `ambiguous.push(iter)` line) as pure-function "ambiguous-class deflectors."

Output:
- `tests/e2e/lib/triage-classifier.js` extended with 3 new rules (no removals; no edits to the VERIFIER_STRONG_AGREEMENT arrow, Rule 1, Rule 2, Rule 3, or the cluster pre-filter block)
- `tests/unit/triage-classifier.test.js` extended with Vitest cases pinning each new rule, the Tier-C-no-mask guard, the cluster sample-size invariant, the 7→10 coverage assertion, and the byte-stability of `VERIFIER_STRONG_AGREEMENT` + Rule 2
- `tests/e2e/specs/fault-injection.spec.js` extended with additive `fault_injection_status` emission on the `appendCase` call so the TRIAGE-03 heuristic has data to consume in CI
- One atomic commit shipping all three file changes
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-heuristic-first-triage-extension/64-CONTEXT.md
@.planning/research/PITFALLS.md
@tests/e2e/lib/triage-classifier.js
@tests/e2e/lib/error-codes.js
@tests/unit/triage-classifier.test.js
@tests/e2e/specs/fault-injection.spec.js

<interfaces>
<!-- Key types and call sites the executor needs. Extracted from codebase. Use these directly — no exploration required. -->

From `tests/e2e/lib/triage-classifier.js` (920 LOC):
- Line 43 (LOAD-BEARING — body BYTE-UNCHANGED):
  `export const VERIFIER_STRONG_AGREEMENT = ({ status, tier_used } = {}) =>`
  `  status === 'pass' && (tier_used === 'A' || tier_used === 'B');`
- Line 400 — `export async function runTriage({ inputLlmReport, inputRerunReport, invokeLlm, writeReport, now, sourcePaths })`
- Lines 427-444 — Rule 1 (FLAKE short-circuit). Pattern: `report.findings.push({ ... path_taken: 'heuristic' }); continue;`
- Lines 447-471 — Rule 2 (CONFIRMED + VERIFIER_STRONG_AGREEMENT + classification in RULE2_SEVERITY). BODY BYTE-UNCHANGED.
- Lines 473-499 — Rule 3 (NOT_REPLAYABLE + LLM_HALLUCINATED_SELECTION/LLM_API_ERROR/HARNESS_ERROR/PASS). BODY BYTE-UNCHANGED.
- **INSERTION POINT: AFTER line 501 (Rule 3's closing `}` + blank line), BEFORE line 503 `ambiguous.push(iter);` (Rule 4).**
- Lines 506-561 — D-11 cluster pre-filter block. BYTE-UNCHANGED.
- Existing `path_taken: 'heuristic'` and `triage_confidence` fields on findings: Rules 1-3 set `path_taken: 'heuristic'` on the pushed finding. The 3 new rules MUST do the same (push a finding with `path_taken: 'heuristic'`) AND set `triage_confidence` to one of `'heuristic'` / `'heuristic_mutator_aware'` / `'heuristic_fault_injection'` per CONTEXT.md D-03.

From `tests/e2e/lib/error-codes.js`:
- Line 51: `export const EXTENSION_NOT_LOADED = 'EXTENSION_NOT_LOADED';`
- Line 56: `export const GOOGLE_DOM_DRIFT = 'GOOGLE_DOM_DRIFT';`
- Line 64: `export const WORKER_FALLBACK_FAILED = 'WORKER_FALLBACK_FAILED';`
- All three are already in `ERROR_CLASSES` frozen array (lines 98-110). Plan does NOT modify error-codes.js.

From `tests/unit/triage-classifier.test.js` (1931 LOC):
- Existing factory `makeIteration(overrides)` (line 89) — extend overrides for `error_reason`, `issue_body`, `fault_injection_status` per new test cases (these are new optional fields on iter; no schema change required since the existing runTriage code only reads what each rule looks at).
- Existing factory `makeRerunEntry(overrides)` (line 121).
- Existing helper `makeMockInvokeLlm()` (line 183) — used for callCount assertions.
- The existing 22 "Phase 34" test cases (lines 287-820) are preserved BYTE-IDENTICAL.
- Insertion point for new describe blocks: after the existing `describe('runTriage — Tier C escalation ...')` block (line 582 onward) and before the `describe('runTriage — triage-report.json schema ...')` block (line 636). All new tests live in NEW describe blocks; no existing block is edited.

From `tests/e2e/specs/fault-injection.spec.js` (198 LOC):
- Line 49: `const CASE_ID = 'US11427642-spec-short-1';`
- Line 68: `let errorClass = null;`
- Line 162-164: catch block sets `caseStatus = 'failed'; errorClass = WORKER_FALLBACK_FAILED;`
- Line 173-182: `finally` block calls `appendCase(REPORT_PATH, { id, status, errorClass, citation, verifier_verdict, artifacts, duration_ms })`.
- **Producer co-design:** the spec must additively emit `fault_injection_status: { worker_fallback_failed: true }` on the failure path, and `fault_injection_status: { worker_fallback_failed: false }` on the pass path. Inserted as a new property of the appendCase payload (additive — `appendCase` in `tests/e2e/lib/report.js:136` does a read-modify-write of the case entry and is tolerant of unknown extra fields).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 3 new heuristic rules to runTriage (CONTEXT D-03 insertion point) + producer co-design in fault-injection spec</name>
  <files>tests/e2e/lib/triage-classifier.js, tests/e2e/specs/fault-injection.spec.js</files>
  <behavior>
    Tests below ship in Task 2; this <behavior> block documents the expected post-edit behavior so the executor writes code against fixed contracts.
    - Rule 5 (EXTENSION_NOT_LOADED — per CONTEXT D-03 TRIAGE-01): when `iter.classification === 'EXTENSION_NOT_LOADED'` OR `/extension (?:not.*loaded|failed.*attach)/i.test(iter.error_reason ?? '')`, push a finding with `category: 'EXTENSION_NOT_LOADED'`, `severity: 'medium'`, `path_taken: 'heuristic'`, `triage_confidence: 'heuristic'`, `confidence: 0.85` (per Pitfall 6 confidence-ceiling guidance for rules WITHOUT a VERIFIER_STRONG_AGREEMENT gate), `root_cause_hypothesis: 'Extension failed to attach to the page'`, rationale describing the trigger (classification match vs error_reason regex match), then `continue`.
    - Rule 6 (GOOGLE_DOM_DRIFT mutator-aware — per CONTEXT D-03 TRIAGE-02): compute `const hasMutatorMarker = /<!-- fp: [0-9a-f]{12} -->/.test(iter.issue_body ?? '');` and `const hasDomDriftSelector = /(?:patent-result|section\[itemprop="claims"\]|\bmain\b|\barticle\b)/.test(iter.issue_body ?? '');` and `const isMutatorInjected = hasMutatorMarker && hasDomDriftSelector;`. When `iter.classification === 'GOOGLE_DOM_DRIFT' && isMutatorInjected`, push a finding with `category: 'GOOGLE_DOM_DRIFT'`, `severity: 'medium'`, `path_taken: 'heuristic'`, `triage_confidence: 'heuristic_mutator_aware'`, `confidence: 0.85`, `root_cause_hypothesis: 'Mutator-injected synthetic DOM drift (Phase 61 DIAG-01 marker present)'`, rationale citing both the marker and the selector match, then `continue`. When `iter.classification === 'GOOGLE_DOM_DRIFT' && !isMutatorInjected` (real DOM drift), DO NOT push; fall through to Rule 4 (ambiguous).
    - Rule 7 (WORKER_FALLBACK_FAILED — per CONTEXT D-03 TRIAGE-03): when `iter.fault_injection_status?.worker_fallback_failed === true` OR `iter.classification === 'WORKER_FALLBACK_FAILED'`, push a finding with `category: 'WORKER_FALLBACK_FAILED'`, `severity: 'medium'`, `path_taken: 'heuristic'`, `triage_confidence: 'heuristic_fault_injection'`, `confidence: 0.85`, `root_cause_hypothesis: 'Phase 30 fault-injection spec failed: Worker/USPTO fallback did not produce accurate citation'`, rationale citing whether the trigger was the fault_injection_status field or the classification, then `continue`. When `iter.fault_injection_status` is absent (undefined), the rule is a no-op for that branch (`?.` operator returns undefined, `=== true` is false) — graceful degradation as specified in CONTEXT.md.
    - Rule ordering: insert Rule 5 → Rule 6 → Rule 7 between current line 501 and current line 503. All 3 rules are pure-function "ambiguous-class deflectors" — they MUST NOT touch FLAKE/strong-agreement paths (Rules 1/2/3) and MUST NOT gate on `rerunEntry?.verdict === 'CONFIRMED'` (Pitfall 6 guard — only the existing Rule 2 may CONFIRMED-gate, and only with VERIFIER_STRONG_AGREEMENT).
    - Fault-injection producer co-design (TRIAGE-03 conditional): in `tests/e2e/specs/fault-injection.spec.js`, extend the `appendCase` payload (lines 173-182) to additively include `fault_injection_status: { worker_fallback_failed: caseStatus === 'failed' }`. This is a single-line additive change inside the existing object literal — no new imports, no shape change to existing fields.
  </behavior>
  <action>
    Edit `tests/e2e/lib/triage-classifier.js`. Locate the closing `}` of Rule 3 (the existing `if (NOT_REPLAYABLE_EFFECTIVE && RULE3_CLASSIFICATIONS.includes(iter.classification)) { ... continue; }` block, which currently ends at line 499). The blank-line separator at line 500 and the comment at line 501 (`// D-03 Rule 4: ambiguous — Tier C CONFIRMED or any other case not matched above`) are existing — your insertion point is BEFORE the Rule 4 comment block, immediately after Rule 3's closing brace. Insert 3 new rule blocks per the contracts in <behavior> above. Each new rule follows the existing pattern: a comment header `// D-03 Rule N: <name> (Phase 64 TRIAGE-0X)`, the condition, a single `report.findings.push({ ... })` setting `iteration_n, severity, category, root_cause_hypothesis, confidence, rationale, path_taken: 'heuristic'` PLUS the new `triage_confidence` field, then `continue;`. Renumber the Rule 4 comment from "Rule 4" to "Rule 7" in your inserted comments (do NOT edit the existing Rule 4 line — it currently says "Rule 4: ambiguous" and stays byte-for-byte; the executor's three new rule comments are Rules 5/6/7 — Rule 4 ambiguous-fallthrough remains the LAST rule in the chain). DO NOT touch the `VERIFIER_STRONG_AGREEMENT` arrow at line 43-44. DO NOT touch Rule 1 (FLAKE) at lines 433-445. DO NOT touch Rule 2 (CONFIRMED + strong) at lines 447-471. DO NOT touch Rule 3 (NOT_REPLAYABLE + specific) at lines 473-499. DO NOT touch the cluster pre-filter block at lines 506-561. DO NOT add imports — error-codes.js constants are NOT needed since the rules compare against string literals (matching the existing pattern in `RULE3_CLASSIFICATIONS` at line 475). Then edit `tests/e2e/specs/fault-injection.spec.js`: extend the `appendCase` call payload (currently at lines 174-181) to additively include a `fault_injection_status: { worker_fallback_failed: caseStatus === 'failed' }` property. Place it AFTER `verifier_verdict` and BEFORE `artifacts` — one new line inside the object literal. No other edits to that file.
  </action>
  <verify>
    <automated>
node -e "const fs=require('fs'); const src=fs.readFileSync('tests/e2e/lib/triage-classifier.js','utf8'); const lines=src.split('\n'); const non=lines.filter(l => !/^\s*(\/\/|\*|\/\*)/.test(l) && l.trim().length).join('\n'); /* Rule 5/6/7 markers */ if (!/triage_confidence: 'heuristic'/.test(non)) throw new Error('Rule 5 marker missing'); if (!/triage_confidence: 'heuristic_mutator_aware'/.test(non)) throw new Error('Rule 6 marker missing'); if (!/triage_confidence: 'heuristic_fault_injection'/.test(non)) throw new Error('Rule 7 marker missing'); /* Verify VERIFIER_STRONG_AGREEMENT arrow body byte-unchanged */ const v=src.match(/export const VERIFIER_STRONG_AGREEMENT = \(\{ status, tier_used \} = \{\}\) =>\n  status === 'pass' && \(tier_used === 'A' \|\| tier_used === 'B'\);/); if (!v) throw new Error('VERIFIER_STRONG_AGREEMENT body byte-changed'); /* Verify Rule 2 body byte-unchanged — extract from RULE2_SEVERITY to the trailing continue */ const r2=src.match(/const RULE2_SEVERITY = \{ WRONG_CITATION: 'high', VERIFIER_DISAGREE: 'medium' \};[\s\S]*?path_taken: 'heuristic',\n      \}\);\n      continue;/); if (!r2) throw new Error('Rule 2 body byte-changed'); console.log('OK: rules inserted; VERIFIER_STRONG_AGREEMENT + Rule 2 body byte-unchanged');"
node -e "const fs=require('fs'); const s=fs.readFileSync('tests/e2e/specs/fault-injection.spec.js','utf8'); if (!/fault_injection_status: \{ worker_fallback_failed: caseStatus === 'failed' \}/.test(s)) throw new Error('producer co-design missing'); console.log('OK: fault-injection producer wired');"
    </automated>
  </verify>
  <done>
    Three new rule blocks inserted at the correct location in `triage-classifier.js`; VERIFIER_STRONG_AGREEMENT arrow body + Rule 2 body grep-verified byte-unchanged; `tests/e2e/specs/fault-injection.spec.js` `appendCase` payload additively includes `fault_injection_status`. No other file modifications. ESLint clean on both files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest pins for all 3 new rules + Tier-C-no-mask + cluster sample-size invariant + 7→10 coverage assertion + byte-stability source-greps</name>
  <files>tests/unit/triage-classifier.test.js</files>
  <behavior>
    New describe blocks appended after the existing `describe('runTriage — Tier C escalation ...')` block (which currently ends around line 633) and before `describe('runTriage — triage-report.json schema ...')` (line 636). The new blocks are isolated — no edits to any existing test. Required pins:

    1. `describe('runTriage — Phase 64 EXTENSION_NOT_LOADED heuristic (TRIAGE-01)')` — 2 tests:
       - Rule 5a: `iter.classification === 'EXTENSION_NOT_LOADED'` → heuristic resolution; invokeLlm callCount === 0; finding.severity === 'medium'; finding.category === 'EXTENSION_NOT_LOADED'; finding.path_taken === 'heuristic'; written report's finding contains `triage_confidence: 'heuristic'`.
       - Rule 5b: `iter.error_reason` matches `/extension.*(not.*loaded|failed.*attach)/i` (with `iter.classification` set to a non-matching class like `HARNESS_ERROR` so Rule 3 does not fire FIRST — use a CONFIRMED rerun verdict so Rule 3's NOT_REPLAYABLE-or-missing guard fails) → heuristic resolution; same shape as Rule 5a.

    2. `describe('runTriage — Phase 64 GOOGLE_DOM_DRIFT mutator-aware heuristic (TRIAGE-02)')` — 3 tests:
       - Rule 6a: mutator marker present (`<!-- fp: abc123def456 -->`) AND selector `'patent-result'` present in `iter.issue_body` AND `iter.classification === 'GOOGLE_DOM_DRIFT'` → heuristic resolution; invokeLlm callCount === 0; `triage_confidence: 'heuristic_mutator_aware'`.
       - Rule 6b (real DOM drift — no marker): `iter.classification === 'GOOGLE_DOM_DRIFT'`, `iter.issue_body` contains the selector but NO mutator marker → finding goes to ambiguous; invokeLlm callCount > 0 OR finding ends up with `path_taken: 'llm_single_parse_error'` or similar (i.e., NOT `path_taken: 'heuristic'`). The point is the heuristic does NOT fire — real drift escalates.
       - Rule 6c (marker present, selector absent): `iter.issue_body` contains the marker but no recognized selector → heuristic does NOT fire (defensive; the marker alone is not sufficient).

    3. `describe('runTriage — Phase 64 WORKER_FALLBACK_FAILED heuristic (TRIAGE-03)')` — 3 tests:
       - Rule 7a: `iter.fault_injection_status = { worker_fallback_failed: true }` → heuristic resolution; invokeLlm callCount === 0; `triage_confidence: 'heuristic_fault_injection'`.
       - Rule 7b: `iter.classification === 'WORKER_FALLBACK_FAILED'` (with `fault_injection_status` absent) → heuristic resolution.
       - Rule 7c (graceful degradation): `iter.fault_injection_status === undefined` AND `iter.classification` is a class NOT covered by any rule (e.g., `NO_CITATION_PRODUCED`) AND rerun verdict CONFIRMED with Tier A verifier verdict but classification not in RULE2_SEVERITY → finding falls through to ambiguous (Rule 4); confirms the new rule is a no-op when its data is absent.

    4. `describe('runTriage — Phase 64 TRIAGE-04 invariants (Pitfall 2 + Pitfall 10)')` — 4 tests:
       - `T_TIER_C_NO_MASK`: build an iter with `classification: 'GOOGLE_DOM_DRIFT'`, `verifier_verdict: { status: 'pass', tier_used: 'C' }`, rerun CONFIRMED, AND mutator marker present. Assert the new Rule 6 fires (heuristic — because Rule 6 does NOT gate on verifier_verdict, it gates on the mutator marker). Then build a SECOND iter with `classification: 'VERIFIER_DISAGREE'`, `verifier_verdict: { status: 'pass', tier_used: 'C' }`, rerun CONFIRMED, NO mutator marker. Assert this iter goes to ambiguous (Rule 2 cannot fire because VERIFIER_STRONG_AGREEMENT returns false for Tier C; no new rule applies). The TWO assertions together prove Tier C remains escalated except in the mutator-aware deliberate-bypass case (which has its OWN justification: the mutator is a synthetic test, not a real verifier disagreement).
       - `T_VSA_BODY_UNCHANGED`: source-grep `tests/e2e/lib/triage-classifier.js` for the literal multi-line declaration `export const VERIFIER_STRONG_AGREEMENT = ({ status, tier_used } = {}) =>\n  status === 'pass' && (tier_used === 'A' || tier_used === 'B');` via a single regex; assert match. (NOT a sha256 pin — a literal-line regex pin matching the orchestrator's "byte-unchanged" requirement.)
       - `T_RULE2_BODY_UNCHANGED`: source-grep for the Rule 2 body — anchor on `const RULE2_SEVERITY = { WRONG_CITATION: 'high', VERIFIER_DISAGREE: 'medium' };` through `path_taken: 'heuristic',\n      });\n      continue;` of Rule 2; assert match.
       - `T_NEW_RULES_NO_CONFIRMED_GATE_WITHOUT_VSA` (Pitfall 6 source-grep): the source text of the file, AFTER stripping comment-only lines (`grep -v '^\s*//' | grep -v '^\s*/\*' | grep -v '^\s*\*'`), must NOT contain any non-comment line that simultaneously matches BOTH `rerunEntry?.verdict === 'CONFIRMED'` AND lacks `VERIFIER_STRONG_AGREEMENT` in the same `if (...)` condition for the new rules. Implementation: extract each `if (...)` block; the only `CONFIRMED`-gated block in the file is the existing Rule 2, which DOES gate on `VERIFIER_STRONG_AGREEMENT`. Assert count of `CONFIRMED`-gated `if` conditions === 1.

    5. `describe('runTriage — Phase 64 cluster pre-filter sample-size invariant (TRIAGE-04)')` — 1 test:
       - Build 10 iters all with `classification: 'NO_CITATION_PRODUCED'` (a class NOT covered by any new rule — confirms the new rules do not steal ambiguous candidates from the cluster pre-filter), Tier C verifier verdicts, rerun CONFIRMED. Pass a `makeMockInvokeLlm` spy. Assert the spy is called EXACTLY 1 time (cluster path — because 10 ≥ CLUSTER_THRESHOLD of 5 same-category ambiguous). This pins the v4.2 behavior: 10 same-category ambiguous findings still produce exactly 1 grouped LLM call after Phase 64.

    6. `describe('runTriage — Phase 64 coverage assertion (7 → 10 heuristic-resolvable classes)')` — 1 test:
       - Define a frozen test fixture `HEURISTIC_RESOLVABLE_CLASSES = Object.freeze(['FLAKE','WRONG_CITATION','VERIFIER_DISAGREE','LLM_HALLUCINATED_SELECTION','LLM_API_ERROR','HARNESS_ERROR','PASS','EXTENSION_NOT_LOADED','GOOGLE_DOM_DRIFT','WORKER_FALLBACK_FAILED'])` (length 10). Pre-Phase-64 baseline was 7 (FLAKE + WRONG_CITATION + VERIFIER_DISAGREE + LLM_HALLUCINATED_SELECTION + LLM_API_ERROR + HARNESS_ERROR + PASS); Phase 64 adds 3 (EXTENSION_NOT_LOADED + GOOGLE_DOM_DRIFT + WORKER_FALLBACK_FAILED) → 10. Assert `HEURISTIC_RESOLVABLE_CLASSES.length === 10`. Then run runTriage with one iter per class (using factories that trigger each rule), collect distinct `category` values where `path_taken === 'heuristic'`, assert `new Set(categories).size === 10` AND `new Set(categories)` equals `new Set(HEURISTIC_RESOLVABLE_CLASSES)`.
  </behavior>
  <action>
    Append new describe blocks to `tests/unit/triage-classifier.test.js` between the existing Tier-C-escalation describe (currently ending around line 633) and the schema-test describe at line 636. Reuse the existing `makeIteration`, `makeRerunEntry`, `makeLlmReport`, `makeRerunReport`, `makeMockInvokeLlm`, `makeWriteReport` factories. For test cases that need new optional fields (`error_reason`, `issue_body`, `fault_injection_status`), pass them via the `overrides` argument to `makeIteration` — these become new properties on the returned iter object without changing the existing factory body. For the 7→10 coverage assertion test, the executor iterates the `HEURISTIC_RESOLVABLE_CLASSES` array, constructs one iter per class with the minimal triggering shape (e.g., FLAKE → rerun verdict FLAKE; LLM_HALLUCINATED_SELECTION → rerun NOT_REPLAYABLE + classification; EXTENSION_NOT_LOADED → classification match; GOOGLE_DOM_DRIFT → classification + issue_body with mutator marker AND selector; etc.), feeds them all into one runTriage call, and asserts the resulting distinct heuristic categories. DO NOT edit any existing test or factory. DO NOT modify import lines (the existing import from `'../e2e/lib/triage-classifier.js'` already pulls `runTriage`, `VERIFIER_STRONG_AGREEMENT`, `CLUSTER_THRESHOLD` — all sufficient). Add `import fs from 'node:fs'` and `import path from 'node:path'` if not already in scope — they ARE already imported (lines 38-40).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/triage-classifier.test.js --reporter=basic 2>&1 | tail -40</automated>
  </verify>
  <done>
    All new describe blocks pass; all pre-existing triage-classifier tests still pass (zero regressions); the test file has zero deletions vs HEAD (verify via `git diff --numstat tests/unit/triage-classifier.test.js | awk '{print $2}'` showing 0 lines deleted); `npx vitest run tests/unit/triage-classifier.test.js` returns exit 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iteration-shape consumer → runTriage rule chain | iter.classification / iter.issue_body / iter.fault_injection_status / iter.error_reason are inputs that the new rules MAY heuristically resolve; trust boundary is the same as the pre-Phase-64 Rule 1/2/3 inputs |
| Heuristic resolution path → LLM second-pass | new rules pull iters OUT of the ambiguous bucket; Phase 34 D-02 invariant requires Tier C verdicts to ALWAYS escalate via LLM cluster pre-filter |
| Mutator-injected synthetic content → triage decision | Phase 61 mutator marker `<!-- fp: <12-hex> -->` is the *only* signal that distinguishes synthetic GOOGLE_DOM_DRIFT from real drift; if the marker shape changes or is forgeable, the new Rule 6 heuristic mask real drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01 | Tampering | tests/e2e/lib/triage-classifier.js — VERIFIER_STRONG_AGREEMENT body at line 43-44 | mitigate | Vitest pin `T_VSA_BODY_UNCHANGED` (Task 2) source-greps the literal multi-line declaration; PITFALLS Pitfall 10 enforces byte-stability across every v4.3 phase |
| T-64-02 | Tampering | tests/e2e/lib/triage-classifier.js — Rule 2 body (lines 447-471) | mitigate | Vitest pin `T_RULE2_BODY_UNCHANGED` (Task 2) source-greps the literal Rule 2 block — covers the Pitfall 6 "Tier C masking via new rule that forgets VERIFIER_STRONG_AGREEMENT" hazard by anchoring on the existing correct gate |
| T-64-03 | Elevation of Privilege | runTriage Rule 5/6/7 inserts | mitigate | Vitest pin `T_TIER_C_NO_MASK` (Task 2) constructs Tier-C inputs that prior to Phase 64 escalated to LLM, asserts they STILL escalate (or in the mutator-aware case, fire by the documented marker rule — not by accident); pin `T_NEW_RULES_NO_CONFIRMED_GATE_WITHOUT_VSA` source-greps the file for any `CONFIRMED`-gated `if` lacking a VERIFIER_STRONG_AGREEMENT call, count must remain 1 (existing Rule 2) |
| T-64-04 | Denial of Service | Cluster pre-filter call-count regression | mitigate | Vitest pin `cluster sample-size invariant` (Task 2) constructs 10 same-category NO_CITATION_PRODUCED ambiguous iters, asserts invokeLlm called exactly 1 time — confirms cluster pre-filter still groups (cluster call count NOT decreased vs v4.2 baseline) |
| T-64-05 | Spoofing | Forgeable mutator marker → real DOM drift heuristically resolved as synthetic | accept | The mutator marker is `<!-- fp: [0-9a-f]{12} -->` — a 12-hex fingerprint. A real Google Patents DOM drift would have to include a literal HTML comment matching this exact shape AND one of four DOM selectors. PITFALLS Pitfall 2 documents this as ASSUMED-safe because the marker is emitted by the Phase 61 mutator only inside the auto-fix loop's synthetic issue bodies — not by Google Patents production HTML. Trust boundary acknowledged; mitigation deferred to v4.4+ if observed in practice. |
| T-64-06 | Information Disclosure | New triage_confidence field exposes mutator-vs-real distinction in downstream forensic queries | accept | The distinct labels `'heuristic'`/`'heuristic_mutator_aware'`/`'heuristic_fault_injection'` are intentional stratification for A/B winner / weekly digest queries (per CONTEXT.md specifics). No PII; field is in the triage-report.json output which already contains rerun verdicts and verifier verdicts. |

(Package-legitimacy gate skipped: this plan adds no npm/pip/cargo installs.)
</threat_model>

<verification>

## Phase 64 Plan 01 — Overall Checks

1. `git diff --stat tests/e2e/lib/triage-classifier.js` shows additions only (zero deletions; the 3 new rules are pure inserts between current line 501 and current line 503). Verify via `git diff --numstat tests/e2e/lib/triage-classifier.js | awk '{print $2}'` → 0.
2. `grep -c "triage_confidence:" tests/e2e/lib/triage-classifier.js` (non-comment) → exactly 3 (one per new rule).
3. `grep -c "rerunEntry?.verdict === 'CONFIRMED'" tests/e2e/lib/triage-classifier.js` (non-comment, comment-stripped via `grep -v '^\s*//'`) → exactly 1 (existing Rule 2 only; pins Pitfall 6 source-grep).
4. `npx vitest run tests/unit/triage-classifier.test.js` exits 0 — all pre-existing 22+ Phase 34 tests PLUS the new Phase 64 tests pass.
5. `git diff --numstat tests/unit/triage-classifier.test.js | awk '{print $2}'` → 0 (zero deletions from the test file; new tests are pure additions).
6. `git diff tests/e2e/specs/fault-injection.spec.js` shows exactly one new property line in the `appendCase` call payload (`fault_injection_status: { worker_fallback_failed: caseStatus === 'failed' },`). No other edits.
7. Coverage assertion: the `HEURISTIC_RESOLVABLE_CLASSES.length === 10` test passes AND the Set-equality check passes — proving 7→10 growth.
8. Cluster sample-size invariant: the 10-iter NO_CITATION_PRODUCED test passes with invokeLlm callCount === 1 (cluster path, not single path).
9. ESLint clean across all three edited files: `npx eslint tests/e2e/lib/triage-classifier.js tests/unit/triage-classifier.test.js tests/e2e/specs/fault-injection.spec.js` exits 0.

</verification>

<success_criteria>

Plan complete when:

- [ ] 3 new rules inserted in `triage-classifier.js` between current line 501 (Rule 3 close) and current line 503 (Rule 4 head — `ambiguous.push(iter);`); each rule sets `path_taken: 'heuristic'` AND a distinct `triage_confidence` label
- [ ] `VERIFIER_STRONG_AGREEMENT` arrow body at line 43-44 BYTE-UNCHANGED (Vitest pin + source-grep verification)
- [ ] Rule 2 body (current lines 447-471) BYTE-UNCHANGED (Vitest pin)
- [ ] All pre-existing Phase 34 + Phase 45 tests in `tests/unit/triage-classifier.test.js` stay green; no test deletions
- [ ] New Vitest cases pass: per-rule resolution (Rules 5a/5b/6a/6b/6c/7a/7b/7c), `T_TIER_C_NO_MASK`, `T_VSA_BODY_UNCHANGED`, `T_RULE2_BODY_UNCHANGED`, `T_NEW_RULES_NO_CONFIRMED_GATE_WITHOUT_VSA`, cluster sample-size invariant (10 NO_CITATION_PRODUCED → 1 cluster call), 7→10 coverage assertion
- [ ] `tests/e2e/specs/fault-injection.spec.js` appendCase payload additively emits `fault_injection_status: { worker_fallback_failed: <bool> }`
- [ ] ESLint clean on all 3 modified files
- [ ] ONE atomic commit shipping all 3 file changes — subject: `feat(64): heuristic-first triage extension — EXTENSION_NOT_LOADED + GOOGLE_DOM_DRIFT mutator-aware + WORKER_FALLBACK_FAILED (TRIAGE-01..04)`
- [ ] `git diff` on `auto-fix-promote.mjs` shows ZERO changes (Pitfall 10 — assertTripleGate body untouched even incidentally)

</success_criteria>

<output>
Create `.planning/phases/64-heuristic-first-triage-extension/64-01-SUMMARY.md` when done. SUMMARY records:
- Insertion point used (post-line-501 confirmation)
- Exact line offsets for the 3 new rule starts in the post-edit file
- VERIFIER_STRONG_AGREEMENT line 43-44 body verification result
- Rule 2 body byte-stability verification result
- Pre-Phase-64 baseline heuristic-resolvable count (7) vs post-Phase-64 (10) test fixture
- Cluster sample-size invariant test result (1 grouped call for 10 same-category NO_CITATION_PRODUCED)
- Total Vitest count delta (pre vs post)
- Single commit hash
- Any deviation from the planned insertion location with rationale
</output>
