---
phase: 65-expanded-fix-scaffolds
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/lib/fix-prompt-builder.js
  - tests/e2e/lib/error-codes.js
  - tests/e2e/scripts/inject-defect.mjs
  - tests/e2e/lib/llm-router.js
  - .github/workflows/v40-pdfjs-frame-shift.yml
  - .github/workflows/v40-auto-fix.yml
  - tests/unit/fix-prompt-builder.test.js
autonomous: true
requirements:
  - SCAF-01
  - SCAF-02

must_haves:
  truths:
    - "buildFixPrompt({errorClass:'VERIFIER_DISAGREE', issueBody:'<x>'}) returns {ok:true, systemPrompt, userPrompt, model} — no longer unsupported-class"
    - "buildFixPrompt({errorClass:'FRAME_SHIFT_DETECTED', issueBody:'<x>'}) returns {ok:true, systemPrompt, userPrompt, model}"
    - "ERROR_CLASSES array contains 'FRAME_SHIFT_DETECTED' as last (newest) entry; order of pre-existing entries is byte-unchanged"
    - "VERIFIER_DISAGREE_CONTRACT references the Phase 35 Verifier Disagreement template headers (Expected citation / Observed citation / Verifier tier / Rerun verdict)"
    - "FRAME_SHIFT_DETECTED_CONTRACT references the <frame_shift_evidence> envelope produced by v40-pdfjs-frame-shift.yml"
    - "v40-pdfjs-frame-shift.yml emits a GitHub issue body with a <frame_shift_evidence> section on frame-shift detection (in addition to the current process.exit(1) red-X signal)"
    - "v40-auto-fix.yml precheck loop (line 91) accepts VERIFIER_DISAGREE and FRAME_SHIFT_DETECTED"
    - "inject-defect.mjs ERROR_CLASSES Set contains VERIFIER_DISAGREE and FRAME_SHIFT_DETECTED (mutator allowlist parity)"
    - "llm-router.js has // MODEL_DEFAULT_OK: VERIFIER_DISAGREE and // MODEL_DEFAULT_OK: FRAME_SHIFT_DETECTED comments — both default to sonnet"
    - "PROMPT_SCAFFOLDS is still Object.frozen and now has 7 keys (was 5)"
    - "buildScaffoldSystemPrompt function body is byte-unchanged vs HEAD"
    - "buildFixPrompt function body is byte-unchanged vs HEAD"
  artifacts:
    - path: "tests/e2e/lib/fix-prompt-builder.js"
      provides: "VERIFIER_DISAGREE_CONTRACT/_SYSTEM + FRAME_SHIFT_DETECTED_CONTRACT/_SYSTEM constants; PROMPT_SCAFFOLDS extended to 7 keys"
      contains: "VERIFIER_DISAGREE_SYSTEM"
    - path: "tests/e2e/lib/error-codes.js"
      provides: "FRAME_SHIFT_DETECTED export + ERROR_CLASSES array append"
      contains: "FRAME_SHIFT_DETECTED"
    - path: "tests/e2e/scripts/inject-defect.mjs"
      provides: "Mutator-allowlist extension: VERIFIER_DISAGREE + FRAME_SHIFT_DETECTED"
      contains: "VERIFIER_DISAGREE"
    - path: "tests/e2e/lib/llm-router.js"
      provides: "MODEL_DEFAULT_OK comment annotations for VERIFIER_DISAGREE + FRAME_SHIFT_DETECTED"
      contains: "MODEL_DEFAULT_OK: VERIFIER_DISAGREE"
    - path: ".github/workflows/v40-pdfjs-frame-shift.yml"
      provides: "FRAME_SHIFT_DETECTED issue producer (gh issue create with frame_shift_evidence body + FRAME_SHIFT_DETECTED + triage labels) on frame-shift detection branch"
      contains: "FRAME_SHIFT_DETECTED"
    - path: ".github/workflows/v40-auto-fix.yml"
      provides: "Precheck enum extended to accept VERIFIER_DISAGREE + FRAME_SHIFT_DETECTED"
      contains: "FRAME_SHIFT_DETECTED"
  key_links:
    - from: "tests/e2e/lib/fix-prompt-builder.js PROMPT_SCAFFOLDS"
      to: "buildScaffoldSystemPrompt({className, fixSurfaceContract})"
      via: "Object.freeze() respread (additive only)"
      pattern: "VERIFIER_DISAGREE: \\(\\) => VERIFIER_DISAGREE_SYSTEM"
    - from: ".github/workflows/v40-pdfjs-frame-shift.yml frame-shift-detected branch"
      to: ".github/workflows/v40-auto-fix.yml triage workflow"
      via: "gh issue create with FRAME_SHIFT_DETECTED label + frame_shift_evidence body"
      pattern: "FRAME_SHIFT_DETECTED"
    - from: "tests/e2e/lib/error-codes.js ERROR_CLASSES"
      to: "Plan 65-02 drift guard test"
      via: "iteration source — Plan 02 asserts the 7 scaffold-supported entries appear at all 5 sites"
      pattern: "FRAME_SHIFT_DETECTED"
---

<objective>
Add the `VERIFIER_DISAGREE` and `FRAME_SHIFT_DETECTED` fix scaffolds to the
auto-fix loop's prompt registry, wire `FRAME_SHIFT_DETECTED` end-to-end across
all canonical enumeration surfaces (error-codes.js, workflow precheck, mutator
allowlist, model-routing comment), and extend the v40-pdfjs-frame-shift
workflow to emit a triage issue with a `<frame_shift_evidence>` envelope on
detection.

Purpose: Close two known auto-fix coverage gaps — (1) `VERIFIER_DISAGREE` is
already in `ERROR_CLASSES` and produced by Phase 64 Rule 2 but the dispatcher
returns `{ok:false, escalate:'unsupported-class:VERIFIER_DISAGREE'}` because
the scaffold is missing; (2) the existing pdfjs frame-shift pre-flight
workflow detects frame shifts but only exits with a red X — there is no
auto-fix path to remediate. This plan delivers both scaffolds with their
contracts, completes the 5-site enumeration wiring for both ERROR_CLASSes
(so Plan 02's drift guard test passes when added), and gives
`FRAME_SHIFT_DETECTED` a producer site.

Output:
- 2 new scaffold thunks in `PROMPT_SCAFFOLDS` (5 to 7 keys; `Object.freeze` invariant preserved by respread)
- 1 new ERROR_CLASS string + append to `ERROR_CLASSES` array in `error-codes.js`
- 5-site enumeration completed for VERIFIER_DISAGREE + FRAME_SHIFT_DETECTED
- v40-pdfjs-frame-shift.yml workflow emits a triage issue on frame-shift detection (in addition to the existing red-X exit code 1 signal)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-expanded-fix-scaffolds/65-CONTEXT.md
@tests/e2e/lib/fix-prompt-builder.js
@tests/e2e/lib/error-codes.js
@tests/e2e/lib/llm-router.js
@tests/e2e/scripts/inject-defect.mjs
@.github/workflows/v40-pdfjs-frame-shift.yml
@.github/workflows/v40-auto-fix.yml

<interfaces>
<!-- Key APIs the executor needs. Extracted from fix-prompt-builder.js HEAD.
     The plan is ADDITIVE — these signatures must be byte-unchanged. -->

From tests/e2e/lib/fix-prompt-builder.js:
- `export function buildScaffoldSystemPrompt({ className, fixSurfaceContract }): string` — line 117. Pure. No I/O. Same inputs same output. Splices `className` into the opening sentence and `fixSurfaceContract` into the `## Fix surface contract` section.
- `export const PROMPT_SCAFFOLDS = Object.freeze({ ... })` — line 357. Maps `ERROR_CLASS` string to thunk returning the SYSTEM prompt string. Frozen at module load; runtime mutation throws TypeError in strict mode (pinned by `tests/unit/fix-prompt-builder.test.js` Phase 45 mutation guard).
- `export function buildFixPrompt({ errorClass, issueBody }): {ok:true, systemPrompt, userPrompt, model} | {ok:false, escalate}` — line 406. Looks up the scaffold; returns `unsupported-class:<class>` if not found. The function BODY is byte-unchanged — only the registry is extended.

From tests/e2e/lib/error-codes.js:
- `export const VERIFIER_DISAGREE = 'VERIFIER_DISAGREE'` — line 55. ALREADY EXISTS. Already present in `ERROR_CLASSES` at line 103.
- `export const ERROR_CLASSES = Object.freeze([...])` — line 98. Append-only extension; pre-existing entry order is byte-unchanged.

From tests/e2e/lib/llm-router.js:
- `export const MODEL_ROUTES = Object.freeze({ GOOGLE_DOM_DRIFT: 'claude-opus-4-7', LLM_HALLUCINATED_SELECTION: 'claude-opus-4-7' })` — line 60. Sparse-by-design; every other class falls through to sonnet via `??`. New scaffolds default to sonnet (per CONTEXT D-02) — annotated with `// MODEL_DEFAULT_OK: <CLASS>` comments rather than added to the table.

From tests/e2e/scripts/inject-defect.mjs:
- `export const ERROR_CLASSES = new Set(['WRONG_CITATION', 'LLM_HALLUCINATED_SELECTION', 'WORKER_FALLBACK_FAILED', 'GOOGLE_DOM_DRIFT', 'HARNESS_ERROR'])` — line 64. Mutator allowlist; extending requires adding both new classes.

From .github/workflows/v40-auto-fix.yml line 91:
- Precheck `for cls in FLAKE LLM_API_ERROR WRONG_CITATION LLM_HALLUCINATED_SELECTION WORKER_FALLBACK_FAILED GOOGLE_DOM_DRIFT HARNESS_ERROR PASS; do` — appended-to here, two new tokens added.
</interfaces>

<current_state_facts>
Confirmed via grep at planning time:
- `VERIFIER_DISAGREE` exists in `error-codes.js` (lines 16, 55, 103) — DO NOT re-add to `ERROR_CLASSES`.
- `FRAME_SHIFT_DETECTED` does NOT exist in `error-codes.js`, `fix-prompt-builder.js`, `inject-defect.mjs`, `v40-auto-fix.yml`, or `llm-router.js` — this is a wholly new ERROR_CLASS.
- `v40-pdfjs-frame-shift.yml` currently uses `process.exit(1)` on detection with a `console.error('FRAME-SHIFT DETECTED on cases: ...')` log — no issue producer exists yet.
</current_state_facts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add VERIFIER_DISAGREE + FRAME_SHIFT_DETECTED scaffolds and append FRAME_SHIFT_DETECTED to error-codes.js</name>
  <files>tests/e2e/lib/fix-prompt-builder.js, tests/e2e/lib/error-codes.js, tests/unit/fix-prompt-builder.test.js</files>
  <behavior>
    - Test 1 (existing file extension): `PROMPT_SCAFFOLDS.VERIFIER_DISAGREE` is a function; calling it returns a non-empty string containing the literal `### Verifier Disagreement` (template header) and the literal `Verifier tier:` and `Expected citation` (Phase 35 template parity).
    - Test 2 (existing file extension): `PROMPT_SCAFFOLDS.FRAME_SHIFT_DETECTED` is a function; calling it returns a non-empty string containing the literal `<frame_shift_evidence>` and the literal `pdfjs-dist` (the producer-envelope contract).
    - Test 3 (existing file extension): `PROMPT_SCAFFOLDS` still has `Object.isFrozen` true and now has exactly 7 keys: WRONG_CITATION, LLM_HALLUCINATED_SELECTION, WORKER_FALLBACK_FAILED, GOOGLE_DOM_DRIFT, HARNESS_ERROR, VERIFIER_DISAGREE, FRAME_SHIFT_DETECTED.
    - Test 4 (existing file extension): `buildFixPrompt({errorClass:'VERIFIER_DISAGREE', issueBody:'### Verifier Disagreement\nExpected citation: 1:5\nObserved citation: 1:7\n'})` returns `{ok:true, systemPrompt:<string>, userPrompt:<envelope>, model:'claude-sonnet-4-6'}`. Envelope contract: `userPrompt` starts with `<issue_body_untrusted>\n` and ends with `\n</issue_body_untrusted>`.
    - Test 5 (existing file extension): `buildFixPrompt({errorClass:'FRAME_SHIFT_DETECTED', issueBody:'<frame_shift_evidence>...</frame_shift_evidence>'})` returns `{ok:true, ..., model:'claude-sonnet-4-6'}`.
    - Test 6 (existing file extension): `error-codes.js` exports a new `FRAME_SHIFT_DETECTED` string; `ERROR_CLASSES.includes('FRAME_SHIFT_DETECTED')` is true; `ERROR_CLASSES.indexOf('FRAME_SHIFT_DETECTED')` equals `ERROR_CLASSES.length - 1` (appended at end per CONTEXT D-02 additive-only).
    - Test 7 (byte-stability of existing): existing Phase 45 assertions on `WRONG_CITATION`, `LLM_HALLUCINATED_SELECTION`, `WORKER_FALLBACK_FAILED`, `GOOGLE_DOM_DRIFT`, `HARNESS_ERROR` SYSTEM-prompt substrings continue to pass without test-file modification.
  </behavior>
  <action>
    Step A — `tests/e2e/lib/error-codes.js`:
      1. Add `export const FRAME_SHIFT_DETECTED = 'FRAME_SHIFT_DETECTED';` after the `WORKER_FALLBACK_FAILED` export (line 64) OR after the Phase 31 exports. Place it grouped with the Phase 30+ producer-extension constants for narrative coherence. Include a comment line: `// Phase 65 (SCAF-02) — v40-pdfjs-frame-shift workflow detected that a pdfjs-dist bump altered verifier verdicts; the issue body contains a <frame_shift_evidence> envelope.`
      2. Append `'FRAME_SHIFT_DETECTED'` as the LAST entry in the `ERROR_CLASSES` Object.freeze array (currently ends at line 109 with `'LLM_API_ERROR'`). Preserve order of all pre-existing entries byte-unchanged.

    Step B — `tests/e2e/lib/fix-prompt-builder.js`:
      1. Add `VERIFIER_DISAGREE_CONTRACT` as a `const` multi-line string after `HARNESS_ERROR_CONTRACT` (current line ~296). Contract text directs the LLM to inspect the Phase 35 Verifier Disagreement section. EDITABLE SURFACE is `src/` citation pipeline (selection.js + popup.js + Cloudflare Worker + PDF extraction). It MUST contain the literal headers `### Verifier Disagreement`, `Expected citation:`, `Observed citation:`, `Verifier tier:`, `Rerun verdict:` (Phase 35 issue template parity). It MUST contain a DO NOT block analogous to the existing 5 contracts (DO NOT: widen verifier tolerance, fix by adjusting verifier thresholds, swap to a less-strict verifier tier).
      2. Add `FRAME_SHIFT_DETECTED_CONTRACT` as a `const` multi-line string after `VERIFIER_DISAGREE_CONTRACT`. Contract text directs the LLM to inspect the `<frame_shift_evidence>` envelope (produced by `.github/workflows/v40-pdfjs-frame-shift.yml`). EDITABLE SURFACE: pdfjs frame-mapping helpers (`tests/e2e/lib/pdf-verifier.js` and any production-code consumers of pdfjs frame coordinates). It MUST contain the literal `<frame_shift_evidence>`, `pdfjs-dist`, and a DO NOT block (DO NOT: widen verifier verdict-comparison tolerance, fix by reverting the pdfjs-dist bump, edit `tests/golden/baseline.json`).
      3. Add `const VERIFIER_DISAGREE_SYSTEM = buildScaffoldSystemPrompt({ className: 'VERIFIER_DISAGREE', fixSurfaceContract: VERIFIER_DISAGREE_CONTRACT });` after the existing 5 `*_SYSTEM` constants block (around line 331).
      4. Add `const FRAME_SHIFT_DETECTED_SYSTEM = buildScaffoldSystemPrompt({ className: 'FRAME_SHIFT_DETECTED', fixSurfaceContract: FRAME_SHIFT_DETECTED_CONTRACT });` immediately after.
      5. Extend the `PROMPT_SCAFFOLDS` `Object.freeze` literal (line 357) by adding two new thunks: `VERIFIER_DISAGREE: () => VERIFIER_DISAGREE_SYSTEM,` and `FRAME_SHIFT_DETECTED: () => FRAME_SHIFT_DETECTED_SYSTEM,`. Preserve the existing 5 entries in their existing order — additive only.
      6. DO NOT touch `buildScaffoldSystemPrompt` body (Phase 45 byte-stability invariant). DO NOT touch `buildFixPrompt` body. DO NOT touch `SKIP_CLASS_ESCALATIONS`. DO NOT touch the `routeModel` import.

    Step C — `tests/unit/fix-prompt-builder.test.js`:
      1. Add a new `describe('Phase 65 — VERIFIER_DISAGREE + FRAME_SHIFT_DETECTED scaffolds', ...)` block at the end of the file. Do NOT modify any existing describe blocks — Phase 45 byte-stability is the test-side mirror of the source-side invariant.
      2. Add the 7 test assertions enumerated in `<behavior>` above.
      3. Use the existing imports of `buildFixPrompt`, `PROMPT_SCAFFOLDS`, `ENVELOPE_OPEN`, `ENVELOPE_CLOSE` (lines 37-45) — they cover everything needed.

    Step D — Verify trust invariants programmatically by running the existing Vitest suite. The pre-existing Phase 45 assertions must remain green (byte-stability proof on the 5 existing scaffolds is end-to-end via the existing test file; Plan 02 adds the explicit sha256 pins, but this plan must not break the existing assertions).
  </action>
  <verify>
    <automated>cd /home/fatduck/patent-cite-tool &amp;&amp; npx vitest run tests/unit/fix-prompt-builder.test.js --reporter=verbose</automated>
  </verify>
  <done>
    - `npx vitest run tests/unit/fix-prompt-builder.test.js` exits 0 with all pre-existing tests still green PLUS the 7 new Phase 65 assertions green.
    - `node -e "import('./tests/e2e/lib/error-codes.js').then(m =&gt; console.log(m.ERROR_CLASSES.includes('FRAME_SHIFT_DETECTED')))"` prints `true`.
    - `node -e "import('./tests/e2e/lib/fix-prompt-builder.js').then(m =&gt; console.log(Object.keys(m.PROMPT_SCAFFOLDS).length))"` prints `7`.
    - `git diff HEAD -- tests/e2e/lib/fix-prompt-builder.js | grep -v '^#' | grep -c '^-[^-]'` returns 0 (additive-only — no line deletions in the source file).
    - `git diff HEAD -- tests/e2e/lib/error-codes.js | grep -v '^#' | grep -c '^-[^-]'` returns 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Complete 5-site enumeration wiring (workflow precheck, inject-defect Set, llm-router MODEL_DEFAULT_OK comments)</name>
  <files>.github/workflows/v40-auto-fix.yml, tests/e2e/scripts/inject-defect.mjs, tests/e2e/lib/llm-router.js</files>
  <action>
    Step A — `.github/workflows/v40-auto-fix.yml` line ~91:
      1. Extend the `for cls in ...` precheck loop to include `VERIFIER_DISAGREE` and `FRAME_SHIFT_DETECTED`. Final token list (preserve existing order; append the two new ones before `PASS`): `FLAKE LLM_API_ERROR WRONG_CITATION LLM_HALLUCINATED_SELECTION WORKER_FALLBACK_FAILED GOOGLE_DOM_DRIFT HARNESS_ERROR VERIFIER_DISAGREE FRAME_SHIFT_DETECTED PASS`.
      2. Update the corresponding `gh issue comment` skip message (line ~100) to include the two new tokens in the human-readable enumeration.
      3. DO NOT change anything else in this workflow file — the Phase 50/53/57 trust invariants on `v40-auto-fix.yml` (including the `git push origin main` count == 1 lock from CONTEXT.md scope-lock) hold byte-unchanged elsewhere. Verify with `grep -c 'git push origin main' .github/workflows/v40-auto-fix.yml` equals 1 after edits.

    Step B — `tests/e2e/scripts/inject-defect.mjs` line 64 (`export const ERROR_CLASSES = new Set([...])`):
      1. Add `'VERIFIER_DISAGREE'` and `'FRAME_SHIFT_DETECTED'` to the Set literal. Place after `'HARNESS_ERROR'`.
      2. Update the comment block above (lines 61-63) to reflect the v4.3 Phase 65 expansion (mention that the Set MUST match the workflow precheck enumeration at `v40-auto-fix.yml:91`).
      3. DO NOT touch the `SOURCE_TAG` constant (MUTATOR-04 / T-59-04 co-design invariant from Phase 59).
      4. DO NOT touch the `SEED_RE` regex or any other constants.

    Step C — `tests/e2e/lib/llm-router.js`:
      1. ADD two `// MODEL_DEFAULT_OK: <CLASS>` comments. Recommended placement: extend the JSDoc block above `MODEL_ROUTES` (lines 46-58) with two new lines: `// MODEL_DEFAULT_OK: VERIFIER_DISAGREE — defaults to claude-sonnet-4-6 (Phase 65 SCAF-03 drift-guard satisfier).` and `// MODEL_DEFAULT_OK: FRAME_SHIFT_DETECTED — defaults to claude-sonnet-4-6 (Phase 65 SCAF-02; pdfjs frame-mapping fixes do not need opus reasoning).`
      2. Per CONTEXT D-02: do NOT add VERIFIER_DISAGREE or FRAME_SHIFT_DETECTED to the `MODEL_ROUTES` table — they default to sonnet. The drift guard (Plan 02) accepts EITHER a `MODEL_ROUTES` entry OR a `// MODEL_DEFAULT_OK: <CLASS>` comment as satisfaction; choose the comment path per CONTEXT.
      3. DO NOT touch `routeModel` function body.
      4. DO NOT touch `Object.freeze(MODEL_ROUTES)` shape.

    Trust invariants:
      - `Object.isFrozen(MODEL_ROUTES)` still true.
      - `MODEL_ROUTES` table has exactly 2 entries (GOOGLE_DOM_DRIFT, LLM_HALLUCINATED_SELECTION) — UNCHANGED.
      - `routeModel('VERIFIER_DISAGREE')` returns `'claude-sonnet-4-6'` via `??` fallthrough.
      - `routeModel('FRAME_SHIFT_DETECTED')` returns `'claude-sonnet-4-6'`.
      - `inject-defect.mjs:SOURCE_TAG` literal `'fixture-mutator-uat-47b'` byte-unchanged (Phase 59 MUTATOR-04 invariant).
  </action>
  <verify>
    <automated>cd /home/fatduck/patent-cite-tool &amp;&amp; grep -q 'VERIFIER_DISAGREE FRAME_SHIFT_DETECTED PASS' .github/workflows/v40-auto-fix.yml &amp;&amp; grep -q "'VERIFIER_DISAGREE'" tests/e2e/scripts/inject-defect.mjs &amp;&amp; grep -q "'FRAME_SHIFT_DETECTED'" tests/e2e/scripts/inject-defect.mjs &amp;&amp; grep -q 'MODEL_DEFAULT_OK: VERIFIER_DISAGREE' tests/e2e/lib/llm-router.js &amp;&amp; grep -q 'MODEL_DEFAULT_OK: FRAME_SHIFT_DETECTED' tests/e2e/lib/llm-router.js &amp;&amp; [ "$(grep -c 'git push origin main' .github/workflows/v40-auto-fix.yml)" = "1" ] &amp;&amp; node -e "import('./tests/e2e/lib/llm-router.js').then(m =&gt; { const r = m.routeModel('VERIFIER_DISAGREE'); const f = m.routeModel('FRAME_SHIFT_DETECTED'); if (r !== 'claude-sonnet-4-6' || f !== 'claude-sonnet-4-6') { console.error('FAIL routeModel default fallthrough'); process.exit(1); } console.log('routeModel default OK'); })" &amp;&amp; npx vitest run tests/unit/llm-router.test.js --reporter=verbose</automated>
  </verify>
  <done>
    - Workflow precheck loop accepts VERIFIER_DISAGREE + FRAME_SHIFT_DETECTED.
    - Mutator Set allows both classes for synthetic injection.
    - llm-router has both `MODEL_DEFAULT_OK` annotation comments; `MODEL_ROUTES` table unchanged.
    - `routeModel` defaults both classes to sonnet via `??` fallthrough.
    - `git push origin main` count in v40-auto-fix.yml still equals 1 (Phase 57 scope-lock preserved).
    - `npx vitest run tests/unit/llm-router.test.js` exits 0 (existing freeze + purity + routing tests all green).
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend v40-pdfjs-frame-shift.yml to emit a FRAME_SHIFT_DETECTED triage issue on detection</name>
  <files>.github/workflows/v40-pdfjs-frame-shift.yml</files>
  <action>
    Goal: on frame-shift detection, the workflow MUST create a `triage`-labeled GitHub issue with a `FRAME_SHIFT_DETECTED` label and a body containing a `<frame_shift_evidence>` envelope. The existing `process.exit(1)` red-X gate is preserved as a secondary signal — the issue producer is ADDITIVE.

    Step A — Permissions:
      1. Update `permissions:` block (line 62-63) from `contents: read` to `contents: read` + `issues: write`. The `gh issue create` call requires `issues: write`.

    Step B — Restructure the "Compare citation outputs (frame-shift detection)" step (lines 149-196):
      1. Replace the inline `node -e` that exits 1 on diff. Instead, write the diff envelope to a temp file (`/tmp/frame-shift-evidence.txt`) AND set a step output `frame_shift=1` when diffs are non-empty. The temp file content MUST contain (a) the PREV and NEW pdfjs versions from `steps.delta.outputs`, (b) the list of diverging case IDs, (c) the per-case normalized JSON diff (oldR cases vs newR cases).
      2. The node block still calls `process.exit(1)` when diffs exist (preserves the existing red-X behavior) — but the exit happens AFTER the temp file is written. Use `try { ... fs.writeFileSync(...) } finally { if (diffs.length) process.exit(1); }` shape, OR move the exit to a separate step gated on `if: steps.compare.outputs.frame_shift == '1'` (cleaner).
      3. Recommended cleaner shape: split into two steps:
         - Step "Compare citation outputs (frame-shift detection)" — writes evidence file, sets `frame_shift=1`/`frame_shift=0` step output, exits 0 ALWAYS (so the next step can run).
         - Step "Emit FRAME_SHIFT_DETECTED triage issue" — `if: steps.compare.outputs.frame_shift == '1'`. Body:
           ```
           gh issue create \
             --title "FRAME_SHIFT_DETECTED: pdfjs-dist ${{ steps.delta.outputs.prev }} → ${{ steps.delta.outputs.new }} altered verifier verdicts" \
             --label "triage,FRAME_SHIFT_DETECTED,auto-fix:enqueue" \
             --body "$(cat /tmp/frame-shift-evidence.txt)"
           ```
         - Step "Fail the check (red X)" — `if: steps.compare.outputs.frame_shift == '1'` — `run: exit 1`.

    Step C — Body format MUST match what the new `FRAME_SHIFT_DETECTED_CONTRACT` in `fix-prompt-builder.js` expects. The body MUST contain:
      1. A `<frame_shift_evidence>` opening tag and `</frame_shift_evidence>` closing tag wrapping the evidence block (so the scaffold contract's `Inspect the <frame_shift_evidence> envelope` directive matches).
      2. The PREV and NEW pdfjs-dist versions (from `steps.delta.outputs.prev` and `.new`).
      3. The diverging case IDs as a list.
      4. The per-case normalized JSON diff (oldR vs newR), pretty-printed.
      5. A note: `Auto-Fix path: this issue is wired into the v40-auto-fix workflow via the FRAME_SHIFT_DETECTED label. The auto-fix scaffold lives in tests/e2e/lib/fix-prompt-builder.js (Phase 65 SCAF-02).`

    Step D — Trust invariants:
      - The `verifier-frozen-preflight` job MUST still exit non-zero on detection (red X preserved).
      - The `pdfjs-dist` install steps (lines 105-117) are UNCHANGED.
      - The two `npm run e2e:regression` steps (lines 119-147) are UNCHANGED.
      - The `concurrency` block (line 58-60) is UNCHANGED.

    Step E — `.github/workflows/v40-auto-fix.yml` triggers on `issues.opened` (verify this is already true; the precheck step at line 91 fires on label match). The `FRAME_SHIFT_DETECTED` label (added by Step C above) is sufficient to trigger the existing precheck loop after Task 2's enumeration update.

    Step F — actionlint sanity:
      1. Run `actionlint` against the updated workflow if available (`command -v actionlint && actionlint .github/workflows/v40-pdfjs-frame-shift.yml`). If actionlint is not on PATH, document as `MISSING — actionlint not installed; rely on the next CI run for syntactic validation`.
  </action>
  <verify>
    <automated>cd /home/fatduck/patent-cite-tool &amp;&amp; grep -q 'issues: write' .github/workflows/v40-pdfjs-frame-shift.yml &amp;&amp; grep -q 'FRAME_SHIFT_DETECTED' .github/workflows/v40-pdfjs-frame-shift.yml &amp;&amp; grep -q 'frame_shift_evidence' .github/workflows/v40-pdfjs-frame-shift.yml &amp;&amp; grep -q 'gh issue create' .github/workflows/v40-pdfjs-frame-shift.yml &amp;&amp; node -e "const fs=require('node:fs'); const y=fs.readFileSync('.github/workflows/v40-pdfjs-frame-shift.yml','utf8'); if (!/exit 1/.test(y)) { console.error('FAIL: red-X exit 1 missing'); process.exit(1); } console.log('red-X preserved'); " &amp;&amp; (command -v actionlint &amp;&amp; actionlint .github/workflows/v40-pdfjs-frame-shift.yml || echo 'actionlint not installed — skipping')</automated>
  </verify>
  <done>
    - Workflow `permissions:` includes `issues: write`.
    - On frame-shift detection (diff non-empty), workflow runs `gh issue create` with a `triage,FRAME_SHIFT_DETECTED,auto-fix:enqueue` label set and a body containing `<frame_shift_evidence>...</frame_shift_evidence>`.
    - Red-X behavior preserved: workflow still exits non-zero when a frame shift is detected (existing Pitfall 6 defense unchanged).
    - The skip-notice step (current line 198-200) for `steps.delta.outputs.skip == '1'` is UNCHANGED.
    - actionlint (if available) reports no errors against the modified workflow.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub issue body → fix-prompt-builder | Untrusted attacker-controlled prompt injection vector. New scaffolds wrap input in the existing `<issue_body_untrusted>` envelope; the trust-boundary section of buildScaffoldSystemPrompt is byte-unchanged. |
| pdfjs-dist bump PR → frame-shift workflow → triage issue | An attacker-author of a pdfjs-dist bump PR could control the diff content. The frame-shift workflow runs on PR `synchronize` events but the issue is created from the workflow's own `gh issue create`, which uses the workflow's GITHUB_TOKEN (issues: write scope only). The body is COMPUTED from the regression report — not directly attacker-controlled. |
| FRAME_SHIFT_DETECTED label → v40-auto-fix → scaffolds | The label is set by the trusted workflow; a malicious actor cannot synthesize this label without write access. The auto-fix loop's existing diff-guard (scripts/check-diff-guard.mjs) blocks any LLM-proposed diff touching FORBIDDEN_PATHS — Phase 67's PITER-05 will add fix-prompt-builder.js + llm-router.js to that list. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Tampering | FRAME_SHIFT_DETECTED_CONTRACT prompt injection | mitigate | Contract text wrapped in existing `<issue_body_untrusted>` envelope via buildScaffoldSystemPrompt (no changes to envelope logic); contract instructs LLM to treat envelope contents as DATA per existing Trust boundary section. |
| T-65-02 | Tampering | VERIFIER_DISAGREE_CONTRACT prompt injection | mitigate | Same envelope mechanism as T-65-01. |
| T-65-03 | Elevation of Privilege | v40-pdfjs-frame-shift `issues: write` permission upgrade | accept | Permission is scoped to issues only (no `contents: write`, no `pull-requests: write`). The workflow's only network action is `gh issue create` to its own repo. The token is the workflow's GITHUB_TOKEN, scoped to this single workflow run. |
| T-65-04 | Information Disclosure | Issue body contains pdfjs version + per-case diff data | accept | The PREV/NEW pdfjs versions are public (package.json is public); per-case diff data is the citation output which is already public in the test corpus. No PII, no secrets. |
| T-65-05 | Denial of Service | Synthetic FRAME_SHIFT_DETECTED issue flood via abusive PR resubmissions | accept | Concurrency block (line 58-60) cancels in-flight runs on the same PR. Two distinct PRs each bumping pdfjs to a different version would create at most 2 issues. Per-issue cap ($1) + per-PR cap ($2) limit downstream auto-fix spend. |
| T-65-06 | Tampering | New ERROR_CLASS enum drift across the 5 sites | mitigate | Plan 02's `tests/unit/error-class-enumeration-drift.test.js` Vitest gate asserts presence at all 5 sites for every scaffold-supported ERROR_CLASS; SCAF-03 wires this in. Drift CAUSES TEST FAILURE — fail-closed. |
| T-65-SC | Tampering | npm package legitimacy | mitigate | No new npm dependencies — entirely additive edits to existing JS files. Zero new package installs. No supply-chain surface added. |
</threat_model>

<verification>
Phase-level checks performed at the end of Plan 01:
- `node -e "import('./tests/e2e/lib/fix-prompt-builder.js').then(m => { const k = Object.keys(m.PROMPT_SCAFFOLDS); if (k.length !== 7) { console.error('FAIL key count', k.length); process.exit(1); } console.log('PASS 7 keys', k); })"` prints `PASS 7 keys [...]`.
- `node -e "import('./tests/e2e/lib/error-codes.js').then(m => console.log(m.ERROR_CLASSES.includes('FRAME_SHIFT_DETECTED') && m.ERROR_CLASSES.includes('VERIFIER_DISAGREE')))"` prints `true`.
- `grep -c 'FRAME_SHIFT_DETECTED' .github/workflows/v40-auto-fix.yml .github/workflows/v40-pdfjs-frame-shift.yml tests/e2e/scripts/inject-defect.mjs tests/e2e/lib/llm-router.js tests/e2e/lib/fix-prompt-builder.js tests/e2e/lib/error-codes.js` returns ≥1 for each file.
- `grep -c 'VERIFIER_DISAGREE' .github/workflows/v40-auto-fix.yml tests/e2e/scripts/inject-defect.mjs tests/e2e/lib/llm-router.js tests/e2e/lib/fix-prompt-builder.js tests/e2e/lib/error-codes.js` returns ≥1 for each file.
- `npx vitest run tests/unit/fix-prompt-builder.test.js tests/unit/llm-router.test.js` exits 0.
- `grep -c 'git push origin main' .github/workflows/v40-auto-fix.yml` equals 1 (Phase 57 scope-lock preserved).
- `git diff HEAD -- tests/e2e/lib/fix-prompt-builder.js | grep -v '^#' | grep -c '^-export\|^-function\|^-const ' | head -1` returns 0 (no exports/functions/consts deleted — additive-only).
</verification>

<success_criteria>
- `buildFixPrompt({errorClass:'VERIFIER_DISAGREE', issueBody:'<body>'})` returns `{ok:true, ...}` (was `{ok:false, escalate:'unsupported-class:VERIFIER_DISAGREE'}` before this plan).
- `buildFixPrompt({errorClass:'FRAME_SHIFT_DETECTED', issueBody:'<body>'})` returns `{ok:true, ...}`.
- VERIFIER_DISAGREE and FRAME_SHIFT_DETECTED are present at ALL 5 enumeration sites (error-codes.js, v40-auto-fix.yml precheck, PROMPT_SCAFFOLDS, inject-defect.mjs Set, llm-router.js [via MODEL_DEFAULT_OK comment]).
- v40-pdfjs-frame-shift.yml emits a triage issue with `<frame_shift_evidence>` body on detection; existing red-X exit code 1 preserved.
- All trust invariants preserved: PROMPT_SCAFFOLDS Object.frozen; ERROR_CLASSES order byte-unchanged for pre-existing entries; buildScaffoldSystemPrompt + buildFixPrompt bodies byte-unchanged; MODEL_ROUTES shape unchanged; SOURCE_TAG `'fixture-mutator-uat-47b'` byte-unchanged; `git push origin main` count in v40-auto-fix.yml equals 1.
- All existing Vitest tests still pass (no regressions in fix-prompt-builder.test.js or llm-router.test.js).
</success_criteria>

<output>
Create `.planning/phases/65-expanded-fix-scaffolds/65-01-SUMMARY.md` when done. Summary MUST record:
- The exact sha256 of each of the 7 PROMPT_SCAFFOLDS-resolved systemPrompt strings (so Plan 02 can copy the 5 EXISTING ones verbatim into its byte-stability pin without re-deriving). Use `node -e "import('./tests/e2e/lib/fix-prompt-builder.js').then(m => { const k = Object.keys(m.PROMPT_SCAFFOLDS); const c = require('node:crypto'); for (const x of k) console.log(x, c.createHash('sha256').update(m.PROMPT_SCAFFOLDS[x]()).digest('hex')); })"` to compute.
- Confirmation that the 5 PRE-EXISTING scaffold sha256s match a fresh recomputation from HEAD (proves the 5 existing scaffolds are byte-stable through this plan — additive-only invariant honored).
- The exact line range where VERIFIER_DISAGREE_CONTRACT and FRAME_SHIFT_DETECTED_CONTRACT live in fix-prompt-builder.js (so Plan 02 can reference them).
</output>
</content>
</invoke>