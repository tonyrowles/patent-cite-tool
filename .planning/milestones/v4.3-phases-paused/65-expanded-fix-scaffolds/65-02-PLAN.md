---
phase: 65-expanded-fix-scaffolds
plan: 02
type: execute
wave: 2
depends_on:
  - 65-01
files_modified:
  - tests/unit/error-class-enumeration-drift.test.js
  - tests/unit/fix-prompt-builder-byte-stability.test.js
autonomous: true
requirements:
  - SCAF-03
  - SCAF-04

must_haves:
  truths:
    - "tests/unit/error-class-enumeration-drift.test.js exists and asserts that every scaffold-supported ERROR_CLASS (the 7 PROMPT_SCAFFOLDS keys) is present at all 5 canonical enumeration sites"
    - "The drift-guard test produces actionable error messages of the form 'ERROR_CLASS X is in PROMPT_SCAFFOLDS but missing from <site Y>: <expected file path/literal>' on failure"
    - "tests/unit/fix-prompt-builder-byte-stability.test.js exists and pins sha256 digests of the 5 Phase 45 scaffolds (WRONG_CITATION, LLM_HALLUCINATED_SELECTION, WORKER_FALLBACK_FAILED, GOOGLE_DOM_DRIFT, HARNESS_ERROR)"
    - "Byte-stability test sha256 digests are the EXACT values produced by Plan 01's HEAD-state PROMPT_SCAFFOLDS thunks — proves the 5 existing scaffolds did NOT drift through Plan 01's additive edits"
    - "Byte-stability test ALSO pins sha256 digests for the 2 new scaffolds (VERIFIER_DISAGREE + FRAME_SHIFT_DETECTED) — establishes the Phase 65 baseline that future phases must respect"
    - "Both new Vitest files run under fileParallelism:false (existing project convention) and exit 0 when invoked individually"
    - "Drift-guard test currently passes (Plan 01 wired all 5 sites correctly); a deliberate removal of any site entry causes the test to fail with the actionable message"
  artifacts:
    - path: "tests/unit/error-class-enumeration-drift.test.js"
      provides: "5-site drift guard for every scaffold-supported ERROR_CLASS"
      contains: "PROMPT_SCAFFOLDS"
      min_lines: 80
    - path: "tests/unit/fix-prompt-builder-byte-stability.test.js"
      provides: "sha256 pins for all 7 scaffold systemPrompts"
      contains: "createHash"
      min_lines: 40
  key_links:
    - from: "tests/unit/error-class-enumeration-drift.test.js"
      to: "tests/e2e/lib/error-codes.js + fix-prompt-builder.js + llm-router.js + .github/workflows/v40-auto-fix.yml + tests/e2e/scripts/inject-defect.mjs"
      via: "fs.readFileSync + grep-equivalent regex matching"
      pattern: "PROMPT_SCAFFOLDS.+VERIFIER_DISAGREE"
    - from: "tests/unit/fix-prompt-builder-byte-stability.test.js"
      to: "tests/e2e/lib/fix-prompt-builder.js PROMPT_SCAFFOLDS"
      via: "import + sha256 digest computation"
      pattern: "createHash\\('sha256'\\)"
---

<objective>
Ship two new Vitest gates: (a) a 5-site enumeration drift guard that
asserts every scaffold-supported ERROR_CLASS (every key in PROMPT_SCAFFOLDS)
is present at all 5 canonical enumeration surfaces; (b) a byte-stability
sha256 pin for all 7 scaffold systemPrompt strings (5 pre-existing + 2 added
in Plan 01).

Purpose: The 5-site drift guard is the LOAD-BEARING gate that prevents
future enumeration drift — failure means an ERROR_CLASS was added to
PROMPT_SCAFFOLDS without all 5 canonical sites being updated. The
byte-stability pins anchor the 5 existing scaffolds (Phase 45 baseline)
plus the 2 new scaffolds (Phase 65 baseline) — future edits to
buildScaffoldSystemPrompt or any contract require deliberate pin update.

Output:
- `tests/unit/error-class-enumeration-drift.test.js` — iterates `Object.keys(PROMPT_SCAFFOLDS)`, for each class asserts presence at sites 1-5 with actionable failure messages.
- `tests/unit/fix-prompt-builder-byte-stability.test.js` — 7 sha256-equality assertions (one per PROMPT_SCAFFOLDS key).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-expanded-fix-scaffolds/65-CONTEXT.md
@.planning/phases/65-expanded-fix-scaffolds/65-01-PLAN.md
@.planning/phases/65-expanded-fix-scaffolds/65-01-SUMMARY.md
@tests/e2e/lib/fix-prompt-builder.js
@tests/e2e/lib/error-codes.js
@tests/e2e/lib/llm-router.js
@tests/e2e/scripts/inject-defect.mjs
@.github/workflows/v40-auto-fix.yml

<interfaces>
<!-- Critical: this plan READS files; it does NOT modify the source library. -->

5 canonical enumeration sites (as defined in CONTEXT.md SCAF-03):
  1. `tests/e2e/lib/error-codes.js` — `ERROR_CLASSES` Object.freeze array (source of truth for the closed enum, line 98)
  2. `.github/workflows/v40-auto-fix.yml` — precheck `for cls in ...` loop (line 91; Plan 01 Task 2A extended this)
  3. `tests/e2e/lib/fix-prompt-builder.js` — `PROMPT_SCAFFOLDS` Object.freeze registry (line 357; Plan 01 Task 1B5 extended this)
  4. `tests/e2e/scripts/inject-defect.mjs` — `ERROR_CLASSES` Set literal (line 64; Plan 01 Task 2B extended this)
  5. `tests/e2e/lib/llm-router.js` — `MODEL_ROUTES` Object.freeze table OR `// MODEL_DEFAULT_OK: <CLASS>` comment (Plan 01 Task 2C added the comment annotations)

Drift-guard scope decision:
- Iterate `Object.keys(PROMPT_SCAFFOLDS)` (the 7 scaffold-supported classes), NOT `ERROR_CLASSES` (the 11-element closed enum).
- Rationale: pre-existing ERROR_CLASSES members (EXTENSION_NOT_LOADED, NO_CITATION_PRODUCED, UI_BROKEN, USPTO_API_DRIFT, FLAKE, LLM_API_ERROR) are NOT scaffold-supported by design (FLAKE/LLM_API_ERROR/PASS hit SKIP_CLASS_ESCALATIONS short-circuit; the others have no current producer). Iterating PROMPT_SCAFFOLDS keys gives the precise "must be at all 5 sites" set without false positives on intentional gaps.
- This scope is the LOAD-BEARING design choice; document inline in the test file header.

From Plan 01's 65-01-SUMMARY.md (consumed for byte-stability pins):
- 7 sha256 hex digests, one per PROMPT_SCAFFOLDS key. Copied verbatim into Plan 02's pin map.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write 5-site enumeration drift guard Vitest test</name>
  <files>tests/unit/error-class-enumeration-drift.test.js</files>
  <behavior>
    - Test 1: `Object.keys(PROMPT_SCAFFOLDS)` returns exactly 7 keys (sanity gate; if Plan 01 regressed, this fails fast).
    - Test 2 (parameterized via `it.each`): for each key K in `Object.keys(PROMPT_SCAFFOLDS)`, assert `ERROR_CLASSES.includes(K)` in error-codes.js — UNLESS K is `'HARNESS_ERROR'` (which is intentionally NOT in ERROR_CLASSES per the existing source comment lines 79-85). Document the HARNESS_ERROR exception inline.
    - Test 3 (parameterized): for each K, assert the v40-auto-fix.yml precheck line at ~91 contains the literal token K (read file, regex match against `for cls in [^;]*\b${K}\b`).
    - Test 4 (parameterized): for each K, assert `inject-defect.mjs` ERROR_CLASSES Set contains `'${K}'` (read file, regex match against `'${K}'`).
    - Test 5 (parameterized): for each K, assert llm-router.js EITHER (a) has K as a key in MODEL_ROUTES OR (b) contains the literal `// MODEL_DEFAULT_OK: ${K}` comment line.
    - Test 6: failure-message-shape — manually construct a synthetic "what if K is missing from site 2" scenario and assert the error message contains the literal `is in PROMPT_SCAFFOLDS but missing from` substring. (Implementation: factor the per-site check into a helper that returns `{ok:boolean, message:string}`; the test calls the helper with a synthetic missing-K input. Helper is unit-testable; does NOT actually mutate any source file.)
  </behavior>
  <action>
    Step A — Create `tests/unit/error-class-enumeration-drift.test.js`:

    File header MUST document:
      - Phase 65 SCAF-03 — 5-site enumeration drift guard.
      - Iteration scope = `Object.keys(PROMPT_SCAFFOLDS)` (NOT ERROR_CLASSES), with rationale.
      - The 5 sites explicitly named with file paths.
      - HARNESS_ERROR exception (in PROMPT_SCAFFOLDS but NOT in ERROR_CLASSES by Phase 45 design).

    Imports:
      - `import { describe, it, expect } from 'vitest';`
      - `import fs from 'node:fs';`
      - `import path from 'node:path';`
      - `import { fileURLToPath } from 'node:url';`
      - `import { PROMPT_SCAFFOLDS } from '../../tests/e2e/lib/fix-prompt-builder.js';`
      - `import { ERROR_CLASSES } from '../../tests/e2e/lib/error-codes.js';`
      - `import { MODEL_ROUTES } from '../../tests/e2e/lib/llm-router.js';`

    Constants:
      - `const REPO_ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..', '..');`
      - `const SCAFFOLD_KEYS = Object.keys(PROMPT_SCAFFOLDS);`
      - `const HARNESS_ERROR_EXCEPTION = 'HARNESS_ERROR'; // by design — see tests/e2e/lib/error-codes.js:79-85`

    Helper functions (each returns `{ok:boolean, message:string}`):
      - `checkErrorCodesArray(className)` — reads `tests/e2e/lib/error-codes.js`, asserts `ERROR_CLASSES` array (imported above) includes className OR className is the documented exception.
      - `checkWorkflowPrecheck(className)` — reads `.github/workflows/v40-auto-fix.yml`, asserts the precheck `for cls in ...` loop line contains the className token (regex: `/for\s+cls\s+in\s+[^;]*\b<CLASS>\b/`).
      - `checkPromptScaffolds(className)` — uses imported PROMPT_SCAFFOLDS; asserts `typeof PROMPT_SCAFFOLDS[className] === 'function'`. (Trivially true for iteration source — but the helper validates the contract regardless.)
      - `checkInjectDefectSet(className)` — reads `tests/e2e/scripts/inject-defect.mjs`, asserts the `ERROR_CLASSES = new Set([...])` literal contains `'<CLASS>'`.
      - `checkLlmRouterCoverage(className)` — asserts EITHER `className in MODEL_ROUTES` OR `// MODEL_DEFAULT_OK: <CLASS>` literal substring present in `tests/e2e/lib/llm-router.js`.

    Failure messages MUST take the shape: `ERROR_CLASS '<CLASS>' is in PROMPT_SCAFFOLDS but missing from <SITE_NAME>: expected literal '<PATTERN>' in <FILE_PATH>`.

    `describe` blocks:
      - `describe('Phase 65 SCAF-03 — 5-site enumeration drift guard', () => {` containing:
        - `it('PROMPT_SCAFFOLDS has 7 keys', () => { expect(SCAFFOLD_KEYS.length).toBe(7); })`
        - `it.each(SCAFFOLD_KEYS)('site 1 (error-codes.js ERROR_CLASSES): %s', (k) => { const r = checkErrorCodesArray(k); expect(r.ok, r.message).toBe(true); })`
        - Similar `it.each` blocks for sites 2-5.
        - `it('helper failure-message shape', () => { const r = checkWorkflowPrecheck('___NONEXISTENT_CLASS___'); expect(r.ok).toBe(false); expect(r.message).toMatch(/is in PROMPT_SCAFFOLDS but missing from/); })`

    Step B — Run the test to confirm it currently passes (Plan 01 wired all 5 sites correctly). If any assertion fails, the cause is a Plan 01 gap — STOP and surface the gap; do NOT add the missing token in Plan 02 (separation of concerns).

    Step C — Manual smoke: temporarily comment out one site entry (e.g., remove `VERIFIER_DISAGREE` from the workflow precheck), re-run the test, confirm it fails with the actionable message. Then restore.

    Trust invariants:
      - Test file imports NO transport-layer code (no `llm-driver.js`, no `auto-fix.mjs`, no `@anthropic-ai/sdk`). Pure-function library imports only (fix-prompt-builder.js + error-codes.js + llm-router.js are all pure per D-04). Filesystem reads are limited to the 3 explicit paths above.
      - Test runs under existing Vitest config (fileParallelism: false per CONTEXT code_context).
  </behavior>
  <action>
    Implement the file per the behavior block above. Do NOT modify any source file. Do NOT add VERIFIER_DISAGREE or FRAME_SHIFT_DETECTED to any production file — those were Plan 01's job; this plan only validates.
  </action>
  <verify>
    <automated>cd /home/fatduck/patent-cite-tool &amp;&amp; npx vitest run tests/unit/error-class-enumeration-drift.test.js --reporter=verbose</automated>
  </verify>
  <done>
    - `npx vitest run tests/unit/error-class-enumeration-drift.test.js` exits 0.
    - Test file exists with min 80 lines.
    - All 7 PROMPT_SCAFFOLDS keys parameterize the 5 site checks (35 it.each invocations total — 7 keys × 5 sites; plus sanity + helper-shape tests).
    - Failure messages take the literal shape `is in PROMPT_SCAFFOLDS but missing from <site>` (verified by Test 6).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write byte-stability sha256 pin Vitest test for all 7 scaffolds</name>
  <files>tests/unit/fix-prompt-builder-byte-stability.test.js</files>
  <behavior>
    - Test 1: for each of WRONG_CITATION, LLM_HALLUCINATED_SELECTION, WORKER_FALLBACK_FAILED, GOOGLE_DOM_DRIFT, HARNESS_ERROR — compute sha256(PROMPT_SCAFFOLDS[K]()) and assert equality with the pinned hex digest from `SCAFFOLD_SHA256_PIN_PHASE_45_BASELINE`. These 5 digests come from Plan 01's 65-01-SUMMARY.md (already-computed values; Plan 01 produced them by hashing HEAD-state outputs).
    - Test 2: for each of VERIFIER_DISAGREE, FRAME_SHIFT_DETECTED — compute sha256 and assert equality with the pinned hex digest from `SCAFFOLD_SHA256_PIN_PHASE_65_BASELINE`. These 2 digests come from Plan 01's 65-01-SUMMARY.md (newly-computed during Plan 01 implementation; this pin establishes them as the v4.3 baseline).
    - Test 3: failure shape — pin map MUST contain exactly 7 entries; assert `Object.keys(PHASE_45_BASELINE).length + Object.keys(PHASE_65_BASELINE).length === 7`.
    - Test 4: re-run determinism — call `PROMPT_SCAFFOLDS[K]()` twice in succession; assert both calls return byte-identical strings (proves no hidden non-determinism in buildScaffoldSystemPrompt — D-04 purity invariant defense-in-depth).
  </behavior>
  <action>
    Step A — Create `tests/unit/fix-prompt-builder-byte-stability.test.js`:

    File header MUST document:
      - Phase 65 SCAF-04 — byte-stability sha256 pin for all 7 scaffolds.
      - PHASE_45_BASELINE = 5 digests pinned as the Phase 45 baseline (the 5 existing scaffolds, byte-unchanged through Phase 65 Plan 01).
      - PHASE_65_BASELINE = 2 digests pinned as the new Phase 65 baseline (the 2 new scaffolds).
      - Update protocol: if `buildScaffoldSystemPrompt` body or any `*_CONTRACT` constant changes, run `node -e "import('./tests/e2e/lib/fix-prompt-builder.js').then(m => { const c = require('node:crypto'); for (const k of Object.keys(m.PROMPT_SCAFFOLDS)) console.log(k, c.createHash('sha256').update(m.PROMPT_SCAFFOLDS[k]()).digest('hex')); })"`, copy new digests into the pin maps, then re-run this test.

    Imports:
      - `import { describe, it, expect } from 'vitest';`
      - `import { createHash } from 'node:crypto';`
      - `import { PROMPT_SCAFFOLDS } from '../../tests/e2e/lib/fix-prompt-builder.js';`

    Pin maps (values supplied by Plan 01's 65-01-SUMMARY.md — Plan 02 executor copies them verbatim):
      - `const PHASE_45_BASELINE = Object.freeze({ WRONG_CITATION: '<hex>', LLM_HALLUCINATED_SELECTION: '<hex>', WORKER_FALLBACK_FAILED: '<hex>', GOOGLE_DOM_DRIFT: '<hex>', HARNESS_ERROR: '<hex>' });`
      - `const PHASE_65_BASELINE = Object.freeze({ VERIFIER_DISAGREE: '<hex>', FRAME_SHIFT_DETECTED: '<hex>' });`
      - The `<hex>` placeholders MUST be replaced with the actual values from 65-01-SUMMARY.md. Source the file: `cat .planning/phases/65-expanded-fix-scaffolds/65-01-SUMMARY.md | grep -E '^[A-Z_]+ [0-9a-f]{64}'` extracts the lines.

    Helper:
      - `function sha256(s) { return createHash('sha256').update(s, 'utf8').digest('hex'); }`

    `describe` blocks:
      - `describe('Phase 65 SCAF-04 — scaffold byte-stability sha256 pins', () => {` containing:
        - `it.each(Object.entries(PHASE_45_BASELINE))('Phase 45 baseline: %s', (k, expected) => { expect(sha256(PROMPT_SCAFFOLDS[k]())).toBe(expected); })`
        - `it.each(Object.entries(PHASE_65_BASELINE))('Phase 65 baseline: %s', (k, expected) => { expect(sha256(PROMPT_SCAFFOLDS[k]())).toBe(expected); })`
        - `it('pin maps cover all 7 scaffold keys', () => { const pinned = new Set([...Object.keys(PHASE_45_BASELINE), ...Object.keys(PHASE_65_BASELINE)]); const live = new Set(Object.keys(PROMPT_SCAFFOLDS)); expect(pinned).toEqual(live); })`
        - `it.each(Object.keys(PROMPT_SCAFFOLDS))('determinism: %s returns byte-identical string on re-invocation', (k) => { expect(PROMPT_SCAFFOLDS[k]()).toBe(PROMPT_SCAFFOLDS[k]()); })`

    Step B — Source the digests from Plan 01's SUMMARY.md. If the SUMMARY.md does not contain the digests (Plan 01 forgot to emit them), STOP and surface the gap — do NOT hand-compute fresh digests in Plan 02 (the whole point of putting the computation in Plan 01 is to anchor the baseline at the moment of source-code commit, not during test authoring).

    Step C — Run the test; confirm all 7 sha256 assertions pass.

    Trust invariants:
      - Test imports ONLY `node:crypto` + the pure fix-prompt-builder library. No I/O beyond the import.
      - The pin maps are `Object.freeze`'d to prevent accidental mutation by future tests.
      - The "pin maps cover all 7 scaffold keys" assertion FAILS LOUDLY if a future phase adds a new scaffold without updating the pin maps — fail-closed posture.
  </behavior>
  <action>
    Implement the file per the behavior block. Pin digests are copy-pasted from 65-01-SUMMARY.md verbatim — do NOT modify them.
  </action>
  <verify>
    <automated>cd /home/fatduck/patent-cite-tool &amp;&amp; npx vitest run tests/unit/fix-prompt-builder-byte-stability.test.js --reporter=verbose</automated>
  </verify>
  <done>
    - `npx vitest run tests/unit/fix-prompt-builder-byte-stability.test.js` exits 0.
    - Test file exists with min 40 lines.
    - All 7 sha256 assertions pass.
    - Pin maps are `Object.freeze`'d.
    - Pin-map-coverage assertion present (guards against future drift).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runtime → source files (read-only) | Drift-guard test reads 3 source files via `fs.readFileSync`. The reads are scoped to specific paths under the repo root; no network, no exec. |
| Future PR → drift-guard test | The drift guard is the gate — a future PR that adds a new scaffold but forgets one of the 5 sites causes test FAILURE in CI. The test ITSELF is on FORBIDDEN_PATHS-adjacent territory (tests/unit/) but is NOT in the FORBIDDEN_PATHS regex bank (auto-fix loop can edit tests/unit/ files; the diff-guard blocks ONLY tests/test-cases.js + golden + quarantine + .github/workflows/v40-* + ledger + CODEOWNERS). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-DG-01 | Tampering | Drift-guard test bypass via test deletion | accept | Tests in tests/unit/ are not in FORBIDDEN_PATHS by design (the auto-fix loop must be able to edit unit tests to make them pass). Mitigation is procedural: CODEOWNERS review of `tests/unit/error-class-enumeration-drift.test.js` edits surfaces deletion in human review. Out-of-scope for Phase 65. |
| T-65-DG-02 | Tampering | Drift-guard scope shrinkage (e.g., changing iteration source to a 5-key subset of PROMPT_SCAFFOLDS) | mitigate | Inline doc-comment in the test file makes the iteration scope (`Object.keys(PROMPT_SCAFFOLDS)`) the LOAD-BEARING design choice with rationale. Future scope changes require visible diff hits on the doc-comment block. |
| T-65-BS-01 | Tampering | Byte-stability pin update without code review | mitigate | Pin updates require human-edit of `PHASE_45_BASELINE` or `PHASE_65_BASELINE` Object.freeze literals. Any pin change visible in PR diff; reviewer can ask "what code changed to invalidate the old pin?". |
| T-65-BS-02 | Tampering | New scaffold added without pin map update | mitigate | "Pin maps cover all 7 scaffold keys" assertion fails loudly — defense-in-depth catches the omission. |
| T-65-DG-SC | Tampering | npm packages used in test | mitigate | Test uses only `vitest`, `node:fs`, `node:path`, `node:url`, `node:crypto` — all pre-existing pinned deps. No new npm packages introduced. |
</threat_model>

<verification>
- `npx vitest run tests/unit/error-class-enumeration-drift.test.js tests/unit/fix-prompt-builder-byte-stability.test.js --reporter=verbose` exits 0.
- The full vitest suite still passes: `npx vitest run --reporter=dot` exits 0 (regression-free).
- The two new test files together exercise 7 scaffolds × 5 sites + 7 sha256 pins + sanity/helper tests = ~50 individual assertions.
- `grep -c "PROMPT_SCAFFOLDS" tests/unit/error-class-enumeration-drift.test.js` returns ≥ 1 (test reads the registry).
- `grep -c "createHash" tests/unit/fix-prompt-builder-byte-stability.test.js` returns ≥ 1.
- Drift-guard test failure-message format honored: `grep -c 'is in PROMPT_SCAFFOLDS but missing from' tests/unit/error-class-enumeration-drift.test.js` returns ≥ 1.
</verification>

<success_criteria>
- Two new test files exist under `tests/unit/`, both exit 0 when run individually.
- Drift-guard test parameterizes over `Object.keys(PROMPT_SCAFFOLDS)` (the 7 scaffold-supported classes) and asserts presence at all 5 canonical enumeration sites.
- Byte-stability test pins sha256 digests for all 7 scaffolds; pin maps are `Object.freeze`'d; coverage-assertion guards against future scaffold additions without pin updates.
- All sha256 digests in the byte-stability test match the digests produced from HEAD-state (the 5 pre-existing scaffolds are byte-unchanged through Phase 65; the 2 new scaffolds establish the Phase 65 baseline).
- No source files modified; this plan is test-only.
- No new npm dependencies.
</success_criteria>

<output>
Create `.planning/phases/65-expanded-fix-scaffolds/65-02-SUMMARY.md` when done. Summary MUST record:
- Number of `it.each` invocations generated by the parameterized drift-guard tests (expected: 35 = 7 scaffolds × 5 sites).
- Number of sha256 pins in the byte-stability test (expected: 7 = 5 Phase 45 baseline + 2 Phase 65 baseline).
- Confirmation that all 7 sha256 digests match the values from Plan 01's 65-01-SUMMARY.md.
- Verification line: `npx vitest run --reporter=dot` exit code (expected: 0).
- Note any pre-existing Vitest failures (e.g., llm-ledger Test 48, v40-verifier-gate-yaml V2 — known from Phase 53/54/55 closures) — these are NOT Phase 65 regressions and should be flagged as unchanged-from-baseline.
</output>
</content>
</invoke>