---
phase: 01-worker-route-kv-schema-privacy-compliance-groundwork
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - store-assets/store-listing.md
  - docs/privacy/index.html
  - worker/src/index.js
  - worker/tests/report-route.test.js
autonomous: true
requirements: [PRIV-03, PRIV-04, LIMIT-01]
must_haves:
  truths:
    - "An operator following the store-listing.md Submission Checklist files a TRUE CWS data-use declaration (Website Content checked), consistent with the updated Subsection 4 body"
    - "The privacy policy has no internal contradiction: 'Information We Collect' and 'Data Sharing' are qualified for voluntary bug reports, and the revision date reflects the June 2026 update"
    - "duplicate_count goes 0 -> 1 on the first duplicate (not 0 -> 2), and a test asserts the exact value"
    - "Legitimate scrollY/viewportWidth/viewportHeight value of 0 is preserved in the KV record (not coerced to null)"
  artifacts:
    - path: "store-assets/store-listing.md"
      provides: "Internally consistent CWS data-use declaration (checklist + quick reference + subsection 4 all agree)"
      contains: "Website Content (voluntary bug reports only)"
    - path: "docs/privacy/index.html"
      provides: "Reconciled privacy policy with qualified collection/sharing claims and June 2026 date"
      contains: "June 2026"
    - path: "worker/src/index.js"
      provides: "Correct duplicate_count semantics and nullish-coalescing for numeric diagnostic fields"
      contains: "duplicate_count ?? 0"
    - path: "worker/tests/report-route.test.js"
      provides: "Exact-value duplicate_count assertion"
      contains: "toBe(1)"
  key_links:
    - from: "store-assets/store-listing.md Submission Checklist (line ~157)"
      to: "store-assets/store-listing.md Subsection 4 body (line ~128)"
      via: "both must instruct checking Website Content"
      pattern: "Check \\*\\*Website Content\\*\\*"
    - from: "docs/privacy/index.html Information We Collect"
      to: "docs/privacy/index.html Bug Report Feature section"
      via: "qualified carve-out wording consistent across both sections"
      pattern: "During normal citation use"
    - from: "worker/src/index.js checkAndHandleDuplication()"
      to: "worker/src/report-schema.md duplicate_count contract"
      via: "?? 0 increment so count matches 'initialized to 0, incremented on each dup'"
      pattern: "duplicate_count \\?\\? 0"
---

<objective>
Close the four verification gaps that block Phase 1 completion (status: gaps_found, 11/15). Two are BLOCKERS — internally contradictory compliance documents that, if followed, produce a false Chrome Web Store data-use declaration (CR-02) and an internally inconsistent privacy policy (WR-07/WR-08). Two are cheap, in-scope correctness fixes to the Worker that strengthen already-verified truths (WR-01 duplicate_count off-by-one, WR-03 falsy-zero coercion).

Purpose: A "privacy compliance groundwork" phase cannot ship with compliance documents that instruct the operator to file a false declaration or that contradict themselves. The two doc blockers directly defeat the phase's stated purpose. The two worker fixes correct telemetry accuracy and diagnostic-data fidelity at near-zero cost while the relevant code is already in context.

Output: Reconciled store-listing.md and privacy policy; corrected duplicate_count semantics with an exact-value test; nullish-coalescing for numeric diagnostic fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-worker-route-kv-schema-privacy-compliance-groundwork/01-VERIFICATION.md
@.planning/phases/01-worker-route-kv-schema-privacy-compliance-groundwork/01-REVIEW.md

<out_of_scope>
The following review findings are deliberately NOT in this plan — do not touch them:

- WR-06 (manifest required-vs-optional `data_collection_permissions`): a maintainer policy/compliance judgment call. The implementation matches PRIV-01 as written. Verification report marks this "Human Verification Required." Leave src/manifest.firefox.json unchanged.
- CR-01 (patentNumber/patentUrl format validation): a threat-model scope decision flagged "Human Verification Required" (PROXY_TOKEN is "friction not security" per D-01). Do not add regex validation in this plan.
- PRIV-05 (web-ext lint): deferred to Phase 5 UAT-04 per the plan's explicit allowance. No Firefox build is available here.
- WR-02 (test-mode does not suppress rl:{ip} / dedup KV writes): threads an isTestMode flag through multiple paths; risk of destabilizing the 22 passing tests. Deferred to Phase 2 planning awareness.
- WR-10 (tests make real outbound network calls): the fetchMock refactor touches every report test and risks destabilizing the green suite. Deferred to Phase 2.
- WR-04, WR-05, IN-01..IN-06: out of scope for this gap-closure pass (Phase 2 awareness items).

If you believe any out-of-scope item is trivially safe and want it included, STOP and surface it — do not silently expand scope.
</out_of_scope>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile store-listing.md CWS data-use declaration (BLOCKER CR-02 + WR-09)</name>
  <files>store-assets/store-listing.md</files>
  <read_first>
    - store-assets/store-listing.md (read the WHOLE file — Subsection 4 body at ~line 128, the Submission Checklist at ~line 157, and the Quick Reference table at ~line 176 must all be made to agree; current line numbers may have shifted, locate by content)
    - .planning/phases/01-worker-route-kv-schema-privacy-compliance-groundwork/01-VERIFICATION.md (gap #1 `missing:` entries hold the exact target wordings)
  </read_first>
  <action>
    Make four edits so the data-use declaration is internally consistent (the bug report feature ships to ALL users, so Website Content collection is unconditional, not conditional):

    1. Submission Checklist item currently reading "**Privacy tab — Data use:** All data type checkboxes unchecked; all three certification statements checked" — replace its text with exactly: "**Privacy tab — Data use:** Check **Website Content** (bug report selection text); leave all other data type checkboxes unchecked; all three certification statements checked."

    2. Quick Reference table row currently reading "| Data collected | None |" — replace with exactly: "| Data collected | Website Content (voluntary bug reports only) |".

    3. Subsection 4 "Data types collected" sentence currently reading "If your users may submit voluntary bug reports, check **Website Content**" — reword the conditional to the definitive: "The extension includes a voluntary bug report feature, so check **Website Content**". Keep the rest of that sentence (the "patent selection text (up to approximately 200 characters) may be included..." clause) intact.

    4. (WR-09) In the "Description (plain text — no HTML)" block, the line containing the HTML anchor `<a href="https://tonyrowles.github.io/patent-cite-tool/privacy">privacy policy</a>` must be replaced with a bare URL since the CWS description field does not render HTML. New ending: "...submit an optional diagnostic report to help the developer fix it. Full details: https://tonyrowles.github.io/patent-cite-tool/privacy".

    Do not alter Sections 1-2, the permission justifications table, or the certification statements (already correct).
  </action>
  <verify>
    <automated>cd /home/fatduck/patent-cite-tool && grep -F 'Check **Website Content** (bug report selection text)' store-assets/store-listing.md && grep -F '| Data collected | Website Content (voluntary bug reports only) |' store-assets/store-listing.md && grep -F 'The extension includes a voluntary bug report feature, so check **Website Content**' store-assets/store-listing.md && ! grep -qF 'All data type checkboxes unchecked' store-assets/store-listing.md && ! grep -qF '| Data collected | None |' store-assets/store-listing.md && ! grep -qiE '<a href=' store-assets/store-listing.md</automated>
  </verify>
  <acceptance_criteria>
    - The Submission Checklist Data-use item contains the literal string "Check **Website Content** (bug report selection text)".
    - The Quick Reference table contains the literal row "| Data collected | Website Content (voluntary bug reports only) |".
    - Subsection 4 contains the literal string "The extension includes a voluntary bug report feature, so check **Website Content**".
    - The string "All data type checkboxes unchecked" appears NOWHERE in the file.
    - The string "| Data collected | None |" appears NOWHERE in the file.
    - No `<a href=` HTML anchor appears anywhere in the file; the description references the privacy policy as a bare URL "https://tonyrowles.github.io/patent-cite-tool/privacy".
  </acceptance_criteria>
  <done>store-listing.md is internally consistent: every place that describes the CWS data-use declaration instructs checking Website Content; the plain-text description contains no HTML.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile privacy policy collection/sharing claims and bump revision date (BLOCKER WR-07/WR-08)</name>
  <files>docs/privacy/index.html</files>
  <read_first>
    - docs/privacy/index.html (read the WHOLE file — the carve-out at ~line 94, "Information We Collect" at ~lines 96-104, "Data Sharing" at ~line 124-125, the "Last updated" line at ~line 92, and the existing "Bug Report Feature" section at ~lines 144-165 must all agree; locate by content, line numbers may shift)
    - .planning/phases/01-worker-route-kv-schema-privacy-compliance-groundwork/01-VERIFICATION.md (gap #2 `missing:` entries hold the target wordings)
  </read_first>
  <action>
    Make three edits so the absolute "no collection / no sharing" claims are qualified for the voluntary bug report feature and the revision date reflects the change:

    1. "Last updated" paragraph currently reading "Last updated: March 2026" — change to exactly "Last updated: June 2026". (The policy itself states the date reflects the most recent revision; a new data-collection section was added, so the date must move.)

    2. "Information We Collect" section — the intro currently reads "We collect <strong>no personal information</strong> of any kind. Specifically:" followed by a `<ul>` whose first `<li>` says "No personal information is collected or stored". Qualify the absolute claim to scope it to normal citation use. Replace the intro paragraph with: "During normal citation use, we collect <strong>no personal information</strong> of any kind. If you voluntarily submit a bug report, the data described in the Bug Report Feature section below is collected. During normal citation use, specifically:". Replace the first `<li>` "No personal information is collected or stored" with "No personal information is collected or stored during normal citation use". Leave the other four `<li>` items (browsing history, analytics, account, cookies) unchanged.

    3. "Data Sharing" section currently reading "We do not sell, trade, or transfer any data to third parties. No data is collected that could be shared." — replace with: "We do not sell or trade your data. Reports submitted through the voluntary Bug Report Feature are stored on Cloudflare (KV) and a notification containing report fields is delivered via Discord. Cloudflare and Discord act as service providers (processors) for that feature; data is not sold or shared with any other party. Outside the Bug Report Feature, no data is collected that could be shared."

    Do not alter the "Services Used", "Permissions Used", "Changes to This Policy", "Contact", or "Bug Report Feature" sections (the Bug Report Feature section is already correct and is the source of truth these edits must align to). Note: WR-08's "Destinations" wording inside the Bug Report Feature section ("No data is shared with third parties") is acceptable to leave as-is because it is immediately scoped to the maintainer-only channel; the Data Sharing edit above resolves the top-level contradiction. If you choose to also soften that one sentence for full consistency, change "These are the only destinations. No data is shared with third parties." to "These are the only destinations; Cloudflare and Discord act as service providers for this feature." — this is optional polish, not required by the gate.
  </action>
  <verify>
    <automated>cd /home/fatduck/patent-cite-tool && grep -F 'Last updated: June 2026' docs/privacy/index.html && grep -F 'During normal citation use, we collect' docs/privacy/index.html && grep -F 'service providers (processors)' docs/privacy/index.html && ! grep -qF 'Last updated: March 2026' docs/privacy/index.html && ! grep -qF 'No data is collected that could be shared.</p>' docs/privacy/index.html</automated>
  </verify>
  <acceptance_criteria>
    - The file contains the literal string "Last updated: June 2026" and does NOT contain "Last updated: March 2026".
    - "Information We Collect" intro contains the literal string "During normal citation use, we collect".
    - "Data Sharing" section contains the literal string "service providers (processors)".
    - The standalone absolute claim "No data is collected that could be shared." no longer ends the Data Sharing paragraph (it may only appear in the qualified form "Outside the Bug Report Feature, no data is collected that could be shared.").
    - The "Bug Report Feature" section's field-by-field disclosure list is unchanged.
  </acceptance_criteria>
  <done>The privacy policy has no internal contradiction: collection and sharing claims are scoped to normal citation use, the bug-report destinations are named as processors, and the revision date is June 2026.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix duplicate_count off-by-one and falsy-zero coercion in the Worker (WR-01 + WR-03)</name>
  <files>worker/src/index.js, worker/tests/report-route.test.js</files>
  <read_first>
    - worker/src/index.js (read buildKvRecord() at ~lines 219-243 for the falsy-coercion fields, and checkAndHandleDuplication() at ~lines 277-303 for the duplicate_count increment)
    - worker/tests/report-route.test.js (read the dedup describe block at ~lines 159-212; note PATENT_DEDUP is reused across multiple `it` blocks so the existing count is order-dependent — the new exact-count test MUST use its own fresh patent number and its own IP to be deterministic)
    - .planning/phases/01-worker-route-kv-schema-privacy-compliance-groundwork/01-REVIEW.md (WR-01 and WR-03 hold the exact one-line fixes)
  </read_first>
  <behavior>
    - After a single first-then-duplicate sequence for a brand-new fingerprint, the stored record's duplicate_count is exactly 1 (was 2 due to `|| 1`).
    - A report body with scrollY: 0 (and viewportWidth: 0 / viewportHeight: 0) stores 0 in the KV record, not null.
    - All 22 pre-existing tests still pass.
  </behavior>
  <action>
    WR-01 (duplicate_count off-by-one): In checkAndHandleDuplication(), the increment line currently reads `existing.duplicate_count = (existing.duplicate_count || 1) + 1;`. Because buildKvRecord initializes duplicate_count to 0 and 0 is falsy, `0 || 1` yields 1, so the first duplicate jumps to 2. Change it to use nullish coalescing: `existing.duplicate_count = (existing.duplicate_count ?? 0) + 1;` so the first duplicate goes 0 -> 1, matching the report-schema.md contract ("initialized to 0; incremented on each subsequent identical submission").

    WR-03 (falsy-zero coercion): In buildKvRecord(), three numeric diagnostic fields use `||` which destroys a legitimate 0. Change exactly these three lines to use `??`:
    - `scrollY: body.scrollY || null,` -> `scrollY: body.scrollY ?? null,`
    - `viewportWidth: body.viewportWidth || null,` -> `viewportWidth: body.viewportWidth ?? null,`
    - `viewportHeight: body.viewportHeight || null,` -> `viewportHeight: body.viewportHeight ?? null,`
    Do NOT change the `||` on the string fields (selectionText, returnedCitation, browser, os, etc.) — for those, empty-string-to-null is the existing intended behavior and changing them is out of scope.

    Test tightening (WR-01): Add a NEW deterministic `it` block in the dedup describe block that proves the exact count. Use a brand-new patent number constant (e.g. add `const PATENT_DEDUP_EXACT = '12505009';`) and a brand-new IP constant (e.g. `const IP_DEDUP_EXACT = '10.0.0.9';`) so it does not collide with PATENT_DEDUP's accumulated state. The test sends exactly two identical requests (first -> 201 deduped:false, second -> 200 deduped:true), then lists `report:{fingerprint}:` keys, gets the single record, and asserts `expect(record.duplicate_count).toBe(1);`. Leave the existing loose `toBeGreaterThanOrEqual(2)` test in place OR retarget it — prefer adding the new exact test alongside it so you do not perturb the existing order-dependent assertions; if you retarget the existing one, ensure it remains green given the shared PATENT_DEDUP state.
  </action>
  <verify>
    <automated>cd /home/fatduck/patent-cite-tool/worker && grep -F 'existing.duplicate_count = (existing.duplicate_count ?? 0) + 1;' src/index.js && grep -F 'scrollY:           body.scrollY ?? null,' src/index.js && grep -F 'toBe(1)' tests/report-route.test.js && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/index.js contains the literal "existing.duplicate_count = (existing.duplicate_count ?? 0) + 1;" and does NOT contain "(existing.duplicate_count || 1)".
    - src/index.js contains "body.scrollY ?? null", "body.viewportWidth ?? null", and "body.viewportHeight ?? null"; the string fields still use `|| null`.
    - report-route.test.js contains a new exact-count assertion `toBe(1)` for duplicate_count on a fresh, dedicated patent number + IP.
    - `cd worker && npm test` reports all tests passing (>= 23 tests, up from 22; zero failures).
  </acceptance_criteria>
  <done>duplicate_count increments 0 -> 1 on first dup with an exact-value test guarding it; numeric diagnostic 0-values survive into KV; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> Chrome Web Store / AMO | Compliance documents instruct a human what to declare; a wrong instruction crosses into a false regulatory declaration |
| extension client -> Worker /report | Untrusted JSON body crosses into buildKvRecord() (already gated by Bearer auth + allowlist; this plan only touches null-coalescing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Repudiation | store-listing.md / privacy policy compliance claims | mitigate | Reconcile checklist, quick reference, and privacy sections so the operator cannot unknowingly file a false data-use declaration (Tasks 1 & 2) — removes the contradiction that is itself the compliance risk |
| T-01-10 | Tampering | buildKvRecord falsy coercion (scrollY=0) | accept-after-fix | WR-03 switches numeric fields to `??`; the data is non-sensitive diagnostic telemetry and already capped/allowlisted upstream — fixing fidelity, no new attack surface introduced |
| T-01-11 | Information Disclosure | privacy policy understating collection | mitigate | Task 2 explicitly names Cloudflare KV + Discord as processors and qualifies the "no collection" claim — closes the disclosure-completeness gap WR-07/WR-08 |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan — all edits are to existing committed files; no new dependencies. No legitimacy checkpoint required. |
</threat_model>

<verification>
Phase-level re-verification after this plan should flip the two FAILED/PARTIAL truths (14 and 15 in 01-VERIFICATION.md) to VERIFIED:

- Truth 14 (store-listing data-use declaration): grep confirms checklist, quick reference, and Subsection 4 all instruct Website Content; no "All data type checkboxes unchecked" / "Data collected | None" remains.
- Truth 15 (privacy policy qualified): grep confirms "During normal citation use" qualifier, "service providers (processors)" sharing disclosure, and "Last updated: June 2026".
- LIMIT-01 telemetry (WR-01): `cd worker && npm test` green with a new exact `toBe(1)` duplicate_count assertion.

Run order is irrelevant — the three tasks touch disjoint files and have no interdependencies.
</verification>

<success_criteria>
- store-assets/store-listing.md: Submission Checklist + Quick Reference + Subsection 4 all instruct checking Website Content; no HTML anchor in the plain-text description.
- docs/privacy/index.html: Information We Collect and Data Sharing are scoped to normal citation use and name Cloudflare/Discord as processors; revision date is June 2026.
- worker/src/index.js: duplicate_count uses `?? 0`; scrollY/viewportWidth/viewportHeight use `??`.
- worker/tests/report-route.test.js: exact `toBe(1)` duplicate_count test added; full suite passes (>= 23 tests, 0 failures).
</success_criteria>

<output>
Create `.planning/phases/01-worker-route-kv-schema-privacy-compliance-groundwork/01-03-SUMMARY.md` when done.
</output>
