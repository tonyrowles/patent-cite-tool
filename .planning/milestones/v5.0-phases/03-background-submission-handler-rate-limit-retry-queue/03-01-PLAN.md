---
phase: 03-background-submission-handler-rate-limit-retry-queue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/report-transport.js
autonomous: true
requirements: [XPORT-05, LIMIT-03, QUEUE-01, QUEUE-02, QUEUE-03, QUEUE-04]
must_haves:
  truths:
    - "D-01: backoff retries fire via in-session setTimeout (BACKOFF_MS = [2000, 8000, 30000]); zero references to chrome.alarms anywhere in the helper"
    - "D-03: each bugReportQueue entry persists attemptCount and nextAttemptAt so backoff progresses across drains and is NOT reset"
    - "D-04: 4xx (except 429) permanent-drops the entry without retry; 429/5xx/network-throw re-queue with backoff; after attemptCount reaches 3 the entry is dropped silently"
    - "D-05: checkAndUpdateRateLimit runs FIRST inside submitReport — before any bugReportQueue write and before any fetch; on ceiling the helper returns { rateLimited: true } with no queue write and no fetch"
    - "D-06/D-08: submitReport resolves a structured result { ok, queued, fingerprint, rateLimited, dropped } — the superset of XPORT-05's { ok, queued, fingerprint }"
    - "QUEUE-01: the queue entry is written to chrome.storage.local under bugReportQueue and the set() awaited BEFORE fetch() is called; removed atomically on a 200/201 success"
    - "QUEUE-02: queue cap is 20 (oldest by enqueuedAt dropped when full) and 7-day TTL entries are pruned; backoff schedule is 2s/8s/30s, max 3 attempts"
    - "LIMIT-03: sliding window of max 5 timestamps over 10 minutes under bugReportRateLimitWindow, pruned on each attempt; a non-blocked submit appends its timestamp"
  artifacts:
    - path: "src/shared/report-transport.js"
      provides: "submitReport, drainQueueOnce, checkAndUpdateRateLimit, _resetMutex exports; rate-limit + disk-first queue + retry/backoff/drain logic"
      exports: ["submitReport", "drainQueueOnce"]
      min_lines: 120
  key_links:
    - from: "src/shared/report-transport.js"
      to: "WORKER_REPORT_URL (src/shared/constants.js)"
      via: "fetch POST with Authorization: Bearer PROXY_TOKEN"
      pattern: "fetch\\(\\s*WORKER_REPORT_URL"
    - from: "src/shared/report-transport.js"
      to: "chrome.storage.local bugReportQueue"
      via: "read-modify-write before/after fetch"
      pattern: "bugReportQueue"
---

<objective>
Author the shared transport helper `src/shared/report-transport.js` — a pure ES module (no top-level `chrome.*`) that both backgrounds import. It owns ALL extension-side report logic: client-side sliding-window rate limit (LIMIT-03), disk-first queue persistence (QUEUE-01), 3-attempt exponential backoff retry (QUEUE-02), non-retryable failure handling (QUEUE-03), and the structured return contract the content-script caller maps to toasts (QUEUE-04, D-06/D-08).

Purpose: XPORT-05 mandates an IDENTICAL dispatch shape in Chrome SW and Firefox background. Centralising the logic in one shared module is the DRY mechanism — Plan 03-03 wires this module into both backgrounds; Plan 03-02 unit-tests this module directly. This plan ships the contract and the logic; it does NOT touch the backgrounds or any test file.

Output: `src/shared/report-transport.js` with exports `submitReport`, `drainQueueOnce`, `checkAndUpdateRateLimit`, and a test-only `_resetMutex`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-CONTEXT.md
@.planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-RESEARCH.md
@.planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-PATTERNS.md

<interfaces>
<!-- Phase 2 exports (VERIFIED present in src/shared/constants.js) the helper consumes: -->
From src/shared/constants.js:
  MSG.SUBMIT_REPORT === 'submit-report'   (line 26)
  WORKER_REPORT_URL === 'https://pct.tonyrowles.com/report'   (line 55)
  REPORT_CATEGORIES   (frozen array, line 48)

<!-- PROXY_TOKEN is a LOCAL const, NOT imported from constants.js — mirror offscreen.js:24 / pdf-pipeline.js:23 -->
PROXY_TOKEN literal = '4509b9943f831fb140eb0c3a7304f23cc6f72e41b5e5f8c800a42e94f09cadbe'

<!-- Result contract this module must resolve (D-06/D-08), consumed by Phase 4's dialog: -->
submitReport(payload) -> Promise<{ ok, queued, fingerprint, rateLimited, dropped }>
  - rate-limited:      { ok:false, queued:false, fingerprint:null, rateLimited:true,  dropped:false }
  - success (200/201): { ok:true,  queued:false, fingerprint:<from-response>, rateLimited:false, dropped:false }
  - queued-for-retry:  { ok:false, queued:true,  fingerprint:null, rateLimited:false, dropped:false }
  - permanent-drop:    { ok:false, queued:false, fingerprint:null, rateLimited:false, dropped:true }
drainQueueOnce() -> Promise<void>  (silent; no caller, no return value used — D-07)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author rate-limit + disk-first enqueue + submit path</name>
  <files>src/shared/report-transport.js</files>
  <read_first>
    - src/shared/report-payload-builder.js (lines 1-12: the `import { ... } from './constants.js'` shared-module header pattern; module never references chrome.* at module scope)
    - src/offscreen/offscreen.js:24,42-44,246-247 (PROXY_TOKEN local-const; chrome.storage.local referenced ONLY inside async function bodies; Bearer-header fetch shape)
    - src/firefox/pdf-pipeline.js:23,428-431 (POST with Authorization + Content-Type headers + JSON body)
    - src/background/service-worker.js:249-257 (canonical chrome.storage.local get → modify → set read-modify-write pair)
    - .planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-RESEARCH.md (Code Examples: rate-limit pruning, queue cap-20 eviction, backoff array)
    - .planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-CONTEXT.md (decisions D-04, D-05, D-08)
  </read_first>
  <action>
    Create `src/shared/report-transport.js` as an ES module. Import `{ MSG, WORKER_REPORT_URL }` from `./constants.js`. Define module-scope constants: `PROXY_TOKEN` as a local const (the literal in interfaces, mirroring offscreen.js:24 — do NOT import from constants.js); `BACKOFF_MS = [2000, 8000, 30000]`; `MAX_ATTEMPTS = 3`; `RATE_LIMIT_MAX = 5`; `RATE_LIMIT_WINDOW_MS = 600000`; `QUEUE_KEY = 'bugReportQueue'`; `RATE_KEY = 'bugReportRateLimitWindow'`; `QUEUE_CAP = 20`; `QUEUE_TTL_MS = 604800000`. NEVER write `const storage = chrome.storage.local` at module scope — reference `chrome.storage.local` only inside async function bodies (chrome-stub is injected at runtime, not parse time).

    Export `async function checkAndUpdateRateLimit()`: read `RATE_KEY`, prune timestamps older than `RATE_LIMIT_WINDOW_MS`, return `false` (blocked) if the pruned window length is >= `RATE_LIMIT_MAX`; otherwise push `Date.now()`, write back, return `true` (per D-05 / LIMIT-03 / RESEARCH Code Example).

    Export `async function submitReport(payload)` enforcing the D-05 ordering invariant (rate-limit check → disk write → fetch):
    1. Guard: if `payload` is null/undefined, resolve `{ ok:false, queued:false, fingerprint:null, rateLimited:false, dropped:true }` (V5 defensive guard per RESEARCH Security Domain; do NOT re-validate fields — that is the Phase 2 builder's job).
    2. Call `checkAndUpdateRateLimit()`. If it returns false → resolve `{ ok:false, queued:false, fingerprint:null, rateLimited:true, dropped:false }` with NO queue write and NO fetch.
    3. Build the entry `{ payload, attemptCount: 0, nextAttemptAt: 0, enqueuedAt: Date.now() }`. Read `QUEUE_KEY`, prune entries older than `QUEUE_TTL_MS`, append the entry, and if length > `QUEUE_CAP` drop oldest by `enqueuedAt` (sort ascending, slice last `QUEUE_CAP`). `await chrome.storage.local.set(...)` — this set MUST complete before any fetch (QUEUE-01 disk-first; this is the Pitfall 6 / Pitfall 1 mitigation).
    4. Call the shared `attemptEntry(entry)` helper (Task 2). Map its outcome to the return contract: success → `{ ok:true, queued:false, fingerprint, rateLimited:false, dropped:false }`; queued-for-retry → `{ ok:false, queued:true, fingerprint:null, rateLimited:false, dropped:false }`; permanent-drop (4xx-except-429 or attempts exhausted) → `{ ok:false, queued:false, fingerprint:null, rateLimited:false, dropped:true }`.

    Reference `chrome` only inside function bodies. Errors in fetch are caught inside `attemptEntry`, never thrown out of `submitReport`.
  </action>
  <behavior>
    - Rate-limit ceiling: with 5 timestamps already inside the window in storage, submitReport resolves rateLimited:true; bugReportQueue is never written; fetch is never called.
    - Disk-first: when fetch is stubbed to throw, bugReportQueue still contains the entry after submitReport resolves (the set() ran before fetch).
    - Window pruning: a timestamp older than 10 minutes does not count toward the ceiling; a 6th submit fails only when 5 are within the window.
    - Null payload: returns dropped:true without touching storage.
  </behavior>
  <verify>
    <automated>node -e "import('./src/shared/report-transport.js').then(m => { if (typeof m.submitReport !== 'function' || typeof m.checkAndUpdateRateLimit !== 'function') process.exit(1); console.log('exports ok'); })"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -nE "await chrome\.storage\.local\.set\(" src/shared/report-transport.js` shows the queue set() appears in submitReport BEFORE the fetch call (disk-first ordering); manual read confirms fetch is not invoked on the rate-limited branch.
    - Source assertion: `grep -c "chrome.alarms" src/shared/report-transport.js` returns 0 (D-01).
    - Source assertion: `grep -nE "const (BACKOFF_MS|RATE_LIMIT_MAX|RATE_LIMIT_WINDOW_MS|QUEUE_CAP|QUEUE_TTL_MS)" src/shared/report-transport.js` shows literals 2000/8000/30000, 5, 600000, 20, 604800000.
    - Source assertion: `grep -n "const PROXY_TOKEN" src/shared/report-transport.js` returns 1 (local const, not imported) and `grep -c "import.*PROXY_TOKEN" src/shared/report-transport.js` returns 0.
    - Behavior: module imports without a `ReferenceError: chrome is not defined` (no chrome.* at module scope) — proven by the node -e verify command exiting 0.
  </acceptance_criteria>
  <done>submitReport and checkAndUpdateRateLimit exported; rate-limit-first ordering and disk-first enqueue implemented; module parses in Node with no top-level chrome reference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author attemptEntry, retry/backoff, drain + mutex</name>
  <files>src/shared/report-transport.js</files>
  <read_first>
    - src/background/service-worker.js:49-78 (creatingOffscreen mutex: check-in-flight → assign → await → null-out-in-finally — copy this exact shape for drainingQueue)
    - src/background/service-worker.js:443-451,261-262 (try/catch fire-and-forget error idiom; network throw → retry path)
    - src/firefox/pdf-pipeline.js:428-431 (Bearer POST shape the fetch must use)
    - .planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-RESEARCH.md (Architecture Diagram drain path; Pattern 2 mutex; Code Examples backoff/onStartup)
    - .planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-CONTEXT.md (decisions D-01, D-02, D-03, D-04, D-07)
  </read_first>
  <action>
    In the same file, add the fetch/retry/drain machinery. Reference `chrome` and `fetch` only inside function bodies.

    `async function attemptEntry(entry)` returns one of `'success' | 'queued' | 'dropped'` plus a `fingerprint` on success:
    - POST to `WORKER_REPORT_URL` with headers `{ 'Authorization': \`Bearer ${PROXY_TOKEN}\`, 'Content-Type': 'application/json' }` and `body: JSON.stringify(entry.payload)` (mirror pdf-pipeline.js:428-431). The Bearer token MUST NOT be logged or returned to the caller.
    - On `resp.ok` (200/201): remove this entry from `bugReportQueue` (read-modify-write, filter out by identity/enqueuedAt), parse `fingerprint` from the JSON body, return `{ outcome:'success', fingerprint }`. NOTE per Phase 1 D-02: a server-side dedup hit returns HTTP 200 — treat it as success, never a 4xx drop.
    - On a 4xx that is NOT 429 (`resp.status >= 400 && resp.status < 500 && resp.status !== 429`): remove the entry permanently (no retry per D-04/QUEUE-03), return `{ outcome:'dropped' }`.
    - On 429, 5xx, or a thrown fetch error (network): increment `entry.attemptCount` and set `entry.nextAttemptAt = Date.now() + BACKOFF_MS[entry.attemptCount - 1] ?? last`. If `attemptCount >= MAX_ATTEMPTS` → remove the entry and return `{ outcome:'dropped' }` (silent drop, D-04). Otherwise persist the updated entry back into `bugReportQueue`, schedule `setTimeout(() => drainQueueOnce(), backoffMs)` (D-01 in-session retry), and return `{ outcome:'queued' }`.

    Export `async function drainQueueOnce()` guarded by a module-level `let drainingQueue = null` mutex (copy service-worker.js:49-78 shape: if `drainingQueue` is set, return it; else assign `drainingQueue = _doDrain()`, await in try, null-out in finally). `_doDrain()`: read `QUEUE_KEY`; prune TTL-expired entries (`enqueuedAt` older than `QUEUE_TTL_MS`) and write the pruned queue back; for each entry whose `nextAttemptAt <= Date.now()`, call `attemptEntry(entry)` honoring its persisted `attemptCount`/`nextAttemptAt` (D-03 — backoff is NOT reset). Drain is SILENT (D-07): it returns nothing the caller uses and renders no toast.

    Export `function _resetMutex()` that sets `drainingQueue = null` — test-only, used by Plan 03-02 to simulate SW death (D-09).
  </action>
  <behavior>
    - 201 success removes the entry from bugReportQueue and yields a fingerprint.
    - 400 (non-429 4xx) drops the entry with no attemptCount increment and no setTimeout.
    - 503 increments attemptCount, sets nextAttemptAt to now+2000 on first failure, persists the entry, returns 'queued'.
    - After the 3rd failure (attemptCount reaches 3) the entry is removed silently.
    - drainQueueOnce honors a persisted nextAttemptAt: an entry with nextAttemptAt in the future is skipped this drain (backoff not reset).
    - Concurrent drainQueueOnce calls share one in-flight promise (mutex), so the queue is not double-processed.
  </behavior>
  <verify>
    <automated>node -e "import('./src/shared/report-transport.js').then(m => { if (typeof m.drainQueueOnce !== 'function' || typeof m._resetMutex !== 'function') process.exit(1); console.log('drain exports ok'); })"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "Bearer" src/shared/report-transport.js` shows the Authorization header uses `Bearer ${PROXY_TOKEN}`; `grep -c "console.*PROXY_TOKEN\|console.*Bearer" src/shared/report-transport.js` returns 0 (token never logged).
    - Source assertion: `grep -n "drainingQueue" src/shared/report-transport.js` shows a module-level mutex with the check-in-flight / assign / finally-null pattern.
    - Source assertion: `grep -n "setTimeout" src/shared/report-transport.js` shows backoff scheduling; `grep -c "setInterval\|chrome.alarms" src/shared/report-transport.js` returns 0.
    - Source assertion: the 4xx branch condition `status !== 429` is present (`grep -n "429" src/shared/report-transport.js` shows the except-429 carve-out).
    - Behavior: the node -e verify command exits 0 (drainQueueOnce and _resetMutex exported).
  </acceptance_criteria>
  <done>attemptEntry implements the D-04 4xx/429/5xx/network policy with 2s/8s/30s backoff capped at 3 attempts; drainQueueOnce is mutex-guarded, TTL-pruning, nextAttemptAt-honoring, and silent; _resetMutex exported for tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content script → background (sendMessage) | payload originates in the content-script context; treated as same-extension input (browser-enforced), passed to fetch as-is |
| background → Worker (cross-origin fetch) | carries the Bearer PROXY_TOKEN secret; the only authenticated hop |
| background → chrome.storage.local | local-only queue + rate-limit window; no cross-origin reach |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | PROXY_TOKEN in fetch headers | mitigate | Token is a local const used only in the Authorization header; never `console.log`'d, never placed in the return contract, never returned to content scripts (acceptance_criteria grep guard) |
| T-03-02 | Tampering | payload forwarded to Worker | accept | Same-extension messages only (chrome.runtime.onMessage browser-enforced); the transport does NOT add ip/clientIp/userAgent — it forwards the Phase 2 builder's allowlisted payload verbatim (PAY-03 forbidden fields never reintroduced in transport) |
| T-03-03 | Denial of Service | bugReportQueue unbounded growth | mitigate | Queue cap 20 (oldest-drop), 7-day TTL pruning, 3-attempt max — all enforced in enqueue + drain |
| T-03-04 | Spoofing | rate-limit reset via extension restart | mitigate | bugReportRateLimitWindow persisted in chrome.storage.local (not in-memory), so restart does not reset the window. NOTE: this client-side limit is a self-throttle, NOT a server security control — the Worker's IP-keyed limit (Phase 1) is the actual abuse defense |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 3 installs ZERO packages (RESEARCH Package Legitimacy Audit: none evaluated); no install task exists in this plan |
</threat_model>

<verification>
- `node -e` import smoke for both tasks exits 0 (all four exports present).
- Static greps in acceptance_criteria all pass (disk-first ordering, no chrome.alarms, no token logging, except-429 carve-out, literal caps).
- Full behavioral proof is Plan 03-02's Vitest suite (this plan ships the logic; 03-02 proves SC1-SC4).
</verification>

<success_criteria>
- `src/shared/report-transport.js` exists, exports `submitReport`, `drainQueueOnce`, `checkAndUpdateRateLimit`, `_resetMutex`.
- Module imports in Node with no top-level `chrome` reference (no ReferenceError).
- Rate-limit-first ordering (D-05), disk-first enqueue (QUEUE-01), 2s/8s/30s × 3 backoff (QUEUE-02), 4xx-except-429 drop policy (QUEUE-03/D-04), and the `{ ok, queued, fingerprint, rateLimited, dropped }` contract (D-06/D-08) are all implemented.
- Zero `chrome.alarms`, zero `setInterval`, zero new npm dependencies.
</success_criteria>

<output>
Create `.planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-01-SUMMARY.md` when done
</output>
