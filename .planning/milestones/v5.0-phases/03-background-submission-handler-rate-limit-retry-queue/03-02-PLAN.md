---
phase: 03-background-submission-handler-rate-limit-retry-queue
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/unit/report-transport-chrome.test.js
  - tests/unit/report-transport-firefox.test.js
  - vitest.config.chrome.js
  - vitest.config.firefox.js
autonomous: true
requirements: [XPORT-06, LIMIT-03, QUEUE-01, QUEUE-02, QUEUE-03, QUEUE-04]
must_haves:
  truths:
    - "XPORT-06 / SC1: a Vitest static-grep assertion reads every file in src/content/ and asserts no `fetch(WORKER_REPORT_URL` (and no `fetch('https://pct.tonyrowles.com`) literal — passes"
    - "D-09: per-target Vitest (vitest.config.chrome.js + vitest.config.firefox.js) drives submitReport/drainQueueOnce directly with a stateful mocked chrome.storage.local and vi.useFakeTimers() for 2s/8s/30s backoff"
    - "D-09 / SC3: SW termination is simulated by calling _resetMutex() then re-invoking drainQueueOnce() against persisted storage; the queued report survives and is retried"
    - "SC2 / LIMIT-03: a test proves the 5th submit succeeds, the 6th is rateLimited (no fetch), and a 7th after the 10-minute window (advanced via fake timers) succeeds again"
    - "SC4 / QUEUE-03 / D-04: tests prove 4xx-except-429 permanent-drops, 5xx retries 3× on the 2s/8s/30s schedule then drops silently, and 429 retries with the same backoff"
    - "QUEUE-01: a test proves the entry is present in bugReportQueue when fetch throws (disk-first) and absent after a 201 success"
    - "QUEUE-02: tests prove cap-20 oldest-drop eviction and 7-day TTL pruning"
    - "D-10: NO live browser SW stop+restart here — termination is simulated in Node via _resetMutex + drain (live test deferred to Phase 5 UAT-05)"
  artifacts:
    - path: "tests/unit/report-transport-chrome.test.js"
      provides: "Vitest suite covering SC1-SC4, LIMIT-03, QUEUE-01..04 against the shared helper"
      contains: "submitReport"
    - path: "tests/unit/report-transport-firefox.test.js"
      provides: "Firefox-target Vitest suite (identical behavioral assertions per XPORT-05 identical-dispatch)"
      contains: "drainQueueOnce"
  key_links:
    - from: "vitest.config.chrome.js"
      to: "tests/unit/report-transport-chrome.test.js"
      via: "include array extension"
      pattern: "report-transport-chrome.test.js"
    - from: "vitest.config.firefox.js"
      to: "tests/unit/report-transport-firefox.test.js"
      via: "include array extension"
      pattern: "report-transport-firefox.test.js"
---

<objective>
Author the per-target Vitest suites that prove all four Phase 3 Success Criteria against the shared helper from Plan 03-01, and wire the new test files into both per-target Vitest configs.

Purpose: D-09 mandates per-target Vitest (`test:chrome` / `test:firefox`) driving the `MSG.SUBMIT_REPORT` logic directly — no UI, no live browser. The suites import `src/shared/report-transport.js` directly (NOT the background modules, which register top-level listeners on import). SW termination is SIMULATED (D-09/D-10): live stop+restart is deferred to Phase 5 UAT-05. The XPORT-06 static-grep guard (SC1) lives here too.

Output: `tests/unit/report-transport-chrome.test.js`, `tests/unit/report-transport-firefox.test.js`, and the `include`-array extensions to both per-target configs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-CONTEXT.md
@.planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-RESEARCH.md
@.planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-PATTERNS.md
@.planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-01-SUMMARY.md

<interfaces>
<!-- Exports from Plan 03-01 (src/shared/report-transport.js) under test: -->
submitReport(payload) -> Promise<{ ok, queued, fingerprint, rateLimited, dropped }>
drainQueueOnce() -> Promise<void>
checkAndUpdateRateLimit() -> Promise<boolean>
_resetMutex() -> void   (test-only; sets the drain mutex to null to simulate SW death)

<!-- Storage keys + constants the assertions reference: -->
QUEUE key: 'bugReportQueue'   RATE key: 'bugReportRateLimitWindow'
caps: 5/10min, queue cap 20, 7-day TTL, max 3 attempts, backoff 2s/8s/30s

<!-- vitest.config.chrome.js current include (lines 9-12) — APPEND only: -->
  'tests/unit/text-matcher.test.js',
  'tests/unit/shared-matching.test.js',
<!-- alias (lines 16-20) is scoped to /src\/shared\/matching\.js/ ONLY — it does NOT redirect
     constants.js or report-transport.js; no new alias needed. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Chrome-target Vitest suite (SC1-SC4)</name>
  <files>tests/unit/report-transport-chrome.test.js</files>
  <read_first>
    - tests/setup/chrome-stub.js:18-27 (bare vi.fn() base stubs for chrome.storage.local.get/.set — these are OVERRIDDEN per-test, never modified globally)
    - tests/unit/select-cron-cases.test.js:33-35 (vi.useFakeTimers()/vi.useRealTimers() in beforeEach/afterEach)
    - tests/unit/report-payload-builder.test.js:1-32 (file header + import-from-src/shared style) and :178-187 (static-grep via readFileSync + not.toMatch — the XPORT-06 guard pattern)
    - src/shared/report-transport.js (the module under test — exact export names + result-contract field names)
    - .planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-RESEARCH.md (Pattern 5 stateful storage mock; SW-termination simulation example; Code Examples)
    - .planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-CONTEXT.md (D-09, D-10)
  </read_first>
  <action>
    Create `tests/unit/report-transport-chrome.test.js`. Import `{ describe, it, expect, beforeEach, afterEach, vi }` from `vitest`, `{ readFileSync }` from `node:fs`, path/url helpers, and `{ submitReport, drainQueueOnce, checkAndUpdateRateLimit, _resetMutex }` from `'../../src/shared/report-transport.js'`. Do NOT import any background module (they register top-level listeners on import — RESEARCH anti-pattern).

    In `beforeEach`: `vi.useFakeTimers()`; reset a module-local `_localStore = {}`; override `chrome.storage.local.get.mockImplementation` to read from `_localStore` (handle string-key, array-keys, and null→all per RESEARCH Pattern 5); override `chrome.storage.local.set.mockImplementation` to `Object.assign(_localStore, obj)`; `vi.stubGlobal('fetch', vi.fn())`; and call `_resetMutex()`. In `afterEach`: `vi.useRealTimers()`; `vi.unstubAllGlobals()`. (chrome itself is already globally stubbed by tests/setup/chrome-stub.js via setupFiles — override only the storage methods.)

    Author tests covering all four Success Criteria. Use a helper to stub a fetch response `{ ok, status, json: async () => (...) }`:
    - SC1 / XPORT-06: readFileSync every file in `src/content/` (glob the dir) and assert none matches `/fetch\s*\(\s*WORKER_REPORT_URL/` nor `/fetch\s*\(\s*['"]https:\/\/pct\.tonyrowles\.com/`.
    - SC2 / LIMIT-03: 5 sequential submits (fetch → 201) succeed; the 6th resolves `{ rateLimited: true }` and `fetch` is NOT called for it (assert call count unchanged); then `vi.advanceTimersByTime(600001)` (or seed window timestamps older than 10 min) and assert a 7th submit succeeds again (window pruning).
    - SC3 / QUEUE-01: stub fetch to throw → assert `_localStore.bugReportQueue` has length 1 after submitReport (disk-first); then SW-death simulation — `_resetMutex()`, stub fetch to resolve 201, `await drainQueueOnce()`, assert `_localStore.bugReportQueue` is empty (retried + removed). Also assert a 201 success path removes the entry atomically.
    - SC4 / QUEUE-03 / D-04: (a) fetch → 400 ⇒ submitReport resolves `dropped:true`, queue empty, no setTimeout pending; (b) fetch → 503 three times ⇒ assert backoff via `vi.advanceTimersByTime(2000)` then 8000 then 30000 re-drives drain, attemptCount progresses, and after the 3rd failure the entry is dropped silently (queue empty, result not `ok`); (c) fetch → 429 ⇒ entry retries with backoff (not dropped on first 429).
    - QUEUE-02: enqueue 21 entries ⇒ assert queue length is 20 and the oldest `enqueuedAt` was dropped; seed an entry with `enqueuedAt` older than 7 days ⇒ assert it is pruned on the next drain.

    Use `vi.advanceTimersByTime(...)` to drive the setTimeout-scheduled retries deterministically (no real waits).
  </action>
  <verify>
    <automated>npm run test:chrome 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `npm run test:chrome` exits 0 with the new `report-transport-chrome.test.js` reporting all SC1-SC4 + LIMIT-03 + QUEUE-01..04 cases passing.
    - Behavior: the SC2 case asserts `fetch` mock call-count is unchanged across the 6th (rate-limited) submit (no Worker invocation — matches ROADMAP SC2 literally).
    - Behavior: the SC3 case asserts `_localStore.bugReportQueue.length === 1` immediately after a throwing fetch (disk-first proven) and `=== 0` after the post-`_resetMutex` drain (SW-termination-survives-and-retries proven).
    - Behavior: the SC4 5xx case advances fake timers across 2000/8000/30000 ms and asserts exactly 3 attempts before silent drop.
    - Source assertion: `grep -c "import.*service-worker\|import.*firefox/background" tests/unit/report-transport-chrome.test.js` returns 0 (imports the shared helper only, never a background module).
  </acceptance_criteria>
  <done>report-transport-chrome.test.js exists and `npm run test:chrome` (after Task 3 config wiring) passes all SC1-SC4 + LIMIT-03 + QUEUE-01..04 assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Author the Firefox-target Vitest suite</name>
  <files>tests/unit/report-transport-firefox.test.js</files>
  <read_first>
    - tests/unit/report-transport-chrome.test.js (the Chrome suite authored in Task 1 — the Firefox suite mirrors it; XPORT-05 mandates identical dispatch, so behavioral assertions are identical)
    - tests/setup/chrome-stub.js:18-27 (same base stubs; Firefox config uses the same setupFiles)
    - .planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-PATTERNS.md (report-transport-firefox.test.js section: A2 — Chrome/Firefox logic is byte-identical; this file is the divergence extension point)
  </read_first>
  <action>
    Create `tests/unit/report-transport-firefox.test.js` mirroring the Chrome suite's behavioral assertions (the shared helper is target-agnostic, so the same submitReport/drainQueueOnce/rate-limit/queue behaviors are asserted). The only distinguishing element is the `describe` block name / header comment marking it the Firefox-target run. Its presence in `vitest.config.firefox.js` (Task 3) ensures `npm run test:firefox` exercises the transport logic against the Firefox setup. Keep the same stateful-storage-mock + fake-timer + fetch-stub harness as Task 1. Include at minimum the SC2 rate-limit, SC3 disk-first + SW-termination, and SC4 retry-policy cases (the load-bearing cross-target behaviors).
  </action>
  <verify>
    <automated>npm run test:firefox 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `npm run test:firefox` exits 0 with `report-transport-firefox.test.js` reporting its SC2/SC3/SC4 cases passing.
    - Source assertion: `grep -c "import.*service-worker\|import.*firefox/background" tests/unit/report-transport-firefox.test.js` returns 0 (shared-helper import only).
    - Behavior: the Firefox suite asserts the same `{ ok, queued, fingerprint, rateLimited, dropped }` contract as the Chrome suite (XPORT-05 identical-dispatch parity).
  </acceptance_criteria>
  <done>report-transport-firefox.test.js exists; `npm run test:firefox` (after Task 3) passes the cross-target behavioral assertions.</done>
</task>

<task type="auto">
  <name>Task 3: Wire both test files into the per-target Vitest configs</name>
  <files>vitest.config.chrome.js, vitest.config.firefox.js</files>
  <read_first>
    - vitest.config.chrome.js:9-20 (the explicit `include` array — NOT a glob — and the matching-scoped alias at 16-20)
    - vitest.config.firefox.js:9-20 (identical structure; alias targets dist/firefox/matching-exports.js)
    - .planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-RESEARCH.md (Pitfall 4: per-target configs use explicit include arrays; new files are silently ignored unless added)
  </read_first>
  <action>
    Append `'tests/unit/report-transport-chrome.test.js'` to the `include` array in `vitest.config.chrome.js` and `'tests/unit/report-transport-firefox.test.js'` to the `include` array in `vitest.config.firefox.js`. Do NOT touch the `alias` blocks — they are scoped to `matching.js` only and correctly do NOT redirect `report-transport.js` or `constants.js` (Pitfall 6). Do NOT switch the include arrays to glob patterns (out of scope; preserves existing behavior).
  </action>
  <verify>
    <automated>grep -c "report-transport-chrome.test.js" vitest.config.chrome.js && grep -c "report-transport-firefox.test.js" vitest.config.firefox.js && npm run test:chrome >/dev/null 2>&1 && npm run test:firefox >/dev/null 2>&1 && echo "both targets green"</automated>
  </verify>
  <acceptance_criteria>
    - CLI output: `grep -c "report-transport-chrome.test.js" vitest.config.chrome.js` returns 1 and `grep -c "report-transport-firefox.test.js" vitest.config.firefox.js` returns 1.
    - Source assertion: `git diff vitest.config.chrome.js vitest.config.firefox.js` shows ONLY include-array additions; the `alias` blocks are byte-unchanged.
    - Test command: both `npm run test:chrome` and `npm run test:firefox` exit 0 (new files are now collected and pass).
  </acceptance_criteria>
  <done>Both per-target configs include their respective report-transport test file; both target test runs are green; alias blocks unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → mocked chrome/fetch | tests run in Node with stubbed globals; no real network, no real storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | global chrome-stub mutation | mitigate | Storage mocks are per-test `mockImplementation` overrides in `beforeEach`; the global `tests/setup/chrome-stub.js` is NOT modified, so the 55+ existing tests are unaffected (RESEARCH anti-pattern) |
| T-03-06 | Information Disclosure | PROXY_TOKEN appearing in test fixtures | accept | Tests stub `fetch` and never assert on the token value; no new exposure surface beyond the source-side guard in Plan 03-01 |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs ZERO packages; Vitest ^3.0.0 already present (Environment Availability table); no install task |
</threat_model>

<verification>
- `npm run test:chrome` and `npm run test:firefox` both exit 0 with the new transport suites collected and passing.
- XPORT-06 static-grep guard passes (no `fetch(WORKER_REPORT_URL` in `src/content/`).
- SC1-SC4 all proven in the Chrome suite; cross-target parity proven in the Firefox suite.
- SW termination is simulated (no live browser) — D-10 honored.
</verification>

<success_criteria>
- Both new test files exist and import the shared helper directly (no background-module import).
- `npm run test:chrome` and `npm run test:firefox` exit 0; new files collected via the updated include arrays.
- SC1 (XPORT-06 grep), SC2 (5-ok/6-blocked/7-after-window), SC3 (disk-first survives simulated SW termination + retried), SC4 (4xx-drop / 5xx-3×-backoff / 429-retry) all asserted and passing.
- Zero new npm dependencies; alias blocks byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-02-SUMMARY.md` when done
</output>
