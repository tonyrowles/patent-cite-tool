---
phase: 03-background-submission-handler-rate-limit-retry-queue
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/background/service-worker.js
  - src/firefox/background.js
autonomous: true
requirements: [XPORT-05, XPORT-06]
must_haves:
  truths:
    - "XPORT-05: an IDENTICAL `MSG.SUBMIT_REPORT` branch is added to BOTH src/background/service-worker.js and src/firefox/background.js — each calls submitReport(message.payload).then(sendResponse) and `return true` to keep the channel open"
    - "D-06: the background owns all logic by delegating to src/shared/report-transport.js; the branch returns the structured result via sendResponse and renders NO toast itself"
    - "D-02: drainQueueOnce() is called opportunistically at the entry of each onMessage listener (any-wake drain) AND from onInstalled, in both backgrounds"
    - "D-02 / SC3: a NEW chrome.runtime.onStartup listener is added to both backgrounds (the first onStartup in the codebase) that fires drainQueueOnce() on browser restart"
    - "D-07: onStartup / onInstalled / opportunistic drains are fire-and-forget and silent (no sendResponse, no toast)"
    - "XPORT-06: content scripts are NOT modified — all cross-origin POSTs route through the background; src/content/ contains no fetch(WORKER_REPORT_URL"
  artifacts:
    - path: "src/background/service-worker.js"
      provides: "SUBMIT_REPORT branch + onStartup drain + onInstalled/opportunistic drain wiring (Chrome SW)"
      contains: "SUBMIT_REPORT"
    - path: "src/firefox/background.js"
      provides: "identical SUBMIT_REPORT branch + drain wiring (Firefox event page)"
      contains: "SUBMIT_REPORT"
  key_links:
    - from: "src/background/service-worker.js"
      to: "src/shared/report-transport.js (submitReport, drainQueueOnce)"
      via: "ESM import + onMessage branch"
      pattern: "submitReport|drainQueueOnce"
    - from: "src/firefox/background.js"
      to: "src/shared/report-transport.js (submitReport, drainQueueOnce)"
      via: "ESM import + onMessage branch"
      pattern: "submitReport|drainQueueOnce"
---

<objective>
Wire the Plan 03-01 shared helper into both background entry points with an IDENTICAL dispatch shape (XPORT-05): a `MSG.SUBMIT_REPORT` branch in each `onMessage` listener that delegates to `submitReport` and returns the result via `sendResponse`, plus opportunistic + onStartup + onInstalled drain triggers (D-02).

Purpose: This is the integration layer that makes the full content-script → background → Worker submission path live and testable end-to-end without UI. The background owns all logic (D-06) — it renders no toasts (that is Phase 4's content-script caller). Content scripts are untouched, satisfying XPORT-06's route-through-background constraint.

Output: modified `src/background/service-worker.js` and `src/firefox/background.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-CONTEXT.md
@.planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-RESEARCH.md
@.planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-PATTERNS.md
@.planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-01-SUMMARY.md

<interfaces>
<!-- Plan 03-01 exports (src/shared/report-transport.js) wired here: -->
submitReport(payload) -> Promise<{ ok, queued, fingerprint, rateLimited, dropped }>
drainQueueOnce() -> Promise<void>   (silent; fire-and-forget)

<!-- Chrome SW (src/background/service-worker.js) anchor lines [VERIFIED via grep]: -->
  line 49   : let creatingOffscreen = null   (mutex precedent)
  line 84   : chrome.runtime.onInstalled.addListener(() => { ... })
  line 132  : chrome.runtime.onMessage.addListener((message, sender, sendResponse) => { ...
  line 153-155 : } else if (message.type === MSG.GET_STATUS) { handleGetStatus(sendResponse); return true; }
  (NO onStartup listener exists anywhere — Phase 3 adds the first one)

<!-- Firefox background (src/firefox/background.js) anchor lines [VERIFIED via grep]: -->
  line 60   : chrome.runtime.onInstalled.addListener(() => { ... })
  line 106  : chrome.runtime.onMessage.addListener((message, sender, sendResponse) => { ...
  line 115-117 : } else if (message.type === MSG.GET_STATUS) { handleGetStatus(sendResponse); return true; }
  (Firefox imports shared modules at the top, e.g. line ~17: import { MSG, ... } from '../shared/constants.js')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire SUBMIT_REPORT branch + drain triggers into Chrome service-worker.js</name>
  <files>src/background/service-worker.js</files>
  <read_first>
    - src/background/service-worker.js:132-157 (onMessage dispatch chain — the if/else-if to extend)
    - src/background/service-worker.js:153-156 (GET_STATUS branch — the EXACT async-sendResponse + `return true` precedent to mirror)
    - src/background/service-worker.js:84-113 (onInstalled block — add a drain call at the end of its body)
    - src/background/service-worker.js:49-78 (creatingOffscreen mutex — confirms the codebase mutex precedent the helper already follows)
    - src/background/service-worker.js:261-262 (fire-and-forget `.catch(() => {})` idiom for drain calls)
    - .planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-PATTERNS.md (service-worker.js section: opportunistic-drain entry wiring, onStartup shape, onInstalled extension)
    - .planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-CONTEXT.md (D-02, D-06, D-07)
  </read_first>
  <action>
    Add `import { submitReport, drainQueueOnce } from '../shared/report-transport.js';` to the existing top-of-file import block (alongside the existing `'../shared/constants.js'` import — verify the relative path from `src/background/` is `../shared/`).

    In the `chrome.runtime.onMessage.addListener` callback (line 132): as the FIRST statement inside the callback body, call `drainQueueOnce();` fire-and-forget (no await) — this is the D-02 opportunistic any-wake drain. Then, after the existing last `else if` branch (the GET_STATUS branch at 153-155), add a new branch:
    `} else if (message.type === MSG.SUBMIT_REPORT) { submitReport(message.payload).then(result => sendResponse(result)).catch(() => sendResponse({ ok: false, queued: false, fingerprint: null, rateLimited: false, dropped: true })); return true; }` — mirroring the GET_STATUS `return true` async pattern exactly (Pitfall 3: the channel must stay open). The branch renders NO toast (D-06) — it only returns the structured result.

    At the END of the existing `onInstalled` handler body (line 84-113, before the closing brace), add `drainQueueOnce();` (D-02 drain on install/update).

    Immediately AFTER the onInstalled block, add the first onStartup listener in the codebase: `chrome.runtime.onStartup.addListener(() => { drainQueueOnce(); });` — fire-and-forget, silent (D-07).

    Do NOT refactor the existing dispatch chain into a router; this is additive branch insertion only. Do NOT add any toast call here.
  </action>
  <verify>
    <automated>node --check src/background/service-worker.js && grep -c "SUBMIT_REPORT" src/background/service-worker.js && grep -c "onStartup" src/background/service-worker.js</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "MSG.SUBMIT_REPORT" src/background/service-worker.js` returns 1 (one branch added).
    - Source assertion: `grep -c "chrome.runtime.onStartup" src/background/service-worker.js` returns 1 (the first onStartup in the codebase).
    - Source assertion: `grep -c "drainQueueOnce" src/background/service-worker.js` returns >= 3 (onMessage-entry + onInstalled + onStartup).
    - Source assertion: `grep -n "return true" src/background/service-worker.js` shows the SUBMIT_REPORT branch returns true (channel stays open for async sendResponse).
    - Source assertion: the SUBMIT_REPORT branch contains no `showSuccessToast`/`showFailureToast`/toast call (D-06 — background renders no toast); `grep -c "Toast" src/background/service-worker.js` is unchanged from baseline.
    - Behavior: `node --check src/background/service-worker.js` exits 0 (valid syntax).
  </acceptance_criteria>
  <done>Chrome SW imports the shared helper, has one SUBMIT_REPORT async branch (return true), an opportunistic onMessage-entry drain, an onInstalled drain, and the codebase's first onStartup drain; no toast logic added.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the IDENTICAL branch + drain triggers into Firefox background.js</name>
  <files>src/firefox/background.js</files>
  <read_first>
    - src/firefox/background.js:106-122 (onMessage dispatch chain — identical structure to Chrome SW)
    - src/firefox/background.js:115-117 (GET_STATUS branch — the `return true` precedent to mirror)
    - src/firefox/background.js:57-73 (onInstalled block — add a drain call at the end of its body)
    - src/firefox/background.js:17-25 (top-of-file shared-module import style: `import { MSG, ... } from '../shared/constants.js'`)
    - src/background/service-worker.js (the Chrome wiring authored in Task 1 — the Firefox branch must be byte-identical in dispatch shape per XPORT-05)
    - .planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-PATTERNS.md (firefox/background.js section)
    - .planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-CONTEXT.md (XPORT-05 identical-dispatch requirement)
  </read_first>
  <action>
    Add `import { submitReport, drainQueueOnce } from '../shared/report-transport.js';` to the top-of-file import block (the same `../shared/` style as the existing constants import at line ~17).

    Apply the SAME edits as Task 1, IDENTICALLY (XPORT-05 — no Chrome/Firefox divergence in the report pipeline):
    - First statement inside the `onMessage` callback (line 106): `drainQueueOnce();` fire-and-forget (D-02 opportunistic drain).
    - After the GET_STATUS branch (115-117), add the IDENTICAL `} else if (message.type === MSG.SUBMIT_REPORT) { submitReport(message.payload).then(result => sendResponse(result)).catch(() => sendResponse({ ok: false, queued: false, fingerprint: null, rateLimited: false, dropped: true })); return true; }` branch.
    - At the end of the onInstalled handler body (57-73), add `drainQueueOnce();`.
    - After the onInstalled block, add `chrome.runtime.onStartup.addListener(() => { drainQueueOnce(); });` (Firefox event pages support onStartup; first one here too).

    The dispatch branch body must be byte-identical to the Chrome branch from Task 1. No toast logic. No router refactor.
  </action>
  <verify>
    <automated>node --check src/firefox/background.js && grep -c "SUBMIT_REPORT" src/firefox/background.js && grep -c "onStartup" src/firefox/background.js</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "MSG.SUBMIT_REPORT" src/firefox/background.js` returns 1.
    - Source assertion: `grep -c "chrome.runtime.onStartup" src/firefox/background.js` returns 1.
    - Source assertion: `grep -c "drainQueueOnce" src/firefox/background.js` returns >= 3 (onMessage-entry + onInstalled + onStartup).
    - Behavior — identical dispatch (XPORT-05): the SUBMIT_REPORT branch body in src/firefox/background.js is character-identical to the one in src/background/service-worker.js. Prove by extracting both branch bodies and `diff`-ing them (e.g. `diff <(grep -A2 "MSG.SUBMIT_REPORT" src/background/service-worker.js) <(grep -A2 "MSG.SUBMIT_REPORT" src/firefox/background.js)` returns no differences).
    - Behavior: `node --check src/firefox/background.js` exits 0.
  </acceptance_criteria>
  <done>Firefox background imports the shared helper and has the byte-identical SUBMIT_REPORT branch + opportunistic/onInstalled/onStartup drain wiring as the Chrome SW; no toast logic.</done>
</task>

<task type="auto">
  <name>Task 3: Build + verify XPORT-06 guard and full extension test suite green</name>
  <files>src/background/service-worker.js, src/firefox/background.js</files>
  <read_first>
    - scripts/build.js (esbuild config: confirms src/shared/*.js bundles into both Chrome ESM and Firefox ESM background outputs — no build-config change needed for the new import)
    - .planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-RESEARCH.md (Environment Availability: run `npm run build` so the per-target alias to dist/*/matching-exports.js resolves and the whole suite stays green; the new transport tests do not use the alias but the pre-existing text-matcher/shared-matching tests do)
  </read_first>
  <action>
    Run `npm run build` to confirm the new `report-transport.js` import bundles cleanly into both `dist/chrome/background/service-worker.js` and `dist/firefox/background/service-worker.js` (esbuild already globs `src/shared/*.js`; no config edit needed). Then run the full extension test suite (`npm run test:src`, `npm run test:chrome`, `npm run test:firefox`) to confirm the wiring did not regress any existing test and that the XPORT-06 static-grep guard (from Plan 03-02) still passes against the unmodified `src/content/`. If `npm run build` is unavailable in CI, fall back to `node --check` on both files (already done in Tasks 1-2) — the build step is to keep the alias-dependent pre-existing tests green per RESEARCH Environment Availability. Make NO source edits in this task beyond fixing any build/import-path error surfaced.
  </action>
  <verify>
    <automated>npm run build >/dev/null 2>&1 && grep -rc "fetch(WORKER_REPORT_URL" src/content/ | grep -v ':0' | wc -l | grep -q '^0$' && echo "XPORT-06 guard: zero content fetches"</automated>
  </verify>
  <acceptance_criteria>
    - CLI output: `npm run build` exits 0; both `dist/chrome/background/service-worker.js` and `dist/firefox/background/service-worker.js` contain the bundled transport logic (`grep -lc "bugReportQueue" dist/chrome/background/service-worker.js dist/firefox/background/service-worker.js` lists both).
    - CLI output: `grep -r "fetch(WORKER_REPORT_URL" src/content/` returns zero results (XPORT-06 — content scripts never POST cross-origin).
    - Test command: `npm run test:src`, `npm run test:chrome`, and `npm run test:firefox` all exit 0 (no regression; transport suites from Plan 03-02 pass against the bundled wiring).
  </acceptance_criteria>
  <done>Both backgrounds bundle cleanly; the XPORT-06 guard returns zero content-script fetches; the full extension test suite (src + chrome + firefox targets) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content script → background (chrome.runtime.sendMessage) | same-extension message; browser-enforced origin; payload forwarded to the helper |
| background → Worker | the only cross-origin hop; Bearer PROXY_TOKEN lives in the helper (Plan 03-01), never in these background files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | malicious page calls sendMessage with a fabricated SUBMIT_REPORT payload | accept | chrome.runtime.onMessage only accepts same-extension messages by default (no externally_connectable report port opened); the helper forwards the Phase 2 builder's allowlisted payload and never adds ip/clientIp/userAgent (PAY-03 forbidden fields never reintroduced in transport) |
| T-03-08 | Repudiation / Channel-close | SUBMIT_REPORT branch fails to `return true` | mitigate | The branch mirrors the GET_STATUS `return true` precedent exactly (Pitfall 3); acceptance_criteria grep confirms `return true` present, so sendResponse is never dropped |
| T-03-09 | Information Disclosure | toast/log leaking PROXY_TOKEN at the wiring layer | mitigate | These files contain no token and no toast (D-06 background-renders-no-toast); acceptance_criteria confirms the branch adds no Toast call and no token reference |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed; `npm run build` uses the already-present esbuild toolchain; no install task |
</threat_model>

<verification>
- `node --check` passes on both background files; `npm run build` bundles the new import into both dist targets.
- Each background has exactly one SUBMIT_REPORT branch (return true), an opportunistic onMessage-entry drain, an onInstalled drain, and a new onStartup drain.
- The two SUBMIT_REPORT branch bodies are byte-identical (XPORT-05).
- XPORT-06 guard: zero `fetch(WORKER_REPORT_URL` in `src/content/`.
- Full extension suite (src + chrome + firefox) green — no regression and Plan 03-02's transport suites pass against the wired build.
</verification>

<success_criteria>
- Both backgrounds import the shared helper and wire an IDENTICAL `MSG.SUBMIT_REPORT` async branch (return true) plus opportunistic/onInstalled/onStartup drains (D-02/D-07).
- Background renders no toast (D-06); content scripts unmodified (XPORT-06).
- `npm run build` succeeds; `npm run test:src` / `test:chrome` / `test:firefox` all green.
- Zero new npm dependencies.
</success_criteria>

<output>
Create `.planning/phases/03-background-submission-handler-rate-limit-retry-queue/03-03-SUMMARY.md` when done
</output>
