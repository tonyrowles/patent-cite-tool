---
phase: 04-report-dialog-ui-citation-ui-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/report-dialog.js
  - src/content/content-script.js
  - src/background/service-worker.js
  - src/firefox/background.js
autonomous: true
requirements: [PAY-08, PAY-09]
must_haves:
  truths:
    - "PAY-08: extension-tagged console.error/console.warn (prefixes [SW]/[PCT]/[Offscreen]/[Firefox]) are appended to chrome.storage.local key bugReportErrorBuffer; host-page console output is NOT captured (D-08)"
    - "PAY-08: the bugReportErrorBuffer ring holds at most 20 entries, dropping oldest first (ring-overwrite at 20)"
    - "PAY-09: getReportDiagnostics() returns { xpathNode, scrollY, viewportWidth, viewportHeight } read from live page state at call time, and selectionText from window.getSelection() (captured at Report-button-click time per RESEARCH Pitfall 3)"
    - "PAY-09: getPdfParseStatus(patentType) derives 'success'|'failed'|'skipped'|'cache-hit'|null from chrome.storage.local currentPatent per the derivation rule (application→'skipped'; status==='parsed'&&source===null→'cache-hit'; status==='parsed'→'success'; error/no-text-layer/unavailable→'failed'; else null)"
    - "installErrorBuffer() is invoked once in the content script, the Chrome service worker, AND the Firefox background so the ring pools extension errors from all three contexts (D-08)"
  artifacts:
    - path: "src/content/report-dialog.js"
      provides: "installErrorBuffer(), getReportDiagnostics(), getPdfParseStatus(), getBrowserString(), getOsString() exports"
      min_lines: 90
    - path: "src/background/service-worker.js"
      provides: "installErrorBuffer() call at module init"
      contains: "installErrorBuffer"
    - path: "src/firefox/background.js"
      provides: "installErrorBuffer() call at module init"
      contains: "installErrorBuffer"
  key_links:
    - from: "console.error / console.warn override"
      to: "chrome.storage.local key bugReportErrorBuffer"
      via: "appendToBuffer fire-and-forget RMW"
      pattern: "bugReportErrorBuffer"
    - from: "getPdfParseStatus"
      to: "chrome.storage.local.currentPatent"
      via: "status+source derivation"
      pattern: "chrome\\.storage\\.local\\.get\\(['\"]currentPatent"
---

<objective>
Build the net-new diagnostic data layer that the Phase 4 report dialog and payload depend on: the PAY-08 error-log ring buffer (`bugReportErrorBuffer`) and the PAY-09 live capture helpers (`xpathNode`, `scrollY`, viewport, `pdfParseStatus`, browser/OS). These have no codebase analog and are independently unit-testable without a live DOM.

Purpose: The dialog (Plan 02) consumes these helpers at submit time; building them first means the dialog plan receives concrete, importable contracts rather than exploring derivation rules.
Output: New `src/content/report-dialog.js` (helpers section + exports only — the dialog UI is Plan 02) plus `installErrorBuffer()` wiring into all three script contexts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-report-dialog-ui-citation-ui-wiring/04-RESEARCH.md
@.planning/phases/04-report-dialog-ui-citation-ui-wiring/04-PATTERNS.md
@.planning/phases/04-report-dialog-ui-citation-ui-wiring/04-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Do NOT re-derive from codebase. -->

PAY-09 context fields consumed downstream by buildReportPayload (src/shared/report-payload-builder.js, Phase 2):
  context = { patentNumber, patentUrl?, selectionText, returnedCitation, confidenceTier,
              extensionVersion, browser, os, xpathNode, scrollY, viewportWidth,
              viewportHeight, pdfParseStatus }
  errors  → mapped to errorLog (array of ring-buffer entries)

currentPatent shape (src/background/service-worker.js) used by getPdfParseStatus:
  { status: 'fetching'|'parsing'|'parsed'|'error'|'no-text-layer'|'unavailable', source: 'google'|'uspto'|null, ... }
  Cache-hit signal (RESEARCH Pattern 4 / Pitfall 6, VERIFIED service-worker.js:391-406):
    status==='parsed' && source===null  ⇒ 'cache-hit'

content-script.js init region already imports from ../shared/constants.js and reads
chrome.storage.local. service-worker.js line 11-12 and firefox/background.js line 17-25
already import from ../shared/constants.js and ../shared/report-transport.js — add the
installErrorBuffer import alongside.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PAY-08 error ring buffer (installErrorBuffer + appendToBuffer)</name>
  <files>src/content/report-dialog.js</files>
  <read_first>
    - src/content/report-dialog.js (the file being created)
    - RESEARCH.md Pattern 5 (PAY-08 explicit tag-filter wrapper, lines 310-366) — the authoritative approach; AND Anti-Patterns (bind-before-replace, fire-and-forget set)
    - RESEARCH.md Pitfall 4 (ring buffer RMW races, lines 471-475) — accept occasional loss; never throw, never block
    - 04-PATTERNS.md "PAY-08 ring buffer storage RMW pattern" (lines 278-303) + "chrome.storage.local Read-Modify-Write" shared pattern (lines 693-702)
    - src/background/service-worker.js lines 391-406 (chrome.storage.local get/modify/set analog)
    - CONTEXT.md D-08 (extension-tagged-only) + RESEARCH Open Question 1 ([PCT] is the content-script prefix)
  </read_first>
  <behavior>
    - isExtensionTagged(args): true only when args[0] is a string starting with one of ['[SW]','[PCT]','[Offscreen]','[Firefox]']; false for non-string first arg or host-page strings
    - installErrorBuffer() binds origError/origWarn FIRST, then replaces console.error/console.warn; the override always calls the bound original, then conditionally fires appendToBuffer for extension-tagged args only
    - appendToBuffer('error'|'warn', args): RMW on chrome.storage.local 'bugReportErrorBuffer'; pushes { level, message (joined+truncated to 500 chars), ts }; trims to last 20 when length>20; swallows all errors (never throws)
    - calling console.error('[PCT] x') appends one entry; console.error('host page msg') appends nothing; 25 tagged errors leave exactly 20 entries (oldest dropped)
  </behavior>
  <action>
    Create `src/content/report-dialog.js`. Add module-level constants `ERROR_BUFFER_KEY = 'bugReportErrorBuffer'`, `BUFFER_MAX = 20`, `EXTENSION_PREFIXES = ['[SW]','[PCT]','[Offscreen]','[Firefox]']` (per D-08). Implement and `export function installErrorBuffer()` exactly per RESEARCH Pattern 5: bind `origError`/`origWarn` before replacing `console.error`/`console.warn`; the override calls the bound original synchronously, then `if (isExtensionTagged(args)) appendToBuffer(level, args)` as fire-and-forget (do NOT `await` from the override; attach `.catch(()=>{})` or wrap appendToBuffer body in try/catch). `appendToBuffer` does the get/push/trim/set RMW on `ERROR_BUFFER_KEY` and swallows all errors per Pitfall 4. Add an idempotency guard (module-level boolean) so a second `installErrorBuffer()` call in the same context does not double-wrap console. Import nothing chrome-specific (chrome.* are runtime globals). NEVER place fenced code in production — write the implementation directly.
  </action>
  <verify>
    <automated>node -e "globalThis.chrome={storage:{local:{_s:{},get(k){return Promise.resolve({[k]:this._s[k]})},set(o){Object.assign(this._s,o);return Promise.resolve()}}}}; import('./src/content/report-dialog.js').then(async m=>{m.installErrorBuffer(); console.error('[PCT] boom'); console.error('host noise'); await new Promise(r=>setTimeout(r,50)); const b=globalThis.chrome.storage.local._s.bugReportErrorBuffer||[]; if(b.length!==1||!b[0].message.includes('[PCT]')) throw new Error('expected 1 tagged entry, got '+JSON.stringify(b)); console.log('PAY-08 OK');})"</automated>
  </verify>
  <done>installErrorBuffer + appendToBuffer exported; tagged-only capture verified; ring caps at 20; never throws.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PAY-09 capture helpers (getReportDiagnostics, getPdfParseStatus, browser/OS)</name>
  <files>src/content/report-dialog.js</files>
  <read_first>
    - src/content/report-dialog.js (now contains Task 1's buffer code)
    - RESEARCH.md Pattern 3 (XPath-from-selection getNodeXPath, lines 239-278) + Pattern 4 (pdfParseStatus derivation table + getPdfParseStatus, lines 280-308) + Pitfall 6 (cache-hit source===null ambiguity)
    - RESEARCH.md Open Question 2 (browser/OS low-fidelity navigator slices) — PAY-03 forbids full userAgent
    - 04-PATTERNS.md "PAY-09 Capture Helpers" (lines 527-547) — capture at click time, window.getSelection() analog at content-script.js:184
    - src/content/content-script.js lines 459-468 (currentPatent read pattern) + extractPatentInfo lines 27-45
    - src/shared/report-payload-builder.js (the context field names this must populate)
  </read_first>
  <behavior>
    - getNodeXPath(node): walks parentNode counting same-tag prior siblings, returns '/tag[idx]/...' string; returns '/html/body' for body; never throws
    - getReportDiagnostics(): returns { xpathNode, scrollY: window.scrollY, viewportWidth: window.innerWidth, viewportHeight: window.innerHeight, selectionText: window.getSelection()?.toString() ?? null }; xpathNode from current selection's startContainer parent; all null-safe
    - getPdfParseStatus('application') === 'skipped' without reading storage
    - getPdfParseStatus('grant') with currentPatent {status:'parsed',source:null} === 'cache-hit'; {status:'parsed',source:'google'} === 'success'; {status:'error'} === 'failed'; null patent === null
    - getBrowserString()/getOsString() return short low-fidelity slices (e.g. 'Chrome/125', 'Windows 10'); never the full userAgent (PAY-03)
  </behavior>
  <action>
    Append to `src/content/report-dialog.js`: `getNodeXPath(node)` and `getXPathFromSelection()` per RESEARCH Pattern 3 (try/catch → null). Export `getReportDiagnostics()` returning `{ xpathNode, scrollY, viewportWidth, viewportHeight, selectionText }` captured from live page-context APIs (`window.scrollY`, `window.innerWidth`, `window.innerHeight`, `window.getSelection()`) — these MUST be read at the moment of call (Plan 02 calls this at Report-button-click time per RESEARCH Pitfall 3, not at submit). Export `async function getPdfParseStatus(patentType)` implementing the derivation table verbatim from RESEARCH Pattern 4: `'application'→'skipped'`; else read `chrome.storage.local.get('currentPatent')` and map `status==='parsed'&&source===null→'cache-hit'`, `status==='parsed'→'success'`, `['error','no-text-layer','unavailable'].includes(status)→'failed'`, else `null`; wrap in try/catch→null. Export `getBrowserString()` (`navigator.userAgent.match(/Chrome\/[\d.]+/)?.[0] ?? navigator.userAgent.match(/Firefox\/[\d.]+/)?.[0] ?? null`) and `getOsString()` (low-fidelity platform token) per RESEARCH Open Question 2 — NEVER include the full userAgent string (PAY-03). Do not import getCitationHost (Plan 02 receives `shadow` as a param).
  </action>
  <verify>
    <automated>node -e "globalThis.chrome={storage:{local:{_s:{currentPatent:{status:'parsed',source:null}},get(k){return Promise.resolve({[k]:this._s[k]})},set(o){Object.assign(this._s,o);return Promise.resolve()}}}}; import('./src/content/report-dialog.js').then(async m=>{const a=await m.getPdfParseStatus('application'); const c=await m.getPdfParseStatus('grant'); globalThis.chrome.storage.local._s.currentPatent={status:'parsed',source:'google'}; const s=await m.getPdfParseStatus('grant'); globalThis.chrome.storage.local._s.currentPatent={status:'error'}; const f=await m.getPdfParseStatus('grant'); if(a!=='skipped'||c!=='cache-hit'||s!=='success'||f!=='failed') throw new Error('pdfParseStatus map wrong: '+[a,c,s,f]); console.log('PAY-09 derivation OK');})"</automated>
  </verify>
  <done>getReportDiagnostics, getPdfParseStatus, getBrowserString, getOsString exported; pdfParseStatus derivation matches the 5-case table; no full userAgent in output.</done>
</task>

<task type="auto">
  <name>Task 3: Wire installErrorBuffer() into all three script contexts</name>
  <files>src/content/content-script.js, src/background/service-worker.js, src/firefox/background.js</files>
  <read_first>
    - src/content/content-script.js lines 1-15 (import block) — add report-dialog import; module-init region near lines 153-171
    - src/background/service-worker.js lines 11-12 (existing imports) + lines 85-140 (onInstalled/onStartup/onMessage init region)
    - src/firefox/background.js lines 17-25 (existing imports) + lines 60-126 (init region)
    - CONTEXT.md D-08 (capture in both script contexts) + RESEARCH Pattern 5 "Background script installation" (lines ~364)
  </read_first>
  <action>
    Import `installErrorBuffer` from `./report-dialog.js` in `src/content/content-script.js` and from `../content/report-dialog.js` in both backgrounds (esbuild bundles per-target per RESEARCH Pattern 7 / PATTERNS esbuild note — report-dialog.js has zero DOM-construction code yet so it bundles cleanly into the background ESM target). Call `installErrorBuffer()` exactly once at module-init top-level in each of the three files (content-script.js, service-worker.js, firefox/background.js). The idempotency guard from Task 1 protects against accidental double-invocation. Do NOT add any `console.error` calls in this task — wiring only. Verify the background bundles still build.
  </action>
  <verify>
    <automated>node scripts/build.js >/dev/null 2>&1 && grep -rq "installErrorBuffer" dist/chrome/background/service-worker.js && grep -rq "installErrorBuffer" dist/firefox/background.js && echo "buffer wired into both backgrounds + builds clean"</automated>
  </verify>
  <done>installErrorBuffer imported and called in all three contexts; `node scripts/build.js` succeeds; the call appears in both built background bundles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host page (Google Patents) → extension console | host-page console.error/warn could be captured into the ring buffer if the tag filter is wrong |
| ring buffer → outbound payload | buffer contents flow into the report payload sent to the Worker; must contain only extension-tagged diagnostic strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | installErrorBuffer tag filter | mitigate | isExtensionTagged requires args[0] to start with an explicit extension prefix ([SW]/[PCT]/[Offscreen]/[Firefox]); host-page console strings are never appended (D-08). Verified by Task 1 automated check (host string → 0 entries). |
| T-04-02 | Information Disclosure | getBrowserString/getOsString | mitigate | Low-fidelity navigator slices only (Chrome/NNN, OS token); the full navigator.userAgent string is never written to context (PAY-03 forbidden field). |
| T-04-03 | Denial of Service | console.error override recursion | mitigate | origError/origWarn bound BEFORE replacement; override never calls the replaced console method; appendToBuffer swallows all errors (RESEARCH Anti-Pattern + Pitfall 4). |
| T-04-04 | Information Disclosure | getReportDiagnostics xpathNode | accept | xpathNode is the user's own selected DOM node path — intentionally included per PAY-09 for triage; no host-page content outside the selection is scraped. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages this phase (RESEARCH Package Legitimacy Audit: N/A; DoD zero-new-dependency constraint). No install tasks present. |
</threat_model>

<verification>
- `node scripts/build.js` succeeds (report-dialog.js bundles into content + both backgrounds).
- Task 1 + Task 2 automated node checks pass (tagged-only capture, ring cap, pdfParseStatus derivation).
- `grep -rn "userAgent" src/content/report-dialog.js` shows only low-fidelity `.match(...)` slices, never a raw assignment of the full string into context.
</verification>

<success_criteria>
- bugReportErrorBuffer captures extension-tagged console.error/warn only, caps at 20, never throws (PAY-08).
- getReportDiagnostics + getPdfParseStatus + browser/OS helpers return the PAY-09 context fields from live state with the correct pdfParseStatus derivation.
- installErrorBuffer is invoked in content script, Chrome SW, and Firefox background.
</success_criteria>

<output>
Create `.planning/phases/04-report-dialog-ui-citation-ui-wiring/04-01-SUMMARY.md` when done.
</output>
