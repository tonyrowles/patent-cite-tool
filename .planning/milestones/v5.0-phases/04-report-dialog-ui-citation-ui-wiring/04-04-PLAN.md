---
phase: 04-report-dialog-ui-citation-ui-wiring
plan: 04
type: tdd
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - tests/unit/report-dialog-buffer.test.js
  - tests/unit/report-dialog-diagnostics.test.js
  - tests/unit/report-trigger-mapping.test.js
autonomous: true
requirements: [PAY-08, PAY-09, CAP-02, TRIG-01, TRIG-02, TRIG-03, TRIG-04]
must_haves:
  truths:
    - "SC4-proof (PAY-08): a Vitest suite drives installErrorBuffer in a node env with a stateful chrome.storage.local mock, proving extension-tagged-only capture (D-08), the 20-entry ring cap, and never-throws"
    - "SC4-proof (PAY-09): a Vitest suite pins getPdfParseStatus across all 5 derivation cases (application/cache-hit/success/failed/null) and asserts getBrowserString/getOsString never emit the full navigator.userAgent (PAY-03)"
    - "SC2-proof (CAP-02/D-06): a test feeds a context through buildReportPayload with includeSelectionText:false and asserts the selectionText key is ENTIRELY ABSENT, and present when true — the [Remove selection text] omission contract the dialog relies on"
    - "SC1-proof (TRIG-01..04): a Vitest suite pins mapConfidenceTier (>=0.95 green / >=0.80 yellow / else red) and mapOutcomeToReportCategory across no-match/yellow/worker-error/green so the green-hidden invariant and category pre-selection are regression-locked"
    - "Static guard: no file in src/content/ contains fetch(WORKER_REPORT_URL (XPORT-06 preserved) and report-dialog.js never calls getCitationHost("
  artifacts:
    - path: "tests/unit/report-dialog-buffer.test.js"
      provides: "PAY-08 ring buffer behavior tests"
      contains: "bugReportErrorBuffer"
    - path: "tests/unit/report-dialog-diagnostics.test.js"
      provides: "PAY-09 pdfParseStatus + browser/OS + selectionText-omission tests"
      contains: "getPdfParseStatus"
    - path: "tests/unit/report-trigger-mapping.test.js"
      provides: "TRIG mapping + static-grep guards"
      contains: "mapConfidenceTier"
  key_links:
    - from: "vitest run (test:src, node env)"
      to: "report-dialog.js helpers + report-payload-builder.js + content-script mapping exports"
      via: "stateful chrome.storage.local mock + module import"
      pattern: "vi\\.stubGlobal\\('chrome'"
---

<objective>
Author the Vitest suites that regression-lock Phase 4's four ROADMAP Success Criteria using the EXISTING node test environment (jsdom is intentionally NOT added — zero-dependency DoD). Tests target the extracted pure-ish logic (PAY-08 buffer, PAY-09 derivation, the D-06 selectionText omission, and the TRIG mapping helpers) via the established stateful chrome-mock pattern, plus static-grep guards for XPORT-06 and the getCitationHost lifecycle rule. Live DOM behavior (focus trap, click-outside) is proven in Phase 5 UAT-05; here we lock everything node-testable.

Purpose: SC1 (trigger/category), SC2 (selection-text omission), SC4 (live diagnostic fields + buffer) become CI-enforced. SC3 (dismiss/focus-trap) is partially covered by static structure assertions and deferred to Phase 5 live UAT.
Output: three new test files, all green under `npm run test:src`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-report-dialog-ui-citation-ui-wiring/04-RESEARCH.md
@.planning/phases/04-report-dialog-ui-citation-ui-wiring/04-PATTERNS.md
@src/content/report-dialog.js
@src/shared/report-payload-builder.js

<interfaces>
<!-- Test environment + analogs -->

vitest.config.js => environment: 'node' (NO jsdom; do NOT add it). report-dialog.js DOM
construction code (showReportDialog) cannot run under node — test the EXPORTED pure-ish
helpers + static-grep the DOM module, not live rendering. (PATTERNS.md "Note on DOM testing".)

Analog to copy verbatim: tests/unit/report-transport-chrome.test.js
  - buildChromeMock(store) stateful chrome.storage.local mock (lines 43-74) +
    add runtime.getManifest: vi.fn(()=>({version:'5.0.0'}))
  - beforeEach/afterEach vi.stubGlobal('chrome',...) / vi.unstubAllGlobals()
  - readdirSync + readFileSync static-grep pattern (lines 130-154)
Fixture factory analog: tests/unit/report-payload-builder.test.js makeReportInputs (lines 64-88).

Exports under test:
  report-dialog.js: installErrorBuffer, getPdfParseStatus, getBrowserString, getOsString
    (getReportDiagnostics needs window/getSelection — node-stub window minimally or skip live, grep instead)
  report-payload-builder.js: buildReportPayload (Phase 2)
  content-script.js: mapConfidenceTier, mapOutcomeToReportCategory
    => Plan 03 MUST export these (add `export` keyword). If content-script.js cannot export
       cleanly from the IIFE entry, replicate the two tiny pure functions in the test via the
       documented spec and ALSO static-grep their presence — prefer real export.
</interfaces>
</context>

<feature>
  <name>PAY-08 ring buffer regression suite</name>
  <files>tests/unit/report-dialog-buffer.test.js, src/content/report-dialog.js</files>
  <behavior>
    - installErrorBuffer + console.error('[PCT] x') => exactly 1 bugReportErrorBuffer entry whose message includes '[PCT]'
    - console.error('host-page noise') and console.warn('Polymer thing') => 0 entries (tag filter, D-08)
    - 25 tagged console.error calls => buffer length exactly 20, oldest dropped (ring cap)
    - console.warn('[SW] w') captured with level 'warn'
    - appendToBuffer never throws even if chrome.storage.local.set rejects (stub a rejecting set; assert no unhandled throw)
  </behavior>
  <implementation>Copy buildChromeMock + beforeEach/afterEach from report-transport-chrome.test.js. Await a microtask/timer after each console call (fire-and-forget). Inspect _localStore.bugReportErrorBuffer directly. Use the node-flush helper `await new Promise(r=>setTimeout(r,0))`.</implementation>
</feature>

<feature>
  <name>PAY-09 + SC2 selection-omission suite</name>
  <files>tests/unit/report-dialog-diagnostics.test.js, src/content/report-dialog.js</files>
  <behavior>
    - getPdfParseStatus('application') === 'skipped' (no storage read)
    - currentPatent {status:'parsed',source:null} => 'cache-hit'; {status:'parsed',source:'google'} => 'success'; {status:'error'} => 'failed'; {status:'no-text-layer'} => 'failed'; null/absent => null; {status:'fetching'} => null
    - getBrowserString()/getOsString(): stub navigator.userAgent='...Chrome/125...'; assert result is the short slice 'Chrome/125' and that NEITHER helper returns a string equal to the full userAgent (PAY-03)
    - SC2 (D-06): buildReportPayload(makeReportInputs({includeSelectionText:false})) => 'selectionText' NOT in Object.keys(payload); includeSelectionText:true => present
  </behavior>
  <implementation>buildChromeMock for getPdfParseStatus storage cases; vi.stubGlobal('navigator',{userAgent:'...'}) for browser/OS. Import buildReportPayload + the makeReportInputs factory style from report-payload-builder.test.js for the omission assertions (these belong here because they prove the dialog's [Remove selection text] contract end-to-end).</implementation>
</feature>

<feature>
  <name>SC1 trigger-mapping + static guards suite</name>
  <files>tests/unit/report-trigger-mapping.test.js, src/content/content-script.js</files>
  <behavior>
    - mapConfidenceTier(0.97)==='green'; (0.85)==='yellow'; (0.80)==='yellow'; (0.79)==='red'
    - mapOutcomeToReportCategory('no-match',null)==='no_match'; ('paragraph-not-found',null)==='no_match'; ('no-position-map',null)==='tool_not_working'; ('lookup-failed',null)==='tool_not_working'; (null,0.85)==='inaccurate_citation'; (null,0.98)===null (green => no button, TRIG-04)
    - static-grep: no file in src/content/ matches /fetch\s*\(\s*WORKER_REPORT_URL/ (XPORT-06)
    - static-grep: src/content/report-dialog.js does NOT contain 'getCitationHost(' (RESEARCH Pitfall 1 lifecycle rule)
    - static-grep: src/content/citation-ui.js contains 'cite-report-btn' and the green guard token "confidenceTier !== 'green'"
  </behavior>
  <implementation>Import mapConfidenceTier + mapOutcomeToReportCategory from content-script.js (Plan 03 exports them). Static-grep via readdirSync/readFileSync mirroring report-transport-chrome.test.js:130-154. These four tests collectively lock SC1 + the TRIG-04 invariant + the two cross-cutting structural invariants.</implementation>
</feature>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED+GREEN — PAY-08 buffer suite</name>
  <files>tests/unit/report-dialog-buffer.test.js</files>
  <read_first>
    - tests/unit/report-transport-chrome.test.js (buildChromeMock 43-74, beforeEach/afterEach 105-127)
    - 04-PATTERNS.md report-dialog-buffer.test.js section (lines 659-668)
    - src/content/report-dialog.js (installErrorBuffer impl from Plan 01)
  </read_first>
  <behavior>See the "PAY-08 ring buffer regression suite" feature block above.</behavior>
  <action>Write tests/unit/report-dialog-buffer.test.js per the feature behavior. RED: run first to confirm meaningful assertions. GREEN: it should pass against Plan 01's implementation as-is (no production change expected); if a real bug surfaces (e.g. recursion, missing cap), fix report-dialog.js minimally and note it. Use the stateful chrome mock + setTimeout(0) flush.</action>
  <verify>
    <automated>npx vitest run tests/unit/report-dialog-buffer.test.js</automated>
  </verify>
  <done>PAY-08 suite green: tagged-only capture, 20-cap, warn level, never-throws all asserted.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: RED+GREEN — PAY-09 derivation + SC2 selection-omission suite</name>
  <files>tests/unit/report-dialog-diagnostics.test.js</files>
  <read_first>
    - tests/unit/report-transport-chrome.test.js (chrome mock) + tests/unit/report-payload-builder.test.js (makeReportInputs 64-88)
    - src/content/report-dialog.js (getPdfParseStatus, getBrowserString, getOsString from Plan 01)
    - src/shared/report-payload-builder.js (D-06 omission)
  </read_first>
  <behavior>See the "PAY-09 + SC2 selection-omission suite" feature block above.</behavior>
  <action>Write tests/unit/report-dialog-diagnostics.test.js per the feature behavior: all 5 pdfParseStatus cases via the storage mock, browser/OS slices via stubbed navigator (assert NOT equal to full userAgent), and the buildReportPayload includeSelectionText false/true key-presence assertions (SC2/D-06). GREEN against Plans 01-02; fix production minimally only if a real derivation bug is found.</action>
  <verify>
    <automated>npx vitest run tests/unit/report-dialog-diagnostics.test.js</automated>
  </verify>
  <done>PAY-09 derivation (5 cases) + low-fidelity browser/OS + SC2 selectionText omission/presence all green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: RED+GREEN — SC1 trigger-mapping + static structural guards</name>
  <files>tests/unit/report-trigger-mapping.test.js, src/content/content-script.js</files>
  <read_first>
    - tests/unit/report-transport-chrome.test.js (static-grep readdirSync/readFileSync 130-154)
    - src/content/content-script.js (mapConfidenceTier, mapOutcomeToReportCategory from Plan 03 — confirm they are `export`ed; if not, add the export keyword as the GREEN step)
    - RESEARCH.md Pattern 6 (mapping spec) + 04-UI-SPEC.md Auto-Surface table
  </read_first>
  <behavior>See the "SC1 trigger-mapping + static guards suite" feature block above.</behavior>
  <action>Write tests/unit/report-trigger-mapping.test.js. RED: importing the two mapping helpers from content-script.js will fail if Plan 03 did not export them — the GREEN step is to add `export` to `mapConfidenceTier` and `mapOutcomeToReportCategory` in content-script.js (esbuild IIFE entry tolerates named exports; they are tree-shaken in the bundle). Add the three static-grep guards (no fetch(WORKER_REPORT_URL in src/content/; no getCitationHost( in report-dialog.js; cite-report-btn + green-guard token in citation-ui.js). Confirm the full `node scripts/build.js` still succeeds after the export edit.</action>
  <verify>
    <automated>npx vitest run tests/unit/report-trigger-mapping.test.js && node scripts/build.js >/dev/null 2>&1 && echo "SC1 mapping + guards green + build clean"</automated>
  </verify>
  <done>Tier bands + category mapping pinned (incl. TRIG-04 green=>null); XPORT-06 + getCitationHost + green-guard static guards pass; build still succeeds after exporting the helpers.</done>
</task>

<task type="auto">
  <name>Task 4: Full suite green + vitest include wiring</name>
  <files>tests/unit/report-dialog-buffer.test.js, tests/unit/report-dialog-diagnostics.test.js, tests/unit/report-trigger-mapping.test.js</files>
  <read_first>
    - vitest.config.js (confirm tests/unit/*.test.js is already globbed by the test:src include — the three new files should be auto-discovered; only touch config if they are not)
  </read_first>
  <action>Run `npm run test:src` and confirm all three new files are discovered and green alongside the existing suite. If the include glob does not pick them up, add them; otherwise no config change. Do NOT add jsdom or any new dependency. Run `npm run lint` + `npm run test:lint` to confirm no new lint/AMO warnings.</action>
  <verify>
    <automated>npm run test:src && npm run lint</automated>
  </verify>
  <done>npm run test:src green (all Phase 4 suites discovered + passing); lint clean; zero new dependencies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → production modules | tests import and exercise production code; they must not weaken production invariants to pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Tampering | static-grep guards | mitigate | The XPORT-06 (no cross-origin fetch in src/content/), getCitationHost-lifecycle, and green-guard grep assertions are themselves the regression locks; they fail CI if a future edit reintroduces the forbidden pattern. |
| T-04-14 | Repudiation | SC2 omission proof | mitigate | The includeSelectionText:false => key-absent assertion proves the privacy toggle (PAY-03/CAP-02) at CI time, not just by manual UAT. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages — jsdom explicitly NOT added (DoD). Tests run in the existing node env. No install tasks. |
</threat_model>

<verification>
- `npm run test:src` green including the three new suites.
- `npm run lint` + `npm run test:lint` clean.
- `node scripts/build.js` succeeds after the mapping-helper export edit.
</verification>

<success_criteria>
- SC1 trigger/category mapping + TRIG-04 green-hidden invariant regression-locked.
- SC2 [Remove selection text] omission proven via buildReportPayload key-presence assertion.
- SC4 diagnostic fields (pdfParseStatus derivation) + PAY-08 ring buffer behavior pinned.
- XPORT-06 and getCitationHost-lifecycle structural invariants enforced by static grep.
- Zero new npm dependencies.
</success_criteria>

<output>
Create `.planning/phases/04-report-dialog-ui-citation-ui-wiring/04-04-SUMMARY.md` when done.
</output>
