---
phase: 4
slug: report-dialog-ui-citation-ui-wiring
status: approved
shadcn_initialized: false
preset: none
created: 2026-06-13
reviewed_at: 2026-06-13
---

# Phase 4 — UI Design Contract: Report Dialog UI + Citation-UI Wiring

> Visual and interaction contract for `src/content/report-dialog.js` and the
> Report button wired into `src/content/citation-ui.js`.
> Generated by gsd-ui-researcher. Consumed by gsd-planner and gsd-executor.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none | No React/Next.js/Vite framework; plain ES module injected into a Shadow DOM |
| Preset | not applicable | CSS is authored inline as template-literal strings in JS (existing pattern) |
| Component library | none (native DOM) | Zero new dependencies constraint (sixth consecutive milestone) |
| Icon library | Unicode glyphs only | Flag glyph `⚑` (U+2691) for Report button; no SVG icon library |
| Font | -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif | citation-ui.js:425 |
| Monospace font | 'SF Mono', 'Consolas', 'Monaco', monospace | citation-ui.js:437 (citation text only — not used in dialog) |

**shadcn gate result:** Not applicable. This is a browser extension content script rendered via
Shadow DOM inside an ES module bundle. No React, no npm component library, no registry. The
existing design system is the CSS authored in `getCitationPopupCSS()` / `getSuccessToastCSS()` /
`getFailureToastCSS()` in `citation-ui.js`. All new CSS in `report-dialog.js` must match those
existing token values exactly.

---

## Spacing Scale

Declared values. The existing `citation-ui.js` popup uses an implicit 8-point rhythm. All new
spacing in `report-dialog.js` must use these values. **Source: citation-ui.js extracted CSS.**

| Token | Value | Usage in this phase |
|-------|-------|---------------------|
| xs | 4px | Icon gap in Report button; copy button padding-x partial; position clamp inset |
| sm | 8px | `.cite-row` gap; popup padding horizontal; copy button padding-x; `.cite-row` gap |
| md | 12px | Report panel padding (closest multiple of 4 to the popup's 10px right-pad + 2px rounding) |
| base | 16px | Note textarea horizontal padding; "What's included" section indent |
| lg | 24px | Not used in this popup-scale component |
| xl | 32px | Not used in this popup-scale component |

**Exceptions:**

> **Preamble:** This component deliberately inherits the existing `citation-ui.js` spacing scale
> rather than a fresh 4px grid, per locked decision D-02 ("reuse the existing citation-pill
> aesthetic"). The popup's own CSS uses sub-grid values (10px, 6px, 2px) that are not multiples
> of 4. Every such value in this spec is inherited verbatim and is listed below with its source
> and justification. Snapping them to a 4px grid would break visual consistency with the parent
> popup and violate D-02.

| Value | Location in spec | Justification |
|-------|-----------------|---------------|
| `10px` (inline-padding) | Popup `padding: 8px 10px` (citation-ui.js:424) | Inherited verbatim from citation-ui.js:424 — D-02 mandates matching the existing compact-popup token; the extension's established UI is not on a 4px grid. |
| `14px` (panel padding-x) | Report panel `padding: 12px 14px` | Inherited from the popup's 10px inline-padding rhythm, nudged 2px wider to accommodate form content; compact-popup density convention consistent with the parent popup's 8px/10px rhythm (D-02). |
| `6px` (radio group gap) | Radio group `gap: 6px` | Compact-popup density convention; consistent with the parent popup's 8px/10px rhythm (D-02). |
| `10px` (radio group margin) | Radio group `margin-bottom: 10px` | Inherited verbatim from citation-ui.js compact-popup spacing rhythm — D-02 mandates matching the existing aesthetic; the extension's established UI is not on a 4px grid. |
| `6px` (radio option gap) | Radio option label `gap: 6px` | Compact-popup density convention; consistent with the parent popup's 8px/10px rhythm (D-02). |
| `6px 8px` (textarea padding) | Note textarea `padding: 6px 8px` | Inherited verbatim from citation-ui.js compact-popup token rhythm — D-02 mandates matching the existing aesthetic; the extension's established UI is not on a 4px grid. |
| `2px` (counter margin) | Character counter `margin-top: 2px` | Compact-popup density convention; a 4px margin-top would add excessive separation between textarea and its live counter in this sub-scale context (D-02). |
| `6px` (toggle margin) | Selection-text toggle `margin-top: 6px` | Compact-popup density convention; consistent with the parent popup's 8px/10px rhythm (D-02). |
| `2px 6px` (nudge button padding) | Nudge Report button `padding: 2px 6px` | Compact-popup density convention; consistent with the parent popup's sub-grid rhythm (D-02). Button is icon+text and must remain compact enough to fit in `.cite-row`. |

**Standard non-4-multiple values also inherited:**
- Touch-target minimum: Report button click target must be at least 24px × 24px (icon-only mode); nudge mode adds text so it naturally exceeds this.
- Positioning offsets: `top = rect.bottom + 8` (popup standard) and `left` clamped with `8px` inset (citation-ui.js:213-214) — replicated for the anchored report panel.

---

## Typography

All type in the report dialog inherits from the popup's established CSS. No new typefaces.
**Source: citation-ui.js extracted CSS.**

This component uses **3 type sizes** (collapsed from 4 to eliminate ambiguous 1px steps).
The 12px size that appeared in the previous draft is absorbed into the 11px meta tier and
the 13px body tier — both groups of elements are compact-popup scale and do not need a
distinct 12px rung.

| Role | Size | Weight | Line Height | CSS class / element |
|------|------|--------|-------------|---------------------|
| Body / form label / button label | 13px | 400 (regular) or 500 (medium) | 1.4 | `.cite-report-panel`, radio labels, disclosure text, Submit button, Cancel button, nudge label |
| Small / meta / counter | 11px | 400 (regular) | 1.4 | `.cite-report-counter` (note char count), "What's included" field labels and values, "see what's sent" inline toggle, selection-text toggle label |
| Decorative glyph | 14px | 400 (regular) | 1.0 | `⚑` flag glyph in icon-only Report button only — size chosen to render the Unicode glyph at a visually balanced 24px touch target; not a text hierarchy level |

**Why 3 sizes and not 4:** The existing popup uses 11px and 13px as its two functional text sizes
(citation-ui.js:437, :475). A separate 12px rung would be a 1px delta with no perceptible hierarchy
difference. The 14px entry is exclusively for the decorative flag glyph and should not be treated
as a content hierarchy level.

**Font stack (verbatim from citation-ui.js:425):**
`-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif`

**Weight rule:** Use exactly 2 weights — 400 (body, labels, meta) and 500 (buttons, nudge label). Do not introduce 600 or 700 inside the report dialog (citation text uses 600 but that is outside this component).

---

## Color

All hex values are extracted verbatim from `citation-ui.js`. No new color values are introduced.

### Base palette (from citation-ui.js)

| Role | Hex | Usage in this phase |
|------|-----|---------------------|
| Dominant surface (60%) | `#ffffff` | Report panel background; `cite-popup` background (citation-ui.js:419) |
| Secondary surface (30%) | `#f9fafb` | Copy button background, toggle background, "What's included" section background (citation-ui.js:452) |
| Border / divider | `#e5e7eb` | Panel border, textarea border, divider lines (citation-ui.js:421, 449) |
| Border secondary | `#d1d5db` | Copy button border (citation-ui.js:449) |
| Body text | `#1a1a1a` | Panel body text (citation-ui.js:427) |
| Secondary text | `#6b7280` | Note character counter, "What's included" field values, meta labels (citation-ui.js:475) |
| Muted text | `#374151` | Copy button label (citation-ui.js:451) |

### Semantic accent colors (10%) — reserved list

| Color | Hex | Reserved for (only these elements) |
|-------|-----|-------------------------------------|
| Success green (accent) | `#ecfdf5` bg / `#a7f3d0` border / `#065f46` text | Submit button (primary CTA), success toast after submit, "Report sent" confirmation |
| Amber / yellow (nudge) | `#f59e0b` | Confidence dot in medium-confidence mode (citation-ui.js:470); Report button soft highlight background in nudge mode |
| Red / error (destructive) | `#fef2f2` bg / `#fecaca` border / `#991b1b` text | Error popup, failure toast (citation-ui.js:489-491, 539-551); no destructive actions in Phase 4 that require a confirmation modal |
| Blue (cite float btn) | `#3b82f6` | Existing Cite floating button only — NOT used in report dialog |

**Accent reserved for (report dialog scope):**
- Submit button: green (`#ecfdf5` background, `#065f46` text, `#a7f3d0` border) — primary CTA only
- Report button nudge highlight: amber `#f59e0b` as a left-border or background tint (soft, not saturated fill)
- Failure/rate-limit toast: red semantic (`#fef2f2` / `#fecaca` / `#991b1b`)
- Radio selected state: `#3b82f6` (blue border on selected radio circle) — smallest use, follows browser radio default but overridden inside Shadow DOM

**The Cite floating button's blue (`#3b82f6`) is NOT used in the report dialog.** The dialog aesthetic is white-on-grey, not blue.

---

## Component Anatomy

### Focal Point

**The Submit ("Submit report") button with its green accent (`#ecfdf5` background, `#065f46` text,
`#a7f3d0` border) is the dialog's primary visual focal point and the only element using a non-neutral
color.** The auto-selected radio option is the first keyboard focus target on open, but is visually
secondary to the CTA. The executor should ensure the Submit button's green is the only saturated
element in the panel and is never obscured by the collapsible section when expanded.

---

### 1. Report Button (CAP-03) — added to `.cite-row` in `citation-ui.js`

**Normal state (icon-only, never shown on green — TRIG-04):**
```
[⚑]
```
- Element: `<button class="cite-report-btn">`
- `title="Report a problem"`, `aria-label="Report a problem with this citation"`
- Width: auto; min-width: 24px; height: 24px
- Background: transparent
- Border: none
- Color: `#6b7280` (muted)
- Font-size: 14px (glyph only — decorative; see Typography section)
- Cursor: pointer
- Hidden entirely (`display: none`) when `confidenceTier === 'green'` (TRIG-04 invariant)

**Nudge state (failure / yellow / Worker-error — D-06):**
```
[⚑ Report a problem]
```
- Same element with `textContent = '⚑ Report a problem'`
- Background: `rgba(245, 158, 11, 0.08)` (amber tint, 8% opacity)
- Border: none
- Border-radius: 4px
- Padding: 2px 6px (see Exceptions — compact-popup density convention, D-02)
- Color: `#92400e` (amber-900 — readable on white)
- Font-size: 13px; font-weight: 500

**Hover state (both modes):**
- Background: `#f3f4f6` (citation-ui.js copy-btn hover: citation-ui.js:459)
- Color: `#1a1a1a`

**Focus state:**
- `outline: 2px solid #3b82f6; outline-offset: 2px` (meets WCAG 2.4.11 focus visible)

**Disabled state:** Not applicable. Button is either shown or hidden (no half-disabled state).

---

### 2. Report Dialog Panel (CAP-01) — anchored `<div role="dialog">` in shadow root

**Dimensions:**
- `min-width: 220px`
- `max-width: 320px`
- `width: auto`
- `padding: 12px 14px` (see Exceptions — compact-popup density convention, D-02)

**Visual shell (matches cite-popup exactly):**
- `background: #ffffff`
- `border: 1px solid #e5e7eb`
- `border-radius: 8px`
- `box-shadow: 0 2px 8px rgba(0,0,0,0.14)` (citation-ui.js:423)
- `font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif`
- `font-size: 13px`
- `color: #1a1a1a`
- `position: relative`
- `pointer-events: auto`

**ARIA attributes:**
- `role="dialog"`, `aria-modal="true"`, `aria-label="Report a citation problem"`

**Positioning:** Same anchored logic as `showCitationPopup` — `top = rect.bottom + 8`, clamped so panel does not overflow viewport; `left = rect.left`, clamped with 8px inset.

---

### 3. Category Radio Group (CAP-01)

Four options rendered as native `<input type="radio">` elements with `<label>` wrappers.

| Value | Label | Auto-selected for |
|-------|-------|-------------------|
| `inaccurate_citation` | Inaccurate citation | yellow-confidence outcomes (TRIG-02) |
| `no_match` | No match found | no-match / paragraph-not-found errors (TRIG-01) |
| `tool_not_working` | Tool not working | Worker errors, PDF-not-available, lookup-failed (TRIG-01 / TRIG-03) |
| `other` | Other | Never auto-selected; available for manual selection |

**States:**
- Default: `<input>` with `border: 1px solid #d1d5db`, `accent-color: #3b82f6`
- Selected: native radio checked state; label `font-weight: 500`, `color: #1a1a1a`
- Hover: label `color: #1a1a1a`
- Tab order: radio group receives tab focus as a group; arrow keys cycle within group (native radio behavior)

**Layout:** `display: flex; flex-direction: column; gap: 6px; margin-bottom: 10px`
(see Exceptions — both values are compact-popup density conventions, D-02)

Each option: `<label style="display:flex; align-items:center; gap:6px; cursor:pointer">`
(see Exceptions — 6px gap is compact-popup density convention, D-02)

---

### 4. Note Textarea (CAP-01)

- Element: `<textarea class="cite-report-note" maxlength="256" rows="3" placeholder="Optional — add context for the maintainer">`
- `width: 100%; box-sizing: border-box`
- `border: 1px solid #d1d5db; border-radius: 4px`
- `padding: 6px 8px` (see Exceptions — compact-popup density convention, D-02)
- `font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif`
- `font-size: 13px; color: #1a1a1a`
- `resize: vertical`
- `background: #f9fafb`

**Focus state:** `border-color: #3b82f6; outline: none; box-shadow: 0 0 0 2px rgba(59,130,246,0.2)`

**Character counter (live):**
- Element: `<div class="cite-report-counter">` positioned `text-align: right; font-size: 11px; color: #6b7280; margin-top: 2px`
  (see Exceptions — 2px margin-top is compact-popup density convention, D-02)
- Content: `"0 / 256"` updating on `input` event; counts `note.length` (UTF-16 code units per Pitfall 7 resolution)
- At limit (256): color changes to `#dc2626` (red) to signal cap reached

---

### 5. Privacy Disclosure (CAP-01) — one-line, always visible

- Element: `<div class="cite-report-disclosure">`
- Copy: `"Includes patent #, your selection, URL, extension version — "`
  followed by `<button class="cite-report-whats-included-toggle">see what's sent</button>` (acts as the expand trigger)
- `font-size: 11px; color: #6b7280; margin-bottom: 8px`
- The inline button is styled as a text link: `background: none; border: none; color: #3b82f6; font-size: 11px; cursor: pointer; padding: 0; text-decoration: underline`

---

### 6. "What's included" Collapsible Panel (CAP-02)

**Collapsed state (default — D-04):** Height 0, `overflow: hidden`, toggle shows "see what's sent".

**Expanded state:** `border-top: 1px solid #f3f4f6; margin-top: 8px; padding-top: 8px`

**Field list (friendly labels — D-03):**

| Label | Value source | Shown when |
|-------|-------------|------------|
| Patent number | `context.patentNumber` | Always |
| The text you selected | `context.selectionText` | When `includeSelectionText: true`; **hidden** when toggle is ON |
| Page address | `window.location.href` | Always |
| Browser & OS | `context.browser` + `context.os` | Always |
| Extension version | `context.extensionVersion` | Always |
| Problem category | selected radio label | Always |
| Your note | note text (or "none") | Always |
| Recent error log | N entries | Only when buffer non-empty; label "N recent error entries" |
| Diagnostic: scroll position | `context.scrollY` px | Always (even if 0) |
| Diagnostic: viewport size | `context.viewportWidth` × `context.viewportHeight` | Always |
| Diagnostic: selected node | `context.xpathNode` (truncated to 60 chars with `…`) | Only if non-null |
| Diagnostic: PDF status | `context.pdfParseStatus` | Always |

**Layout:** `display: flex; flex-direction: column; gap: 4px`

Each row: `display: flex; justify-content: space-between; font-size: 11px`
- Label: `color: #6b7280; flex-shrink: 0; padding-right: 8px`
- Value: `color: #1a1a1a; text-align: right; word-break: break-all`

**[Remove selection text] toggle:**
- Placed as last item in the expanded panel, below the divider
- Element: `<label style="display:flex; align-items:center; gap:6px; cursor:pointer; margin-top:6px; font-size:11px; color:#374151">`
  with `<input type="checkbox" class="cite-report-selection-toggle">` + text "Remove selection text"
  (see Exceptions — 6px gap and 6px margin-top are compact-popup density conventions, D-02)
- Checkbox `accent-color: #3b82f6`
- When checked: hides the "The text you selected" row above; updates `includeSelectionText` state
- Initial state: loaded from `chrome.storage.local` key `reportDialogRemoveSelectionText` (CAP-02 sticky)
- On change: immediately save to `chrome.storage.local`; immediately update the row visibility

---

### 7. Button Row (CAP-01 / CAP-04)

Layout: `display: flex; justify-content: flex-end; gap: 8px; margin-top: 12px`

**Submit button:**
- `<button class="cite-report-submit">`
- Default label: "Submit report"
- `background: #ecfdf5; border: 1px solid #a7f3d0; color: #065f46`
- `border-radius: 4px; padding: 4px 12px; font-size: 13px; font-weight: 500; cursor: pointer`
- Hover: `background: #d1fae5`
- Focus: `outline: 2px solid #059669; outline-offset: 2px`
- **Loading state** (while `sendMessage` in flight): `disabled` attribute; label changes to "Submitting…"; `opacity: 0.6; cursor: not-allowed`
- **Disabled state:** identical to loading state; occurs while `category` is null (should not be possible since category is always pre-selected or chosen by user, but defensive)

**Cancel button:**
- `<button class="cite-report-cancel">`
- Label: "Cancel"
- `background: #f9fafb; border: 1px solid #d1d5db; color: #374151`
- `border-radius: 4px; padding: 4px 12px; font-size: 13px; font-weight: 400; cursor: pointer`
- Hover: `background: #f3f4f6`
- Focus: `outline: 2px solid #3b82f6; outline-offset: 2px`
- Click: dismiss dialog without submission

---

## Interaction States

### Report Button States

| State | Visual |
|-------|--------|
| Hidden (green outcome) | `display: none` — button not in DOM; TRIG-04 invariant |
| Icon-only (not in nudge) | `⚑` glyph, muted `#6b7280`, transparent background |
| Nudge (failure/yellow/error) | `⚑ Report a problem` text, amber-tinted background |
| Hover | `#f3f4f6` background |
| Focus | 2px blue outline |
| Active/pressed | `opacity: 0.8` |

### Dialog Panel States

| State | Trigger | Visual response |
|-------|---------|----------------|
| Open | Report button click | Panel appended to shadow root, focus moves to first radio (or first focusable element) |
| Submitting | Submit button click | Submit button disabled + "Submitting…", Cancel stays active |
| Submit success | `result.ok === true` | Panel removed; `showSuccessToast('Report sent — thank you', rect)` |
| Submit queued | `result.queued === true` | Panel removed; `showSuccessToast('Report saved — will retry when online', rect)` |
| Submit rate-limited | `result.rateLimited === true` | Panel removed; `showFailureToast('Too many reports in a short period — please wait a few minutes', rect)` |
| Submit dropped | `result.dropped === true` | Panel removed silently (no toast per Phase 3 D-07) |
| Escape dismiss | Escape key inside shadow root | `e.stopPropagation(); e.preventDefault()` then dismiss; focus restored to Report button |
| Cancel dismiss | Cancel button click | Dismiss; focus restored to Report button |
| Click-outside dismiss | `mousedown` outside `citationHost` | Same as Cancel (reuses citation-ui.js:224-231 pattern) |
| "What's included" expand | Toggle button click | Panel height transitions; aria-expanded flips |

### Shadow DOM Lifecycle Constraint (RESEARCH Pitfall 1)

The dialog panel **must not** call `getCitationHost()` while it is open. The sequence on submit-close is:
1. Remove only the dialog element from the shadow root (`panel.remove()`)
2. Then call `showSuccessToast` / `showFailureToast` which call `getCitationHost()` safely

---

## Accessibility Contract (CAP-04)

| Requirement | Implementation |
|-------------|---------------|
| `role="dialog"` | On the panel `<div>` |
| `aria-modal="true"` | On the panel `<div>` |
| `aria-label` | `"Report a citation problem"` on panel |
| `aria-label` on Report button | `"Report a problem with this citation"` |
| `aria-expanded` | On the "see what's sent" toggle button; `false` collapsed, `true` expanded |
| `aria-label` on textarea | `"Optional note, 256 character limit"` |
| `aria-live` | On character counter: `aria-live="polite"` so screen readers announce count changes |
| Focus trap | `keydown` on `shadowRoot`; Tab cycles within dialog; Shift+Tab reverses; uses `shadowRoot.activeElement` (not `document.activeElement`) |
| Focus on open | First radio button receives focus (`setTimeout(..., 0)`) |
| Focus on close | Report button (`triggerElement.focus()`) on all dismiss paths |
| Escape key | `e.stopPropagation(); e.preventDefault()` in shadow root keydown handler to prevent Google Patents keyboard bindings from intercepting |
| Tab escape guard | `e.stopPropagation()` on Tab key in shadow root keydown handler |

**Focusable elements in tab order:**
1. First radio (inaccurate_citation) — initially focused on dialog open
2. Second radio (no_match)
3. Third radio (tool_not_working)
4. Fourth radio (other)
5. Textarea
6. "see what's sent" toggle button
7. [Remove selection text] checkbox (only visible when expanded)
8. Submit button
9. Cancel button

---

## Copywriting Contract

All copy is **locked** — sourced from REQUIREMENTS.md (authority) and CONTEXT.md D-03/D-07.

| Element | Copy | Source |
|---------|------|--------|
| Primary CTA | "Submit report" | CAP-01 (verb + noun; not "Send" to match the feedback toast) |
| Cancel button | "Cancel" | Standard; CAP-04 |
| Report button (icon-only) | `⚑` (flag glyph) with `title="Report a problem"` | CAP-03, D-06 |
| Report button (nudge) | `⚑ Report a problem` | CAP-03, D-06 |
| Privacy disclosure (one-line) | "Includes patent #, your selection, URL, extension version — see what's sent" | CAP-01 (verbatim from requirement) |
| Note placeholder | "Optional — add context for the maintainer" | D-07 (note is optional for all categories) |
| Collapsible toggle (collapsed) | "see what's sent" (inline in disclosure sentence) | CAP-02 |
| [Remove selection text] | "Remove selection text" | CAP-02 (verbatim from requirement) |
| Submit loading | "Submitting…" | Default (no upstream spec; standard pattern) |
| Toast: ok | "Report sent — thank you" | QUEUE-04 / CONTEXT.md specifics (authoritative) |
| Toast: queued | "Report saved — will retry when online" | QUEUE-04 / CONTEXT.md specifics (authoritative) |
| Toast: rate-limited | "Too many reports in a short period — please wait a few minutes" | REQUIREMENTS.md LIMIT-03 (wins over ROADMAP SC2 per CONTEXT.md factual correction) |
| No empty state | The dialog has no empty state — it always opens with at least the auto-selected category pre-populated | — |
| No destructive confirmation | Phase 4 has no destructive actions requiring confirmation (Cancel and dismiss are non-destructive; no data is sent) | CAP-04 |

**Radio option labels (verbatim — must match `REPORT_CATEGORIES` mapping):**

| Category constant | Display label |
|------------------|---------------|
| `inaccurate_citation` | Inaccurate citation |
| `no_match` | No match found |
| `tool_not_working` | Tool not working |
| `other` | Other |

---

## Auto-Surface and Trigger Logic (TRIG-01..04)

These are behavioral rules, not visual rules, but they gate which visual state the Report button enters on load.

| Outcome | confidenceTier | Category auto-selected | Button state |
|---------|----------------|----------------------|--------------|
| Green (confidence ≥ 0.95) | `green` | — | Hidden entirely (TRIG-04) |
| Yellow (0.80 ≤ confidence < 0.95) | `yellow` | `inaccurate_citation` | Nudge mode |
| Red-confidence success (confidence < 0.80) | `red` | `inaccurate_citation` | Nudge mode |
| No match / paragraph not found | `red` | `no_match` | Nudge mode |
| PDF failed / not available / lookup-failed | `red` | `tool_not_working` | Nudge mode |
| Transient state ("PDF still being analyzed") | — | — | No button (transient, not a failure) |
| Not on a patent page | — | — | No button (out of scope) |

---

## Submit → Toast Result Mapping

Post-close toast behavior. Dialog is **always removed first**, then toast is shown (Pitfall 1 mitigation).

| Phase 3 return field | Toast function called | Copy |
|---------------------|----------------------|------|
| `ok: true` | `showSuccessToast(copy, rect)` | "Report sent — thank you" |
| `queued: true` | `showSuccessToast(copy, rect)` | "Report saved — will retry when online" |
| `rateLimited: true` | `showFailureToast(copy, rect)` | "Too many reports in a short period — please wait a few minutes" |
| `dropped: true` | (none — silent) | — |

---

## Diagnostic Capture Timing

| Field | Captured at | Not at |
|-------|-------------|--------|
| `xpathNode` | Report button click (before user interacts with dialog) | Submit time — selection clears on click (Pitfall 3) |
| `selectionText` | Report button click | Submit time |
| `scrollY`, `viewportWidth`, `viewportHeight` | Report button click (`window.scrollY`, `window.innerWidth`, `window.innerHeight`) | Submit time |
| `pdfParseStatus` | Submit time (async `chrome.storage.local.get('currentPatent')`) | Click time — derived from currentPatent; async is safe here |
| `bugReportErrorBuffer` | Submit time (async `chrome.storage.local.get('bugReportErrorBuffer')`) | Pre-capture unnecessary |

---

## Registry Safety

| Registry | Blocks used | Safety gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable (no shadcn in this project) |
| npm packages | none | not applicable (zero new dependencies) |

**Registry safety: not applicable.** This phase adds zero new npm packages, no third-party
registries, and no script-injected resources. All UI is implemented with native DOM APIs.

---

## Design Token Summary

Extracted verbatim from `src/content/citation-ui.js`. Executor must not introduce new hex values.

```
/* Surfaces */
--surface-primary: #ffffff;       /* citation-ui.js:419 */
--surface-secondary: #f9fafb;     /* citation-ui.js:452 */
--surface-hover: #f3f4f6;         /* citation-ui.js:460 */
--surface-error: #fef2f2;         /* citation-ui.js:489 */
--surface-success: #ecfdf5;       /* citation-ui.js:519 */

/* Borders */
--border-default: #e5e7eb;        /* citation-ui.js:421, 501 */
--border-secondary: #d1d5db;      /* citation-ui.js:449 */
--border-success: #a7f3d0;        /* citation-ui.js:523 */
--border-error: #fecaca;          /* citation-ui.js:540 */

/* Text */
--text-primary: #1a1a1a;          /* citation-ui.js:427 */
--text-secondary: #6b7280;        /* citation-ui.js:475 */
--text-muted: #374151;            /* citation-ui.js:451 */
--text-success: #065f46;          /* citation-ui.js:526 */
--text-error: #991b1b;            /* citation-ui.js:543 */

/* Accent */
--accent-blue: #3b82f6;           /* citation-ui.js:399 — Cite button only */
--accent-blue-dark: #2563eb;      /* citation-ui.js:409 — hover only */
--accent-green: #10b981;          /* citation-ui.js:469 — confidence dot high */
--accent-amber: #f59e0b;          /* citation-ui.js:470 — confidence dot medium */
--accent-red: #ef4444;            /* citation-ui.js:471 — confidence dot low */

/* Radius */
--radius-sm: 4px;                 /* copy button: citation-ui.js:448 */
--radius-md: 6px;                 /* loading, float-btn: citation-ui.js:393 */
--radius-lg: 8px;                 /* popup panel: citation-ui.js:422 */
--radius-pill: 12px;              /* success toast: citation-ui.js:522 */

/* Shadow */
--shadow-popup: 0 2px 8px rgba(0,0,0,0.14);   /* citation-ui.js:423 */
--shadow-float: 0 1px 4px rgba(0,0,0,0.18);   /* citation-ui.js:405 */
--shadow-toast: 0 1px 4px rgba(0,0,0,0.10);   /* citation-ui.js:527 */

/* Animation */
--fade-in: cite-fade-in 0.15s ease forwards;  /* citation-ui.js:531 */
```

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

## Pre-Population Traceability

| Decision / Token | Source |
|-----------------|--------|
| Anchored panel (not centered modal) | CONTEXT.md D-01 |
| Reuse citation-pill aesthetic | CONTEXT.md D-02 |
| Friendly field labels in "What's included" | CONTEXT.md D-03 |
| Panel collapsed by default | CONTEXT.md D-04 |
| Dialog opens on click only (no auto-open) | CONTEXT.md D-05 |
| Icon-only + nudge on failure; hidden on green | CONTEXT.md D-06, TRIG-04 |
| Note is optional for all categories | CONTEXT.md D-07 |
| Error buffer: extension-tagged only | CONTEXT.md D-08 |
| All hex color tokens | `src/content/citation-ui.js` CSS functions (extracted verbatim) |
| Font stack | `citation-ui.js:425` |
| Spacing values (8px popup gap, 8px inset) | `citation-ui.js:424, 213-214` |
| Border-radius values | `citation-ui.js:393, 422, 448, 522` |
| Shadow values | `citation-ui.js:405, 423, 527` |
| Toast copy (ok / queued / rateLimited) | REQUIREMENTS.md QUEUE-04 + LIMIT-03 (LIMIT-03 wording wins per CONTEXT.md correction) |
| Radio labels | `src/shared/constants.js` REPORT_CATEGORIES |
| Trigger→category mapping | RESEARCH.md Pattern 6 / trigger site map |
| getCitationHost() shadow-wipe pitfall | RESEARCH.md Pitfall 1 (verified in citation-ui.js:27-32) |
| Focus trap via shadowRoot.activeElement | RESEARCH.md Pattern 2 |
| XPath capture at click time | RESEARCH.md Pitfall 3 |
| 256 char counter counts code units | RESEARCH.md Pitfall 7 |
| pdfParseStatus derivation | RESEARCH.md Pattern 4 (verified in service-worker.js:391-406) |
