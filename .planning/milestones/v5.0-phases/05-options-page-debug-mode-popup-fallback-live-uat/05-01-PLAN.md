---
phase: 05-options-page-debug-mode-popup-fallback-live-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/options/options.html
  - src/options/options.js
  - src/content/content-script.js
  - src/content/citation-ui.js
autonomous: true
requirements: [DBG-01, DBG-02]
must_haves:
  truths:
    - "DBG-01: options page has a debugMode checkbox (default false) persisted to chrome.storage.sync, following the includePatentNumber pattern verbatim"
    - "DBG-02: content-script reads debugMode live on each citation via the onChanged → cachedSettings pattern; toggling the options checkbox changes Report-button visibility on the very next citation with no extension reload"
    - "D-06: debugMode added to DEFAULT_SETTINGS (default false) and to the onChanged sync branch; threaded through the reportOutcome object to citation-ui.js (no circular import)"
    - "D-05: on a green outcome with debugMode ON, the Report button is the plain icon-only ⚑ glyph with NO amber nudge tint; non-green outcomes keep the Phase-4 amber nudge unchanged"
    - "TRIG-04 invariant preserved: with debugMode OFF, the Report button is NOT visible on green outcomes (the confidenceTier !== 'green' token remains in citation-ui.js)"
    - "The green-debug icon-only Report button retains an aria-label (e.g. 'Report a problem with this citation') for accessibility (checker FLAG)"
  artifacts:
    - path: "src/options/options.html"
      provides: "debugMode checkbox setting-group"
      contains: "id=\"debugMode\""
    - path: "src/options/options.js"
      provides: "debugMode load + auto-save listener"
      contains: "debugMode"
    - path: "src/content/content-script.js"
      provides: "debugMode in DEFAULT_SETTINGS + onChanged + threaded into reportOutcome"
      contains: "debugMode"
    - path: "src/content/citation-ui.js"
      provides: "relaxed TRIG-04 guard with debugMode OR-condition + plain-icon green path"
      contains: "isGreenDebug"
  key_links:
    - from: "src/options/options.js"
      to: "chrome.storage.sync.debugMode"
      via: "addEventListener('change') → storage.sync.set"
      pattern: "chrome\\.storage\\.sync\\.set.*debugMode"
    - from: "src/content/content-script.js"
      to: "src/content/citation-ui.js"
      via: "reportOutcome.debugMode passed into showCitationPopup"
      pattern: "debugMode: cachedSettings\\.debugMode"
    - from: "src/content/citation-ui.js"
      to: "Report button visibility on green"
      via: "guard relaxation"
      pattern: "confidenceTier !== 'green' \\|\\| reportOutcome\\.debugMode"
---

<objective>
Ship the Debug Mode feature (DBG-01, DBG-02): an options-page checkbox that, when enabled, makes the Report button appear on EVERY citation outcome including green successes — so the maintainer can catch confidently-wrong citations the tool did not flag.

Purpose: Closes DBG-01/DBG-02 of Phase 5. The Phase-4 TRIG-04 invariant ("no Report button on green") relaxes only when debugMode is on, and the green-debug button uses a plain icon (D-05) — no alarming amber nudge, because green is not a failure.

Output: debugMode checkbox in options; live-read in content script; relaxed guard in citation-ui with plain-icon green path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-CONTEXT.md
@.planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-PATTERNS.md
@.planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-UI-SPEC.md

<interfaces>
<!-- Extracted from codebase — executor should use these directly, no exploration needed. -->

From src/content/content-script.js (DEFAULT_SETTINGS ~185-189, onChanged ~204-209, showCitationPopup call sites ~491-501 and ~594-604):
- DEFAULT_SETTINGS object: { triggerMode, displayMode, includePatentNumber } — add debugMode: false
- onChanged listener guards each sync key: `if (changes.<key>) cachedSettings.<key> = changes.<key>.newValue;`
- Both showCitationPopup call sites pass a reportOutcome object: { category, confidenceTier } — add `debugMode: cachedSettings.debugMode`

From src/content/citation-ui.js (TRIG-04 guard ~170-182, showReportDialog caller ~179, second caller in showErrorPopup ~304):
- Current guard: `if (reportOutcome && reportOutcome.confidenceTier !== 'green') { ... reportBtn.textContent = '⚑ Report a problem'; ... }`
- reportBtn already sets `.title = 'Report a problem'` and `.setAttribute('aria-label', 'Report a problem with this citation')`
- showCitationPopup signature: `showCitationPopup(citation, rect, confidence, displayMode, matchedText, reportOutcome = null)`

From src/options/options.js (load ~44-57, auto-save ~73-79) and options.html (includePatentNumber group ~214-224):
- Load: chrome.storage.sync.get({ ...defaults }, (settings) => { ...; if (patentNumCheckbox) patentNumCheckbox.checked = settings.includePatentNumber; })
- Save: `if (patentNumCheckbox) { patentNumCheckbox.addEventListener('change', () => { chrome.storage.sync.set({ includePatentNumber: patentNumCheckbox.checked }, () => showSaved(patentNumSaved)); }); }`
- showSaved(el) fades a Saved span in/out over 1500ms
</interfaces>

<note>
05-PATTERNS.md has verbatim copy-ready snippets for every edit in this plan (DBG-01 checkbox HTML, options.js load+save, DEFAULT_SETTINGS, onChanged, citation-ui guard). 05-UI-SPEC.md §1, §Debug Mode Report Button on Green, and the Accessibility Contract are the visual authority. Do NOT introduce new colors/sizes — inherit options page + Phase-4 tokens.
</note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DBG-01 options checkbox (HTML + auto-save)</name>
  <read_first>
    - src/options/options.html (includePatentNumber .setting-group, lines ~214-224 — the verbatim analog)
    - src/options/options.js (storage.sync.get defaults ~44-57; patentNumCheckbox load + auto-save block ~73-79; showSaved ~28-35)
    - 05-PATTERNS.md "src/options/options.html (config)" + "src/options/options.js (controller)" sections (copy-ready snippets)
    - 05-UI-SPEC.md "### 1. DBG-01 Debug Mode Checkbox" + "Design Token Summary > Options Page Tokens"
  </read_first>
  <action>
    In src/options/options.html, append a new .setting-group after the includePatentNumber group (still inside .settings-card, BEFORE the closing .settings-card div). Use the DBG-01 structure from 05-PATTERNS.md / 05-UI-SPEC.md §1 verbatim: a .setting-label containing only `<span class="saved-feedback" id="debugModeSaved">Saved</span>`, a .checkbox-row with `<input type="checkbox" id="debugMode">` and `<label for="debugMode">Debug Mode — always show Report button</label>`, and a `.setting-description` with margin-top:8px and the exact copy "Surfaces the Report button on every citation, including successful ones. Useful for catching confidently-wrong citations the tool didn't flag." Introduce no new CSS — the existing .setting-group + .setting-group border-top rule and the .checkbox-row accent-color (#2563eb) apply automatically.
    In src/options/options.js: add `debugMode: false` to the chrome.storage.sync.get defaults object (per DBG-01). After settings resolve, look up `const debugModeCheckbox = document.getElementById('debugMode')` and set `debugModeCheckbox.checked = settings.debugMode` (guarded by `if (debugModeCheckbox)`). Add an auto-save block copied verbatim from the patentNumCheckbox pattern: look up `const debugModeSaved = document.getElementById('debugModeSaved')`, then on the checkbox 'change' event call `chrome.storage.sync.set({ debugMode: debugModeCheckbox.checked }, () => showSaved(debugModeSaved))`. Do NOT add the #report section or hash routing here — that is plan 05-03.
  </action>
  <verify>
    <automated>npm run build && grep -q 'id="debugMode"' dist/chrome/options/options.html && grep -q 'chrome.storage.sync.set({ debugMode' dist/chrome/options/options.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - options.html contains a setting-group with `id="debugMode"` checkbox, label "Debug Mode — always show Report button", and the DBG-01 description text verbatim
    - options.js storage.sync.get defaults include `debugMode: false`
    - options.js has a 'change' listener that calls `chrome.storage.sync.set({ debugMode: ... })` then `showSaved(debugModeSaved)`
    - `npm run build` succeeds; the debugMode checkbox markup is present in dist/chrome/options/options.html
    - No new CSS rules, colors, or font sizes introduced (UI-SPEC: inherit existing tokens)
  </acceptance_criteria>
  <done>debugMode checkbox renders in options, toggles persist to chrome.storage.sync, "Saved" feedback fires on change.</done>
</task>

<task type="auto">
  <name>Task 2: DBG-02 live-read + D-05 plain-icon green path</name>
  <read_first>
    - src/content/content-script.js (DEFAULT_SETTINGS ~185-189; onChanged sync branch ~204-209; both showCitationPopup call sites ~491-501 and ~594-604)
    - src/content/citation-ui.js (TRIG-04 guard ~170-182; showReportDialog caller ~179; showErrorPopup caller ~304)
    - 05-PATTERNS.md "src/content/content-script.js" + "src/content/citation-ui.js" sections (DBG-02 additions, exact tokens)
    - 05-UI-SPEC.md "### Debug Mode Report Button on Green (D-05 / DBG-02)" (the relaxed-guard code + amber-only-on-non-green rule)
    - tests/unit/report-trigger-mapping.test.js:207-231 (the getCitationHost ban + `confidenceTier !== 'green'` token guard — BOTH must stay satisfied)
  </read_first>
  <action>
    In src/content/content-script.js: append `debugMode: false` to DEFAULT_SETTINGS (per D-06; comment: DBG-01/02 default off, TRIG-04 holds). Inside the onChanged listener's `if (area === 'sync')` block, add one line: `if (changes.debugMode) cachedSettings.debugMode = changes.debugMode.newValue;`. At BOTH showCitationPopup call sites, add `debugMode: cachedSettings.debugMode` to the reportOutcome object literal (alongside category + confidenceTier). This threads the live value through the call signature and avoids a circular import from citation-ui.js back into content-script.js (RESEARCH Pattern 3).
    In src/content/citation-ui.js: relax the TRIG-04 guard to `if (reportOutcome && (reportOutcome.confidenceTier !== 'green' || reportOutcome.debugMode))`. KEEP the literal substring `confidenceTier !== 'green'` intact (existing Vitest guard asserts it). Inside the block, compute `const isGreenDebug = reportOutcome.confidenceTier === 'green';`. Set `reportBtn.textContent = isGreenDebug ? '⚑' : '⚑ Report a problem';` (D-05: plain icon on green debug). Apply the amber nudge inline styles (background rgba(245,158,11,0.08), color #92400e, fontSize 13px, fontWeight 500, padding 2px 6px, borderRadius 4px) ONLY when `!isGreenDebug`; the green-debug button keeps the transparent base .cite-report-btn style (#6b7280). PRESERVE the existing `reportBtn.title = 'Report a problem'` AND `reportBtn.setAttribute('aria-label', 'Report a problem with this citation')` on BOTH paths (the icon-only green button MUST retain its aria-label — checker FLAG). Do NOT change the showReportDialog call signature in this plan beyond what already exists; the mountContext refactor (passing `{ mode: 'shadow', root: shadow }`) lands in plan 05-02 which owns the caller update. If plan 05-02 has not yet run, leave the existing `showReportDialog(shadow, ...)` call as-is — this plan only touches the visibility guard and button styling.
  </action>
  <verify>
    <automated>npm run build && npm run test:chrome 2>&1 | tail -5; npx vitest run tests/unit/report-trigger-mapping.test.js 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - content-script.js DEFAULT_SETTINGS contains `debugMode: false`
    - content-script.js onChanged sync branch contains `if (changes.debugMode) cachedSettings.debugMode = changes.debugMode.newValue;`
    - Both showCitationPopup call sites pass `debugMode: cachedSettings.debugMode` in the reportOutcome object
    - citation-ui.js guard reads `reportOutcome.confidenceTier !== 'green' || reportOutcome.debugMode` (substring `confidenceTier !== 'green'` still present — existing guard test passes)
    - citation-ui.js has `isGreenDebug` branch: textContent `⚑` (no nudge text, no amber) on green-debug; amber inline styles applied only on non-green
    - The Report button retains both `.title` and `.setAttribute('aria-label', ...)` on the green-debug icon-only path
    - tests/unit/report-trigger-mapping.test.js passes (getCitationHost ban + green-guard token both satisfied)
  </acceptance_criteria>
  <done>With debugMode OFF, green outcomes show no Report button; with debugMode ON, green outcomes show a plain-icon ⚑ button (no amber) with an aria-label; non-green outcomes are unchanged; existing trigger-mapping tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| options page → chrome.storage.sync | User toggles a boolean; trusted UI on extension origin |
| chrome.storage.sync → content script | Boolean read live into cachedSettings; no untrusted content |
| content script → Google Patents page DOM | Report button injected into the closed-shadow citation host (existing isolation, unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | debugMode boolean in storage.sync | accept | Boolean flag only controls Report-button visibility; no privileged action; worst case is the button showing on green (intended for the toggler) |
| T-05-02 | Information Disclosure | green-debug Report button | accept | Showing the button does not submit anything; submission still requires explicit user click + Submit (Phase 4 contract); no new data path |
| T-05-03 | Elevation of Privilege | reportOutcome.debugMode threading | mitigate | Passed by value through the existing call signature (no new import); citation-ui.js gains no new capability beyond reading a boolean |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies (sixth consecutive milestone — DoD); no install tasks in this plan; nothing to slopcheck |
</threat_model>

<verification>
- `npm run build` succeeds for both targets.
- `npm run test:chrome` and `npx vitest run tests/unit/report-trigger-mapping.test.js` pass.
- Manual sanity (covered live in 05-05 UAT-05/SC1): toggling debugMode mid-session changes green-outcome Report-button visibility on the next citation without reload.
</verification>

<success_criteria>
- DBG-01: debugMode checkbox in options, default false, persisted to chrome.storage.sync, follows includePatentNumber verbatim.
- DBG-02: live-read on each citation; no reload needed.
- D-05: plain icon on green-debug, no amber.
- TRIG-04 preserved when debugMode off; existing guard tests green.
</success_criteria>

<output>
Create `.planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-01-SUMMARY.md` when done.
</output>
