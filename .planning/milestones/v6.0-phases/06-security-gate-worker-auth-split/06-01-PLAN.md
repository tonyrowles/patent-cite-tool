---
phase: 06-security-gate-worker-auth-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build.js
  - src/offscreen/offscreen.js
  - src/firefox/pdf-pipeline.js
  - src/shared/report-transport.js
  - .gitignore
  - .github/workflows/ci.yml
autonomous: true
requirements: [SEC-01, SEC-02]
user_setup:
  - service: github-actions
    why: "CI build step needs the rotated PROXY_TOKEN as a repository secret; without it the next push fails the build (build aborts when PROXY_TOKEN env is unset)."
    env_vars:
      - name: PROXY_TOKEN
        source: "GitHub repo → Settings → Secrets and variables → Actions → New repository secret"

must_haves:
  truths:
    - "SEC-01: grep -rn for the literal token string in src/ returns zero matches; only the __PROXY_TOKEN__ placeholder remains"
    - "SEC-02: running the extension build with PROXY_TOKEN set injects that value into the bundle; running it with PROXY_TOKEN unset aborts the build with a non-zero exit code"
    - "The committed source carries no token literal — the token enters only at build time from process.env.PROXY_TOKEN"
  artifacts:
    - path: "scripts/build.js"
      provides: "esbuild define for __PROXY_TOKEN__ on every config + fail-loud guard when PROXY_TOKEN is unset"
      contains: "__PROXY_TOKEN__"
    - path: "src/offscreen/offscreen.js"
      provides: "PROXY_TOKEN sourced from __PROXY_TOKEN__ build define, no literal"
      contains: "__PROXY_TOKEN__"
    - path: "src/firefox/pdf-pipeline.js"
      provides: "PROXY_TOKEN sourced from __PROXY_TOKEN__ build define, no literal"
      contains: "__PROXY_TOKEN__"
    - path: "src/shared/report-transport.js"
      provides: "PROXY_TOKEN sourced from __PROXY_TOKEN__ build define, no literal"
      contains: "__PROXY_TOKEN__"
    - path: ".gitignore"
      provides: "root-level .dev.vars and .env ignored so local token files never commit"
      contains: ".env"
    - path: ".github/workflows/ci.yml"
      provides: "PROXY_TOKEN secret wired into the Build step env"
      contains: "PROXY_TOKEN"
  key_links:
    - from: "scripts/build.js"
      to: "src/**/*.js (__PROXY_TOKEN__)"
      via: "esbuild define text substitution at compile time"
      pattern: "__PROXY_TOKEN__"
    - from: ".github/workflows/ci.yml"
      to: "scripts/build.js (process.env.PROXY_TOKEN)"
      via: "step-scoped env: PROXY_TOKEN secret"
      pattern: "PROXY_TOKEN"
---

<objective>
Remove the compromised `PROXY_TOKEN` literal from all committed source and replace it with a build-time esbuild `define` injection sourced from `process.env.PROXY_TOKEN`. After this plan, the token exists in committed source only as the placeholder identifier `__PROXY_TOKEN__`; its real value is supplied at build time (CI secret or git-ignored local file) and substituted by esbuild into the bundle.

Purpose: SEC-01/SEC-02 — the live token is already compromised (plaintext in three source files). This plan lands all the CODE for rotation. The live secret rotation (`wrangler secret put`) and the GitHub secret creation are human steps handled in plan 06-04.
Output: Token-free source, esbuild define wiring with a fail-loud build guard, updated `.gitignore`, CI build step env wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-security-gate-worker-auth-split/06-CONTEXT.md
@.planning/phases/06-security-gate-worker-auth-split/06-RESEARCH.md
@.planning/phases/06-security-gate-worker-auth-split/06-PATTERNS.md

<interfaces>
<!-- esbuild define performs literal text substitution before bundling. -->
<!-- Verified: esbuild.transform('const x = __PROXY_TOKEN__;', { define: { '__PROXY_TOKEN__': '"v"' } }) → 'const x = "v";' -->

scripts/build.js structure (verified by grep):
- args parsed at line 23 (`const args = process.argv.slice(2);`)
- four named esbuild config functions returning config objects:
  getIifeConfig (line 32), getEsmConfig (line 43), getFirefoxIifeConfig (line 118), getFirefoxEsmConfig (line 129)
- two inline esbuild.build({...}) calls for matching-exports at lines ~198 and ~210
- two esbuild.context(...) calls (watch mode) at lines ~229-230 that reuse getIifeConfig/getEsmConfig
- main().catch((err) => { console.error(err); process.exit(1); }) error-exit pattern

Token literal positions (verified by grep -rn 'PROXY_TOKEN' src/):
- src/offscreen/offscreen.js:24      const PROXY_TOKEN = '4509b9943f...';
- src/firefox/pdf-pipeline.js:23     const PROXY_TOKEN = '4509b9943f...';
- src/shared/report-transport.js:27  const PROXY_TOKEN = '4509b9943f...';
(Downstream usages at offscreen 249/298/425, pdf-pipeline 196/325/433, report-transport 189 all reference the `PROXY_TOKEN` const by name and need NO change — preserve the const name.)

.gitignore line 3: worker/.dev.vars (existing analog for the new root entries)
.github/workflows/ci.yml: Build step at lines 44-45 (`- name: Build (Chrome + Firefox)` / `run: npm run build`); an existing env: block pattern exists at line ~91 on another step.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire esbuild define + fail-loud token guard into scripts/build.js</name>
  <files>scripts/build.js</files>
  <read_first>
    - scripts/build.js (the whole file — confirm the four config functions, the two inline matching-exports builds, the watch-mode context calls, and the main().catch exit pattern)
    - .planning/phases/06-security-gate-worker-auth-split/06-RESEARCH.md (section "esbuild `define` Injection (SEC-02)")
    - .planning/phases/06-security-gate-worker-auth-split/06-PATTERNS.md (section "scripts/build.js — esbuild `define` injection")
  </read_first>
  <behavior>
    - Build with PROXY_TOKEN unset → process exits non-zero with an error message naming PROXY_TOKEN; no bundle produced.
    - Build with PROXY_TOKEN=somevalue → the four bundle outputs contain the substituted value where `__PROXY_TOKEN__` appeared in source.
    - The matching-exports bundles and watch-mode context builds also carry the define so no `__PROXY_TOKEN__` identifier survives in any output.
  </behavior>
  <action>
    Immediately after the arg-parsing block (line 23 `const args = process.argv.slice(2);`), read `const PROXY_TOKEN = process.env.PROXY_TOKEN;` and add a fail-loud guard: if it is falsy, `console.error` a message that names the `PROXY_TOKEN` environment variable and call `process.exit(1)` (mirrors the existing `main().catch` exit-non-zero convention). Add a `define: { '__PROXY_TOKEN__': JSON.stringify(PROXY_TOKEN) }` field to EVERY esbuild config object that bundles src/ code: the four named config functions (getIifeConfig, getEsmConfig, getFirefoxIifeConfig, getFirefoxEsmConfig) AND the two inline `esbuild.build({...})` matching-exports configs. The watch-mode `esbuild.context(...)` calls reuse getIifeConfig/getEsmConfig so they inherit the define automatically. Do NOT touch the worker — `worker/src/index.js` reads `env.PROXY_TOKEN` from the Wrangler secret binding and is not bundled by esbuild (RESEARCH Pitfall 4). The guard must run before any esbuild call so a missing token aborts before producing a token-less bundle.
  </action>
  <verify>
    <automated>PROXY_TOKEN= node scripts/build.js 2>&1 | grep -qi 'PROXY_TOKEN' && PROXY_TOKEN= node scripts/build.js; test $? -ne 0 && echo GUARD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `PROXY_TOKEN= node scripts/build.js` exits non-zero and prints a message containing "PROXY_TOKEN".
    - `grep -c "__PROXY_TOKEN__" scripts/build.js` returns at least 6 (one define per src-bundling config).
    - `grep -c "process.env.PROXY_TOKEN" scripts/build.js` returns >= 1.
    - A guarded constant is declared before the first esbuild call.
  </acceptance_criteria>
  <done>Build aborts loudly when PROXY_TOKEN is unset; every src-bundling esbuild config carries the `__PROXY_TOKEN__` define; worker source untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the token literal in all three source files</name>
  <files>src/offscreen/offscreen.js, src/firefox/pdf-pipeline.js, src/shared/report-transport.js</files>
  <read_first>
    - src/offscreen/offscreen.js (line 24 and the three Bearer usages at 249/298/425)
    - src/firefox/pdf-pipeline.js (line 23 and usages at 196/325/433)
    - src/shared/report-transport.js (line 27 and usage at 189)
    - .planning/phases/06-security-gate-worker-auth-split/06-PATTERNS.md (section "token literal replacement")
  </read_first>
  <action>
    In each of the three files, replace the line `const PROXY_TOKEN = '4509b9943f...';` with `const PROXY_TOKEN = __PROXY_TOKEN__;` preceded by a one-line comment noting the token is injected at build time by esbuild define (SEC-02) and is never a literal. Preserve the `const PROXY_TOKEN` name verbatim so every downstream `Bearer ${PROXY_TOKEN}` usage in the same file keeps working unchanged. Make NO other edits. After this task the literal token string `4509b9943f` must not appear anywhere under src/, and the only `PROXY_TOKEN`-related token reference is the `__PROXY_TOKEN__` placeholder plus the existing `Bearer ${PROXY_TOKEN}` usages.
  </action>
  <verify>
    <automated>grep -rn "4509b9943f" src/ | grep -v '^#'; test $(grep -rc "4509b9943f" src/ | grep -v ':0$' | wc -l) -eq 0 && echo NO_LITERAL_OK && grep -rc "__PROXY_TOKEN__" src/ | grep -v ':0$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "4509b9943f" src/` returns zero matches.
    - `grep -rln "__PROXY_TOKEN__" src/` lists exactly the three files: offscreen.js, pdf-pipeline.js, report-transport.js.
    - Each file still declares `const PROXY_TOKEN = __PROXY_TOKEN__;` (name preserved).
    - No downstream `Bearer ${PROXY_TOKEN}` usage was altered.
  </acceptance_criteria>
  <done>All three literals removed; `__PROXY_TOKEN__` placeholder in place; const name and all Bearer usages preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Gitignore root token files + wire PROXY_TOKEN into CI build step</name>
  <files>.gitignore, .github/workflows/ci.yml</files>
  <read_first>
    - .gitignore (confirm existing `worker/.dev.vars` entry at line 3 as the analog)
    - .github/workflows/ci.yml (Build step at lines 44-45; the existing step-scoped `env:` block near line 91 as the analog)
    - .planning/phases/06-security-gate-worker-auth-split/06-PATTERNS.md (sections ".gitignore" and ".github/workflows/ci.yml")
  </read_first>
  <action>
    In `.gitignore`, add two new entries adjacent to the existing `worker/.dev.vars` line: `.dev.vars` and `.env` (root-level local token files for `npm run build`; without these, a developer's local token file could commit and re-expose the rotated token — RESEARCH Pitfall 8). In `.github/workflows/ci.yml`, add a step-scoped `env:` block to the existing `Build (Chrome + Firefox)` step (lines 44-45) exposing `PROXY_TOKEN: ${{ secrets.PROXY_TOKEN }}`, mirroring the existing `env:` block convention elsewhere in the workflow. Do NOT change the `run: npm run build` line or any other step. The GitHub repository secret itself is created by the human in plan 06-04 — this task only wires the reference; the build will correctly fail-loud until that secret exists.
  </action>
  <verify>
    <automated>grep -qx ".env" .gitignore && grep -qx ".dev.vars" .gitignore && grep -q "secrets.PROXY_TOKEN" .github/workflows/ci.yml && echo WIRING_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains standalone lines `.dev.vars` and `.env`.
    - `.github/workflows/ci.yml` Build step has `env:` with `PROXY_TOKEN: ${{ secrets.PROXY_TOKEN }}`.
    - The `run: npm run build` command is unchanged.
  </acceptance_criteria>
  <done>Root token files ignored; CI Build step references the PROXY_TOKEN secret; no other workflow changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed source → build artifact | The token must NOT cross this boundary in committed form; it is injected only at build time |
| developer machine / CI runner → git | Local `.dev.vars`/`.env` and CI env carry the secret; neither may be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Token literal in committed source (`src/**`) | mitigate | Replace all three literals with `__PROXY_TOKEN__` (Task 2); grep gate proves `4509b9943f` absent under src/ |
| T-06-02 | Information Disclosure | Local token file accidentally committed | mitigate | `.gitignore` adds `.dev.vars` and `.env` (Task 3) |
| T-06-03 | Tampering | Silent empty-string token from a missing env var producing a broken-but-shipping bundle | mitigate | Fail-loud build guard exits non-zero when `PROXY_TOKEN` unset (Task 1) |
| T-06-SC | Tampering | npm installs | accept | Zero new packages this phase (REQUIREMENTS.md zero-dep constraint); no install tasks, no new supply-chain surface |
</threat_model>

<verification>
- `grep -rn "4509b9943f" src/` → zero matches (old literal gone everywhere).
- `grep -rn "PROXY_TOKEN" src/` → only `__PROXY_TOKEN__` placeholder + `Bearer ${PROXY_TOKEN}` usages (no raw token value).
- `PROXY_TOKEN= node scripts/build.js` → non-zero exit, error names PROXY_TOKEN.
- `PROXY_TOKEN=smoketest123 npm run build && grep -rl "smoketest123" dist/chrome dist/firefox` → at least one bundle file contains the injected value (smoke-proves SEC-02 substitution). (This end-to-end build-smoke assertion is also exercised by the test plan 06-03.)
</verification>

<success_criteria>
- No token literal in any committed source file (SEC-01 code portion).
- esbuild `define` injects `PROXY_TOKEN` from `process.env.PROXY_TOKEN` at build time; build fails loudly without it (SEC-02).
- `.gitignore` blocks root token files; CI Build step references the GitHub secret.
</success_criteria>

<output>
Create `.planning/phases/06-security-gate-worker-auth-split/06-01-SUMMARY.md` when done.
</output>
