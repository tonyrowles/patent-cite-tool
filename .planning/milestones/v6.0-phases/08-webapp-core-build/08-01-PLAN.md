---
phase: 08-webapp-core-build
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build.js
  - webapp/index.html
  - webapp/wrangler.toml
  - package.json
autonomous: true
requirements: [APP-10]
must_haves:
  truths:
    - "`node scripts/build.js --webapp-only` exits 0 with NO PROXY_TOKEN env var set and produces dist/webapp/"
    - "dist/webapp/ contains index.html, app.bundle.js (ESM, <2MB), lib/pdf.mjs, lib/pdf.worker.mjs"
    - "Running --webapp-only after a --chrome-only build does NOT delete dist/chrome/"
    - "index.html renders the full UI-SPEC structure: H1, tagline, patent input, passage section, submit, results-area, options strip, trust footer (APP-10 trust signals present as static text)"
    - "webapp/wrangler.toml uses [assets] with directory = ../dist/webapp and no main key"
    - "Phase 8 adds ZERO new npm packages (supply-chain guard, T-08-SC)"
  artifacts:
    - path: "scripts/build.js"
      provides: "buildWebapp() target gated by --webapp-only; PROXY_TOKEN guard bypassed for webapp"
      contains: "buildWebapp"
    - path: "webapp/index.html"
      provides: "Full UI-SPEC markup + inline <style>; loads app.bundle.js as type=module"
      min_lines: 200
    - path: "webapp/wrangler.toml"
      provides: "Workers Assets config pointing at ../dist/webapp"
      contains: "[assets]"
    - path: "package.json"
      provides: "build:webapp npm script"
      contains: "build:webapp"
  key_links:
    - from: "scripts/build.js buildWebapp()"
      to: "webapp/js/app.js"
      via: "esbuild entryPoint (entry created in plan 03; build target references it)"
      pattern: "webapp/js/app.js"
    - from: "webapp/index.html"
      to: "./app.bundle.js"
      via: "<script type=\"module\" src>"
      pattern: "app.bundle.js"
---

<objective>
Create the webapp build target and the HTML shell. Extend `scripts/build.js` with a
`--webapp-only` target (`buildWebapp()`) that bypasses the PROXY_TOKEN guard, cleans
only `dist/webapp/`, bundles `webapp/js/app.js` as ESM with `./lib/pdf.mjs` external,
and copies `index.html` + `src/lib/`. Author the complete `webapp/index.html` (markup +
inline `<style>`) per the UI-SPEC, and the `webapp/wrangler.toml` Workers Assets config.

Purpose: This is the scaffold every other Phase 8 plan builds on. Success criterion 5
(dist/webapp/ output) and the static-text portion of APP-10 (trust signals in footer)
land here.
Output: `scripts/build.js` (modified), `webapp/index.html`, `webapp/wrangler.toml`,
`package.json` (build:webapp script), and a verified `dist/webapp/` directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-webapp-core-build/08-UI-SPEC.md
@.planning/phases/08-webapp-core-build/08-RESEARCH.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

scripts/build.js current structure (the --chrome-only analog):
- Lines 24-26: `args.includes('--chrome-only')` flag parsing pattern.
- Lines 58-63: the PROXY_TOKEN guard — `const PROXY_TOKEN = process.env.PROXY_TOKEN; if (!PROXY_TOKEN) { console.error(...); process.exit(1); }`. THIS must be bypassed for --webapp-only.
- Lines 83-104 getEsmConfig(): the ESM target analog — `format: 'esm'`, `platform: 'browser'`, `external: ['../lib/pdf.mjs']`, `define: { '__PROXY_TOKEN__': ... }`. The webapp config drops the `define` entirely.
- Lines 110-122 copyStaticAssets(): `fs.cpSync('src/lib', 'dist/chrome/lib', {recursive:true})` + `fs.copyFileSync(...html...)` pattern.
- Lines 310-330 main(): unconditional `fs.rmSync('dist', ...)` at line 313, then flag dispatch (`chromeOnly` / `firefoxOnly` / default).

package.json scripts: build:chrome = "node scripts/build.js --chrome-only" (line 10). Add build:webapp analogously.

pdf-parser.js imports `'../lib/pdf.mjs'` (line 9). For the webapp bundle landing at dist/webapp/app.bundle.js, the external path that keeps the 3MB library out of the bundle is `'./lib/pdf.mjs'` (sibling lib/, NOT ../lib/).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --webapp-only build target to scripts/build.js</name>
  <files>scripts/build.js, package.json, webapp/js/app.js</files>
  <read_first>
    - scripts/build.js (the file being modified — read lines 1-130 and 305-336 for flag parsing, getEsmConfig, copyStaticAssets, main())
    - .planning/phases/08-webapp-core-build/08-RESEARCH.md (Pattern 1 + Pitfalls 1-3 — exact build gotchas)
  </read_first>
  <action>
    Add `const webappOnly = args.includes('--webapp-only');` near the other flag parses (~line 26).
    Restructure the PROXY_TOKEN guard at lines 58-63: change `if (!PROXY_TOKEN)` to
    `if (!webappOnly && !PROXY_TOKEN)` so the webapp build proceeds with no token (the
    webapp carries NO token — SEC-03 / Pitfall 1). Add an async `buildWebapp()` function:
    it (a) cleans ONLY dist/webapp via `fs.rmSync('dist/webapp', {recursive:true, force:true})`,
    (b) `fs.mkdirSync('dist/webapp/lib', {recursive:true})`, (c) runs `esbuild.build` on
    entryPoints `['webapp/js/app.js']` with `bundle:true`, `format:'esm'`, `platform:'browser'`,
    `outfile:'dist/webapp/app.bundle.js'`, `external:['./lib/pdf.mjs']`, and NO `define` block
    (no __PROXY_TOKEN__ — Pitfall 1/2), (d) `fs.copyFileSync('webapp/index.html', 'dist/webapp/index.html')`,
    (e) `fs.cpSync('src/lib', 'dist/webapp/lib', {recursive:true})`.
    In main(): guard the top-of-function `fs.rmSync('dist', ...)` so it runs ONLY when NOT
    webappOnly (Pitfall 3 — must not wipe dist/chrome). Add a `webappOnly` branch in the
    dispatch that calls `await buildWebapp()` and does NOT call buildTestExports (that needs
    a token). Add `"build:webapp": "node scripts/build.js --webapp-only"` to package.json scripts.
    Note: webapp/js/app.js does not exist yet (created in plan 08-03). To make this task's
    build verifiable in isolation, create a minimal placeholder `webapp/js/app.js` containing
    only a single-line comment `// app entry — implemented in 08-03`. Plan 08-03 overwrites it.
    Do NOT inline any pipeline logic here.
  </action>
  <verify>
    <automated>cd /home/fatduck/patent-cite-tool && unset PROXY_TOKEN && node scripts/build.js --chrome-only >/dev/null 2>&1; PROXY_TOKEN= node scripts/build.js --webapp-only && test -f dist/webapp/index.html && test -f dist/webapp/app.bundle.js && test -f dist/webapp/lib/pdf.mjs && test -f dist/webapp/lib/pdf.worker.mjs && test -d dist/chrome && echo "BUILD_OK $(wc -c < dist/webapp/app.bundle.js) bytes"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build.js --webapp-only` exits 0 with PROXY_TOKEN unset.
    - dist/webapp/ has index.html, app.bundle.js, lib/pdf.mjs, lib/pdf.worker.mjs.
    - app.bundle.js is under 2,000,000 bytes (pdf.mjs stayed external — Pitfall 2).
    - dist/chrome/ still exists after the webapp build (Pitfall 3 — no global dist wipe).
    - package.json has a build:webapp script.
  </acceptance_criteria>
  <done>--webapp-only produces the four required artifacts, does not require a token, and does not delete sibling builds.</done>
</task>

<task type="auto">
  <name>Task 2: Author webapp/index.html (full UI-SPEC markup + inline styles) and wrangler.toml</name>
  <files>webapp/index.html, webapp/wrangler.toml</files>
  <read_first>
    - .planning/phases/08-webapp-core-build/08-UI-SPEC.md (the visual/interaction source of truth — Page Structure, Component Specifications 1-11, Copywriting Contract, Accessibility Contract; reproduce hex/spacing/typography tokens verbatim)
    - src/options/options.html (the brand aesthetic analog — centered card, footer, select/checkbox styling)
    - src/popup/popup.html (the status-chip color palette analog — .status, .status-ready/.status-error/.status-unavailable)
    - .planning/phases/08-webapp-core-build/08-RESEARCH.md (Pattern 8 — wrangler.toml [assets] shape)
  </read_first>
  <action>
    Create `webapp/index.html` implementing the UI-SPEC Page Structure exactly: `<main class="container">`
    (max-width 540px), header (H1 "Patent Citation Tool" 20px/700/#1e3a8a + tagline 13px/#64748b),
    `.card` (#fff, border-radius 10px, the exact box-shadow from UI-SPEC). Inside the card, in order:
    patent-input section (label "Patent number", monospace input, placeholder "e.g. US10123456",
    plus an empty `<p role="alert" class="field-error" hidden>` for the APP-02 message), passages
    section containing one passage row (label "Passage 1", textarea rows=3, placeholder per UI-SPEC)
    with the "+ Add another passage" button below, the "Get citation" submit button, a
    `<section id="results-area" aria-live="polite" aria-atomic="false">` (empty), and the options
    strip (format `<select id="citation-format">` with Short/Long options, prefix
    `<input type="checkbox" id="include-patent-number">`, both with `<label for>`). Footer with the
    two trust signals "Deterministic — no AI inference" · "No data stored" and the Privacy Policy link
    (APP-10 — static text satisfies the trust-signal requirement). Put ALL CSS in a single inline
    `<style>` block using the UI-SPEC color/spacing/typography tokens verbatim (#f9fafb bg, status-chip
    colors from popup.html lines 23-27, focus ring #2563eb, .sr-only helper). Include the
    `<script type="module" src="./app.bundle.js"></script>` tag. Every input/textarea/select has a
    matching `<label for>`; loading/no-match/error containers are described in the UI-SPEC but are
    INJECTED by app.js at runtime (plan 08-03) — do NOT pre-render result rows here, only the empty
    results-area and the static chrome. Then create `webapp/wrangler.toml` with `name = "patent-cite-webapp"`,
    a `compatibility_date`, and an `[assets]` block `directory = "../dist/webapp"` and NO `main` key
    (Pattern 8 / Pitfall 7 — never use the deprecated `[site]`).
  </action>
  <verify>
    <automated>cd /home/fatduck/patent-cite-tool && grep -q 'id="results-area"' webapp/index.html && grep -q 'aria-live="polite"' webapp/index.html && grep -q 'Deterministic — no AI inference' webapp/index.html && grep -q 'No data stored' webapp/index.html && grep -q 'id="citation-format"' webapp/index.html && grep -q 'id="include-patent-number"' webapp/index.html && grep -q 'app.bundle.js' webapp/index.html && grep -q '\[assets\]' webapp/wrangler.toml && grep -q 'directory = "../dist/webapp"' webapp/wrangler.toml && ! grep -q '\[site\]' webapp/wrangler.toml && echo "HTML_TOML_OK"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains: H1 "Patent Citation Tool", results-area with aria-live=polite, both trust-signal strings, citation-format select, include-patent-number checkbox, the module script tag.
    - Every form control has an associated `<label for>` (Accessibility Contract).
    - All color/spacing values trace to UI-SPEC tokens (no invented hex values).
    - wrangler.toml has `[assets]` + `directory = "../dist/webapp"`, no `[site]`, no `main`.
  </acceptance_criteria>
  <done>index.html renders the full static UI-SPEC chrome and loads the module bundle; wrangler.toml is a valid Workers Assets config.</done>
</task>

<task type="auto">
  <name>Task 3: Supply-chain guard — assert zero new npm packages (T-08-SC)</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (dependencies + devDependencies — confirm nothing new added)
    - .planning/phases/08-webapp-core-build/08-RESEARCH.md (Package Legitimacy Audit — all four tools pre-existing, zero installs this phase)
  </read_first>
  <action>
    This phase installs NO new packages (RESEARCH Package Legitimacy Audit: esbuild, pdfjs-dist,
    vitest, wrangler are all pre-existing and Approved). Verify the supply-chain invariant
    automatically: confirm `git diff package.json` shows ONLY the `build:webapp` script line added
    (no new keys under `dependencies` / `devDependencies`), and confirm `package-lock.json` has no
    new package entries introduced by this phase. If any dependency was added, STOP and surface it —
    no package may be added in Phase 8. No code is written in this task; it is a guard.
  </action>
  <verify>
    <automated>cd /home/fatduck/patent-cite-tool && CHANGED=$(git diff --unified=0 package.json | grep -E '^\+' | grep -v '^\+\+\+' | grep -vc 'build:webapp' || true); DEPLINES=$(git diff package.json | grep -E '^\+' | grep -iE '"[^"]+": *"\^?[0-9]' | grep -vc build || true); test "$DEPLINES" -eq 0 && echo "NO_NEW_DEPS_OK (non-script added lines: $CHANGED)"</automated>
  </verify>
  <acceptance_criteria>
    - No new entries under dependencies/devDependencies in package.json (only the build:webapp script).
    - The automated check prints NO_NEW_DEPS_OK.
  </acceptance_criteria>
  <done>The zero-new-dependency invariant (seventh consecutive milestone) is verified for Phase 8.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build env → webapp bundle | webapp must carry NO secret; PROXY_TOKEN must never be injected into dist/webapp |
| npm registry → project | supply-chain: any new dependency is an attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | dist/webapp/app.bundle.js | mitigate | No `define: __PROXY_TOKEN__` in buildWebapp(); grep-guarded in 08-03 that no Bearer/token literal appears in webapp src |
| T-08-02 | Tampering | scripts/build.js dist cleanup | accept | Webapp build scoped to dist/webapp only; sibling builds untouched — low risk, build-time only |
| T-08-SC | Tampering | npm installs | mitigate | Zero new packages (RESEARCH audit); Task 3 automated guard asserts no package.json dependency additions |
</threat_model>

<verification>
- `PROXY_TOKEN= node scripts/build.js --webapp-only` exits 0 and yields the four required files.
- app.bundle.js < 2MB (pdf.mjs external).
- dist/chrome survives a subsequent webapp build.
- index.html + wrangler.toml pass the grep gate.
- Task 3 supply-chain guard prints NO_NEW_DEPS_OK.
</verification>

<success_criteria>
Phase success criterion 5 (dist/webapp/ output + wrangler.toml) is met. The static portion
of APP-10 (trust signals in footer) is present. The HTML shell exposes every element id and
label that plans 08-02/08-03 wire against. Zero new npm dependencies confirmed.
</success_criteria>

<output>
Create `.planning/phases/08-webapp-core-build/08-01-SUMMARY.md` when done.
</output>
