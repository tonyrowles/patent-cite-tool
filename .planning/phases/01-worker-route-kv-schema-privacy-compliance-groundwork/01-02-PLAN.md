---
phase: 01-worker-route-kv-schema-privacy-compliance-groundwork
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/manifest.firefox.json
  - docs/privacy/index.html
  - store-assets/store-listing.md
autonomous: true
requirements: [PRIV-01, PRIV-02, PRIV-03, PRIV-04, PRIV-05]
must_haves:
  truths:
    - "manifest.firefox.json declares data_collection_permissions required ['technicalAndInteraction','websiteActivity'] and optional ['websiteContent']"
    - "The privacy policy at docs/privacy/index.html has a 'Bug Report Feature' section documenting the payload field-by-field, 90-day retention, maintainer-only purpose, the per-submission selection-text opt-out, KV+Discord as the only destinations, and that no IP is stored"
    - "The privacy policy's prior 'collects no personal data' statement is qualified for voluntary bug reports"
    - "store-assets/store-listing.md acknowledges voluntary website-content submission via bug reporting and updates the data-use declaration"
    - "web-ext lint stays clean after the manifest edit (PRIV-05) when a Firefox build is available"
  artifacts:
    - path: "src/manifest.firefox.json"
      provides: "Updated data_collection_permissions declaration (PRIV-01)"
      contains: "websiteActivity"
    - path: "docs/privacy/index.html"
      provides: "Bug Report Feature privacy section (PRIV-03)"
      contains: "Bug Report Feature"
    - path: "store-assets/store-listing.md"
      provides: "Updated data-use declaration for voluntary bug reporting (PRIV-04)"
      contains: "bug report"
  key_links:
    - from: "src/manifest.firefox.json data_collection_permissions"
      to: "worker/src/report-schema.md payload allowlist"
      via: "declared collection types must match the actual transmitted fields (PRIV-01 <-> PAY-01 cross-check)"
      pattern: "websiteContent"
    - from: "docs/privacy/index.html Bug Report Feature section"
      to: "the PAY-01 field allowlist"
      via: "field-by-field disclosure of what is transmitted"
      pattern: "Bug Report Feature"
---

<objective>
Land all three store-submission privacy/compliance gates that must ship in the same commit window as the Worker route: update the Firefox manifest `data_collection_permissions` (PRIV-01), add a "Bug Report Feature" section to the privacy policy (PRIV-03) and qualify the prior "no data collected" statement, amend the CWS store-listing data-use declaration (PRIV-04), and confirm the Chrome manifest needs no change (PRIV-02) and `web-ext lint` stays clean (PRIV-05).

Purpose: This is BLOCK-01. A manifest declaring `data_collection_permissions: ["none"]` while the `/report` route exists, or a privacy policy still asserting "no personal data collected," is a reviewable AMO/CWS contradiction. These docs-side changes are independent of the Worker code (disjoint files) and run in parallel with Plan 01.

Output: Compliant Firefox manifest, updated privacy policy, updated store-listing draft.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-worker-route-kv-schema-privacy-compliance-groundwork/01-CONTEXT.md
@.planning/phases/01-worker-route-kv-schema-privacy-compliance-groundwork/01-PATTERNS.md
@.planning/phases/01-worker-route-kv-schema-privacy-compliance-groundwork/01-RESEARCH.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension -> AMO/CWS reviewers | Declared data-collection metadata must match actual runtime behavior or submission is rejected |
| privacy policy -> end user | The policy is the legal disclosure of what the bug-report feature transmits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-04 | Repudiation / compliance | manifest + privacy policy + store listing | mitigate | `data_collection_permissions` declares the actual collected types (PRIV-01); privacy policy documents the payload field-by-field with 90-day retention and no-IP statement (PRIV-03); store listing data-use updated (PRIV-04). `websiteContent` is `optional` (not `required`) because selection text is user-controlled via the [Remove selection text] toggle — REQUIREMENTS.md PRIV-01 is the authority over the PITFALLS.md divergence. |
| T-1-07 | Information Disclosure | privacy policy accuracy | mitigate | Policy explicitly states no IP is stored and KV+Discord are the only destinations, matching the PAY-03 hard constraint enforced in Plan 01. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Update Firefox manifest data_collection_permissions and confirm Chrome manifest + web-ext lint</name>
  <files>src/manifest.firefox.json</files>
  <read_first>
    - src/manifest.firefox.json (read full file — current data_collection_permissions value is at browser_specific_settings.gecko.data_collection_permissions, lines 11-14: required ["none"], optional [])
    - src/manifest.json (Chrome — confirm there is no data_collection_permissions field equivalent; PRIV-02 is verify-only)
    - .planning/REQUIREMENTS.md (PRIV-01, PRIV-02, PRIV-05 — REQUIREMENTS.md is the AUTHORITY on the taxonomy)
    - .planning/phases/01-worker-route-kv-schema-privacy-compliance-groundwork/01-PATTERNS.md (manifest before/after block ~lines 288-310)
    - scripts/check-firefox-data-collection.cjs (the PRIV-01 assertion this task must make pass)
  </read_first>
  <action>
    In `src/manifest.firefox.json`, edit `browser_specific_settings.gecko.data_collection_permissions`: set `required` to `["technicalAndInteraction", "websiteActivity"]` and `optional` to `["websiteContent"]`. `websiteContent` goes in `optional` (NOT `required`) because selection text is user-controlled per-submission via the [Remove selection text] toggle — REQUIREMENTS.md PRIV-01 is the authority and supersedes the PITFALLS.md divergence that placed it in `required`. Preserve all surrounding JSON structure and key ordering; change only the two arrays.

    PRIV-02 is verify-only: confirm `src/manifest.json` (Chrome) has NO data-collection manifest field to change (CWS uses store listing + privacy policy + in-product disclosure, not a manifest field). Do not edit the Chrome manifest.

    PRIV-05: `web-ext lint` must stay clean after the edit. If a built Firefox bundle exists at `dist/firefox/`, run `npx web-ext lint dist/firefox/` and confirm exit 0. If no build is available in this context, note it in the SUMMARY and defer the live lint to Phase 5 UAT-04 — the manifest JSON validity + value correctness is still asserted by the automated check below.
  </action>
  <verify>
    <automated>node scripts/check-firefox-data-collection.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-firefox-data-collection.cjs` exits 0 (prints "PRIV-01 OK").
    - `grep -c 'technicalAndInteraction' src/manifest.firefox.json` returns 1, `grep -c 'websiteActivity' src/manifest.firefox.json` returns 1, `grep -c 'websiteContent' src/manifest.firefox.json` returns 1.
    - The `["none"]` value is gone from the data_collection_permissions block.
    - `src/manifest.json` (Chrome) is unchanged: `git diff --quiet src/manifest.json` exits 0.
    - If `dist/firefox/` exists, `npx web-ext lint dist/firefox/` exits 0; otherwise the deferral is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>Firefox manifest declares the PRIV-01 taxonomy with websiteContent as optional; Chrome manifest confirmed unchanged; web-ext lint clean or deferred with note.</done>
</task>

<task type="auto">
  <name>Task 2: Add Bug Report Feature section to privacy policy and amend store listing</name>
  <files>docs/privacy/index.html, store-assets/store-listing.md</files>
  <read_first>
    - docs/privacy/index.html (read full file — the existing h2 section pattern at ~lines 124-136, the current "no personal data" statement at ~line 94, and the Contact section ending at ~line 142 before the footer at ~line 144)
    - store-assets/store-listing.md (read full file — Subsection 4 "Data Use Practices" at ~lines 125-135 with the "Data types collected: none" certification, and Section 1 Description at ~lines 29-46)
    - worker/src/report-schema.md (if Plan 01 has produced it — the canonical field allowlist to disclose; if not yet present, use the PAY-01 allowlist in 01-RESEARCH.md Code Examples ~lines 507-531)
    - .planning/REQUIREMENTS.md (PRIV-03, PRIV-04)
    - .planning/phases/01-worker-route-kv-schema-privacy-compliance-groundwork/01-PATTERNS.md (privacy policy + store listing content specs ~lines 314-371)
  </read_first>
  <action>
    In `docs/privacy/index.html`, insert a new `<h2>Bug Report Feature</h2>` section immediately after the closing `</p>` of the existing "Contact" section (~line 142), before the `<footer>` (~line 144). Follow the existing h2 + p + ul structure used by the "Permissions Used" / "Data Sharing" sections. The section MUST document:
    - Field-by-field what is transmitted (the PAY-01 allowlist: patent number, patent URL, selection text [opt-out], returned citation, confidence tier, extension version, browser, OS, xpath node, scroll/viewport diagnostics, pdf parse status, trigger mode, error log buffer, free-text note).
    - 90-day retention.
    - Purpose: maintainer triage only.
    - The per-submission [Remove selection text] opt-out for selection text.
    - Destinations: Cloudflare KV (`BUG_REPORTS` namespace) and a maintainer-only Discord channel — and nowhere else.
    - That NO IP address is stored in report records (PAY-03).
    - That submission is voluntary and user-initiated (explicit Submit click).
    Also qualify the existing "Patent Citation Tool does not collect, store, or transmit any personal data" statement (~line 94) by adding a clause such as "except as described in the Bug Report Feature section below" so the policy is internally consistent.

    In `store-assets/store-listing.md`, amend Subsection 4 "Data Use Practices": update the "Data types collected" line to acknowledge that Website Content (patent selection text, up to ~200 chars) may be voluntarily submitted via the bug report feature when the user does not toggle it off, and add a carve-out paragraph stating that normal citation-only operation collects no data while voluntary bug-report submission transmits the diagnostic fields listed in the privacy policy to first-party infrastructure. Add one sentence to the Section 1 Description (~lines 29-46) mentioning the voluntary "report a problem" capability and linking the updated privacy policy. Keep all heading levels and table formatting identical — change only content within Subsection 4 and the one Description sentence.
  </action>
  <verify>
    <automated>grep -q 'Bug Report Feature' docs/privacy/index.html; r1=$?; grep -qi '90' docs/privacy/index.html; r2=$?; grep -qiE 'no ip|not store.*ip|ip address' docs/privacy/index.html; r3=$?; grep -qi 'bug report' store-assets/store-listing.md; r4=$?; grep -qi 'website content' store-assets/store-listing.md; r5=$?; test $r1 -eq 0 -a $r2 -eq 0 -a $r3 -eq 0 -a $r4 -eq 0 -a $r5 -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Bug Report Feature' docs/privacy/index.html` returns at least 1.
    - The privacy policy section mentions 90-day retention, the [Remove selection text] opt-out, KV + Discord destinations, and an explicit no-IP-stored statement (each grep-checkable).
    - The prior "does not collect ... any personal data" sentence is qualified (a clause referencing the Bug Report Feature section is present in the same paragraph).
    - `store-assets/store-listing.md` mentions the bug-report capability and updates the data-use declaration to acknowledge voluntary website-content submission (grep matches `bug report` and `website content`).
    - `docs/privacy/index.html` remains well-formed HTML (the new `<h2>`/`<p>`/`<ul>` are properly closed; the `<footer>` still follows the new section).
  </acceptance_criteria>
  <done>Privacy policy documents the bug-report payload field-by-field with retention, opt-out, destinations, and no-IP statement; the store listing data-use declaration is updated; the prior no-collection statement is qualified.</done>
</task>

</tasks>

<verification>
- Firefox manifest declares the PRIV-01 taxonomy (websiteActivity + technicalAndInteraction required; websiteContent optional); no `["none"]` remains.
- Chrome manifest unchanged (PRIV-02 verify-only).
- Privacy policy has a Bug Report Feature section (PRIV-03) and the prior no-collection statement is qualified.
- Store listing data-use declaration updated (PRIV-04).
- `web-ext lint` clean if a Firefox build is available; otherwise deferred to Phase 5 UAT-04 with a SUMMARY note (PRIV-05).
</verification>

<success_criteria>
- BLOCK-01 resolved: manifest, privacy policy, and store listing all consistent with the actual transmitted bug-report payload.
- No AMO/CWS reviewable contradiction (declared collection matches behavior; policy no longer claims zero collection unconditionally).
- `websiteContent` correctly placed in `optional` per the authoritative PRIV-01 spec.
</success_criteria>

<output>
Create `.planning/phases/01-worker-route-kv-schema-privacy-compliance-groundwork/01-02-SUMMARY.md` when done
</output>
