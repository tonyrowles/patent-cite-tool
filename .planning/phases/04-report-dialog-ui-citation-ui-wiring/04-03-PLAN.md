---
phase: 04-report-dialog-ui-citation-ui-wiring
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/content/citation-ui.js
  - src/content/content-script.js
autonomous: true
requirements: [CAP-03, TRIG-01, TRIG-02, TRIG-03, TRIG-04]
must_haves:
  truths:
    - "CAP-03 (D-06): a Report button is injected into .cite-row adjacent to the Copy button; icon-only (⚑, title='Report a problem') in the normal case, with a '⚑ Report a problem' nudge label on failure/yellow/Worker-error; clicking it calls showReportDialog(shadow, reportOutcome, rect)"
    - "TRIG-04: the Report button is NOT rendered on green / high-confidence success (confidenceTier==='green' => button not appended); this green-hidden invariant holds and is the Phase 4 default (Phase 5 DBG relaxes it)"
    - "TRIG-01: no-match / paragraph-not-found outcomes surface the button with category 'no_match' pre-selected"
    - "TRIG-02: yellow-confidence outcomes (0.80<=confidence<0.95, incl. Tier-5/0.85-cap) surface the button with category 'inaccurate_citation' pre-selected"
    - "TRIG-03: Worker-fallback / PDF-failed / no-position-map / lookup-failed outcomes surface the button with category 'tool_not_working' pre-selected"
    - "D-05: the button only surfaces the affordance — the dialog opens only on click, never auto-opening"
    - "Outcome mapping: mapConfidenceTier(confidence) and mapOutcomeToReportCategory(errorCode, confidence) live in content-script.js and a reportOutcome {category, confidenceTier} object is threaded through the extended showCitationPopup/showErrorPopup signatures (default null = no button)"
  artifacts:
    - path: "src/content/citation-ui.js"
      provides: "Report button injection in .cite-row + .cite-report-btn CSS + extended showCitationPopup/showErrorPopup signatures"
      contains: "cite-report-btn"
    - path: "src/content/content-script.js"
      provides: "mapConfidenceTier, mapOutcomeToReportCategory, reportOutcome threading at all trigger sites"
      contains: "mapConfidenceTier"
  key_links:
    - from: "citation-ui.js Report button click"
      to: "report-dialog.js showReportDialog"
      via: "import + click handler"
      pattern: "showReportDialog\\("
    - from: "content-script.js trigger sites"
      to: "citation-ui.js showCitationPopup/showErrorPopup reportOutcome param"
      via: "mapped {category, confidenceTier} object"
      pattern: "confidenceTier"
---

<objective>
Wire the Report button into the citation popup (`citation-ui.js`) and the auto-surfacing trigger logic into `content-script.js`. Each citation outcome computes a `reportOutcome` `{ category, confidenceTier }` threaded through the extended `showCitationPopup`/`showErrorPopup` signatures; the button renders (icon-only or nudge) for failure/yellow/error outcomes and is hidden entirely on green (TRIG-04). Clicking it opens Plan 02's `showReportDialog`.

Purpose: This is the CAP-03 affordance + TRIG-01..04 surfacing rules — the entry point that makes the whole report flow reachable from a real citation moment.
Output: Report button + trigger wiring; the in-citation report flow is end-to-end functional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-report-dialog-ui-citation-ui-wiring/04-UI-SPEC.md
@.planning/phases/04-report-dialog-ui-citation-ui-wiring/04-RESEARCH.md
@.planning/phases/04-report-dialog-ui-citation-ui-wiring/04-PATTERNS.md
@.planning/phases/04-report-dialog-ui-citation-ui-wiring/04-CONTEXT.md
@src/content/citation-ui.js

<interfaces>
<!-- Contracts the executor needs. -->

From Plan 02 (src/content/report-dialog.js):
  export function showReportDialog(shadow, reportOutcome, selectionRect)
  reportOutcome = { category: 'inaccurate_citation'|'no_match'|'tool_not_working'|null,
                    confidenceTier: 'green'|'yellow'|'red' }

Current signatures to extend (add trailing optional `reportOutcome = null`):
  citation-ui.js:111  showCitationPopup(citation, rect, confidence, displayMode, matchedText)
  citation-ui.js:240  showErrorPopup(errorMessage, rect)

Confidence bands (citation-ui.js:124, VERIFIED): high >=0.95 (green/TRIG-04), medium >=0.80 (yellow/TRIG-02), low <0.80 (red).
Tier-5 gutter-tolerant match outputs exactly 0.85 => falls in the yellow band (RESEARCH MEDIUM src).

content-script.js trigger sites (RESEARCH Trigger-Detection Site Map, VERIFIED):
  line 452  app patent success  showCitationPopup(...)            => tier from confidence; category 'inaccurate_citation' when <0.95 else null
  line 454  app patent null     showErrorPopup('Paragraph not found in application')  => {no_match, red}
  line 469  grant status!=parsed showErrorPopup(statusMsg)         => 'PDF analysis failed'/'PDF not available' => {tool_not_working, red}; 'still analyzing' => NO button (transient)
  line 536  grant CITATION_RESULT success  showCitationPopup(...)  => same as line 452
  line 543  grant error 'no-match'        showErrorPopup(...)      => {no_match, red}
  line 545  grant error 'no-position-map' showErrorPopup(...)      => {tool_not_working, red}
  line 547  grant error 'lookup-failed'   showErrorPopup(...)      => {tool_not_working, red}
  line 438  'Not on a patent page'  => NO button (out of scope)
  Silent-mode branch (lines 513-528): NO report button (no popup is shown in silent mode).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Report button in .cite-row + CSS + extended signatures (CAP-03, TRIG-04)</name>
  <files>src/content/citation-ui.js</files>
  <read_first>
    - src/content/citation-ui.js lines 111-232 (showCitationPopup, .cite-row construction 128-150, click handler 171-201, positioning 203-231), lines 240-270 (showErrorPopup), getCitationPopupCSS 417-497 (.cite-copy-btn 447-461 base)
    - 04-UI-SPEC.md Component Anatomy #1 (Report Button — normal/nudge/hover/focus states, exact hex), Interaction States "Report Button States", Auto-Surface table, Copywriting (icon-only ⚑ + 'Report a problem' title; nudge '⚑ Report a problem')
    - RESEARCH.md "Report Button in cite-row" (lines 550-571) + Pattern 1 mount note (never call getCitationHost)
    - 04-PATTERNS.md citation-ui.js modified section: cite-row construction (350-376), Report button injection (379-397), .cite-report-btn CSS (400-423), signature extension (425-439)
  </read_first>
  <action>
    Extend the two signatures: `showCitationPopup(citation, rect, confidence, displayMode, matchedText, reportOutcome = null)` and `showErrorPopup(errorMessage, rect, reportOutcome = null)`. In `showCitationPopup`, after the Copy button (and confidence dot) is appended to `.cite-row` and before `popup.appendChild(row)`, inject the Report button ONLY when `reportOutcome && reportOutcome.confidenceTier !== 'green'` (TRIG-04: on green, do not append — button absent from DOM): create `<button class="cite-report-btn">` with `title="Report a problem"`, `aria-label="Report a problem with this citation"`; set `textContent` to `'⚑ Report a problem'` in nudge mode (failure/yellow/error are always non-green here) per D-06; add a `click` handler that calls `e.stopPropagation()` then `showReportDialog(shadow, reportOutcome, rect)` (import `showReportDialog` from `./report-dialog.js`), passing the button element as the trigger for focus-restore. In `showErrorPopup`, the popup has no `.cite-row`; append the Report button into the error popup container under the same non-green guard. Add `.cite-report-btn` CSS to `getCitationPopupCSS()` using only UI-SPEC tokens: icon-only variant (transparent bg, `#6b7280`, 14px glyph, min 24×24 touch target), nudge variant (`rgba(245,158,11,0.08)` bg, `#92400e`, 13px/500, 4px radius, `2px 6px` padding), hover (`#f3f4f6`/`#1a1a1a`), focus (`outline: 2px solid #3b82f6; outline-offset: 2px`). Do NOT auto-open the dialog — the button only surfaces it (D-05).
  </action>
  <verify>
    <automated>node scripts/build.js >/dev/null 2>&1 && grep -q "cite-report-btn" src/content/citation-ui.js && grep -q "showReportDialog" src/content/citation-ui.js && grep -q "reportOutcome = null" src/content/citation-ui.js && grep -q "confidenceTier !== 'green'" src/content/citation-ui.js && echo "report button + green-hidden guard + extended signatures OK"</automated>
  </verify>
  <done>Report button injected into .cite-row and error popup behind a non-green guard; clicking calls showReportDialog with the button as trigger; signatures extended with optional reportOutcome; CSS uses only UI-SPEC tokens; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Outcome mapping + trigger wiring at all content-script.js call sites (TRIG-01..04)</name>
  <files>src/content/content-script.js</files>
  <read_first>
    - src/content/content-script.js lines 433-488 (generateCitation: app success 450-455, grant status branch 459-471), lines 509-550 (handleCitationResult: silent branch 513-528, success 533-541, error 543-548), extractPatentInfo 27-45
    - RESEARCH.md Pattern 6 (mapConfidenceTier + mapOutcomeToReportCategory, lines 393-419) + Trigger-Detection Site Map (lines 641-677) + Note on TRIG-03 (Worker error arrives via currentPatent.status, lines 677)
    - 04-PATTERNS.md content-script.js modified section (446-523): currentPatent read, call-sites-to-extend, new helper functions
    - 04-UI-SPEC.md "Auto-Surface and Trigger Logic" table (the authoritative outcome→tier→category→button-state matrix)
  </read_first>
  <action>
    Add two pure helpers to `content-script.js`: `mapConfidenceTier(confidence)` returning `'green'` (>=0.95), `'yellow'` (>=0.80), else `'red'` (per citation-ui.js:124 bands; Tier-5/0.85 lands yellow); and `mapOutcomeToReportCategory(errorCode, confidence)` returning `'no_match'` for `'no-match'`/`'paragraph-not-found'`, `'tool_not_working'` for `'no-position-map'`/`'lookup-failed'`/`'pdf-not-available'`, `'inaccurate_citation'` for a success with `confidence<0.95`, else `null` (green). Thread a `reportOutcome` object into every popup call per the site map: line ~452 app success and ~536 grant success → `showCitationPopup(..., { category: mapOutcomeToReportCategory(null, confidence), confidenceTier: mapConfidenceTier(confidence) })`; line ~454 → `showErrorPopup('Paragraph not found in application', rect, { category: 'no_match', confidenceTier: 'red' })`; line ~469 status branch → for `'PDF analysis failed'`/`'PDF not available'` pass `{ category: 'tool_not_working', confidenceTier: 'red' }`, but for the transient `'PDF is still being analyzed...'` pass NO reportOutcome (omit/null — no button, transient); lines ~543-547 → map `message.error` (`'no-match'`→no_match; `'no-position-map'`/other→tool_not_working) into `{ category, confidenceTier: 'red' }`. Leave line ~438 'Not on a patent page' and the entire silent-mode branch (513-528) WITHOUT a reportOutcome (no button). Since `reportOutcome` defaults to `null`, untouched/transient sites simply render no button.
  </action>
  <verify>
    <automated>node scripts/build.js >/dev/null 2>&1 && grep -q "mapConfidenceTier" src/content/content-script.js && grep -q "mapOutcomeToReportCategory" src/content/content-script.js && grep -q "confidenceTier: 'red'" src/content/content-script.js && node -e "const s=require('fs').readFileSync('src/content/content-script.js','utf8'); if(!/return 'green'/.test(s)||!/return 'yellow'/.test(s)) throw new Error('tier bands missing'); console.log('mapping + wiring OK');"</automated>
  </verify>
  <done>mapConfidenceTier + mapOutcomeToReportCategory added; reportOutcome threaded into the success + error/no-match + PDF-failed sites; transient 'still analyzing', 'Not on a patent page', and silent-mode sites pass no reportOutcome (TRIG-04 + scope). Build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| citation outcome → button visibility | the green-hidden invariant (TRIG-04) is a correctness/noise boundary, not a security one |
| Report button → dialog | the button passes the (extension-computed) reportOutcome into the dialog; no host-page data crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Information Disclosure | reportOutcome threading | accept | reportOutcome carries only an extension-computed category string + tier; no PII or host-page content; low value. |
| T-04-11 | Tampering | Report button DOM injection | mitigate | Button built via createElement with static textContent (⚑ glyph + fixed label); no innerHTML, no page-derived string interpolation. |
| T-04-12 | Spoofing | green-hidden invariant bypass | mitigate | The non-green guard (`confidenceTier !== 'green'`) gates appendChild so a green outcome cannot surface the button; verified by static grep + Plan 04 tests. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages this phase. No install tasks. |
</threat_model>

<verification>
- `node scripts/build.js` succeeds; the bundled content IIFE includes the Report button and trigger wiring.
- `grep -rn "fetch(WORKER_REPORT_URL" src/content/` returns zero results (XPORT-06 invariant preserved — this plan adds no fetch).
- Static checks: `cite-report-btn`, `showReportDialog`, `mapConfidenceTier`, `mapOutcomeToReportCategory`, the green guard all present.
- `web-ext lint` on dist/firefox stays clean.
</verification>

<success_criteria>
- Report button surfaces with the correct pre-selected category for no-match (TRIG-01), yellow (TRIG-02), and Worker/PDF-error (TRIG-03) outcomes, and is hidden on green (TRIG-04).
- Clicking the button opens the dialog (D-05 — surface-only, click-to-open).
- Outcome→tier→category mapping matches the UI-SPEC Auto-Surface table exactly.
</success_criteria>

<output>
Create `.planning/phases/04-report-dialog-ui-citation-ui-wiring/04-03-SUMMARY.md` when done.
</output>
