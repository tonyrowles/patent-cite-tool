---
phase: 05-options-page-debug-mode-popup-fallback-live-uat
plan: 04
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - tests/unit/debug-mode-page-dialog.test.js
  - vitest.config.js
autonomous: true
requirements: [DBG-01, DBG-02, CAP-05, CAP-06]
must_haves:
  truths:
    - "DBG-01: a Vitest assertion confirms debugMode is in DEFAULT_SETTINGS (content-script.js) and in the options.js storage.sync.get defaults, defaulting to false"
    - "DBG-02: static-grep assertions confirm content-script.js onChanged updates cachedSettings.debugMode and both showCitationPopup call sites thread debugMode into reportOutcome; citation-ui.js guard contains both 'confidenceTier !== \\'green\\'' AND 'reportOutcome.debugMode' AND an 'isGreenDebug' plain-icon branch"
    - "D-05: a static-grep assertion confirms the green-debug button path sets the plain '⚑' glyph (no amber nudge) and retains an aria-label on the icon-only button"
    - "CAP-05: static-grep confirms popup.js writes pendingOptionsHash and calls openOptionsPage, and popup.html contains reportLink"
    - "CAP-06: static-grep confirms options.js imports showReportDialog, contains mode: 'page', reads currentPatent, and handles location.hash/pendingOptionsHash routing; report-dialog.js still does NOT contain 'getCitationHost(' and DOES contain 'mountContext'"
    - "Page-mode safety: static-grep confirms report-dialog.js has installFocusTrapPage using document.activeElement and that the stale-context banner is set via .textContent (no .innerHTML on the banner)"
    - "The new test file is wired into vitest.config.js include globs so npm run test:src executes it"
  artifacts:
    - path: "tests/unit/debug-mode-page-dialog.test.js"
      provides: "node-env static-grep + behavior assertions for DBG-01/02, CAP-05/06, D-01/02/05/07/08"
      contains: "describe"
      min_lines: 60
    - path: "vitest.config.js"
      provides: "include glob covering the new test file (if not already matched)"
      contains: "tests/unit"
  key_links:
    - from: "tests/unit/debug-mode-page-dialog.test.js"
      to: "src/content/citation-ui.js"
      via: "static-grep on guard tokens"
      pattern: "reportOutcome\\.debugMode"
    - from: "tests/unit/debug-mode-page-dialog.test.js"
      to: "src/content/report-dialog.js"
      via: "static-grep mountContext + getCitationHost ban"
      pattern: "mountContext"
    - from: "tests/unit/debug-mode-page-dialog.test.js"
      to: "src/options/options.js"
      via: "static-grep page-mode + currentPatent"
      pattern: "mode: 'page'"
---

<objective>
Add a node-env Vitest suite that pins the Phase-5 build-side invariants (DBG-01/02, CAP-05/06 and the locked decisions D-01/D-02/D-05/D-07/D-08) via static-grep assertions over the source files, mirroring the existing tests/unit/report-trigger-mapping.test.js guard pattern. These tests prevent regression of the load-bearing tokens (the relaxed TRIG-04 guard, the mountContext refactor, the getCitationHost ban, the pendingOptionsHash wiring, the page-mode .textContent XSS guard).

Purpose: Without these guards, a future refactor could silently drop the debugMode threading, re-introduce extractPatentInfo into the page path, or break the bundling import — none of which the build step catches. The Phase-4 dialog suites already exist and stay green; this plan adds the Phase-5-specific guards.

Output: tests/unit/debug-mode-page-dialog.test.js plus any vitest.config.js include-glob wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-PATTERNS.md

@.planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-01-SUMMARY.md
@.planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-02-SUMMARY.md
@.planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-03-SUMMARY.md

@tests/unit/report-trigger-mapping.test.js
@vitest.config.js

<interfaces>
<!-- Static-grep test pattern from report-trigger-mapping.test.js (the analog) -->
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';
const __dirname = path.dirname(fileURLToPath(import.meta.url));
// const src = readFileSync(path.resolve(__dirname, '../../src/content/<file>.js'), 'utf8');
// expect(src).toContain('<token>')  /  expect(src).not.toContain('<token>')

<!-- vitest.config.js: confirm the include glob matches tests/unit/*.test.js so the new file runs under npm run test:src -->
</interfaces>

<note>
05-PATTERNS.md "New unit tests (node-env)" lists the six guard assertions to author. report-trigger-mapping.test.js:207-231 is the verbatim structural analog (getCitationHost ban + green-guard token). Keep assertions over SOURCE files (src/...), not dist — source is the contract.
</note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phase-5 static-grep guard suite</name>
  <read_first>
    - tests/unit/report-trigger-mapping.test.js (full — the static-grep describe/it analog, esp. lines 203-231)
    - vitest.config.js (include globs — confirm tests/unit/*.test.js is matched)
    - src/content/content-script.js, src/content/citation-ui.js, src/content/report-dialog.js, src/options/options.js, src/options/options.html, src/popup/popup.js, src/popup/popup.html (the final post-05-01/02/03 source — assert the tokens that landed)
    - 05-PATTERNS.md "New unit tests (node-env)" (the six assertions)
  </read_first>
  <behavior>
    - DBG-01: content-script.js DEFAULT_SETTINGS contains "debugMode: false"; options.js storage.sync.get defaults contain "debugMode: false"
    - DBG-02: content-script.js onChanged contains "changes.debugMode" and "cachedSettings.debugMode = changes.debugMode.newValue"; content-script.js contains "debugMode: cachedSettings.debugMode" (threaded into reportOutcome)
    - DBG-02/TRIG-04: citation-ui.js contains BOTH "confidenceTier !== 'green'" AND "reportOutcome.debugMode" AND "isGreenDebug"
    - D-05: citation-ui.js green-debug path sets the bare flag glyph (toContain "'⚑'") and the icon-only button still calls setAttribute('aria-label', ...) (aria-label retained)
    - D-08/CAP-06: report-dialog.js contains "mountContext" and "installFocusTrapPage" and does NOT contain "getCitationHost("
    - D-01 XSS: report-dialog.js stale banner is set via ".textContent" and report-dialog.js does NOT use ".innerHTML" on cite-report-stale-banner (assert the banner className appears and innerHTML is not applied to it — assert file does not contain "stale-banner" adjacent to innerHTML; simplest: assert src contains "cite-report-stale-banner" and the banner assignment uses textContent)
    - CAP-05: popup.html contains 'id="reportLink"'; popup.js contains "pendingOptionsHash" and "openOptionsPage"
    - CAP-06: options.js contains "showReportDialog", "mode: 'page'", "currentPatent", and "location.hash" (or pendingOptionsHash routing); options.html contains 'id="report"' and 'id="reportDialogMount"'
  </behavior>
  <action>
    Create tests/unit/debug-mode-page-dialog.test.js following the report-trigger-mapping.test.js static-grep structure. Read each source file once via readFileSync(path.resolve(__dirname, '../../src/...'), 'utf8'). Group assertions into describe blocks per requirement (DBG-01, DBG-02 + D-05, CAP-05, CAP-06 + D-08, page-mode safety + D-01). Use expect(src).toContain(token) / .not.toContain(token) exactly as the analog does. Cite the requirement/decision id in each it() description (e.g. "DBG-02: citation-ui.js guard relaxes on reportOutcome.debugMode"). Do NOT import the extension modules (they reference chrome.* and DOM globals) — this is a pure source-text guard suite, identical in approach to report-trigger-mapping.test.js. If vitest.config.js does not already include tests/unit/*.test.js in its include globs, add the glob so npm run test:src runs the new file; if it already matches, leave the config untouched (note that in the summary).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/debug-mode-page-dialog.test.js 2>&1 | tail -25 && npm run test:src 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/debug-mode-page-dialog.test.js exists with describe blocks for DBG-01, DBG-02/D-05, CAP-05, CAP-06/D-08, and page-mode/D-01 safety
    - Every listed behavior assertion is present and passes against the final source files
    - The suite asserts report-dialog.js does NOT contain 'getCitationHost(' and DOES contain 'mountContext' + 'installFocusTrapPage'
    - The suite asserts citation-ui.js contains both "confidenceTier !== 'green'" and "reportOutcome.debugMode" and "isGreenDebug"
    - The suite is included by vitest.config.js (runs under npm run test:src)
    - npm run test:src passes (full source suite green, no pre-existing tests broken)
  </acceptance_criteria>
  <done>The Phase-5 build invariants are pinned by a passing node-env Vitest suite wired into npm run test:src.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → source files | Read-only static analysis; no runtime, no network, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering | guard tokens silently removed in future refactor | mitigate | Static-grep assertions fail CI if the load-bearing tokens (debugMode threading, mountContext, getCitationHost ban, .textContent banner) are removed |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies; Vitest already pinned ^3.0.0; no install tasks; nothing to slopcheck |
</threat_model>

<verification>
- npx vitest run tests/unit/debug-mode-page-dialog.test.js passes.
- npm run test:src passes (no regression in the existing source suites).
</verification>

<success_criteria>
- Static guards pin DBG-01/02, CAP-05/06, D-01/D-05/D-08 tokens.
- Suite wired into npm run test:src.
- No existing test broken.
</success_criteria>

<output>
Create `.planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-04-SUMMARY.md` when done.
</output>
