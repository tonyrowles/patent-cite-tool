---
phase: 05-options-page-debug-mode-popup-fallback-live-uat
plan: 05
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified:
  - .planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-UAT-RUNBOOK.md
  - .planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-UAT-RESULTS.md
autonomous: false
requirements: [UAT-01, UAT-02, UAT-03, UAT-04, UAT-05, UAT-06]
must_haves:
  truths:
    - "D-03: live UAT runs against the PRODUCTION Worker https://pct.tonyrowles.com/report (real Discord + real KV); test notes carry 'v5.0 UAT-0N smoke'; NOT X-PCT-Test-Mode (which would suppress the KV writes UAT-01/03 must verify)"
    - "D-04: maximum automation — Claude scripts every scriptable verification (npm run build, web-ext lint, wrangler kv key get/list against namespace cefe2733c0074fe2a28a49ff536de105, fingerprint-match, no-ip schema check, dedup duplicate_count, grep guards) and records evidence; the OPERATOR performs only irreducible browser steps (live submit on Google Patents, Chrome SW stop/restart) — Claude cannot drive a real browser and does NOT autonomously fire production Discord/KV writes"
    - "UAT-01: a live submission writes a KV record under report:{fp}:{ts} matching the PAY-01 schema with NO ip/clientIp/userAgent field, a fingerprint that matches the Discord embed footer, and a Discord rich-embed notification"
    - "UAT-02: client-side rate-limit boundary — the 6th submission within a 10-min window is blocked (LIMIT-03 toast, no Worker/KV write) and the 7th after 10 min succeeds (operator-confirmed browser; Claude-verified via KV record count)"
    - "UAT-03: server-side dedup — two identical-fingerprint submissions within 15 min yield exactly ONE KV record with duplicate_count=2 and ONE primary Discord notification; a third after 15 min creates a NEW record (Claude-verified via wrangler kv key list/get)"
    - "UAT-04: privacy audit fully scriptable — web-ext lint clean on dist/firefox, grep 'discord.com/api/webhooks' returns zero, no fetch(WORKER_REPORT_URL in src/content/, privacy policy URL returns 200, manifest data_collection_permissions cross-checked against the PAY-01 schema"
    - "UAT-05: cross-browser parity — same flow on Chrome (SW may terminate; operator stops/restarts SW with a queued report and confirms retry on next load) and Firefox event page; Claude verifies the resulting KV record"
    - "UAT-06: [Remove selection text] toggle — operator toggles ON and submits; Claude verifies selectionText is null in the KV record; operator confirms it is absent from the Discord embed and the toggle state persists across re-opens"
  artifacts:
    - path: ".planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-UAT-RUNBOOK.md"
      provides: "numbered operator+scriptable steps per UAT-01..06 with exact commands and exact browser actions"
      contains: "UAT-01"
      min_lines: 80
    - path: ".planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-UAT-RESULTS.md"
      provides: "evidence table — Claude pre-fills scriptable rows; operator fills manual rows; final PASS/FAIL per UAT item"
      contains: "duplicate_count"
  key_links:
    - from: "05-UAT-RUNBOOK.md"
      to: "production Worker KV"
      via: "wrangler kv key list/get against namespace cefe2733c0074fe2a28a49ff536de105"
      pattern: "cefe2733c0074fe2a28a49ff536de105"
    - from: "05-UAT-RESULTS.md"
      to: "PAY-01 schema / PAY-03 no-ip"
      via: "scripted node assertion on the KV record"
      pattern: "ip"
---

<objective>
Close the v5.0 milestone with live DoD evidence for UAT-01..06. Claude produces a step-by-step 05-UAT-RUNBOOK.md (operator vs scriptable split per item) and an evidence-capture 05-UAT-RESULTS.md; the operator performs the irreducible browser steps against the PRODUCTION Worker (D-03), reports outcomes, and Claude runs every scriptable verification (wrangler KV checks, web-ext lint, grep guards) and records the evidence (D-04). This plan also closes Phase 4's deferred 04-HUMAN-UAT.md live tests (focus trap, dismiss paths, sticky toggle, live triggers, live payload) via UAT-05/UAT-06.

Purpose: The DoD requires live proof — real Discord webhook + real KV record retrievable via wrangler — before milestone close. Claude cannot drive a real browser, so the live submits and the Chrome SW stop/restart are the operator's; everything else is scripted.

Output: 05-UAT-RUNBOOK.md + 05-UAT-RESULTS.md with all scriptable evidence recorded and operator slots filled.

CRITICAL — this plan has blocking human checkpoints. Claude MUST NOT autonomously POST to the production Worker, fire Discord, or write KV. Those side-effects come ONLY from the operator's live browser submits. Claude's automation is READ-ONLY against production (wrangler kv key get/list) plus local build/lint/grep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-CONTEXT.md
@.planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-RESEARCH.md
@.planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-PATTERNS.md

@.planning/phases/04-report-dialog-ui-citation-ui-wiring/04-HUMAN-UAT.md
@worker/src/report-schema.md

@.planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-01-SUMMARY.md
@.planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-02-SUMMARY.md
@.planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-03-SUMMARY.md
@.planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-04-SUMMARY.md

<interfaces>
<!-- Verified UAT tooling (05-RESEARCH.md, wrangler 4.54.0) -->
KV namespace id: cefe2733c0074fe2a28a49ff536de105  (worker/wrangler.toml:11)
KV key format:   report:{fingerprint}:{timestamp}   (PAY-02)
Production Worker: https://pct.tonyrowles.com/report

List by fingerprint:  wrangler kv key list --namespace-id=cefe2733c0074fe2a28a49ff536de105 --prefix "report:<fp>:"
Get a record:         wrangler kv key get  --namespace-id=cefe2733c0074fe2a28a49ff536de105 "report:<fp>:<ts>"
No-ip / schema check:  pipe the get output to node -e to assert !('ip' in r) && !('clientIp' in r) && !('userAgent' in r) && r.fingerprint
web-ext lint (UAT-04): npm run test:lint   (npx web-ext lint --source-dir dist/firefox --ignore-files 'lib/**'; exit 0)
Webhook grep (UAT-04): grep -r 'discord.com/api/webhooks' . --exclude-dir=.git   → zero results
Content fetch grep:    grep -rn 'fetch.*WORKER_REPORT_URL' src/content/   → zero results
Privacy URL (UAT-04):  curl -s -o /dev/null -w "%{http_code}" <privacy policy URL from PRIV-03>   → 200

<!-- 05-RESEARCH.md "## UAT Automation Split" gives the per-item SCRIPTABLE/OPERATOR breakdown verbatim -->
</interfaces>

<note>
05-RESEARCH.md "## UAT Automation Split (D-04)" is the authoritative per-item breakdown (UAT-01..06 sub-step tables with exact commands and irreducible operator steps) — transcribe it into the runbook. 04-HUMAN-UAT.md lists the 5 deferred Phase-4 live tests that UAT-05/UAT-06 close. Verify the exact privacy-policy URL from PRIV-03 (Phase 1 docs/privacy) before scripting the curl 200 check.
</note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 05-UAT-RUNBOOK.md + 05-UAT-RESULTS.md and run all scriptable verifications</name>
  <read_first>
    - 05-RESEARCH.md "## UAT Automation Split (D-04)" (per-item SCRIPTABLE/OPERATOR tables — transcribe verbatim)
    - 05-PATTERNS.md "New UAT artifacts" (runbook + results table structure)
    - 04-HUMAN-UAT.md (the 5 deferred Phase-4 live tests UAT-05/06 close)
    - worker/src/report-schema.md (PAY-01 allowlist — the no-ip schema assertion fields)
    - docs/privacy/ (resolve the canonical privacy-policy URL for the UAT-04 curl check — PRIV-03)
  </read_first>
  <action>
    Write 05-UAT-RUNBOOK.md: a numbered, per-UAT (UAT-01 through UAT-06) runbook. For each UAT item, transcribe the 05-RESEARCH.md automation-split table, labelling every sub-step SCRIPTABLE (with the exact bash/wrangler/node command) or OPERATOR (with the exact browser action and what to record — e.g. "read fp:XXXXXXXX from the Discord embed footer", "stop the SW at chrome://extensions > Details > Service worker > Terminate"). Make explicit at the top that D-03 targets the PRODUCTION Worker with note "v5.0 UAT-0N smoke" and that Claude's automation is read-only against production. Include the dist load steps (npm run build; load unpacked dist/chrome and dist/firefox).
    Write 05-UAT-RESULTS.md: an evidence table with columns | UAT | Step | Mode | Command/Action | Status | Evidence |. Pre-fill every SCRIPTABLE row with its command and leave Status/Evidence blank for now; leave OPERATOR rows for the operator to fill with timestamp + observed outcome. Add a per-UAT PASS/FAIL summary row and a milestone-DoD footer.
    Then RUN the fully-scriptable verifications that need NO operator action and record their evidence into 05-UAT-RESULTS.md now: (a) npm run build; (b) npm run test:lint (UAT-04 web-ext lint exit 0); (c) grep -r 'discord.com/api/webhooks' . --exclude-dir=.git (UAT-04 → expect zero); (d) grep -rn 'fetch.*WORKER_REPORT_URL' src/content/ (→ expect zero); (e) curl the privacy-policy URL for HTTP 200 (UAT-04); (f) node-read src/manifest.firefox.json data_collection_permissions and record it alongside the PAY-01 schema fields for the manifest↔payload cross-check (UAT-04). Do NOT run any wrangler write or any POST to the Worker. The wrangler kv key get/list verifications for UAT-01/02/03/05/06 depend on a fingerprint that only exists AFTER the operator's live submit — leave those rows scripted-but-pending and run them in Task 2 once the operator supplies the fingerprint(s).
  </action>
  <verify>
    <automated>test -f .planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-UAT-RUNBOOK.md && test -f .planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-UAT-RESULTS.md && npm run build && npm run test:lint 2>&1 | tail -3 && (grep -r 'discord.com/api/webhooks' . --exclude-dir=.git && echo 'WEBHOOK LEAK - FAIL' || echo 'no webhook in repo - PASS') && (grep -rn 'fetch.*WORKER_REPORT_URL' src/content/ && echo 'CONTENT FETCH - FAIL' || echo 'no content fetch - PASS')</automated>
  </verify>
  <acceptance_criteria>
    - 05-UAT-RUNBOOK.md exists with per-item UAT-01..06 steps, each labelled SCRIPTABLE or OPERATOR, with exact commands / exact browser actions; header states D-03 production target + "v5.0 UAT-0N smoke" note + Claude read-only-against-production rule
    - 05-UAT-RESULTS.md exists with the evidence table; all SCRIPTABLE rows pre-filled with their command
    - UAT-04 fully recorded as PASS evidence now: web-ext lint exit 0; zero 'discord.com/api/webhooks' in repo; zero fetch(WORKER_REPORT_URL in src/content/; privacy URL 200; manifest data_collection_permissions captured vs PAY-01 schema
    - No wrangler write and no POST to the production Worker was issued by Claude
    - The wrangler kv rows for UAT-01/02/03/05/06 are present but marked pending-operator-fingerprint
  </acceptance_criteria>
  <done>The runbook and results file are authored; every operator-independent verification (UAT-04 + the build/grep guards) is run and recorded as evidence; production-write rows await the operator's live submit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full bug-report pipeline is built and loaded as unpacked extensions. 05-UAT-RUNBOOK.md is ready with step-by-step operator actions. Claude has already recorded all operator-independent evidence (UAT-04 privacy audit + build/grep guards) in 05-UAT-RESULTS.md.
  </what-built>
  <how-to-verify>
    Follow 05-UAT-RUNBOOK.md and perform the irreducible browser steps against the PRODUCTION Worker, then report outcomes back so Claude can run the scripted KV verifications:

    1. UAT-01 (Chrome): On a real Google Patents page, trigger a no-match or yellow-confidence outcome, click Report, set the note "v5.0 UAT-01 smoke", Submit. Report back: the fp:XXXXXXXX from the Discord embed footer, and confirm the Discord rich-embed appeared.
    2. UAT-02 (Chrome): Submit 5 reports, then attempt a 6th within 10 minutes. Confirm the 6th shows the "Too many reports" toast and does NOT submit. Wait 10 minutes, submit a 7th; report its fingerprint. Confirm whether a Worker call occurred for the blocked 6th (it must not).
    3. UAT-03 (dedup): Submit twice with the SAME patent + SAME category + SAME selection within 15 minutes (note "v5.0 UAT-03 smoke"); report the fingerprint and confirm only ONE Discord notification fired. Wait 15+ minutes, submit a third identical one; report it.
    4. UAT-05 (cross-browser + SW): Repeat the UAT-01 submit in Firefox (about:debugging) and report the fingerprint. In Chrome, force a report into the queue (submit while offline via DevTools), then chrome://extensions > Details > Service worker > Terminate, go back online, and trigger the SW to restart; report whether the queued report retried. 
    5. UAT-06 (toggle): Open the dialog, expand "What's included", toggle [Remove selection text] ON, Submit (note "v5.0 UAT-06 smoke"); report the fingerprint, confirm the Discord embed has NO selection-text block, and confirm the toggle is still ON when you re-open the dialog.

    For each item, paste the fingerprint(s) and your observed browser/Discord outcomes. Claude will then run the wrangler kv key list/get checks and finalize PASS/FAIL.
  </how-to-verify>
  <resume-signal>Paste the fingerprints and per-UAT browser/Discord outcomes, or describe any failures.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run wrangler KV verifications from operator fingerprints; finalize PASS/FAIL</name>
  <read_first>
    - 05-UAT-RESULTS.md (the pending scripted rows authored in Task 1)
    - worker/src/report-schema.md (PAY-01 allowlist — fields to assert present/absent)
    - 05-RESEARCH.md "## UAT Automation Split" UAT-01/03/05/06 wrangler sub-steps
  </read_first>
  <action>
    Using the fingerprint(s) the operator supplied, run the read-only wrangler verifications and record evidence into 05-UAT-RESULTS.md:
    UAT-01: wrangler kv key list --namespace-id=cefe2733c0074fe2a28a49ff536de105 --prefix "report:<fp>:" (assert >=1 record); wrangler kv key get the record and pipe to a node assertion that ('ip' in r)===false, ('clientIp' in r)===false, ('userAgent' in r)===false, r.fingerprint is present and equals the embed fingerprint, and the schema matches the PAY-01 allowlist; duplicate_count===0 on the first record.
    UAT-02: assert the blocked-6th produced NO new KV record (list count unchanged) and the 7th produced one new record.
    UAT-03: assert exactly ONE record for the fingerprint after the second submit with duplicate_count===2; after the third (post-15-min) submit, assert TWO records for that fingerprint and the newest has duplicate_count===0.
    UAT-05: assert a KV record exists for the Firefox submit fingerprint and for the Chrome post-SW-restart retried fingerprint.
    UAT-06: wrangler kv key get the UAT-06 record and assert selectionText===null (PAY-01 has nullable selectionText — null, since the toggle removed it).
    Mark each UAT item PASS only when BOTH the operator-confirmed evidence (browser/Discord) AND the Claude-verified evidence (KV/CLI) are recorded; otherwise FAIL with the specific gap. Write the milestone-DoD footer summarizing all six. If any wrangler call needs auth, surface an auth checkpoint rather than guessing.
    Test-record cleanup (D-04 discretion): note the UAT KV keys; either issue wrangler kv key delete for the "v5.0 UAT smoke" records or document that they expire via the 90-day TTL — record the choice in the results footer.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-UAT-RESULTS.md && grep -q "duplicate_count" .planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-UAT-RESULTS.md && grep -Eq "PASS|FAIL" .planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-UAT-RESULTS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - UAT-01: KV record under report:{fp}:{ts} verified present, PAY-01 schema match, NO ip/clientIp/userAgent, fingerprint matches Discord embed, duplicate_count=0 — recorded with the actual key + values
    - UAT-02: no KV record for the blocked 6th; a record for the 7th — recorded
    - UAT-03: exactly 1 record with duplicate_count=2 within 15 min; 2 records (newest duplicate_count=0) after 15 min — recorded
    - UAT-04: already PASS from Task 1 (web-ext lint, grep guards, privacy URL, manifest cross-check)
    - UAT-05: KV records for both Firefox and Chrome-post-SW-restart fingerprints — recorded; operator-confirmed SW retry
    - UAT-06: KV record selectionText===null — recorded; operator-confirmed absence from Discord embed + sticky toggle
    - Every UAT item has a final PASS/FAIL with both operator and Claude evidence; milestone-DoD footer present; test-record cleanup decision documented
    - Only read-only wrangler calls were issued (no POST to the Worker, no Discord/KV write from Claude)
  </acceptance_criteria>
  <done>All six UAT items have recorded operator + Claude evidence and a final PASS/FAIL; the milestone DoD live-evidence requirement is satisfied (or specific gaps are documented for gap-closure).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → production Worker /report | The ONLY path that writes production Discord/KV during UAT; user-initiated, rate-limited, fingerprint-deduped (v1 trust model) |
| Claude → production KV | Read-only (wrangler kv key get/list); no writes |
| repo → shipped extension | Webhook URL must never be present (DoD grep guard) |
| production KV record | Must contain no ip/clientIp/userAgent (PAY-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Information Disclosure | webhook URL leaking into shipped extension | mitigate | UAT-04 grep 'discord.com/api/webhooks' returns zero; grep guard recorded as PASS evidence (DoD) |
| T-05-14 | Information Disclosure | IP / PII persisted in KV report record | mitigate | UAT-01/03/05/06 KV records asserted to have no ip/clientIp/userAgent via scripted node check (PAY-03) |
| T-05-15 | Tampering | Claude autonomously firing production Discord/KV writes | mitigate | Blocking human checkpoint gates all live submits; Claude automation is read-only against production (D-04); no POST issued by Claude |
| T-05-16 | Spoofing | unauthenticated abuse of /report | accept | v1 trust model is unauthenticated + rate-limited (LIMIT-02) + fingerprint-deduped (LIMIT-01); CAPTCHA explicitly out of scope (REQUIREMENTS Out of Scope) |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies; wrangler already installed (4.54.0); no install tasks; nothing to slopcheck |
</threat_model>

<verification>
- 05-UAT-RUNBOOK.md and 05-UAT-RESULTS.md exist and are complete.
- UAT-04 evidence (web-ext lint, webhook grep, content-fetch grep, privacy URL, manifest cross-check) recorded as PASS by Claude.
- UAT-01/02/03/05/06 have operator-confirmed browser/Discord evidence AND Claude-verified wrangler KV evidence, each with a final PASS/FAIL.
- No production write was performed by Claude (read-only wrangler + local build/grep only).
</verification>

<success_criteria>
- D-03: live UAT against production Worker; "v5.0 UAT-0N smoke" notes.
- D-04: operator/automation split explicit per item; Claude scripts all scriptable evidence; operator does irreducible browser steps; no autonomous production side-effects.
- UAT-01..06 each PASS with dual (operator + Claude) evidence, or documented gaps for gap-closure.
- Phase-4 deferred live tests (04-HUMAN-UAT.md) closed via UAT-05/06.
</success_criteria>

<output>
Create `.planning/phases/05-options-page-debug-mode-popup-fallback-live-uat/05-05-SUMMARY.md` when done.
</output>
