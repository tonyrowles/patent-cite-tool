---
phase: 06-security-gate-worker-auth-split
plan: 03
type: tdd
wave: 2
depends_on: ["06-02"]
files_modified:
  - worker/tests/security-gate.test.js
autonomous: true
requirements: [SEC-02, SEC-04, SEC-05, WRKR-01, WRKR-02, WRKR-03, WRKR-04]

must_haves:
  truths:
    - "WRKR-04: a request to a patent route with 20210123456A1 returns 400 before any USPTO fetch; a granted number (12505414) is NOT rejected by the published-app guard"
    - "WRKR-02: GET /cache returns 200/404 for Bearer, 200/404 for valid Origin (with reflected-origin CORS + Vary), and 401/403 for neither"
    - "WRKR-01: GET /webapp/pdf requires a valid Origin (403 without); a valid-Origin request passes auth"
    - "WRKR-03: a webapp (Origin) POST /cache results in a stored KV payload carrying source:\"webapp\""
    - "SEC-04: the 31st webapp GET from one IP within the window returns 429 with Retry-After:60"
    - "SEC-05: POST /cache returns 503 once the wq counter reaches 900; X-PCT-Test-Mode suppresses the wq and wrl counters and the cache write"
    - "SEC-02: an extension build with PROXY_TOKEN set injects that value into the bundle (build-smoke)"
  artifacts:
    - path: "worker/tests/security-gate.test.js"
      provides: "Miniflare integration coverage for WRKR-01..04, SEC-04, SEC-05 + testMode suppression"
      contains: "security-gate"
      min_lines: 120
  key_links:
    - from: "worker/tests/security-gate.test.js"
      to: "worker/src/index.js (default export worker.fetch)"
      via: "createExecutionContext + worker.fetch(request, env, ctx)"
      pattern: "worker.fetch"
    - from: "tests"
      to: "env.PATENT_CACHE / env.BUG_REPORTS (Miniflare KV)"
      via: "cloudflare:workers env binding, PROXY_TOKEN='test-token'"
      pattern: "from 'cloudflare:workers'"
---

<objective>
Add `worker/tests/security-gate.test.js` — Miniflare integration tests proving the Phase 6 Worker behaviors hold: published-application rejection (WRKR-04), dual-auth GET /cache (WRKR-02), GET /webapp/pdf Origin auth (WRKR-01), POST /cache webapp provenance (WRKR-03), webapp rate-limit 429 (SEC-04), and daily-write-guard 503 + testMode suppression (SEC-05). Add a build-smoke assertion for esbuild token injection (SEC-02).

Purpose: The verification spine of the security gate — the tests that prove the gate actually holds before any webapp code ships.
Output: One new test file; `cd worker && npm test` green; `npm test` (root) green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-security-gate-worker-auth-split/06-RESEARCH.md
@.planning/phases/06-security-gate-worker-auth-split/06-PATTERNS.md
@.planning/phases/06-security-gate-worker-auth-split/06-02-PLAN.md

<interfaces>
<!-- Test harness pattern verified from worker/tests/test-mode.test.js + report-route.test.js -->

Imports (copy verbatim):
  import { env } from 'cloudflare:workers';
  import { createExecutionContext, waitOnExecutionContext } from 'cloudflare:test';
  import { describe, it, expect } from 'vitest';
  import worker from '../src/index.js';

const TEST_TOKEN = 'test-token'; // matches worker/vitest.config.js miniflare.bindings.PROXY_TOKEN

Invocation triad (copy verbatim per test):
  const ctx = createExecutionContext();
  const response = await worker.fetch(request, env, ctx);
  await waitOnExecutionContext(ctx);

Origin request:  new Request(`https://worker.example.com${path}`, { method, headers: { 'Origin':'https://cite.tonyrowles.com', 'CF-Connecting-IP': ip } })
Bearer request:  new Request(`https://worker.example.com${path}`, { method, headers: { 'Authorization':`Bearer ${TEST_TOKEN}`, 'CF-Connecting-IP': ip } })

Isolation rules (Miniflare KV is shared within the file):
- distinct patent numbers per describe block (avoid dedup collisions)
- distinct IPs per describe block (avoid rate-limit counter collisions)
- rate-limit test: dedicated IP, send 31 requests
- write-guard test: do NOT use testMode (let it write toward 900); all OTHER POST /cache tests SET X-PCT-Test-Mode:true to preserve guard budget; run the write-guard block last or with an isolated date key
- published-app numbers asserting 400: '20210123456A1', 'US20210123456A1', '20210123456'; granted controls (NOT rejected): '12505414', '12505414B2'
- wq key derivation mirrors worker: `wq:` + new Date().toISOString().slice(0,10).replace(/-/g,'')
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write security-gate integration tests for WRKR-01..04 and SEC-04</name>
  <files>worker/tests/security-gate.test.js</files>
  <read_first>
    - worker/tests/test-mode.test.js (imports, TEST_TOKEN, invocation triad, KV-empty assertion)
    - worker/tests/report-route.test.js (distinct-IP / distinct-patent isolation pattern)
    - worker/src/index.js (the routes added in plan 06-02 — confirm exact status codes and CORS header names)
    - .planning/phases/06-security-gate-worker-auth-split/06-PATTERNS.md (section "worker/tests/security-gate.test.js")
  </read_first>
  <behavior>
    - Published-app (WRKR-04): GET /cache?patent=20210123456A1 (Bearer) → 400; GET /webapp/pdf?patent=US20210123456A1 (Origin) → 400; control GET /cache?patent=12505414 → NOT 400 from the published-app guard (200 or 404).
    - Dual-auth GET /cache (WRKR-02): Bearer → 200/404 (not 401); valid Origin → 200/404 with Access-Control-Allow-Origin reflecting the origin + Vary:Origin; neither → 401 or 403.
    - GET /webapp/pdf (WRKR-01): no Origin + no Bearer → 403; valid Origin passes auth (assert NOT 403/401 — a 200 or a 502 from the USPTO fetch both prove auth passed).
    - Rate limit (SEC-04): the 31st Origin GET to /webapp/pdf from a dedicated IP → 429 with Retry-After:60.
  </behavior>
  <action>
    Create `worker/tests/security-gate.test.js` mirroring `test-mode.test.js`. One describe block per requirement, each with its own patent-number and IP constants to prevent KV state collisions. Use the invocation triad for every assertion. For WRKR-04 assert exact status 400 on published-app inputs and assert the granted control is not 400-from-guard. For WRKR-02 assert all three auth outcomes and, on the Origin path, assert `response.headers.get('Access-Control-Allow-Origin') === 'https://cite.tonyrowles.com'` and `response.headers.get('Vary') === 'Origin'`. For WRKR-01 assert 403 without auth and not-403 with valid Origin. For SEC-04 send 31 requests from a dedicated IP and assert the 31st is 429 with `Retry-After: 60`. This is a TDD plan: write the tests first; they fail only if 06-02 behavior is wrong. If an expectation mismatches the actual 06-02 implementation, fix the test expectation to match a CORRECT security behavior — never weaken worker/src/index.js to make a wrong test pass.
  </action>
  <verify>
    <automated>cd worker && npx vitest run tests/security-gate.test.js 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `worker/tests/security-gate.test.js` exists with describe blocks naming WRKR-01, WRKR-02, WRKR-04, SEC-04.
    - `cd worker && npx vitest run tests/security-gate.test.js` passes all WRKR-01/02/04 and SEC-04 cases.
    - The file imports from `cloudflare:workers` and `cloudflare:test` and uses `worker.fetch`.
    - At least one assertion checks `Access-Control-Allow-Origin` reflects the specific origin and `Vary: Origin` is present.
  </acceptance_criteria>
  <done>WRKR-01/02/04 and SEC-04 are covered by passing Miniflare integration tests with proper KV/IP isolation.</done>
</task>

<task type="tdd">
  <name>Task 2: Add WRKR-03 provenance, SEC-05 write-guard 503 + testMode suppression, and SEC-02 build-smoke</name>
  <files>worker/tests/security-gate.test.js</files>
  <read_first>
    - worker/src/index.js checkDailyWriteGuard + the POST /cache origin path (from 06-02)
    - worker/tests/test-mode.test.js (the KV-empty assertion pattern for testMode suppression)
    - .planning/phases/06-security-gate-worker-auth-split/06-RESEARCH.md ("Daily Write Guard" + "X-PCT-Test-Mode Extension to New Write Paths" + "POST /cache Provenance Field")
  </read_first>
  <behavior>
    - WRKR-03: an Origin POST /cache (non-testMode, fresh isolated key) writes a KV payload whose stored JSON has source === "webapp".
    - SEC-05: after the wq:YYYYMMDD counter reaches 900, the next non-testMode Origin POST /cache → 503.
    - testMode suppression: an Origin POST /cache with X-PCT-Test-Mode:true returns 201 but the target cache key is absent from env.PATENT_CACHE.list() and the wq counter is unchanged.
    - SEC-02: running the extension build with a sentinel PROXY_TOKEN produces a bundle containing that value.
  </behavior>
  <action>
    Add a WRKR-03 describe block: a non-testMode Origin POST /cache to a dedicated isolated patent key with a minimal JSON body, then read back via `env.PATENT_CACHE.get(key,{type:'json'})` and assert `stored.source === 'webapp'`. Add a SEC-05 describe block (run LAST or with an isolated date key): pre-seed the counter via `env.PATENT_CACHE.put('wq:'+todayKey, '900', { expirationTtl: 172800 })` (todayKey derived exactly as the worker derives it), then assert one non-testMode Origin POST /cache (fresh patent) → 503. Add a testMode-suppression test: an Origin POST /cache with X-PCT-Test-Mode:true → 201, then assert the target key is NOT in `env.PATENT_CACHE.list()` and the wq counter value is unchanged from before the call. Implement SEC-02 as a shell assertion in <verify> (not inside vitest) because the build writes to dist/ outside the worker test sandbox: run the build with a sentinel token and grep the output bundles. Keep strict KV isolation; do not let the SEC-05 seeded counter poison the WRKR-03 or suppression assertions.
  </action>
  <verify>
    <automated>cd worker && npx vitest run tests/security-gate.test.js 2>&1 | tail -8</automated>
    <automated>PROXY_TOKEN=smoketest_sec02 npm run build >/dev/null 2>&1 && grep -rl "smoketest_sec02" dist/chrome dist/firefox >/dev/null && echo SEC02_INJECT_OK</automated>
  </verify>
  <acceptance_criteria>
    - The test file has describe blocks naming WRKR-03 and SEC-05 and a testMode-suppression test.
    - `cd worker && npx vitest run tests/security-gate.test.js` passes all cases (full file green).
    - WRKR-03 reads back the stored payload and asserts `source === 'webapp'`.
    - SEC-05 asserts a 503 after the wq counter is at 900.
    - `PROXY_TOKEN=smoketest_sec02 npm run build` then grep of dist/chrome+dist/firefox finds the sentinel (SEC-02 injection proven).
    - `cd worker && npm test` (full worker suite) and `npm test` (root, all suites) both pass.
  </acceptance_criteria>
  <done>WRKR-03, SEC-05, testMode suppression, and SEC-02 build-smoke all covered and green; full worker suite and root suite pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → production KV | Tests run against Miniflare (PROXY_TOKEN='test-token'); must never hit live USPTO or production KV |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Tampering | Test run leaking into production KV / daily quota | mitigate | Miniflare-only via cloudflare:test; all non-guard POST tests use X-PCT-Test-Mode:true; no --remote; no live USPTO fetch asserted |
| T-06-12 | Repudiation | False-green from KV state bleed between describe blocks | mitigate | Distinct patent numbers + IPs per block; SEC-05 seeded counter isolated/last |
| T-06-SC | Tampering | npm installs | accept | Zero new packages; uses already-installed vitest pool-workers |
</threat_model>

<verification>
- `cd worker && npx vitest run tests/security-gate.test.js` → all cases green.
- `cd worker && npm test` → full worker suite green (existing + new).
- `npm test` (root) → all suites (src, chrome, firefox, lint, worker) green.
- `PROXY_TOKEN=smoketest_sec02 npm run build && grep -rl smoketest_sec02 dist/chrome dist/firefox` → at least one match (SEC-02).
</verification>

<success_criteria>
- WRKR-01..04 and SEC-04/SEC-05 each have a passing Miniflare integration assertion.
- testMode suppression verified for the wq and wrl counters and the cache write.
- SEC-02 token injection verified by build-smoke.
- No test hits live USPTO or production KV.
</success_criteria>

<output>
Create `.planning/phases/06-security-gate-worker-auth-split/06-03-SUMMARY.md` when done.
</output>
