---
phase: 10-retirement-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/v40-auto-fix.yml
  - tests/e2e/scripts/v40-auto-fix-yaml.test.js
  - tests/e2e/scripts/e2e-inject-defect.test.js
  - tests/e2e/scripts/inject-defect.mjs
  - tests/e2e/scripts/e2e-explore-ci-guard.test.js
  - tests/e2e/scripts/e2e-explore-phase-flag.test.js
  - scripts/e2e-explore.mjs
  - package.json
  - tests/unit/error-class-enumeration-drift.test.js
  - tests/unit/v4-touchpoints.test.js
  - tests/unit/safe-append-ledger.test.js
  - tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js
  - scripts/quarantine-append.mjs
  - .github/workflows/v40-pdfjs-frame-shift.yml
  - .gitignore
autonomous: true
requirements: [RTR-01, RTR-02, RTR-03]
user_setup: []

must_haves:
  truths:
    - "RTR-02: A maintainer inspecting the repo finds no .github/workflows/v40-auto-fix.yml — the old issues:labeled synthetic trigger cannot fire"
    - "RTR-01: tests/e2e/scripts/inject-defect.mjs is gone and no test imports it"
    - "RTR-03: scripts/e2e-explore.mjs is gone and `npm run e2e:explore` is absent (npm errors helpfully)"
    - "No deleted artifact is referenced by any remaining test in a way that fails npm test (no dangling readFileSync/spawn/import/grep against the three deleted files)"
    - "scripts/ ledger machinery (safeAppendLedger, invokeAnthropicSdkWithLedger, two-commit split, cpr@v8 pattern) is untouched — Phase 12 reuse preserved (D-01b)"
  artifacts:
    - path: "tests/unit/error-class-enumeration-drift.test.js"
      provides: "Drift guard with Sites 2 (v40-auto-fix.yml) and 4 (inject-defect.mjs) removed, 3 remaining sites intact"
      contains: "SCAFFOLD_KEYS"
    - path: "package.json"
      provides: "scripts block with the e2e:explore entry removed"
  key_links:
    - from: "tests/unit/error-class-enumeration-drift.test.js"
      to: "remaining 3 enumeration sites (error-codes.js, fix-prompt-builder.js, llm-router.js)"
      via: "per-site helpers"
      pattern: "checkErrorCodesArray|checkPromptScaffolds|checkLlmRouterCoverage"
---

<objective>
Hard-delete the three retired v4.3 autonomous-machinery artifacts and surgically repair every live test that referenced them, so the working tree is clean (no dead-code, no skip carve-outs per D-06) and `npm test` stays green.

Purpose: Clear the retired synthetic-defect path (RTR-01), the dead v40-auto-fix workflow (RTR-02), and the autonomous explore driver (RTR-03) before any v6.1 pipeline code is written. The old `issues: labeled` trigger is structurally incompatible with the new `report-fix-candidate` trigger (STATE.md load-bearing decision).
Output: Three deleted files, four deleted dependent test files, surgical edits to four shared test files, package.json script removal, and comment-only cleanups.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-retirement-scaffolding/10-CONTEXT.md

<interfaces>
<!-- Verified blast radius (extracted from codebase during planning). The executor must -->
<!-- handle ALL of these — CONTEXT.md named only the inject-defect contract test + drift Site 4. -->

Deleting .github/workflows/v40-auto-fix.yml breaks these LIVE tests (each reads the YAML):
  - tests/e2e/scripts/v40-auto-fix-yaml.test.js  — beforeAll fs.readFileSync(YAML_PATH) → ENOENT fails whole file. Tests ONLY the deleted YAML → DELETE the test file.
  - tests/unit/v4-touchpoints.test.js TP-01 second `it(...)` — reads v40-auto-fix.yml in the test body. (Other TP-01 producer assertions + TP-02..TP-05 read OTHER live files and must stay.)
  - tests/unit/error-class-enumeration-drift.test.js Site 2 (checkWorkflowPrecheck reads v40-auto-fix.yml at SITE_PATHS.workflow).
  - tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js COMMIT-04 `it(...)` — execSync `grep -c 'git push origin main' .github/workflows/v40-auto-fix.yml` → grep exits 2 on missing file → execSync throws.

Deleting tests/e2e/scripts/inject-defect.mjs breaks:
  - tests/e2e/scripts/e2e-inject-defect.test.js — `import { ... } from './inject-defect.mjs'` (tests I1-I8 test ONLY the deleted file) → DELETE the test file.
  - tests/unit/error-class-enumeration-drift.test.js Site 4 (checkInjectDefectSet reads SITE_PATHS.injectDefect).

Deleting scripts/e2e-explore.mjs breaks:
  - tests/e2e/scripts/e2e-explore-ci-guard.test.js — spawnSync(node, [SCRIPT_PATH ...]) (tests ONLY the deleted script) → DELETE.
  - tests/e2e/scripts/e2e-explore-phase-flag.test.js — spawnSync(node, [SCRIPT_PATH ...]) (tests ONLY the deleted script) → DELETE.
  - tests/unit/safe-append-ledger.test.js — `fs.readFileSync('scripts/e2e-explore.mjs')` into `exploreSrc` then asserts `countOccurrences(exploreSrc, 'safeAppendLedger(LEDGER_PATH') >= 2`. (The promoteSrc + autoFixSrc assertions in the SAME test read OTHER live files and must stay.) → remove ONLY the exploreSrc readFileSync + its assertion.

error-class-enumeration-drift.test.js drift-guard structure (verified):
  - SCAFFOLD_KEYS = Object.keys(PROMPT_SCAFFOLDS) (7 keys), iterated via it.each across 5 sites.
  - "sanity: PROMPT_SCAFFOLDS has exactly 7 keys" — KEEP unchanged (the scaffold count is NOT changing in this plan).
  - SITE_PATHS object has `workflow` (Site 2) and `injectDefect` (Site 4) entries → remove both entries, both helper functions (checkWorkflowPrecheck, checkInjectDefectSet), both it.each blocks, and the Site 2/Site 4 lines in the two "helper failure-message shape" tests + the "HARNESS_ERROR ... still required at sites 2-5" assertions for those two sites. Keep Sites 1,3,5 fully intact. The "exactly 7 keys" sanity assertion and the HARNESS_ERROR site-1 exception test stay.

Comment-only refs (NO runtime break — soften/leave per D-04 discretion, do NOT delete logic):
  - scripts/quarantine-append.mjs L221/225 — provenance comments naming inject-defect.mjs.
  - .github/workflows/v40-pdfjs-frame-shift.yml L218/258/274 — narrative comments naming "v40-auto-fix workflow".
  - .gitignore L24 — comment "Local cron log for npm run e2e:explore".
  - eslint.config.js L133 — grandfather comment naming scripts/e2e-explore.mjs. It is a COMMENT, not a `files:` glob entry; leave the config logic untouched (deleting the file does not break lint).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire v40-auto-fix.yml and repair its 4 dependent tests (RTR-02)</name>
  <files>.github/workflows/v40-auto-fix.yml, tests/e2e/scripts/v40-auto-fix-yaml.test.js, tests/unit/v4-touchpoints.test.js, tests/unit/error-class-enumeration-drift.test.js, tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js, .github/workflows/v40-pdfjs-frame-shift.yml</files>
  <read_first>
    - .github/workflows/v40-auto-fix.yml (the file being deleted; grep it first for any badge/cross-link)
    - tests/e2e/scripts/v40-auto-fix-yaml.test.js (delete — tests only this YAML)
    - tests/unit/v4-touchpoints.test.js (surgical — TP-01 consumer assertion only)
    - tests/unit/error-class-enumeration-drift.test.js (surgical — Site 2 only in this task; Site 4 in Task 2)
    - tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js (surgical — COMMIT-04 only)
    - .github/workflows/v40-pdfjs-frame-shift.yml (comment-only refs L218/258/274)
    - .planning/STATE.md (D-01b: do NOT touch scripts/ ledger/cpr machinery)
  </read_first>
  <action>
    Delete .github/workflows/v40-auto-fix.yml entirely (D-01). First `git grep -n v40-auto-fix.yml` and clean any badge/status/doc cross-link outside .planning/milestones/ archive (D-01a). Do NOT modify any file under scripts/ that implements the ledger/cpr@v8/invokeAnthropicSdkWithLedger/safeAppendLedger patterns (D-01b) — only the YAML is retired.
    Delete tests/e2e/scripts/v40-auto-fix-yaml.test.js (its beforeAll reads the now-deleted YAML; every case targets only that file).
    In tests/unit/v4-touchpoints.test.js: remove ONLY the TP-01 `it('consumer (v40-auto-fix.yml) trigger uses issues.labeled with triage filter', ...)` block (it reads the deleted YAML). Keep the TP-01 producer assertions (issue-payload-builder.js) and TP-02..TP-05 intact. Update the TP-01 describe-block header comment so it no longer claims a v40-auto-fix.yml consumer contract.
    In tests/unit/error-class-enumeration-drift.test.js: remove the Site 2 wiring — the `workflow` entry in SITE_PATHS, the `checkWorkflowPrecheck` helper, the Site 2 `it.each` block, the Site 2 line in both "helper failure-message shape" tests, and the `checkWorkflowPrecheck(HARNESS_ERROR_EXCEPTION)` line in the "still required at sites 2-5" test. Renumber the doc-comment "5 CANONICAL ENUMERATION SITES" list to reflect the removed site, leaving Sites 1/3/5 helpers and the "exactly 7 keys" sanity test untouched. (Site 4 is removed in Task 2 — same file, single executor, sequential edits.)
    In tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js: remove ONLY the `COMMIT-04` `it(...)` block (its execSync greps the deleted YAML and throws on ENOENT). Leave all other cost-ledger assertions intact.
    In .github/workflows/v40-pdfjs-frame-shift.yml: soften the L218/258/274 narrative comments so they no longer point at the deleted v40-auto-fix workflow (comment text only — no `on:`/`jobs:` logic change, no behavior change to the frame-shift workflow).
  </action>
  <verify>
    <automated>test ! -f .github/workflows/v40-auto-fix.yml && test ! -f tests/e2e/scripts/v40-auto-fix-yaml.test.js && npm run test:src</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/v40-auto-fix.yml` exits non-zero (file absent).
    - `test -f tests/e2e/scripts/v40-auto-fix-yaml.test.js` exits non-zero (deleted).
    - `git grep -n 'v40-auto-fix.yml' -- ':!.planning/milestones/' ':!.planning/research*' tests/ scripts/ .github/` returns ZERO matches that perform a runtime read/spawn/grep of the file (remaining hits, if any, are softened narrative comments only).
    - `grep -n "git push origin main" tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js` does not reference v40-auto-fix.yml (COMMIT-04 removed).
    - `grep -c "checkWorkflowPrecheck" tests/unit/error-class-enumeration-drift.test.js` returns 0.
    - `npm run test:src` exits 0.
    - No file under `scripts/` appears in `git diff --name-only` except none (this task touches zero scripts/ files).
  </acceptance_criteria>
  <done>v40-auto-fix.yml deleted, its 4 dependent live tests repaired or removed, scripts/ ledger machinery untouched, `npm run test:src` green.</done>
</task>

<task type="auto">
  <name>Task 2: Hard-delete inject-defect.mjs + e2e-explore.mjs, repair dependent tests, remove e2e:explore script (RTR-01, RTR-03)</name>
  <files>tests/e2e/scripts/inject-defect.mjs, tests/e2e/scripts/e2e-inject-defect.test.js, scripts/e2e-explore.mjs, tests/e2e/scripts/e2e-explore-ci-guard.test.js, tests/e2e/scripts/e2e-explore-phase-flag.test.js, tests/unit/safe-append-ledger.test.js, tests/unit/error-class-enumeration-drift.test.js, package.json, scripts/quarantine-append.mjs, .gitignore</files>
  <read_first>
    - tests/e2e/scripts/inject-defect.mjs (delete)
    - tests/e2e/scripts/e2e-inject-defect.test.js (delete — imports inject-defect.mjs, tests only it)
    - scripts/e2e-explore.mjs (delete)
    - tests/e2e/scripts/e2e-explore-ci-guard.test.js (delete — spawns the deleted script)
    - tests/e2e/scripts/e2e-explore-phase-flag.test.js (delete — spawns the deleted script)
    - tests/unit/safe-append-ledger.test.js (surgical — remove exploreSrc readFileSync + its assertion only)
    - tests/unit/error-class-enumeration-drift.test.js (surgical — Site 4 only; Site 2 was removed in Task 1)
    - package.json (remove the "e2e:explore" script key)
    - scripts/quarantine-append.mjs (comment-only refs L221/225 — soften, runtime untouched)
    - .gitignore (comment-only ref L24)
  </read_first>
  <action>
    Hard-delete tests/e2e/scripts/inject-defect.mjs and scripts/e2e-explore.mjs (D-06 — git history is the archive; no skip/ignore carve-outs).
    Delete tests/e2e/scripts/e2e-inject-defect.test.js (D-02 — imports the deleted inject-defect.mjs; its I1-I8 MUTATOR cases test only that file).
    Delete tests/e2e/scripts/e2e-explore-ci-guard.test.js and tests/e2e/scripts/e2e-explore-phase-flag.test.js (both spawnSync the deleted e2e-explore.mjs via SCRIPT_PATH and test only it).
    In tests/unit/safe-append-ledger.test.js: remove ONLY the `exploreSrc = fs.readFileSync('scripts/e2e-explore.mjs')` declaration and the `countOccurrences(exploreSrc, 'safeAppendLedger(LEDGER_PATH') >= 2` assertion (plus its comment line). Keep the promoteSrc and autoFixSrc reads and their assertions — those guard live files (D-01b ledger-discipline reuse). Confirm the remaining `safeAppendLedger` coverage count test still has a valid subject.
    In tests/unit/error-class-enumeration-drift.test.js: remove the Site 4 wiring — the `injectDefect` entry in SITE_PATHS, the `checkInjectDefectSet` helper, the Site 4 `it.each` block, the Site 4 line in both "helper failure-message shape" tests, and the `checkInjectDefectSet(HARNESS_ERROR_EXCEPTION)` line in the "still required at sites 2-5" test (D-03). Keep Sites 1/3/5 and the "exactly 7 keys" sanity test intact.
    In package.json: remove the `"e2e:explore": "node scripts/e2e-explore.mjs"` script entry (D-08) so `npm run e2e:explore` is absent.
    In scripts/quarantine-append.mjs: soften the L221/225 provenance comments so they no longer point at the deleted inject-defect.mjs (D-04 — comment text only; verify NO runtime import/spawn of inject-defect.mjs exists and leave the corpus-append logic byte-unchanged).
    In .gitignore: soften the L24 comment that names `npm run e2e:explore` (keep the ignore pattern itself if it still serves a purpose; comment-only edit).
  </action>
  <verify>
    <automated>test ! -f tests/e2e/scripts/inject-defect.mjs && test ! -f scripts/e2e-explore.mjs && ! npm run e2e:explore 2>/dev/null && npm run test:src</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/e2e/scripts/inject-defect.mjs` exits non-zero (deleted).
    - `test -f scripts/e2e-explore.mjs` exits non-zero (deleted).
    - `test -f tests/e2e/scripts/e2e-inject-defect.test.js` exits non-zero, `test -f tests/e2e/scripts/e2e-explore-ci-guard.test.js` exits non-zero, `test -f tests/e2e/scripts/e2e-explore-phase-flag.test.js` exits non-zero.
    - `npm run e2e:explore` exits non-zero with an npm "missing script" error (script key removed).
    - `git grep -n "checkInjectDefectSet" tests/unit/error-class-enumeration-drift.test.js` returns 0.
    - `git grep -nE "(import|from|spawnSync|readFileSync)[^\n]*(inject-defect\.mjs|e2e-explore\.mjs)" tests/ scripts/` returns ZERO runtime references (only softened narrative comments may remain).
    - The quarantine-append corpus-append logic diff is comment-only: `git diff scripts/quarantine-append.mjs` shows changes confined to comment lines.
    - `npm run test:src` exits 0.
  </acceptance_criteria>
  <done>inject-defect.mjs and e2e-explore.mjs hard-deleted, 3 dependent test files deleted, safe-append-ledger + drift Site 4 surgically repaired, e2e:explore npm script gone, comment refs softened, `npm run test:src` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only deletes files and edits tests/comments. No new runtime input handling, no network, no LLM call, no user-controlled data path is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | Accidental deletion/edit of reused `scripts/` ledger machinery (safeAppendLedger, invokeAnthropicSdkWithLedger, two-commit split, cpr@v8) needed by Phase 12 | mitigate | Tasks touch ZERO scripts/ runtime logic except comment-only softening in quarantine-append.mjs; acceptance criteria assert `git diff scripts/quarantine-append.mjs` is comment-only and no other scripts/ file is modified (D-01b). |
| T-10-02 | Denial of Service (CI breakage) | A dangling readFileSync/spawn/import/grep against a deleted artifact left in a remaining test breaks `npm test`, blocking all future merges | mitigate | Full verified blast radius enumerated in `<interfaces>`; each dependent test is deleted or surgically repaired; `npm run test:src` is the gate on both tasks; repo-wide grep acceptance criteria assert zero runtime references survive. |
| T-10-03 | Repudiation | Retired YAML/scripts vanish with no audit trail | accept | Git history is the archive (D-06 locked decision); the deleted files remain fully recoverable via `git show`/`git log --follow`. No separate archive required for CODE (distinct from the .planning docs archive rule). |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NO packages (deletion + test-edit only); no new dependency surface. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npm run test:src` exits 0 after each task (run after each deletion per CONTEXT `<specifics>`, not only at the end).
- `git grep` for runtime references to the three deleted artifacts returns zero outside the `.planning/milestones/` and `.planning/research*` archives.
- `git diff --name-only` shows no change to any `scripts/` file other than the comment-only `quarantine-append.mjs` softening.
- Final whole-suite proof (build + chrome + firefox + lint + web-ext) is owned by Plan 03 (RTR-05).
</verification>

<success_criteria>
- v40-auto-fix.yml, inject-defect.mjs, e2e-explore.mjs are gone (RTR-01, RTR-02, RTR-03).
- `npm run e2e:explore` is absent.
- All dependent live tests repaired/removed; `npm run test:src` green.
- scripts/ ledger/cpr machinery untouched (Phase 12 reuse preserved).
</success_criteria>

<output>
Create `.planning/phases/10-retirement-scaffolding/10-01-SUMMARY.md` when done.
</output>
