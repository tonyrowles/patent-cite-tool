---
phase: 10-retirement-scaffolding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/lib/fix-prompt-builder.js
  - .planning/RESUME-V4.3.md
  - .planning/milestones/v4.3-phases-paused/RESUME-V4.3.md
  - .planning/STATE.md
autonomous: true
requirements: [RTR-04]
user_setup: []

must_haves:
  truths:
    - "D-09 / D-10: Phase 12 can `import { REPORT_FIX_SCAFFOLD }` (or read the standalone key) from tests/e2e/lib/fix-prompt-builder.js without a file-not-found or undefined-export failure — a bare importable stub with the prompt body marked TODO, NOT a pre-built envelope/diff-fence/fix-surface contract (success criterion #5)"
    - "RTR-04 / D-07: .planning/RESUME-V4.3.md is archived literally under .planning/milestones/v4.3-phases-paused/ (moved via git mv, not deleted — docs-archive rule, distinct from the hard-delete code rule) with a clear SUPERSEDED note at the top"
    - "RTR-04: STATE.md records that the RESUME-V4.3.md re-enable checklist is VOIDED — synthetic-trigger contract tests are removed (not un-skipped), and the issues:labeled / v40-auto-promote pull_request:closed triggers are NOT to be restored (D-12)"
    - "The fix-prompt-builder.js purity guard stays green — the stub imports NO node:fs / node:child_process / node:path / @anthropic-ai/sdk"
    - "D-11: The frozen PROMPT_SCAFFOLDS map remains exactly 7 keys — REPORT_FIX_SCAFFOLD is a SEPARATE standalone export, not a new map entry (byte-stability + drift 7-key pins stay green)"
  artifacts:
    - path: "tests/e2e/lib/fix-prompt-builder.js"
      provides: "Bare pure REPORT_FIX_SCAFFOLD standalone export, prompt body marked TODO for Phase 12"
      contains: "REPORT_FIX_SCAFFOLD"
    - path: ".planning/milestones/v4.3-phases-paused/RESUME-V4.3.md"
      provides: "Archived v4.3 re-enable checklist with SUPERSEDED-by-v6.1 note"
      contains: "SUPERSEDED"
    - path: ".planning/STATE.md"
      provides: "Explicit post-retirement record that the RESUME-V4.3 re-enable checklist is voided"
      contains: "VOIDED"
  key_links:
    - from: "tests/e2e/lib/fix-prompt-builder.js"
      to: "Phase 12 fix-generation import site"
      via: "named export REPORT_FIX_SCAFFOLD"
      pattern: "export\\s+const\\s+REPORT_FIX_SCAFFOLD"
---

<objective>
Stand up the v6.1 workspace: add a bare, pure, importable `REPORT_FIX_SCAFFOLD` stub to fix-prompt-builder.js, archive the voided `RESUME-V4.3.md` checklist literally under `.planning/milestones/`, and record the voiding in STATE.md.

Purpose: Give Phase 12 a known import location (success criterion #5) without pre-building the envelope/diff-fence/fix-surface contract — Phase 12 has its own research-phase and would rework a fleshed skeleton (D-10). Archive the v4.3 resume checklist per the docs-archive rule (D-07), distinct from the hard-delete rule for code.
Output: One new standalone export, one moved+annotated archive doc, one STATE.md bookkeeping edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-retirement-scaffolding/10-CONTEXT.md

<interfaces>
<!-- Verified constraints (extracted from codebase during planning). -->

fix-prompt-builder.js purity is pinned by tests/unit/eslint-fix-prompt-builder-guard.test.js:
  - Forbids importing node:fs, node:child_process, node:path, @anthropic-ai/sdk into this file.
  - The REPORT_FIX_SCAFFOLD stub MUST add ZERO new imports. A plain string/thunk/object literal only.

PROMPT_SCAFFOLDS is FROZEN at exactly 7 keys and double-pinned — the stub must NOT be added to it:
  - tests/unit/fix-prompt-builder-byte-stability.test.js line ~35:
      expect(Object.keys(PROMPT_SCAFFOLDS).sort()).toEqual(Object.keys(PINS).sort())  // PINS has 7 keys
  - tests/unit/error-class-enumeration-drift.test.js: it('sanity: PROMPT_SCAFFOLDS has exactly 7 keys')
  → Therefore REPORT_FIX_SCAFFOLD MUST be a SEPARATE top-level `export const`, NOT a key inside the Object.freeze({...}) PROMPT_SCAFFOLDS literal at L483 (D-11 standalone-export path). Placing it in the map would break BOTH pins.

Existing structure of tests/e2e/lib/fix-prompt-builder.js (verified line refs):
  - Named exports already present: ENVELOPE_OPEN (L72), ENVELOPE_CLOSE (L75), DIFF_FENCE_START (L81), DIFF_FENCE_END (L84), buildScaffoldSystemPrompt (L117), PROMPT_SCAFFOLDS (L483, frozen 7-key), buildFixPrompt (L534).
  - Place REPORT_FIX_SCAFFOLD as its own `export const` near the other top-level constants (e.g. just after PROMPT_SCAFFOLDS or with the envelope constants) — a bare value with a TODO(Phase 12) marker in its content. Do NOT call buildScaffoldSystemPrompt for it (avoid coupling to the pinned scaffold corpus).

Archive target (verified to exist): .planning/milestones/v4.3-phases-paused/ already holds paused phase dirs 61,62,64,65,66,67. RESUME-V4.3.md currently lives at .planning/RESUME-V4.3.md (top line: "# Resuming v4.3 (Auto-Fix Loop Closure) — restore checklist"). Use `git mv` so history follows.

STATE.md already references the supersession in "Key Locked Decisions (v6.1)" bullet ("Phase 10 retirement supersedes RESUME-V4.3.md"). D-12 asks for an EXPLICIT post-retirement record — add a dated line under the Decisions / Accumulated Context area stating the checklist is VOIDED and naming the three triggers NOT to be restored.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bare pure REPORT_FIX_SCAFFOLD standalone export (success criterion #5)</name>
  <files>tests/e2e/lib/fix-prompt-builder.js</files>
  <read_first>
    - tests/e2e/lib/fix-prompt-builder.js (the file being edited; note PROMPT_SCAFFOLDS frozen map at L483 and existing top-level exports)
    - tests/unit/eslint-fix-prompt-builder-guard.test.js (purity guard — must stay green)
    - tests/unit/fix-prompt-builder-byte-stability.test.js (7-key pin — must stay green)
    - tests/unit/error-class-enumeration-drift.test.js ("exactly 7 keys" sanity test — must stay green)
    - .planning/phases/10-retirement-scaffolding/10-CONTEXT.md (D-09/D-10/D-11)
  </read_first>
  <behavior>
    - REPORT_FIX_SCAFFOLD is importable: `import { REPORT_FIX_SCAFFOLD } from 'tests/e2e/lib/fix-prompt-builder.js'` resolves to a defined value (string or pure thunk/object).
    - Importing the module triggers no I/O and adds no forbidden imports — eslint purity guard tests (Tests 1-4) still pass.
    - PROMPT_SCAFFOLDS still has exactly 7 keys — byte-stability and drift sanity tests still pass.
    - The stub's content carries a TODO marker referencing Phase 12 (the real prompt body is deferred to Phase 12's research-phase per D-10).
  </behavior>
  <action>
    Add a single top-level `export const REPORT_FIX_SCAFFOLD = ...` to tests/e2e/lib/fix-prompt-builder.js as a SEPARATE export — NOT a key inside the frozen PROMPT_SCAFFOLDS map (D-11; adding it to the map breaks the 7-key byte-stability and drift pins). Keep it bare and pure (D-09): a plain string constant (or a trivial pure thunk returning a string) whose content is a clearly-marked `TODO(Phase 12): REPORT_FIX_SCAFFOLD prompt body — KV-report → src/shared matching-core diff; designed in Phase 12 research-phase`. Do NOT import node:fs/node:child_process/node:path/@anthropic-ai/sdk, do NOT pre-build the envelope/diff-fence/fix-surface contract shape (D-10). Place it near the existing top-level constants for discoverability.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/eslint-fix-prompt-builder-guard.test.js tests/unit/fix-prompt-builder-byte-stability.test.js tests/unit/error-class-enumeration-drift.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "export\s+const\s+REPORT_FIX_SCAFFOLD" tests/e2e/lib/fix-prompt-builder.js` returns 1.
    - `node -e "import('./tests/e2e/lib/fix-prompt-builder.js').then(m => { if (m.REPORT_FIX_SCAFFOLD === undefined) process.exit(1) })"` exits 0 (importable, defined).
    - `grep -nE "REPORT_FIX_SCAFFOLD\s*:" tests/e2e/lib/fix-prompt-builder.js` returns nothing inside the `Object.freeze({` PROMPT_SCAFFOLDS block (it is a standalone export, not a map key).
    - `grep -vE "^\s*//" tests/e2e/lib/fix-prompt-builder.js | grep -E "from 'node:(fs|child_process|path)'|@anthropic-ai/sdk"` returns nothing (no forbidden imports added).
    - The stub content contains the literal `TODO` and `Phase 12`.
    - `npx vitest run tests/unit/eslint-fix-prompt-builder-guard.test.js tests/unit/fix-prompt-builder-byte-stability.test.js tests/unit/error-class-enumeration-drift.test.js` exits 0.
  </acceptance_criteria>
  <done>REPORT_FIX_SCAFFOLD is a bare pure standalone export, importable from a known location, with PROMPT_SCAFFOLDS still 7 keys and the purity guard green.</done>
</task>

<task type="auto">
  <name>Task 2: Archive RESUME-V4.3.md with SUPERSEDED note and record voiding in STATE.md (RTR-04, D-12)</name>
  <files>.planning/RESUME-V4.3.md, .planning/milestones/v4.3-phases-paused/RESUME-V4.3.md, .planning/STATE.md</files>
  <read_first>
    - .planning/RESUME-V4.3.md (the doc being moved; top line "# Resuming v4.3 ... restore checklist")
    - .planning/STATE.md ("Key Locked Decisions (v6.1)" + Accumulated Context > Decisions)
    - .planning/phases/10-retirement-scaffolding/10-CONTEXT.md (D-07, D-12)
  </read_first>
  <action>
    `git mv .planning/RESUME-V4.3.md .planning/milestones/v4.3-phases-paused/RESUME-V4.3.md` (D-07 — move, do NOT delete; the docs-archive rule is distinct from the code hard-delete rule). At the very top of the moved file, prepend a clear note: `> SUPERSEDED by v6.1 (Phase 10, 2026-06-17) — this re-enable checklist is VOIDED. The synthetic-trigger contract tests it referenced were REMOVED, not un-skipped; the issues:labeled and v40-auto-promote pull_request:closed triggers are NOT to be restored.` (exact wording is Claude's discretion per CONTEXT, but it must name the voiding and that the checklist is superseded).
    In .planning/STATE.md (D-12): add an explicit dated record under Accumulated Context > Decisions stating the RESUME-V4.3.md re-enable checklist is VOIDED post-retirement — synthetic-trigger contract tests removed (not un-skipped), and the issues:labeled / v40-auto-promote pull_request:closed triggers NOT to be restored. Reference that RESUME-V4.3.md now lives at .planning/milestones/v4.3-phases-paused/. Do not disturb existing STATE.md sections beyond this additive record (and any progress/last_activity fields the executor normally maintains).
  </action>
  <verify>
    <automated>test -f .planning/milestones/v4.3-phases-paused/RESUME-V4.3.md && test ! -f .planning/RESUME-V4.3.md && grep -q SUPERSEDED .planning/milestones/v4.3-phases-paused/RESUME-V4.3.md && grep -q VOIDED .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/milestones/v4.3-phases-paused/RESUME-V4.3.md` exits 0 AND `test -f .planning/RESUME-V4.3.md` exits non-zero (moved, not copied).
    - `git log --follow --oneline .planning/milestones/v4.3-phases-paused/RESUME-V4.3.md` shows history pre-dating this phase (proves `git mv`, not delete+create).
    - `grep -qi SUPERSEDED .planning/milestones/v4.3-phases-paused/RESUME-V4.3.md` succeeds and the note appears in the first 5 lines.
    - `grep -qi VOIDED .planning/STATE.md` succeeds and the line names the three not-to-be-restored triggers (issues:labeled, v40-auto-promote pull_request:closed, synthetic-trigger contract tests removed not un-skipped).
  </acceptance_criteria>
  <done>RESUME-V4.3.md archived under .planning/milestones/v4.3-phases-paused/ with a SUPERSEDED note; STATE.md explicitly records the voided re-enable checklist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Stub addition + doc move + STATE edit. No runtime input, no network, no LLM call, no user-controlled data path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | The REPORT_FIX_SCAFFOLD stub weakens the fix-prompt-builder.js purity guard by importing an impure module, or breaks the 7-key PROMPT_SCAFFOLDS pins by being added to the frozen map | mitigate | Stub is a bare standalone `export const` with no imports (D-09); acceptance criteria assert zero forbidden imports and that the export is NOT inside the frozen map; the three guard/pin test files are the per-task gate. |
| T-10-05 | Repudiation | The voided RESUME-V4.3 checklist could be silently re-enabled by a future maintainer, restoring the retired synthetic path | mitigate | Doc archived with an in-file SUPERSEDED/VOIDED note AND STATE.md records the voiding naming the exact triggers not to restore (D-12); the contract tests that backed those triggers are removed in Plan 01, so re-enabling has no green path. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NO packages. No dependency surface. |
</threat_model>

<verification>
- Purity + byte-stability + drift sanity tests pass after the stub addition.
- RESUME-V4.3.md is recoverable with full `git log --follow` history at its new archive path.
- STATE.md voiding record is greppable and names the three not-to-restore triggers.
- Whole-suite green proof is owned by Plan 03 (RTR-05).
</verification>

<success_criteria>
- REPORT_FIX_SCAFFOLD importable + pure; PROMPT_SCAFFOLDS unchanged at 7 keys (success criterion #5).
- RESUME-V4.3.md archived literally with SUPERSEDED note (RTR-04).
- STATE.md records the voided checklist (RTR-04 / D-12).
</success_criteria>

<output>
Create `.planning/phases/10-retirement-scaffolding/10-02-SUMMARY.md` when done.
</output>
