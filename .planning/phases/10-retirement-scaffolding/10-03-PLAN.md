---
phase: 10-retirement-scaffolding
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - .planning/phases/10-retirement-scaffolding/10-03-VERIFICATION.md
autonomous: true
requirements: [RTR-05]
user_setup: []

must_haves:
  truths:
    - "RTR-05: `npm test` exits 0 with a test count >= the pre-retirement baseline-minus-deleted-files (no unexpected loss of coverage beyond the 4 intentionally-deleted dependent test files)"
    - "RTR-05: the 75-case golden corpus passes 100% — proving retirement changed no citation behavior"
    - "No dangling reference to any of the three deleted artifacts (v40-auto-fix.yml, inject-defect.mjs, e2e-explore.mjs) survives in live (non-archive) code, tests, scripts, configs, or workflows"
    - "The scripts/ ledger machinery and cpr@v8 pattern reused by Phase 12 are intact (safeAppendLedger single-canonical-call invariant still holds)"
  artifacts:
    - path: ".planning/phases/10-retirement-scaffolding/10-03-VERIFICATION.md"
      provides: "Recorded RTR-05 proof: full-suite exit code, test count, golden-corpus result, dangling-ref grep output"
      contains: "RTR-05"
  key_links:
    - from: "npm test"
      to: "75-case golden corpus + full vitest + lint + web-ext"
      via: "package.json test script (build → test:src → test:chrome → test:firefox → lint → test:lint)"
      pattern: "golden"
---

<objective>
Prove the retirement changed nothing in the citation pipeline: run the FULL `npm test` chain green at the expected count, confirm the 75-case golden corpus passes 100%, and prove via repo-wide grep that no dangling reference to the three deleted artifacts survives. Record the evidence.

Purpose: RTR-05 is the acceptance gate for the entire phase (CONTEXT `<specifics>`: "the phase's whole value is prove nothing broke"). This is the [BLOCKING] final proof distinct from the per-task `test:src` runs in Plans 01-02.
Output: A VERIFICATION.md capturing the green proof and the dangling-reference sweep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-retirement-scaffolding/10-CONTEXT.md
@.planning/phases/10-retirement-scaffolding/10-01-PLAN.md
@.planning/phases/10-retirement-scaffolding/10-02-PLAN.md

<interfaces>
<!-- Verified during planning. -->

`npm test` = build && test:src (vitest run, root config: include tests/**/*.test.js) && test:chrome && test:firefox && lint && test:lint (web-ext lint of dist/firefox).

The 75-case golden corpus runs inside the vitest suite (test:src). It must pass 100% — this is the citation-behavior-unchanged proof.

Pre-retirement test-file count was 94 (`find tests -name "*.test.js" | wc -l`). Plan 01 intentionally DELETES 3 dependent test files (v40-auto-fix-yaml.test.js, e2e-inject-defect.test.js, e2e-explore-ci-guard.test.js) and Plan 01/02 deletes a 4th (e2e-explore-phase-flag.test.js) → expected post-retirement count is 90. Any OTHER reduction is an unexpected coverage loss and must be investigated, not accepted.

Deleted artifacts to sweep for (live tree only — EXCLUDE .planning/milestones/ and .planning/research* which are historical archives that legitimately still name them):
  - .github/workflows/v40-auto-fix.yml
  - tests/e2e/scripts/inject-defect.mjs
  - scripts/e2e-explore.mjs

safeAppendLedger single-canonical-call invariant (STATE.md Permanent Invariants): `grep -rn "appendLedgerEntry(LEDGER_PATH" scripts/` count must remain 1 — proof the ledger machinery was not collaterally damaged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] RTR-05 full-suite green proof + golden corpus 100% + dangling-ref sweep</name>
  <files>.planning/phases/10-retirement-scaffolding/10-03-VERIFICATION.md</files>
  <read_first>
    - package.json (the test script chain: build → test:src → test:chrome → test:firefox → lint → test:lint)
    - .planning/phases/10-retirement-scaffolding/10-01-PLAN.md (what was deleted/edited)
    - .planning/phases/10-retirement-scaffolding/10-02-PLAN.md (stub + archive)
    - .planning/STATE.md (Permanent Invariants — safeAppendLedger single-canonical-call)
  </read_first>
  <action>
    Run the full `npm test` chain and capture the exit code and the vitest pass/fail/total counts. Confirm the 75-case golden corpus passes 100% (grep the vitest output for the golden-corpus suite result; if it is not obviously surfaced, run the golden-corpus test file directly via vitest and confirm 0 failures). Confirm the post-retirement test-file count is 90 (`find tests -name "*.test.js" | wc -l`) — exactly the pre-retirement 94 minus the 4 intentionally-deleted dependent test files; any other delta is an unexpected coverage loss to investigate before declaring RTR-05 met.
    Run the dangling-reference sweep: for each of the three deleted artifacts, `git grep -n` across the LIVE tree (exclude `.planning/milestones/` and `.planning/research*`) and confirm any surviving hits are softened narrative comments only — no runtime `import`/`from`/`spawnSync`/`readFileSync`/`require`/`grep`-of-file against a deleted path.
    Confirm the ledger reuse invariant survived: `grep -rn "appendLedgerEntry(LEDGER_PATH" scripts/` count is exactly 1 (D-01b — Phase 12 reuse intact).
    Record all evidence (exit codes, counts, golden-corpus result, the three grep outputs, the ledger-invariant count) in .planning/phases/10-retirement-scaffolding/10-03-VERIFICATION.md. If any check fails, do NOT mark RTR-05 met — report the failing check and stop.
  </action>
  <verify>
    <automated>npm test && test "$(find tests -name '*.test.js' | wc -l)" -eq 90 && test "$(grep -rn 'appendLedgerEntry(LEDGER_PATH' scripts/ | wc -l)" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (build + full vitest + chrome + firefox + lint + web-ext lint all green).
    - The 75-case golden corpus reports 0 failures / 75 passing in the vitest output (recorded verbatim in 10-03-VERIFICATION.md).
    - `find tests -name '*.test.js' | wc -l` equals 90 (94 baseline − 4 intentionally-deleted dependent test files).
    - For EACH deleted artifact, `git grep -nE "(import|from|spawnSync|readFileSync|require|grep -c)[^\n]*(v40-auto-fix\.yml|inject-defect\.mjs|e2e-explore\.mjs)" -- ':!.planning/milestones/' ':!.planning/research*'` returns ZERO runtime-reference matches.
    - `grep -rn "appendLedgerEntry(LEDGER_PATH" scripts/ | wc -l` equals 1.
    - .planning/phases/10-retirement-scaffolding/10-03-VERIFICATION.md exists and contains the literal `RTR-05`, the `npm test` exit code, the golden-corpus result line, the test-file count, and the three dangling-ref grep outputs.
  </acceptance_criteria>
  <done>`npm test` green, golden corpus 100%, test-file count exactly 90, zero dangling runtime references, ledger invariant intact, evidence recorded — RTR-05 met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan: runs the existing suite and greps. No code change, no runtime input, no network, no LLM call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Information disclosure (false green) | A passing `npm test` that silently dropped citation coverage (e.g. golden corpus skipped, or coverage loss masked by deleted test files) could declare RTR-05 met while behavior actually drifted | mitigate | Acceptance criteria assert the golden corpus reports 75 passing explicitly AND the test-file count equals exactly 90 (no unexpected deletions); any other delta blocks the RTR-05 declaration. |
| T-10-07 | Tampering (undetected) | Collateral damage to the reused scripts/ ledger machinery could pass the suite but break Phase 12 | mitigate | Explicit `appendLedgerEntry(LEDGER_PATH ...)` single-canonical-call count == 1 assertion (STATE.md Permanent Invariant) included as a verify gate. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NO packages; `npm test` runs the existing locked dependency tree. No new dependency surface. |
</threat_model>

<verification>
- Full `npm test` exits 0; golden corpus 75/75; test-file count 90; zero dangling runtime references; ledger invariant count 1.
- Evidence persisted in 10-03-VERIFICATION.md for audit.
</verification>

<success_criteria>
- RTR-05 met: suite green, golden corpus 100%, no dangling references, reused ledger machinery intact, evidence recorded.
</success_criteria>

<output>
Create `.planning/phases/10-retirement-scaffolding/10-03-SUMMARY.md` when done (and the 10-03-VERIFICATION.md evidence file referenced above).
</output>
