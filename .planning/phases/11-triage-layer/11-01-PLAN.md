---
phase: 11-triage-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/report-classifier.mjs
  - tests/unit/report-classifier.test.js
autonomous: true
requirements: [TRI-01, TRI-02, TRI-03, TRI-04, TRI-05]
must_haves:
  truths:
    - "TRI-01: classifyReport() returns one of {real_bug, noise, duplicate, user_error, infrastructure, ambiguous} for any KV record using only heuristic field checks (no LLM, no I/O)"
    - "TRI-02: every named rule (RULE_INFRASTRUCTURE, RULE_PDF_ERROR, RULE_REAL_BUG_GREEN, RULE_REAL_BUG_DUPS, RULE_QUARANTINE_HIT, RULE_NO_MATCH_NOISE, RULE_AMBIGUOUS) has a dedicated Vitest test fed real buildReportPayload() output spread with server-side fields (duplicate_count, fingerprint, timestamp)"
    - "TRI-03: a record with confidenceTier:green + category:inaccurate_citation OR duplicate_count >= dupThreshold OR patentNumber in quarantine set classifies as real_bug"
    - "TRI-04: classifier loads golden + quarantine patent number Sets via pure file reads at module load and reports inGoldenCorpus / inQuarantineCorpus on every result"
    - "TRI-05: category:tool_not_working and pdfParseStatus:error classify as infrastructure (never real_bug)"
    - "D-01: golden-corpus membership does NOT change classification — same auto-promote signals apply regardless of golden membership"
    - "D-03: quarantine-corpus membership is a positive real_bug signal (RULE_QUARANTINE_HIT)"
  artifacts:
    - path: "scripts/report-classifier.mjs"
      provides: "Pure heuristic classifier + corpus Sets + named rule constants + env thresholds"
      contains: "export function classifyReport"
      min_lines: 60
    - path: "tests/unit/report-classifier.test.js"
      provides: "One named test per heuristic rule using buildReportPayload() input"
      contains: "buildReportPayload"
      min_lines: 80
  key_links:
    - from: "tests/unit/report-classifier.test.js"
      to: "src/shared/report-payload-builder.js"
      via: "import buildReportPayload for real input shape (TRI-02)"
      pattern: "buildReportPayload"
    - from: "scripts/report-classifier.mjs"
      to: "tests/golden/baseline.json + tests/e2e/test-cases-quarantine.js"
      via: "pure file reads at module load (TRI-04)"
      pattern: "baseline|TEST_CASES_QUARANTINE"
---

<objective>
Build `scripts/report-classifier.mjs`: a pure, zero-I/O heuristic classifier that maps a `BUG_REPORTS` KV record onto one of {real_bug, noise, duplicate, user_error, infrastructure, ambiguous} using only field checks, plus golden/quarantine corpus cross-check via pure file reads. Every named rule is pinned by a Vitest test fed real `buildReportPayload()` output.

Purpose: This is the deterministic decision core of the triage layer. Building it pure and standalone makes every rule independently testable (TRI-02 hard constraint) and lets Plan 03's orchestrator consume it without re-deriving classification logic.
Output: `scripts/report-classifier.mjs` + `tests/unit/report-classifier.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-triage-layer/11-CONTEXT.md
@.planning/phases/11-triage-layer/11-PATTERNS.md
@.planning/phases/11-triage-layer/11-RESEARCH.md

<interfaces>
<!-- Real input shape the classifier must accept. From src/shared/report-payload-builder.js. -->
<!-- buildReportPayload() output fields: category, patentNumber, patentUrl, selectionText? (ABSENT when includeSelectionText=false), returnedCitation, confidenceTier (string 'green'|'yellow'|'red'), extensionVersion, browser, os, pdfParseStatus, triggerMode, errorLog, note -->
<!-- Server-side fields NOT in builder output (added by Worker on the KV record): duplicate_count, fingerprint, timestamp -->
<!-- REPORT_CATEGORIES (frozen, src/shared/constants.js): ['inaccurate_citation', 'no_match', 'tool_not_working', 'other'] -->

<!-- Corpus key formats (verified): -->
<!-- tests/golden/baseline.json keys: 'US11427642-spec-short-1' → patent = key.split('-')[0] -->
<!-- tests/e2e/test-cases-quarantine.js: export const TEST_CASES_QUARANTINE = [{ id: 'US...-...' }] → patent = c.id.split('-')[0] -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure heuristic classifier with named rules and corpus cross-check</name>
  <files>scripts/report-classifier.mjs</files>
  <read_first>
    - scripts/review-reports.mjs (lines 37-132: the exported pure-helper block — mirror its module structure: a comment divider then pure named exports; copy the `TTL_90_DAYS`-style `parseInt(process.env... ?? 'default', 10)` threshold convention)
    - src/shared/report-payload-builder.js (the EXACT output field set this classifier consumes; note selectionText conditional-key behavior)
    - src/shared/constants.js (REPORT_CATEGORIES frozen array — category values the rules switch on)
    - .planning/phases/11-triage-layer/11-PATTERNS.md (§ scripts/report-classifier.mjs: classifyReport signature, rule priority order, corpus loading via id.split('-')[0])
    - tests/e2e/test-cases-quarantine.js (confirm `export const TEST_CASES_QUARANTINE` and `.id` field shape)
  </read_first>
  <action>
    Create `scripts/report-classifier.mjs` as a pure ESM module with NO CLI guard and NO network/wrangler/gh I/O (corpus file reads at module load are the only fs access; they are deterministic). Export seven named-rule string constants: RULE_INFRASTRUCTURE, RULE_PDF_ERROR, RULE_REAL_BUG_GREEN, RULE_REAL_BUG_DUPS, RULE_QUARANTINE_HIT, RULE_NO_MATCH_NOISE, RULE_AMBIGUOUS (each value equals its own name string). Export `const CLASSIFICATIONS = ['real_bug','noise','duplicate','user_error','infrastructure','ambiguous']`.

    Export env thresholds following the existing `parseInt(process.env.X ?? 'default', 10)` convention (analog: review-reports.mjs TTL_90_DAYS module const): `DUP_THRESHOLD` (default 3), `POST_FIX_SUPPRESS_DAYS` (default 30), `MAX_FIXES_PER_RUN` (default 10).

    At module load, build and export two patent-number Sets via pure reads (TRI-04): `GOLDEN_PATENTS` from `tests/golden/baseline.json` keys mapped through `key.split('-')[0]`; `QUARANTINE_PATENTS` from `TEST_CASES_QUARANTINE.map(c => c.id.split('-')[0])`. Use `createRequire(import.meta.url)` for the JSON and a static `import { TEST_CASES_QUARANTINE }` for the quarantine module. Resolve paths from `dirname(fileURLToPath(import.meta.url))` joined with `'..'`.

    Export `function classifyReport(record, { goldenPatents = GOLDEN_PATENTS, quarantinePatents = QUARANTINE_PATENTS, dupThreshold = DUP_THRESHOLD } = {})`. Destructure { category, confidenceTier, pdfParseStatus, errorLog, duplicate_count, patentNumber }; compute `const dups = duplicate_count ?? 0`. Apply rules in PRIORITY ORDER (first match wins): (1) category === 'tool_not_working' → infrastructure / RULE_INFRASTRUCTURE / rationale 'category:tool_not_working — Worker route failure, not a matching-core bug'; (2) pdfParseStatus === 'error' → infrastructure / RULE_PDF_ERROR / 'pdfParseStatus:error — PDF parse failure, not a citation mismatch'; (3) category === 'inaccurate_citation' && confidenceTier === 'green' → real_bug / RULE_REAL_BUG_GREEN / 'confidenceTier:green + category:inaccurate_citation — high-confidence real bug signal'; (4) dups >= dupThreshold → real_bug / RULE_REAL_BUG_DUPS / `duplicate_count:${dups} >= threshold:${dupThreshold} — high-frequency report`; (5) quarantinePatents.has(patentNumber) → real_bug / RULE_QUARANTINE_HIT / 'patent in quarantine corpus — known problematic selection'; (6) category === 'no_match' && !(errorLog?.length) → noise / RULE_NO_MATCH_NOISE / 'category:no_match with no errorLog — likely no PDF available'; (7) catch-all → ambiguous / RULE_AMBIGUOUS / 'no heuristic rule matched — requires manual review'.

    The return object MUST always carry: `{ classification, ruleName, rationale, inGoldenCorpus: goldenPatents.has(patentNumber), inQuarantineCorpus: quarantinePatents.has(patentNumber) }`. Per D-01, golden membership is reported but NEVER alters classification — the rules above must not branch on `inGoldenCorpus`. Per D-03, quarantine membership IS a positive real_bug signal (rule 5).
  </action>
  <verify>
    <automated>node -e "import('./scripts/report-classifier.mjs').then(m => { const r = m.classifyReport({ category:'inaccurate_citation', confidenceTier:'green', patentNumber:'US11427642', duplicate_count:0 }); if (r.classification!=='real_bug' || r.ruleName!=='RULE_REAL_BUG_GREEN') process.exit(1); const i = m.classifyReport({ category:'tool_not_working', patentNumber:'X', duplicate_count:99 }); if (i.classification!=='infrastructure') process.exit(1); console.log('OK', m.GOLDEN_PATENTS.size, m.QUARANTINE_PATENTS.size); })"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/report-classifier.mjs` exports `classifyReport`, all 7 RULE_* constants, CLASSIFICATIONS, GOLDEN_PATENTS, QUARANTINE_PATENTS, DUP_THRESHOLD, POST_FIX_SUPPRESS_DAYS, MAX_FIXES_PER_RUN.
    - The verify command exits 0 (green+inaccurate→real_bug via RULE_REAL_BUG_GREEN; tool_not_working→infrastructure even with high duplicate_count, proving priority order).
    - `grep -c "export function classifyReport" scripts/report-classifier.mjs` ≥ 1.
    - Module contains NO `execSync`, NO `execFileSync`, NO `wrangler`, NO `gh ` invocation: `grep -E "execSync|execFileSync|wrangler|child_process" scripts/report-classifier.mjs` returns nothing (pure module, anti-pattern guard).
    - `GOLDEN_PATENTS.size` > 0 and `QUARANTINE_PATENTS.size` > 0 (corpus reads succeeded).
    - classifyReport return object always includes `inGoldenCorpus` and `inQuarantineCorpus` booleans regardless of classification.
  </acceptance_criteria>
  <done>classifyReport is a pure first-match-wins rule engine over KV fields, reports corpus membership without letting golden membership change the verdict (D-01), and treats quarantine membership as a real_bug signal (D-03).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest pin for every named rule using real buildReportPayload() output</name>
  <files>tests/unit/report-classifier.test.js</files>
  <read_first>
    - scripts/report-classifier.mjs (the module under test — created in Task 1)
    - tests/unit/review-reports.test.js (lines 1-32: import block + the `rec = (over={}) => ({...})` fixture-factory pattern to mirror)
    - tests/unit/report-payload-builder.test.js (lines 25-57: how buildReportPayload is called with a fresh context fixture — SCHEMA_ALLOWLIST / fresh-fixture pattern)
    - src/shared/report-payload-builder.js (buildReportPayload signature: `{ context, category, note, settings, errors, includeSelectionText }`)
    - .planning/phases/11-triage-layer/11-PATTERNS.md (§ tests/unit/report-classifier.test.js: kvRecord factory, named-rule test pattern; RESEARCH.md Pitfall 1)
  </read_first>
  <behavior>
    - RULE_INFRASTRUCTURE: category:'tool_not_working' record → classification 'infrastructure', ruleName RULE_INFRASTRUCTURE (even when duplicate_count is high — proves priority over RULE_REAL_BUG_DUPS).
    - RULE_PDF_ERROR: pdfParseStatus:'error' record → 'infrastructure', RULE_PDF_ERROR.
    - RULE_REAL_BUG_GREEN: category:'inaccurate_citation' + confidenceTier:'green' → 'real_bug', RULE_REAL_BUG_GREEN.
    - RULE_REAL_BUG_DUPS: duplicate_count:5 (>= default threshold 3), category not green-inaccurate → 'real_bug', RULE_REAL_BUG_DUPS. (Pitfall 1: duplicate_count MUST be explicitly spread onto the record — it is absent from buildReportPayload output.)
    - RULE_QUARANTINE_HIT: patentNumber that IS a member of QUARANTINE_PATENTS, no higher rule matches → 'real_bug', RULE_QUARANTINE_HIT.
    - RULE_NO_MATCH_NOISE: category:'no_match' with empty/absent errorLog → 'noise', RULE_NO_MATCH_NOISE.
    - RULE_AMBIGUOUS: category:'other', yellow tier, no signals → 'ambiguous', RULE_AMBIGUOUS.
    - D-01 guard: a record whose patentNumber IS in GOLDEN_PATENTS but matches RULE_REAL_BUG_GREEN still classifies real_bug AND reports inGoldenCorpus:true (golden membership does not block).
  </behavior>
  <action>
    Create `tests/unit/report-classifier.test.js`. Import `{ describe, it, expect } from 'vitest'`, `{ buildReportPayload } from '../../src/shared/report-payload-builder.js'`, and from `'../../scripts/report-classifier.mjs'`: classifyReport, all 7 RULE_* constants, GOLDEN_PATENTS, QUARANTINE_PATENTS.

    Define a `kvRecord(overContext = {}, overRecord = {})` factory that calls buildReportPayload with a BASE_CONTEXT (patentNumber, confidenceTier, returnedCitation, extensionVersion, pdfParseStatus:'ok') and `includeSelectionText: false`, then spreads the server-side fields explicitly: `{ ...payload, fingerprint:'aabbccddee11', timestamp:1718500000000, duplicate_count:0, ...overRecord }`. This is the load-bearing Pitfall-1 mitigation — `duplicate_count` MUST come from the explicit spread, never from buildReportPayload output.

    Write one `describe` block per named rule (7 total) plus a D-01 golden-membership-does-not-block test. For RULE_QUARANTINE_HIT, derive a real quarantine patent number with `const qPatent = [...QUARANTINE_PATENTS][0]` and assert it is defined before using it. For the D-01 test, use `const goldenPatent = [...GOLDEN_PATENTS][0]` as patentNumber on a green+inaccurate record; assert classification real_bug, ruleName RULE_REAL_BUG_GREEN, and result.inGoldenCorpus === true.

    Each test asserts both `result.classification` and `result.ruleName`. Do NOT fabricate plain object records — every record must originate from buildReportPayload spread with server-side fields (TRI-02).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/report-classifier.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/report-classifier.test.js` exits 0.
    - `grep -c "RULE_" tests/unit/report-classifier.test.js` ≥ 8 (all 7 rules + assertions referenced).
    - `grep -c "buildReportPayload" tests/unit/report-classifier.test.js` ≥ 2 (real input shape used, TRI-02).
    - `grep "duplicate_count" tests/unit/report-classifier.test.js` shows it spread in the record factory (Pitfall 1 mitigation present).
    - A test named for RULE_REAL_BUG_DUPS passes with `duplicate_count: 5` and would FAIL if duplicate_count were dropped (verify by reading: the dups record sets duplicate_count via overRecord, not buildReportPayload).
    - The D-01 test asserts `inGoldenCorpus === true` AND `classification === 'real_bug'` on the same record (golden membership does not block promotion).
  </acceptance_criteria>
  <done>Every named heuristic rule has a green Vitest pin fed real buildReportPayload() output spread with server-side duplicate_count/fingerprint/timestamp; the D-01 invariant (golden membership reported but non-blocking) is pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KV record → classifier input | User-supplied fields (category, note, selectionText, errorLog, patentNumber) enter the classifier from the BUG_REPORTS KV namespace. In THIS plan the classifier is pure and performs NO shell-out or LLM call, so injection cannot execute here — but it must not log raw user fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information disclosure | classifyReport rationale string | mitigate | Rationale strings are built from FIXED literals + bounded computed values (`duplicate_count` number, threshold number) only — never interpolate `note`, `selectionText`, or `errorLog` into the rationale. Task 1 rationale templates contain no user-text interpolation. |
| T-11-02 | Tampering | report-classifier.mjs purity | mitigate | Classifier must contain no execSync/execFileSync/wrangler/gh — enforced by the acceptance-criteria grep guard in Task 1. Shell/argument injection is structurally impossible in a pure module (no I/O surface in this plan). |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies this phase (RESEARCH.md: "Package Legitimacy Audit not applicable"). No install tasks exist; no legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run tests/unit/report-classifier.test.js` exits 0.
- `npm run test:src` exits 0 (no regression in the wider unit suite from adding the new module).
- classifier module is import-clean: `node -e "import('./scripts/report-classifier.mjs')"` resolves without throwing (corpus reads succeed).
</verification>

<success_criteria>
- All five requirements (TRI-01, TRI-02, TRI-03, TRI-04, TRI-05) are exercised by green tests.
- Every named rule is independently pinned (TRI-02).
- Golden membership is reported but never blocks (D-01); quarantine membership is a real_bug signal (D-03).
- The module is pure (no I/O beyond deterministic corpus reads) and ready for Plan 03 to import.
</success_criteria>

<output>
Create `.planning/phases/11-triage-layer/11-01-SUMMARY.md` when done.
</output>
