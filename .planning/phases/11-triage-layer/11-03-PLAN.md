---
phase: 11-triage-layer
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - scripts/ingest-reports.mjs
  - tests/unit/ingest-reports.test.js
  - .github/workflows/v61-ingest-reports.yml
  - tests/unit/ingest-reports-wrangler-guard.test.js
  - tests/unit/v61-ingest-reports-yaml.test.js
  - .gitignore
autonomous: true
requirements: [ING-01, ING-02, ING-03, ING-04, TRI-07, PROMO-01, PROMO-02, PROMO-03, PROMO-04]
must_haves:
  truths:
    - "ING-01: `node scripts/ingest-reports.mjs` reads BUG_REPORTS KV via --remote (delegated to review-reports.mjs), prints a structured JSON list of pending reports, and emits a per-run triage-report artifact"
    - "ING-02: ingest-reports.mjs imports loadReports/filterReports/writeStatus/getNamespaceId from review-reports.mjs — it contains NO inline wrangler shell-out"
    - "ING-03: a second run on the same report set creates no duplicate Issue (findExistingIssueByKvKey short-circuits createIssueWithLabels)"
    - "ING-04: every processed record gets a _review.status write-back — promoted→triaged, skipped→wontfix (D-06 vocabulary)"
    - "TRI-07: a triage-report JSON artifact is written with one entry per processed report {fingerprint, kv_key, patent_number, category, classification, rule_name, rationale, promotion_decision, promotion_source, github_issue_number}"
    - "PROMO-01: a promoted report becomes a report-fix-candidate Issue with a human-readable body (NOT a raw KV dump) carrying the <!-- kv-key: report:{fp}:{ts} --> pointer (D-10)"
    - "PROMO-02: `ingest-reports.mjs promote <fp> <ts>` force-promotes ANY record, bypassing the status==open filter (so a wontfix record can still be promoted — D-06)"
    - "PROMO-03: manual promote uses the exact same promoteRecord path as auto and records promotion_source:'manual' in the artifact"
    - "PROMO-04: the only trigger in v61-ingest-reports.yml is workflow_dispatch (no schedule/push/pull_request — pinned by a static-grep test)"
    - "D-05: find-or-create the Issue FIRST, then write KV _review.status SECOND (self-healing on partial failure)"
    - "D-02: when a promoted patent IS in the golden corpus, the Issue body carries the note 'patent in golden corpus — protect the existing golden case'"
    - "D-07/D-08: a report is skipped when isPostFixSuppressed(patentNumber) is true (merged auto-fix/* PR or closed candidate Issue within the 30-day env-configurable window); the suppressed entry carries suppressed_by_post_fix:true and createIssueWithLabels is NOT called — pinned by a mandatory suppression-path integration test"
    - "D-10/D-11: selectionText omitted from Issue body when absent; triage JSON is an upload-artifact in CI + gitignored local path, never committed to main"
    - "D-12: durability of the audit trail = GitHub Issues (permanent, promoted) + KV _review.status (all processed) + the per-run triage JSON snapshot (one entry per processed report)"
    - "COST-02: the per-run promotion cap defaults to 5 (MAX_FIXES_PER_RUN from report-classifier.mjs) and the workflow max_fixes input default is '5' — surplus promoted reports remain queued"
    - "STATE.md invariant: all wrangler kv calls in the triage layer carry --remote (pinned by ingest-reports-wrangler-guard.test.js)"
  artifacts:
    - path: "scripts/ingest-reports.mjs"
      provides: "CLI orchestrator: list (default) + promote subcommand, classifier wiring, D-05 promotion ordering, Issue body builder, artifact emission"
      contains: "from './review-reports.mjs'"
      min_lines: 120
    - path: ".github/workflows/v61-ingest-reports.yml"
      provides: "workflow_dispatch-only entry point + upload-artifact (D-11, PROMO-04); max_fixes input default '5' (COST-02)"
      contains: "workflow_dispatch"
    - path: "tests/unit/ingest-reports.test.js"
      provides: "Orchestration tests: parseArgs, Issue body shape, idempotency (mock gh), promote bypass, post-fix suppression path, artifact shape"
      min_lines: 80
    - path: "tests/unit/ingest-reports-wrangler-guard.test.js"
      provides: "Static-grep: all triage-layer wrangler kv calls include --remote; ingest delegates to review-reports.mjs"
      min_lines: 30
    - path: "tests/unit/v61-ingest-reports-yaml.test.js"
      provides: "Static-grep: workflow_dispatch-only, upload-artifact present, retention-days set (PROMO-04, TRI-07)"
      min_lines: 25
  key_links:
    - from: "scripts/ingest-reports.mjs"
      to: "scripts/review-reports.mjs"
      via: "import loadReports/filterReports/writeStatus/getNamespaceId (ING-02 reuse)"
      pattern: "from ['\"]\\./review-reports\\.mjs['\"]"
    - from: "scripts/ingest-reports.mjs"
      to: "scripts/report-classifier.mjs"
      via: "import classifyReport + GOLDEN_PATENTS/QUARANTINE_PATENTS"
      pattern: "from ['\"]\\./report-classifier\\.mjs['\"]"
    - from: "scripts/ingest-reports.mjs"
      to: "scripts/gh-client.mjs"
      via: "import makeKvReportGhClient (find-or-create + suppression)"
      pattern: "from ['\"]\\./gh-client\\.mjs['\"]"
    - from: ".github/workflows/v61-ingest-reports.yml"
      to: "scripts/ingest-reports.mjs"
      via: "node scripts/ingest-reports.mjs run step + upload-artifact"
      pattern: "ingest-reports\\.mjs"
---

<objective>
Build `scripts/ingest-reports.mjs` — the maintainer-triggered orchestrator that ties Plan 01 (classifier) and Plan 02 (KV I/O + gh client) together: read open BUG_REPORTS via the reused review-reports.mjs I/O, classify each, apply post-fix suppression, auto-promote real bugs (and manually promote any record via the `promote` subcommand) into `report-fix-candidate` Issues following the strict D-05 find-or-create-then-write ordering, write `_review.status` back, and emit a per-run triage-report JSON artifact. Add the `workflow_dispatch`-only workflow, the gitignore entry, and the two static-grep guard tests.

Purpose: This is the single command that delivers the Phase 11 goal. The Issue body format + `report-fix-candidate` label is a load-bearing contract for Phase 12's v61-report-fix.yml — it must be stable and human-readable.
Output: `scripts/ingest-reports.mjs`, `tests/unit/ingest-reports.test.js`, `.github/workflows/v61-ingest-reports.yml`, `tests/unit/ingest-reports-wrangler-guard.test.js`, `tests/unit/v61-ingest-reports-yaml.test.js`, `.gitignore` (add `.triage-reports/`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-triage-layer/11-CONTEXT.md
@.planning/phases/11-triage-layer/11-PATTERNS.md
@.planning/phases/11-triage-layer/11-RESEARCH.md
@.planning/phases/11-triage-layer/11-01-SUMMARY.md
@.planning/phases/11-triage-layer/11-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 (scripts/review-reports.mjs, now exported): -->
<!--   getNamespaceId(wranglerToml) -> nsId ; loadReports(nsId) -> records[] (each decorated with _key,_fp,_ts,_expiration) -->
<!--   filterReports(reports, { status:'open' }) ; sortReports(reports) ; getRecord(nsId, name) ; writeStatus(nsId, fp, ts, state) ; REVIEW_STATES -->
<!--   reviewStatus(record) -> current _review.status string -->
<!-- From Plan 01 (scripts/report-classifier.mjs): -->
<!--   classifyReport(record, {goldenPatents, quarantinePatents, dupThreshold}) -> {classification, ruleName, rationale, inGoldenCorpus, inQuarantineCorpus} -->
<!--   GOLDEN_PATENTS, QUARANTINE_PATENTS (Sets) ; MAX_FIXES_PER_RUN (default 5), POST_FIX_SUPPRESS_DAYS, DUP_THRESHOLD -->
<!-- From Plan 02 (scripts/gh-client.mjs): -->
<!--   makeKvReportGhClient(repo) -> { findExistingIssueByKvKey(kvKey), createIssueWithLabels(title,body,labels), isPostFixSuppressed(patentNumber, days), addLabel, listWithSearch } -->

<!-- CLI guard to copy verbatim (review-reports.mjs lines 231-238): -->
<!--   if (process.argv[1] && fileURLToPath(import.meta.url) === process.argv[1]) { try { main(process.argv.slice(2)); } catch (err) { console.error(`✖ ${err.message}`); process.exit(1); } } -->

<!-- kv-key marker (Pitfall 2 — use canonical fields, NOT _fp/_ts decorators): -->
<!--   const kvKey = `report:${record.fingerprint}:${record.timestamp}` -->

<!-- Issue body shape (D-10 / PROMO-01) — human-readable, selectionText OMITTED when absent: -->
<!--   ## Bug report: {patentNumber} ({category}) ; table of Patent/Category/Confidence tier/Returned citation/Classification ; **Triage rationale:** ... ; D-02 golden note if inGoldenCorpus ; quarantine note if inQuarantineCorpus ; **User note:** {note} if present ; trailing <!-- kv-key: ... --> -->

<!-- Workflow analog: .github/workflows/e2e-ingest-llm-report.yml (workflow_dispatch + upload-artifact@v4); issues:write permission required (e2e-weekly-digest.yml line 35 rationale) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ingest-reports.mjs orchestrator — classify, suppress, D-05 promote, write-back, artifact (ING-01/02/03/04, TRI-07, PROMO-01/02/03)</name>
  <files>scripts/ingest-reports.mjs</files>
  <read_first>
    - scripts/review-reports.mjs (lines 111-132 parseArgs pattern; lines 192-228 main() body — KV load + filter; lines 231-238 CLI guard to copy verbatim; lines 32-33 WORKER_DIR derivation for reading wrangler.toml ONLY, not for KV I/O)
    - scripts/report-classifier.mjs (Plan 01 — classifyReport signature + GOLDEN_PATENTS/QUARANTINE_PATENTS/MAX_FIXES_PER_RUN/POST_FIX_SUPPRESS_DAYS; MAX_FIXES_PER_RUN defaults to 5)
    - scripts/gh-client.mjs (Plan 02 — makeKvReportGhClient methods)
    - .planning/phases/11-triage-layer/11-PATTERNS.md (§ scripts/ingest-reports.mjs: imports, parseArgs flags, promoteRecord D-05 sequence, artifact write block)
    - .planning/phases/11-triage-layer/11-RESEARCH.md (Pitfalls 2,5,6,7 — kv-key canonical fields, D-05 ordering, promote bypass, selectionText omission)
    - src/shared/report-payload-builder.js (confirm selectionText is an ABSENT key when opted out — body builder must use a presence check, not `?? '(none)'`)
  </read_first>
  <behavior>
    - parseArgs: default command 'list'; `promote <fp> <ts>` subcommand; flags --namespace-id, --json, --dry-run, --force, --max-fixes <n>, -h/--help. Unknown -flag throws.
    - buildReportIssueBody(record, { classification, rationale, kvKey, inGoldenCorpus, inQuarantineCorpus }): produces the D-10 human-readable markdown; the trailing line is exactly `<!-- kv-key: ${kvKey} -->`; selectionText line is ENTIRELY ABSENT when record.selectionText == null (Pitfall 7); golden note present iff inGoldenCorpus (D-02 exact string 'patent in golden corpus — protect the existing golden case'); User note line present iff record.note is non-empty.
    - promoteRecord(nsId, record, classification, rationale, promotionSource, ghClient): D-05 order — (1) kvKey from canonical record.fingerprint+timestamp; (2) ghClient.findExistingIssueByKvKey(kvKey) FIRST; (3) if found → skip create, still writeStatus(...,'triaged') (self-heal), artifact promotion_decision 'skip-dedup'; (4) if not found → createIssueWithLabels(title, body, ['report-fix-candidate']) → ONLY on success writeStatus(...,'triaged'); artifact records github_issue_number + promotion_source.
    - list run: load open reports (filterReports status:'open'), for each classify; if classification real_bug AND isPostFixSuppressed(patentNumber) is TRUE → DO NOT promote, write the artifact entry with suppressed_by_post_fix:true and promotion_decision 'skip-suppressed', and write _review.status 'wontfix' (or leave skipped per design) — createIssueWithLabels is NOT called for a suppressed report; else if classification real_bug AND NOT suppressed AND under MAX_FIXES_PER_RUN cap → promoteRecord(...,'auto'); else if classification in {noise,user_error,infrastructure} → writeStatus(...,'wontfix'); ambiguous/duplicate → leave status, record decision 'skip'.
    - promote subcommand: getRecord by `report:${fp}:${ts}`, BYPASS the status==open filter entirely (D-06/PROMO-02), classify for rationale, call promoteRecord(...,'manual') — same downstream path (PROMO-03).
    - --dry-run: classify + build artifact but make NO gh create and NO writeStatus calls.
    - Always emit the artifact at the end (even on partial failure paths) and print structured JSON of pending reports to stdout (ING-01).
  </behavior>
  <action>
    Create `scripts/ingest-reports.mjs`. Do NOT import execFileSync/execSync (no inline wrangler — Pitfall 3); import `{ readFileSync, writeFileSync, mkdirSync } from 'node:fs'`, `{ fileURLToPath } from 'node:url'`, `{ dirname, join } from 'node:path'`. Import `{ getNamespaceId, filterReports, sortReports, loadReports, writeStatus, getRecord, reviewStatus } from './review-reports.mjs'`; `{ classifyReport, GOLDEN_PATENTS, QUARANTINE_PATENTS, MAX_FIXES_PER_RUN, POST_FIX_SUPPRESS_DAYS } from './report-classifier.mjs'`; `{ makeKvReportGhClient } from './gh-client.mjs'`.

    Derive `HERE = dirname(fileURLToPath(import.meta.url))` and `WORKER_DIR = join(HERE, '..', 'worker')` — used ONLY to read wrangler.toml for getNamespaceId. Never reimplement the wrangler() helper (Pitfall 3): all KV reads/writes go through the imported review-reports.mjs functions.

    Export pure helpers for testability: `parseArgs(argv)` (mirror review-reports.mjs lines 111-132; commands list/promote; flags listed in behavior — the --max-fixes default falls back to MAX_FIXES_PER_RUN which is 5), `buildReportIssueTitle(record)` (e.g. `Bug report: ${record.patentNumber} (${record.category})`), `buildReportIssueBody(record, opts)` (D-10 markdown per the interfaces block — selectionText presence check via `record.selectionText != null ? ... : null` then `.filter(Boolean).join('\n')`; D-02 golden note literal string), and `buildArtifactEntry(record, classifyResult, { promotionSource, promotionDecision, issueNumber, suppressed })` (TRI-07 shape: fingerprint, kv_key, patent_number, category, classification, rule_name, rationale, in_golden_corpus, in_quarantine_corpus, promotion_decision, promotion_source, github_issue_number, suppressed_by_post_fix, kv_status_written, processed_at — and MUST NOT include selectionText/note/xpathNode per the security threat model).

    Implement `promoteRecord(...)` with the exact D-05 ordering above (accept ghClient and optionally a writeStatus dependency so Task 3 can inject mocks). Implement `main(argv)`: parseArgs; resolve nsId from `getNamespaceId(readFileSync(join(WORKER_DIR,'wrangler.toml'),'utf8'))` unless --namespace-id given; instantiate `ghClient = makeKvReportGhClient(process.env.GITHUB_REPOSITORY)`. For `list`: load+filter status:'open', sort, classify each, apply suppression (call ghClient.isPostFixSuppressed(patentNumber) for real_bug candidates — suppressed → artifact suppressed_by_post_fix:true, NOT promoted, no createIssueWithLabels) + cap + promote/skip logic, accumulate artifact entries, print JSON. For `promote <fp> <ts>`: fetch single record bypassing status filter, classify, promoteRecord(...,'manual'). Honor --dry-run (no side effects). Write the artifact: `mkdirSync(TRIAGE_REPORTS_DIR,{recursive:true})` where TRIAGE_REPORTS_DIR = `join(HERE,'..','.triage-reports')`; CI path (process.env.CI) → `triage-report.json` (fixed name for upload-artifact); local → `triage-report-${Date.now()}.json`; `writeFileSync(outPath, JSON.stringify(entries, null, 2))`.

    Add the CLI guard verbatim from review-reports.mjs lines 231-238 (fileURLToPath form — NOT the e2e-report-issue.mjs `file://` form). Redact user-controlled fields from any console.log: log only fingerprint, category, patentNumber, classification (security threat T-11-04).
  </action>
  <verify>
    <automated>node -e "import('./scripts/ingest-reports.mjs').then(m => { const a = m.parseArgs(['promote','aabb','123']); if (a.command!=='promote'||a.positionals[0]!=='aabb') process.exit(1); const body = m.buildReportIssueBody({ patentNumber:'US11427642', category:'inaccurate_citation', confidenceTier:'green', returnedCitation:'2:15', note:'' }, { classification:'real_bug', rationale:'r', kvKey:'report:aabb:123', inGoldenCorpus:true, inQuarantineCorpus:false }); if (!body.includes('<!-- kv-key: report:aabb:123 -->')) process.exit(1); if (!body.includes('golden corpus')) process.exit(1); if (body.includes('Selected text')) process.exit(1); console.log('OK'); })"</automated>
  </verify>
  <acceptance_criteria>
    - The verify node check exits 0: parseArgs recognizes the promote subcommand; buildReportIssueBody emits the exact `<!-- kv-key: report:aabb:123 -->` marker, includes the D-02 golden-corpus note when inGoldenCorpus, and OMITS any selectionText line (Pitfall 7).
    - `grep -c "from './review-reports.mjs'" scripts/ingest-reports.mjs` ≥ 1 and `grep -cE "execFileSync|execSync|wrangler\\(" scripts/ingest-reports.mjs` is 0 (ING-02: no inline KV shell-out — Pitfall 3).
    - `grep "report-fix-candidate" scripts/ingest-reports.mjs` present (PROMO-01 label).
    - `grep "record.fingerprint" scripts/ingest-reports.mjs` present AND `grep -E "record\\._fp|record\\._ts" scripts/ingest-reports.mjs` for kv-key construction is absent (Pitfall 2 — canonical fields).
    - `grep "isPostFixSuppressed" scripts/ingest-reports.mjs` present — suppression is checked for real_bug candidates before promotion (D-07/D-08).
    - Source shows findExistingIssueByKvKey called BEFORE createIssueWithLabels in promoteRecord, and writeStatus(...'triaged') only after create succeeds (D-05 ordering — verify by reading promoteRecord).
    - CLI guard uses `fileURLToPath(import.meta.url) === process.argv[1]` (not the `file://` form).
    - `npm run test:src` exits 0 (no regression; this task's behavior is pinned by Task 3 tests).
  </acceptance_criteria>
  <done>ingest-reports.mjs is a pure consumer of the three Plan-01/02 modules; it classifies open reports, applies post-fix suppression (suppressed real_bugs are NOT promoted and carry suppressed_by_post_fix:true) and the per-run cap (default 5), promotes via the strict D-05 find-or-create-then-write sequence, writes D-06 status vocabulary, builds the D-10 human-readable Issue body (selectionText omitted when absent, D-02 golden note when applicable), supports the manual `promote` bypass (PROMO-02/03), and emits the TRI-07 artifact with no user-PII fields.</done>
</task>

<task type="auto">
  <name>Task 2: workflow_dispatch workflow + gitignore + two static-grep guard tests (PROMO-04, TRI-07, COST-02, STATE.md --remote invariant)</name>
  <files>.github/workflows/v61-ingest-reports.yml, .gitignore, tests/unit/ingest-reports-wrangler-guard.test.js, tests/unit/v61-ingest-reports-yaml.test.js</files>
  <read_first>
    - .github/workflows/e2e-ingest-llm-report.yml (workflow_dispatch + actions/upload-artifact@v4 shape to mirror)
    - .github/workflows/e2e-weekly-digest.yml (line ~35: the `issues: write` permission + its load-bearing rationale comment to copy)
    - tests/unit/eslint-sdk-guard.test.js (lines 17-32: exact readFileSync + REPO_ROOT + describe/it/expect boilerplate to copy for both guard tests)
    - .planning/phases/11-triage-layer/11-PATTERNS.md (§ v61-ingest-reports.yml full YAML; § ingest-reports-wrangler-guard.test.js and § v61-ingest-reports-yaml.test.js full test bodies)
    - .gitignore (current contents — append, do not overwrite)
  </read_first>
  <action>
    Create `.github/workflows/v61-ingest-reports.yml`: `name: v6.1 Ingest Bug Reports`; `on: workflow_dispatch:` with inputs `dry_run` (boolean, default false) and `max_fixes` (string, default '5' — matches MAX_FIXES_PER_RUN / COST-02; NOT '10') — NO schedule/push/pull_request (PROMO-04). `permissions: { contents: read, issues: write }` with the load-bearing comment copied from e2e-weekly-digest.yml (gh issue create 403s without issues:write). `concurrency: { group: v61-ingest-reports, cancel-in-progress: false }`. One job `ingest` on ubuntu-latest, timeout-minutes 15, env passing GH_TOKEN (secrets.GITHUB_TOKEN), GITHUB_REPOSITORY, CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID, DRY_RUN (inputs.dry_run), MAX_FIXES_PER_RUN (inputs.max_fixes). Steps: actions/checkout@v5, actions/setup-node@v5 (node 22, cache npm), `npm ci`, `node scripts/ingest-reports.mjs`, then `actions/upload-artifact@v4` with `if: always()`, name `triage-report-${{ github.run_id }}`, path `.triage-reports/triage-report.json`, retention-days 90, if-no-files-found error. The `max_fixes` input description should state the default is 5.

    Append `.triage-reports/` to `.gitignore` (D-11 — local artifacts never committed; the two-commit ledger split is the ONLY permitted push to main).

    Create `tests/unit/ingest-reports-wrangler-guard.test.js` (copy eslint-sdk-guard.test.js boilerplate): readFileSync scripts/review-reports.mjs, scripts/ingest-reports.mjs, scripts/gh-client.mjs. Assert: (a) every `['kv', 'key', ...]` args array in review-reports.mjs includes `'--remote'` (match `/\['kv', 'key',[^\]]+\]/g`, filter out comment lines with `grep -v '^#'`-equivalent line filtering, assert length>0 and each contains --remote); (b) ingest-reports.mjs has no raw wrangler kv call without --remote (match `/wrangler.*kv/g`, each must contain --remote — expected zero matches since it delegates); (c) ingest-reports.mjs imports from './review-reports.mjs' (ING-02 positive); (d) ingest-reports.mjs CLI guard uses `fileURLToPath(import.meta.url)` and `process.argv[1]`.

    Create `tests/unit/v61-ingest-reports-yaml.test.js` (copy boilerplate): readFileSync the workflow. Assert: file exists; text matches `/workflow_dispatch/`; text does NOT match `/^\s+schedule:/m`, `/^\s+push:/m`, `/^\s+pull_request:/m` (PROMO-04); text matches `/actions\/upload-artifact/` and `/triage-report/` (TRI-07); text matches `/retention-days/`; text matches `/issues:\s*write/` (gh create permission); the `max_fixes` input default is `'5'` (NOT '10') — assert the default literal `'5'` appears in the max_fixes input block and `default: '10'` does NOT appear (COST-02).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/ingest-reports-wrangler-guard.test.js tests/unit/v61-ingest-reports-yaml.test.js && grep -q ".triage-reports/" .gitignore && echo GITIGNORE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/ingest-reports-wrangler-guard.test.js tests/unit/v61-ingest-reports-yaml.test.js` exits 0 (both guard tests green).
    - `grep -q ".triage-reports/" .gitignore` succeeds (D-11 — local artifacts gitignored, never committed).
    - The wrangler-guard test asserts every `['kv', 'key', ...]` array in review-reports.mjs contains `--remote` (STATE.md invariant) and that ingest-reports.mjs imports from `./review-reports.mjs` (ING-02 positive). Comment lines are filtered before counting so header prose cannot self-invalidate the grep (no bare `== 0` on unfiltered files).
    - The yaml-guard test asserts `workflow_dispatch` present AND `schedule:`/`push:`/`pull_request:` ABSENT (PROMO-04), plus `actions/upload-artifact` + `triage-report` + `retention-days` (TRI-07) and `issues: write`, AND the `max_fixes` input default is `'5'` (COST-02 — `default: '10'` absent).
    - `.github/workflows/v61-ingest-reports.yml` exists; `grep -c "workflow_dispatch" .github/workflows/v61-ingest-reports.yml` ≥ 1; `grep -cE "^\s+schedule:|^\s+push:|^\s+pull_request:" .github/workflows/v61-ingest-reports.yml` is 0; the max_fixes input `default:` value is `'5'` (`grep "default: '10'" .github/workflows/v61-ingest-reports.yml` returns nothing).
    - The workflow runs `node scripts/ingest-reports.mjs` and uploads `.triage-reports/triage-report.json` with `if: always()`.
  </acceptance_criteria>
  <done>The workflow_dispatch-only entry point exists with upload-artifact (PROMO-04, TRI-07, D-11) and a `max_fixes` input defaulting to '5' (COST-02), `.triage-reports/` is gitignored, and the two static-grep guards pin the --remote invariant, the no-cron trigger, and the cap default.</done>
</task>

<task type="auto">
  <name>Task 3: ingest-reports orchestration tests — idempotency, D-05 ordering, promote bypass, post-fix suppression, artifact (ING-01/02/03/04, PROMO-01/02/03, TRI-06)</name>
  <files>tests/unit/ingest-reports.test.js</files>
  <read_first>
    - scripts/ingest-reports.mjs (Task 1 output — the exported parseArgs, buildReportIssueTitle, buildReportIssueBody, buildArtifactEntry, promoteRecord surfaces under test)
    - tests/unit/e2e-report-issue.test.js (lines 1-45: vi.fn() mock-client injection pattern; how a mock ghClient with findExistingIssueByKvKey/createIssueWithLabels is constructed and asserted with toHaveBeenCalled/not.toHaveBeenCalled)
    - tests/unit/review-reports.test.js (the `rec = (over={}) => ({...})` fixture-factory pattern; how records are shaped for these tests)
    - .planning/phases/11-triage-layer/11-PATTERNS.md (§ tests/unit/ingest-reports.test.js: makeMockGhClient, idempotency test pattern)
  </read_first>
  <action>
    Create `tests/unit/ingest-reports.test.js`. Import `{ describe, it, expect, vi } from 'vitest'` and the exported pure surfaces from `'../../scripts/ingest-reports.mjs'` (parseArgs, buildReportIssueTitle, buildReportIssueBody, buildArtifactEntry, and promoteRecord — invoke promoteRecord with an injected mock ghClient and an injected writeStatus spy). Define `makeMockGhClient({ existingIssue = null, createdNumber = 42, suppressed = false } = {})` returning `{ findExistingIssueByKvKey: vi.fn().mockReturnValue(existingIssue), createIssueWithLabels: vi.fn().mockReturnValue({ number: createdNumber }), isPostFixSuppressed: vi.fn().mockReturnValue(suppressed) }`. The `suppressed` parameter provides the true-return branch (the current makeMockGhClient hardcodes false) so the suppression path can be exercised.

    Cover: (1) parseArgs — `list` default, `promote aabb 123` → command 'promote' with positionals; unknown -flag throws. (2) buildReportIssueBody — contains the exact `<!-- kv-key: ... -->` marker; OMITS any selectionText line when record has no selectionText (Pitfall 7); includes the D-02 golden note when inGoldenCorpus true; includes a `**User note:**` line iff record.note non-empty; is human-readable markdown (has the `## Bug report:` heading), NOT a JSON dump. (3) ING-03 idempotency — with a mock ghClient whose findExistingIssueByKvKey returns an existing issue, promoteRecord does NOT call createIssueWithLabels (assert `not.toHaveBeenCalled()`) and still attempts the KV write (self-heal, D-05). (4) D-05 ordering — with no existing issue, assert findExistingIssueByKvKey is called before createIssueWithLabels and the KV write happens only after create (inspect `.mock.invocationCallOrder` or a writeStatus spy). (5) PROMO-03 — promoteRecord with promotionSource 'manual' produces an artifact entry with `promotion_source: 'manual'`. (6) buildArtifactEntry privacy — the entry contains fingerprint/classification/rule_name/rationale/promotion_source but does NOT contain selectionText, note, or xpathNode (`expect(entry).not.toHaveProperty('selectionText')` etc.). (7) MANDATORY post-fix suppression INTEGRATION test (TRI-06 / D-07/D-08) — drive the list-processing path (or a small extracted `processReport`-style helper) for a real_bug-classified record with a mock ghClient created via `makeMockGhClient({ suppressed: true })`; assert (a) `createIssueWithLabels` is NOT called, (b) the resulting triage artifact entry carries `suppressed_by_post_fix: true` (and `promotion_decision: 'skip-suppressed'`), and (c) the record's `_review.status` is written `wontfix`/skipped per the design (assert via the injected writeStatus spy — either called with 'wontfix' or not called for the create path). Add a complementary `suppressed: false` assertion proving the SAME real_bug record DOES promote (createIssueWithLabels called) when not suppressed, isolating suppression as the cause.

    Mock the KV writeStatus via dependency injection (promoteRecord / processing-helper parameter) or `vi.mock('../../scripts/review-reports.mjs', ...)` for writeStatus only. Do NOT call the live wrangler or gh CLI.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/ingest-reports.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/ingest-reports.test.js` exits 0.
    - An ING-03 test asserts `createIssueWithLabels` is NOT called when findExistingIssueByKvKey returns an existing issue (idempotency proven via mock).
    - A D-05 test asserts find-before-create ordering and KV-write-after-create-success.
    - A PROMO-02/03 test asserts the manual promote path records `promotion_source: 'manual'`.
    - A privacy test asserts the artifact entry has NO `selectionText`, `note`, or `xpathNode` key (`expect(entry).not.toHaveProperty('selectionText')` etc.).
    - A body test asserts `buildReportIssueBody` omits selectionText when absent and includes the D-02 golden note when inGoldenCorpus.
    - A MANDATORY suppression-path integration test (TRI-06) uses `makeMockGhClient({ suppressed: true })` and asserts: `createIssueWithLabels` is NOT called, the artifact entry has `suppressed_by_post_fix: true` (and `promotion_decision: 'skip-suppressed'`), and `_review.status` is written `wontfix`/skipped per design; a paired `suppressed: false` assertion proves the same real_bug record DOES promote when not suppressed.
    - `makeMockGhClient` exposes a `suppressed` parameter so `isPostFixSuppressed` can return true (the true-return branch is reachable — it no longer hardcodes false).
    - `grep -c "not.toHaveBeenCalled\|toHaveBeenCalled" tests/unit/ingest-reports.test.js` ≥ 3 (mock-based behavioral assertions present, including the suppression-path call/not-call assertions).
  </acceptance_criteria>
  <done>The orchestration logic is pinned: idempotent re-runs create no duplicate Issue (ING-03), D-05 ordering is enforced, manual promote records promotion_source:'manual' (PROMO-03), the post-fix suppression path is integration-tested (suppressed real_bug → no Issue, suppressed_by_post_fix:true, wontfix/skip status; TRI-06/D-07/D-08), the artifact carries no user PII, and the Issue body is human-readable with the kv-key marker and conditional D-02/selectionText handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KV record fields → gh CLI | Untrusted `patentNumber` flows into suppression search queries; `note`/`selectionText` flow into the Issue body (constructed here, sent via gh-client). |
| KV record fields → Issue markdown / artifact JSON / stdout logs | A malicious `note` could contain a forged `<!-- kv-key -->` marker; user fields could leak into logs or the durable artifact. |
| Triage artifact → main branch | D-11: the artifact must never be committed to main (the two-commit ledger split is the ONLY permitted direct push to main — STATE.md invariant). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Tampering | Issue body construction (ingest-reports.mjs → gh-client createIssueWithLabels) | mitigate | Body is built as a string and handed to gh exclusively via gh-client's `--body-file -` stdin path (Plan 02 T-11-02). ingest-reports.mjs performs NO direct gh shell-out — verified by the acceptance grep that execSync/execFileSync count is 0. |
| T-11-03 | Tampering (dedup poisoning) | kv-key marker construction | mitigate | The marker is built from canonical `record.fingerprint`+`record.timestamp` (server-computed, Pitfall 2), never from user free-text. A user cannot forge a marker for a kv-key they do not own. Acceptance grep asserts `record._fp`/`record._ts` are NOT used in kv-key construction. |
| T-11-04 | Information disclosure | console.log + triage artifact + Issue body | mitigate | (a) Logs emit only fingerprint/category/patentNumber/classification — never note/selectionText/errorLog. (b) buildArtifactEntry excludes selectionText/note/xpathNode — pinned by the Task 3 privacy test (`not.toHaveProperty`). (c) Issue body omits selectionText when absent (D-10, Pitfall 7) — pinned by a Task 3 body test. |
| T-11-05 | Repudiation / integrity | triage artifact destination | mitigate | Artifact written ONLY to `.triage-reports/` (gitignored) locally and uploaded via actions/upload-artifact in CI — never committed to main. `.gitignore` entry pinned by Task 2; the workflow has no commit/push step. Preserves the STATE.md "two-commit ledger split is the ONLY permitted push to main" invariant. |
| T-11-06 | Elevation / safety | premature KV status write (D-05 inversion) | mitigate | writeStatus('triaged') is called ONLY after createIssueWithLabels succeeds (Pitfall 5). Inverting the order would permanently suppress a report with no Issue. Pinned by the Task 3 D-05 ordering test. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies (RESEARCH.md: Package Legitimacy Audit not applicable). No install tasks; no legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run tests/unit/ingest-reports.test.js tests/unit/ingest-reports-wrangler-guard.test.js tests/unit/v61-ingest-reports-yaml.test.js` exits 0.
- `npm test` (full suite: build + vitest + chrome + firefox + lint + web-ext lint) exits 0 — wave-merge gate; confirms the extraction (Plan 02) + orchestrator introduced no regression and the 75-case golden corpus is untouched.
- `node -e "import('./scripts/ingest-reports.mjs')"` resolves (all three module imports wire correctly).
- `grep -cE "execSync|execFileSync|wrangler\\(" scripts/ingest-reports.mjs` is 0 (ING-02 — no inline KV/gh shell-out).
- `grep -c "schedule:" .github/workflows/v61-ingest-reports.yml` is 0 (PROMO-04 — no cron).
</verification>

<success_criteria>
- All nine requirements (ING-01..04, TRI-07, PROMO-01..04) are exercised by green tests.
- A maintainer can run `node scripts/ingest-reports.mjs` (list) and `... promote <fp> <ts>` (manual bypass) — both follow the identical D-05 promote path.
- Re-running on the same report set creates no duplicate Issues (ING-03, idempotency-pinned).
- The post-fix suppression path is integration-tested: a suppressed real_bug creates no Issue and is recorded suppressed_by_post_fix:true (TRI-06).
- The per-run cap and workflow max_fixes default to 5 (COST-02).
- The Issue body is human-readable, carries the kv-key pointer, omits selectionText when absent, and adds the D-02 golden note — the load-bearing Phase 12 contract is stable.
- The triage artifact is upload-artifact in CI + gitignored locally; nothing is committed to main (D-11).
- The --remote invariant and workflow_dispatch-only trigger are pinned by static-grep tests.
</success_criteria>

<output>
Create `.planning/phases/11-triage-layer/11-03-SUMMARY.md` when done.
</output>
</content>
