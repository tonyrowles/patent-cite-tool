---
phase: 12-fix-generation-regression-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/lib/fix-primitives.js
  - scripts/auto-fix.mjs
  - tests/e2e/lib/llm-driver.js
  - eslint.config.js
  - tests/unit/fix-primitives.test.js
  - tests/unit/llm-driver.test.js
autonomous: true
requirements: [COST-01, COST-04]
must_haves:
  truths:
    - "D-02: both auto-fix.mjs and the new report dispatcher import parseFencedDiff/changedPathsFromDiff from one shared module (no re-derivation)"
    - "COST-01: invokeAnthropicSdkWithLedger accepts a source param so report-fix callers record source:'report-fix-api' (default 'auto-fix-api' preserves legacy behavior)"
    - "fix-primitives.js is purity-clean (no fs/child_process/path/sdk imports), guarded by ESLint + Vitest"
  artifacts:
    - path: "tests/e2e/lib/fix-primitives.js"
      provides: "parseFencedDiff, changedPathsFromDiff, re-exported DIFF_FENCE_START/END"
      exports: ["parseFencedDiff", "changedPathsFromDiff", "DIFF_FENCE_START", "DIFF_FENCE_END"]
    - path: "tests/e2e/lib/llm-driver.js"
      provides: "invokeAnthropicSdkWithLedger with optional source param"
      contains: "source = 'auto-fix-api'"
    - path: "tests/unit/fix-primitives.test.js"
      provides: "behavior + purity pins for extracted primitives"
  key_links:
    - from: "scripts/auto-fix.mjs"
      to: "tests/e2e/lib/fix-primitives.js"
      via: "import + re-export of parseFencedDiff/changedPathsFromDiff"
      pattern: "from '.*fix-primitives\\.js'"
    - from: "tests/e2e/lib/llm-driver.js ledger write sites"
      to: "source param"
      via: "both former 'auto-fix-api' literals replaced by the param"
      pattern: "source,"
---

<objective>
Extract the invariant-pinned diff primitives from `scripts/auto-fix.mjs` into a new shared, purity-guarded module that both the legacy v4.0 dispatcher and the new v6.1 `report-fix.mjs` dispatcher import (D-02), and add an optional `source` parameter to `invokeAnthropicSdkWithLedger` so the report-fix path records `source:'report-fix-api'` (COST-01) without bypassing the ledger guard.

Purpose: Establish one source of truth for diff extraction (no duplicated regex that could drift from the COST-04/diff-fence pins) and close the documented ledger-leak vector where the SDK driver hardcodes `source:'auto-fix-api'` (project_auto_fix_ledger_leak_vector).
Output: `tests/e2e/lib/fix-primitives.js`, updated `auto-fix.mjs` re-export, updated `llm-driver.js` signature, ESLint purity entry, new `fix-primitives.test.js`, updated `llm-driver.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fix-generation-regression-gate/12-CONTEXT.md
@.planning/phases/12-fix-generation-regression-gate/12-RESEARCH.md
@.planning/phases/12-fix-generation-regression-gate/12-PATTERNS.md

<interfaces>
<!-- Exact current state — extracted from codebase, no exploration needed. -->

scripts/auto-fix.mjs:371 — export function parseFencedDiff(llmText) { ... }  (returns {ok,reason}|{ok,diff})
scripts/auto-fix.mjs:401 — export function changedPathsFromDiff(diff) { ... }  (returns string[])
tests/e2e/lib/fix-prompt-builder.js:81 — export const DIFF_FENCE_START = '===DIFF_START===';
tests/e2e/lib/fix-prompt-builder.js:84 — export const DIFF_FENCE_END = '===DIFF_END===';

tests/e2e/lib/llm-driver.js:525 — export async function invokeAnthropicSdkWithLedger({ systemPrompt, systemBlocks, userPrompt, model='claude-sonnet-4-6', maxTokens=4096, timeoutMs=120_000, phase, issueId, prNumber, forceApi=false } = {})
tests/e2e/lib/llm-driver.js:617 — source: 'auto-fix-api',   (error-branch ledger write)
tests/e2e/lib/llm-driver.js:651 — source: 'auto-fix-api',   (success-branch ledger write)

Exact primitive bodies to move are in PATTERNS.md lines 141-184 (parseFencedDiff with escapeRe helper, and changedPathsFromDiff). Copy them VERBATIM — do not re-derive.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create fix-primitives.js and retarget auto-fix.mjs to import from it</name>
  <files>tests/e2e/lib/fix-primitives.js, scripts/auto-fix.mjs, eslint.config.js, tests/unit/fix-primitives.test.js</files>
  <read_first>
    - scripts/auto-fix.mjs (lines 371-417 — the parseFencedDiff body + escapeRe helper + changedPathsFromDiff body being moved; plus the existing imports of DIFF_FENCE_START/END from fix-prompt-builder.js)
    - tests/e2e/lib/fix-prompt-builder.js (lines 1-18 purity header pattern; lines 81-84 DIFF_FENCE_START/END constants)
    - tests/e2e/lib/safe-append-ledger.js (sibling module placement convention)
    - .planning/phases/12-fix-generation-regression-gate/12-PATTERNS.md (lines 123-187 — exact bodies to extract + retargeting note)
  </read_first>
  <behavior>
    - parseFencedDiff('a\n===DIFF_START===\ndiff --git ...\n===DIFF_END===\n') returns { ok:true, diff:'diff --git ...' }
    - parseFencedDiff with no fences returns { ok:false, reason:'no-fences' }
    - parseFencedDiff with two start fences returns { ok:false, reason:'multiple-diff-blocks' }
    - parseFencedDiff with mismatched fence counts returns { ok:false, reason:'unbalanced-fences' }
    - parseFencedDiff(42) returns { ok:false, reason:'non-string-llm-text' }
    - changedPathsFromDiff strips 'b/' prefix, skips '/dev/null', dedupes repeated +++ headers
    - changedPathsFromDiff(null) returns []
    - DIFF_FENCE_START re-exported from fix-primitives.js equals '===DIFF_START==='; DIFF_FENCE_END equals '===DIFF_END==='
  </behavior>
  <action>
    Create `tests/e2e/lib/fix-primitives.js` with the D-04 purity header (NO node:fs, node:child_process, node:path, @anthropic-ai/sdk imports). Move the EXACT bodies of `parseFencedDiff` (auto-fix.mjs:371-394, including the local `escapeRe` helper) and `changedPathsFromDiff` (auto-fix.mjs:401-417) into it as named exports — copy verbatim, do not re-derive the regex. Re-export `DIFF_FENCE_START` and `DIFF_FENCE_END` from `./fix-prompt-builder.js` so callers have one import target. Then edit `scripts/auto-fix.mjs` to remove the two local function definitions and instead `import { parseFencedDiff, changedPathsFromDiff } from '../tests/e2e/lib/fix-primitives.js'` and re-export them (`export { parseFencedDiff, changedPathsFromDiff }`) so any existing test that imports them from `auto-fix.mjs` keeps working. Add a per-file ESLint block entry for `tests/e2e/lib/fix-primitives.js` in `eslint.config.js` mirroring the existing `fix-prompt-builder.js` restricted-import block (same 4 restricted imports — PROMPT-04 pattern). Create `tests/unit/fix-primitives.test.js` covering all behaviors above plus a purity pin asserting the source text of fix-primitives.js does NOT contain 'node:fs', 'node:child_process', 'node:path', '@anthropic-ai/sdk' (read the file via fs in the test).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/fix-primitives.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/fix-primitives.test.js` exits 0
    - `grep -c "function parseFencedDiff" scripts/auto-fix.mjs` returns 0 (definition moved; only the re-export remains)
    - `grep -E "from '\.\./tests/e2e/lib/fix-primitives\.js'" scripts/auto-fix.mjs` matches (auto-fix imports the shared module)
    - `grep -vE '^\s*(//|\*)' tests/e2e/lib/fix-primitives.js | grep -cE "node:fs|node:child_process|node:path|@anthropic-ai/sdk"` returns 0 (purity, comments excluded)
    - Any pre-existing test that imports parseFencedDiff/changedPathsFromDiff from auto-fix.mjs still passes (run `npx vitest run` for the broader unit suite)
  </acceptance_criteria>
  <done>fix-primitives.js exists with both functions + re-exported fences; auto-fix.mjs imports & re-exports them; purity + behavior pins green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add optional source param to invokeAnthropicSdkWithLedger</name>
  <files>tests/e2e/lib/llm-driver.js, tests/unit/llm-driver.test.js</files>
  <read_first>
    - tests/e2e/lib/llm-driver.js (lines 476-560 — signature, inverse-CI gate, doc block; lines 607-652 — the two appendLedgerEntry sites with source:'auto-fix-api')
    - tests/unit/llm-driver.test.js (existing assertions on source:'auto-fix-api' that must remain green)
    - .planning/phases/12-fix-generation-regression-gate/12-PATTERNS.md (lines 227-280 — exact signature change + both write-site edits)
    - .planning/phases/12-fix-generation-regression-gate/12-RESEARCH.md (lines 679-683 — Pitfall 5: two hardcoded sites at 617 and 651)
  </read_first>
  <behavior>
    - Calling invokeAnthropicSdkWithLedger WITHOUT a source arg writes ledger entries with source 'auto-fix-api' (unchanged legacy behavior)
    - Calling with source:'report-fix-api' writes both the error-branch and success-branch ledger entries with source 'report-fix-api'
    - The default-param value is the string literal 'auto-fix-api'
  </behavior>
  <action>
    In `tests/e2e/lib/llm-driver.js`, add `source = 'auto-fix-api'` to the destructured parameter object of `invokeAnthropicSdkWithLedger` (currently ending at `forceApi = false`). Replace the hardcoded `source: 'auto-fix-api',` at BOTH ledger write sites (line ~617 error branch, line ~651 success branch) with bare `source,`. Do not change the inverse-CI gate, transport, or any other behavior. Update `tests/unit/llm-driver.test.js`: keep the existing default-behavior assertions (no source arg → 'auto-fix-api') green, and add a new test that passes source:'report-fix-api' and asserts the written ledger entry carries that source. Use the existing test's ledger-capture mechanism (E2E_LEDGER_PATH_OVERRIDE redirect or the existing mock) — do not invent a new harness.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/llm-driver.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/llm-driver.test.js` exits 0 (both legacy default + new report-fix-api assertions)
    - `grep -c "source: 'auto-fix-api'" tests/e2e/lib/llm-driver.js` returns 1 (only the default-param literal remains; both write-site literals removed)
    - `grep -c "source = 'auto-fix-api'" tests/e2e/lib/llm-driver.js` returns 1 (the new default param)
    - `grep -c "^      source,$" tests/e2e/lib/llm-driver.js` is >= 2 OR both write sites use bare `source,` (manual confirm both branches updated)
  </acceptance_criteria>
  <done>invokeAnthropicSdkWithLedger accepts source with default 'auto-fix-api'; both write sites use the param; default + report-fix-api tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM call → committed ledger | Cost-accounting entries must be attributable; a path that writes the wrong/absent `source` corrupts cap math and forensics |
| local dev run → committed ledger | safeAppendLedger CI gate prevents local pollution; this plan must not introduce a bypass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Repudiation | llm-driver.js ledger source | mitigate | source param defaults to 'auto-fix-api' (legacy unchanged) and report-fix path passes 'report-fix-api'; both write sites updated; pinned by llm-driver.test.js (COST-01) |
| T-12-02 | Tampering | fix-primitives.js purity | mitigate | ESLint per-file restricted-import block + Vitest purity pin assert no fs/child_process/path/sdk imports (no shell/IO surface introduced in the shared primitive) |
| T-12-03 | Tampering | diff-fence extraction drift | accept | parseFencedDiff body copied verbatim from the COST-04-pinned source; behavior pins in fix-primitives.test.js lock the extraction contract |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced (RESEARCH Package Legitimacy Audit confirms zero new deps); nothing to verify |
</threat_model>

<verification>
- `npx vitest run` (full unit suite) exits 0 — no existing import of the moved primitives breaks.
- `grep -rn "appendLedgerEntry(LEDGER_PATH" scripts/` count unchanged (this plan adds no new direct ledger writes).
</verification>

<success_criteria>
- fix-primitives.js created, purity-clean, behavior-pinned; auto-fix.mjs sources the primitives from it (D-02 single source of truth).
- invokeAnthropicSdkWithLedger has an optional source param; report-fix callers can record source:'report-fix-api' (COST-01); legacy default preserved.
</success_criteria>

<output>
Create `.planning/phases/12-fix-generation-regression-gate/12-01-SUMMARY.md` when done.
</output>
