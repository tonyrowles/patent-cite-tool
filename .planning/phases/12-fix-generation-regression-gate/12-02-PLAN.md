---
phase: 12-fix-generation-regression-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/lib/fix-prompt-builder.js
  - tests/unit/fix-prompt-builder.test.js
autonomous: true
requirements: [FIX-02, FIX-03]
must_haves:
  truths:
    - "FIX-02: REPORT_FIX_SCAFFOLD targets only the src/shared/ matching core and enumerates all 10 current FORBIDDEN_PATHS entries as plain-text LLM instruction"
    - "FIX-03: user-controlled field names (note, selectionText, errorLog) never appear in the REPORT_FIX_SCAFFOLD system prompt; the <report_data> envelope is named there but populated only in the user turn"
    - "REPORT_FIX_SCAFFOLD stays a standalone top-level export, NOT a key in frozen PROMPT_SCAFFOLDS (PROMPT_SCAFFOLDS still has exactly 7 keys; 7 byte-stability sha256 pins stay green)"
  artifacts:
    - path: "tests/e2e/lib/fix-prompt-builder.js"
      provides: "REPORT_FIX_SCAFFOLD real prompt body (replaces line-521 stub)"
      contains: "export const REPORT_FIX_SCAFFOLD"
    - path: "tests/unit/fix-prompt-builder.test.js"
      provides: "Phase 12 scaffold content + structure + sha256 byte-stability pins"
  key_links:
    - from: "REPORT_FIX_SCAFFOLD"
      to: "check-diff-guard.mjs FORBIDDEN_PATHS (10 entries)"
      via: "all 10 path substrings present as plain-text instruction"
      pattern: "tests/e2e/lib/llm-router\\.js"
    - from: "REPORT_FIX_SCAFFOLD"
      to: "diff fence contract"
      via: "===DIFF_START=== / ===DIFF_END=== literals present"
      pattern: "===DIFF_START==="
---

<objective>
Replace the `REPORT_FIX_SCAFFOLD` stub (fix-prompt-builder.js:521) with the researched prompt body (FIX-02/FIX-03): a system prompt that constrains the LLM to fix only the `src/shared/` matching core, enumerates all 10 current FORBIDDEN_PATHS, names the `<report_data>` untrusted-envelope trust boundary, includes the overfit DO-NOT instruction, and pins the `===DIFF_START===`/`===DIFF_END===` output fence — kept as a standalone export so the 7 frozen PROMPT_SCAFFOLDS byte-stability pins do not break.

Purpose: The prompt body is the milestone's highest-uncertainty deliverable (ROADMAP research flag) and the structural prompt-injection defense; the dispatcher (Plan 03) and workflow (Plan 04) consume this export.
Output: Real `REPORT_FIX_SCAFFOLD` body + a new Phase 12 test block (content + structure + sha256 byte-stability) in `fix-prompt-builder.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fix-generation-regression-gate/12-CONTEXT.md
@.planning/phases/12-fix-generation-regression-gate/12-RESEARCH.md
@.planning/phases/12-fix-generation-regression-gate/12-PATTERNS.md

<interfaces>
<!-- Exact current state. -->

tests/e2e/lib/fix-prompt-builder.js:483 — export const PROMPT_SCAFFOLDS = Object.freeze({ ... })  (7 keys — DO NOT add REPORT_FIX_SCAFFOLD here)
tests/e2e/lib/fix-prompt-builder.js:494-521 — REPORT_FIX_SCAFFOLD stub block (replace the line-521 string)
tests/e2e/lib/fix-prompt-builder.js:81 — DIFF_FENCE_START = '===DIFF_START==='
tests/e2e/lib/fix-prompt-builder.js:84 — DIFF_FENCE_END = '===DIFF_END==='
tests/e2e/lib/fix-prompt-builder.js:117-178 — buildScaffoldSystemPrompt 5-section template to study (trust-boundary → fix-surface → forbidden-paths → diff-size-cap → output-format)

The 10 LOCKED FORBIDDEN_PATHS literals (from scripts/check-diff-guard.mjs:53-64) that MUST appear as substrings in the prompt:
  tests/test-cases.js
  tests/golden/baseline.json
  tests/e2e/test-cases-quarantine.js
  .github/workflows/v40-          (the v40 workflow namespace prefix)
  tests/e2e/.llm-spend-ledger.json
  .github/CODEOWNERS
  tests/e2e/.rerun-ring-buffer.json
  tests/e2e/.flake-suppression.json
  tests/e2e/lib/fix-prompt-builder.js
  tests/e2e/lib/llm-router.js

The full drafted prompt body is in 12-RESEARCH.md lines 290-375. Implement THAT draft (do not re-derive); ensure all 10 paths above are present (the draft already lists them).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace REPORT_FIX_SCAFFOLD stub with the researched prompt body</name>
  <files>tests/e2e/lib/fix-prompt-builder.js</files>
  <read_first>
    - tests/e2e/lib/fix-prompt-builder.js (lines 1-18 purity header; 81-84 fence constants; 117-178 buildScaffoldSystemPrompt 5-section template; 483-521 PROMPT_SCAFFOLDS map + the REPORT_FIX_SCAFFOLD stub)
    - .planning/phases/12-fix-generation-regression-gate/12-RESEARCH.md (lines 276-375 — the drafted REPORT_FIX_SCAFFOLD body to implement)
    - scripts/check-diff-guard.mjs (lines 53-64 — the authoritative 10-entry FORBIDDEN_PATHS bank the prompt text must mirror)
    - .planning/phases/12-fix-generation-regression-gate/12-PATTERNS.md (lines 190-223 — stub-replacement rules, content requirements, NOT-in-PROMPT_SCAFFOLDS rule)
  </read_first>
  <behavior>
    - typeof REPORT_FIX_SCAFFOLD === 'string' and it is NOT in PROMPT_SCAFFOLDS (Object.keys(PROMPT_SCAFFOLDS).length stays 7)
    - REPORT_FIX_SCAFFOLD contains all 10 FORBIDDEN_PATHS substrings listed in interfaces
    - REPORT_FIX_SCAFFOLD contains '<report_data>' and does NOT contain '<issue_body_untrusted>'
    - REPORT_FIX_SCAFFOLD contains '===DIFF_START===' and '===DIFF_END==='
    - REPORT_FIX_SCAFFOLD does NOT contain the user-field tokens 'note:', 'selectionText:', 'errorLog:' (FIX-03 — those belong to the user turn built in Plan 03)
    - REPORT_FIX_SCAFFOLD names the three src/shared targets: matching.js, position-map-builder.js, pdf-parser.js
    - REPORT_FIX_SCAFFOLD contains an explicit DO-NOT-hardcode-patent-number instruction (overfit guard FIX-04 prep)
  </behavior>
  <action>
    Replace the single-line stub at fix-prompt-builder.js:521 (`export const REPORT_FIX_SCAFFOLD = 'TODO(Phase 12)...'`) with the `[...].join('\n')` array form implementing the drafted body from 12-RESEARCH.md lines 290-375. Keep it OUTSIDE the frozen PROMPT_SCAFFOLDS map (standalone top-level export). Use the literal fence strings '===DIFF_START===' / '===DIFF_END===' (or DIFF_FENCE_START/DIFF_FENCE_END concatenated) in the output-format section. Enumerate all 10 FORBIDDEN_PATHS exactly as substrings (mirror check-diff-guard.mjs:53-64 — note the bank has 10 entries, not the 6 some older comments claim). Do NOT mention note/selectionText/errorLog by name. Keep the file purity-clean (no new imports). Maintain the existing module-header purity invariant.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/fix-prompt-builder.test.js tests/unit/fix-prompt-builder-byte-stability.test.js tests/unit/eslint-fix-prompt-builder-guard.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The three named test files exit 0 (existing 7 sha256 byte-stability pins for PROMPT_SCAFFOLDS stay green — proves REPORT_FIX_SCAFFOLD was NOT added to the frozen map)
    - `node -e "import('./tests/e2e/lib/fix-prompt-builder.js').then(m=>{const p=['tests/test-cases.js','tests/golden/baseline.json','tests/e2e/test-cases-quarantine.js','.github/workflows/v40-','tests/e2e/.llm-spend-ledger.json','.github/CODEOWNERS','tests/e2e/.rerun-ring-buffer.json','tests/e2e/.flake-suppression.json','tests/e2e/lib/fix-prompt-builder.js','tests/e2e/lib/llm-router.js'];const miss=p.filter(x=>!m.REPORT_FIX_SCAFFOLD.includes(x));if(miss.length)throw new Error('MISSING '+miss);console.log('all 10 present')})"` prints "all 10 present"
    - `node -e "import('./tests/e2e/lib/fix-prompt-builder.js').then(m=>{if(m.REPORT_FIX_SCAFFOLD.includes('<issue_body_untrusted>'))throw 'wrong envelope';if(!m.REPORT_FIX_SCAFFOLD.includes('<report_data>'))throw 'no report_data';console.log('envelope ok')})"` prints "envelope ok"
  </acceptance_criteria>
  <done>REPORT_FIX_SCAFFOLD is the real prompt body, standalone export, all 10 forbidden paths + report_data envelope + diff fences present; 7 frozen pins green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Phase 12 REPORT_FIX_SCAFFOLD pins (content, structure, sha256)</name>
  <files>tests/unit/fix-prompt-builder.test.js</files>
  <read_first>
    - tests/unit/fix-prompt-builder.test.js (the additive-pin block pattern around lines 604-705 and the tail near 820)
    - tests/unit/fix-prompt-builder-byte-stability.test.js (lines 17-47 — sha256 pin helper pattern)
    - .planning/phases/12-fix-generation-regression-gate/12-PATTERNS.md (lines 528-583 — the exact Phase 12 describe() block to add, including all assertions)
  </read_first>
  <behavior>
    - Test asserts typeof REPORT_FIX_SCAFFOLD === 'string' AND typeof PROMPT_SCAFFOLDS.REPORT_FIX_SCAFFOLD === 'undefined'
    - Test asserts Object.keys(PROMPT_SCAFFOLDS).length === 7
    - Test asserts all 10 FORBIDDEN_PATHS substrings present (FIX-02)
    - Test asserts 'note:', 'selectionText:', 'errorLog:' ABSENT from REPORT_FIX_SCAFFOLD (FIX-03)
    - Test asserts '<report_data>' present and '<issue_body_untrusted>' absent
    - Test asserts '===DIFF_START===' and '===DIFF_END===' present
    - Test pins sha256(REPORT_FIX_SCAFFOLD) to the digest computed from the final body
  </behavior>
  <action>
    Add a `describe('Phase 12 REPORT_FIX_SCAFFOLD', ...)` block to `tests/unit/fix-prompt-builder.test.js` implementing every assertion in 12-PATTERNS.md lines 538-583. For the sha256 pin: after Task 1 is final, compute the digest with `node -e "import('./tests/e2e/lib/fix-prompt-builder.js').then(m=>import('node:crypto').then(c=>console.log(c.createHash('sha256').update(m.REPORT_FIX_SCAFFOLD,'utf8').digest('hex'))))"` and paste that hex into the pin assertion. Ensure REPORT_FIX_SCAFFOLD and PROMPT_SCAFFOLDS are imported at the top of the test file if not already.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/fix-prompt-builder.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/fix-prompt-builder.test.js` exits 0 including the new Phase 12 block
    - `grep -c "Phase 12 REPORT_FIX_SCAFFOLD" tests/unit/fix-prompt-builder.test.js` returns 1
    - `grep -E "toBe\('[0-9a-f]{64}'\)" tests/unit/fix-prompt-builder.test.js` matches (a real 64-hex sha256 pin, not a placeholder)
    - The FIX-03 absent-token assertions for 'note:', 'selectionText:', 'errorLog:' are present (grep the test file)
  </acceptance_criteria>
  <done>Phase 12 scaffold pins exist and pass; sha256 is a real digest; FIX-02 + FIX-03 + 7-key-count guards all asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user report fields → LLM | note/selectionText/errorLog are user-authored; they must never enter the system prompt (only the user-turn <report_data> envelope) |
| FORBIDDEN_PATHS bank → scaffold text | drift between check-diff-guard.mjs and the plain-text instruction lets the LLM target an unlisted protected path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering (prompt injection) | REPORT_FIX_SCAFFOLD system prompt | mitigate | <report_data> trust-boundary instruction in system prompt; user fields never named in system prompt (FIX-03 absent-token Vitest pins); escaping happens in the user-turn builder (Plan 03) |
| T-12-05 | Elevation of Privilege (scope escape) | scaffold forbidden-paths section | mitigate | All 10 current FORBIDDEN_PATHS enumerated as plain-text LLM instruction; Vitest pins all 10 substrings present; defense-in-depth with checkDiffGuard in Plan 03 |
| T-12-06 | Tampering (overfit) | scaffold DO-NOT instruction | mitigate | Explicit "do not hardcode the patent number" instruction; enforced post-apply by the FIX-04 dispatcher scan (Plan 03) |
| T-12-07 | Tampering (drift) | frozen PROMPT_SCAFFOLDS byte-stability | mitigate | REPORT_FIX_SCAFFOLD kept standalone; 7-key-count + 7 sha256 pins remain green; new separate sha256 pin locks the scaffold body |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan (RESEARCH Package Legitimacy Audit: zero new deps) |
</threat_model>

<verification>
- 7 existing PROMPT_SCAFFOLDS sha256 byte-stability pins stay green (REPORT_FIX_SCAFFOLD not in the frozen map).
- fix-prompt-builder.js purity/ESLint guard test stays green (no new I/O imports).
</verification>

<success_criteria>
- REPORT_FIX_SCAFFOLD is the real, researched prompt body (FIX-02 fix-surface + 10 forbidden paths; FIX-03 <report_data> envelope; overfit DO-NOT; diff fences).
- It is a standalone export; the 7 frozen scaffold pins and key-count drift guard remain green.
- A new sha256 pin + content/structure pins lock the scaffold against future drift.
</success_criteria>

<output>
Create `.planning/phases/12-fix-generation-regression-gate/12-02-SUMMARY.md` when done.
</output>
