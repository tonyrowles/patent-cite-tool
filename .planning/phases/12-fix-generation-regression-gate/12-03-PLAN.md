---
phase: 12-fix-generation-regression-gate
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - scripts/report-fix.mjs
  - tests/unit/report-fix.test.js
autonomous: true
requirements: [FIX-01, FIX-03, FIX-04, FIX-05, COST-01, COST-03, D-05, D-06]
must_haves:
  truths:
    - "D-01: scripts/report-fix.mjs is a FRESH dispatcher entry point — auto-fix.mjs is not reused or extended for the report flow"
    - "D-07: report flow reuses existing labels only — human-review-required on the overfit PR, auto-fix-stuck on the exhausted/aborted source Issue (no new labels)"
    - "FIX-01: report-fix.mjs builds the LLM prompt from a KV record and invokes invokeAnthropicSdkWithLedger (the workflow supplies the wrangler --remote KV fetch)"
    - "FIX-03: note/selectionText/errorLog are FORBIDDEN_DELIMITERS-escaped and wrapped in the <report_data> envelope in the user turn only"
    - "FIX-04: D-03 soft-flag — when the candidate diff contains patentNumber as a string literal in src/, the dispatcher signals overfit (withhold auto-fix:verified, apply human-review-required, PR-body note) without dropping the candidate"
    - "FIX-05: selectionText is omitted entirely from the user turn when absent in the KV record"
    - "COST-01: every ledger write in report-fix.mjs goes through safeAppendLedger / invokeAnthropicSdkWithLedger with source:'report-fix-api' — never appendLedgerEntry(LEDGER_PATH directly"
    - "COST-03/D-05: only golden/quarantine regressions consume an iteration; parseFencedDiff/checkDiffGuard/git-apply-check failures hard-abort to auto-fix-stuck without burning the 3-iteration budget"
    - "D-06: dispatcher queries GitHub for an existing open auto-fix/<fp-short> PR/branch before spending LLM budget; reuse-on-retrigger, skip otherwise"
  artifacts:
    - path: "scripts/report-fix.mjs"
      provides: "fresh KV-report → src/shared diff dispatcher (D-01); buildReportUserTurn, overfit scan, hard-abort, idempotency"
      contains: "REPORT_FIX_SCAFFOLD"
    - path: "tests/unit/report-fix.test.js"
      provides: "unit pins for prompt assembly, escaping, FIX-05 omission, overfit scan, source routing"
  key_links:
    - from: "scripts/report-fix.mjs"
      to: "REPORT_FIX_SCAFFOLD + invokeAnthropicSdkWithLedger"
      via: "system prompt = scaffold, source:'report-fix-api'"
      pattern: "source: ['\"]report-fix-api['\"]"
    - from: "scripts/report-fix.mjs"
      to: "fix-primitives.js + check-diff-guard.mjs"
      via: "parseFencedDiff → changedPathsFromDiff → checkDiffGuard sequence"
      pattern: "checkDiffGuard"
    - from: "buildReportUserTurn"
      to: "<report_data> envelope"
      via: "escaped user fields in user turn only"
      pattern: "<report_data>"
---

<objective>
Build the fresh `scripts/report-fix.mjs` dispatcher (D-01) that converts a KV bug-report record into a `src/shared/` matching-core diff: assemble the user turn with the `<report_data>` envelope and FORBIDDEN_DELIMITERS-escaped user fields (FIX-03), embed the matching-core source files (Option C single-turn SDK transport), omit `selectionText` when absent (FIX-05), invoke `invokeAnthropicSdkWithLedger` with `source:'report-fix-api'` (FIX-01/COST-01), run the diff-validation sequence with D-05 hard-abort semantics, scan for the FIX-04 overfit literal (D-03 soft-flag), and enforce D-06 GitHub-authoritative idempotency — all on extracted shared primitives, with zero re-derivation of invariant-pinned machinery.

Purpose: This is the orchestration core of the fix-generation path; the workflow (Plan 04) invokes it.
Output: `scripts/report-fix.mjs` + `tests/unit/report-fix.test.js`.

CRITICAL FORK RESOLUTION (transport): Per RESEARCH Option C, use SDK transport (invokeAnthropicSdkWithLedger, single-turn) and embed src/shared/matching.js + position-map-builder.js verbatim in the user turn under <matching_core_source>; include pdf-parser.js only when pdfParseStatus === 'error'. NOTE FOR EXECUTOR: matching.js (729 lines) and position-map-builder.js (819 lines) are far larger than RESEARCH's "~130 line" estimate — embedding both is ~1650 lines / ~55KB. This is within Sonnet's context window but is a real token cost. Cap selectionText at 1000 chars, note at 256, errorLog at 5×200; this keeps the dominant cost the source files. Do NOT switch to subscription transport (claude -p) — it has an inverse CI gate and cannot run in CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fix-generation-regression-gate/12-CONTEXT.md
@.planning/phases/12-fix-generation-regression-gate/12-RESEARCH.md
@.planning/phases/12-fix-generation-regression-gate/12-PATTERNS.md

<interfaces>
<!-- Exact contracts the dispatcher consumes (Plans 01/02 provide the first two). -->

tests/e2e/lib/fix-prompt-builder.js — export const REPORT_FIX_SCAFFOLD (string, Plan 02); DIFF_FENCE_START, DIFF_FENCE_END
tests/e2e/lib/fix-primitives.js (Plan 01) — parseFencedDiff(llmText) -> {ok,reason}|{ok,diff}; changedPathsFromDiff(diff) -> string[]
tests/e2e/lib/llm-driver.js (Plan 01) — invokeAnthropicSdkWithLedger({systemPrompt, userPrompt, model, maxTokens, phase, issueId, source, forceApi}) — pass source:'report-fix-api'
tests/e2e/lib/safe-append-ledger.js — safeAppendLedger(LEDGER_PATH, entry)  (CI-gated; entry.source must be 'report-fix-api')
tests/e2e/lib/llm-ledger.js — export const LEDGER_PATH
scripts/check-diff-guard.mjs — export function checkDiffGuard(changedPaths) -> {ok,violations}; export const FORBIDDEN_PATHS
scripts/gh-client.mjs — makeKvReportGhClient(repo) -> { findExistingIssueByKvKey, createIssueWithLabels, isPostFixSuppressed, listWithSearch, addLabel }
  NOTE: gh-client has NO PR-by-head method. For D-06, either add a thin method OR call `execFileSync('gh',['pr','list','--head',`auto-fix/${fpShort}`,'--state','open','--json','number'])` (CWE-94 arg-array, NEVER a shell string).

KV record fields (src/shared/report-payload-builder.js + server-added): category, patentNumber, patentUrl, selectionText?(absent when opted out), returnedCitation, confidenceTier, pdfParseStatus, errorLog[], note, duplicate_count.

FORBIDDEN_DELIMITERS escaping: issue-payload-builder.js:51-119 has a PRIVATE escapeForbiddenDelimiters (not exported). Re-implement the same pattern for the '<report_data>'/'</report_data>' delimiters in report-fix.mjs (PATTERNS.md:398-407 says re-implement, do not couple). The pattern inserts a '-DELIMITER-ESCAPED-...' marker before the delimiter's final char.

buildReportUserTurn reference implementation: 12-RESEARCH.md lines 378-447 (field ordering, FIX-05 omission, errorLog/note conditional blocks, <matching_core_source> embed). Follow it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildReportUserTurn — escaped <report_data> envelope + FIX-05 omission + matching-core embed</name>
  <files>scripts/report-fix.mjs, tests/unit/report-fix.test.js</files>
  <read_first>
    - tests/e2e/lib/issue-payload-builder.js (lines 51-119 — escapeForbiddenDelimiters pattern + FORBIDDEN_DELIMITERS list to mirror for <report_data>)
    - src/shared/report-payload-builder.js (the KV field names + FIX-05 conditional spread for selectionText)
    - .planning/phases/12-fix-generation-regression-gate/12-RESEARCH.md (lines 378-447 — buildReportUserTurn reference; lines 240-247 — which fields are user-controlled vs safe)
    - src/shared/constants.js (REPORT_CATEGORIES — to validate category)
  </read_first>
  <behavior>
    - buildReportUserTurn output starts with '<report_data>' and ends with '</report_data>'
    - When kvRecord.selectionText is null/undefined, the output contains NO 'selectionText' line (FIX-05)
    - When kvRecord.selectionText is present, it is escaped (no raw '</report_data>' can survive in it) and truncated to 1000 chars
    - note is escaped + truncated to 256 chars; errorLog entries escaped + capped to 5×200 chars; absent note/errorLog produce no line
    - Safe enum/computed fields (category, patentNumber, patentUrl, returnedCitation, confidenceTier, pdfParseStatus, duplicate_count) appear unescaped
    - The <matching_core_source> block embeds matching.js + position-map-builder.js; it embeds pdf-parser.js only when pdfParseStatus === 'error'
    - A user field containing a literal '</report_data>' is neutralized (escaped) so it cannot break out of the envelope
  </behavior>
  <action>
    In `scripts/report-fix.mjs`, implement and export `buildReportUserTurn(kvRecord, matchingCoreSources)` per the 12-RESEARCH.md:378-447 reference. Re-implement a local `escapeForbiddenDelimiters` mirroring issue-payload-builder.js:51-119 but targeting the '<report_data>' / '</report_data>' delimiters (insert the escape marker before the delimiter's final char). Apply it to note (slice 256), selectionText (slice 1000), and each errorLog entry (slice 200, max 5). Omit selectionText entirely when absent (FIX-05). Embed matching.js + position-map-builder.js into <matching_core_source>; include pdf-parser.js only when pdfParseStatus === 'error'. The dispatcher reads these source files via fs in the orchestration entry (Task 2) and passes them as matchingCoreSources; buildReportUserTurn itself takes the strings as args (keep it testable without fs). Add `tests/unit/report-fix.test.js` covering every behavior above, including the break-out attempt where selectionText contains '</report_data>'.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/report-fix.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/report-fix.test.js` exits 0
    - Test proves FIX-05: with selectionText absent, output has no 'selectionText' occurrence
    - Test proves escaping: selectionText='x</report_data>y' yields output where the count of '</report_data>' equals 1 (only the real closing tag), i.e. the injected one is neutralized
    - Test proves <matching_core_source> includes pdf-parser source only when pdfParseStatus==='error'
    - `grep -c "selectionText" scripts/report-fix.mjs` shows selectionText handled conditionally (manual: the line push is guarded)
  </acceptance_criteria>
  <done>buildReportUserTurn produces an escaped, envelope-wrapped user turn with FIX-05 omission and conditional matching-core embed; unit-pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dispatcher orchestration — idempotency, LLM call, D-05 hard-abort sequence, FIX-04 overfit scan</name>
  <files>scripts/report-fix.mjs, tests/unit/report-fix.test.js</files>
  <read_first>
    - scripts/auto-fix.mjs (lines 62-95 imports; 1059-1099 git-apply-check + hard-abort pattern; the iteration-loop structure)
    - scripts/ingest-reports.mjs (lines 19-47 dispatcher/parseArgs structure; MAX_FIXES_PER_RUN cap pattern)
    - scripts/gh-client.mjs (makeKvReportGhClient methods; addLabel; CWE-94 execFileSync arg-array hygiene)
    - scripts/check-diff-guard.mjs (checkDiffGuard signature + FORBIDDEN_PATHS)
    - tests/e2e/lib/safe-append-ledger.js (safeAppendLedger signature + CI gate)
    - .planning/phases/12-fix-generation-regression-gate/12-PATTERNS.md (lines 29-119 — imports, safeAppendLedger source:'report-fix-api', git-apply-check, CWE-94, idempotency, MAX_FIXES patterns)
    - .planning/phases/12-fix-generation-regression-gate/12-RESEARCH.md (lines 145-167 step flow; 465-469 overfit scan)
  </read_first>
  <behavior>
    - D-06: when an open auto-fix/<fp-short> PR/branch exists and the run is not a re-trigger, the dispatcher skips before any LLM call (writes a skip ledger entry, exits early)
    - The system prompt passed to invokeAnthropicSdkWithLedger IS REPORT_FIX_SCAFFOLD; the call passes source:'report-fix-api'
    - D-05: parseFencedDiff failure → hard abort → auto-fix-stuck label on the source Issue → NO iteration consumed
    - D-05: checkDiffGuard violation (forbidden path) → hard abort → auto-fix-stuck → NO iteration consumed
    - D-05: git apply --check failure → hard abort → auto-fix-stuck → NO iteration consumed
    - COST-03: only a clean parse+guard+apply that THEN fails regression re-prompts and consumes one of the 3 iterations
    - FIX-04/D-03: when the validated diff contains patentNumber as a string literal in a src/ hunk, dispatcher returns an overfit signal (so the workflow withholds auto-fix:verified, adds human-review-required, notes the PR body) — the candidate is NOT dropped
    - COST-02: MAX_FIXES_PER_RUN (default 5) parsed as a non-negative integer; invalid value throws
    - All ledger writes use safeAppendLedger/invokeAnthropicSdkWithLedger with source:'report-fix-api'; no direct appendLedgerEntry(LEDGER_PATH
  </behavior>
  <action>
    Complete `scripts/report-fix.mjs` orchestration. Imports per PATTERNS.md:33-57 (parseArgs, fs.readFileSync for matching-core source, gh-client makeKvReportGhClient, fix-primitives parseFencedDiff/changedPathsFromDiff, REPORT_FIX_SCAFFOLD, invokeAnthropicSdkWithLedger, LEDGER_PATH, safeAppendLedger, checkDiffGuard). Read the KV record from a file path arg (the workflow writes the wrangler --remote output to a tempfile — D-01: the dispatcher does NOT call wrangler itself). Implement: (1) compute fpShort from the fingerprint; (2) D-06 idempotency check via a CWE-94-safe `gh pr list --head auto-fix/<fpShort> --state open --json number` arg-array (or a thin gh-client method) — skip with a ledger entry unless --re-trigger; (3) read matching.js + position-map-builder.js (+ pdf-parser.js if pdfParseStatus==='error') and call buildReportUserTurn; (4) the iteration loop (max 3) calling invokeAnthropicSdkWithLedger({systemPrompt:REPORT_FIX_SCAFFOLD, userPrompt, source:'report-fix-api', phase:'phase-12', issueId}); (5) per-iteration sequence parseFencedDiff → changedPathsFromDiff → checkDiffGuard → execFileSync('git',['apply','--check'],{input:diff}) with D-05 hard-abort (label auto-fix-stuck via gh addLabel, safeAppendLedger errorReason, return) on ANY of those failing; regression handling is delegated to the workflow (Plan 04 runs npm test + quarantine and re-invokes with feedback) OR, if you run regression in-dispatcher, only regression consumes an iteration; (6) FIX-04 overfit scan — after a clean apply, grep the diff's added src/ lines for the patentNumber literal and emit an overfit flag (e.g. write `overfit=true` to GITHUB_OUTPUT or stdout JSON) without aborting; (7) COST-02 MAX_FIXES_PER_RUN guard. ALL ledger writes via safeAppendLedger / the SDK driver with source:'report-fix-api'. NEVER call appendLedgerEntry(LEDGER_PATH directly. Extend `tests/unit/report-fix.test.js` with pins for: the overfit scan (positive + negative), the D-05 hard-abort returns for malformed/forbidden/unapplicable diffs (mock the gh-client + git), MAX_FIXES_PER_RUN validation, and source:'report-fix-api' on the SDK call (assert the arg).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/report-fix.test.js && grep -rn "appendLedgerEntry(LEDGER_PATH" scripts/report-fix.mjs | grep -vc "safeAppendLedger"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/report-fix.test.js` exits 0
    - `grep -c "appendLedgerEntry(LEDGER_PATH" scripts/report-fix.mjs` returns 0 (COST-01 / ledger-leak invariant — only safeAppendLedger is used)
    - `grep -c "source: 'report-fix-api'\|source:'report-fix-api'" scripts/report-fix.mjs` is >= 1 (COST-01)
    - `grep -c "checkDiffGuard" scripts/report-fix.mjs` is >= 1 (D-05 forbidden-path gate)
    - `grep -c "REPORT_FIX_SCAFFOLD" scripts/report-fix.mjs` is >= 1 (FIX-02 scaffold wired as system prompt)
    - No raw shell-string gh/git calls: `grep -nE "execSync\(|exec\(['\"]gh |exec\(['\"]git " scripts/report-fix.mjs` returns nothing (CWE-94 — execFileSync arg-arrays only)
    - Test proves D-05: a forbidden-path diff results in an addLabel('...','auto-fix-stuck') call and NO iteration increment (mocked)
    - Test proves FIX-04: a diff containing the patentNumber literal in a +src/ line emits the overfit flag
  </acceptance_criteria>
  <done>report-fix.mjs orchestrates idempotency → LLM (source:'report-fix-api') → D-05 hard-abort sequence → FIX-04 overfit signal, fully ledger-guarded; unit-pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KV record (user-authored fields) → LLM prompt | note/selectionText/errorLog are adversary-influenced; escaped + enveloped in user turn only |
| LLM output (diff) → working tree | untrusted generated diff; must pass parseFencedDiff + checkDiffGuard + git apply --check before any apply |
| dispatcher → committed ledger | all spend must be attributable via source:'report-fix-api'; no leak path |
| dispatcher → GitHub (labels/PR) | shell-injection surface; execFileSync arg-arrays only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Tampering (prompt injection) | buildReportUserTurn | mitigate | FORBIDDEN_DELIMITERS escape on note/selectionText/errorLog; <report_data> envelope; break-out test (selectionText containing '</report_data>' neutralized) |
| T-12-09 | Elevation of Privilege (scope escape) | diff-validation sequence | mitigate | checkDiffGuard against the 10-entry FORBIDDEN_PATHS bank BEFORE git apply; D-05 hard-abort on violation; pinned by report-fix.test.js |
| T-12-10 | Tampering (overfit) | FIX-04 scan | mitigate | post-apply scan for patentNumber literal in +src/ lines; D-03 soft-flag (human-review-required, withhold auto-fix:verified) — no silent overfit to the merge gate |
| T-12-11 | Repudiation / Info Disclosure (ledger leak) | report-fix.mjs ledger writes | mitigate | ALL writes via safeAppendLedger/SDK driver with source:'report-fix-api'; grep gate asserts zero direct appendLedgerEntry(LEDGER_PATH; closes project_auto_fix_ledger_leak_vector |
| T-12-12 | Injection (shell) | gh/git invocations | mitigate | execFileSync(cmd,[args]) arg-arrays everywhere; body via --body-file/stdin; grep gate forbids exec/execSync shell strings (CWE-94) |
| T-12-13 | DoS (cost exhaustion) | iteration loop | mitigate | COST-03 3-iteration cap (only regressions consume iterations, D-05); COST-02 MAX_FIXES_PER_RUN guard; monthly cap enforced inside invokeAnthropicSdkWithLedger |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No installs (RESEARCH Package Legitimacy Audit: zero new deps) |
</threat_model>

<verification>
- `grep -rn "appendLedgerEntry(LEDGER_PATH" scripts/` count remains 1 (only auto-fix.mjs's existing local guard) — report-fix.mjs adds none.
- Diff-validation sequence order is parse → changedPaths → guard → apply --check (read the source to confirm D-05 ordering).
</verification>

<success_criteria>
- report-fix.mjs is a fresh dispatcher (D-01) on shared primitives (D-02) that wires REPORT_FIX_SCAFFOLD as the system prompt and the escaped <report_data> user turn (FIX-01/FIX-03).
- FIX-04 overfit soft-flag (D-03), FIX-05 omission, D-05 hard-abort vs COST-03 iteration semantics, D-06 idempotency, COST-01 source routing all implemented + unit-pinned.
</success_criteria>

<output>
Create `.planning/phases/12-fix-generation-regression-gate/12-03-SUMMARY.md` when done.
</output>
