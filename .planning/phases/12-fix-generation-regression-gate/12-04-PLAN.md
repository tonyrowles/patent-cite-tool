---
phase: 12-fix-generation-regression-gate
plan: 04
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - .github/workflows/v61-report-fix.yml
  - tests/unit/v61-report-fix-yaml.test.js
autonomous: false
requirements: [FIX-01, GATE-01, GATE-02, GATE-03, GATE-04, COST-02, COST-03, COST-04]
must_haves:
  truths:
    - "FIX-01: v61-report-fix.yml triggers on issues:labeled, gates on report-fix-candidate, fetches the KV record via wrangler --remote from worker/, and invokes the report-fix dispatcher"
    - "GATE-01: the workflow runs golden corpus + quarantine spec in-workflow before opening a PR; regressions drive the 3-iteration re-prompt loop (D-04)"
    - "GATE-02: a passing candidate opens a DRAFT PR on auto-fix/<fp-short> via peter-evans/create-pull-request@v8; never a direct push to main for the fix branch"
    - "GATE-03: v40-verifier-gate.yml is reused as-is (NOT renamed, no new gate) — the auto-fix/* branch makes the verifier-gate required-status fire automatically"
    - "GATE-04: no auto-merge flag anywhere — the no-auto-merge static-grep Vitest test is extended to cover v61-report-fix.yml"
    - "COST-04: the ledger commit step precedes the create-pull-request step (two-commit-split ordering invariant), pinned by a YAML-contract Vitest ordering test"
    - "COST-04 DEVIATION (Critical Fork #1): the ledger is pushed to a ledger-snapshots/report-fix-<fp-short> branch, NOT direct to main, because ruleset 17086676 blocks github-actions[bot] direct-to-main pushes (verified against v40-cost-ledger-snapshot.yml S8)"
    - "COST-01: the workflow carries source:'report-fix-api' traceability and routes LLM calls through the ledger-guarded dispatcher"
  artifacts:
    - path: ".github/workflows/v61-report-fix.yml"
      provides: "KV fetch → dispatcher → in-workflow regression → ledger-branch commit → draft PR"
      contains: "report-fix-candidate"
    - path: "tests/unit/v61-report-fix-yaml.test.js"
      provides: "YAML-contract pins: trigger, --remote, COST-04 ordering, GATE-04 no-auto-merge, draft PR, source"
  key_links:
    - from: "v61-report-fix.yml"
      to: "scripts/report-fix.mjs"
      via: "node scripts/report-fix.mjs run step"
      pattern: "report-fix\\.mjs"
    - from: "ledger commit step"
      to: "create-pull-request step"
      via: "ledger step ID precedes CPR step ID (COST-04 ordering)"
      pattern: "create-pull-request@v8"
    - from: "auto-fix/<fp-short> draft PR"
      to: "v40-verifier-gate.yml"
      via: "branch prefix makes the reused gate fire (GATE-03)"
      pattern: "auto-fix/"
---

<objective>
Build `.github/workflows/v61-report-fix.yml` — the issues:labeled-triggered workflow that fetches the KV record via `wrangler --remote` (FIX-01), runs the report-fix dispatcher inside a 3-iteration loop driven by an in-workflow golden+quarantine regression run (GATE-01/D-04/COST-03), commits the ledger to a dedicated branch BEFORE creating the draft PR (COST-04 two-commit-split ordering), opens a draft PR on `auto-fix/<fp-short>` (GATE-02) that the reused `v40-verifier-gate.yml` gates automatically (GATE-03), with no auto-merge anywhere (GATE-04) — plus the `v61-report-fix-yaml.test.js` contract test.

Purpose: This is the CI orchestration that ties the dispatcher, scaffold, primitives, and regression corpora into the delivered draft PR.
Output: `v61-report-fix.yml` + `tests/unit/v61-report-fix-yaml.test.js`.

CRITICAL FORK #1 RESOLUTION — read before writing the COST-04 step:
The COST-04 requirement text and CONTEXT.md describe the ledger commit as a direct `git push origin HEAD:main` ("the ONLY permitted direct push to main"). VERIFIED during planning: ruleset 17086676 BLOCKS github-actions[bot] direct-to-main pushes. The established v40-cost-ledger-snapshot.yml already had to switch — its S8 test pins `git push origin HEAD:ledger-snapshots/daily-${SNAPSHOT_DATE}` and explicitly asserts AGAINST bare push-to-main (lines 96-103, comment lines 85-99). Per RESEARCH Pitfall 2 fallback, THIS plan uses the established ledger-branch pattern: push the ledger to `ledger-snapshots/report-fix-${FP_SHORT}` BEFORE the CPR step. The COST-04 invariant that survives is the ORDERING (ledger commit step precedes create-PR step); the "to main" wording is a DEVIATION that the human reviewer must confirm in Task 3's checkpoint. Do NOT write a push-to-main the ruleset will reject.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fix-generation-regression-gate/12-CONTEXT.md
@.planning/phases/12-fix-generation-regression-gate/12-RESEARCH.md
@.planning/phases/12-fix-generation-regression-gate/12-PATTERNS.md

<interfaces>
<!-- Exact analog contracts. -->

.github/workflows/v61-ingest-reports.yml — Phase 11: permissions, env (GH_TOKEN, CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID, ANTHROPIC_API_KEY), checkout@v5 + setup-node@v5 node 22 + npm ci, MAX_FIXES_PER_RUN.
.github/workflows/v40-cost-ledger-snapshot.yml:104-111 — the [skip ci] commit + `git push origin HEAD:ledger-snapshots/daily-${{ env.SNAPSHOT_DATE }}` pattern (the verified ledger-branch pattern to mirror).
.github/workflows/v40-verifier-gate.yml — reused AS-IS; job name `verifier-gate:` is the ruleset-17086676 required-status slot — DO NOT rename, DO NOT add a new gate. Triggers on pull_request for auto-fix/* — draft PRs fire it.
scripts/report-fix.mjs (Plan 03) — `node scripts/report-fix.mjs --kv-json <tempfile> [--re-trigger]`; emits overfit signal + diff; ledger-guarded with source:'report-fix-api'.
peter-evans/create-pull-request@v8 — draft:true, branch:auto-fix/<fp-short>, base:main.

KV fetch (MANDATORY): working-directory: worker; `wrangler kv get "$KV_KEY" --namespace-id <id> --remote`. Without --remote, reads local miniflare (false-empty []).
KV key extraction from Issue body: `<!-- kv-key: report:{fp}:{ts} -->` comment.

YAML test analogs: tests/unit/v61-ingest-reports-yaml.test.js (structure) + tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js (S14 ordering via yaml.search) + tests/e2e/scripts/v40-verifier-gate-yaml.test.js:159-166 (no-auto-merge grep).
Comment-paraphrase discipline: YAML comments must NOT literally spell 'auto-merge', 'gh pr merge --auto', '--enable-auto-merge', 'pull_request_target', 'id-token: write', 'actions: write' — paraphrase them (the no-auto-merge grep is file-text-level, not comment-filtered).
Required full pin list: 12-PATTERNS.md lines 500-524.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write v61-report-fix-yaml.test.js contract (RED) then v61-report-fix.yml (GREEN)</name>
  <files>.github/workflows/v61-report-fix.yml, tests/unit/v61-report-fix-yaml.test.js</files>
  <read_first>
    - tests/unit/v61-ingest-reports-yaml.test.js (whole file — YAML static-grep test structure, beforeAll readFileSync that throws ENOENT in RED)
    - tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js (lines 96-103 S8 ledger-branch push pin; lines 137-160 S14 ordering via yaml.search; lines 21-29 comment-paraphrase discipline)
    - tests/e2e/scripts/v40-verifier-gate-yaml.test.js (lines 159-166 — no-auto-merge grep to replicate)
    - .github/workflows/v61-ingest-reports.yml (whole file — permissions/env/checkout analog)
    - .github/workflows/v40-cost-ledger-snapshot.yml (lines 104-111 — verified ledger-branch commit+push pattern)
    - .planning/phases/12-fix-generation-regression-gate/12-PATTERNS.md (lines 284-376 workflow shape; 500-524 full pin list)
  </read_first>
  <behavior>
    - Test file exists; beforeAll reads .github/workflows/v61-report-fix.yml
    - Trigger is `issues:` `labeled` (NOT workflow_dispatch / push / pull_request / schedule)
    - `report-fix-candidate` appears in a label-gate condition
    - `--remote` appears wherever `wrangler kv` appears; the wrangler step uses working-directory: worker (or cd worker)
    - COST-04 ordering: the ledger-commit step appears BEFORE the create-pull-request step (yaml.search index comparison)
    - `[skip ci]` present in the ledger commit message
    - Ledger push targets a ledger-snapshots/report-fix-* branch, NOT bare `git push` and NOT `HEAD:main` (DEVIATION pin)
    - `peter-evans/create-pull-request@v8` present; `draft: true` present; `auto-fix/` branch prefix present
    - GATE-04: 'gh pr merge --auto', 'auto-merge: true', '--enable-auto-merge' all ABSENT
    - `report-fix-api` appears (COST-01 traceability); contents:write, pull-requests:write, issues:write present; ANTHROPIC_API_KEY + CLOUDFLARE_API_TOKEN present; node-version 22; no continue-on-error:true on gating steps
  </behavior>
  <action>
    First write `tests/unit/v61-report-fix-yaml.test.js` implementing every pin in 12-PATTERNS.md:500-524 PLUS the Critical-Fork-#1 deviation pins: assert the ledger push targets `ledger-snapshots/report-fix-` (toContain) and assert NO `git push origin HEAD:main` and NO bare `git push` (mirror v40-cost-ledger-snapshot-yaml S8 negative pins). Run it (RED — ENOENT). Then write `.github/workflows/v61-report-fix.yml` to GREEN per 12-PATTERNS.md:284-376 and 12-RESEARCH.md:537-620: trigger issues:labeled; scope-gate step on report-fix-candidate; checkout@v5 + setup-node@v5 node 22 + npm ci; KV-key extraction from issue body + `cd worker && wrangler kv get ... --remote` (working-directory: worker); COST-02 MAX_FIXES_PER_RUN env; the 3-iteration loop that runs `node scripts/report-fix.mjs --kv-json` then `npm test` (golden) + the quarantine spec (GATE-01/D-04) and re-prompts on regression; ledger commit step `git config bot identity` + `git add tests/e2e/.llm-spend-ledger.json` + `git commit -m "[skip ci] ledger: report-fix ${FP_SHORT}"` + `git push origin HEAD:ledger-snapshots/report-fix-${FP_SHORT}` (DEVIATION — NOT main) + `git checkout -- tests/e2e/.llm-spend-ledger.json` to keep the ledger out of the CPR snapshot; THEN the create-pull-request@v8 draft step on auto-fix/${FP_SHORT}. Honor comment-paraphrase discipline (do not literally spell the forbidden auto-merge tokens in comments). Do NOT add or rename any verifier-gate (GATE-03 reuse-as-is).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/v61-report-fix-yaml.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/v61-report-fix-yaml.test.js` exits 0 (all pins green)
    - `grep -c "report-fix-candidate" .github/workflows/v61-report-fix.yml` >= 1
    - `grep -c -- "--remote" .github/workflows/v61-report-fix.yml` >= 1 AND `grep -c "working-directory: worker\|cd worker" .github/workflows/v61-report-fix.yml` >= 1
    - `grep -c "ledger-snapshots/report-fix-" .github/workflows/v61-report-fix.yml` >= 1 (DEVIATION pattern)
    - `grep -c "git push origin HEAD:main" .github/workflows/v61-report-fix.yml` == 0 (ruleset-blocked path not used)
    - `grep -c "create-pull-request@v8" .github/workflows/v61-report-fix.yml` >= 1 AND `grep -c "draft: true" .github/workflows/v61-report-fix.yml` >= 1
    - `grep -cE "auto-merge: true|gh pr merge --auto|--enable-auto-merge" .github/workflows/v61-report-fix.yml` == 0 (GATE-04)
    - `grep -c "verifier-gate:" .github/workflows/v61-report-fix.yml` == 0 (GATE-03 — no new/renamed gate; reuse is implicit via branch prefix)
  </acceptance_criteria>
  <done>v61-report-fix.yml exists and passes its contract test; ledger-branch deviation + COST-04 ordering + GATE-01..04 pins all green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the GATE-04 no-auto-merge static-grep test to cover v61-report-fix.yml</name>
  <files>tests/e2e/scripts/v40-verifier-gate-yaml.test.js</files>
  <read_first>
    - tests/e2e/scripts/v40-verifier-gate-yaml.test.js (lines 159-166 — the no-auto-merge grep block; find the named test that iterates workflow YAMLs)
    - .planning/phases/12-fix-generation-regression-gate/12-CONTEXT.md (GATE-04: EXTEND the static-grep test to cover the new v61-report-fix.yml)
    - .planning/STATE.md (Permanent Invariants table — "No auto-merge of src/ fix PRs" enforcement)
  </read_first>
  <behavior>
    - The named GATE-04 no-auto-merge test now reads v61-report-fix.yml in addition to the v40 set
    - It asserts 'gh pr merge --auto', 'auto-merge: true', and '--enable-auto-merge' are ABSENT from v61-report-fix.yml
    - The existing v40 coverage remains green (no regression)
  </behavior>
  <action>
    Locate the GATE-04 no-auto-merge static-grep test (in v40-verifier-gate-yaml.test.js or wherever the cross-YAML auto-merge invariant lives — grep the repo for 'auto-merge: true' in test files to find it). Extend it to include `.github/workflows/v61-report-fix.yml` in the set of YAMLs scanned for absence of the three auto-merge tokens. If the invariant is currently a per-file test, add an equivalent assertion covering v61-report-fix.yml. Keep all existing assertions intact. (Note: Task 1 already pins no-auto-merge inside v61-report-fix-yaml.test.js; this task satisfies the CONTEXT.md mandate to extend the PERMANENT cross-workflow invariant test specifically — the milestone-level GATE-04 guard, not just the per-file one.)
  </action>
  <verify>
    <automated>npx vitest run tests/e2e/scripts/v40-verifier-gate-yaml.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/e2e/scripts/v40-verifier-gate-yaml.test.js` exits 0
    - `grep -c "v61-report-fix" tests/e2e/scripts/v40-verifier-gate-yaml.test.js` >= 1 (the new workflow is in the GATE-04 invariant's coverage) — OR, if the invariant lives elsewhere, that file references v61-report-fix.yml
    - Existing v40 no-auto-merge assertions still pass
  </acceptance_criteria>
  <done>The permanent GATE-04 cross-workflow no-auto-merge invariant test covers v61-report-fix.yml; full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Confirm COST-04 ledger-branch deviation (ruleset 17086676)</name>
  <action>Pause for human confirmation that the COST-04 ledger commit landing on `ledger-snapshots/report-fix-<fp-short>` (instead of the literal push-to-main) is acceptable, given ruleset 17086676 blocks github-actions[bot] direct-to-main pushes. The ordering invariant (ledger before create-PR) is preserved either way; only the landing branch deviates from the COST-04/CONTEXT wording.</action>
  <what-built>
    The COST-04 ledger step in v61-report-fix.yml pushes the ledger to `ledger-snapshots/report-fix-<fp-short>` BEFORE the create-PR step, instead of the literal "direct push to main" described by COST-04 and CONTEXT.md. This is a DEVIATION forced by ruleset 17086676 (verified: github-actions[bot] cannot push to main; v40-cost-ledger-snapshot.yml already uses the ledger-snapshots/* fallback and its S8 test pins against push-to-main). The COST-04 ordering invariant (ledger commit precedes create-PR) IS preserved and pinned.
  </what-built>
  <how-to-verify>
    1. Open `.github/workflows/v61-report-fix.yml` and confirm the ledger step pushes to `ledger-snapshots/report-fix-${FP_SHORT}` (not main) and appears BEFORE the create-pull-request@v8 step.
    2. Confirm you accept that the ledger lands on a ledger-snapshots/* branch (consistent with the daily snapshot pattern) rather than directly on main — the spend is still committed + auditable, just not on main until a snapshot/merge consolidates it.
    3. Confirm this matches your intent for COST-04, OR direct an alternative (e.g., a PAT-authed push-to-main actor, or a ledger PR).
  </how-to-verify>
  <resume-signal>Type "approved" to accept the ledger-snapshots/report-fix-* deviation, or describe the alternative ledger-landing strategy you want.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Issue label event → CI workflow | issues:labeled fires on any label; the workflow must gate on report-fix-candidate |
| KV store → workflow | wrangler must read PRODUCTION KV (--remote), not local miniflare |
| workflow → main branch | ruleset 17086676 governs pushes; the fix branch is never pushed to main; the ledger uses a snapshots branch |
| draft PR → merge | human approval is a permanent invariant; no auto-merge in any workflow YAML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-14 | Elevation of Privilege (unauthorized merge) | v61-report-fix.yml | mitigate | GATE-04 no-auto-merge: per-file pins (Task 1) + extended permanent cross-workflow invariant test (Task 2); draft PR requires human approval |
| T-12-15 | Info Disclosure (false-empty KV) | wrangler kv step | mitigate | --remote mandatory + working-directory: worker; Vitest pins both (Pitfall 3 / wrangler-local-default memory) |
| T-12-16 | Elevation of Privilege (scope escape via PR diff) | draft PR + verifier-gate | mitigate | GATE-03 reuses v40-verifier-gate.yml as-is (diff-size cap + forbidden-path guard + regression rerun) on the auto-fix/* branch; required-status slot job name unchanged |
| T-12-17 | DoS (cost exhaustion) | in-workflow iteration loop | mitigate | GATE-01/D-04 regression-driven loop capped at 3 (COST-03); COST-02 MAX_FIXES_PER_RUN env; dispatcher routes spend through the ledger guard |
| T-12-18 | Tampering (ledger-on-main bypass attempt) | COST-04 step | mitigate | ledger pushed to ledger-snapshots/report-fix-* (ruleset-compliant); ordering-before-CPR pinned; ledger reset out of working tree before CPR snapshot so it is NOT in the fix diff (would be a forbidden-path violation) |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new packages; only pinned actions (checkout@v5, setup-node@v5, create-pull-request@v8) already in repo (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- The reused v40-verifier-gate.yml is NOT renamed and no second gate is introduced (GATE-03): `grep -rc "verifier-gate:" .github/workflows/ ` shows the job name only in v40-verifier-gate.yml.
- Ledger is reset out of the working tree before the CPR step so it never enters the fix branch diff.
</verification>

<success_criteria>
- v61-report-fix.yml: issues:labeled → report-fix-candidate gate → wrangler --remote KV fetch → dispatcher in a 3-iteration regression-driven loop → ledger-snapshots/report-fix-* commit (COST-04 ordering preserved, deviation human-confirmed) → draft PR on auto-fix/<fp-short> → reused verifier-gate fires (GATE-03) → no auto-merge (GATE-04).
- YAML-contract + extended cross-workflow GATE-04 tests green; COST-04 ordering + ledger-branch deviation pinned.
</success_criteria>

<output>
Create `.planning/phases/12-fix-generation-regression-gate/12-04-SUMMARY.md` when done.
</output>
