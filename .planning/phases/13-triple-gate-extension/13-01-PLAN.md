---
phase: 13-triple-gate-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/auto-fix-promote.mjs
  - tests/unit/auto-fix-promote-gate.test.js
  - .github/workflows/v61-report-fix.yml
  - tests/unit/v61-report-fix-yaml.test.js
autonomous: true
requirements: [GATE-05]
must_haves:
  truths:
    - "GATE-05 / D-02: assertTripleGate Leg 3 accepts sourceIssueLabels containing 'report-fix-candidate' (new T7 case does not throw) — maps to ROADMAP success criterion #1"
    - "GATE-05 / D-02: the legacy 'triage' path still passes (T4 green) and 'auto-fix:partial-verified' is still rejected (T5 green) — flat OR adds a label, never narrows; maps to ROADMAP success criterion #3"
    - "GATE-05: the assertTripleGate body edit and its PROMOTE-04 EXPECTED_BODY pin update land in ONE commit, with the pin test green afterward (no window where the gate is unenforced) — maps to ROADMAP success criterion #2"
    - "GATE-05 / D-04: the v61-report-fix.yml PR body emits '<!-- source_issue: ${{ github.event.issue.number }} -->' so parseSourceIssue resolves a v6.1 source issue via its PREFERRED path (parser at auto-fix-promote.mjs:270 unmodified)"
    - "GATE-05 / D-03: hard-fail preserved — an unresolvable source issue still throws TRIPLE_GATE_FAILED (no graceful-skip path added)"
    - "GATE-05 / D-01: v40-auto-promote.yml trigger YAML is untouched (no pull_request:closed restored; 'fires after merge' is operator dispatch); D-05 gh issue close interlock untouched"
    - "GATE-05: _skipCiGuard:true non-comment grep-count stays exactly 1 (trust invariant unperturbed by Leg-3 edit)"
  artifacts:
    - path: "scripts/auto-fix-promote.mjs"
      provides: "assertTripleGate Leg 3 widened to flat OR (triage OR report-fix-candidate)"
      contains: "report-fix-candidate"
    - path: "tests/unit/auto-fix-promote-gate.test.js"
      provides: "PROMOTE-04 EXPECTED_BODY pin updated to new Leg 3 + new T7 accepts-report-fix-candidate case"
      contains: "report-fix-candidate"
    - path: ".github/workflows/v61-report-fix.yml"
      provides: "source_issue HTML-comment marker in create-pull-request body block"
      contains: "<!-- source_issue:"
    - path: "tests/unit/v61-report-fix-yaml.test.js"
      provides: "YAML-contract assertion pinning the source_issue marker presence"
      contains: "source_issue"
  key_links:
    - from: ".github/workflows/v61-report-fix.yml"
      to: "scripts/auto-fix-promote.mjs parseSourceIssue"
      via: "<!-- source_issue: N --> marker in PR body (PREFERRED parse path)"
      pattern: "<!--\\s*source_issue:"
    - from: "scripts/auto-fix-promote.mjs assertTripleGate Leg 3"
      to: "tests/unit/auto-fix-promote-gate.test.js EXPECTED_BODY"
      via: "byte-unchanged body pin (same-commit invariant)"
      pattern: "report-fix-candidate"
---

<objective>
Deliver GATE-05: widen `assertTripleGate` Leg 3 so a merged v6.1 report-fix PR (sourced from a `report-fix-candidate` Issue) can drive the existing merge → quarantine → golden-corpus promote cycle, without regressing the legacy `triage` path and without eroding the `_skipCiGuard:true` trust invariant.

Three coordinated, RESEARCH-verified edits:
1. `assertTripleGate` Leg 3 (`scripts/auto-fix-promote.mjs:125-128`) → flat OR accepting `report-fix-candidate` alongside `triage` (D-02).
2. The PROMOTE-04 byte-unchanged body pin (`tests/unit/auto-fix-promote-gate.test.js:512`) `EXPECTED_BODY` updated in the SAME COMMIT as edit #1 (success criterion #2), plus a new T7 trust-boundary case.
3. The `<!-- source_issue: ${{ github.event.issue.number }} -->` marker added to the `v61-report-fix.yml` PR body (D-04) so the unchanged `parseSourceIssue` resolves the source issue; pinned by a new YAML-contract assertion.

Purpose: close the human-merge → golden-corpus promotion loop for v6.1-sourced fixes while keeping every trust pin green.
Output: 4 files edited across 2 commits (gate-body+pin together; YAML marker+assertion separately).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-triple-gate-extension/13-CONTEXT.md
@.planning/phases/13-triple-gate-extension/13-RESEARCH.md

<interfaces>
<!-- Contracts the executor needs. Verified against live code 2026-06-17. Use directly — no codebase exploration needed. -->

assertTripleGate current Leg 3 (scripts/auto-fix-promote.mjs:125-128, VERIFIED):
  // Leg 3 — source-issue carries triage (Phase 34 triage-classifier verdict).
  if (!Array.isArray(sourceIssueLabels) || !sourceIssueLabels.includes('triage')) {
    throw new Error("TRIPLE_GATE_FAILED: sourceIssueLabels — source issue missing 'triage'");
  }

De Morgan target: !(A || B) === !A && !B. The widened if-condition is ONE line (longer, still one line). Function body stays 15 lines — the PROMOTE-04 slice(startIdx, startIdx + 15) constant does NOT change.

PROMOTE-04 pin mechanic (tests/unit/auto-fix-promote-gate.test.js:512-538, VERIFIED):
  dynamic anchor findIndex((l) => l.startsWith('export function assertTripleGate')),
  then slice(startIdx, startIdx + 15).join('\n') compared to EXPECTED_BODY (an array .join('\n')).
  Only EXPECTED_BODY array positions 10, 11, 12 (0-indexed: the comment, the if-line, the throw-line) change.
  Positions 0-9 and 13-14 are UNCHANGED. Match single-quote/double-quote string-literal style exactly.

T-cases (describe 'assertTripleGate (Phase 44)', lines 70-132). T7 inserts after T6 (line 130), before the block's closing `});` (line 132). Existing cases that MUST stay green:
  T3 (line 88, sourceIssueLabels:['bug']) toThrow(/...missing 'triage'/) — substring regex, survives the longer message.
  T4 (line 96, sourceIssueLabels:['triage','WRONG_CITATION']) not.toThrow — legacy path.
  T5 (line 111, prLabels:['auto-fix:partial-verified']) toThrow — Leg 1, unrelated to Leg 3.

v61-report-fix.yml body block (lines 293-308, VERIFIED). Insert the marker on a new line right after the existing `**Source Issue:** #${{ github.event.issue.number }}` line (296); the prose line stays. github.event.issue.number is the correct, sole issue-number expression (trigger is issues:labeled).

parseSourceIssue regex (auto-fix-promote.mjs:270, DO NOT MODIFY): /<!--\s*source_issue:\s*(\d+)\s*-->/ (PREFERRED) with a `Fix #N` fallback.

v61-report-fix-yaml.test.js idiom: `yaml` loaded at module scope via fs.readFileSync; assertions are `expect(yaml).toContain(literal)`. New assertion goes inside describe('v61-report-fix.yml static guards (Plan 12-04)', ...) (line 43) before its closing `});` (line 260). Existing analog at line 68-69 pins 'report-fix-candidate'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen assertTripleGate Leg 3 + update PROMOTE-04 pin + add T7 (ONE commit)</name>
  <files>scripts/auto-fix-promote.mjs, tests/unit/auto-fix-promote-gate.test.js</files>
  <read_first>
    - scripts/auto-fix-promote.mjs:115-129 (assertTripleGate body — the edit site)
    - tests/unit/auto-fix-promote-gate.test.js:512-538 (PROMOTE-04 EXPECTED_BODY pin — pinned to the body above)
    - tests/unit/auto-fix-promote-gate.test.js:70-132 (the assertTripleGate Phase-44 describe block — T1..T6; T7 inserts after T6 at line 130)
    - tests/unit/auto-fix-promote-gate.test.js:553-561 (_skipCiGuard grep-count==1 invariant — must stay green)
    - .planning/phases/13-triple-gate-extension/13-RESEARCH.md "Edit Site 1", "Edit Site 2", "New Test Case: T7" sections
  </read_first>
  <behavior>
    - T7 (new): assertTripleGate({ prLabels:['auto-fix:verified'], merged:true, sourceIssueLabels:['report-fix-candidate'] }) does NOT throw.
    - T4 (existing): sourceIssueLabels:['triage','WRONG_CITATION'] still does NOT throw (legacy path preserved).
    - T3 (existing): sourceIssueLabels:['bug'] still throws, matched by /...missing 'triage'/ (substring regex survives the extended message).
    - T5 (existing): prLabels:['auto-fix:partial-verified'] still throws on Leg 1.
    - PROMOTE-04 (existing, updated): the live body slice equals the updated EXPECTED_BODY exactly.
    - _skipCiGuard:true non-comment grep-count stays exactly 1.
  </behavior>
  <action>
    In `scripts/auto-fix-promote.mjs`, edit ONLY the `assertTripleGate` Leg 3 block (lines 125-128). Do NOT touch Legs 1-2, `assertPartialGate` (line ~172), `parseSourceIssue` (line 270), `runPromote`, or the single `_skipCiGuard: true` occurrence.
    - Change the if-condition to the flat OR via De Morgan: `if (!Array.isArray(sourceIssueLabels) || (!sourceIssueLabels.includes('triage') && !sourceIssueLabels.includes('report-fix-candidate'))) {`.
    - Extend the throw message string to `TRIPLE_GATE_FAILED: sourceIssueLabels — source issue missing 'triage' or 'report-fix-candidate'` (the legacy substring `missing 'triage'` MUST remain a prefix so T3's regex still matches).
    - Update the Leg-3 comment (line 125) from "carries triage (Phase 34 triage-classifier verdict)" to reflect both labels are now accepted, e.g. "source-issue carries triage or report-fix-candidate".
    - Keep the function body at exactly 15 lines (the if stays ONE line) so the PROMOTE-04 slice constant 15 is unchanged.
    In `tests/unit/auto-fix-promote-gate.test.js`:
    - Update `EXPECTED_BODY` (lines 520-536) array positions 10, 11, 12 (0-indexed: the Leg-3 comment, the if-line, the throw-line) to match the edited body byte-for-byte. Positions 0-9 and 13-14 stay UNCHANGED. Preserve the exact single-quote vs double-quote string-literal style and escaped inner quotes used by the surrounding array entries. Do NOT change the slice constant (`startIdx + 15`).
    - Add `T7` inside `describe('assertTripleGate (Phase 44)')` immediately after T6 (after line 130, before the closing `});` on line 132): an `it(...)` asserting that `assertTripleGate({ prLabels:['auto-fix:verified'], merged:true, sourceIssueLabels:['report-fix-candidate'] })` does NOT throw. Reference GATE-05 / Phase 13 / Leg-3 OR widening in the test title.
    - Do NOT modify T1-T6 (they stay green unchanged).
    Both file edits land in the SAME commit (success criterion #2 — no window where the gate is unenforced).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/auto-fix-promote-gate.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/auto-fix-promote.mjs` contains `includes('report-fix-candidate')` inside the assertTripleGate Leg-3 if-condition (flat OR), and the throw message contains `missing 'triage' or 'report-fix-candidate'`.
    - `grep -n "export function assertPartialGate" -A20 scripts/auto-fix-promote.mjs` shows assertPartialGate Leg 3 STILL requires only `'triage'` (unmodified).
    - `node -e "const l=require('fs').readFileSync('scripts/auto-fix-promote.mjs','utf8').split(/\r?\n/); const i=l.findIndex(x=>x.startsWith('export function assertTripleGate')); console.log(l.slice(i,i+15).length===15)"` prints `true` (body still 15 lines).
    - `grep -c "_skipCiGuard:\s*true" scripts/auto-fix-promote.mjs` via the test's non-comment filter == 1 (the grep-count invariant test passes).
    - `npx vitest run tests/unit/auto-fix-promote-gate.test.js` exits 0 with: PROMOTE-04 green, T7 green (report-fix-candidate accepts), T4 green (triage still passes), T3 green (substring regex survives), T5 green (partial-verified still rejected).
    - The gate-body edit in `auto-fix-promote.mjs` and the EXPECTED_BODY pin update in the test file appear in the SAME git commit.
  </acceptance_criteria>
  <done>The OR-widened gate accepts report-fix-candidate AND triage, still rejects partial-verified, the body pin is byte-consistent and updated in the same commit, and the _skipCiGuard count stays 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add source_issue marker to v61 PR body + YAML-contract assertion (separate commit)</name>
  <files>.github/workflows/v61-report-fix.yml, tests/unit/v61-report-fix-yaml.test.js</files>
  <read_first>
    - .github/workflows/v61-report-fix.yml:293-308 (the create-pull-request body block — the edit site)
    - scripts/auto-fix-promote.mjs:270 (parseSourceIssue — the PREFERRED /<!--\s*source_issue:\s*(\d+)\s*-->/ regex this marker feeds; DO NOT MODIFY)
    - tests/unit/v61-report-fix-yaml.test.js:43-69 (describe block + the existing report-fix-candidate toContain idiom at lines 68-69)
    - .planning/phases/13-triple-gate-extension/13-RESEARCH.md "Edit Site 3", "Edit Site 4" sections
  </read_first>
  <behavior>
    - The v61-report-fix.yml PR body contains the literal token `<!-- source_issue:` and the `${{ github.event.issue.number }}` expression, so parseSourceIssue's PREFERRED regex resolves the source issue for a v6.1 PR.
    - The existing `**Source Issue:** #${{ github.event.issue.number }}` prose line is retained for human readability.
    - A YAML-contract test fails if a future commit drops the marker.
  </behavior>
  <action>
    In `.github/workflows/v61-report-fix.yml`, inside the `create-pull-request` `body: |` block, add a new line immediately after the existing `**Source Issue:** #${{ github.event.issue.number }}` line (line 296) containing exactly: `<!-- source_issue: ${{ github.event.issue.number }} -->`. Keep the same body-block indentation as the surrounding lines. Do NOT remove or alter the existing prose `**Source Issue:**` line, and do NOT touch any other part of the workflow (trigger block, gh issue close, commit-message). `parseSourceIssue` is NOT modified — this marker feeds its existing PREFERRED path.
    In `tests/unit/v61-report-fix-yaml.test.js`, add a new `it(...)` inside `describe('v61-report-fix.yml static guards (Plan 12-04)')` (before its closing `});` at line 260), following the existing `expect(yaml).toContain(...)` idiom (analog at lines 68-69). Assert the body contains `<!-- source_issue:` and `${{ github.event.issue.number }}`. Reference D-04 / Phase 13 / parseSourceIssue PREFERRED path in the test title.
    Commit this separately from Task 1 (D-04 marker + its YAML test in their own commit).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/v61-report-fix-yaml.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<!-- source_issue: \${{ github.event.issue.number }} -->" .github/workflows/v61-report-fix.yml` returns 1.
    - The original `**Source Issue:** #${{ github.event.issue.number }}` prose line is still present (`grep -c "\*\*Source Issue:\*\*" .github/workflows/v61-report-fix.yml` returns 1).
    - `node -e "const y=require('fs').readFileSync('.github/workflows/v61-report-fix.yml','utf8'); console.log(/<!--\s*source_issue:\s*\\\${{ github.event.issue.number }}\s*-->/.test(y))"` prints `true` (marker matches the parseSourceIssue regex shape).
    - The v40-auto-promote.yml trigger YAML is unchanged: `git diff --name-only` for this task does NOT list `.github/workflows/v40-auto-promote.yml`, and `scripts/auto-fix-promote.mjs` parseSourceIssue is unchanged (not in this commit's diff).
    - `npx vitest run tests/unit/v61-report-fix-yaml.test.js` exits 0 with the new marker assertion green and all prior assertions green.
    - This change is in a SEPARATE commit from Task 1.
  </acceptance_criteria>
  <done>The v61 PR body emits the source_issue marker (prose line retained), parseSourceIssue can resolve a v6.1 source issue via its unchanged PREFERRED path, and a YAML-contract test pins the marker — committed separately from the gate-body change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source Issue labels → assertTripleGate Leg 3 | An Issue's labels decide whether a merged PR may drive golden-corpus promotion. Widening which labels are accepted is a trust-surface change. |
| v61 PR body → parseSourceIssue | The PR body string is parsed to identify the source issue; a missing/malformed marker must fail loud, not silently promote. |
| auto-fix-promote.mjs body ↔ PROMOTE-04 pin | The gate body must never be silently rewritten; the byte-pin is the tamper-evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Elevation of Privilege | assertTripleGate Leg 3 OR widening | mitigate | The widening only ADDS `report-fix-candidate` (no narrowing, no bypass). The label is a vetted Phase-11 signal (green-tier / dup≥3 / quarantine corpus hit) carrying equivalent trust to `triage`. Promotion still requires Leg 1 (`auto-fix:verified`) + Leg 2 (`merged===true`) + a human merge (GATE-04). T7 + retained T4/T5 pin the boundary. |
| T-13-02 | Spoofing | mislabeled/attacker-controlled `report-fix-candidate` Issue driving unwarranted promotion | accept | `report-fix-candidate` is applied only by the Phase-11 triage heuristics on auto-promote signals; the repo's Issue-label write surface is maintainer-controlled. Residual risk is bounded by the mandatory human merge (GATE-04) before any promote fires. Accepted: no PII, promotion is human-gated and reversible via golden-corpus review. |
| T-13-03 | Denial of Service / Tampering | unresolvable or deleted source issue | mitigate | D-03 hard-fail preserved — `parseSourceIssue` throws `TRIPLE_GATE_FAILED: cannot identify source issue` and Leg 3 throws on missing label; the golden corpus is never mutated on an unverifiable signal. No graceful-skip path added. |
| T-13-04 | Tampering | silent rewrite of the gate body / erosion of `_skipCiGuard:true` invariant | mitigate | PROMOTE-04 byte pin updated in the SAME commit as the body (atomic, no unenforced window) and the `_skipCiGuard:true` non-comment grep-count==1 test both stay green; T-13 acceptance criteria assert both. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (code/config/test edits only; RESEARCH §Environment Availability confirms no new tooling). No legitimacy checkpoint required. |
</threat_model>

<verification>
Run the affected suites:
- `npx vitest run tests/unit/auto-fix-promote-gate.test.js tests/unit/v61-report-fix-yaml.test.js` — PROMOTE-04, T3, T4, T5, T7, the `_skipCiGuard` grep-count invariant, and the new source_issue marker assertion all green.
- Full gate before close: `npm test` (build + vitest + chrome + firefox + lint + web-ext lint) exits 0. Note: no manifest changed, so the separate `npm run test:lint` path is unaffected.
- Trust-invariant spot checks: assertPartialGate Leg 3 still requires only `triage`; v40-auto-promote.yml trigger YAML untouched; parseSourceIssue unmodified.
</verification>

<success_criteria>
- assertTripleGate Leg 3 accepts `report-fix-candidate` OR `triage` (ROADMAP #1, #3), partial-verified still rejected.
- The gate body edit and PROMOTE-04 EXPECTED_BODY pin update are in ONE commit; pin test green on the branch (ROADMAP #2 — no unenforced window).
- The v61 PR body emits `<!-- source_issue: ${{ github.event.issue.number }} -->`; parseSourceIssue (unmodified) resolves the source issue (D-04); pinned by a YAML-contract assertion.
- D-01 honored (v40-auto-promote.yml trigger YAML untouched), D-03 hard-fail preserved, D-05 close interlock untouched, `_skipCiGuard:true` grep-count stays 1.
- `npx vitest run tests/unit/auto-fix-promote-gate.test.js tests/unit/v61-report-fix-yaml.test.js` and `npm test` exit 0.
</success_criteria>

<output>
Create `.planning/phases/13-triple-gate-extension/13-01-SUMMARY.md` when done.
</output>
