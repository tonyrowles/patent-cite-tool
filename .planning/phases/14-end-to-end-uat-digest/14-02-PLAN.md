---
phase: 14-end-to-end-uat-digest
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/14-end-to-end-uat-digest/14-HUMAN-UAT.md
autonomous: true
requirements: [UAT-01, UAT-02, UAT-03]
must_haves:
  truths:
    - "ONE consolidated operator-dispatchable 14-HUMAN-UAT.md runbook exists, mirroring 12-HUMAN-UAT.md structure (D-09)"
    - "The runbook folds in the 5 deferred live-CI behaviors from 12-HUMAN-UAT.md (D-09)"
    - "UAT-01 covers the full live chain: seeded broken-patent report via real POST /report → triage → promote → LLM fix → verifier-gate → human merge → operator-dispatched v40-auto-promote → Issue close + ledger entries (ROADMAP success criterion 1)"
    - "UAT-02 exercises ingest-reports.mjs promote <fp> <ts> live on a NOT-auto-promoted/ambiguous seed, proving the escape hatch bypasses the auto-promote status filter (D-08, ROADMAP success criterion 2)"
    - "UAT-03 documents the LIVE half: monthly cap enforced across real Actions invocations (live merge-driven; the local npm-test + ledger-cap half is verified in Plan 01)"
    - "The runbook documents a revert plan for the main + golden-corpus mutation the live merge produces (D-06)"
    - "The runbook includes a pre-LLM spend-confirmation step (check monthly ledger headroom before the LLM call) (D-06)"
    - "The runbook calls out that the one-time seed is a TEST FIXTURE, not a revival of the retired v4.3 synthetic-injection architecture (D-07)"
  artifacts:
    - path: ".planning/phases/14-end-to-end-uat-digest/14-HUMAN-UAT.md"
      provides: "consolidated v6.1 live-validation operator runbook"
      contains: "## Revert Plan"
  key_links:
    - from: "14-HUMAN-UAT.md UAT-01 test block"
      to: "POST /report → BUG_REPORTS KV → ingest-reports.mjs → report-fix-candidate Issue → v61-report-fix.yml → v40-verifier-gate.yml → human merge → v40-auto-promote.yml"
      via: "operator-dispatched live chain"
      pattern: "v40-auto-promote"
    - from: "14-HUMAN-UAT.md UAT-02 test block"
      to: "ingest-reports.mjs promote <fp> <ts>"
      via: "manual-promote escape hatch on a non-auto-promoted seed"
      pattern: "ingest-reports.mjs promote"
---

<objective>
Author the single consolidated `14-HUMAN-UAT.md` operator runbook (D-09) covering UAT-01, UAT-02, and the live half of UAT-03, plus the 5 deferred live-CI behaviors from `12-HUMAN-UAT.md`.

Purpose: One operator-dispatchable document to run the entire v6.1 live validation chain against production, with the D-06 revert plan + spend-confirmation guardrails and the D-07 test-fixture distinction documented so the milestone has one place to prove the pipeline works end-to-end.
Output: `.planning/phases/14-end-to-end-uat-digest/14-HUMAN-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-end-to-end-uat-digest/14-CONTEXT.md
@.planning/phases/14-end-to-end-uat-digest/14-PATTERNS.md
@.planning/phases/12-fix-generation-regression-gate/12-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the consolidated 14-HUMAN-UAT.md runbook (UAT-01/02/03 + 5 deferred behaviors)</name>
  <files>.planning/phases/14-end-to-end-uat-digest/14-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/12-fix-generation-regression-gate/12-HUMAN-UAT.md (FULL — front-matter shape lines 1-13, the 5 deferred test blocks lines 15-33, Summary block lines 35-43; this is the structural analog AND the source of the 5 folded behaviors)
    - .planning/phases/14-end-to-end-uat-digest/14-CONTEXT.md (D-06 revert-plan + spend-confirmation guardrails; D-07 test-fixture distinction; D-08 manual-promote; the canonical_refs pipeline file list)
    - .planning/phases/14-end-to-end-uat-digest/14-PATTERNS.md ("14-HUMAN-UAT.md" section: front-matter + test-block style + "Content to fold in"; "Ledger cap-headroom check" shared pattern with the monthlyTotal/fs.existsSync gotcha)
    - scripts/ingest-reports.mjs (lines 1-103: usage banner + parseArgs — the `promote <fp> <ts>` escape-hatch subcommand for UAT-02)
  </read_first>
  <action>
    Create `.planning/phases/14-end-to-end-uat-digest/14-HUMAN-UAT.md` mirroring `12-HUMAN-UAT.md` EXACTLY: the same front-matter (`status: partial`, `phase: 14-end-to-end-uat-digest`, `source: [14-VERIFICATION.md]`, `started`/`updated` ISO), a `## Current Test` block, a `## Tests` section of `### N. <title> (<req-ids>)` headings each with one `expected:` operator-observable paragraph and a `result: [pending]` line, a `## Summary` block, and a `## Gaps` section. Fold in ALL 5 deferred live-CI behaviors verbatim-in-spirit from 12-HUMAN-UAT.md tests 1-5 (Issue-label→draft-PR via v61-report-fix.yml with `wrangler --remote` KV fetch; overfit soft-flag → `human-review-required`; 3-iteration exhaustion → `auto-fix-stuck`; D-06 GitHub-authoritative idempotency; verifier-gate required-status binding via ruleset 17086676). Then add: UAT-01 — the full live chain (seed a deliberately-citation-failing report for a THROWAWAY/test patent through the genuine production `POST /report` intake → `BUG_REPORTS` KV → `ingest-reports.mjs` triage → `report-fix-candidate` Issue → `v61-report-fix.yml` LLM fix → draft PR with `<!-- source_issue: N -->` marker → `v40-verifier-gate.yml` passes → human merge → operator-dispatched `v40-auto-promote.yml` → Issue close + ledger entries with `source:'report-fix-api'`); UAT-02 — exercise `node scripts/ingest-reports.mjs promote <fp> <ts>` LIVE on a NOT-auto-promoted/`ambiguous`/below-threshold seed, proving it bypasses the auto-promote status filter and forces the full pipeline (D-08); UAT-03 (live half ONLY) — monthly cap enforced across real Actions invocations (note that `npm test` golden-100% + the ledger-cap assertion are verified in-session by Plan 01, NOT here). Add a `## Revert Plan` section (D-06) documenting how to revert the `main` + golden-corpus mutation the live merge produces, a `## Spend Confirmation` (pre-LLM) step checking monthly ledger headroom (the `fs.existsSync(LEDGER_PATH)`-before-`monthlyTotal` gotcha; NEVER set `E2E_LEDGER_PATH_OVERRIDE`), and a `## Test Fixture, Not Synthetic Revival` note (D-07) clarifying the one-time seed is a test fixture, not a reintroduction of the retired v4.3 synthetic-injection architecture. Update the `## Summary` `total:`/`pending:` counts to match the number of test blocks authored.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-end-to-end-uat-digest/14-HUMAN-UAT.md && grep -q "## Revert Plan" .planning/phases/14-end-to-end-uat-digest/14-HUMAN-UAT.md && grep -q "ingest-reports.mjs promote" .planning/phases/14-end-to-end-uat-digest/14-HUMAN-UAT.md && grep -q "v40-auto-promote" .planning/phases/14-end-to-end-uat-digest/14-HUMAN-UAT.md && echo "runbook OK"</automated>
  </verify>
  <acceptance_criteria>
    - 14-HUMAN-UAT.md exists with front-matter `phase: 14-end-to-end-uat-digest` and `status: partial`
    - Contains UAT-01, UAT-02, UAT-03 test blocks tagged with those req-ids (`grep -E "UAT-0[123]" 14-HUMAN-UAT.md` matches all three)
    - Contains all 5 deferred behaviors from 12-HUMAN-UAT.md (Issue-label→draft-PR, overfit soft-flag, 3-iteration exhaustion, idempotency, verifier-gate binding) — `grep -c "result: \[pending\]"` >= 8 (5 deferred + 3 UAT)
    - Contains `## Revert Plan` documenting the main + golden-corpus mutation revert (D-06)
    - Contains a spend-confirmation step referencing the monthly ledger headroom check before the LLM call (D-06)
    - Contains the D-07 test-fixture-not-synthetic-revival note
    - UAT-02 block references `ingest-reports.mjs promote <fp> <ts>` on a non-auto-promoted seed (D-08)
    - UAT-01 block references the operator-dispatched `v40-auto-promote.yml` post-merge step and ledger entries (success criterion 1)
    - The `## Summary` `total:` count equals the number of `### N.` test blocks
  </acceptance_criteria>
  <done>14-HUMAN-UAT.md authored: 5 folded behaviors + UAT-01/02/03 live, with revert plan, spend-confirmation, and D-07 fixture note; summary counts consistent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → production POST /report intake | The live UAT seeds a synthetic report through the real public intake path |
| Live LLM fix call → monthly ledger / spend | Real Anthropic spend is incurred during the live UAT chain |
| Auto-fix PR → main + golden corpus | A merged fix mutates main and the golden baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Tampering | Seeded report into production POST /report | mitigate | The seed MUST be a deliberately throwaway/test patent, unmistakably a test record; the runbook documents a `## Revert Plan` for the main + golden-corpus mutation (D-06). Acceptance criterion requires the revert plan section. |
| T-14-06 | Repudiation/Spend | Live LLM fix call against the monthly cap | mitigate | The runbook includes a pre-LLM spend-confirmation step checking monthly ledger headroom (`fs.existsSync(LEDGER_PATH)` before `monthlyTotal`, never `E2E_LEDGER_PATH_OVERRIDE`); UAT-03 confirms the cap is enforced across real Actions invocations (D-06). |
| T-14-07 | Elevation of Privilege | Misreading the seed as autonomous-machinery revival | mitigate | The runbook explicitly documents (D-07) that the one-time seed is a TEST FIXTURE, not a revival of the retired v4.3 synthetic-injection architecture — preserving the "human bug reports only" signal model. |
| T-14-08 | Spoofing/Bypass | Human merge gate on src/ fix PRs | accept | The human merge gate is a permanent invariant (no auto-merge); the runbook's UAT-01 step is operator-driven (D-08). No automation bypasses the gate. |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No package installs — this plan authors a Markdown runbook only. |
</threat_model>

<verification>
- `test -f .planning/phases/14-end-to-end-uat-digest/14-HUMAN-UAT.md` succeeds
- The runbook contains `## Revert Plan`, a spend-confirmation step, and the D-07 fixture note
- `grep -c "result: \[pending\]" 14-HUMAN-UAT.md` >= 8 (5 deferred + 3 UAT)
- UAT-01/02/03 req-ids present; UAT-02 references `ingest-reports.mjs promote`; UAT-01 references `v40-auto-promote.yml`
</verification>

<success_criteria>
- UAT-01: live full-chain test block authored (success criterion 1)
- UAT-02: manual-promote escape-hatch test block authored (success criterion 2)
- UAT-03 live half: cap-enforcement-across-Actions test block authored (live half of success criterion 3; local half in Plan 01)
- D-06 revert-plan + spend-confirmation + D-07 fixture-distinction guardrails all present
- ONE consolidated operator-dispatchable runbook folding the 5 deferred 12-HUMAN-UAT.md behaviors (D-09)
</success_criteria>

<output>
Create `.planning/phases/14-end-to-end-uat-digest/14-02-SUMMARY.md` when done
</output>
