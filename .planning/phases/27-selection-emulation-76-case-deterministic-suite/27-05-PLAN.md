---
phase: 27-selection-emulation-76-case-deterministic-suite
plan: 05
type: execute
wave: 3
depends_on: ["27-03", "27-04"]
files_modified: []
autonomous: false
requirements: [SEL-03, DIAG-01, DIAG-02]
tags: [e2e, verification, regression-run, silent-run, smoke-run, run-and-prove]

must_haves:
  truths:
    - "`npm run e2e:regression` exits 0 against a freshly-built `dist/chrome/` — all 76 cases pass sync-mode round-trip vs `tests/golden/baseline.json`."
    - "`npm run e2e:silent` exits 0 — both silent-mode cases on US11427642 read the expected citation from `window.__lastCopiedText__`."
    - "`npm run e2e:smoke` exits 0 — the Phase 26 infra smoke + the 5 Phase 27 @smoke-tagged regression cases all pass (~30-45s wall-clock)."
    - "On at least one intentional failure (operator-triggered, manually), `tests/e2e/artifacts/{runId}/{caseId}-screenshot.png` and `{caseId}-dom.html` are present and inspectable — proves the DIAG-01 + DIAG-02 catch-block wiring works in practice."
    - "Phase 27 SUMMARY documents pass/fail counts, total wall-clock, any per-case flake observations, and any tuning recommendations for Phase 29's cron."
  artifacts:
    - path: .planning/phases/27-selection-emulation-76-case-deterministic-suite/27-05-SUMMARY.md
      provides: "Phase 27 execution evidence — pass counts, runtimes, artifact verification"
      contains: "76"
    - path: tests/e2e/artifacts/{runId}/
      provides: "On-disk evidence directory (gitignored) from the regression run; contents inspected for the manual-failure DIAG verification"
  key_links:
    - from: "Plan 27-05"
      to: "Plan 27-03 (regression.spec.js) + Plan 27-04 (silent.spec.js + e2e:* scripts)"
      via: "npm run e2e:regression, npm run e2e:silent, npm run e2e:smoke"
      pattern: "npm run e2e:"
---

<objective>
This plan is "run the suite and prove it works." No new code or lib changes — Plans 01-04 already shipped every primitive, spec, and npm script. This plan executes the three e2e commands, captures the outcomes, and documents the result. It also runs the **manual artifact verification** (per VALIDATION.md "Manual-Only Verifications") to confirm DIAG-01 + DIAG-02 actually produce useful failure artifacts.

Purpose: end-to-end proof that SEL-03 (76-case replay), HARN-04 (silent clipboard end-to-end), and DIAG-01/DIAG-02 (artifacts on failure) all work against a real Google Patents page. Plan 05 is what flips Phase 27 from "code is written" to "code is shipped and proven".

Output:
- `.planning/phases/27-selection-emulation-76-case-deterministic-suite/27-05-SUMMARY.md` — execution evidence, pass counts, runtimes, flake list, recommendations.
- Phase 27 final phase-level success: Plan 05's SUMMARY closes the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-selection-emulation-76-case-deterministic-suite/27-CONTEXT.md
@.planning/phases/27-selection-emulation-76-case-deterministic-suite/27-RESEARCH.md
@.planning/phases/27-selection-emulation-76-case-deterministic-suite/27-VALIDATION.md
@.planning/phases/27-selection-emulation-76-case-deterministic-suite/27-01-SUMMARY.md
@.planning/phases/27-selection-emulation-76-case-deterministic-suite/27-02-SUMMARY.md
@.planning/phases/27-selection-emulation-76-case-deterministic-suite/27-03-SUMMARY.md
@.planning/phases/27-selection-emulation-76-case-deterministic-suite/27-04-SUMMARY.md

<interfaces>
Plan 05 has no consumed interfaces — it RUNS the prior plans' outputs.

Commands to run (all from `package.json`):
```bash
npm run e2e:smoke      # ~30-45s; 1 Phase 26 smoke + 5 Phase 27 @smoke-tagged regression cases
npm run e2e:silent     # ~10-15s; 2 cases on US11427642
npm run e2e:regression # ~10-15 min; all 76 cases
```

All three chain `build:chrome` first via the npm script (no manual `npm run build:chrome` needed beforehand, but doing it explicitly first is harmless and saves a few seconds).
</interfaces>

<research_pointers>
- 27-VALIDATION.md "Manual-Only Verifications" — three manual checks: CAPTCHA tolerance, visual confirmation, artifact usefulness on intentional failure.
- 27-VALIDATION.md "Sampling Rate" — `Before /gsd-verify-work` row: full test + e2e:regression + e2e:silent green.
- 27-RESEARCH.md "Common Pitfalls" Pitfall A (cold-load latency — keep `timeout: 60_000` from playwright.config.js).
- 27-RESEARCH.md "Common Pitfalls" Pitfall D (CAPTCHA — if observed, retune THROTTLE_MS upward and re-run).
- 27-CONTEXT.md `<decisions>` — sets expectations for runtime budgets (smoke ~30s; silent ~10s; regression ~10-15 min).
</research_pointers>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 27-05-01: Run `npm run e2e:smoke` and capture outcome</name>
  <files></files>
  <read_first>
    - package.json (confirm `e2e:smoke` script string contains `--grep @smoke`)
    - tests/e2e/specs/smoke.spec.js (confirm `@smoke` in title)
    - tests/e2e/specs/regression.spec.js (confirm 5 @smoke-tagged tests present)
    - .planning/phases/27-selection-emulation-76-case-deterministic-suite/27-VALIDATION.md row 27-05-03
  </read_first>
  <action>
1. Confirm `dist/chrome/` is fresh:
   ```bash
   npm run build:chrome
   ```
   (If already fresh, `e2e:smoke` will rebuild via chain — harmless.)

2. Run the smoke suite from the repo root:
   ```bash
   npm run e2e:smoke
   ```
   Capture: stdout, exit code, total wall-clock, per-test pass/fail list, and any console warnings.

3. Expected outcome:
   - Exit code 0.
   - Tests run: 1 (Phase 26 smoke) + 5 (Phase 27 @smoke-tagged regression cases) = 6 tests.
   - Wall-clock: 30-45 seconds.
   - No console errors related to selection, citation, or shadow DOM access.

4. If exit non-zero:
   - Inspect `tests/e2e/artifacts/{runId}/` for any captured screenshot + DOM snapshot.
   - Identify the failing test by name.
   - **Common failure modes** (and their fixes):
     - Pre-flight DOM-drift smoke fails → check seed patent US11427642 manually in a real browser. If Google Patents has restructured, document in SUMMARY and STOP; the suite needs a CONTEXT update.
     - Single regression case fails on assertion mismatch → likely the deep-normalize is incomplete or baseline drift; record the case ID, the observed citation, and the expected citation in SUMMARY.
     - All 5 regression cases fail with `selectText: DOM_DRIFT` → the selection algorithm or container-priority list is wrong; record full error from artifacts.
     - CAPTCHA detected by pre-flight → bump `THROTTLE_MS` in regression.spec.js (e.g., 4_000) and re-run; document the tuning.
   - Do NOT modify any spec or lib code to "fix" a failing test without first documenting the failure in the SUMMARY. If fix is needed, ask the user before editing — Plan 05 is verification, not patching.

5. If pass: record the smoke run's stats (test count, total time, any noise in stdout) in the SUMMARY draft for Task 27-05-04 (final summary).
  </action>
  <verify>
    <automated>npm run e2e:smoke</automated>
  </verify>
  <acceptance_criteria>
    - `npm run e2e:smoke` exits 0
    - Stdout indicates ≥ 6 tests passed (1 Phase 26 smoke + ≥5 @smoke-tagged regression cases)
    - No "DOM_DRIFT" or "CAPTCHA_DETECTED" errors in stdout
    - Wall-clock < 90 seconds (sanity bound; expected 30-45s)
    - No artifact files in `tests/e2e/artifacts/{runId}/` (artifacts only written on failure; passing run leaves the directory empty for this caseId set OR no runId dir at all)
  </acceptance_criteria>
  <done>
    `npm run e2e:smoke` exits 0; 6+ tests pass; runtime within budget. Stats captured for the SUMMARY.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 27-05-02: Run `npm run e2e:silent` and capture outcome</name>
  <files></files>
  <read_first>
    - package.json (confirm `e2e:silent` script targets `specs/silent.spec.js`)
    - tests/e2e/specs/silent.spec.js (confirm 2 silent-mode tests on US11427642)
    - .planning/phases/27-selection-emulation-76-case-deterministic-suite/27-VALIDATION.md row 27-05-02
  </read_first>
  <action>
1. Run the silent-mode suite:
   ```bash
   npm run e2e:silent
   ```
   Capture: stdout, exit code, total wall-clock, both per-test outcomes.

2. Expected outcome:
   - Exit code 0.
   - Tests run: 2 (`US11427642-silent-spec-short`, `US11427642-silent-cross-col`).
   - Wall-clock: 10-20 seconds.
   - Both tests assert citation matches `baseline.json["US11427642-spec-short-1"].citation` and `baseline.json["US11427642-cross-col"].citation` respectively.

3. If exit non-zero:
   - `mode` assertion failed → Plan 27-02's `getCitation` silent-mode path may have a bug; record the actual `observed` shape in SUMMARY.
   - Empty citation parsed from `__lastCopiedText__` → the clipboard-observer shim may not be capturing the silent-mode copy event; check `page.evaluate(() => window.__lastCopiedText__)` directly via a one-off debug run.
   - Citation mismatch → the silent-mode citation MAY differ from the auto-mode citation due to silent-mode formatting (e.g., silent mode prepends the selected text). Inspect the raw shim contents in the artifact's DOM snapshot.
   - Same patching-versus-documenting rule as Task 27-05-01: document first, ask before fixing.

4. If pass: record stats for the SUMMARY.

**Note on silent-mode citation format:** RESEARCH.md "Citation Observer" `readSilentCitation` regex matches the trailing citation token from the silent-mode payload (`"{originalSelectedText} {citation}"`). The auto-mode pill's `.cite-text` produces just the citation. If the silent spec fails on string equality because the silent-mode raw contains the selection followed by the citation, that's expected — `getCitation`'s regex SHOULD strip it down. If it doesn't, the regex needs adjustment; document and ask.
  </action>
  <verify>
    <automated>npm run e2e:silent</automated>
  </verify>
  <acceptance_criteria>
    - `npm run e2e:silent` exits 0
    - Stdout indicates 2 tests passed
    - Both tests pass within 30 seconds total wall-clock (sanity bound)
    - No "FAILED to read clipboard" or "empty __lastCopiedText__" errors in stdout
  </acceptance_criteria>
  <done>
    `npm run e2e:silent` exits 0; both silent-mode cases pass; runtime within budget; stats captured for SUMMARY.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 27-05-03: Run `npm run e2e:regression` (full 76 cases) and capture outcome</name>
  <files></files>
  <read_first>
    - package.json (confirm `e2e:regression` script targets `specs/regression.spec.js`)
    - tests/e2e/specs/regression.spec.js (confirm `for (const tc of TEST_CASES)` loop registers 76 tests)
    - tests/golden/baseline.json (sanity-check: 76 entries present)
    - .planning/phases/27-selection-emulation-76-case-deterministic-suite/27-VALIDATION.md rows 27-05-01 and "Manual-Only Verifications"
  </read_first>
  <action>
1. Run the full regression suite from the repo root:
   ```bash
   npm run e2e:regression
   ```
   Expected wall-clock: 10-15 minutes (76 cases × ~10s/case avg + 2s throttle + cold-cache PDF parse on chemical patents).

   IMPORTANT: This task may take 10-15 minutes. Use the `Bash` tool with `timeout: 1200000` (20 min ceiling).

2. Capture: stdout, exit code, total wall-clock, full per-test outcome list (pass/fail per caseId), any per-case timeouts, any CAPTCHA detections.

3. Expected outcome:
   - Exit code 0.
   - Tests run: 76 (one per `TEST_CASES` entry).
   - Wall-clock: 10-15 min ± 5 min.
   - All assertions pass: `observed.citation === baseline[caseId].citation` AND `observed.confidence === colorFromNumericConfidence(baseline[caseId].confidence)` for all 76.

4. **If 1-3 cases fail intermittently (flakes):**
   - Re-run with `npm run e2e:regression` once more. If the same case fails again, it's likely deterministic; if a different case fails, it's a flake.
   - Document flake patterns in the SUMMARY (case IDs, observed citations, suspected cause: cold-cache timeout / CAPTCHA / DOM race / specific normalizer edge case).
   - Recommend in the SUMMARY whether each flake needs a Plan-level fix or whether Phase 29's `retries: 2` (cron) is sufficient.

5. **If many cases fail (>5):**
   - Likely a systemic issue — DOM drift on Google Patents, normalizer regression, or the `setTriggerMode` race didn't actually take effect on the destination page.
   - Inspect `tests/e2e/artifacts/{runId}/` for the first 3 failures. Compare expected citation to observed citation to triage.
   - If ALL cases fail with `DOM_DRIFT` from the pre-flight `beforeAll`, Google Patents has restructured — STOP, document in SUMMARY, escalate to user. This is NOT a fix-in-Phase-27 situation.
   - If failures are scattered across categories with no obvious pattern, document each case's observed-vs-expected and ask the user whether to (a) patch the normalizer in Plan 01's deep-normalize logic, (b) update baseline.json (rare — baseline is supposed to be frozen ground truth), or (c) defer to a Phase 27 follow-up.

6. **If 0 failures:** Phase 27 deterministic suite is green. Proceed to Task 27-05-04 manual artifact verification.

Capture the full TAP-or-list output of the regression run as a code block in the SUMMARY.
  </action>
  <verify>
    <automated>npm run e2e:regression</automated>
  </verify>
  <acceptance_criteria>
    - `npm run e2e:regression` exits 0
    - Stdout indicates 76 tests passed (one per `TEST_CASES` entry)
    - Total wall-clock < 20 min (RESEARCH bound) — record actual time in SUMMARY
    - No pre-flight `DOM_DRIFT` or `CAPTCHA_DETECTED` errors
    - No more than 1 flake-prone case identified across two runs (if a second run is needed)
  </acceptance_criteria>
  <done>
    `npm run e2e:regression` exits 0; 76 tests pass; total wall-clock and per-case timings captured for the SUMMARY. Any flake observations documented.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 27-05-04: Manual artifact verification (DIAG-01 + DIAG-02 useful-on-failure check)</name>
  <what-built>
    Plans 01-04 wired `captureScreenshot` and `captureDomSnapshot` to the per-test catch block in `tests/e2e/specs/regression.spec.js`. Tasks 27-05-01 through 27-05-03 ran the three e2e suites and they all exit 0 — meaning no actual failure has yet exercised the catch-block path.

    Per VALIDATION.md "Manual-Only Verifications" we need ONE intentional failure to confirm the artifacts (a) are actually written to disk, (b) are at the documented path `tests/e2e/artifacts/{runId}/{caseId}-{screenshot.png,dom.html}`, and (c) contain content useful enough to triage the failure (screenshot shows the highlighted selection + the pill; DOM snapshot includes the shadow root contents).
  </what-built>
  <how-to-verify>
1. Open `tests/golden/baseline.json` in an editor. Find one entry — any will do; use `US11427642-spec-short-1` for predictability. Note its current value (e.g. `"citation": "1:26-27"`).

2. Temporarily change that entry's `citation` to a deliberately-wrong value:
   ```json
   "US11427642-spec-short-1": { "citation": "99:99-99", "confidence": 0.98 },
   ```
   Save the file.

3. Run a targeted Playwright command to execute only that one case:
   ```bash
   npx playwright test --config tests/e2e/playwright.config.js --grep "US11427642-spec-short-1"
   ```
   The test should FAIL with `expect(observed.citation).toBe("99:99-99")` mismatching the real citation.

4. After the failure:
   - Note the `RUN_ID` from the spec invocation (the ISO timestamp printed by Playwright, or visible in the artifacts directory listing).
   - List the artifacts:
     ```bash
     ls -la tests/e2e/artifacts/
     # → should show one directory keyed by RUN_ID (timestamp)
     ls -la tests/e2e/artifacts/{RUN_ID}/
     # → should show US11427642-spec-short-1-screenshot.png and US11427642-spec-short-1-dom.html
     ```
   - Open the screenshot in any image viewer. Confirm: the Google Patents page is visible, the highlight (selected text in blue) appears on the expected paragraph, and the citation pill `"1:26-27"` is visible somewhere on the page.
   - Open the DOM snapshot HTML in a browser or text editor. Confirm: it contains a `<div data-testid="pct-citation-pill">` element AND the shadow root's `<span class="cite-text">1:26-27</span>` content is in the file (proving the shadow root was serialized, satisfying DIAG-02's "Shadow DOM contents" clause).

5. **Restore `baseline.json`** to its original value (`"citation": "1:26-27"`). Confirm the restoration via:
   ```bash
   git diff tests/golden/baseline.json
   # → should show ZERO output (file matches HEAD)
   ```

6. Optionally delete the artifact directory you just created (it is gitignored, harmless to keep, but cleanup is tidy):
   ```bash
   rm -rf tests/e2e/artifacts/{RUN_ID}/
   ```

Report back to the executor (resume signal): "approved" (artifacts present, screenshot useful, DOM has shadow content, baseline.json restored) OR describe the specific gap (e.g., "screenshot missing", "DOM snapshot empty", "shadow root not serialized").
  </how-to-verify>
  <resume-signal>Type "approved" once: (1) both artifact files exist at the documented paths, (2) the screenshot visually confirms highlight + pill, (3) the DOM HTML contains the shadow root's `.cite-text` content, AND (4) `git diff tests/golden/baseline.json` is empty. Otherwise describe what went wrong.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 27-05-05: Write `27-05-SUMMARY.md` documenting execution evidence</name>
  <files>.planning/phases/27-selection-emulation-76-case-deterministic-suite/27-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/27-selection-emulation-76-case-deterministic-suite/27-01-SUMMARY.md
    - .planning/phases/27-selection-emulation-76-case-deterministic-suite/27-02-SUMMARY.md
    - .planning/phases/27-selection-emulation-76-case-deterministic-suite/27-03-SUMMARY.md
    - .planning/phases/27-selection-emulation-76-case-deterministic-suite/27-04-SUMMARY.md
    - The captured stdout from Tasks 27-05-01, 27-05-02, 27-05-03 (held in this task's working memory)
    - Operator's resume signal from Task 27-05-04 (held in memory)
  </read_first>
  <action>
Create `.planning/phases/27-selection-emulation-76-case-deterministic-suite/27-05-SUMMARY.md`. Use this structure:

```markdown
---
phase: 27-selection-emulation-76-case-deterministic-suite
plan: 05
type: verification
date: <ISO>
status: complete
---

# Phase 27 Plan 05 — Execution Evidence

## Suite Runs

### `npm run e2e:smoke`
- Tests passed: <N> / <N>
- Wall-clock: <Ms>
- Notes: <any noise>

### `npm run e2e:silent`
- Tests passed: 2 / 2
- Wall-clock: <Ms>
- Notes: <any silent-mode-specific observations>

### `npm run e2e:regression`
- Tests passed: 76 / 76
- Wall-clock: <Mm>
- Per-case max time: <Ms> (case: <ID>)
- Per-case avg time: <Ms>
- Re-runs required: <0|1>
- Flakes identified: <list of {caseId, observed_failure_mode, recommendation}>

## Failure-Artifact Verification (DIAG-01 + DIAG-02)

- Operator forced a baseline mismatch on `US11427642-spec-short-1`.
- Resulting artifacts:
  - `tests/e2e/artifacts/<RUN_ID>/US11427642-spec-short-1-screenshot.png` — <yes|no> (size: <KB>)
  - `tests/e2e/artifacts/<RUN_ID>/US11427642-spec-short-1-dom.html` — <yes|no> (size: <KB>, contains `.cite-text`: <yes|no>)
- Operator verdict: approved / <description of issue>
- `git diff tests/golden/baseline.json` after restoration: <empty | unexpected diff>

## Tuning Observations (for Phase 29 cron)

- THROTTLE_MS = 2_000 sufficient? <yes|no, recommendation>
- Cold-cache PDF parse worst case (chemical patents): <Ms>
- Pre-flight smoke time: <Ms>
- CAPTCHA detector triggered: <no|yes (count, when)>

## Phase 27 Requirements Coverage

| Requirement | Status | Evidence |
|-------------|--------|----------|
| SEL-01 | green | `tests/e2e/lib/selection.js` shipped (Plan 01); `npm run e2e:regression` exit 0 |
| SEL-02 | green | `getCitation` shipped (Plan 02); `npm run e2e:silent` exit 0 |
| SEL-03 | green | `regression.spec.js` shipped (Plan 03); 76/76 pass |
| SEL-04 | green | Fresh persistent context per test (Phase 26 lock); no inter-case leakage observed across 76 tests |
| DIAG-01 | green | `tests/e2e/artifacts/<RUN_ID>/US11427642-spec-short-1-screenshot.png` produced on intentional failure |
| DIAG-02 | green | `tests/e2e/artifacts/<RUN_ID>/US11427642-spec-short-1-dom.html` produced with shadow content |

## Decisions & Open Items

- <Any unexpected behaviors encountered, with disposition: fixed / deferred / accepted as-is>

## Next Phase Readiness

Phase 28 (Independent PDF Verifier) is unblocked: Phase 27's regression spec emits per-case `{caseId, observed_citation, baseline_citation}` that the verifier can consume. Plan 28 will likely add a verifier verdict in the same catch path that currently writes screenshot + DOM snapshot.
```

Fill in every `<...>` placeholder with values captured from Tasks 27-05-01 through 27-05-04. If any task produced a non-green outcome that was nonetheless documented and accepted, set the corresponding row's Status to "yellow — see notes" and document the rationale.

After writing, run:
```bash
test -f .planning/phases/27-selection-emulation-76-case-deterministic-suite/27-05-SUMMARY.md && grep -q "76 / 76" .planning/phases/27-selection-emulation-76-case-deterministic-suite/27-05-SUMMARY.md
```
If the regression suite didn't hit 76/76, the executor should ask the user before continuing with the SUMMARY's "complete" status.
  </action>
  <verify>
    <automated>test -f .planning/phases/27-selection-emulation-76-case-deterministic-suite/27-05-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/27-selection-emulation-76-case-deterministic-suite/27-05-SUMMARY.md` exists
    - Contains pass counts for all three suites (smoke, silent, regression)
    - Contains the manual-verification verdict from Task 27-05-04
    - Contains the requirement-coverage table with at least 6 rows (SEL-01..04, DIAG-01, DIAG-02)
    - `git diff tests/golden/baseline.json` exits 0 (no residual changes from the manual failure-verification)
  </acceptance_criteria>
  <done>
    27-05-SUMMARY.md exists with all three suite runtimes + manual-verification outcome + requirement-coverage table + Phase 28 readiness note. `tests/golden/baseline.json` is unchanged from HEAD.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node test process ↔ Playwright Chromium ↔ Google Patents | 76 sequential nav over 10-15 min; full real-network exposure to a third-party host |
| Operator ↔ filesystem | The manual artifact-verification task temporarily modifies `tests/golden/baseline.json` to force a failure — this is a controlled, reversible edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering / supply chain | `@playwright/test` | accept (inherited) | No new dependencies; reuses Phase 26 `@playwright/test@1.60.0` exact pin (T-26-01 reference). |
| T-27-02 | Information Disclosure (artifacts) | screenshot + DOM-snapshot files | mitigate | `tests/e2e/artifacts/` is gitignored. Task 27-05-04's manual verification produces real artifacts that the operator inspects and then deletes; nothing is committed. |
| T-27-03 | Denial of Service via external dependency | 76 nav to Google Patents | mitigate | Plan 03's 2-second throttle + pre-flight CAPTCHA detector apply during this plan's regression run. If CAPTCHA fires, the suite aborts with one diagnostic. Plan 05's SUMMARY documents whether CAPTCHA triggered. |
| T-27-04 | Information Disclosure via tmpdir | per-test tmpdir | accept (inherited) | Phase 26 cleanup removes the dir. |
| T-27-05 | Tampering (operator edits baseline.json) | manual verification step | mitigate | Task 27-05-04 explicitly requires `git diff tests/golden/baseline.json` to be empty after restoration; Task 27-05-05 acceptance criteria re-verifies this. |
</threat_model>

<verification>
At end of plan:
1. `npm run e2e:smoke` exits 0 (Task 27-05-01).
2. `npm run e2e:silent` exits 0 (Task 27-05-02).
3. `npm run e2e:regression` exits 0 (Task 27-05-03) — all 76 cases pass.
4. The manual verification confirms screenshot + DOM artifacts are produced on intentional failure (Task 27-05-04).
5. `.planning/phases/27-selection-emulation-76-case-deterministic-suite/27-05-SUMMARY.md` exists with execution evidence (Task 27-05-05).
6. `git diff tests/golden/baseline.json` exits 0 (no residual change from manual verification).
7. `npm run test:src` exits 0 (vitest suite still green).
</verification>

<success_criteria>
- All three e2e commands (`smoke`, `silent`, `regression`) exit 0.
- Manual artifact verification approves DIAG-01 + DIAG-02 wiring.
- `27-05-SUMMARY.md` documents pass counts, wall-clocks, flakes, and Phase 29 tuning recommendations.
- `tests/golden/baseline.json` is unchanged from HEAD after the manual verification.
- Phase 27 deterministic regression suite is proven against a real Google Patents page — Phase 28 is unblocked.
</success_criteria>

<output>
After completion, `.planning/phases/27-selection-emulation-76-case-deterministic-suite/27-05-SUMMARY.md` exists and serves as the Phase 27 close-out evidence. The orchestrator's `/gsd-verify-work` will read it.
</output>
