---
phase: 32-human-uat-verification
plan: 03
type: execute
wave: 2
depends_on: [32-02]
files_modified:
  - scripts/e2e-explore.mjs
  - tests/e2e/scripts/e2e-explore-phase-flag.test.js
autonomous: true
requirements: [UAT-02]

must_haves:
  truths:
    - "`npm run e2e:explore -- --phase 32` parses the flag with strict regex `^\\d+$`; rejects `--phase v32`, `--phase=32`, and missing value (Pitfall 2)"
    - "Pre-flight check: `e2e-explore.mjs --phase 32` aborts with exit code 6 and a clear stderr message when the existing ledger's phase-32 sum is already >=$10 (D-15)"
    - "Mid-run check: after each `appendLedgerEntry`, the phase-32 sum is re-evaluated; >=$8 prints a warn line, >=$10 returns stopAll:true which breaks the iteration loop and lets finalizeLlmReport run (D-16; reuses existing stopAll mechanism)"
    - "`phase` field is stamped on EVERY `appendLedgerEntry` call inside `runOneIteration` (both first call line 191-196 and retry line 225-232)"
    - "Backward compatibility: invoking without `--phase` results in `phase: null` (or absent) on ledger entries; Phase 31 call sites and `scripts/llm-cron-run.sh` behavior is unchanged"
    - "CI guard at e2e-explore.mjs:72 still fires when CI=true (Pitfall 11 — e2e-explore-ci-guard.test.js still green)"
    - "Exit code 6 is new (phase cap); exit codes 0-5 retain their existing semantics; the top-of-file exit-code comment block is updated"
  artifacts:
    - path: "scripts/e2e-explore.mjs"
      provides: "Extended explore driver with --phase flag, pre-flight phase cap, mid-run phase cap, phase-tagged ledger entries"
    - path: "tests/e2e/scripts/e2e-explore-phase-flag.test.js"
      provides: "Vitest spec from Wave 0 Plan 32-01 now passes all four phase-flag tests"
  key_links:
    - from: "parseArgs"
      to: "phase field on returned object"
      via: "argv parse with strict regex `^\\d+$`"
      pattern: "parseArgs.*phase.*\\\\d"
    - from: "main()"
      to: "checkPhaseSpendCap"
      via: "pre-flight phase cap check after initialCap block"
      pattern: "checkPhaseSpendCap.*initialLedger"
    - from: "runOneIteration"
      to: "appendLedgerEntry"
      via: "phase field stamped on entry shape"
      pattern: "appendLedgerEntry.*phase"
    - from: "runOneIteration mid-run check"
      to: "stopAll: true"
      via: "phase total >=10 returns stopAll flag; main loop break propagates"
      pattern: "stopAll.*phase"
---

<objective>
Wire the per-phase cap helpers from Plan 32-02 into `scripts/e2e-explore.mjs` as: (1) a strict `--phase <id>` CLI flag, (2) a startup pre-flight check (D-15), (3) a mid-run check after each ledger append (D-16), and (4) a `phase` field stamped on every ledger entry inside `runOneIteration`. The existing `stopAll: true` mechanism (already plumbed through `main()` at line 428) handles loop-break and ensures `finalizeLlmReport` runs — no main-loop refactor needed (per PATTERNS.md and RESEARCH Pitfall 6).

Purpose: Enable `npm run e2e:explore -- --phase 32` per D-14, with the spend-cap guardrails per D-13/D-15/D-16, so the Wave 3 manual UAT execution can run safely against the live Max 5 subscription without burning more than $10 of subscription credit on Phase 32.

Output: A modified `scripts/e2e-explore.mjs` with the four behaviors above and a passing `tests/e2e/scripts/e2e-explore-phase-flag.test.js` from Wave 0 (no longer RED). The existing `tests/e2e/scripts/e2e-explore-ci-guard.test.js` MUST still pass (Pitfall 11 — CI guard intact). All 461+ existing Vitest tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-human-uat-verification/32-CONTEXT.md
@.planning/phases/32-human-uat-verification/32-RESEARCH.md
@.planning/phases/32-human-uat-verification/32-PATTERNS.md
@.planning/phases/32-human-uat-verification/32-VALIDATION.md
@.planning/phases/32-human-uat-verification/32-02-SUMMARY.md

<interfaces>
<!-- The contracts the executor needs — both consumed (Plan 32-02 exports) and modified (e2e-explore.mjs hooks) -->

From tests/e2e/lib/llm-ledger.js (Plan 32-02 ships these — IMPORT and USE):
```javascript
export const PHASE_HARD_CAP_USD = 10;
export const PHASE_WARN_THRESHOLD_USD = 8;
export function phaseTotal(ledger, phase): number;
export function checkPhaseSpendCap(ledger, phase): {
  status: 'ok'|'warn'|'block', phase_total_usd: number, phase: string, message: string
};
```

From scripts/e2e-explore.mjs (current — these are the modification points; line numbers per RESEARCH Insertion Map):
```javascript
// Lines 29-36 — exit codes comment block (EXTEND with code 6)
// Lines 81-107 — parseArgs (ADD --phase branch with strict regex)
// Lines 191-196 — first appendLedgerEntry call (ADD phase field)
// Lines 225-232 — retry appendLedgerEntry call (ADD phase field)
// Line 395 — main() destructure of parseArgs (ADD phase)
// After line 409 — pre-flight phase cap check (ADD new block)
// Line 427 — runOneIteration call (PASS phase through args)
// After line 196 AND line 232 — mid-run phase cap check (RETURN stopAll on block)
```

From tests/e2e/scripts/e2e-explore-phase-flag.test.js (Wave 0 — these tests must now PASS):
```javascript
// 4 tests created in Wave 0 — currently RED:
//   1. rejects --phase v32 → exit 2, stderr contains "invalid --phase value"
//   2. rejects --phase=32  → exit 2, stderr indicates equals syntax unsupported
//   3. rejects --phase (no value) → exit 2, stderr indicates missing value
//   4. accepts --phase 32 → exits with code != 2 (proves parseArgs accepted)
```

Existing exit codes (DO NOT change semantics):
```
0 — run completed
1 — CI guard fired
2 — bad --iterations argument (now ALSO bad --phase value or equals syntax or missing value)
3 — claude CLI not found
4 — monthly spend cap reached at STARTUP
5 — fatal/unexpected error in main()
6 — NEW: phase spend cap reached at STARTUP or mid-run
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add --phase parsing + pre-flight + mid-run cap + ledger-entry stamping in e2e-explore.mjs</name>
  <files>scripts/e2e-explore.mjs</files>
  <read_first>
    - scripts/e2e-explore.mjs (ENTIRE FILE — 442 lines; understand existing parseArgs, main flow, runOneIteration, stopAll plumbing, exit-code comment block)
    - tests/e2e/lib/llm-ledger.js (post-Plan 32-02 — verify new exports `PHASE_HARD_CAP_USD`, `PHASE_WARN_THRESHOLD_USD`, `phaseTotal`, `checkPhaseSpendCap` are present)
    - tests/e2e/scripts/e2e-explore-phase-flag.test.js (Wave 0 — read EXACT assertion text so production messages match)
    - tests/e2e/scripts/e2e-explore-ci-guard.test.js (Pitfall 11 — verify the CI guard at line 72 is untouched after this plan's edits)
    - .planning/phases/32-human-uat-verification/32-PATTERNS.md (section "scripts/e2e-explore.mjs" — concrete pattern excerpts for arg-parser, pre-flight, mid-run, ledger-entry shape)
    - .planning/phases/32-human-uat-verification/32-RESEARCH.md (Insertion Map "scripts/e2e-explore.mjs" — exact line numbers; Code Example 1 + 2 — concrete pre-flight and mid-run code; Pitfall 2 — equals-syntax rejection; Pitfall 6 — stopAll already plumbed)
  </read_first>
  <behavior>
    - `parseArgs` accepts `--phase <value>` where `<value>` matches strict regex `^\d+$`; returns `{ iterations, phase }` (phase defaults to `null` when flag absent)
    - `parseArgs` rejects `--phase=32` (equals syntax not supported per Pitfall 2): exit code 2, stderr starts with `[e2e-explore]` and contains "equals syntax" or "use `--phase <value>`" or similar grep-friendly phrase
    - `parseArgs` rejects `--phase v32` (non-numeric): exit code 2, stderr starts with `[e2e-explore]` and contains "invalid --phase value: v32"
    - `parseArgs` rejects `--phase` with no following value: exit code 2, stderr starts with `[e2e-explore]` and contains "missing value for --phase"
    - `--help` text updated to document `--phase N` option
    - `main()` destructures `{ iterations, phase }` from `parseArgs(process.argv)`
    - After existing `initialCap` block (after line 409), a new block runs `checkPhaseSpendCap(initialLedger, phase)` ONLY when `phase != null`; on status='block' prints message and exits with code 6; on status='warn' prints warning and continues (mirrors existing checkSpendCap pattern at 402-409)
    - `runOneIteration` accepts `phase` in its args destructure
    - Both `appendLedgerEntry` calls inside `runOneIteration` (first at 191-196; retry at 225-232) include a `phase` field on the entry object (uses the closure-captured `phase` argument)
    - After EACH `appendLedgerEntry` call inside `runOneIteration` (both first and retry sites), a mid-run check runs ONLY when `phase != null`: re-reads ledger via `readLedger(LEDGER_PATH)`, calls `checkPhaseSpendCap(freshLedger, phase)`, on status='block' prints message and returns `{ stopAll: true }` (which already breaks main's loop and lets finalizeLlmReport run per existing plumbing); on status='warn' prints warning and continues
    - Top-of-file exit-code comment block (lines 29-36) updated to add `6 — phase spend cap reached at STARTUP or mid-run`
    - All new stderr lines use `[e2e-explore]` prefix (matches existing convention at lines 73, 87, 113, 136, 403)
    - CI guard at line 72 (`if (process.env.CI || process.env.GITHUB_ACTIONS)`) is UNTOUCHED
  </behavior>
  <action>
    Open `scripts/e2e-explore.mjs`. Apply changes in source-line order so insertions don't shift line refs:

    1. Lines 29-36 (top-of-file exit-codes comment): append a new bullet `//   6 — phase spend cap reached at STARTUP or mid-run (D-13/D-15/D-16; --phase flag)`.

    2. Lines 41-43 (imports from `llm-ledger.js`): extend the destructured import to include `phaseTotal, checkPhaseSpendCap, PHASE_HARD_CAP_USD, PHASE_WARN_THRESHOLD_USD`. Final import remains `import { LEDGER_PATH, readLedger, checkSpendCap, appendLedgerEntry, phaseTotal, checkPhaseSpendCap, PHASE_HARD_CAP_USD, PHASE_WARN_THRESHOLD_USD } from '../tests/e2e/lib/llm-ledger.js';` (sorted by existing convention).

    3. Lines 81-107 (parseArgs): add a new `else if` branch for `--phase` between the `--iterations` and `--help` branches. Inside the branch: first detect equals-syntax via `argv[i].startsWith('--phase=')` (reject with message about equals syntax); then if `argv[i] === '--phase'` and `!argv[i+1]` reject with "missing value for --phase"; then validate `argv[i+1]` against `/^\d+$/` (reject with "invalid --phase value: <value>"); on success, set local `phase = argv[i+1]; i++;`. Initialize `let phase = null;` at function top. Return shape becomes `{ iterations, phase }`. Update the `--help` help-block string at lines 92-102 to add a `--phase N` line under `--iterations N`.

    4. Lines 191-196 (first `appendLedgerEntry` call): add `phase` as the last field of the entry literal (use the closure-captured `phase` from `runOneIteration` args).

    5. Immediately after line 196 (before the `if (!parsed.ok)` block at line 198): add the mid-run phase cap check using Code Example 2 from RESEARCH. Guard on `if (phase != null)`. On block: print stderr and `return { stopAll: true };` (the early return is fine — `finalizeLlmReport` runs in main() after the loop break).

    6. Lines 225-232 (retry `appendLedgerEntry` call): add `phase` field (same pattern as the first call; preserves the existing `retry: true` field).

    7. Immediately after line 232 (before `if (retryParsed.ok)` at line 233): add the same mid-run phase cap check pattern. Guard on `if (phase != null)`. On block: `return { stopAll: true };`.

    8. Line 126 (`runOneIteration({ iterationN, runId, reportPath, liveCases })` SIGNATURE — currently destructures these 4 fields): add `phase` to the destructure. The function signature becomes `async function runOneIteration({ iterationN, runId, reportPath, liveCases, phase })`.

    9. Line 395 (main() destructure): change `const { iterations } = parseArgs(process.argv);` to `const { iterations, phase } = parseArgs(process.argv);`.

    10. After line 409 (after the existing initialCap warn block): add a new block per Code Example 1 from RESEARCH. Guard on `if (phase != null)`. Compute `const phaseCap = checkPhaseSpendCap(initialLedger, phase);`. On block: stderr write + `process.exit(6);`. On warn: stderr write + continue.

    11. Line 427 (`await runOneIteration({ iterationN: n, runId, reportPath, liveCases })`): add `phase` to the args object.

    12. Add a one-line stdout print at startup-time (after the `[e2e-explore] run_id=...` line at 414, ideally before the loop) when `phase != null`: `process.stdout.write('[e2e-explore] phase=' + phase + ' (per-phase cap $' + PHASE_HARD_CAP_USD + ' / warn $' + PHASE_WARN_THRESHOLD_USD + ')\n');` so the UAT EVIDENCE log shows the phase context.

    13. Do NOT modify the CI guard at line 72. Do NOT modify `checkClaudeCli`. Do NOT modify the `--iterations` parsing logic. Do NOT modify `finalizeLlmReport` invocation. Do NOT modify any other function.

    Concrete error message wording (Claude's discretion area from CONTEXT.md):
    - Equals syntax: `[e2e-explore] equals syntax not supported for --phase; use \`--phase <value>\``
    - Missing value: `[e2e-explore] missing value for --phase`
    - Invalid value: `[e2e-explore] invalid --phase value: <value> (must match /^\\d+$/)`
  </action>
  <verify>
    <automated>npx vitest run tests/e2e/scripts/e2e-explore-phase-flag.test.js tests/e2e/scripts/e2e-explore-ci-guard.test.js 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 4 phase-flag tests (Wave 0 Plan 32-01) NOW PASS: `npx vitest run tests/e2e/scripts/e2e-explore-phase-flag.test.js` exits 0
    - All existing CI-guard tests still PASS: `npx vitest run tests/e2e/scripts/e2e-explore-ci-guard.test.js` exits 0 (Pitfall 11 — CI guard not regressed)
    - `grep -c "phaseTotal\|checkPhaseSpendCap\|PHASE_HARD_CAP_USD\|PHASE_WARN_THRESHOLD_USD" scripts/e2e-explore.mjs` returns >= 4 (imports + usages)
    - `grep -c "phase:" scripts/e2e-explore.mjs` returns >= 2 (both appendLedgerEntry sites stamped)
    - `grep -c "process.exit(6)" scripts/e2e-explore.mjs` returns 1 (pre-flight phase cap exit)
    - `grep -c "stopAll: true" scripts/e2e-explore.mjs` returns >= 3 (existing monthly mid-run + 2 new phase mid-run sites; or >=4 if all mid-runs split)
    - Top-of-file comment block contains `6 — phase spend cap` (`grep -c "6 — phase spend cap" scripts/e2e-explore.mjs` returns 1)
    - Manual sanity: `node scripts/e2e-explore.mjs --help` (or with CI guard wiped) prints help text that includes `--phase N`
    - Manual sanity: `node scripts/e2e-explore.mjs --phase=32` exits with code 2 and stderr containing "equals syntax"
    - Manual sanity: `node scripts/e2e-explore.mjs --phase v32` exits with code 2 and stderr containing "invalid --phase value: v32"
    - Full Vitest suite passes: `npm run test:src` exits 0 (461+ tests green; new 12+ from Plan 32-02; new 4 now-green from Plan 32-01)
    - CI guard line UNCHANGED: `grep -c "process.env.CI || process.env.GITHUB_ACTIONS" scripts/e2e-explore.mjs` returns 1 (the line at 72 is preserved)
    - The pre-flight phase cap block uses `initialLedger` (the same variable computed at line 401 — does NOT re-read the ledger), to avoid wasting an I/O call
    - The mid-run check re-reads the ledger via `readLedger(LEDGER_PATH)` (it MUST be a fresh read — the just-appended entry is on disk and we want it included in the sum)
  </acceptance_criteria>
  <done>Four behaviors implemented; new phase-flag tests pass; existing CI-guard tests still pass; full test:src suite still green; CI guard line untouched; pre-flight uses already-read ledger, mid-run reads fresh.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend phase-flag test with mid-run/pre-flight integration coverage (optional spawnSync test for cap behavior)</name>
  <files>tests/e2e/scripts/e2e-explore-phase-flag.test.js</files>
  <read_first>
    - tests/e2e/scripts/e2e-explore-phase-flag.test.js (after Task 1 — verify the 4 base tests pass; we add 2 cap-behavior tests on top)
    - tests/e2e/scripts/e2e-explore-ci-guard.test.js (pattern for env-injection — Test 3 lines 51-55)
    - tests/e2e/lib/llm-ledger.js (LEDGER_PATH, appendLedgerEntry — for seeding the ledger before invoking the script)
    - .planning/phases/32-human-uat-verification/32-VALIDATION.md (UAT-02 b/c rows — integration test for pre-flight cap)
  </read_first>
  <behavior>
    Add two new `it(...)` tests to the existing `describe('--phase flag (Phase 32)', ...)` block:
    - "Test 5: pre-flight phase cap blocks startup when ledger phase-32 sum >= $10 (D-15)" — spawnSync wraps the script with a SEEDED `.llm-spend-ledger.json` (use a tmpDir for the ledger path; if LEDGER_PATH is hard-coded in the module the test must mock by environment variable OR seed the real path AND restore it; preferred: skip via `it.skip()` with a comment if the path is hard-coded and there's no env hook); when the seeded ledger has phase-32 entries totaling >=$10, asserts the script exits with code 6 and stderr contains "Phase 32 LLM spend" and "Refusing to invoke"
    - "Test 6: --phase flag absent → no phase enforcement, no new behavior (back-compat)" — spawnSync with no --phase; assert exit is NOT 6 (the script proceeds to other paths — claude check, regular cap check, etc.) and stderr does NOT contain "Phase" with cap-related text (back-compat verification for D-14)
  </behavior>
  <action>
    Open `tests/e2e/scripts/e2e-explore-phase-flag.test.js`. Read LEDGER_PATH from `tests/e2e/lib/llm-ledger.js` to determine if it's hard-coded (it is — relative to module dir). Because LEDGER_PATH is hard-coded (`path.resolve(__dirname, '../.llm-spend-ledger.json')`), the integration test cannot trivially redirect it via env. TWO acceptable approaches: (a) seed the real ledger path and restore it via try/finally (risky — could leak into real spend tracking); (b) mark Test 5 as `it.skip('integration test deferred — LEDGER_PATH is hard-coded; mid-run cap coverage is via unit tests in tests/unit/llm-ledger.test.js')` and add Test 6 as a real test. RECOMMENDED: option (b). Add both tests; mark Test 5 as `it.skip` with a comment referencing the unit test that covers the same behavior. Test 6 is a real spawnSync test that asserts the no-phase code path is back-compat: spawnSync the script with NO `--phase`, env wiped of CI/GITHUB_ACTIONS, assert exit code is NOT 6, stderr does NOT contain "Phase" with cap wording. The test 6 still may exit 3 (claude CLI absent on the runner) or 4 (monthly cap) — assertion is "exit code != 6 AND stderr doesn't say 'phase spend cap'".
  </action>
  <verify>
    <automated>npx vitest run tests/e2e/scripts/e2e-explore-phase-flag.test.js 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "it(" tests/e2e/scripts/e2e-explore-phase-flag.test.js` returns 5 or 6 (4 original + Test 5 .skip + Test 6 real)
    - `grep -c "it.skip" tests/e2e/scripts/e2e-explore-phase-flag.test.js` returns 1 (Test 5 deferred)
    - Test 5 `.skip` comment explicitly references `tests/unit/llm-ledger.test.js` as the unit-test owner of the same behavior
    - Test 6 passes (real test, no skip)
    - `npx vitest run tests/e2e/scripts/e2e-explore-phase-flag.test.js` exits 0 (Tests 1-4 + Test 6 pass; Test 5 skipped cleanly)
    - Full Vitest suite still passes: `npm run test:src` exits 0
  </acceptance_criteria>
  <done>The phase-flag test file now contains 6 tests; 5 run and pass; 1 is intentionally skipped with a documented redirect to the unit-test coverage that already exists from Plan 32-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User CLI argv ↔ parseArgs | argv is attacker-controlled in principle; phase value is validated by strict regex `^\d+$` before any consumption |
| `phase` string ↔ shell / ledger | phase is only used as a string key in JSON; never interpolated into a shell command (no execSync uses phase) |
| Ledger file ↔ disk | Atomic write at llm-ledger.js:191-193 already mitigates (D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-08 | Tampering | `--phase '32; rm -rf /'` shell injection via parseArgs | mitigate | Strict regex `^\d+$` at parseArgs reject path; the value never crosses into a shell context anyway (only used as JSON field + sum filter) — defense in depth |
| T-32-09 | Tampering | `--phase=32` parsing ambiguity (parseArgs uses index-based argv lookup) | mitigate | Explicit `startsWith('--phase=')` rejection branch in parseArgs (Pitfall 2); Test 2 in the spec asserts the rejection |
| T-32-10 | Denial of service | User passes very large `--phase` value (e.g., 1e15) — phaseTotal still O(N_iterations) | accept | phaseTotal cost is bounded by N_iterations regardless of phase value; large phase IDs simply yield 0 sum (no entries match) |
| T-32-11 | Tampering | Mid-run cap mis-evaluation due to stale ledger read (concurrent appendLedgerEntry from another process) | accept | Single-process model (D-01: user runs on their machine); cross-process race documented as out-of-scope in Phase 31 security domain |
| T-32-12 | Denial of service | Pre-flight cap evaluated against stale `initialLedger` (read once at startup) | mitigate | Pre-flight uses `initialLedger` only for the STARTUP check (D-15); mid-run check uses a FRESH `readLedger(LEDGER_PATH)` after each append, so cap evaluation reflects accumulated spend within the current run |
| T-32-13 | Spoofing | CI runner triggers explore script with `--phase 32` evading CI guard | mitigate | CI guard at line 72 (`process.env.CI || process.env.GITHUB_ACTIONS`) is UNTOUCHED; tested by existing e2e-explore-ci-guard.test.js (Pitfall 11) |
| T-32-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; only existing imports from `tests/e2e/lib/llm-ledger.js` (Plan 32-02 deliverable) and Node 22 built-ins |
</threat_model>

<verification>
After both tasks complete:

```bash
# 1. New tests green
npx vitest run tests/e2e/scripts/e2e-explore-phase-flag.test.js

# 2. CI guard still green (Pitfall 11)
npx vitest run tests/e2e/scripts/e2e-explore-ci-guard.test.js

# 3. Full Vitest regression
npm run test:src

# 4. CI guard line preserved
grep "process.env.CI || process.env.GITHUB_ACTIONS" scripts/e2e-explore.mjs

# 5. Phase imports present
grep -E "phaseTotal|checkPhaseSpendCap|PHASE_HARD_CAP_USD|PHASE_WARN_THRESHOLD_USD" scripts/e2e-explore.mjs | head -10

# 6. Both appendLedgerEntry sites stamp phase
grep -B 1 -A 6 "appendLedgerEntry(LEDGER_PATH" scripts/e2e-explore.mjs | grep -c "phase"
# Expected: >= 2 (one per call site)

# 7. Exit code 6 documented and used
grep -c "6 — phase spend cap" scripts/e2e-explore.mjs    # Expected: 1
grep -c "process.exit(6)" scripts/e2e-explore.mjs        # Expected: 1

# 8. Help text mentions --phase
node scripts/e2e-explore.mjs --help 2>&1 | grep -c "phase"
# Expected: >= 1

# 9. Equals syntax rejected
node scripts/e2e-explore.mjs --phase=32 2>&1 | grep -E "(equals|invalid)" ; echo "exit: $?"
# Expected: matches a pattern AND the script exits 2

# 10. Non-numeric rejected
node scripts/e2e-explore.mjs --phase v32 2>&1 | grep "invalid --phase value: v32" ; echo "exit: $?"
# Expected: matches AND script exits 2
```

All 10 gates MUST pass. Plan 32-04 may proceed in parallel (Wave 2) since it modifies disjoint files.
</verification>

<success_criteria>
- `scripts/e2e-explore.mjs` accepts `--phase <numeric>` with strict regex validation; rejects equals-syntax + non-numeric + missing-value with exit code 2 and `[e2e-explore]` prefixed stderr
- Pre-flight phase cap check runs after the existing monthly cap check; exits with code 6 on block (D-15)
- Mid-run phase cap check runs after each ledger append (both first call and retry call); returns `stopAll: true` on block, triggering the existing loop-break and finalizeLlmReport flow (D-16; Pitfall 6 already-handled)
- Every `appendLedgerEntry` call inside `runOneIteration` includes the `phase` field (closure-captured from args)
- Top-of-file exit-code comment documents code 6
- Existing CI guard, --iterations parsing, claude-CLI check, monthly cap behavior, and all 461 existing Vitest tests are unchanged
- All Wave 0 phase-flag tests (Plan 32-01) now PASS (turning Wave 0 RED tests GREEN per the contract)
- The 4 base tests + Test 6 (back-compat) all pass; Test 5 (integration with seeded ledger) is `.skip`-deferred with a documented redirect to the unit-test coverage in `tests/unit/llm-ledger.test.js`
</success_criteria>

<output>
Create `.planning/phases/32-human-uat-verification/32-03-SUMMARY.md` when done. Capture:
- Final exit-code map (with new code 6)
- Exact error message strings chosen for the three rejection paths (Claude's discretion area)
- Confirmation Test 5 deferred to unit tests; Tests 1-4 + 6 green
- Confirmation CI guard line + checkClaudeCli + monthly-cap behavior all untouched
- Wave 0 contract fulfilled: phase-flag spec turned GREEN
</output>
</content>
</invoke>