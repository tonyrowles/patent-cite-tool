---
phase: 33-re-run-validator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/fixtures/uat-phase32-llm-report.json
  - tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js
  - tests/e2e/lib/llm-report.js
  - tests/unit/llm-report.test.js
  - scripts/_migrate-uat-fixture.mjs
autonomous: true
requirements: [RERUN-03]
requirements_addressed: [RERUN-03]

must_haves:
  truths:
    - "D-13: REQUIRED_ENTRY_FIELDS split into REQUIRED_NONNULL_FIELDS + REQUIRED_NULLABLE_FIELDS; keys required on 4 new fields, null permitted"
    - "D-15: uat-phase32-llm-report.json re-stamped in place with schema_version: 1 and 4 null-valued capture-state keys per iteration"
    - "uat-phase32-llm-report.json contains schema_version: 1 at top level"
    - "Every iteration in uat-phase32-llm-report.json has the four keys scroll_y, viewport_width, viewport_height, selected_node_xpath (values may be null)"
    - "appendLlmIteration throws when any of the four new keys is omitted from an iteration object"
    - "appendLlmIteration accepts null values on the four new keys without throwing"
    - "appendLlmIteration still throws when iteration_n, iso, or classification is null or undefined (existing strictness preserved)"
    - "All existing tests in tests/unit/llm-report.test.js and tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js pass after the changes"
  artifacts:
    - path: "tests/e2e/lib/llm-report.js"
      provides: "REQUIRED_NONNULL_FIELDS and REQUIRED_NULLABLE_FIELDS arrays; split validation loop in appendLlmIteration"
      contains: "REQUIRED_NULLABLE_FIELDS"
    - path: "tests/e2e/fixtures/uat-phase32-llm-report.json"
      provides: "Re-stamped fixture with schema_version: 1 and four new null-valued keys on every iteration"
      contains: "\"schema_version\": 1"
    - path: "tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js"
      provides: "Two new assertions: schema_version === 1 + presence of the four keys on every iteration"
      contains: "schema_version"
    - path: "tests/unit/llm-report.test.js"
      provides: "Test 12d-12g (missing-key throws for each new field), Test 12h (null permitted), Test 12i (non-null fields still reject null)"
      contains: "REQUIRED_NULLABLE_FIELDS"
    - path: "scripts/_migrate-uat-fixture.mjs"
      provides: "One-shot Node migration script that re-stamps the committed fixture"
      contains: "schema_version"
  key_links:
    - from: "tests/e2e/lib/llm-report.js"
      to: "tests/e2e/fixtures/uat-phase32-llm-report.json"
      via: "appendLlmIteration schema-guard round-trip in uat-phase32-llm-report.schema.test.js"
      pattern: "every iteration passes appendLlmIteration schema guard"
    - from: "scripts/_migrate-uat-fixture.mjs"
      to: "tests/e2e/fixtures/uat-phase32-llm-report.json"
      via: "Node fs read-modify-write with JSON.parse/JSON.stringify"
      pattern: "schema_version = 1"
---

<objective>
Land D-13 (REQUIRED_ENTRY_FIELDS split) and D-15 (UAT fixture re-stamp) in a single atomic commit so the schema-guard round-trip test never goes red in the intermediate state (Pitfall 4). The plan also extends the unit-test coverage in tests/unit/llm-report.test.js to prove the new "key present, null permitted" semantics for the four capture fields and adds two explicit presence assertions to the existing uat-phase32-llm-report.schema.test.js.

Purpose: Without this plan, Plan 33-02 (validator core) cannot consume an llm-report.json shape that downstream phases rely on, and Plan 33-03 (capture in e2e-explore.mjs) cannot land without breaking the existing schema test. This plan ships the schema lock that every other plan in Phase 33 builds on.
Output: A modified llm-report.js with a split-list validator (option a per RESEARCH.md §Code Examples), a re-stamped committed fixture, extended unit + schema tests, and a one-shot migration script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-re-run-validator/33-CONTEXT.md
@.planning/phases/33-re-run-validator/33-RESEARCH.md
@.planning/phases/33-re-run-validator/33-PATTERNS.md
@tests/e2e/lib/llm-report.js
@tests/unit/llm-report.test.js
@tests/e2e/fixtures/uat-phase32-llm-report.json
@tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js

<interfaces>
<!-- Key contracts the executor needs. Extracted from tests/e2e/lib/llm-report.js. -->

Current state (tests/e2e/lib/llm-report.js):
- Line 55: `const REQUIRED_ENTRY_FIELDS = ['iteration_n', 'iso', 'classification'];`
- Lines 76-91: `atomicWriteJson(destPath, content)` helper — verbatim pattern to mirror in Plan 33-02 (DO NOT extract here; just leave it as-is)
- Lines 215-220: validation loop:
  ```
  export function appendLlmIteration(reportPath, iteration) {
    for (const f of REQUIRED_ENTRY_FIELDS) {
      if (iteration?.[f] === undefined || iteration[f] === null) {
        throw new Error(`appendLlmIteration: missing required field '${f}'`);
      }
    }
  ```

Target state (this plan delivers):
- `REQUIRED_NONNULL_FIELDS = ['iteration_n', 'iso', 'classification']` — preserves existing strictness
- `REQUIRED_NULLABLE_FIELDS = ['scroll_y', 'viewport_width', 'viewport_height', 'selected_node_xpath']` — key must be present, null permitted
- Validation loop becomes two loops: first uses `=== undefined || === null` check; second uses `!(f in (iteration ?? {}))` check
- Public API of appendLlmIteration UNCHANGED (still single-argument, same throw shape, same return behavior). REQUIRED_ENTRY_FIELDS name is removed only if no other module imports it; check with grep before deleting (research §Sources notes no external imports).

Fixture shape (tests/e2e/fixtures/uat-phase32-llm-report.json — 238 lines, 10 iterations):
- Top-level keys today: run_id, started_iso, finished_iso, iterations_total, summary, iterations
- Post-migration top-level keys: schema_version (NEW, value 1), then existing keys preserved in insertion order
- Each entry in iterations[] gains 4 new keys with null values: scroll_y, viewport_width, viewport_height, selected_node_xpath
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the migration script and re-stamp the UAT fixture</name>
  <files>scripts/_migrate-uat-fixture.mjs, tests/e2e/fixtures/uat-phase32-llm-report.json</files>
  <read_first>
    - tests/e2e/fixtures/uat-phase32-llm-report.json (current shape — read top-level keys and one iteration)
    - .planning/phases/33-re-run-validator/33-PATTERNS.md (one-shot migration script pattern in the §"tests/e2e/fixtures/uat-phase32-llm-report.json" section)
    - .planning/phases/33-re-run-validator/33-RESEARCH.md §"Common Pitfalls" Pitfall 4 (atomic-commit ordering requirement)
    - .planning/phases/33-re-run-validator/33-CONTEXT.md D-15 (re-stamp semantics)
  </read_first>
  <behavior>
    - Running `node scripts/_migrate-uat-fixture.mjs` exactly once produces a fixture file with: (a) top-level `schema_version: 1` as the FIRST key, (b) every entry in `iterations[]` gaining `scroll_y: null`, `viewport_width: null`, `viewport_height: null`, `selected_node_xpath: null`, (c) no other field altered, (d) trailing newline preserved.
    - Re-running the script is idempotent: a second invocation leaves the file byte-identical (the script must use a presence check before writing each key, per the PATTERNS.md example using `if (!('scroll_y' in it)) it.scroll_y = null`).
    - `jq '.schema_version'` on the migrated file outputs `1`.
    - `jq '.iterations | length'` still outputs `10` (count unchanged).
    - `jq '.iterations | map(has("scroll_y") and has("viewport_width") and has("viewport_height") and has("selected_node_xpath")) | all'` outputs `true`.
  </behavior>
  <action>
    Create scripts/_migrate-uat-fixture.mjs as a Node ES module (mjs) that imports `fs` from `'node:fs'`, reads `tests/e2e/fixtures/uat-phase32-llm-report.json` via `fs.readFileSync(p, 'utf8')`, JSON.parses it, and produces a NEW object whose first key is `schema_version: 1` followed by the rest of the existing top-level keys in their original order — preserve key ordering by constructing a new object literal in the order `{ schema_version: 1, ...existing }` and writing back. For each entry in `iterations[]`, apply the presence-check idiom `if (!('scroll_y' in it)) it.scroll_y = null` for each of the four new keys (so the script is idempotent per behavior contract). Write back with `fs.writeFileSync(p, JSON.stringify(obj, null, 2) + '\n')` matching the existing file's 2-space indent and trailing newline. Then execute the script ONCE locally to re-stamp the committed fixture in place. The script file itself IS committed (executor must commit it alongside the fixture) per the atomic-commit-coupling rule in Pitfall 4 — but mark it with a header comment "one-shot migration; safe to re-run (idempotent)" and reference Phase 33 D-15 + RESEARCH.md anti-pattern "Do NOT use cat / heredoc" so a future reader understands its purpose.
  </action>
  <verify>
    <automated>node scripts/_migrate-uat-fixture.mjs && jq -e '.schema_version == 1 and (.iterations | map(has("scroll_y") and has("viewport_width") and has("viewport_height") and has("selected_node_xpath")) | all) and (.iterations | length == 10)' tests/e2e/fixtures/uat-phase32-llm-report.json</automated>
  </verify>
  <acceptance_criteria>
    - `jq -e '.schema_version == 1' tests/e2e/fixtures/uat-phase32-llm-report.json` exits 0
    - `jq -e '.iterations | length == 10' tests/e2e/fixtures/uat-phase32-llm-report.json` exits 0
    - `jq -e '[.iterations[].scroll_y, .iterations[].viewport_width, .iterations[].viewport_height, .iterations[].selected_node_xpath] | all(. == null)' tests/e2e/fixtures/uat-phase32-llm-report.json` exits 0 (every value is null since the original fixture had no capture data)
    - `jq -e '.iterations[0] | keys | index("scroll_y") != null and index("viewport_width") != null and index("viewport_height") != null and index("selected_node_xpath") != null' tests/e2e/fixtures/uat-phase32-llm-report.json` exits 0
    - Re-running `node scripts/_migrate-uat-fixture.mjs` a second time leaves the file byte-identical: `cp tests/e2e/fixtures/uat-phase32-llm-report.json /tmp/before.json && node scripts/_migrate-uat-fixture.mjs && diff /tmp/before.json tests/e2e/fixtures/uat-phase32-llm-report.json` exits 0
    - scripts/_migrate-uat-fixture.mjs contains a header comment naming Phase 33 D-15 and explaining idempotency
  </acceptance_criteria>
  <done>Fixture re-stamped in place with schema_version: 1 and four new null-valued keys on every iteration; migration script committed and idempotent on re-run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend appendLlmIteration with split nullable/non-nullable field lists (D-13)</name>
  <files>tests/e2e/lib/llm-report.js, tests/unit/llm-report.test.js</files>
  <read_first>
    - tests/e2e/lib/llm-report.js (current REQUIRED_ENTRY_FIELDS line 55 and appendLlmIteration body lines 215-234)
    - tests/unit/llm-report.test.js (existing Test 12 pattern at lines 279-301 and the makeIteration helper at lines 63-90)
    - .planning/phases/33-re-run-validator/33-RESEARCH.md §"Common Pitfalls" Pitfall 3 (option-a split-list rationale)
    - .planning/phases/33-re-run-validator/33-PATTERNS.md §"tests/e2e/lib/llm-report.js" section (target shape of the split-list extension)
    - .planning/phases/33-re-run-validator/33-CONTEXT.md D-13 (key-must-be-present-null-permitted contract)
  </read_first>
  <behavior>
    - `appendLlmIteration(reportPath, iter)` where `iter` is missing any of `iteration_n`, `iso`, `classification` throws `Error("appendLlmIteration: missing required field '<f>'")` (existing behavior unchanged).
    - `appendLlmIteration(reportPath, iter)` where `iter.iteration_n === null` (or any of the original 3 fields is null) throws — non-null strictness preserved for the original 3.
    - `appendLlmIteration(reportPath, iter)` where `iter` is missing `scroll_y` (or any of viewport_width / viewport_height / selected_node_xpath) throws `Error("appendLlmIteration: missing required field '<f>' (null permitted)")` — message must include the "(null permitted)" suffix to distinguish from the non-null check.
    - `appendLlmIteration(reportPath, iter)` where `iter.scroll_y === null` AND the four new keys are all present AND the original 3 fields are non-null DOES NOT throw.
    - The existing `REQUIRED_ENTRY_FIELDS` export, if present, may be removed only after `grep -rn "REQUIRED_ENTRY_FIELDS" --include='*.js' --include='*.mjs' .` confirms no external import; otherwise keep it as a re-export alias `export const REQUIRED_ENTRY_FIELDS = REQUIRED_NONNULL_FIELDS;` to avoid breaking anything.
  </behavior>
  <action>
    In tests/e2e/lib/llm-report.js, replace the single `REQUIRED_ENTRY_FIELDS` declaration at line 55 with two declarations: `REQUIRED_NONNULL_FIELDS = ['iteration_n', 'iso', 'classification']` (keep the existing JSDoc comment, update it to say "non-null required") and `REQUIRED_NULLABLE_FIELDS = ['scroll_y', 'viewport_width', 'viewport_height', 'selected_node_xpath']` with a new JSDoc explaining "Phase 33 D-13: keys required, null permitted (pre-browser failure paths cannot capture viewport state)". In the `appendLlmIteration` body at lines 215-220, replace the single validation loop with TWO loops: first loop iterates `REQUIRED_NONNULL_FIELDS` with the existing `=== undefined || === null` check and the existing error message `missing required field '${f}'`; second loop iterates `REQUIRED_NULLABLE_FIELDS` with the check `!(f in (iteration ?? {}))` and the error message `missing required field '${f}' (null permitted)`. Run `grep -rn "REQUIRED_ENTRY_FIELDS" --include='*.js' --include='*.mjs' .` — if the only hit is the declaration itself, delete the old name; otherwise add `export const REQUIRED_ENTRY_FIELDS = REQUIRED_NONNULL_FIELDS;` immediately after the new constants to preserve any external import. In tests/unit/llm-report.test.js, update the `makeIteration` helper at lines 63-90 to add the four new fields to the default object (`scroll_y: 0, viewport_width: 1280, viewport_height: 720, selected_node_xpath: '/html/body[1]/div[1]'`) so existing Tests 1-11 still pass. After Test 12c (whichever is the last existing Test 12 case), append four new test cases — Test 12d (missing scroll_y), Test 12e (missing viewport_width), Test 12f (missing viewport_height), Test 12g (missing selected_node_xpath) — each follows the existing Test 12 shape: build a baseline valid iteration via `makeIteration({classification: 'PASS'})`, `delete baseValid.<field>`, expect `appendLlmIteration` to throw matching `/<field>/`. Then append Test 12h (permits null on the four capture fields): build `makeIteration({classification: 'LLM_API_ERROR', scroll_y: null, viewport_width: null, viewport_height: null, selected_node_xpath: null})` and expect `appendLlmIteration(reportPath, ...)`.not.toThrow(). Append Test 12i (still rejects null on the original 3 non-null fields): build `makeIteration({classification: null})` and expect throw matching `/classification/`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/llm-report.test.js --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "REQUIRED_NONNULL_FIELDS\s*=\s*\[.*'iteration_n'.*'iso'.*'classification'.*\]" tests/e2e/lib/llm-report.js` returns exactly one line
    - `grep -n "REQUIRED_NULLABLE_FIELDS\s*=\s*\[.*'scroll_y'.*'viewport_width'.*'viewport_height'.*'selected_node_xpath'.*\]" tests/e2e/lib/llm-report.js` returns exactly one line
    - `grep -n "missing required field.*null permitted" tests/e2e/lib/llm-report.js` returns at least one line (the new error message string)
    - `grep -c "for (const f of REQUIRED_" tests/e2e/lib/llm-report.js` outputs `2` (two validation loops, one per list)
    - `npx vitest run tests/unit/llm-report.test.js --reporter=basic` exits 0 with all tests passing including the new Test 12d, 12e, 12f, 12g, 12h, 12i
    - `grep -cE "Test 12[d-i]" tests/unit/llm-report.test.js` outputs `6`
    - Existing tests 1-11 in tests/unit/llm-report.test.js still pass (no `Test 11` lookup needed — full file green is the contract)
  </acceptance_criteria>
  <done>tests/e2e/lib/llm-report.js carries two field lists and a split validation loop with distinct error messages; tests/unit/llm-report.test.js has six new test cases proving missing-key throws AND null-permitted semantics; full vitest run on tests/unit/llm-report.test.js exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend the UAT fixture schema test with explicit presence assertions</name>
  <files>tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js</files>
  <read_first>
    - tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js (existing structure lines 64-85)
    - tests/e2e/fixtures/uat-phase32-llm-report.json (re-stamped fixture from Task 1)
    - .planning/phases/33-re-run-validator/33-PATTERNS.md §"tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js" (target new-assertion shape)
    - .planning/phases/33-re-run-validator/33-CONTEXT.md D-15 (re-stamped fixture must carry the 4 keys AND schema_version)
  </read_first>
  <behavior>
    - Running `npx vitest run tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js` exits 0.
    - The existing round-trip test "every iteration passes appendLlmIteration schema guard (REQUIRED_ENTRY_FIELDS)" passes (it auto-extends with Task 2's implementation; no test code change needed there).
    - A new test "schema_version: 1 at top level (D-15 Phase 33)" passes against the re-stamped fixture.
    - A new test "every iteration has 4 capture-state keys present (RERUN-03)" passes — asserts every iteration entry has scroll_y, viewport_width, viewport_height, selected_node_xpath keys (values may be null).
  </behavior>
  <action>
    In tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js, locate the existing describe block that contains the lines starting "every iteration passes appendLlmIteration schema guard" (around line 75). Leave the existing tests untouched. After that test, append exactly two new tests using the same `it.skipIf(!fs.existsSync(FIXTURE))` guard pattern: first new test named `'schema_version: 1 at top level (D-15 Phase 33)'` reads the fixture JSON and asserts `expect(fixture.schema_version).toBe(1)`. Second new test named `'every iteration has 4 capture-state keys present (RERUN-03)'` reads the fixture and loops `for (const iter of fixture.iterations)` asserting `expect(iter).toHaveProperty('scroll_y')`, `toHaveProperty('viewport_width')`, `toHaveProperty('viewport_height')`, `toHaveProperty('selected_node_xpath')` (use toHaveProperty so null values pass).
  </action>
  <verify>
    <automated>npx vitest run tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js --reporter=basic` exits 0
    - `grep -c "schema_version: 1 at top level" tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js` outputs at least 1
    - `grep -c "every iteration has 4 capture-state keys present" tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js` outputs at least 1
    - `grep -c "toHaveProperty('scroll_y')" tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js` outputs at least 1
    - The existing test "every iteration passes appendLlmIteration schema guard" still exists (no removal): `grep -c "every iteration passes appendLlmIteration schema guard" tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js` outputs at least 1
  </acceptance_criteria>
  <done>Schema test file gains two new explicit presence assertions covering schema_version and the four capture-state keys; full vitest run on the file exits 0.</done>
</task>

<task type="auto">
  <name>Task 4: Run full test:src + lint to confirm atomic landing</name>
  <files>(no file modifications; verification only)</files>
  <read_first>
    - .planning/phases/33-re-run-validator/33-RESEARCH.md §"Common Pitfalls" Pitfall 4 (atomic-commit ordering — this task is the gate that proves no intermediate failure)
    - .planning/phases/33-re-run-validator/33-VALIDATION.md §"Sampling Rate" ("After every plan wave" entry)
  </read_first>
  <behavior>
    - `npm run test:src` exits 0 with no failures (covers tests/unit/* and tests/e2e/fixtures/*.schema.test.js).
    - `npm run lint` exits 0 (no new lint violations introduced by the changes to llm-report.js).
    - The two changes in this plan (D-13 split-list + D-15 fixture re-stamp + schema test extension) MUST be staged for a SINGLE git commit before the plan closes — the executor verifies this by running `git status --short` and confirming the only modified files are the five listed in `files_modified` frontmatter (plus the new scripts/_migrate-uat-fixture.mjs).
  </behavior>
  <action>
    Run `npm run test:src` and confirm exit 0. Run `npm run lint` and confirm exit 0. Then run `git status --short` and confirm the set of modified/added files matches exactly: M tests/e2e/lib/llm-report.js, M tests/unit/llm-report.test.js, M tests/e2e/fixtures/uat-phase32-llm-report.json, M tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js, ?? scripts/_migrate-uat-fixture.mjs (or M if it already existed). If any other file is modified, investigate. Once green, the executor stages all five files for a single commit (orchestrated separately by execute-plan; this task simply verifies the green-and-coupled state).
  </action>
  <verify>
    <automated>npm run test:src && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:src` exits 0
    - `npm run lint` exits 0
    - `git status --short tests/e2e/lib/llm-report.js tests/unit/llm-report.test.js tests/e2e/fixtures/uat-phase32-llm-report.json tests/e2e/fixtures/uat-phase32-llm-report.schema.test.js scripts/_migrate-uat-fixture.mjs | wc -l` is at least 5 (all five files in modified/added state)
    - `git status --short | grep -v -E "tests/e2e/lib/llm-report\.js|tests/unit/llm-report\.test\.js|tests/e2e/fixtures/uat-phase32-llm-report\.(json|schema\.test\.js)|scripts/_migrate-uat-fixture\.mjs" | grep -v '^??' | wc -l` outputs 0 (no unexpected tracked-file modifications outside the plan's scope)
  </acceptance_criteria>
  <done>Full test:src and lint both green; the D-13 and D-15 changes are staged together for atomic commit per Pitfall 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 33 Plan 01 is pure-source-code refactor + fixture re-stamp. No untrusted input crosses any new boundary; no external services touched. The fixture is committed data read only by tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01-01 | Tampering | scripts/_migrate-uat-fixture.mjs | accept | One-shot script; idempotency check (`if (!('scroll_y' in it))`) protects against double-application. Script is committed for audit but invoked manually; no automation calls it. |
| T-33-01-02 | Information Disclosure | tests/e2e/fixtures/uat-phase32-llm-report.json | accept | Fixture contains real UAT data already committed in Phase 32; no PII, no secrets (verified via Phase 32 sign-off). The four new null values add no new information. |
| T-33-01-03 | Denial of Service | appendLlmIteration validation loop | accept | Two-loop validation adds O(7) string comparisons per iteration call; existing call rate is ≤1 per llm-driver iteration (≤20/run). No DoS surface. |
</threat_model>

<verification>
After all tasks complete:
- `npm run test:src` exits 0
- `npm run lint` exits 0
- `jq -e '.schema_version == 1 and (.iterations | map(has("scroll_y") and has("viewport_width") and has("viewport_height") and has("selected_node_xpath")) | all)' tests/e2e/fixtures/uat-phase32-llm-report.json` exits 0
- `grep -c "REQUIRED_NULLABLE_FIELDS" tests/e2e/lib/llm-report.js` outputs at least 2 (declaration + usage)
- All five modified files staged together for a single commit (executor verifies via `git status --short` before closing the plan).
</verification>

<success_criteria>
- D-13 (REQUIRED_ENTRY_FIELDS split + four new keys required-with-null-permitted) implemented in tests/e2e/lib/llm-report.js per option (a) split-list approach
- D-15 (UAT fixture re-stamped in place with schema_version: 1 and four new null-valued keys on every iteration) shipped
- Pitfall 3 mitigated (null permitted on the 4 new keys without breaking existing strictness)
- Pitfall 4 mitigated (D-13 and D-15 land in same atomic commit; test:src never red mid-PR)
- Pitfall 1 mitigated (schema-guard now catches silent omissions on future call sites)
- RERUN-03 schema-side requirements fully satisfied for downstream plans
</success_criteria>

<output>
Create `.planning/phases/33-re-run-validator/33-01-SUMMARY.md` when done.
</output>
