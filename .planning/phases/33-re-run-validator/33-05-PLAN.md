---
phase: 33-re-run-validator
plan: 05
type: execute
wave: 3
depends_on: ["33-02"]
files_modified:
  - eslint.config.js
  - tests/e2e/scripts/e2e-lint-rerun-guard.test.js
autonomous: true
requirements: [RERUN-04]
requirements_addressed: [RERUN-04]

must_haves:
  truths:
    - "D-16: new ESLint files block matching tests/e2e/lib/rerun-validator.js (per-file, not glob), parallel to the existing pdf-verifier.js block"
    - "eslint.config.js contains a `files: ['tests/e2e/lib/rerun-validator.js']` block with a `no-restricted-imports` rule banning patterns matching src/"
    - "Running `npm run lint` on a temporarily-modified rerun-validator.js that imports from src/ exits non-zero and emits a message containing 'RERUN-04 independence claim' (or 'rerun-validator.js must not import from src/')"
    - "The new ESLint block is parallel to the existing pdf-verifier.js block — per-file (not glob) per D-16 explicit choice"
    - "Running `npm run lint` on the current rerun-validator.js (no src/ imports) exits 0"
  artifacts:
    - path: "eslint.config.js"
      provides: "New per-file ESLint block for tests/e2e/lib/rerun-validator.js with no-restricted-imports rule mirroring the pdf-verifier.js block"
      contains: "rerun-validator.js"
    - path: "tests/e2e/scripts/e2e-lint-rerun-guard.test.js"
      provides: "Scripted smoke test: temporarily inject a src/ import into rerun-validator.js, run npm run lint, expect non-zero exit + the RERUN-04 message, restore original file"
      min_lines: 50
  key_links:
    - from: "eslint.config.js"
      to: "tests/e2e/lib/rerun-validator.js"
      via: "files pattern in flat config block"
      pattern: "tests/e2e/lib/rerun-validator.js"
    - from: "tests/e2e/scripts/e2e-lint-rerun-guard.test.js"
      to: "eslint.config.js"
      via: "spawnSync npm run lint after temporary file mutation"
      pattern: "spawnSync.*npm.*lint"
---

<objective>
Land D-16: extend `eslint.config.js` with a per-file `no-restricted-imports` block for `tests/e2e/lib/rerun-validator.js`, mirroring the existing pdf-verifier.js block at lines 50-71. Ship a scripted smoke test that proves the rule fires by temporarily injecting a `src/` import into rerun-validator.js, spawning `npm run lint`, asserting non-zero exit + the RERUN-04 message in stdout/stderr, and restoring the original file content.

Purpose: Plan 33-02 created rerun-validator.js with no src/ imports (acceptance criterion enforced via grep). RERUN-04 requires this to be ENFORCED at lint time so future edits can't silently re-couple the module to src/. Without lint enforcement, the independence claim is documentation-only.
Output: A new files block in eslint.config.js + a vitest-driven lint-guard smoke test that proves the rule fires on a deliberate violation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-re-run-validator/33-CONTEXT.md
@.planning/phases/33-re-run-validator/33-RESEARCH.md
@.planning/phases/33-re-run-validator/33-PATTERNS.md
@eslint.config.js
@tests/e2e/lib/rerun-validator.js
@tests/e2e/scripts/e2e-explore-ci-guard.test.js

<interfaces>
<!-- Key contracts. -->

Existing eslint.config.js block to clone (lines 50-71):
```
{
  files: ['tests/e2e/lib/pdf-verifier.js'],
  rules: {
    'no-restricted-imports': ['error', {
      patterns: [
        {
          group: [
            '**/src/**',
            '../../../src/**',
            '../../src/**',
            '../src/**',
            '/src/**',
          ],
          message:
            'pdf-verifier.js must not import from src/ — VFY-02 independence claim. ' +
            'Use a fresh implementation; mirror production logic conceptually, do not reuse it. ' +
            'See .planning/phases/28-independent-pdf-verifier/28-RESEARCH.md §"Pattern 4".',
        },
      ],
    }],
  },
},
```

D-16: clone EXACTLY (not a glob like `{pdf-verifier,rerun-validator}.js`); separate per-file block keeps the audit story readable.

New block target (per 33-PATTERNS.md §"eslint.config.js"):
- files: ['tests/e2e/lib/rerun-validator.js']
- Same patterns array
- Message: 'rerun-validator.js must not import from src/ — RERUN-04 independence claim. Use a fresh implementation; mirror production logic conceptually, do not reuse it. See .planning/phases/33-re-run-validator/33-RESEARCH.md §"Pattern 3".'

Pitfall 7: per-file path is exact-match; a rename also requires updating npm script + plan refs so misalignment will be caught. Acceptable trade-off per D-16.

Lint command (package.json line 15): `"lint": "eslint tests/e2e/lib/"`

Smoke test contract (RESEARCH.md §"Common Pitfalls" Pitfall 7):
- Write a temp violating file content into tests/e2e/lib/rerun-validator.js (preserving the original via fs.readFileSync into a memory buffer)
- Run `npm run lint` via spawnSync
- Assert non-zero exit
- Assert stdout/stderr contains 'RERUN-04 independence claim' or 'rerun-validator.js must not import from src/'
- Restore the original file content via fs.writeFileSync (CRITICAL: must use try/finally so a test failure does not leave the codebase broken)
- Final assertion: original content is byte-identical post-restore
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the lint-guard smoke test scaffold</name>
  <files>tests/e2e/scripts/e2e-lint-rerun-guard.test.js</files>
  <read_first>
    - tests/e2e/scripts/e2e-explore-ci-guard.test.js (spawnSync pattern for invoking external commands)
    - tests/e2e/scripts/e2e-explore-phase-flag.test.js (spawnSync test scaffolding)
    - .planning/phases/33-re-run-validator/33-RESEARCH.md §"Common Pitfalls" Pitfall 7 (lint rule scope mismatch — the smoke test proves the rule fires)
    - .planning/phases/33-re-run-validator/33-CONTEXT.md D-16 (per-file scoping)
    - tests/e2e/lib/rerun-validator.js (the file the test will temporarily mutate then restore)
  </read_first>
  <behavior>
    - Test file imports `describe, it, expect, beforeAll, afterAll` from 'vitest', `spawnSync` from 'node:child_process', `fs`, `path`, `fileURLToPath`.
    - Constants: RERUN_VALIDATOR_PATH = `path.resolve(__dirname, '../../../tests/e2e/lib/rerun-validator.js')`.
    - Test uses try/finally to GUARANTEE the original file content is restored even on assertion failure.
    - Test name: "ESLint blocks src/ imports in rerun-validator.js (RERUN-04 independence claim)".
    - Behavior: read original content; write modified content that adds a forbidden import line (e.g., `import 'src/shared/matching.js';` at the top after the existing imports); spawnSync('npm', ['run', 'lint']); restore original content; finally: assert non-zero exit AND stdout/stderr contains 'RERUN-04' OR 'rerun-validator.js must not import from src/'.
    - Pre-implementation (RED state): the test will fail because Task 2's ESLint config block doesn't exist yet — npm run lint will exit 0 even with the violating import. Confirmed by running before Task 2.
    - After Task 2 implementation: test passes.
    - A second "positive" test asserts npm run lint exits 0 on the unmodified rerun-validator.js (sanity check that the rule doesn't fire on legitimate code).
  </behavior>
  <action>
    Create tests/e2e/scripts/e2e-lint-rerun-guard.test.js. Imports as listed. Use `const __dirname = path.dirname(fileURLToPath(import.meta.url));` and `const RERUN_VALIDATOR_PATH = path.resolve(__dirname, '../../../tests/e2e/lib/rerun-validator.js');`. Use a `describe('ESLint RERUN-04 guard', ...)` block with two tests: (1) `it('npm run lint exits 0 on current rerun-validator.js (sanity check)', () => { const r = spawnSync('npm', ['run', 'lint'], { encoding: 'utf8', timeout: 60000 }); expect(r.status).toBe(0); })`. (2) `it('npm run lint blocks src/ imports in rerun-validator.js (RERUN-04 independence claim)', () => { ... try/finally restore ... })`. In test (2): `const originalContent = fs.readFileSync(RERUN_VALIDATOR_PATH, 'utf8');` BEFORE the try block. Inside try: `const violatingContent = originalContent.replace(/^(import.*\n)/, '$1import "../../../src/shared/matching.js";\n');` (injects a violating import after the first existing import; this ensures we hit the patterns matching `../../../src/**`). `fs.writeFileSync(RERUN_VALIDATOR_PATH, violatingContent);` then `const r = spawnSync('npm', ['run', 'lint'], { encoding: 'utf8', timeout: 60000 });` and capture `output = (r.stdout || '') + (r.stderr || '')`. Inside finally: `fs.writeFileSync(RERUN_VALIDATOR_PATH, originalContent);` — CRITICAL: this restore line MUST be inside finally, not after the try. After finally: assertions — `expect(r.status).not.toBe(0)` (lint must fail with violating import); `expect(output).toMatch(/RERUN-04|rerun-validator.js must not import from src\//i)` (the error message names the rule). Also assert that the file was restored correctly via `expect(fs.readFileSync(RERUN_VALIDATOR_PATH, 'utf8')).toBe(originalContent)` (defensive — this should always pass given the finally block worked). Set a longer test timeout via `it.concurrent` is NOT used (we need sequential execution to avoid races); add `{ timeout: 90000 }` as the third arg to `it(...)` since npm run lint can take 10-20 seconds. Add a top-level `beforeAll` that reads original content into a module-scope variable as a paranoid second restore-on-process-exit guard: `process.once('exit', () => { try { if (originalSavedContent) fs.writeFileSync(RERUN_VALIDATOR_PATH, originalSavedContent); } catch {} });` — this catches the pathological case where the process dies between writeFileSync and finally.
  </action>
  <verify>
    <automated>test -f tests/e2e/scripts/e2e-lint-rerun-guard.test.js && grep -c "it(" tests/e2e/scripts/e2e-lint-rerun-guard.test.js</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/scripts/e2e-lint-rerun-guard.test.js exists
    - File contains at least 2 `it(...)` test cases: `grep -c "it(" tests/e2e/scripts/e2e-lint-rerun-guard.test.js` outputs at least 2
    - File uses spawnSync('npm', ['run', 'lint'], ...): `grep -c "spawnSync.*npm.*run.*lint" tests/e2e/scripts/e2e-lint-rerun-guard.test.js` outputs at least 1
    - File uses try/finally restore pattern: `grep -c "try {" tests/e2e/scripts/e2e-lint-rerun-guard.test.js` outputs at least 1 AND `grep -c "} finally {" tests/e2e/scripts/e2e-lint-rerun-guard.test.js` outputs at least 1
    - File asserts the RERUN-04 message: `grep -cE "toMatch.*RERUN-04|RERUN-04 independence claim" tests/e2e/scripts/e2e-lint-rerun-guard.test.js` outputs at least 1
    - File has a process-exit safety net: `grep -c "process.once('exit'" tests/e2e/scripts/e2e-lint-rerun-guard.test.js` outputs at least 1
    - Pre-implementation run (RED state — Task 2 not done yet): `npx vitest run tests/e2e/scripts/e2e-lint-rerun-guard.test.js --reporter=basic 2>&1 | grep -c "FAIL\|fail"` outputs at least 1 (the violating-import test FAILS because the ESLint rule does not yet exist — npm run lint exits 0 even with the bad import). After Task 2, this turns green.
  </acceptance_criteria>
  <done>tests/e2e/scripts/e2e-lint-rerun-guard.test.js exists with 2 tests (sanity-check + violating-import); restore pattern uses try/finally + process.once('exit') belt-and-suspenders; pre-implementation run is RED (lint exits 0 on the violating import because the new block doesn't exist yet).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the rerun-validator.js block to eslint.config.js</name>
  <files>eslint.config.js</files>
  <read_first>
    - eslint.config.js (existing block at lines 50-71 — the exact shape to clone)
    - .planning/phases/33-re-run-validator/33-PATTERNS.md §"eslint.config.js" (target new block)
    - .planning/phases/33-re-run-validator/33-CONTEXT.md D-16 (per-file scoping, not glob)
    - tests/e2e/scripts/e2e-lint-rerun-guard.test.js (the contract from Task 1)
  </read_first>
  <behavior>
    - A new block appears in eslint.config.js after the existing pdf-verifier.js block (lines 50-71 in the original) and before the closing `]` of the exported array.
    - The new block uses `files: ['tests/e2e/lib/rerun-validator.js']` — exact-path string, NOT a glob, per D-16.
    - The patterns array is byte-identical to the pdf-verifier.js block: `'**/src/**', '../../../src/**', '../../src/**', '../src/**', '/src/**'`.
    - The message says "rerun-validator.js must not import from src/ — RERUN-04 independence claim. Use a fresh implementation; mirror production logic conceptually, do not reuse it. See .planning/phases/33-re-run-validator/33-RESEARCH.md §\"Pattern 3\"."
    - Running `npm run lint` on the unmodified rerun-validator.js exits 0 (no false positives).
    - Running `npm run lint` after Task 1's test injects a `import 'src/...'` line into rerun-validator.js exits non-zero with the RERUN-04 message.
    - eslint.config.js parses as valid JavaScript: `node --check eslint.config.js` exits 0.
  </behavior>
  <action>
    Open eslint.config.js. Locate the existing block at lines 50-71 starting with `files: ['tests/e2e/lib/pdf-verifier.js'],` and ending with the closing `},`. Immediately AFTER that block's closing `},` (and BEFORE the next block / closing array bracket), insert a new block with: `files: ['tests/e2e/lib/rerun-validator.js'],` then `rules: { 'no-restricted-imports': ['error', { patterns: [ { group: [ '**/src/**', '../../../src/**', '../../src/**', '../src/**', '/src/**', ], message: 'rerun-validator.js must not import from src/ — RERUN-04 independence claim. ' + 'Use a fresh implementation; mirror production logic conceptually, do not reuse it. ' + 'See .planning/phases/33-re-run-validator/33-RESEARCH.md §"Pattern 3".', }, ], }], },` (preserve the multi-line string concatenation style used in the pdf-verifier.js block — three string-literal segments concatenated with `+`). Close with `},`. Verify the file parses with `node --check eslint.config.js`. Run `npm run lint` and confirm exit 0 (the unmodified rerun-validator.js has no src/ imports so the rule does not fire). Then run `npx vitest run tests/e2e/scripts/e2e-lint-rerun-guard.test.js --reporter=basic` and confirm both tests pass — the sanity-check test passes because npm run lint is green on unmodified code; the violating-import test passes because Task 2's new block fires when the file is temporarily mutated.
  </action>
  <verify>
    <automated>node --check eslint.config.js && npm run lint && npx vitest run tests/e2e/scripts/e2e-lint-rerun-guard.test.js --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `node --check eslint.config.js` exits 0
    - `npm run lint` exits 0 (unmodified rerun-validator.js has no src/ imports)
    - `grep -c "files: \['tests/e2e/lib/rerun-validator.js'\]" eslint.config.js` outputs exactly 1 (per D-16: separate per-file block, not glob)
    - `grep -c "RERUN-04 independence claim" eslint.config.js` outputs at least 1
    - The new block is parallel to the pdf-verifier.js block in structure: `grep -c "no-restricted-imports.*error" eslint.config.js` outputs at least 2 (one for pdf-verifier.js, one for rerun-validator.js)
    - The patterns array in the new block uses the exact same five string entries as the pdf-verifier.js block: `grep -A 6 "tests/e2e/lib/rerun-validator.js" eslint.config.js | grep -cE "'\*\*/src/\*\*'|'\\.\\./\\.\\./\\.\\./src/\\*\\*'|'\\.\\./\\.\\./src/\\*\\*'|'\\.\\./src/\\*\\*'|'/src/\\*\\*'"` outputs at least 5
    - `npx vitest run tests/e2e/scripts/e2e-lint-rerun-guard.test.js --reporter=basic` exits 0 (both tests pass: sanity-check green + violating-import correctly flagged)
  </acceptance_criteria>
  <done>eslint.config.js carries the new per-file block for rerun-validator.js with the same patterns and a RERUN-04 message; npm run lint is green on the unmodified file; the lint-guard test proves the rule fires on a violating import and the file is correctly restored.</done>
</task>

<task type="auto">
  <name>Task 3: Run npm run test:src + npm run lint + the new lint-guard test together</name>
  <files>(no file modifications; verification only)</files>
  <read_first>
    - .planning/phases/33-re-run-validator/33-VALIDATION.md §"Sampling Rate" ("Before /gsd:verify-work" gate)
  </read_first>
  <behavior>
    - The full src test suite passes.
    - npm run lint passes.
    - The lint-guard test passes (proves the new block fires on a violation and the file is restored after).
    - No file in `git status --short` is in an unexpected state — specifically, tests/e2e/lib/rerun-validator.js MUST NOT have a stray injected import after the lint-guard test runs.
  </behavior>
  <action>
    Run `npm run test:src`. Confirm exit 0. Run `npm run lint`. Confirm exit 0. Run `git status --short tests/e2e/lib/rerun-validator.js` — confirm the output is empty (file is in its committed state; no stray mutation left behind by the lint-guard test). Finally, run `git status --short` and confirm the only modified files are the two listed in this plan's `files_modified` (eslint.config.js and tests/e2e/scripts/e2e-lint-rerun-guard.test.js).
  </action>
  <verify>
    <automated>bash -c 'npm run test:src && npm run lint && [ -z "$(git status --short tests/e2e/lib/rerun-validator.js)" ]'</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:src` exits 0
    - `npm run lint` exits 0
    - `git status --short tests/e2e/lib/rerun-validator.js` outputs nothing (file is clean — lint-guard test's restore worked)
    - `git status --short | grep -v -E "eslint\.config\.js|tests/e2e/scripts/e2e-lint-rerun-guard\.test\.js" | grep -v '^??' | wc -l` outputs 0 (only the two expected files are modified)
  </acceptance_criteria>
  <done>Full test:src + lint green; rerun-validator.js is in a clean committed state (lint-guard restore worked correctly); only the two expected files are modified by this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ESLint flat config → rerun-validator.js | Per-file scope rule; D-16 chose exact-path matching (not glob) to make the audit story per-file readable. Rename of the file silently invalidates the rule (Pitfall 7) but is caught downstream because rename also requires updating npm script + plan refs. |
| Lint-guard test → rerun-validator.js file content | The test temporarily mutates the file then restores it; try/finally + process.once('exit') guarantee restoration even on test failure. Risk: parallel test runs racing on the same file. Mitigation: vitest runs each test file in sequence by default; the lint-guard test does not use `it.concurrent`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-05-01 | Tampering | rerun-validator.js file content during lint-guard test | mitigate | Test uses try/finally to restore content; process.once('exit') is a belt-and-suspenders second restore path. Acceptance criterion in Task 3 asserts `git status --short tests/e2e/lib/rerun-validator.js` is empty post-test. |
| T-33-05-02 | Repudiation | RERUN-04 independence claim | mitigate | ESLint rule provides a build-time enforcement of the documentation claim; without this rule the claim is only a comment, easily violated by future edits. Lint-guard test proves the rule actually fires. |
| T-33-05-03 | Tampering | eslint.config.js | accept | Single file edited; per-file block pattern audited by Plan 33-05 acceptance criteria (grep gates). |
</threat_model>

<verification>
- `node --check eslint.config.js` exits 0
- `npm run lint` exits 0 (no false positives on the legitimate rerun-validator.js)
- `npx vitest run tests/e2e/scripts/e2e-lint-rerun-guard.test.js --reporter=basic` exits 0 (both tests pass)
- `npm run test:src` exits 0
- `grep -c "tests/e2e/lib/rerun-validator.js" eslint.config.js` outputs at least 1
- `grep -c "RERUN-04 independence claim" eslint.config.js` outputs at least 1
- `git status --short tests/e2e/lib/rerun-validator.js` outputs nothing (no stray mutation)
</verification>

<success_criteria>
- RERUN-04 enforced at lint time: importing from src/ in rerun-validator.js fails npm run lint with a clear message
- D-16 honored (per-file block, not glob; mirrors pdf-verifier.js block exactly)
- Pitfall 7 mitigated by the lint-guard smoke test (proves the rule fires on a violating import)
- The lint-guard test is safe to re-run: try/finally + process.once('exit') guarantee file restoration
</success_criteria>

<output>
Create `.planning/phases/33-re-run-validator/33-05-SUMMARY.md` when done.
</output>
