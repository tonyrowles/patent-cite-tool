---
phase: 36-quarantine-ci-integration-+-pipeline-orchestrator
plan: "03"
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - scripts/e2e-report-issue.mjs
  - tests/unit/e2e-report-issue.test.js
autonomous: true
requirements: [QUAR-04]
requirements_addressed: [QUAR-04]

must_haves:
  truths:
    - "D-15: e2e-report-issue.mjs accepts --source quarantine; it reuses the existing processReport per-case report.json path and stamps the e2e-quarantine label instead of e2e-nightly"
    - "D-15: the label is parameterized through makeRealGhClient(repo, label) so listOpenNightlyIssues + createIssue use e2e-quarantine; existing e2e-nightly behavior is byte-identical (default param)"
    - "D-15 / Phase 35 CR-01: the quarantine branch routes through processReport, inheriting sanitizeCaseId — no parallel filing path that skips the guard"
    - "SC-4: a mock-gh test asserts --source quarantine produces the e2e-quarantine label (NOT e2e-nightly)"
  artifacts:
    - path: "scripts/e2e-report-issue.mjs"
      provides: "--source quarantine branch + label-parameterized makeRealGhClient; regression/triage paths unchanged"
      contains: "quarantine"
    - path: "tests/unit/e2e-report-issue.test.js"
      provides: "label-assertion test for --source quarantine (e2e-quarantine) + sanitizeCaseId-skip assertion on the quarantine path"
      contains: "e2e-quarantine"
  key_links:
    - from: "scripts/e2e-report-issue.mjs CLI dispatch"
      to: "makeRealGhClient(repo, 'e2e-quarantine')"
      via: "source === 'quarantine' ? makeRealGhClient(repo, 'e2e-quarantine') : makeRealGhClient(repo)"
      pattern: "makeRealGhClient\\(repo, 'e2e-quarantine'\\)"
    - from: "makeRealGhClient label param"
      to: "gh issue create --label / gh api ... -f labels="
      via: "the label argument threaded into createIssue + listOpenNightlyIssues"
      pattern: "--label \\$\\{?label"
---

<objective>
Extend `scripts/e2e-report-issue.mjs` with a `--source quarantine` branch (D-15). The quarantine spec (Plan 36-01) emits the same RPT-01 `report.json` shape as regression, so the existing `processReport` per-case path is reused verbatim — the only change is parameterizing the hardcoded `NIGHTLY_LABEL = 'e2e-nightly'` so the quarantine branch stamps `e2e-quarantine`. Because the quarantine branch routes through `processReport`, it inherits the Phase 35 CR-01 `sanitizeCaseId` shell-injection guard for free (D-15) — no parallel filing path. Add a unit test asserting the `e2e-quarantine` label is used (SC-4) and that a shell-metacharacter case id is skipped on the quarantine path (CR-01 reuse).

Purpose: QUAR-04 (filer side) — quarantine failures file issues under a distinct `e2e-quarantine` label so the weekly digest (Phase 37) and humans can tell them apart from golden-suite failures.
Output: modified `scripts/e2e-report-issue.mjs`, extended `tests/unit/e2e-report-issue.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-quarantine-ci-integration-+-pipeline-orchestrator/36-CONTEXT.md
@.planning/phases/36-quarantine-ci-integration-+-pipeline-orchestrator/36-RESEARCH.md
@.planning/phases/36-quarantine-ci-integration-+-pipeline-orchestrator/36-PATTERNS.md

# The file to modify (self-analog — extend the --source family + parameterize the label)
@scripts/e2e-report-issue.mjs

# The unit test to extend
@tests/unit/e2e-report-issue.test.js

<interfaces>
<!-- Contracts the executor needs. Extracted from scripts/e2e-report-issue.mjs — no exploration required. -->

CURRENT state (the three change sites):

  line 43:   const NIGHTLY_LABEL = 'e2e-nightly';   // module constant baked into makeRealGhClient

  parseSourceArgs (lines 566-618): validates --source ∈ {regression, triage} (line 584 rejects anything else with exit 2).
    Returns { source, triageReportPath }.

  makeRealGhClient(repo) (lines 470-546): returns { listOpenNightlyIssues, createIssue, createIssueWithLabels, listOpenWithSearch, addLabel, commentIssue, filerMetaIssue }.
    - listOpenNightlyIssues (475): `gh api repos/${repo}/issues --method GET -f labels=${NIGHTLY_LABEL} -f state=open --paginate`
    - createIssue (488-490): `gh issue create --title "${escapedTitle}" --label ${NIGHTLY_LABEL} --body-file -`
    NOTE: makeRealGhClient is `export function` (Phase 35 Plan 04 exported it). quarantine-append.mjs imports it — keep it exported and keep the (repo) signature working via a DEFAULT param.

  CLI dispatch (lines 679-760):
    - line 692: const gh = makeRealGhClient(repo);
    - source === 'triage' → mainTriage(...)
    - else (regression): --meta-drift branch (705-739), then default: reads tests/e2e/artifacts/{runId}/report.json (lines 742-749) → processReport(report, { ghClient: gh, runId, repo }) (line 759).

  processReport(report, { ghClient, runId, repo }) (lines 426-464): filters cases, calls sanitizeCaseId (line 435) BEFORE any shell interpolation, then ghClient.createIssue / commentIssue. The label is NOT a processReport arg — it is baked into the gh client. So the quarantine label must be threaded via makeRealGhClient(repo, 'e2e-quarantine').

  sanitizeCaseId (lines 130-140): throws on CASE_ID_RE mismatch (shell metachars, markdown injection). processReport catches the throw and skips the case (lines 434-439).

Existing unit-test patterns (tests/unit/e2e-report-issue.test.js):
  - processReport tests inject a mock ghClient { listOpenNightlyIssues, createIssue, commentIssue } and record calls (analog ~lines 293-374).
  - sanitizeCaseId tests assert throw on metachars (lines 102-111); a processReport "skips invalid case IDs" test exists (~lines 358-373).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --source quarantine + parameterize the label through makeRealGhClient</name>
  <files>scripts/e2e-report-issue.mjs</files>
  <read_first>
    - scripts/e2e-report-issue.mjs (lines 43, 470-546 makeRealGhClient, 566-618 parseSourceArgs, 679-760 CLI dispatch — the four edit sites)
    - 36-RESEARCH.md §"Pattern 3: --source quarantine reuses processReport, parameterizes the label" (the minimal-change recipe + the buildIssueTitle discretion note)
    - 36-PATTERNS.md §"scripts/e2e-report-issue.mjs (MODIFIED)" (Change 1/2/3 + the security note)
  </read_first>
  <action>
    Make four minimal edits to scripts/e2e-report-issue.mjs (D-15). Keep the regression + triage paths byte-identical.

    Change 1 — parseSourceArgs accept quarantine (line 584): change the validity check from
      if (next !== 'regression' && next !== 'triage')
    to also allow 'quarantine':
      if (next !== 'regression' && next !== 'triage' && next !== 'quarantine')
    Update the invalid-value stderr message (line 585-586) to mention quarantine, and update the parseSourceArgs JSDoc (lines 556-560) to document the third value. Leave the `--source triage requires --triage-report` check (line 610) unchanged — it is scoped to triage only, so quarantine (which uses the default report.json path, not --triage-report) is unaffected.

    Change 2 — parameterize the label in makeRealGhClient (lines 470-546): change the signature to
      export function makeRealGhClient(repo, label = NIGHTLY_LABEL)
    and replace the two NIGHTLY_LABEL references inside with the `label` parameter:
      - listOpenNightlyIssues (line 475): `... -f labels=${label} ...`
      - createIssue (line 490): `... --label ${label} ...`
    Leave createIssueWithLabels (used only by the triage path, which passes its own ordered label array) UNCHANGED. The DEFAULT param value = NIGHTLY_LABEL preserves every existing caller: makeRealGhClient(repo) still yields an e2e-nightly client (regression + triage + quarantine-append.mjs import all keep working).

    Change 3 — CLI dispatch quarantine branch (lines 679-760): construct the gh client with the quarantine label when source==='quarantine':
      const gh = source === 'quarantine' ? makeRealGhClient(repo, 'e2e-quarantine') : makeRealGhClient(repo);
    Then route source==='quarantine' through the SAME default report.json block the regression branch uses (lines 742-749 read tests/e2e/artifacts/{runId}/report.json; line 758-759 processReport). Concretely: the `else` branch currently handles regression; restructure so that source==='triage' → mainTriage, and source==='regression' || source==='quarantine' → the report.json + processReport path. The --meta-drift sub-branch (lines 705-739) stays regression-only (do NOT run it for quarantine — quarantine has no DOM-drift pre-flight). Only the gh client's label differs between regression and quarantine; the report path, sanitizeCaseId, fingerprint dedup, and processReport call are all shared.

    Change 4 — header comment: add a line to the top-of-file comment block (lines 1-28) noting "Phase 36 (QUAR-04/D-15): --source quarantine reuses processReport with the e2e-quarantine label."

    DO NOT (anti-patterns, RESEARCH + PATTERNS):
    - Do NOT write a parallel quarantine filing path that bypasses processReport / sanitizeCaseId (CR-01 must be inherited).
    - Do NOT parameterize buildIssueTitle / buildIssueBody (the [e2e-nightly] title prefix may stay — SC-4 only asserts the label arg; keeping the title machinery shared minimizes surface, per RESEARCH Pattern 3 decision point).
    - Do NOT touch the triage path or createIssueWithLabels.
  </action>
  <verify>
    <automated>node --check scripts/e2e-report-issue.mjs && grep -q "makeRealGhClient(repo, 'e2e-quarantine')" scripts/e2e-report-issue.mjs && grep -q "label = NIGHTLY_LABEL" scripts/e2e-report-issue.mjs && node -e "import('./scripts/e2e-report-issue.mjs').then(m=>{const c=m.makeRealGhClient('o/r'); if(typeof c.createIssue!=='function'){console.error('client shape broken');process.exit(1);} console.log('makeRealGhClient default OK');})" && node scripts/e2e-report-issue.mjs --source bogus 2>&1 | grep -qi "invalid --source"</automated>
  </verify>
  <done>e2e-report-issue.mjs parses clean; parseSourceArgs accepts quarantine (and still rejects bogus values with exit 2); makeRealGhClient has a `label = NIGHTLY_LABEL` default and is still exported; the CLI dispatch constructs makeRealGhClient(repo, 'e2e-quarantine') for source==='quarantine' and routes it through processReport. The regression + triage paths are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add the --source quarantine label assertion test + CR-01 sanitizeCaseId-skip assertion</name>
  <files>tests/unit/e2e-report-issue.test.js</files>
  <read_first>
    - tests/unit/e2e-report-issue.test.js (the processReport mock-ghClient block ~lines 293-374; the sanitizeCaseId tests lines 93-120; the "skips invalid case IDs" processReport test ~lines 358-373)
    - 36-RESEARCH.md §"Validation Architecture → Detailed validation specs" item 3 (the SC-4 + CR-01 test recipe — both viable approaches)
    - 36-PATTERNS.md §"tests/unit/e2e-report-issue.test.js (MODIFIED)" (the two approaches + the mock ghClient shape)
  </read_first>
  <action>
    Add a describe block to tests/unit/e2e-report-issue.test.js proving QUAR-04 (filer side) / SC-4. Use the UNIT (pure-function) approach since the existing file already mocks ghClients and avoids execSync (it does not currently mock execSync):

    Test A — label parameterization (SC-4 core): import makeRealGhClient. Because the real client calls execSync (not easily asserted in this unit file), assert the label THREADING at the client-construction boundary instead: spy/stub execSync via vi.mock('node:child_process') OR — simpler and matching the file's style — assert that makeRealGhClient(repo, 'e2e-quarantine') and makeRealGhClient(repo) return clients (both have createIssue/listOpenNightlyIssues) and that the label is wired by driving a thin check. Preferred concrete approach: use vi.spyOn on the child_process execSync import to capture the command string, call client.createIssue('[e2e-nightly] US11427642-claims-1: WRONG_CITATION', 'body'), and assert the captured command contains `--label e2e-quarantine` for the quarantine client and `--label e2e-nightly` for the default client. If mocking execSync proves heavy in this file, fall back to the spawnSync + mock-gh approach (RESEARCH item 3 / PATTERNS approach 2): spawn `node scripts/e2e-report-issue.mjs --source quarantine` with mock-gh on PATH + a fixture report.json under artifacts/{runId}/ and assert the transcript contains `--label e2e-quarantine` (NOT e2e-nightly). Pick whichever keeps the existing test file's conventions; the binding assertion is: the quarantine path uses the e2e-quarantine label and NOT e2e-nightly.

    Test B — CR-01 reuse (sanitizeCaseId live on the quarantine path): mirror the existing "skips invalid case IDs" processReport test. Build a minimal RPT-01 report with one failed case whose id contains a shell metacharacter (e.g. 'US123;rm -rf /'). Inject a mock ghClient that records createIssue/commentIssue calls. Call processReport(report, { ghClient, runId, repo }) — processReport is label-agnostic (label is in the client), so this exercises the same sanitizeCaseId guard the quarantine branch uses. Assert the metachar case produced ZERO createIssue calls (it was skipped, not interpolated). This proves the quarantine path inherits the CR-01 guard (D-15) because it routes through the identical processReport.

    Keep the new tests in a describe('--source quarantine (QUAR-04 / D-15)') block. Do not modify existing tests.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/e2e-report-issue.test.js && grep -q "e2e-quarantine" tests/unit/e2e-report-issue.test.js</automated>
  </verify>
  <done>The new tests pass: one asserts the quarantine path stamps the e2e-quarantine label (and the default stays e2e-nightly), the other asserts a shell-metacharacter case id is skipped by processReport (CR-01 guard live). The full `npx vitest run tests/unit/e2e-report-issue.test.js` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report.json case id → gh CLI | Case ids reach `gh issue create` title/label via execSync string interpolation; sanitizeCaseId is the gate |
| --source flag value → dispatch | CLI input; validated by parseSourceArgs against a closed {regression,triage,quarantine} set |
| label literal → gh --label arg | The quarantine label is a hardcoded literal 'e2e-quarantine' (not user input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03-01 | Tampering | case id shell injection on the quarantine filing path | mitigate | The quarantine branch routes through processReport → sanitizeCaseId (CASE_ID_RE allow-list, line 130/435). No parallel path. Test B asserts a metachar id is skipped. (Phase 35 CR-01 hardened path, reused per D-15.) |
| T-36-03-02 | Tampering | label value injection in `gh issue create --label` | accept | The label is a hardcoded literal 'e2e-quarantine' (not user input) → no interpolation risk. createIssue already escapes `"` in the title (line 488). |
| T-36-03-03 | Elevation | --source accepting an arbitrary value | mitigate | parseSourceArgs validates against the closed {regression,triage,quarantine} set; anything else exits 2. |
| T-36-03-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (zero-new-dependency lock). Test uses vi.spyOn / the existing mock-gh shim. |
</threat_model>

<verification>
- `node --check scripts/e2e-report-issue.mjs` passes; `--source bogus` exits 2; `makeRealGhClient('o/r')` (no label) returns a working e2e-nightly client (back-compat for quarantine-append.mjs's import).
- `npx vitest run tests/unit/e2e-report-issue.test.js` green, including the new SC-4 + CR-01 tests.
- `npm run lint` passes (e2e-report-issue.mjs is in the lint list — line 17).
- `npm run test:src` stays green (regression + triage tests untouched).
</verification>

<success_criteria>
- QUAR-04 (filer side) satisfied: `--source quarantine` files via processReport with the `e2e-quarantine` label; the SC-4 test asserts the label (not e2e-nightly).
- D-15 / CR-01 satisfied: the quarantine path inherits sanitizeCaseId by reusing processReport; the metachar-id-skip test proves it. No parallel filing path.
- Regression + triage behavior byte-identical (default label param, no triage-path edits).
</success_criteria>

<output>
Create `.planning/phases/36-quarantine-ci-integration-+-pipeline-orchestrator/36-03-SUMMARY.md` when done.
</output>
