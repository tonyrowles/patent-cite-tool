---
phase: 37-weekly-analytics-digest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/lib/llm-report.js
  - tests/unit/llm-report.test.js
autonomous: true
requirements: [DIGEST-04]
must_haves:
  truths:
    - "D-01: SUMMARY_KEYS is exported (named export) from tests/e2e/lib/llm-report.js as a frozen 7-element array"
    - "D-01: emptySummary() is rebuilt FROM SUMMARY_KEYS so the summary contract has exactly one source definition"
    - "D-01: the order of SUMMARY_KEYS matches the prior emptySummary() object-literal order (passed → total_cost_usd) so no downstream key ordering shifts"
    - "DIGEST-04: every value classificationToSummaryKey() can return appears in SUMMARY_KEYS, plus total_cost_usd"
  artifacts:
    - path: "tests/e2e/lib/llm-report.js"
      provides: "SUMMARY_KEYS frozen named export; emptySummary() built from it"
      contains: "export const SUMMARY_KEYS"
    - path: "tests/unit/llm-report.test.js"
      provides: "SUMMARY_KEYS export + single-source proof tests"
      contains: "SUMMARY_KEYS"
  key_links:
    - from: "tests/e2e/lib/llm-report.js emptySummary()"
      to: "SUMMARY_KEYS"
      via: "Object.fromEntries(SUMMARY_KEYS.map(...))"
      pattern: "Object\\.fromEntries\\(SUMMARY_KEYS"
---

<objective>
Export `SUMMARY_KEYS` as a frozen, single-source-of-truth array from `tests/e2e/lib/llm-report.js`, and rebuild `emptySummary()` from it so the seven-key summary contract has exactly one definition (D-01). Extend the existing unit suite to prove the export is frozen, has the right keys in the right order, and that `emptySummary()` is genuinely derived from it.

Purpose: Phase 37's `weekly-digest.mjs` (Plan 37-02) imports `SUMMARY_KEYS` to validate the summary structure it consumes and to throw a descriptive error on any missing key (DIGEST-04, no silent zero). Today the contract lives implicitly inside `emptySummary()` (lines 117-127) and `classificationToSummaryKey()` (134-144); there is no exported array. This plan creates the contract.

Output: `SUMMARY_KEYS` named export; `emptySummary()` derived from it; extended `tests/unit/llm-report.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-weekly-analytics-digest/37-CONTEXT.md

# The edit target — read all of it before editing.
@tests/e2e/lib/llm-report.js

# The unit suite to extend (read the header coverage map + the imports at lines 31-45).
@tests/unit/llm-report.test.js

<interfaces>
<!-- The contract being extracted. Current emptySummary() (llm-report.js:117-127): -->
<!--   passed, wrong_citation, verifier_disagree, llm_hallucinated_selection, -->
<!--   llm_api_error, harness_error, total_cost_usd  (all initialised to 0). -->
<!-- classificationToSummaryKey() (134-144) maps: PASS→passed, WRONG_CITATION→wrong_citation, -->
<!--   VERIFIER_DISAGREE→verifier_disagree, LLM_HALLUCINATED_SELECTION→llm_hallucinated_selection, -->
<!--   LLM_API_ERROR→llm_api_error, HARNESS_ERROR→harness_error, default→null. -->
<!-- After this plan, llm-report.js exports: -->
<!--   export const SUMMARY_KEYS = Object.freeze([...7 keys...]) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export SUMMARY_KEYS and rebuild emptySummary() from it</name>
  <files>tests/e2e/lib/llm-report.js</files>
  <read_first>
    - tests/e2e/lib/llm-report.js lines 117-165 (emptySummary, classificationToSummaryKey, recomputeSummary — recomputeSummary calls emptySummary() so the rebuild MUST preserve the all-zeros shape and exact keys).
    - .planning/phases/37-weekly-analytics-digest/37-PATTERNS.md "tests/e2e/lib/llm-report.js (lib/contract)" section — the exact target shape.
  </read_first>
  <behavior>
    - SUMMARY_KEYS is a named export, Object.isFrozen(SUMMARY_KEYS) === true, length === 7.
    - SUMMARY_KEYS order is exactly: passed, wrong_citation, verifier_disagree, llm_hallucinated_selection, llm_api_error, harness_error, total_cost_usd (matches the prior object-literal order).
    - emptySummary() returns an object whose keys deep-equal SUMMARY_KEYS in order, every value 0.
    - Every non-null return value of classificationToSummaryKey() is a member of SUMMARY_KEYS.
    - recomputeSummary() still produces the same summary shape it did before (regression: existing tests 4-7 in llm-report.test.js stay green).
  </behavior>
  <action>
    Add a top-level named export `SUMMARY_KEYS` defined as `Object.freeze([...])` containing the seven keys in the EXACT order of the current `emptySummary()` object literal: passed, wrong_citation, verifier_disagree, llm_hallucinated_selection, llm_api_error, harness_error, total_cost_usd (D-01). Place it near the other module-level constants (e.g. just above `emptySummary`), with a short comment noting it is the single source of truth for the summary contract and that order is load-bearing for downstream key ordering.

    Rewrite `emptySummary()` to build its return object FROM `SUMMARY_KEYS` using `Object.fromEntries(SUMMARY_KEYS.map((k) => [k, 0]))` so there is exactly one definition of the key set (D-01). Do NOT change the function name, signature, or its all-zeros result — `recomputeSummary()` (line 156) and `emptyReport()` (line 188) both call it and must behave identically.

    Leave `classificationToSummaryKey()` (lines 134-144) unchanged — it is the existing closed-enum guard. The invariant to preserve: every string that switch can return is already present in SUMMARY_KEYS, plus the array adds `total_cost_usd` (which is incremented separately in recomputeSummary, not via the switch).

    Do NOT change `LLM_REPORT_SCHEMA_VERSION`, the atomic-write helpers, or any other export. This is an additive export + a single-function internal rebuild.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/llm-report.test.js</automated>
  </verify>
  <done>SUMMARY_KEYS exported and frozen; emptySummary() built from it; existing llm-report.test.js tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SUMMARY_KEYS export + single-source-proof tests</name>
  <files>tests/unit/llm-report.test.js</files>
  <read_first>
    - tests/unit/llm-report.test.js lines 1-50 (header coverage map + the import block at 36-42; you will add SUMMARY_KEYS to that import).
    - tests/e2e/lib/llm-report.js (the export you just added).
  </read_first>
  <behavior>
    - Test A "SUMMARY_KEYS is frozen and has 7 keys": import SUMMARY_KEYS; assert Object.isFrozen(SUMMARY_KEYS) and SUMMARY_KEYS.length === 7.
    - Test B "SUMMARY_KEYS exact order": assert SUMMARY_KEYS deep-equals the explicit array [passed, wrong_citation, verifier_disagree, llm_hallucinated_selection, llm_api_error, harness_error, total_cost_usd].
    - Test C "emptySummary derives from SUMMARY_KEYS (single source)": seed an empty report via initLlmReport and read its summary back from disk; assert Object.keys(summary) deep-equals SUMMARY_KEYS and every value === 0. (emptySummary is not exported, so prove derivation through the public initLlmReport → report.summary surface.)
  </behavior>
  <action>
    Extend the import at lines 36-42 to also import `SUMMARY_KEYS` from `../e2e/lib/llm-report.js`.

    Add a new `describe('SUMMARY_KEYS export (Phase 37 D-01)')` block with Tests A, B, C above. For Test C, reuse the existing per-test tmpDir + initLlmReport pattern already established at the top of the file (RUN_ID constant, reportPath) — read the seeded report's `.summary` back via JSON.parse(fs.readFileSync(...)) exactly as the existing tests do, then assert its keys equal SUMMARY_KEYS. This proves emptySummary() is genuinely built from the array without needing to export emptySummary.

    Update the file-header coverage-map comment to note the three new SUMMARY_KEYS cases.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/llm-report.test.js</automated>
  </verify>
  <done>Three new SUMMARY_KEYS tests pass; full llm-report.test.js suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure in-repo library contract change; no external input, no shellout, no I/O beyond the existing test tmpDir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01-01 | Tampering | downstream consumers' key ordering | mitigate | SUMMARY_KEYS order pinned to prior object-literal order; Test B asserts exact order so a reorder fails CI |
| T-37-01-02 | Tampering | summary contract drift (key added/removed in one place only) | mitigate | emptySummary() built from SUMMARY_KEYS (single source, D-01); Test C proves derivation |
| T-37-01-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies this plan (RESEARCH Package Legitimacy Audit: none); no install step |
</threat_model>

<verification>
- `npx vitest run tests/unit/llm-report.test.js` green (existing + 3 new tests).
- `grep -n "export const SUMMARY_KEYS" tests/e2e/lib/llm-report.js` returns the frozen array.
- `grep -n "Object.fromEntries(SUMMARY_KEYS" tests/e2e/lib/llm-report.js` confirms emptySummary derivation.
</verification>

<success_criteria>
- SUMMARY_KEYS exported, frozen, 7 keys, correct order.
- emptySummary() built from SUMMARY_KEYS (single source).
- No regression in the existing llm-report writer behavior.
</success_criteria>

<output>
Create `.planning/phases/37-weekly-analytics-digest/37-01-SUMMARY.md` when done.
</output>
