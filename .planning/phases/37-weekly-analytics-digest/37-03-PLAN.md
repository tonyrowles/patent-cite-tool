---
phase: 37-weekly-analytics-digest
plan: 03
type: execute
wave: 3
depends_on: ["37-02"]
files_modified:
  - .github/workflows/e2e-weekly-digest.yml
  - tests/e2e/scripts/e2e-weekly-digest-yaml.test.js
autonomous: true
requirements: [DIGEST-02, DIGEST-03]
must_haves:
  truths:
    - "D-09 / DIGEST-02: workflow triggers on schedule cron '0 7 * * 1' (Monday 07:00 UTC) and workflow_dispatch"
    - "D-09 + SECURITY: permissions block has contents: write AND discussions: write AND issues: write (issues:write is load-bearing — the ACTIVE issue-fallback create 403s without it)"
    - "D-07: workflow self-bootstraps the e2e-digest label via gh label create --color <6hex-no-#> --force (mirrors Phase 36 label-ensure)"
    - "D-13/D-06: workflow sets DIGEST_PUBLISH_MODE=issue and invokes node scripts/weekly-digest.mjs / npm run e2e:weekly-digest"
    - "D-11 / DIGEST-03: workflow commits reports/weekly-digest-*.md in the SAME run via git add + (git diff --cached --quiet || git commit) + git push, with [skip ci] in the commit message to avoid re-triggering ci.yml"
    - "DIGEST-03: ISO-week filename reports/weekly-digest-YYYY-WNN.md is the committed artifact"
  artifacts:
    - path: ".github/workflows/e2e-weekly-digest.yml"
      provides: "Monday cron workflow: checkout, setup-node, label-ensure, run digest, commit-in-run"
      contains: "cron: '0 7 * * 1'"
    - path: "tests/e2e/scripts/e2e-weekly-digest-yaml.test.js"
      provides: "grep-based YAML assertions for cron, permissions, label, invocation, commit-in-run, [skip ci]"
      contains: "[skip ci]"
  key_links:
    - from: ".github/workflows/e2e-weekly-digest.yml"
      to: "scripts/weekly-digest.mjs"
      via: "npm run e2e:weekly-digest (or node scripts/weekly-digest.mjs) run step"
      pattern: "weekly-digest"
    - from: ".github/workflows/e2e-weekly-digest.yml commit step"
      to: "reports/weekly-digest-*.md"
      via: "git add + git commit [skip ci] + git push"
      pattern: "git push"
---

<objective>
Create `.github/workflows/e2e-weekly-digest.yml`: a Monday-07:00-UTC cron (plus `workflow_dispatch`) that checks out the repo, ensures the `e2e-digest` label exists, runs `scripts/weekly-digest.mjs` with `DIGEST_PUBLISH_MODE=issue`, and commits the generated `reports/weekly-digest-YYYY-WNN.md` in the same run with a `[skip ci]` message. Add a grep-based YAML test asserting the cron, the full `contents/discussions/issues: write` permission set, the label-ensure step, the script invocation, and the commit-in-run step.

Purpose: This is the scheduler + publisher wiring for the digest (DIGEST-02, DIGEST-03). It addresses the CONFIRMED security gap: D-09 locked only `contents: write` + `discussions: write`, but the ACTIVE publish path is `gh issue create`, which 403s without `issues: write` — so this workflow MUST add `issues: write` (RESEARCH §Security line 494-498; PATTERNS Flagged Issue 1).

Output: `.github/workflows/e2e-weekly-digest.yml`, `tests/e2e/scripts/e2e-weekly-digest-yaml.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-weekly-analytics-digest/37-CONTEXT.md
@.planning/phases/37-weekly-analytics-digest/37-RESEARCH.md
@.planning/phases/37-weekly-analytics-digest/37-PATTERNS.md

# Workflow analog — cron syntax, permissions block, label-ensure step, env-var hop, setup-node/checkout.
@.github/workflows/e2e-nightly.yml

# YAML-grep test analog — readFileSync in beforeAll, expect(yaml).toContain / split-count assertions.
@tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js

<interfaces>
<!-- e2e-nightly.yml analogs: -->
<!--   on: schedule: - cron: '0 6 * * *'  (Phase 37 uses '0 7 * * 1' Monday 07:00 UTC) -->
<!--   concurrency: { group, cancel-in-progress: false } -->
<!--   permissions: { contents: read, issues: write }  (Phase 37 needs contents:write + discussions:write + issues:write) -->
<!--   - uses: actions/checkout@v4   (default persist-credentials: true → GITHUB_TOKEN can push under contents:write) -->
<!--   - uses: actions/setup-node@v4 with node-version: 22 -->
<!--   label-ensure (lines 97-105): gh label create "<name>" --color "<6hex>" --description "..." --force 2>/dev/null || true -->
<!--     e2e-nightly color 0075ca; e2e-quarantine d93f0b → pick distinct (e.g. 5319e7). 6 hex, NO '#'. -->
<!--   env-var hop (CWE-94): set values in step env:, never interpolate ${{ inputs.* }} into run:. -->

<!-- Commit-in-run idiom (RESEARCH Pitfall 3 — NO repo analog, GitHub-standard): -->
<!--   git config user.name "github-actions[bot]" -->
<!--   git config user.email "41898282+github-actions[bot]@users.noreply.github.com" -->
<!--   git add reports/weekly-digest-*.md -->
<!--   git diff --cached --quiet || git commit -m "docs(weekly-digest): <WEEK_LABEL> [skip ci]" -->
<!--   git push -->
<!--   [skip ci] is load-bearing — prevents the bot push from re-triggering ci.yml. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author e2e-weekly-digest.yml (cron, permissions, label, run, commit-in-run)</name>
  <files>.github/workflows/e2e-weekly-digest.yml</files>
  <read_first>
    - .github/workflows/e2e-nightly.yml lines 16-105 (name/on/concurrency/permissions/jobs/steps header, checkout, setup-node, label-ensure step) — the structural analog.
    - .planning/phases/37-weekly-analytics-digest/37-RESEARCH.md Pitfall 3 lines 324-338 (commit-in-run idiom) + §Security lines 494-498 (issues:write requirement).
    - .planning/phases/37-weekly-analytics-digest/37-PATTERNS.md "e2e-weekly-digest.yml" section (the exact header/permissions/label/commit blocks).
  </read_first>
  <action>
    Create `.github/workflows/e2e-weekly-digest.yml`. Structure (adapt e2e-nightly.yml):

    Header comment block summarizing: Monday 07:00 UTC digest; reads prior-week e2e-nightly/e2e-quarantine issues; publishes via the e2e-digest issue fallback (Discussions verified off, D-05); commits reports/weekly-digest-YYYY-WNN.md in-run; per DIGEST-02/03 + the issues:write security note.

    `name: E2E Weekly Digest`.

    `on:` — `schedule: - cron: '0 7 * * 1'` (Monday 07:00 UTC, D-09) and `workflow_dispatch: {}` (manual testing).

    `concurrency:` — `group: e2e-weekly-digest`, `cancel-in-progress: false` (mirror e2e-nightly rationale).

    `permissions:` — `contents: write` (commit-in-run, D-09), `discussions: write` (dormant discussion path, D-09), `issues: write` (LOAD-BEARING — the ACTIVE issue-fallback `gh issue create` 403s without it; RESEARCH §Security). Add an inline comment on `issues: write` explaining D-09 omitted it because the requirement text framed the dormant discussion path as primary, but the active path is issue-create.

    `jobs.weekly-digest` on `ubuntu-latest`, `timeout-minutes: 10`, job-level `env:` with `GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}`, `GITHUB_REPOSITORY: ${{ github.repository }}`, `DIGEST_PUBLISH_MODE: issue` (D-05/D-06 — force the active path for now).

    Steps:
      1. `- uses: actions/checkout@v4` (default persist-credentials:true lets GITHUB_TOKEN push under contents:write — RESEARCH A4; add a comment noting this).
      2. `- uses: actions/setup-node@v4` with `node-version: 22` and the same conditional `cache:` expression as e2e-nightly. Then `npm ci || npm install` (the script imports repo-local ESM only and uses zero runtime deps, but the lint/test toolchain and node_modules resolution still benefit; keep it minimal — install only if a package-lock exists, mirroring e2e-nightly lines 64-70).
      3. Label-ensure step "Ensure e2e-digest label exists": `gh label create "e2e-digest" --color "5319e7" --description "Weekly analytics digest" --force 2>/dev/null || true` (D-07; 6-hex no '#', distinct from 0075ca/d93f0b — comment the requirement). Color is Claude's discretion; 5319e7 (purple) chosen.
      4. Run step "Generate weekly digest": `npm run e2e:weekly-digest` (or `node scripts/weekly-digest.mjs`) with step `env:` carrying GH_TOKEN/GITHUB_REPOSITORY/DIGEST_PUBLISH_MODE if not inherited. After the run, capture the week label for the commit message — recommend the digest writes a small marker the commit step can read, OR recompute the label in the commit step via a `node -e` one-liner calling isoWeekLabel; simplest deterministic approach: have the run step `echo "WEEK_LABEL=$(node -e \"import('./scripts/weekly-digest.mjs').then(m=>console.log(m.isoWeekLabel(new Date())))\")" >> "$GITHUB_ENV"` (env-var hop; the commit step then references ${{ env.WEEK_LABEL }}). Do NOT interpolate untrusted input into run:.
      5. Commit-in-run step "Commit weekly digest" (NO repo analog — GitHub-standard idiom, RESEARCH Pitfall 3): `git config user.name "github-actions[bot]"`; `git config user.email "41898282+github-actions[bot]@users.noreply.github.com"`; `git add reports/weekly-digest-*.md`; `git diff --cached --quiet || git commit -m "docs(weekly-digest): ${{ env.WEEK_LABEL }} [skip ci]"`; `git push`. The `git diff --cached --quiet || git commit` makes it idempotent (no-op when unchanged, D-11); `[skip ci]` is load-bearing — it prevents the bot push from re-triggering ci.yml (which runs on push to the default branch). Add a comment explaining both.

    Do NOT set `E2E_LEDGER_PATH_OVERRIDE` anywhere (it throws in CI per llm-ledger.js:85). Do NOT add `permissions: { id-token }` or anything beyond the three writes.
  </action>
  <verify>
    <automated>node -e "const y=require('node:fs').readFileSync('.github/workflows/e2e-weekly-digest.yml','utf8'); for (const t of [\"cron: '0 7 * * 1'\",'contents: write','discussions: write','issues: write','e2e-digest','[skip ci]','git push']) { if(!y.includes(t)) throw new Error('missing '+t); } console.log('YAML markers OK');"</automated>
  </verify>
  <done>Workflow exists with Monday cron, all three write permissions, label-ensure, digest invocation, and idempotent commit-in-run with [skip ci].</done>
</task>

<task type="auto">
  <name>Task 2: YAML-grep test for the workflow contract</name>
  <files>tests/e2e/scripts/e2e-weekly-digest-yaml.test.js</files>
  <read_first>
    - tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js lines 1-55 (readFileSync in beforeAll, PROJECT_ROOT/YAML_PATH resolution, expect(yaml).toContain / split-count assertion idioms).
  </read_first>
  <action>
    Create `tests/e2e/scripts/e2e-weekly-digest-yaml.test.js` mirroring the Phase 36 YAML-grep harness. Resolve `YAML_PATH = path.resolve(__dirname, '../../..', '.github/workflows/e2e-weekly-digest.yml')`; `beforeAll` reads it into `yaml`. Assertions:
      - Y1 cron + dispatch: `yaml` contains `cron: '0 7 * * 1'` and `workflow_dispatch`.
      - Y2 permissions (the load-bearing security gate): `yaml` contains `contents: write`, `discussions: write`, AND `issues: write` (assert all three; issues:write is the one D-09 omitted).
      - Y3 label-ensure: `yaml` contains `gh label create "e2e-digest"` and `--force`.
      - Y4 invocation: `yaml` contains `weekly-digest` invocation (`npm run e2e:weekly-digest` or `scripts/weekly-digest.mjs`) and `DIGEST_PUBLISH_MODE: issue`.
      - Y5 commit-in-run: `yaml` contains `git add reports/weekly-digest-*.md`, `git diff --cached --quiet || git commit`, `git push`, and `[skip ci]` (the [skip ci] token is asserted explicitly — it is load-bearing to avoid re-triggering ci.yml).
      - Y6 no-ledger-override guard: assert `yaml` does NOT contain `E2E_LEDGER_PATH_OVERRIDE` (`expect(yaml).not.toContain('E2E_LEDGER_PATH_OVERRIDE')` — it throws in CI).
    Keep it zero-dep (plain string/regex on the file text, no js-yaml), matching the Phase 36 analog.
  </action>
  <verify>
    <automated>npx vitest run tests/e2e/scripts/e2e-weekly-digest-yaml.test.js</automated>
  </verify>
  <done>All YAML-grep assertions pass; the test would fail if any of cron / the three write permissions / label / invocation / commit-in-run / [skip ci] were missing, or if E2E_LEDGER_PATH_OVERRIDE appeared.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions cron/dispatch → runner | Scheduled + manual trigger executes with a write-scoped GITHUB_TOKEN |
| commit-in-run → default branch | Bot push of machine-generated markdown to the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03-01 | Elevation | over-broad workflow permissions | mitigate | Minimal permissions block — exactly contents:write + discussions:write + issues:write (each justified); nothing else |
| T-37-03-02 | DoS / loop | bot push re-triggers ci.yml on the default branch | mitigate | `[skip ci]` in the commit message (RESEARCH Pitfall 3); YAML test asserts the token present |
| T-37-03-03 | Tampering | non-idempotent re-run double-commits | mitigate | `git diff --cached --quiet || git commit` no-ops when unchanged; week-stamped filename overwritten by the script (D-11) |
| T-37-03-04 | Elevation | workflow-input injection in run: (CWE-94) | mitigate | env-var hop for any dynamic value (WEEK_LABEL via $GITHUB_ENV); never interpolate ${{ inputs.* }} into run: (e2e-nightly.yml:116 convention) |
| T-37-03-05 | Elevation | ledger override bypasses caps in CI | mitigate | E2E_LEDGER_PATH_OVERRIDE never set; YAML test asserts its ABSENCE (llm-ledger.js:85 throws if set in CI) |
| T-37-03-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies (RESEARCH Package Legitimacy Audit: none); `npm ci` installs only existing locked deps |
</threat_model>

<verification>
- `npx vitest run tests/e2e/scripts/e2e-weekly-digest-yaml.test.js` green.
- `grep -n "issues: write" .github/workflows/e2e-weekly-digest.yml` present (the load-bearing permission).
- `grep -n "\[skip ci\]" .github/workflows/e2e-weekly-digest.yml` present.
- `grep -c "E2E_LEDGER_PATH_OVERRIDE" .github/workflows/e2e-weekly-digest.yml` returns 0.
- Phase gate: `npm run test:src && npm run lint` green.
</verification>

<success_criteria>
- Workflow triggers Monday 07:00 UTC + workflow_dispatch (DIGEST-02).
- permissions: contents:write + discussions:write + issues:write (DIGEST-02 + security gap closed).
- e2e-digest label self-bootstrapped (D-07).
- reports/weekly-digest-YYYY-WNN.md committed in-run with [skip ci] (DIGEST-03, D-11).
</success_criteria>

<output>
Create `.planning/phases/37-weekly-analytics-digest/37-03-SUMMARY.md` when done.
</output>
