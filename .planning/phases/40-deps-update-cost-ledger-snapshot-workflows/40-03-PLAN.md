---
phase: 40-deps-update-cost-ledger-snapshot-workflows
plan: 03
type: execute
wave: 2
depends_on:
  - 40-02
files_modified:
  - .github/workflows/v40-deps-update.yml
  - tests/e2e/scripts/v40-deps-update-yaml.test.js
autonomous: true
requirements:
  - DEPS-01  # cron '0 9 * * 1' + workflow_dispatch + watchlist invocation (script from 40-02)
  - DEPS-02  # nightly-suite gate exists as a job in this workflow named `deps-update-gate` (advisory-only in Phase 40 per CONTEXT — required-status-checks ruleset rule deferred to Phase 47 per CONTEXT line 73 + 40-RESEARCH lines 651-665)
  - DEPS-03  # security vs minor PR partitioning via two `peter-evans/create-pull-request@v8` steps (partition logic itself in 40-02)
user_setup: []

must_haves:
  truths:
    - "A weekly Monday 09:00 UTC cron (plus workflow_dispatch) runs the script from 40-02 and opens up to N security PRs + 1 grouped minor PR via two peter-evans/create-pull-request@v8 steps"
    - "Both PR steps use draft: true (Pitfall 4 defense — manual gate before ready-for-review)"
    - "Both PR steps use secrets.GITHUB_TOKEN (not a PAT) (Pitfall 4 defense — workflow-scoped token)"
    - "Both PR steps use delete-branch: true with constant per-package branch names (40-CONTEXT locked decision #4 — re-runs supersede stale PRs)"
    - "A `deps-update-gate` job runs the full nightly suite (smoke + 76-case regression + fault-injection) against the new dep versions — this job's NAME is the slot reserved for the required-status-checks rule that Phase 47 audit will add to the v4.0-main-protection ruleset"
    - "Vitest YAML-level test pins every load-bearing primitive: cron, watchlist, gate name, draft: true, delete-branch: true, secrets.GITHUB_TOKEN (no PAT), no auto-merge, no [skip ci] (deps-update commits are PR commits, not self-commits)"
  artifacts:
    - path: ".github/workflows/v40-deps-update.yml"
      provides: "Weekly dep-update workflow + nightly-suite gate job"
      contains: "cron: '0 9 * * 1'"
    - path: "tests/e2e/scripts/v40-deps-update-yaml.test.js"
      provides: "YAML-level static-grep contract (D1-D11 per 40-RESEARCH.md lines 300-310 plus the watchlist + gate + token assertions)"
      exports: ["describe", "it"]
  key_links:
    - from: ".github/workflows/v40-deps-update.yml"
      to: "scripts/check-deps-and-pr.mjs"
      via: "node scripts/check-deps-and-pr.mjs"
      pattern: "node scripts/check-deps-and-pr\\.mjs"
    - from: ".github/workflows/v40-deps-update.yml"
      to: "peter-evans/create-pull-request@v8"
      via: "two uses: peter-evans/create-pull-request@v8 blocks (one per partition)"
      pattern: "peter-evans/create-pull-request@v8"
    - from: ".github/workflows/v40-deps-update.yml"
      to: "secrets.GITHUB_TOKEN"
      via: "token: ${{ secrets.GITHUB_TOKEN }} on both PR steps"
      pattern: "secrets\\.GITHUB_TOKEN"
    - from: "tests/e2e/scripts/v40-deps-update-yaml.test.js"
      to: ".github/workflows/v40-deps-update.yml"
      via: "fs.readFileSync(YAML_PATH, 'utf8'); expect(yaml).toContain(token)"
      pattern: "v40-deps-update\\.yml"
---

<objective>
Ship `.github/workflows/v40-deps-update.yml` — the weekly dep-update workflow that invokes 40-02's `scripts/check-deps-and-pr.mjs`, opens security + minor PRs via `peter-evans/create-pull-request@v8`, AND defines a `deps-update-gate` job that runs the full nightly suite against the new versions. Also ship the YAML-level Vitest guard (`tests/e2e/scripts/v40-deps-update-yaml.test.js`) that pins every load-bearing primitive.

Purpose: DEPS-01 (cron + watchlist invocation), DEPS-02 (nightly-suite gate as a named job — slot reserved for Phase 47 required-status-checks rule), DEPS-03 (security vs minor partition wired into two separate `peter-evans/cpr@v8` steps). This is the wiring plan — it depends on 40-02's pure script.

Output: One workflow file (~90-130 lines) + one YAML-test file (~110-150 lines). Zero new dependencies. The frame-shift pre-flight workflow is OUT of this plan's scope (lives in 40-04 as a separate workflow per locked decision #3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/40-deps-update-cost-ledger-snapshot-workflows/40-CONTEXT.md
@.planning/phases/40-deps-update-cost-ledger-snapshot-workflows/40-RESEARCH.md
@.github/workflows/e2e-weekly-digest.yml
@.github/workflows/e2e-nightly.yml
@tests/e2e/scripts/e2e-weekly-digest-yaml.test.js
@scripts/check-deps-and-pr.mjs
@package.json
@CLAUDE.md

<interfaces>
<!-- Pre-extracted contracts from 40-02 + research. -->

From scripts/check-deps-and-pr.mjs (40-02 deliverable — depends_on):
- `$GITHUB_OUTPUT` lines emitted:
  ```
  security_count=<n>
  security_packages=<comma-sep>
  security_branch=v40-deps-update/<first-security-pkg>-security
  minor_count=<n>
  minor_packages=<comma-sep>
  minor_branch=v40-deps-update/minor
  ```
- Body files written:
  ```
  /tmp/security-pr-body.md
  /tmp/minor-pr-body.md
  ```
- Script invocation: `node scripts/check-deps-and-pr.mjs` (or `npm run check-deps-and-pr` via the package.json scripts entry — prefer the explicit `node` form in CI for grep stability).

From 40-RESEARCH.md lines 861-934 (workflow skeleton — copy with the adjustments below).

From .github/workflows/e2e-weekly-digest.yml (style template):
- `name: ...` is human-readable title
- `on: { schedule: [{ cron: '...' }], workflow_dispatch: {} }`
- `concurrency: { group: ..., cancel-in-progress: false }`
- `permissions:` block (least-privilege)
- `jobs.<job-id>.runs-on: ubuntu-latest`
- `jobs.<job-id>.timeout-minutes: <n>`
- `jobs.<job-id>.env: { GH_TOKEN: ${{ secrets.GITHUB_TOKEN }} }`
- Steps use `uses: actions/checkout@v4`, `uses: actions/setup-node@v4` with `node-version: 22` (LITERAL — see 40-01 plan's note on no .nvmrc in repo)

From .github/workflows/e2e-nightly.yml (existing nightly suite invocation — the `deps-update-gate` job must replicate this nightly suite shape but with the candidate `package.json` deps applied). The executor should `grep -n "npm run\|playwright test" .github/workflows/e2e-nightly.yml | head -15` to discover the exact `npm run e2e:smoke`, `npm run e2e:regression`, fault-injection commands AND the env-var/secrets contract, then mirror that contract in the `deps-update-gate` job.

From peter-evans/create-pull-request@v8 action input surface (per 40-RESEARCH.md lines 264-274 + action README cross-check):
- `token` — REQUIRED (use `${{ secrets.GITHUB_TOKEN }}`)
- `branch` — REQUIRED (use the script's `$GITHUB_OUTPUT` value)
- `base` — defaults to default branch; explicitly set to `main` for clarity
- `draft: true` — REQUIRED for Pitfall 4 defense
- `body-path` — path to a markdown file (the script writes to `/tmp/*-pr-body.md`)
- `labels` — comma-separated string (action will create labels if absent)
- `commit-message` — REQUIRED (action makes ONE commit per invocation)
- `delete-branch: true` — REQUIRED per 40-CONTEXT locked decision #4 (re-run supersede)
- `signoff: false` — explicit (bot identity in commit trailer is sufficient)
- `title` — PR title (action will fall back to commit-message if absent — set explicitly for grep)

**Critical**: Field names in peter-evans/cpr@v8 use HYPHENS (`body-path`, `commit-message`, `delete-branch`), NOT underscores. The 40-RESEARCH.md Sources Tertiary footnote flags this as needing verification — confirmed: hyphen form is correct for v8 per action README.

From 40-CONTEXT.md locked decisions and 40-RESEARCH.md research:
- Cron: `0 9 * * 1` (Monday 09:00 UTC) — VERIFIED no conflict (other workflows: `0 6 * * *` nightly, `0 7 * * 1` digest)
- Branch names: CONSTANT per-package (`v40-deps-update/<pkg>-security`, `v40-deps-update/minor`) — locked decision #4
- Required-status-checks rule: ABSENT in Phase 40 (deferred to Phase 47 per CONTEXT line 73 + 40-RESEARCH lines 651-665). The `deps-update-gate` job RUNS but is not a hard gate yet.
- Frame-shift pre-flight: SEPARATE workflow file in 40-04 — DO NOT include in this workflow.
- `tests/e2e/.manual-sdk-bumps.json` updates from the script run: the workflow should `git add tests/e2e/.manual-sdk-bumps.json` and include it in any PR that's opened (the peter-evans/cpr@v8 action picks up all working-tree changes), AND a NEW step should `gh issue create --label manual-sdk-review --title "..."` if the script reports `skipped[]` is non-empty (per 40-RESEARCH.md line 716 recommendation).

Permissions needed:
- `contents: write` — push branches via peter-evans/cpr
- `pull-requests: write` — create/update PRs
- `issues: write` — `gh issue create` for manual-sdk-review (when SDK skip occurs)

Concurrency:
- `concurrency: { group: v40-deps-update, cancel-in-progress: false }` — prevents schedule + workflow_dispatch from racing
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write v40-deps-update-yaml.test.js (RED — workflow does not exist yet)</name>
  <files>tests/e2e/scripts/v40-deps-update-yaml.test.js</files>
  <read_first>
    - tests/e2e/scripts/e2e-weekly-digest-yaml.test.js (Y1-Y6 template — copy file structure)
    - .planning/phases/40-deps-update-cost-ledger-snapshot-workflows/40-RESEARCH.md lines 298-310 (D1-D10 assertion list)
    - .planning/phases/40-deps-update-cost-ledger-snapshot-workflows/40-RESEARCH.md lines 264-274 (peter-evans/cpr@v8 input field names)
    - .planning/phases/40-deps-update-cost-ledger-snapshot-workflows/40-CONTEXT.md lines 27-52 (locked decisions)
    - .planning/research/PITFALLS.md lines 209-260 (Pitfall 4 — defense surfaces this test pins: draft: true, no --auto, GITHUB_TOKEN not PAT)
  </read_first>
  <behavior>
    Assertions (use exact `it()` test names from the list — D-prefixed = 40-RESEARCH.md mapping, X-prefixed = extra Phase 40 safety pins):

    - D1 — `expect(yaml).toContain("cron: '0 9 * * 1'")` (Mon 09:00 UTC EXACT)
    - D2 — `expect(yaml).toContain('workflow_dispatch')` (manual trigger)
    - D3 — `expect(yaml).toContain('contents: write')` AND `expect(yaml).toContain('pull-requests: write')` AND `expect(yaml).toContain('issues: write')`
    - D4 — `expect(yaml).toMatch(/timeout-minutes:\s*(1[5-9]|2\d|30)/)` (timeout in 15-30 min — covers script + nightly suite matrix)
    - D5 — Watchlist tokens present in SCRIPT (not workflow): `const script = fs.readFileSync('scripts/check-deps-and-pr.mjs', 'utf8'); for (const pkg of ['@playwright/test', 'pdfjs-dist', 'sharp', 'vitest', 'esbuild', '@anthropic-ai/sdk']) expect(script).toContain(\`'\${pkg}'\`)` — this assertion verifies the workflow is using the right script, but the watchlist freeze itself is unit-tested in 40-02.
    - D6 — `expect(yaml).toContain('peter-evans/create-pull-request@v8')` (action + major version pin; assert exact, no `@main` or unpinned variant). Also `expect((yaml.match(/peter-evans\/create-pull-request@v8/g) || []).length).toBeGreaterThanOrEqual(2)` (TWO invocations: security + minor partitions).
    - D7 — `expect(yaml).toContain('draft: true')` (Pitfall 4 defense)
    - D8 — `expect(yaml).toContain('node scripts/check-deps-and-pr.mjs')` (script invocation)
    - D9 — `expect(yaml).not.toContain('[skip ci]')` (deps-update commits are PR commits via peter-evans/cpr, NOT self-commits)
    - D10 — `expect(yaml).not.toContain('gh pr merge --auto')` (Pitfall 4 defense — no auto-merge)
    - D11 — `expect(yaml).not.toContain('auto-merge: true')` (Pitfall 4 defense — same surface, different shape)
    - X1 — Token is GITHUB_TOKEN (not PAT): `expect(yaml).toContain('${{ secrets.GITHUB_TOKEN }}')` AND `expect(yaml).not.toMatch(/secrets\.[A-Z_]*PAT/)` (negative pin — fails if any `secrets.*PAT*` literal appears)
    - X2 — Constant per-package branch names: `expect(yaml).toContain('v40-deps-update/')` AND the workflow references the script's outputs (`steps.scan.outputs.security_branch` and `steps.scan.outputs.minor_branch`)
    - X3 — `delete-branch: true` on BOTH PR steps: `expect((yaml.match(/delete-branch:\s*true/g) || []).length).toBeGreaterThanOrEqual(2)`
    - X4 — `body-path:` field used (NOT `body:` direct embed): `expect(yaml).toMatch(/body-path:\s*\/tmp\/(security|minor)-pr-body\.md/)`
    - X5 — A `deps-update-gate` job exists by exact NAME (slot reservation per 40-RESEARCH lines 651-665): `expect(yaml).toMatch(/^\s+deps-update-gate:/m)` (job key matched at indentation; YAML grammar). Also the gate job invokes the nightly suite: `expect(yaml).toMatch(/npm run e2e:(smoke|regression)/)` — the EXACT script names depend on what e2e-nightly.yml uses; executor reads e2e-nightly.yml to confirm.
    - X6 — `concurrency.group: v40-deps-update`: `expect(yaml).toContain('group: v40-deps-update')` AND `expect(yaml).toContain('cancel-in-progress: false')`
    - X7 — Manual-SDK-review issue creation step exists (40-RESEARCH line 716 recommendation): `expect(yaml).toMatch(/gh issue create.*manual-sdk-review/i)` — guarded by a conditional that fires only when the script reports skipped[].length > 0.
    - X8 — Pitfall 1 step 7 (`permissions:` minimization — explicit absence of dangerous permissions): `expect(yaml).not.toContain('id-token: write')` AND `expect(yaml).not.toContain('actions: write')`
  </behavior>
  <action>
    Create `tests/e2e/scripts/v40-deps-update-yaml.test.js`. Mirror `tests/e2e/scripts/e2e-weekly-digest-yaml.test.js` for imports/PROJECT_ROOT/beforeAll structure. Header comment cites Phase 40 Plan 40-03 (DEPS-01 + DEPS-02 + DEPS-03 + Pitfall 4 defenses).

    The `YAML_PATH` constant: `const YAML_PATH = path.resolve(PROJECT_ROOT, '.github/workflows/v40-deps-update.yml');`. A SECOND constant for D5: `const SCRIPT_PATH = path.resolve(PROJECT_ROOT, 'scripts/check-deps-and-pr.mjs');`. The `beforeAll` reads both files (one into `yaml`, one into `script`).

    Each `it()` block name uses the `D<n> — <description>` or `X<n> — <description>` format from the behavior list. Test order: D1, D2, D3, D4, D5, D6, D7, D8, D9, D10, D11, X1, X2, X3, X4, X5, X6, X7, X8 — one assertion group per `it()` block. RED state: `beforeAll` will throw ENOENT on YAML_PATH (workflow doesn't exist yet). Do NOT add a `.skipIf(!fs.existsSync(YAML_PATH))` guard — the test MUST fail in RED so Task 2 has a forcing function (unlike 40-02 Task 1 where the skipIf pattern was correct for a script test importing a module that doesn't exist yet, here the test reads a YAML file at runtime in `beforeAll` and SHOULD ENOENT-fail).

    Total: ~150-200 lines.
  </action>
  <verify>
    <automated>cd /home/fatduck/patent-cite-tool &amp;&amp; npx vitest run tests/e2e/scripts/v40-deps-update-yaml.test.js 2>&amp;1 | tail -15</automated>
  </verify>
  <done>
    Test file exists. `npx vitest run tests/e2e/scripts/v40-deps-update-yaml.test.js` exits non-zero with an ENOENT or "file not found" error from `beforeAll` reading `.github/workflows/v40-deps-update.yml`. The 19 test cases (D1-D11 + X1-X8) are defined. RED state confirmed; ready for Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create .github/workflows/v40-deps-update.yml (GREEN — make D1-D11 + X1-X8 pass)</name>
  <files>.github/workflows/v40-deps-update.yml</files>
  <read_first>
    - tests/e2e/scripts/v40-deps-update-yaml.test.js (Task 1 — GREEN target)
    - .planning/phases/40-deps-update-cost-ledger-snapshot-workflows/40-RESEARCH.md lines 232-280 (peter-evans/cpr@v8 exact step bodies for security + minor)
    - .planning/phases/40-deps-update-cost-ledger-snapshot-workflows/40-RESEARCH.md lines 861-934 (workflow skeleton)
    - .github/workflows/e2e-nightly.yml (full file — discover the exact `npm run e2e:smoke|regression|fault-injection` commands the deps-update-gate job needs to invoke, and the env/secrets contract for those scripts)
    - .github/workflows/e2e-weekly-digest.yml (style template for permissions block, concurrency, setup-node, env block)
    - scripts/check-deps-and-pr.mjs (40-02 deliverable — confirm $GITHUB_OUTPUT key names and body-path locations match what this workflow consumes)
  </read_first>
  <action>
    Create `.github/workflows/v40-deps-update.yml`. Header comment block citing Phase 40 Plan 40-03 (DEPS-01 + DEPS-02 + DEPS-03 + Pitfall 4 defenses + 40-CONTEXT locked decisions #2 cron-pin, #3 separate-frame-shift-file, #4 constant-branch-names + 40-RESEARCH lines 651-665 required-status-checks-deferral).

    Top-level keys:
    - `name: V40 Deps Update`
    - `on: { schedule: [{ cron: '0 9 * * 1' }], workflow_dispatch: {} }`
    - `concurrency: { group: v40-deps-update, cancel-in-progress: false }`
    - `permissions: { contents: write, pull-requests: write, issues: write }`

    Two jobs:

    **Job 1: `dep-scan`** — discovery + PR creation
    - `runs-on: ubuntu-latest`
    - `timeout-minutes: 15`
    - `env: { GH_TOKEN: ${{ secrets.GITHUB_TOKEN }} }`
    - Steps:
      1. `uses: actions/checkout@v4` (default persist-credentials: true)
      2. `uses: actions/setup-node@v4` with `node-version: 22` (literal — no .nvmrc; matches e2e-nightly.yml convention)
      3. `name: Install dependencies` — `run: npm ci`
      4. `name: Run dep scan + partition` — `id: scan`, `run: node scripts/check-deps-and-pr.mjs`. The script (from 40-02) writes to `$GITHUB_OUTPUT` and `/tmp/*-pr-body.md`.
      5. `name: Open security PR` — `if: steps.scan.outputs.security_count != '0'`, `uses: peter-evans/create-pull-request@v8`, with block:
         ```
         token: ${{ secrets.GITHUB_TOKEN }}
         branch: ${{ steps.scan.outputs.security_branch }}
         base: main
         draft: true
         title: "deps(security): bump ${{ steps.scan.outputs.security_packages }}"
         body-path: /tmp/security-pr-body.md
         labels: dependencies,security
         commit-message: "deps(security): bump ${{ steps.scan.outputs.security_packages }}"
         delete-branch: true
         signoff: false
         ```
      6. `name: Open grouped minor PR` — `if: steps.scan.outputs.minor_count != '0'`, `uses: peter-evans/create-pull-request@v8`, with block matching security PR shape but pointing at the minor branch/body:
         ```
         token: ${{ secrets.GITHUB_TOKEN }}
         branch: ${{ steps.scan.outputs.minor_branch }}
         base: main
         draft: true
         title: "deps: weekly minor bumps (${{ steps.scan.outputs.minor_packages }})"
         body-path: /tmp/minor-pr-body.md
         labels: dependencies,minor
         commit-message: "deps: weekly minor bumps"
         delete-branch: true
         signoff: false
         ```
      7. `name: Open manual-SDK-review issue` — `if: steps.scan.outputs.skipped_count != '0' || hashFiles('tests/e2e/.manual-sdk-bumps.json') != ''`. Actually simpler: gate on the script writing the file modified — use `if: steps.scan.outputs.skipped != ''`. The script from 40-02 currently does NOT emit a `skipped` output key — the executor should ADD it. The cleanest path: the executor of Task 2 ALSO patches `scripts/check-deps-and-pr.mjs` to emit `skipped_count=<n>` and `skipped_packages=<comma-sep>` to `$GITHUB_OUTPUT` in `emit()`. This is a tiny extension to 40-02's deliverable; document the patch in 40-03 SUMMARY as a back-port. Run block:
         ```
         gh issue create \
           --title "Manual SDK review required: ${{ steps.scan.outputs.skipped_packages }}" \
           --label "manual-sdk-review" \
           --body "Packages on NEVER_AUTO_BUMP have new versions available. See tests/e2e/.manual-sdk-bumps.json for the audit log." \
           --repo ${{ github.repository }} \
           2>&1 || true
         ```
         The `|| true` prevents a transient gh failure from failing the workflow (the audit-log file is the authoritative source-of-truth; the issue is a discoverability convenience).

    **Job 2: `deps-update-gate`** — the nightly-suite gate, NAMED for the Phase 47 required-status-checks slot reservation (40-RESEARCH lines 651-665)
    - This job runs on the SAME workflow trigger (cron + workflow_dispatch), NOT on `pull_request`. It runs the nightly suite against the CURRENT main branch state — its purpose is to PROVE the gate name exists and the suite shape is wired so Phase 47's CLEANUP audit can add the required-status-checks rule.
    - **IMPORTANT**: this job does NOT block the PR creation in `dep-scan` (no `needs:` dependency). Per 40-RESEARCH lines 651-665 + 40-CONTEXT line 73, the gate is ADVISORY in Phase 40. The dep-update PRs (draft + CODEOWNERS) provide the load-bearing defenses.
    - `runs-on: ubuntu-latest`
    - `timeout-minutes: 30` (nightly suite is long; cap at 30)
    - `env: { GH_TOKEN: ${{ secrets.GITHUB_TOKEN }} }`
    - Steps:
      1. `uses: actions/checkout@v4`
      2. `uses: actions/setup-node@v4` with `node-version: 22`
      3. `name: Install dependencies` — `run: npm ci`
      4. `name: Run nightly suite` — `run: |` block invoking the EXACT scripts that e2e-nightly.yml uses (executor must read e2e-nightly.yml and copy the invocation pattern; do NOT guess script names). Likely candidates: `npm run e2e:smoke`, `npm run e2e:regression`, plus the fault-injection scripts referenced in Phase 30. If e2e-nightly.yml uses Playwright config flags, mirror those.
      5. If the nightly suite invocations need extra env (e.g., `PLAYWRIGHT_BROWSERS_PATH`, `CI=true`), copy from e2e-nightly.yml.

    Do NOT include any of these (forbidden by Task 1 tests):
    - `[skip ci]` (D9 negative — deps-update commits via peter-evans/cpr go to PR branches, not main)
    - `gh pr merge --auto` (D10 negative — Pitfall 4)
    - `auto-merge: true` (D11 negative — Pitfall 4)
    - `id-token: write` or `actions: write` permissions (X8 negative — Pitfall 1 step 7)
    - Any `secrets.*PAT*` literal (X1 negative)
    - Any frame-shift pre-flight job (40-04 owns that as a separate workflow file per locked decision #3)
    - `node-version-file: '.nvmrc'` (no .nvmrc in repo; existing workflows use literal `node-version: 22`)

    **40-02 back-port** (Task 2 must ALSO patch `scripts/check-deps-and-pr.mjs`): add the `skipped_count` and `skipped_packages` lines to `emit()` so the manual-SDK-review issue step (X7) has the right output keys:
    ```js
    appendOutput(`skipped_count=${partition.skipped.length}`);
    appendOutput(`skipped_packages=${partition.skipped.join(',')}`);
    ```
    Also add a unit-test case to `tests/unit/check-deps-and-pr.test.js` Group E asserting these two output lines appear when the partition has skipped items. This is a 6-line code change + a small test extension; treat it as a 40-02 back-port and note in 40-03 SUMMARY. The 40-02 SUMMARY can be re-stamped or this can be noted as an additive change in the 40-03 SUMMARY's "Deviations" section.
  </action>
  <verify>
    <automated>cd /home/fatduck/patent-cite-tool &amp;&amp; npx vitest run tests/e2e/scripts/v40-deps-update-yaml.test.js tests/unit/check-deps-and-pr.test.js 2>&amp;1 | tail -25</automated>
  </verify>
  <done>
    `.github/workflows/v40-deps-update.yml` exists with `cron: '0 9 * * 1'`, two `peter-evans/create-pull-request@v8` invocations, a `deps-update-gate` job, and the manual-SDK-review issue step. All 19 Vitest D1-D11 + X1-X8 cases pass. The 40-02 unit tests still pass (with the 2 new skipped_count/skipped_packages assertions added). Static greps for SUMMARY: `grep -c "peter-evans/create-pull-request@v8" .github/workflows/v40-deps-update.yml` returns 2; `grep -c "draft: true" .github/workflows/v40-deps-update.yml` returns 2; `grep -c "delete-branch: true" .github/workflows/v40-deps-update.yml` returns 2; `grep -c "secrets.GITHUB_TOKEN" .github/workflows/v40-deps-update.yml` returns ≥3 (env block + 2 PR steps); `grep -v '^[[:space:]]*#' .github/workflows/v40-deps-update.yml | grep -c "PAT"` returns 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner (cron) → script subprocess (`npm`/`gh`) | The runner inherits secrets via `secrets.GITHUB_TOKEN`; subprocess output is read into PR body files. A compromised npm registry response (malicious package metadata) could inject content into PR bodies. |
| Runner → repo branches (peter-evans/cpr@v8 pushes) | Two `peter-evans/create-pull-request@v8` action invocations create branches under `v40-deps-update/*` and push them. Any workflow YAML modification that broadens this scope (e.g., to `main` directly, or removing `draft: true`) is a Pitfall 4 surface. |
| Cron trigger surface | Cron typo `0 */9 * * 1` would run every 9 hours on Mondays instead of once per Monday; multiplied by the nightly suite costs, this is a budget runaway risk (Pitfall 2 / Phase 38). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-03-01 | Tampering | Cron schedule | mitigate | YAML-level D1 pins exact `cron: '0 9 * * 1'`. |
| T-40-03-02 | EoP | `peter-evans/cpr@v8` with auto-merge | mitigate | D7 pins `draft: true` on both PR steps; D10 + D11 negative-pin `gh pr merge --auto` and `auto-merge: true` absence (Pitfall 4 defenses 4 + 5). |
| T-40-03-03 | EoP | Token elevation (PAT vs GITHUB_TOKEN) | mitigate | X1 positive-pins `secrets.GITHUB_TOKEN`; negative-pins any `secrets.*PAT*` literal (Pitfall 4 + Pitfall 1 step 7). |
| T-40-03-04 | EoP | `permissions:` block over-scoped | mitigate | X8 negative-pins `id-token: write` and `actions: write` (Pitfall 1 step 7). The block grants only `contents: write` + `pull-requests: write` + `issues: write` — minimum for the workflow's documented operations. |
| T-40-03-05 | Tampering | Branch sprawl from version-encoded names | mitigate | X2 + X3 pin constant per-package branch names + `delete-branch: true` (40-CONTEXT locked decision #4 + 40-RESEARCH Pitfall B). |
| T-40-03-06 | DoS | Premature required-status-checks rule blocks all PRs | mitigate | X5 asserts `deps-update-gate` job EXISTS as a name, but does NOT add it to the v4.0-main-protection ruleset (per 40-RESEARCH lines 651-665 + 40-CONTEXT line 73 — deferred to Phase 47). Phase 47 CLEANUP audit adds the rule after both Phase 40 + Phase 41 have run and stabilized. |
| T-40-03-07 | Tampering | Malicious npm package metadata injected into PR body | accept | `body-path: /tmp/*-pr-body.md` reads markdown that the script generates from validated `npm outdated --json` package names. npm registry package names are subject to npm's own validation; the script does NOT accept user-supplied package names. PR is DRAFT and human-reviewed before merge — secondary defense. |
| T-40-03-08 | Tampering | Manual-SDK-review issue body injection | mitigate | Issue body is a fixed-string template; no user-supplied content concatenated. `${{ steps.scan.outputs.skipped_packages }}` is a script-emitted comma-list of frozen WATCHLIST package names (closed enumeration). |
| T-40-03-SC | Tampering | npm install of a new dependency in this plan | n/a | This plan installs ZERO new npm dependencies — it uses the script from 40-02 (which also installs nothing). No package legitimacy gate applies. |
</threat_model>

<verification>
After Task 2 completes:

```bash
# 1. YAML guard passes
cd /home/fatduck/patent-cite-tool
npx vitest run tests/e2e/scripts/v40-deps-update-yaml.test.js
# Expected: 19/19 tests pass (D1-D11 + X1-X8)

# 2. Unit tests still pass (incl. back-ported skipped_count/skipped_packages assertions)
npx vitest run tests/unit/check-deps-and-pr.test.js
# Expected: all 40-02 tests + 2 new assertions pass

# 3. Cron is unique among workflows
grep -RH "cron:" .github/workflows/*.yml
# Expected: '0 9 * * 1' appears ONLY in v40-deps-update.yml; '0 7 * * 1' in e2e-weekly-digest; '0 2 * * *' in v40-cost-ledger-snapshot; '0 6 * * *' in e2e-nightly

# 4. Two peter-evans/cpr@v8 invocations
grep -c "peter-evans/create-pull-request@v8" .github/workflows/v40-deps-update.yml
# Expected: 2

# 5. Both draft: true
grep -c "draft: true" .github/workflows/v40-deps-update.yml
# Expected: 2

# 6. No PAT references
grep -v '^[[:space:]]*#' .github/workflows/v40-deps-update.yml | grep -c "PAT"
# Expected: 0

# 7. No Phase 39 regression
npx vitest run tests/unit/llm-ledger.test.js
# Expected: 54/54 pass
```
</verification>

<success_criteria>
- [ ] `.github/workflows/v40-deps-update.yml` exists with `cron: '0 9 * * 1'`
- [ ] All 19 YAML-level Vitest cases pass (D1-D11, X1-X8)
- [ ] All 40-02 unit tests pass (with 2 added assertions for skipped_count/skipped_packages outputs)
- [ ] Two `peter-evans/create-pull-request@v8` invocations exist, both `draft: true`, both `delete-branch: true`, both use `secrets.GITHUB_TOKEN`
- [ ] `deps-update-gate` job exists by exact name (slot reservation for Phase 47)
- [ ] Manual-SDK-review issue creation step exists, gated on `steps.scan.outputs.skipped_count != '0'`
- [ ] No new npm dependencies installed
- [ ] No Phase 39 regression (`tests/unit/llm-ledger.test.js` 54/54 still passes)
- [ ] No 40-01 regression (`tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js` still passes)
</success_criteria>

<output>
Create `.planning/phases/40-deps-update-cost-ledger-snapshot-workflows/40-03-SUMMARY.md` when done following @$HOME/.claude/get-shit-done/templates/summary.md
</output>
