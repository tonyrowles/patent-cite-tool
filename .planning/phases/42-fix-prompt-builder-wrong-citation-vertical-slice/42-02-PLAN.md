---
phase: 42-fix-prompt-builder-wrong-citation-vertical-slice
plan: 02
type: execute
wave: 2
depends_on: [42-01]
files_modified:
  - scripts/auto-fix.mjs
  - tests/e2e/lib/llm-driver.js
  - tests/unit/auto-fix.test.js
  - tests/unit/llm-driver-sdk-cache-control.test.js
autonomous: true
requirements: [AUTOFIX-01, AUTOFIX-03, AUTOFIX-04, AUTOFIX-05]
tags: [auto-fix-dispatcher, sdk-call, diff-guard, ls-remote-idempotency, fix-attempts-cap, cache-control, dry-run, no-push]

must_haves:
  truths:
    - "node scripts/auto-fix.mjs --issue <n> reads gh issue view, extracts ERROR_CLASS from labels and 12-hex fingerprint from `<!-- fp: ... -->` body line 1, routes to PROMPT_SCAFFOLDS[errorClass]"
    - "FLAKE / LLM_API_ERROR / PASS classes short-circuit WITHOUT invoking the SDK; the dispatcher writes a zero-cost ledger entry recording the skip (per CONTEXT decision Q2) and exits 0"
    - "Before invoking the SDK, the dispatcher runs `git ls-remote --heads origin auto-fix/<n>-<fp8>` and exits 0 with an 'already attempted' gh issue comment if the branch exists (AUTOFIX-04 idempotency)"
    - "Before invoking the SDK, the dispatcher calls countFixAttempts(ledger, fingerprint); if ≥3, adds `human-review-required` label via `gh issue edit --add-label` (idempotent `gh label create` precedes) and exits code 3 (AUTOFIX-05)"
    - "The dispatcher calls invokeAnthropicSdkWithLedger with model='claude-sonnet-4-6', phase='42-auto-fix', issueId=`issue-<n>`, and (when --cache-control) array-form systemBlocks with cache_control ttl:'1h'"
    - "After SDK call: parseFencedDiff extracts EXACTLY ONE diff between ===DIFF_START=== / ===DIFF_END=== fences; 0 or 2+ matches → ledger entry with errorReason:'malformed-diff' + exit 1"
    - "Diff path extraction via parsing `+++ b/...` lines; checkDiffGuard imported from scripts/check-diff-guard.mjs; violations → ledger entry + gh issue comment + exit 1 (AUTOFIX-03)"
    - "git apply --check (stdin) BEFORE git apply (stdin); apply-check failure → ledger entry with errorReason:'apply-check-failed' + exit 1"
    - "On successful apply: git checkout -b auto-fix/<n>-<fp8> + git commit -am + (UNLESS --no-push) git push -u origin"
    - "--dry-run prints the prompt + would-be cost projection + exits 0 WITHOUT calling the SDK or mutating the tree"
    - "--no-push skips the final git push (executor inspects the branch locally first); commit still lands"
    - "Exit codes: 0=applied/dry-run-ok/idempotent-skip/skip-class; 1=rejected (apply-check/diff-guard/malformed-diff/sdk_error); 2=arg or contract error (missing --issue, missing fingerprint line, no ERROR_CLASS label, multiple ERROR_CLASS labels); 3=fix_attempts cap reached"
    - "invokeAnthropicSdkWithLedger accepts an optional systemBlocks: Array<{type:'text', text:string, cache_control?:{type:'ephemeral', ttl:'5m'|'1h'}}> parameter; when supplied, the SDK request body's system field is the array (NOT the string form); back-compat preserved when only systemPrompt is supplied"
  artifacts:
    - path: "scripts/auto-fix.mjs"
      provides: "CLI dispatcher implementing AUTOFIX-01/03/04/05 + --dry-run + --no-push + skip-class ledger entries"
      min_lines: 250
    - path: "tests/e2e/lib/llm-driver.js"
      provides: "invokeAnthropicSdkWithLedger extended with optional systemBlocks array-form (cache_control support per Pitfall 6); back-compat string-form preserved"
    - path: "tests/unit/auto-fix.test.js"
      provides: "≥12 Vitest cases covering ERROR_CLASS routing (4 paths), git apply --check + diff-guard, ls-remote idempotency, fix_attempts cap, --dry-run no-SDK behavior, --no-push behavior, exit-code matrix; all subprocess calls mocked via vi.mock on node:child_process"
    - path: "tests/unit/llm-driver-sdk-cache-control.test.js"
      provides: "≥2 Vitest cases — string-form back-compat (system is string) and array-form (system is array with cache_control); mock @anthropic-ai/sdk client and assert request-body shape"
  key_links:
    - from: "scripts/auto-fix.mjs"
      to: "tests/e2e/lib/fix-prompt-builder.js"
      via: "named import — buildFixPrompt + PROMPT_SCAFFOLDS + ENVELOPE_OPEN/CLOSE + DIFF_FENCE_START/END (Plan 42-01 outputs)"
      pattern: "import.*from.*fix-prompt-builder"
    - from: "scripts/auto-fix.mjs"
      to: "scripts/check-diff-guard.mjs"
      via: "named import — checkDiffGuard + FORBIDDEN_PATHS (Phase 41-01 frozen)"
      pattern: "import.*from.*check-diff-guard"
    - from: "scripts/auto-fix.mjs"
      to: "tests/e2e/lib/llm-driver.js"
      via: "named import — invokeAnthropicSdkWithLedger (Phase 39 driver, extended in this plan for cache_control)"
      pattern: "import.*invokeAnthropicSdkWithLedger"
    - from: "scripts/auto-fix.mjs"
      to: "tests/e2e/lib/llm-ledger.js"
      via: "named import — countFixAttempts + appendLedgerEntry + readLedger + LEDGER_PATH (Plan 42-01 helper + Phase 39 primitives)"
      pattern: "import.*countFixAttempts"
    - from: "invokeAnthropicSdkWithLedger"
      to: "@anthropic-ai/sdk client.messages.create"
      via: "system field accepts string OR array — array enables cache_control to actually take effect (Pitfall 6)"
      pattern: "system:"
---

<objective>
Ship `scripts/auto-fix.mjs` — the Phase 42 CLI dispatcher that wires together Plan 42-01's `fix-prompt-builder.js` + `countFixAttempts` helper, Phase 41-01's `check-diff-guard.mjs`, and Phase 39's `invokeAnthropicSdkWithLedger` SDK driver into a single command (`node scripts/auto-fix.mjs --issue <n>`) that: routes ERROR_CLASS labels, short-circuits skip classes, enforces the AUTOFIX-04 `git ls-remote` idempotency before LLM calls, enforces the AUTOFIX-05 3-attempt cap, runs `git apply --check` + diff-guard before mutating the tree, branches + commits + pushes on success, and supports `--dry-run` (no SDK call) + `--no-push` (local inspection before push). Also extend `invokeAnthropicSdkWithLedger` with optional array-form `systemBlocks` so the WRONG_CITATION SYSTEM block can carry `cache_control: {type:'ephemeral', ttl:'1h'}` (Pitfall 6 — without this extension, cache_control is silently dropped and the ~30% cost savings on cache reads never materialize).

Purpose: Covers AUTOFIX-01/03/04/05 + the Pitfall 6 driver extension. Plan 42-03's manual demo invokes this dispatcher end-to-end against issue #3.

Output: 1 new dispatcher script, 1 extended driver function (additive back-compat — string-form callers unchanged), 2 Vitest files with ≥14 cases total covering routing + idempotency + diff-guard + cap-enforcement + dry-run + cache-control shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-fix-prompt-builder-wrong-citation-vertical-slice/42-CONTEXT.md
@.planning/phases/42-fix-prompt-builder-wrong-citation-vertical-slice/42-RESEARCH.md
@.planning/phases/42-fix-prompt-builder-wrong-citation-vertical-slice/42-01-SUMMARY.md
@.planning/phases/41-verifier-gate-workflow-verify-single-case-mjs-cli-shim/41-01-SUMMARY.md
@.planning/phases/39-sdk-driver-ledger-v2-branch-protection-wave-0/39-04-SUMMARY.md

# Source files this plan DEPENDS ON
@tests/e2e/lib/fix-prompt-builder.js
@tests/e2e/lib/llm-driver.js
@tests/e2e/lib/llm-ledger.js
@scripts/check-diff-guard.mjs
@tests/e2e/lib/error-codes.js

# Reference scripts that use the same patterns this plan applies
@scripts/verify-single-case.mjs
@scripts/parse-affected-cases.mjs

<interfaces>
<!-- Key contracts this dispatcher consumes. Use these directly — no exploration. -->

From tests/e2e/lib/fix-prompt-builder.js (Plan 42-01 output):
- export const ENVELOPE_OPEN = '<issue_body_untrusted>'
- export const ENVELOPE_CLOSE = '</issue_body_untrusted>'
- export const DIFF_FENCE_START = '===DIFF_START==='
- export const DIFF_FENCE_END = '===DIFF_END==='
- export const PROMPT_SCAFFOLDS: Object.freeze({ WRONG_CITATION: () => string })  // EXACTLY 1 key in Phase 42
- export function buildFixPrompt({errorClass, issueBody}): {ok:true, systemPrompt, userPrompt} | {ok:false, escalate:'re-quarantine'|'retry'|'close-as-pass'|`unsupported-class:${string}`}

From tests/e2e/lib/llm-driver.js (Phase 39 — EXTEND in this plan):
- export async function invokeAnthropicSdkWithLedger({systemPrompt, userPrompt, model='claude-sonnet-4-6', maxTokens=4096, timeoutMs=120_000, phase, issueId, prNumber, forceApi=false}): Promise<{ok:true, llmText, modelId, costUsd, rawJson} | {ok:false, ciGate?, capBlocked?, errorReason?, errorMessage?}>
- Current call site at line 539: `system: systemPrompt` (string-form — DROPS cache_control silently per Pitfall 6)
- EXTENSION REQUIRED (this plan): accept optional `systemBlocks: Array<{type:'text', text:string, cache_control?:{type:'ephemeral', ttl:'5m'|'1h'}}>`; when provided, pass `system: systemBlocks` to client.messages.create; back-compat when only systemPrompt provided
- Return shape on success: `{ok:true, llmText, modelId, costUsd, rawJson}` where llmText is `response.content[0].text`

From tests/e2e/lib/llm-ledger.js (Phase 39 + Plan 42-01):
- export const LEDGER_PATH: string
- export function readLedger(ledgerPath = LEDGER_PATH): {version:1, months:{...}}
- export function appendLedgerEntry(ledgerPath, entry): void
- export function countFixAttempts(ledger, fingerprint): number  // Plan 42-01 addition
- Ledger entry shape for this plan: {iso, model, cost_usd, tokens_in, tokens_out, phase:'42-auto-fix', transport:'sdk', issueId:`issue-<n>`, fingerprint:<12hex>, source:'auto-fix-api', ...optional: errorReason, branchExisted, diffApplied, escalate}

From scripts/check-diff-guard.mjs (Phase 41-01):
- export const FORBIDDEN_PATHS: Object.freeze([RegExp...])  // 6 frozen regexes
- export function checkDiffGuard(changedPaths: string[]): {ok: boolean, violations: string[]}

From tests/e2e/lib/error-codes.js:
- export const ERROR_CLASSES: Object.freeze([...8 canonical class names])
- The 4 in scope for Phase 42 dispatcher: WRONG_CITATION (full builder), FLAKE (skip), LLM_API_ERROR (skip), PASS (skip); other 4 → `{ok:false, escalate:'unsupported-class:<X>'}` per Plan 42-01's buildFixPrompt contract

From scripts/parse-affected-cases.mjs / scripts/verify-single-case.mjs (Phase 41 — convention reference):
- Use `import { parseArgs } from 'node:util'` for argv parsing
- Use `import { execFileSync } from 'node:child_process'` with explicit arg arrays (CWE-94 / Plan 41-03 decision #4)
- Use `if (import.meta.url === \`file://${process.argv[1]}\`)` to make the file dual-purpose (importable for testing + CLI executable)

Live demo target (verified at research time — Plan 42-03 will exercise):
- gh issue view 3 returns body containing `<!-- fp: 139f821b3bb1 -->` on line 1
- Labels contain 'WRONG_CITATION'
- fp8 = '139f821b'; branch name = 'auto-fix/3-139f821b'
- case-id (from `case-id:` line in body): 'US11427642-spec-short-1'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Write failing Vitest cases for auto-fix.mjs dispatcher (routing + ls-remote idempotency + diff-guard + apply-check + cap enforcement + dry-run + no-push) AND for invokeAnthropicSdkWithLedger systemBlocks extension</name>
  <files>tests/unit/auto-fix.test.js, tests/unit/llm-driver-sdk-cache-control.test.js</files>
  <read_first>
    - tests/e2e/lib/fix-prompt-builder.js (Plan 42-01 output — the dispatcher's prompt-building consumer; the named exports the dispatcher will import)
    - tests/e2e/lib/llm-driver.js lines 493-604 (current invokeAnthropicSdkWithLedger; line 539 is the string-form system field this plan extends)
    - tests/e2e/lib/llm-ledger.js the new countFixAttempts export (Plan 42-01) + appendLedgerEntry + LEDGER_PATH
    - scripts/check-diff-guard.mjs lines 46-73 (FORBIDDEN_PATHS regex bank + checkDiffGuard signature; the dispatcher imports these for AUTOFIX-03)
    - scripts/verify-single-case.mjs (Phase 41-02 — argv pattern with parseArgs + dual-purpose `import.meta.url === file://${process.argv[1]}` shim; this dispatcher follows the same pattern)
    - tests/unit/llm-driver.test.js (Phase 39-03 — pattern for mocking @anthropic-ai/sdk via vi.mock; the new cache-control test mirrors this)
    - .planning/phases/42-fix-prompt-builder-wrong-citation-vertical-slice/42-RESEARCH.md §System Architecture Diagram (the 18-step dispatcher sequence; every step has at least one test assertion in this Task), §Code Examples Pattern 3 (parseFencedDiff regex + changedPathsFromDiff parser), Pattern 6 (the invokeAnthropicSdkWithLedger call-site shape + Pitfall 6 array-form requirement), Pattern 7 (live gh issue view 3 JSON payload — the test fixture mirrors this shape byte-for-byte), Pattern 8 (git apply --check + git ls-remote --heads invocation semantics + the execFileSync input:diffText pattern)
  </read_first>
  <behavior>
    - tests/unit/auto-fix.test.js (NEW): ≥12 Vitest cases organized into 6 describe blocks. ALL external side effects mocked via vi.mock — no real gh calls, no real git calls, no real SDK calls, no real ledger writes.

    describe('AUTOFIX-01: ERROR_CLASS routing'):
      * gh issue view returning a body with `<!-- fp: 139f821b3bb1 -->` + labels containing 'WRONG_CITATION' → dispatcher invokes invokeAnthropicSdkWithLedger with phase='42-auto-fix', issueId='issue-3' (SDK call mocked to return a valid fenced diff; downstream git mocks succeed; exit 0)
      * Labels containing 'FLAKE' → no SDK call; ledger entry written with escalate:'re-quarantine'; exit 0
      * Labels containing 'LLM_API_ERROR' → no SDK call; ledger entry with escalate:'retry'; exit 0
      * Labels containing 'PASS' → no SDK call; ledger entry with escalate:'close-as-pass'; exit 0
      * Labels containing NO ERROR_CLASS-matching name → exit code 2 (contract error)
      * Labels containing TWO ERROR_CLASS-matching names (e.g., WRONG_CITATION + FLAKE) → exit code 2 (contract error: ambiguous class)

    describe('AUTOFIX-03: diff-guard + git apply --check pre-application'):
      * LLM returns a fenced diff modifying tests/test-cases.js → checkDiffGuard violations recorded → ledger entry with errorReason mentioning the violated path → gh issue comment posted → exit 1 (git apply NOT invoked — mock verified)
      * LLM returns a fenced diff modifying src/foo.js but git apply --check (mocked to throw err.status===1, err.stderr='hunk failed at line 5') → ledger entry with errorReason:'apply-check-failed' → exit 1 (git apply NOT invoked beyond --check)
      * LLM returns malformed output with zero fences → ledger entry with errorReason starting with 'malformed-diff' → exit 1
      * LLM returns malformed output with 2 fence pairs → ledger entry with errorReason starting with 'malformed-diff' → exit 1
      * LLM returns a fenced diff modifying src/foo.js + git apply --check passes + git apply succeeds → git checkout -b auto-fix/3-139f821b + git commit + (depending on --no-push) git push invoked → exit 0

    describe('AUTOFIX-04: git ls-remote idempotency'):
      * git ls-remote --heads origin auto-fix/3-139f821b mocked to return a non-empty line → SDK NOT invoked → ledger entry with branchExisted:true → gh issue comment with "already attempted" wording → exit 0
      * git ls-remote returns empty output → SDK IS invoked (mock vi.mocked(invokeAnthropicSdkWithLedger).called === true)

    describe('AUTOFIX-05: fix_attempts cap at 3'):
      * Seed ledger with 3 prior matching `phase:'42-auto-fix', fingerprint:'139f821b3bb1'` iterations → dispatcher does NOT invoke SDK → `gh label create human-review-required --force` invoked (idempotent) → `gh issue edit 3 --add-label human-review-required` invoked → exit code 3
      * Seed ledger with 2 prior matching iterations → SDK IS invoked (cap is "≥3 prior", so 3rd attempt is allowed; the 4th would be refused)
      * Seed ledger with 3 matching for fingerprint A + 0 for fingerprint B → dispatching against fingerprint B → SDK IS invoked (counter is per-fingerprint, not global)

    describe('--dry-run mode'):
      * --dry-run set → SDK NOT invoked; git apply NOT invoked; git push NOT invoked; ledger entry NOT written; stdout contains the rendered systemPrompt + userPrompt (envelope-wrapped); exit 0

    describe('--no-push mode'):
      * --no-push set + happy-path SDK + git apply success → git checkout -b + git commit invoked; git push NOT invoked; stdout contains "branch staged locally; push manually with: git push -u origin auto-fix/3-139f821b"; exit 0

    describe('contract errors'):
      * --issue missing → exit code 2 with stderr message naming --issue as required
      * Issue body lacking `<!-- fp: ` line → exit code 2 with stderr message "issue body missing fingerprint:; this is a v3.1 contract violation"

    - tests/unit/llm-driver-sdk-cache-control.test.js (NEW): ≥3 Vitest cases. Mock the `@anthropic-ai/sdk` default-export class so client.messages.create is a vi.fn(); assert the request body shape:
      * Calling invokeAnthropicSdkWithLedger with systemPrompt='SYS' only → captured request body's `system` field is the literal string 'SYS' (back-compat preserved; existing v3.1 behavior unchanged)
      * Calling with systemBlocks: [{type:'text', text:'SYS', cache_control:{type:'ephemeral', ttl:'1h'}}] (no systemPrompt OR ignored when both present) → captured request body's `system` field is the array literal [{type:'text', text:'SYS', cache_control:{type:'ephemeral', ttl:'1h'}}]
      * Calling with NEITHER systemPrompt NOR systemBlocks → returns {ok:false, ...} or throws with a contract-violation error (no SDK call)
      * Setup: process.env.CI='true' or forceApi:true to satisfy the INVERSE CI gate; mock ledger primitives to return $0 caps; mock the client to return a valid usage payload

    ALL tests in BOTH files MUST fail at this commit (RED). Module-not-found on scripts/auto-fix.mjs is the expected RED signal for auto-fix.test.js. For llm-driver-sdk-cache-control.test.js: the test asserting array-form request body fails because the current line 539 always passes a string.
  </behavior>
  <action>
    Write the 2 Vitest files per the behavior block. Conventions to follow:
    - Use `import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'`
    - For auto-fix.test.js: mock node:child_process via `vi.mock('node:child_process', () => ({ execFileSync: vi.fn() }))` so each test programs the mock return per command (gh / git / git apply --check by inspecting first arg + arg array)
    - For auto-fix.test.js: mock the SDK driver via `vi.mock('../tests/e2e/lib/llm-driver.js', () => ({ invokeAnthropicSdkWithLedger: vi.fn() }))` and program the return per test
    - For auto-fix.test.js: use a tmpDir ledger (mkdtempSync + writeFileSync with seed ledger) and pass via E2E_LEDGER_PATH_OVERRIDE env var per Phase 39 LEDGER_PATH convention; alternatively (preferred) mock readLedger / appendLedgerEntry / countFixAttempts directly via `vi.mock('../tests/e2e/lib/llm-ledger.js', ...)`
    - For auto-fix.test.js: invoke the dispatcher logic by importing a NAMED EXPORT (e.g., `import { runDispatcher } from '../../scripts/auto-fix.mjs'`) — DO NOT spawn the script as a subprocess (subprocess testing is integration-shaped and slow); the dispatcher must be split into (1) a `runDispatcher({issue, transport, forceApi, dryRun, noPush}): Promise<exitCode>` named export and (2) a thin `if (import.meta.url === ...)` shim at file end that calls runDispatcher with argv-parsed args. The shim itself is NOT unit-tested; runDispatcher IS.
    - For llm-driver-sdk-cache-control.test.js: use `vi.mock('@anthropic-ai/sdk', () => ({ default: vi.fn().mockImplementation(() => ({ messages: { create: vi.fn() } })) }))` pattern; capture the call args via the inner vi.fn() to assert the system-field shape

    Commit with message: `test(42-02): RED — auto-fix dispatcher routing/idempotency/diff-guard/cap + cache-control driver extension`
  </action>
  <verify>
    <automated>npx vitest run tests/unit/auto-fix.test.js tests/unit/llm-driver-sdk-cache-control.test.js 2>&1 | grep -vE '^#' | grep -E 'Error: Failed to load|Cannot find module|FAIL|failed' | head -5</automated>
  </verify>
  <done>
    Both test files exist; vitest run exits non-zero (module-not-found on auto-fix OR assertion failures on the systemBlocks array assertion); git log shows `test(42-02): RED — ...`; no source files modified in this commit (`git diff HEAD~1 --name-only` returns only the 2 test files); all 18+ pre-existing Plan 42-01 tests + 835+ full suite cases still pass when scoped (`npm run test:src` minus the 2 new files exits 0).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Ship scripts/auto-fix.mjs dispatcher AND extend invokeAnthropicSdkWithLedger with array-form systemBlocks</name>
  <files>scripts/auto-fix.mjs, tests/e2e/lib/llm-driver.js</files>
  <read_first>
    - tests/e2e/lib/llm-driver.js full file 1-605 (the EXTEND target — the systemBlocks parameter goes in the destructured opts at line 503; the client.messages.create call at lines 536-541 is the conditional dispatch point: `system: systemBlocks ?? systemPrompt`)
    - tests/e2e/lib/fix-prompt-builder.js (Plan 42-01 output — the dispatcher imports buildFixPrompt + PROMPT_SCAFFOLDS + DIFF_FENCE_START + DIFF_FENCE_END + ENVELOPE_OPEN + ENVELOPE_CLOSE)
    - scripts/check-diff-guard.mjs (the import target: checkDiffGuard + FORBIDDEN_PATHS — DO NOT re-define the 6 paths inline; import is single-source-of-truth per Phase 41-01 contract)
    - scripts/verify-single-case.mjs (Phase 41-02 — argv pattern + dual-purpose shim; this file is the closest existing analog in shape)
    - tests/e2e/lib/llm-ledger.js (Plan 42-01 output for countFixAttempts; Phase 39 outputs for LEDGER_PATH, readLedger, appendLedgerEntry, checkSpendCap)
    - .planning/phases/42-fix-prompt-builder-wrong-citation-vertical-slice/42-RESEARCH.md §System Architecture Diagram (the 18-step dispatcher sequence — implement each step in order), §Code Examples Pattern 3 (parseFencedDiff: regex /===DIFF_START===\\n([\\s\\S]*?)\\n===DIFF_END===/g; 0 or multiple matches → malformed-diff), Pattern 6 (the invokeAnthropicSdkWithLedger call site shape), Pattern 7 (gh issue view JSON shape — body parsing: fingerprint regex /^<!-- fp: ([0-9a-f]{12}) -->/m, case-id regex /^case-id: (.+)$/m, ERROR_CLASS extraction by filtering labels against ERROR_CLASSES from error-codes.js), Pattern 8 (git apply --check + git ls-remote --heads execFileSync invocation semantics)
  </read_first>
  <action>
    EXTEND `tests/e2e/lib/llm-driver.js` (Pitfall 6 fix): add optional `systemBlocks` to the destructured opts of `invokeAnthropicSdkWithLedger` (around line 502). At the client.messages.create call (currently line 536-541), pass `system: systemBlocks ?? systemPrompt` — when systemBlocks is supplied, the SDK receives the array form and cache_control takes effect; when only systemPrompt is supplied, the string form is preserved (back-compat — all existing callers in v3.1 + Phase 39/40/41 keep working). Add a defensive contract: if BOTH systemBlocks AND systemPrompt are undefined → return `{ok:false, errorReason:'contract-error', errorMessage:'systemBlocks or systemPrompt required'}` BEFORE the cap-precheck (or as part of Step 1). Update the JSDoc block (lines 475-491) to document systemBlocks: `systemBlocks?: Array<{type:'text', text:string, cache_control?:{type:'ephemeral', ttl:'5m'|'1h'}}> — optional array-form system block; enables cache_control. Mutually-or with systemPrompt.`

    Create `scripts/auto-fix.mjs` per the 18-step sequence in RESEARCH §System Architecture Diagram. Module structure:
    - Leading comment block: file purpose, Phase 42 reqs implemented (AUTOFIX-01/03/04/05), Pitfall 6 cache_control consumer, exit codes
    - Imports: `parseArgs` from 'node:util'; `execFileSync` from 'node:child_process'; `buildFixPrompt, PROMPT_SCAFFOLDS, ENVELOPE_OPEN, ENVELOPE_CLOSE, DIFF_FENCE_START, DIFF_FENCE_END` from '../tests/e2e/lib/fix-prompt-builder.js'; `invokeAnthropicSdkWithLedger` from '../tests/e2e/lib/llm-driver.js'; `appendLedgerEntry, readLedger, LEDGER_PATH, countFixAttempts` from '../tests/e2e/lib/llm-ledger.js'; `checkDiffGuard` from './check-diff-guard.mjs'; `ERROR_CLASSES` from '../tests/e2e/lib/error-codes.js'
    - Inline helpers: `parseFencedDiff(llmText)` per RESEARCH §Pattern 3; `changedPathsFromDiff(diff)` parsing `+++ b/...` lines; `extractFingerprint(body)` regex /^<!-- fp: ([0-9a-f]{12}) -->/m (try v2 first; v1 `<!-- fingerprint: ...` is documented as legacy but the live corpus uses v2); `extractCaseId(body)` regex /^case-id: (.+)$/m; `extractErrorClass(labels)` filter against ERROR_CLASSES — return exactly-one or throw contract error
    - Main `runDispatcher({issue, transport='sdk', forceApi=false, dryRun=false, noPush=false})` named export implementing the 18 steps:
      1. Argv already destructured; default transport='sdk' (subscription is Phase 46)
      2. `execFileSync('gh', ['issue', 'view', String(issue), '--json', 'body,labels,title,number,assignees'])` → JSON.parse
      3. Extract fingerprint (exit 2 if missing); compute fp8 = fingerprint.slice(0,8); branchName = `auto-fix/${issue}-${fp8}`
      4. Extract ERROR_CLASS (exit 2 if zero or multi-matches against ERROR_CLASSES set)
      5. `countFixAttempts(readLedger(), fingerprint)` — if ≥3: `execFileSync('gh', ['label','create','human-review-required','--force'])` (ignore err) + `execFileSync('gh', ['issue','edit',String(issue),'--add-label','human-review-required'])` + exit 3
      6. `execFileSync('git', ['ls-remote','--heads','origin', branchName], {encoding:'utf8'})` — if .trim().length > 0: appendLedgerEntry({...skipEntry with branchExisted:true}) + gh issue comment ("already attempted (prior fingerprint <fp>, attempt N)") + exit 0
      7. `buildFixPrompt({errorClass, issueBody})` → if {ok:false, escalate}: appendLedgerEntry({phase:'42-auto-fix', transport:'sdk', fingerprint, escalate, cost_usd:0, ...}) (zero-cost skip-class entry per CONTEXT decision Q2) + log + exit 0
      8. if (dryRun): print {systemPrompt, userPrompt} + "(dry-run: SDK not invoked, ledger not written, branch not pushed; fix_attempts counter NOT incremented per RESEARCH Pitfall 6)" + exit 0
      9. Build systemBlocks array with cache_control: `[{type:'text', text: systemPrompt, cache_control: {type:'ephemeral', ttl:'1h'}}]` (Pitfall 6 — Plan 42-02's driver extension makes this actually take effect)
     10. `await invokeAnthropicSdkWithLedger({systemBlocks, userPrompt, model:'claude-sonnet-4-6', phase:'42-auto-fix', issueId: \`issue-${issue}\`, forceApi})` — handle result.ok===false branches: ciGate → exit 2 with --force-api hint; capBlocked → exit 3 with cap details (re-use the cap-block exit code); sdk_error → exit 1
     11. `parseFencedDiff(result.llmText)` — if !ok: ledger entry with errorReason starting with 'malformed-diff' + exit 1
     12. `changedPathsFromDiff(parsed.diff)` → `checkDiffGuard(paths)` → if !ok: ledger entry with errorReason mentioning the violated paths + gh issue comment naming the violated paths + exit 1
     13. `execFileSync('git', ['apply', '--check'], {input: parsed.diff, stdio: ['pipe','pipe','pipe']})` in try/catch — on catch: ledger entry with errorReason:'apply-check-failed' + stderr snippet (slice 500) + exit 1
     14. `execFileSync('git', ['apply'], {input: parsed.diff, stdio:['pipe','inherit','inherit']})`
     15. `execFileSync('git', ['checkout', '-b', branchName])`
     16. `execFileSync('git', ['commit', '-am', \`Fix #${issue}: ${errorClass}\`])`
     17. if (!noPush): `execFileSync('git', ['push', '-u', 'origin', branchName])`; else: print "branch staged locally; push manually with: git push -u origin <branchName>"
     18. Print suggested gh pr create command: `gh pr create --draft --base main --head ${branchName} --title 'auto-fix: ${errorClass} for ${caseId}' --body '<!-- affected_cases: ${caseId} -->\\nfingerprint: ${fingerprint}\\nfix_attempts: ${attempts + 1}\\nledger_entry: ${ledgerEntryId}'` (manual Phase 42 demo invocation; Phase 43 will automate via peter-evans/create-pull-request@v8)
    - Dual-purpose shim: `if (import.meta.url === \`file://${process.argv[1]}\`)` block parses argv via parseArgs and calls runDispatcher; argv spec: `--issue <required>`, `--transport <default:sdk>`, `--force-api <boolean>`, `--dry-run <boolean>`, `--no-push <boolean>`; missing --issue exits 2

    All 14+ tests from Task 1 MUST PASS. Existing Phase 39 SDK driver tests (any test file covering invokeAnthropicSdkWithLedger) MUST continue to pass (back-compat preserved).

    Commit with message: `feat(42-02): auto-fix.mjs dispatcher + invokeAnthropicSdkWithLedger systemBlocks extension`
  </action>
  <verify>
    <automated>npx vitest run tests/unit/auto-fix.test.js tests/unit/llm-driver-sdk-cache-control.test.js 2>&1 | tail -5 | grep -E 'passed.*\(.*\)|Test Files.*passed'</automated>
  </verify>
  <done>
    Both test files PASS (≥14 new green cases); `npm run test:src` exits 0 with no regressions; `npm run lint` exits 0 (auto-fix.mjs uses execFileSync with explicit arg arrays — no shell injection surface); `git log --oneline -1` shows `feat(42-02): ...`; grep verification:
    - `grep -c "checkDiffGuard\|FORBIDDEN_PATHS" scripts/auto-fix.mjs` returns ≥1 (proves the import landed; NO inline re-definition of the 6 paths)
    - `grep -cE "^const FORBIDDEN_PATHS\s*=" scripts/auto-fix.mjs` returns 0 (proves no duplication of Phase 41-01's frozen bank)
    - `grep -c "phase: '42-auto-fix'" scripts/auto-fix.mjs` returns ≥3 (skip-class entry + branchExisted entry + the main SDK call entry's phase argument)
    - `grep -c "auto-fix/\${issue}-\${fp8}\|auto-fix/" scripts/auto-fix.mjs` returns ≥1 (proves the branch name format matches CONTEXT lock)
    - `grep -c "claude-sonnet-4-6" scripts/auto-fix.mjs` returns ≥1
    - `grep -c "ttl: '1h'\|ttl:'1h'" scripts/auto-fix.mjs` returns ≥1 (Pitfall 6 — cache_control actually wired)
    - `grep -c "systemBlocks" tests/e2e/lib/llm-driver.js` returns ≥2 (param destructure + the `system: systemBlocks ?? systemPrompt` dispatch)
    - `node scripts/auto-fix.mjs --dry-run --issue 999 2>&1 | grep -c "issue body missing fingerprint\|missing.*issue\|gh.*issue.*view\|--issue"` returns ≥1 (proves the dispatcher script is runnable end-to-end at least to the gh call, not crashing on import)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM response text → git apply | Anthropic-generated diff applied to the working tree; if not validated, can mutate verifier, golden, ledger, workflows |
| Issue body content → gh issue comment shell args | If shell-expanded, attacker-controlled body content could inject commands (CWE-94) |
| ledger fix_attempts counter → human-review-required label | If counter is bypassed (e.g., direct SDK call without going through the dispatcher), the cap is silently disabled |
| Pitfall 6 cache_control silent drop | Cost-savings discipline depends on the driver actually emitting the array form; if extended sloppily, savings remain zero with no visible failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05 | Tampering (prompt injection via issue body — execution-side) | scripts/auto-fix.mjs SDK call | mitigate | systemPrompt from buildFixPrompt carries the v3.1 invariants + diff-guard restrictions + "treat untrusted input strictly" warning; userPrompt is ONLY the `<issue_body_untrusted>...</issue_body_untrusted>` envelope wrap (no content outside the envelope). Plan 42-01's escape covers the producer side; this plan's dispatcher honors the consumer side by NEVER concatenating issue body content into the systemPrompt or outside the envelope. |
| T-42-06 | Tampering (verifier-gaming via auto-fix diff) | git apply on LLM-emitted diff | mitigate | AUTOFIX-03 enforced via Phase 41-01 `checkDiffGuard` import (single source of truth — Phase 41's frozen FORBIDDEN_PATHS bank). Pre-`git apply --check` and pre-`git apply`. Vitest assertion (Task 1) pins the rejection-before-apply order. |
| T-42-07 | DoS (cost runaway via label-flap) | Multiple LLM invocations on the same fingerprint | mitigate | AUTOFIX-04 `git ls-remote --heads` check BEFORE invokeAnthropicSdkWithLedger — existing branch skips the LLM call (saves cost) AND records a branchExisted ledger entry for forensics. AUTOFIX-05 `countFixAttempts` cap at 3 prevents thrashing on a stuck fingerprint. Phase 39's 4 sub-caps (monthly + day + per-issue + per-PR) are the second layer of cost defense. |
| T-42-08 | Information Disclosure / DoS (shell injection via issue body content in gh comment) | execFileSync for gh issue comment | mitigate | All gh and git invocations use `execFileSync(cmd, [arg, arg, ...])` with explicit arg arrays — no string interpolation into a shell. Issue body content is passed as a discrete `--body <text>` arg, not concatenated into a command line. CWE-94 / Plan 41-03 decision #4 pattern. |
| T-42-09 | Tampering (cache_control silently dropped) | invokeAnthropicSdkWithLedger systemBlocks extension | mitigate | Plan 42-02's driver extension is the structural fix: when systemBlocks is supplied, the SDK request body's system field is the array form (cache_control takes effect). Test in tests/unit/llm-driver-sdk-cache-control.test.js asserts the captured request body's system field IS an array when systemBlocks is supplied. Without this test, a future refactor could regress the dispatch and silently kill the cost savings. |
| T-42-10 | DoS (concurrent dispatcher invocations from two devs) | scripts/auto-fix.mjs | accept | CONTEXT decision Q3: ACCEPT for Phase 42 local-only. Phase 43's workflow concurrency group (`v40-auto-fix-${{ event.issue.number }}` with cancel-in-progress:false) is the proper defense. Document the local-only acceptance in the SUMMARY. |
| T-42-SC | Tampering | npm/pip/cargo installs | N/A | Phase 42 introduces NO new dependencies. Reuses Phase 39 @anthropic-ai/sdk@0.100.1 install + Phase 41-01 check-diff-guard.mjs (no install). |
</threat_model>

<verification>
- `npx vitest run tests/unit/auto-fix.test.js tests/unit/llm-driver-sdk-cache-control.test.js` exits 0 after Task 2 GREEN (≥14 new green cases)
- `npm run test:src` exits 0 — no regressions in the ~853+ existing test cases (Phase 42-01's ~18 + the 835 baseline + this plan's ~14)
- `npm run lint` exits 0 — auto-fix.mjs uses execFileSync with explicit arg arrays; no `child_process.exec` anywhere
- `node scripts/auto-fix.mjs --help` or `node scripts/auto-fix.mjs` (no args) exits 2 with a usage/error message naming `--issue` as required
- `git log --oneline -2` shows the TDD discipline: `test(42-02): RED — ...` THEN `feat(42-02): ...`
- `grep -c "phase: '42-auto-fix'" scripts/auto-fix.mjs` returns ≥3 (one per ledger entry call site: skip-class + branchExisted + the main SDK call — even though the SDK call itself adds phase via the driver's appendLedgerEntry, the dispatcher passes `phase:'42-auto-fix'` to invokeAnthropicSdkWithLedger so the driver-side entry carries it)
- `grep -c "import.*check-diff-guard" scripts/auto-fix.mjs` returns 1 (single import, no inline FORBIDDEN_PATHS re-definition)
- `grep -c "ttl: '1h'\|ttl:'1h'" scripts/auto-fix.mjs` returns ≥1 (Pitfall 6 — cache_control actually wired into the systemBlocks payload)
- `grep -c "systemBlocks" tests/e2e/lib/llm-driver.js` returns ≥2 (destructure + dispatch)
</verification>

<success_criteria>
- AUTOFIX-01 satisfied: dispatcher routes WRONG_CITATION to full SDK call path; FLAKE/LLM_API_ERROR/PASS short-circuit with their escalate strings; ambiguous or missing ERROR_CLASS labels exit 2 (Vitest pinned)
- AUTOFIX-03 satisfied: dispatcher imports checkDiffGuard from Phase 41-01 (no inline re-definition); runs git apply --check + diff-guard BEFORE git apply; rejection paths post gh issue comments and exit 1 (Vitest pinned)
- AUTOFIX-04 satisfied: git ls-remote --heads check precedes any SDK invocation; existing branch exits 0 with "already attempted" comment and a branchExisted ledger entry — saves cost on label-flap (Vitest pinned)
- AUTOFIX-05 satisfied: countFixAttempts(ledger, fingerprint) ≥3 triggers `gh label create human-review-required --force` + `gh issue edit --add-label` + exit 3 (Vitest pinned)
- CONTEXT decisions honored:
  * Q1 (--no-push flag): implemented; allows demo executor branch inspection before push
  * Q2 (skip-class ledger entries): zero-cost entries written for FLAKE / LLM_API_ERROR / PASS for weekly-digest analytics
  * Q3 (two-dev concurrent): accepted for Phase 42 local-only; Phase 43 workflow concurrency group is the real defense; documented in SUMMARY
  * Q4 (Pitfall 6 cache_control): invokeAnthropicSdkWithLedger extended with optional systemBlocks parameter; dispatcher uses array form with cache_control ttl:'1h' on the SYSTEM block; Vitest pinned
- --dry-run mode: prints the rendered prompt + skip notice; no SDK call, no ledger write, no tree mutation, no push; exit 0
- Exit code matrix matches CONTEXT (0/1/2/3) with Vitest coverage for each path
- Back-compat: all Phase 39/40/41 callers of invokeAnthropicSdkWithLedger (string-form systemPrompt only) continue to work unchanged; existing v3.1 callers of invokeClaudePWithLedger are unaffected
</success_criteria>

<output>
Create `.planning/phases/42-fix-prompt-builder-wrong-citation-vertical-slice/42-02-SUMMARY.md` documenting:
- The 2 TDD commits (RED + GREEN)
- Exact LOC added to scripts/auto-fix.mjs + the systemBlocks delta in tests/e2e/lib/llm-driver.js
- Confirmation that AUTOFIX-01/03/04/05 are shipped + Pitfall 6 cache_control is actually wired (cite the captured-request-body Vitest case as the regression guard)
- Confirmation that Phase 41-01's checkDiffGuard is imported (single source of truth — no inline duplication)
- Confirmation that the 18-step sequence from RESEARCH §System Architecture Diagram is implemented in order; each step traceable to a Vitest case
- Confirmation that CONTEXT Q1 (--no-push), Q2 (skip-class ledger entries), Q3 (accept-concurrency), Q4 (cache_control extension) are all addressed
- Note that Plan 42-03 can now invoke `node scripts/auto-fix.mjs --issue 3 --force-api --no-push` against the live demo target (issue #3, fingerprint 139f821b3bb1, branch auto-fix/3-139f821b)
- Cost projection for the demo: ~$0.05-$0.15 for one Sonnet 4.6 call on a small WRONG_CITATION fix per RESEARCH (5k input + 1k output tokens)
</output>