---
phase: 45-per-error-class-expansion-flake-5-state-machine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/lib/fix-prompt-builder.js
  - tests/unit/fix-prompt-builder.test.js
  - tests/unit/fixtures/llm-hallucinated-selection-issue.md
  - tests/unit/fixtures/worker-fallback-failed-issue.md
  - tests/unit/fixtures/google-dom-drift-issue.md
  - tests/unit/fixtures/harness-error-issue.md
autonomous: true
requirements:
  - PROMPT-03
tags:
  - patent-citation
  - fix-prompt
  - prompt-injection-defense
  - frozen-registry

must_haves:
  truths:
    - "PROMPT_SCAFFOLDS exports EXACTLY 5 keys after this plan: WRONG_CITATION, LLM_HALLUCINATED_SELECTION, WORKER_FALLBACK_FAILED, GOOGLE_DOM_DRIFT, HARNESS_ERROR"
    - "Every new scaffold's userPrompt is wrapped in the <issue_body_untrusted>...</issue_body_untrusted> envelope (PROMPT-01 invariant preserved)"
    - "Every new scaffold's systemPrompt enumerates the 6 forbidden paths verbatim (cite-by-position discipline preserved)"
    - "Object.freeze invariant survives extension — runtime mutation of PROMPT_SCAFFOLDS throws in strict mode"
    - "buildScaffoldSystemPrompt helper is pure (no node:fs / node:child_process / node:path imports) and PROMPT-04 ESLint guard still passes"
    - "Each of the 4 new scaffolds has one historical-replay test using mocked SDK fixture data"
    - "Skip-class returns (FLAKE / LLM_API_ERROR / PASS) remain UNCHANGED from Phase 42 SKIP_CLASS_ESCALATIONS"
  artifacts:
    - path: "tests/e2e/lib/fix-prompt-builder.js"
      provides: "4 new SYSTEM-prompt templates + buildScaffoldSystemPrompt helper + extended PROMPT_SCAFFOLDS registry"
      contains: "PROMPT_SCAFFOLDS, LLM_HALLUCINATED_SELECTION, WORKER_FALLBACK_FAILED, GOOGLE_DOM_DRIFT, HARNESS_ERROR, buildScaffoldSystemPrompt"
    - path: "tests/unit/fix-prompt-builder.test.js"
      provides: "Per-scaffold contract assertions + frozen-registry test + historical-replay tests via mocked SDK"
    - path: "tests/unit/fixtures/llm-hallucinated-selection-issue.md"
      provides: "Synthesized v3.1-shape issue body fixture for LLM_HALLUCINATED_SELECTION"
    - path: "tests/unit/fixtures/worker-fallback-failed-issue.md"
      provides: "Synthesized v3.1-shape issue body fixture for WORKER_FALLBACK_FAILED"
    - path: "tests/unit/fixtures/google-dom-drift-issue.md"
      provides: "Synthesized v3.1-shape issue body fixture for GOOGLE_DOM_DRIFT"
    - path: "tests/unit/fixtures/harness-error-issue.md"
      provides: "Synthesized v3.1-shape issue body fixture for HARNESS_ERROR"
  key_links:
    - from: "tests/e2e/lib/fix-prompt-builder.js"
      to: "tests/e2e/lib/error-codes.js"
      via: "ERROR_CLASSES enum membership — each new scaffold key MUST appear in ERROR_CLASSES"
      pattern: "ERROR_CLASSES.*(?:LLM_HALLUCINATED_SELECTION|WORKER_FALLBACK_FAILED|GOOGLE_DOM_DRIFT|HARNESS_ERROR)"
    - from: "PROMPT_SCAFFOLDS"
      to: "auto-fix.mjs Step 7"
      via: "PROMPT_SCAFFOLDS[errorClass]() lookup at fix-prompt-builder.js:203 — extending registry expands routing without dispatcher changes"
      pattern: "PROMPT_SCAFFOLDS\\[errorClass\\]"
    - from: "tests/unit/fix-prompt-builder.test.js"
      to: "tests/e2e/lib/fix-prompt-builder.js"
      via: "Direct ES import of buildFixPrompt + PROMPT_SCAFFOLDS + ENVELOPE_OPEN/CLOSE"
      pattern: "import.*buildFixPrompt.*from.*fix-prompt-builder"
---

<objective>
Extend Phase 42's frozen PROMPT_SCAFFOLDS registry from 1 key (WRONG_CITATION) to 5 keys by adding LLM_HALLUCINATED_SELECTION, WORKER_FALLBACK_FAILED, GOOGLE_DOM_DRIFT, and HARNESS_ERROR scaffolds. Each new scaffold mirrors the WRONG_CITATION shape — same trust-boundary envelope discipline, same 6 forbidden-paths enumeration, same diff-size cap, same output-format fence spec — with class-specific "fix surface contract" middle sections naming the appropriate source files per FEATURES.md routing table.

Purpose: this is the prompt-engineering surface of v4.0's self-healing loop. With only WRONG_CITATION scaffolded, 4 of the 5 RPT-02 supported ERROR_CLASSes silently escalate to `{ok:false, escalate:'unsupported-class:...'}` at fix-prompt-builder.js:206. Expanding the registry is the gating change that lets Phase 45-03's dispatcher actually route triage-labeled issues for those classes into LLM fix attempts.

Output: 4 new system-prompt template constants, one shared `buildScaffoldSystemPrompt({className, fixSurfaceContract})` helper extracted to avoid 4×80 lines of duplication, the extended `Object.freeze({...})` registry, 4 fixture issue-body files, and per-scaffold Vitest contract tests + 4 historical-replay tests with mocked SDK fixture data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-per-error-class-expansion-flake-5-state-machine/45-CONTEXT.md
@.planning/phases/45-per-error-class-expansion-flake-5-state-machine/45-RESEARCH.md

# Phase 42 surface being extended
@tests/e2e/lib/fix-prompt-builder.js
@tests/unit/fix-prompt-builder.test.js

# Closed enum that each new scaffold key MUST belong to
@tests/e2e/lib/error-codes.js

# Phase 42 mock pattern reference (used in Task 3 historical-replay tests)
@tests/unit/auto-fix.test.js

<interfaces>
<!-- Key exports from the existing Phase 42 surface — executors reuse, never redefine. -->

From tests/e2e/lib/fix-prompt-builder.js (EXISTING — preserve):
```javascript
export const ENVELOPE_OPEN = '<issue_body_untrusted>';
export const ENVELOPE_CLOSE = '</issue_body_untrusted>';
export const DIFF_FENCE_START = '===DIFF_START===';
export const DIFF_FENCE_END = '===DIFF_END===';
export const PROMPT_SCAFFOLDS = Object.freeze({ WRONG_CITATION: () => WRONG_CITATION_SYSTEM });
export function buildFixPrompt({ errorClass, issueBody } = {})
  // returns { ok:true, systemPrompt, userPrompt } OR { ok:false, escalate:string }
const SKIP_CLASS_ESCALATIONS = Object.freeze({
  FLAKE: 're-quarantine',
  LLM_API_ERROR: 'retry',
  PASS: 'close-as-pass',
});
```

From tests/e2e/lib/error-codes.js — the closed RPT-02 enum each new scaffold key MUST appear in:
`ERROR_CLASSES` includes WRONG_CITATION, LLM_HALLUCINATED_SELECTION, WORKER_FALLBACK_FAILED, GOOGLE_DOM_DRIFT, HARNESS_ERROR (verify by reading the file before edits).

LOCKED forbidden paths (the 6 LITERAL strings every system prompt MUST enumerate verbatim):
  - tests/test-cases.js
  - tests/golden/baseline.json
  - tests/e2e/test-cases-quarantine.js
  - .github/workflows/v40-*.yml
  - tests/e2e/.llm-spend-ledger.json
  - .github/CODEOWNERS

Fix-surface per class (from 45-CONTEXT D-04 + FEATURES.md):
  LLM_HALLUCINATED_SELECTION → tests/e2e/lib/select-text.js, spec-extraction code
  WORKER_FALLBACK_FAILED     → src/cf-worker/index.js, src/shared/uspto-fallback.js
  GOOGLE_DOM_DRIFT           → tests/e2e/lib/google-patents-page.js, tests/e2e/lib/select-text.js, selectors/data-testid
  HARNESS_ERROR              → tests/e2e/specs/, fixture loaders, Playwright config
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract buildScaffoldSystemPrompt helper + add 4 new SYSTEM-prompt template constants</name>
  <files>tests/e2e/lib/fix-prompt-builder.js</files>
  <read_first>
    - tests/e2e/lib/fix-prompt-builder.js (the EXISTING WRONG_CITATION_SYSTEM string at lines 80-143 — the 5-section template skeleton: Trust boundary / Citation contract / Forbidden paths / Diff size cap / Output format). The new helper extracts sections 1, 3, 4, 5 (which are LITERALLY IDENTICAL across all 5 classes) and parameterizes section 2 (the class-specific "fix surface contract") plus the opening "your task" sentence.
    - tests/e2e/lib/error-codes.js (confirm LLM_HALLUCINATED_SELECTION, WORKER_FALLBACK_FAILED, GOOGLE_DOM_DRIFT, HARNESS_ERROR all appear in ERROR_CLASSES — they MUST, since these are RPT-02 enum members)
    - eslint.config.js (the no-restricted-imports block for fix-prompt-builder.js — confirm the file is still under purity guard after this edit; the helper is a pure string join, no new imports needed)
  </read_first>
  <behavior>
    Test contract for the new helper and 4 scaffold constants (Task 1 inserts these as a `describe('buildScaffoldSystemPrompt helper')` block in fix-prompt-builder.test.js as part of the same commit; runtime assertions for the 4 PROMPT_SCAFFOLDS entries land in Task 2):
    - `buildScaffoldSystemPrompt({className: 'LLM_HALLUCINATED_SELECTION', fixSurfaceContract: 'foo'})` returns a string containing:
      * The opening "You are a senior TypeScript/JavaScript engineer..." sentence with `LLM_HALLUCINATED_SELECTION` interpolated as the failure-class name
      * The `## Trust boundary` section LITERALLY identical to WRONG_CITATION's (envelope-open/close referenced, untrusted-data warning)
      * The `fixSurfaceContract` argument string spliced into a `## Fix surface contract` section between Trust boundary and Forbidden paths
      * The `## Forbidden paths` section enumerating all 6 LOCKED paths verbatim (`tests/test-cases.js`, `tests/golden/baseline.json`, `tests/e2e/test-cases-quarantine.js`, `.github/workflows/v40-*.yml`, `tests/e2e/.llm-spend-ledger.json`, `.github/CODEOWNERS`)
      * The `## Diff size cap` section with the literal "≤200 lines" / "≤50 lines" thresholds
      * The `## Output format` section with `===DIFF_START===` and `===DIFF_END===` referenced by VALUE
    - The 4 new module-scope template constants (`LLM_HALLUCINATED_SELECTION_SYSTEM`, `WORKER_FALLBACK_FAILED_SYSTEM`, `GOOGLE_DOM_DRIFT_SYSTEM`, `HARNESS_ERROR_SYSTEM`) each invoke `buildScaffoldSystemPrompt` at module load with their class-specific `fixSurfaceContract` string (per the FEATURES.md routing table in the interfaces block above)
    - buildScaffoldSystemPrompt is pure: same inputs → identical string output, no env/IO reads
    - The existing WRONG_CITATION_SYSTEM constant is REFACTORED to be produced by buildScaffoldSystemPrompt as well (single source of truth — eliminates the 80-line duplication AND guarantees byte-stable forbidden-paths section across all 5 classes). The exact STRING value of WRONG_CITATION_SYSTEM may change line-by-line but MUST still contain every existing PROMPT-01/forbidden-paths/diff-cap/output-format substring asserted by tests/unit/fix-prompt-builder.test.js
  </behavior>
  <action>
    1. Read tests/e2e/lib/fix-prompt-builder.js. Identify lines 80-143 (the existing WRONG_CITATION_SYSTEM string concatenation).

    2. Insert a new pure helper `buildScaffoldSystemPrompt({ className, fixSurfaceContract })` BEFORE the WRONG_CITATION_SYSTEM declaration. The helper composes the 5-section template:
       - Opening sentence: "You are a senior TypeScript/JavaScript engineer reviewing an automated regression triage finding. Your task: produce a minimal unified diff that fixes the ${className} failure described in the user-supplied envelope."
       - Trust boundary section: LITERAL copy from existing lines 85-95 (envelope-open / close / untrusted-data warning). Reference ENVELOPE_OPEN and ENVELOPE_CLOSE by value (string interpolation), NEVER hardcode the literal strings — single source of truth lives in the existing constants at lines 46/49.
       - Fix surface contract section: header "## Fix surface contract" followed by a blank line followed by the `fixSurfaceContract` parameter (multi-line string supported)
       - Forbidden paths section: header "## Forbidden paths (NEVER touch these in your diff)" followed by the explanatory sentence "The following paths are LOCKED by the diff-guard regex bank in scripts/check-diff-guard.mjs. A diff touching any of them will be REJECTED before `git apply` runs, and the auto-fix loop will fail closed:" followed by the EXACT 6 bullet lines from existing lines 113-118 (`tests/test-cases.js`, `tests/golden/baseline.json`, `tests/e2e/test-cases-quarantine.js`, `.github/workflows/v40-*.yml`, `tests/e2e/.llm-spend-ledger.json`, `.github/CODEOWNERS`) followed by the closing "Fix the failure in the PRODUCTION CODE..." sentence (generalized from WRONG_CITATION's "Fix the citation" line — see step 3)
       - Diff size cap section: LITERAL copy from existing lines 123-129 (≤200 src, ≤50 tests, TODO(human-review) escape hatch)
       - Output format section: LITERAL copy from existing lines 131-142 (DIFF_FENCE_START/DIFF_FENCE_END by VALUE, "respond with EXACTLY ONE unified diff", empty-fenced-block declined-model escape)
       Join via `.join('\n')` to match the existing array+join pattern.

       Generalize the WRONG_CITATION-specific "Fix the citation in the PRODUCTION CODE (src/ and content scripts), NOT by editing the golden baseline..." sentence to "Fix the failure in the PRODUCTION CODE (src/ and content scripts) and/or the tests/ surface named in the Fix surface contract above. NEVER edit the golden baseline / quarantine corpus / workflows / CODEOWNERS / ledger to make the symptom disappear."

    3. REFACTOR the existing WRONG_CITATION_SYSTEM constant declaration to invoke `buildScaffoldSystemPrompt` with `className: 'WRONG_CITATION'` and the WRONG_CITATION-specific `fixSurfaceContract` (extract the cite-by-position v3.1 section from existing lines 97-105 into the contract argument). Delete the now-redundant inline string array. The WRONG_CITATION_SYSTEM identifier MUST still be defined (PROMPT_SCAFFOLDS.WRONG_CITATION still resolves it via the existing thunk).

    4. Add 4 new module-scope constants — each invokes buildScaffoldSystemPrompt with the per-class fix-surface contract:
       - `LLM_HALLUCINATED_SELECTION_SYSTEM` — fixSurfaceContract names `tests/e2e/lib/select-text.js` plus spec-extraction code as the editable surface; warns that the LLM proposed a selection text not present (per wsNorm) in the patent body, so the fix is usually a sanitizer/normalizer guard, NOT loosening the selection contract.
       - `WORKER_FALLBACK_FAILED_SYSTEM` — fixSurfaceContract names `src/cf-worker/index.js` and `src/shared/uspto-fallback.js` as the editable surface; warns that the Cloudflare Worker's USPTO PDF fallback returned non-2xx or non-PDF content; the fix is usually a header/retry/MIME-type guard on the fallback path.
       - `GOOGLE_DOM_DRIFT_SYSTEM` — fixSurfaceContract names `tests/e2e/lib/google-patents-page.js`, `tests/e2e/lib/select-text.js`, and any `data-testid` selectors as the editable surface; warns that Google Patents' DOM changed and a selector no longer matches; the fix is usually a selector update — NEVER a `wait` increase or a try/catch swallow.
       - `HARNESS_ERROR_SYSTEM` — fixSurfaceContract names `tests/e2e/specs/`, fixture loaders, and the Playwright config as the editable surface; warns that the test harness itself (NOT the product) failed; the fix is usually a fixture path, a teardown ordering issue, or a Playwright config override.

       Each contract MUST be a multi-line string (use `[...].join('\n')` for readability) explaining (a) which file(s) to edit, (b) what kind of fix is appropriate, (c) what to avoid (per the anti-patterns in the warnings above).

    5. NO change to ENVELOPE_OPEN, ENVELOPE_CLOSE, DIFF_FENCE_START, DIFF_FENCE_END (preserve verbatim — these are PROMPT-01 contract).
    6. NO change to SKIP_CLASS_ESCALATIONS Object.freeze (FLAKE / LLM_API_ERROR / PASS escalations stay literal per 45-CONTEXT lock).
    7. NO change to the buildFixPrompt function body (PROMPT_SCAFFOLDS lookup at line 203 continues to work unchanged with new keys).

    Per D-04 purity invariant: do NOT add `node:fs`, `node:child_process`, or `node:path` imports. The PROMPT-04 ESLint rule still applies; verify lint passes (`npm run lint 2>&1 | grep fix-prompt-builder` returns no errors).
  </action>
  <verify>
    <automated>npm test -- fix-prompt-builder.test.js -t "buildScaffoldSystemPrompt helper" 2>&1 | grep -E "passed|failed" | head -5</automated>
    <automated>node -e "import('./tests/e2e/lib/fix-prompt-builder.js').then(m => { const w = m.PROMPT_SCAFFOLDS.WRONG_CITATION(); if (!w.includes('cite-by-position')) process.exit(1); console.log('WRONG_CITATION refactor preserves cite-by-position content'); })"</automated>
    <automated>npm run lint 2>&1 | grep -E "fix-prompt-builder.*(node:fs|node:child_process|node:path)" | (! grep -q . && echo "lint clean" || (echo "PURITY VIOLATION" && exit 1))</automated>
  </verify>
  <done>
    - buildScaffoldSystemPrompt helper exists, is pure, and produces strings containing all 5 required sections
    - WRONG_CITATION_SYSTEM refactored to use the helper; existing fix-prompt-builder.test.js Phase 42 tests still pass (envelope wrapping, forbidden-paths enumeration, frozen registry shape)
    - 4 new module-scope SYSTEM-prompt constants declared with class-specific fix-surface contracts
    - PROMPT-04 ESLint guard passes (no node:* imports)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend PROMPT_SCAFFOLDS Object.freeze registry + Vitest contract suite</name>
  <files>tests/e2e/lib/fix-prompt-builder.js, tests/unit/fix-prompt-builder.test.js</files>
  <read_first>
    - tests/e2e/lib/fix-prompt-builder.js (after Task 1 — buildScaffoldSystemPrompt + 4 new constants now exist; PROMPT_SCAFFOLDS still has only WRONG_CITATION key)
    - tests/unit/fix-prompt-builder.test.js (Phase 42 test structure — describe blocks for envelope wrapping, registry shape, skip-class returns, systemPrompt content. Task 2 ADDS new describe blocks for the 4 new keys without modifying existing ones)
  </read_first>
  <behavior>
    Test contract (Task 2 lands these in tests/unit/fix-prompt-builder.test.js as new describe blocks):
    - PROMPT_SCAFFOLDS exports EXACTLY 5 keys: ['WRONG_CITATION', 'LLM_HALLUCINATED_SELECTION', 'WORKER_FALLBACK_FAILED', 'GOOGLE_DOM_DRIFT', 'HARNESS_ERROR']
    - Each new scaffold value is a thunk (`typeof PROMPT_SCAFFOLDS[key] === 'function'`)
    - Calling each thunk returns a non-empty string
    - PROMPT_SCAFFOLDS is still `Object.isFrozen(PROMPT_SCAFFOLDS) === true`; mutation attempts throw in strict mode (`'use strict'; PROMPT_SCAFFOLDS.NEW_KEY = () => 'x'` raises TypeError)
    - For each of the 4 new classes:
      * `buildFixPrompt({ errorClass: <CLASS>, issueBody: 'BODY' })` returns `{ ok: true, systemPrompt, userPrompt }`
      * `userPrompt` is LITERALLY `<issue_body_untrusted>\nBODY\n</issue_body_untrusted>` (envelope contract, ENVELOPE_OPEN / ENVELOPE_CLOSE imported, NOT hardcoded — assert via template literal using the imported constants)
      * `systemPrompt` contains the class name as the failure-class
      * `systemPrompt` enumerates ALL 6 forbidden paths as literal substrings (`tests/test-cases.js`, `tests/golden/baseline.json`, `tests/e2e/test-cases-quarantine.js`, `.github/workflows/v40-*.yml`, `tests/e2e/.llm-spend-ledger.json`, `.github/CODEOWNERS`)
      * `systemPrompt` contains `===DIFF_START===` and `===DIFF_END===` substrings (fence-by-value)
      * `systemPrompt` contains the class-specific fix-surface file paths:
        - LLM_HALLUCINATED_SELECTION → contains 'tests/e2e/lib/select-text.js'
        - WORKER_FALLBACK_FAILED → contains 'src/cf-worker/index.js' AND 'src/shared/uspto-fallback.js'
        - GOOGLE_DOM_DRIFT → contains 'tests/e2e/lib/google-patents-page.js' AND 'data-testid'
        - HARNESS_ERROR → contains 'tests/e2e/specs/' AND 'Playwright config'
    - Skip-class behavior UNCHANGED: `buildFixPrompt({errorClass: 'FLAKE'})` STILL returns `{ ok: false, escalate: 're-quarantine' }` (Phase 42 contract preserved)
    - Unsupported class behavior UNCHANGED: `buildFixPrompt({errorClass: 'BOGUS'})` returns `{ ok: false, escalate: 'unsupported-class:BOGUS' }`
  </behavior>
  <action>
    1. Edit tests/e2e/lib/fix-prompt-builder.js. Extend the `PROMPT_SCAFFOLDS = Object.freeze({...})` literal at line 155 to spread in 4 new thunk entries:
       ```
       export const PROMPT_SCAFFOLDS = Object.freeze({
         WRONG_CITATION: () => WRONG_CITATION_SYSTEM,
         LLM_HALLUCINATED_SELECTION: () => LLM_HALLUCINATED_SELECTION_SYSTEM,
         WORKER_FALLBACK_FAILED: () => WORKER_FALLBACK_FAILED_SYSTEM,
         GOOGLE_DOM_DRIFT: () => GOOGLE_DOM_DRIFT_SYSTEM,
         HARNESS_ERROR: () => HARNESS_ERROR_SYSTEM,
       });
       ```
       Per 45-RESEARCH Pattern 1: do NOT refactor to a `register()` factory — preserve `Object.freeze` literal. The lookup at line 203 (`PROMPT_SCAFFOLDS[errorClass]`) handles the new keys automatically.

    2. Edit tests/unit/fix-prompt-builder.test.js. ADD these new describe blocks at the end of the file (preserve all Phase 42 describe blocks unchanged):
       - `describe('Phase 45 PROMPT-03 extension: 4 new ERROR_CLASS scaffolds')` containing:
         * `it('PROMPT_SCAFFOLDS exports exactly 5 keys (WRONG_CITATION + 4 Phase 45 additions)')` — assert `Object.keys(PROMPT_SCAFFOLDS).sort()` deep-equals `['GOOGLE_DOM_DRIFT', 'HARNESS_ERROR', 'LLM_HALLUCINATED_SELECTION', 'WORKER_FALLBACK_FAILED', 'WRONG_CITATION']`
         * `it('PROMPT_SCAFFOLDS remains Object.frozen after extension')` — assert `Object.isFrozen(PROMPT_SCAFFOLDS) === true` and a `expect(() => { 'use strict'; PROMPT_SCAFFOLDS.X = () => 'x'; }).toThrow(TypeError)` mutation guard
         * For each of the 4 new classes, one `it('<CLASS>: buildFixPrompt returns ok:true envelope-wrapped result with class-specific fix surface')` test asserting (a) `ok === true`, (b) `userPrompt === ${ENVELOPE_OPEN}\n${BODY}\n${ENVELOPE_CLOSE}` LITERAL (use the imported ENVELOPE_OPEN/CLOSE constants), (c) systemPrompt contains the class name, (d) systemPrompt contains the class-specific surface paths per the behavior table above, (e) systemPrompt contains all 6 forbidden paths (verbatim substrings), (f) systemPrompt contains `===DIFF_START===` and `===DIFF_END===`
       - `describe('Phase 45 PROMPT-03 extension: skip-class returns unchanged')` containing one re-assertion that `buildFixPrompt({errorClass: 'FLAKE'})` STILL returns `{ ok: false, escalate: 're-quarantine' }` — regression guard against accidentally moving FLAKE into PROMPT_SCAFFOLDS.

    Per 45-CONTEXT Decision lock: do NOT modify SKIP_CLASS_ESCALATIONS. FLAKE remains a skip class even though Phase 45-03 adds new dispatcher side effects for it (the side effects live in auto-fix.mjs Step 7, not in this file).
  </action>
  <verify>
    <automated>npm test -- fix-prompt-builder.test.js 2>&1 | tail -20</automated>
    <automated>node -e "import('./tests/e2e/lib/fix-prompt-builder.js').then(m => { const k = Object.keys(m.PROMPT_SCAFFOLDS).sort().join(','); if (k !== 'GOOGLE_DOM_DRIFT,HARNESS_ERROR,LLM_HALLUCINATED_SELECTION,WORKER_FALLBACK_FAILED,WRONG_CITATION') { console.error('KEYS:', k); process.exit(1); } console.log('5 keys present'); })"</automated>
    <automated>grep -v '^//' tests/e2e/lib/fix-prompt-builder.js | grep -c 'PROMPT_SCAFFOLDS\b' | awk '$1<2{exit 1} {print "PROMPT_SCAFFOLDS referenced "$1" times in code"}'</automated>
  </verify>
  <done>
    - PROMPT_SCAFFOLDS literally has 5 keys, all thunks, returning non-empty strings
    - Object.isFrozen still true; mutation test throws
    - 4 new Vitest classes each pass the envelope-wrap + forbidden-paths + class-specific-surface assertion battery
    - Phase 42 Vitest tests for WRONG_CITATION + skip-class + frozen-registry still green (regression-free extension)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Historical-replay fixtures + mocked-SDK integration tests (4 cases)</name>
  <files>tests/unit/fixtures/llm-hallucinated-selection-issue.md, tests/unit/fixtures/worker-fallback-failed-issue.md, tests/unit/fixtures/google-dom-drift-issue.md, tests/unit/fixtures/harness-error-issue.md, tests/unit/fix-prompt-builder.test.js</files>
  <read_first>
    - tests/e2e/test-cases-quarantine.js (the schema for synthesized issue bodies: id, patentFile, selectedText, category, stable_runs, source_triage_finding_id, added_iso — fixture bodies follow this shape PLUS the v3.1 issue-body envelope `<!-- fp: 12hex --> + case-id: <id> + ERROR_CLASS rationale section`)
    - tests/unit/auto-fix.test.js (lines 53-69 — the vi.mock pattern for invokeAnthropicSdkWithLedger, the canonical Phase 42 mock pattern to mirror)
    - scripts/auto-fix.mjs (lines 90-95 — extractFingerprint regex: `<!-- fp: ([0-9a-f]{12}) -->` — each fixture body MUST satisfy this regex, since downstream integration tests in Plan 45-03 will reuse these fixtures)
  </read_first>
  <behavior>
    Test contract (Task 3 lands these in tests/unit/fix-prompt-builder.test.js as a new `describe('Phase 45 PROMPT-03 extension: historical-replay (mocked SDK)')` block):
    - For each of the 4 new classes:
      * Read the corresponding fixture file from `tests/unit/fixtures/<class-slug>-issue.md` via `fs.readFileSync` at test-time (acceptable — this is a TEST file, not the pure library)
      * Invoke `buildFixPrompt({ errorClass: <CLASS>, issueBody: fixtureBody })` and assert `ok: true`
      * Assert `userPrompt` starts with `<issue_body_untrusted>\n` and ends with `\n</issue_body_untrusted>` (envelope contract)
      * Assert the fixture body's `<!-- fp: ... -->` line is preserved verbatim inside the envelope
      * Assert `systemPrompt` mentions the class name and contains at least one class-specific fix-surface file path (subset of Task 2's assertion battery; this test focuses on integration with realistic body data)
    - Each fixture file is a valid v3.1-shape issue body:
      * First line: `<!-- fp: <12-hex> -->` (satisfies extractFingerprint at scripts/auto-fix.mjs:90-93)
      * Second line: `case-id: <kebab-case-id>` (satisfies extractCaseId at scripts/auto-fix.mjs:102-105)
      * Followed by an ERROR_CLASS-specific rationale section (synthesized — realistic content explaining the failure mode for that class)
    Each fixture has at least one rationale section with ≥3 lines of body text (so prompts are non-trivial under test).
  </behavior>
  <action>
    1. Create `tests/unit/fixtures/` directory if it does not exist.

    2. Create 4 fixture files (each ≥15 lines including header + body), modeled after the v3.1 issue-payload-builder output shape:

       a) `tests/unit/fixtures/llm-hallucinated-selection-issue.md` — fingerprint `a1b2c3d4e5f6`, case-id `US11427642-hallucinated-1`. Body explains: extension's LLM proposed a selection string ("the present invention provides...") that wsNorm() could not locate in the patent body; verifier returned status=fail with tier_used='D'; rerun verdict CONFIRMED (3/3). Rationale points to `tests/e2e/lib/select-text.js` and the spec-extraction sanitizer as the likely fix surface.

       b) `tests/unit/fixtures/worker-fallback-failed-issue.md` — fingerprint `b2c3d4e5f6a1`, case-id `US10000000-uspto-fallback-1`. Body explains: Cloudflare Worker fetched the USPTO PDF as MIME `text/html` (not `application/pdf`); fallback path returned non-2xx; extension surfaced "PDF unavailable" to user. Rationale points to `src/cf-worker/index.js` MIME-type guard + `src/shared/uspto-fallback.js` retry policy.

       c) `tests/unit/fixtures/google-dom-drift-issue.md` — fingerprint `c3d4e5f6a1b2`, case-id `US11427642-dom-drift-1`. Body explains: Google Patents removed `[data-testid="patent-text"]` from the page in a UI deploy; `tests/e2e/lib/google-patents-page.js` selector returned null; harness reported "no patent body found". Rationale points to selector update in `google-patents-page.js`. Explicitly warns against widening the selector with `wait` or try/catch.

       d) `tests/unit/fixtures/harness-error-issue.md` — fingerprint `d4e5f6a1b2c3`, case-id `US11427642-harness-1`. Body explains: Playwright spec setup failed with "fixture not found" because the patent JSON file referenced by `patentFile` did not exist on disk. Rationale points to `tests/e2e/specs/cite.spec.js` fixture loader and the missing fixture path.

    3. Edit tests/unit/fix-prompt-builder.test.js. ADD a new `describe('Phase 45 PROMPT-03 extension: historical-replay fixtures (no SDK mocked)')` block. For each of the 4 classes, add an `it()` test that:
       a) Reads the fixture file via `fs.readFileSync(path.join(__dirname, 'fixtures', '<class-slug>-issue.md'), 'utf8')` — pin `__dirname` via `path.dirname(fileURLToPath(import.meta.url))` since this file uses ES modules
       b) Invokes `buildFixPrompt({ errorClass, issueBody: fixtureBody })` and asserts `ok: true`
       c) Asserts the userPrompt format: `expect(userPrompt.startsWith('<issue_body_untrusted>\n')).toBe(true)` and `expect(userPrompt.endsWith('\n</issue_body_untrusted>')).toBe(true)`
       d) Asserts the fingerprint line is preserved verbatim inside the envelope: `expect(userPrompt).toContain('<!-- fp: ' + <expected-12-hex> + ' -->')`
       e) Asserts the systemPrompt contains the class name and at least one class-specific surface path

    4. Per 45-RESEARCH Pattern 4: the SDK is NOT mocked in THIS file (this is the pure builder test). Plan 45-03's `tests/unit/auto-fix.test.js` extensions will mock invokeAnthropicSdkWithLedger and replay the SAME fixtures through the full dispatcher (`runDispatcher`) to assert end-to-end diff-application — the fixtures created here are deliberately reused there. Document this in a comment at the top of the new describe block.

    Per project skill `.claude/skills/` / `.agents/skills/` — neither exists (confirmed `ls` returned not-found); no additional skill rules to honor.
  </action>
  <verify>
    <automated>npm test -- fix-prompt-builder.test.js -t "historical-replay" 2>&1 | tail -20</automated>
    <automated>ls tests/unit/fixtures/{llm-hallucinated-selection,worker-fallback-failed,google-dom-drift,harness-error}-issue.md 2>&1 | grep -c issue.md | awk '$1!=4{exit 1} {print "4 fixture files present"}'</automated>
    <automated>for f in tests/unit/fixtures/{llm-hallucinated-selection,worker-fallback-failed,google-dom-drift,harness-error}-issue.md; do grep -E '^<!-- fp: [0-9a-f]{12} -->$' "$f" >/dev/null || { echo "MISSING fp in $f"; exit 1; }; done; echo "all 4 fixtures have valid fp header"</automated>
  </verify>
  <done>
    - 4 fixture files exist under tests/unit/fixtures/, each with valid v3.1-shape header (fp + case-id) and a class-specific rationale body
    - Vitest historical-replay tests pass for all 4 classes (ok:true + envelope + fingerprint preservation + class-specific surface mention)
    - Fixtures are explicitly documented as reusable by Plan 45-03 integration tests
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub issue body → fix-prompt-builder | Issue bodies are attacker-controlled (any GitHub user with repo access can file an issue); the 4 new scaffolds inherit Phase 42's `<issue_body_untrusted>` envelope discipline |
| PROMPT_SCAFFOLDS thunk return values → systemPrompt argument to Anthropic SDK | Internal-only — strings are built at module load from frozen templates; no external input crosses this boundary |
| Fixture files → Vitest test runtime | Test-only boundary; fixtures are committed and CODEOWNED via `tests/` glob; no production impact |

## STRIDE Threat Register (Phase 45 — Pitfall 5 defense extension)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01-01 | Information disclosure | LLM_HALLUCINATED_SELECTION userPrompt | mitigate | userPrompt LITERALLY `${ENVELOPE_OPEN}\n${safeBody}\n${ENVELOPE_CLOSE}` (same template as WRONG_CITATION at fix-prompt-builder.js:213); NOTHING outside the envelope. Task 2's regression test asserts envelope contract per class. |
| T-45-01-02 | Tampering | All 4 new scaffolds (system-prompt content) | mitigate | systemPrompt is built at module load from frozen templates + buildScaffoldSystemPrompt helper output; no runtime mutation surface (Object.freeze on PROMPT_SCAFFOLDS, plus the 4 new constants are module-scope `const`). |
| T-45-01-03 | Spoofing (attacker forges forbidden-path enumeration omission) | new scaffold systemPrompt content | mitigate | Single-source-of-truth via buildScaffoldSystemPrompt helper — all 5 classes get the SAME 6 forbidden paths verbatim. Task 2's "all 6 forbidden paths" assertion runs against every scaffold. |
| T-45-01-04 | Elevation of privilege (PROMPT_SCAFFOLDS mutated at runtime to register a malicious scaffold) | PROMPT_SCAFFOLDS registry | mitigate | Object.freeze preserved on the extended registry; Task 2 includes a strict-mode mutation guard test that asserts mutation throws TypeError. |
| T-45-01-05 | Tampering (purity violation — node:fs import sneaks in via buildScaffoldSystemPrompt) | tests/e2e/lib/fix-prompt-builder.js | mitigate | PROMPT-04 ESLint guard (eslint.config.js no-restricted-imports block) blocks node:fs / node:child_process / node:path on this file path; verify lint passes after Task 1. |
| T-45-01-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — Phase 45 installs ZERO new packages (45-RESEARCH Package Legitimacy Audit confirms). No slopcheck applicable. |

**Pitfall 5 defense contribution from this plan:** the 4 new scaffolds inherit the SAME envelope + forbidden-paths discipline as WRONG_CITATION. Defense-in-depth across the prompt surface means a prompt-injection attack via a crafted issue body for ANY of the 4 new classes is no more powerful than the same attack against WRONG_CITATION (already mitigated by Phase 42's PROMPT-01 envelope and verifier-gate diff-guard chain). Plan 45-02 extends the diff-guard FORBIDDEN_PATHS bank to cover the 2 new state files (ring buffer + suppression) — that is the load-bearing security extension; this plan's contribution is keeping the prompt-side envelope discipline byte-stable across the 4 new classes.
</threat_model>

<verification>
- All Phase 42 fix-prompt-builder.test.js tests remain green (regression-free extension)
- PROMPT_SCAFFOLDS has EXACTLY 5 keys after this plan
- Each new scaffold passes envelope-wrap + forbidden-paths + fence-by-value + class-specific-surface assertions
- 4 fixture files exist and satisfy the v3.1 issue-body shape (extractFingerprint regex passes on each)
- `npm test -- fix-prompt-builder.test.js` ends with `Test Files  1 passed (1)` and zero failures
- `npm run lint` passes with no fix-prompt-builder.js purity violations
</verification>

<success_criteria>
- Refactored `WRONG_CITATION_SYSTEM` (now produced by `buildScaffoldSystemPrompt`) still satisfies every existing Phase 42 fix-prompt-builder.test.js assertion (envelope, forbidden paths, fence-by-value, citation contract substring)
- 4 new system prompt template constants exist as module-scope `const`s
- PROMPT_SCAFFOLDS literally has 5 keys: WRONG_CITATION, LLM_HALLUCINATED_SELECTION, WORKER_FALLBACK_FAILED, GOOGLE_DOM_DRIFT, HARNESS_ERROR
- Object.freeze invariant preserved (mutation throws in strict mode — Vitest pinned)
- SKIP_CLASS_ESCALATIONS map UNCHANGED (FLAKE / LLM_API_ERROR / PASS still return same escalations)
- 4 fixture issue-body files exist under `tests/unit/fixtures/` with valid v3.1 shape
- 4 historical-replay Vitest tests pass (one per new class), reusing those fixtures
- PROMPT-04 ESLint guard still passes (no `node:fs` / `node:child_process` / `node:path` imports in `tests/e2e/lib/fix-prompt-builder.js`)
</success_criteria>

<output>
Create `.planning/phases/45-per-error-class-expansion-flake-5-state-machine/45-01-SUMMARY.md` when done.
</output>
