---
phase: 47
plan: 04
type: execute
wave: 5
depends_on:
  - 47-03
files_modified:
  - tests/unit/codeowners-pinned.test.js
  - .planning/v4.0-MILESTONE-AUDIT.md
autonomous: false
requirements:
  - CLEANUP-04
user_setup:
  - service: github
    why: "Task 2 invokes `gh api` against the live GitHub repo settings (allow_auto_merge + rulesets + required_status_checks). If required_status_checks is missing `verifier-gate` or `deps-update-gate`, Task 2 patches the ruleset via `gh api -X PUT` — requires authenticated `gh` CLI with repo admin permission."
    env_vars: []
    dashboard_config:
      - task: "Confirm `gh auth status` exits 0 BEFORE Task 2; confirm authenticated account has repo admin scope (Settings → Manage access)"
        location: "Local shell + GitHub web UI"
      - task: "Be ready to confirm/abort the ruleset PATCH if `verifier-gate` or `deps-update-gate` is missing — per CLAUDE.md C1/C2/C3 the patch is gated on explicit user approval (not auto-applied)"
        location: "Interactive checkpoint within Task 2"
tags:
  - v4.0
  - cleanup
  - branch-protection
  - codeowners
  - milestone-audit
must_haves:
  truths:
    - "CODEOWNERS static-grep test (tests/unit/codeowners-pinned.test.js) pins all 5 currently-present paths in last-matching-rule order (broader paths BEFORE more-specific ones) — protects against alphabetical reorder that would shadow `/tests/golden/` under `/tests/`"
    - "Live `gh api` audit confirms: (i) `repos/<o>/<r>.allow_auto_merge === false`, (ii) ruleset on `main` has `bypass_actors == []` (Do not allow bypassing: ON), (iii) `required_status_checks` contains both `verifier-gate` AND `deps-update-gate` (or whatever exact context strings GitHub stores per Open Question 2 verification)"
    - "If `required_status_checks` is missing either job, the ruleset is patched via `gh api -X PUT` with the FULL ruleset object (Pitfall 3 — PUT not PATCH) — gated by explicit user approval per CLAUDE.md C1/C2/C3; live evidence + before/after JSON recorded in v4.0-MILESTONE-AUDIT.md"
    - "v4.0-MILESTONE-AUDIT.md is created at `.planning/v4.0-MILESTONE-AUDIT.md` with all 6 required sections in the frontmatter `scores` block AND markdown body: `integration:` (links INT-FIX-* commit SHAs from 47-01), `nyquist:` (per-phase compliance table from 47-02 SUMMARY), `human_verification:` (5 items: 1 PASS/FAIL from UAT-47-c + 4 DEFERRED from 47-UAT-DEFERRED.md), `branch_protection:` (gh-api evidence from Task 2 here), `gaps:` (unresolved findings), `tech_debt:` (any new entries surfaced by 47-02 PARTIAL outcomes + UAT-47-c FAILURE if any)"
    - "Plan 47-01 and 47-02 deliverables are preserved and not modified by 47-04 — this plan READS their outputs into the audit file; it does not overwrite VALIDATION.md or revert ledger seed"
  artifacts:
    - path: "tests/unit/codeowners-pinned.test.js"
      provides: "Vitest static-grep test pinning 5 CODEOWNERS paths in last-matching-rule order"
      min_lines: 40
      contains: "tests/golden"
    - path: ".planning/v4.0-MILESTONE-AUDIT.md"
      provides: "Authoritative v4.0 milestone audit — consumed by the lifecycle audit-milestone step; 6 required sections (integration / nyquist / human_verification / branch_protection / gaps / tech_debt)"
      min_lines: 100
      contains: "branch_protection:"
  key_links:
    - from: "tests/unit/codeowners-pinned.test.js"
      to: ".github/CODEOWNERS"
      via: "fs.readFileSync + per-rule regex + order assertion"
      pattern: "/src/.*@tonyrowles"
    - from: "Task 2 gh api ruleset audit"
      to: ".planning/v4.0-MILESTONE-AUDIT.md `branch_protection:` block"
      via: "live JSON output captured + serialized into audit YAML"
      pattern: "allow_auto_merge:\\s*false"
    - from: ".planning/phases/47-v4-0-cleanup/47-01-SUMMARY.md INT-FIX-* commit SHAs"
      to: ".planning/v4.0-MILESTONE-AUDIT.md `## Cross-Phase Integration` section"
      via: "Task 3 reads 47-01-SUMMARY + git log + writes commit SHAs into audit body"
      pattern: "INT-FIX-(LEDGER|CAL|LOCK)"
    - from: ".planning/phases/47-v4-0-cleanup/47-02-SUMMARY.md per-phase table"
      to: ".planning/v4.0-MILESTONE-AUDIT.md `## Nyquist Coverage` section"
      via: "Task 3 transposes per-phase table from 47-02 SUMMARY into audit body"
      pattern: "nyquist_compliant"
    - from: ".planning/phases/47-v4-0-cleanup/47-UAT-EVIDENCE.md + 47-UAT-DEFERRED.md"
      to: ".planning/v4.0-MILESTONE-AUDIT.md `human_verification:` YAML block + `## Human-Verification Items` markdown body"
      via: "Task 3 reads both UAT docs + writes 5 entries (1 PASS/FAIL + 4 DEFERRED)"
      pattern: "outcome:\\s*(PASS|FAIL|DEFERRED)"
---

<objective>
Close CLEANUP-04: (a) add a vitest static-grep test pinning the 5 CODEOWNERS paths in their load-bearing last-matching-rule order, (b) audit live GitHub repo settings via `gh api` and (if necessary, with explicit user approval) patch the `main` ruleset's `required_status_checks` to include `verifier-gate` + `deps-update-gate`, (c) bootstrap `.planning/v4.0-MILESTONE-AUDIT.md` consolidating outcomes from Plans 47-01 (INT-FIX-* commits), 47-02 (Nyquist per-phase table), and 47-03 (UAT outcomes) into the canonical milestone audit file consumed by the lifecycle audit-milestone step.

Purpose: Close v4.0 with an authoritative, single-source-of-truth audit doc + the load-bearing CODEOWNERS pin protected by automated test + verified branch-protection state on the live repo. Mirrors v3.1-MILESTONE-AUDIT.md shape, extended for v4.0's larger surface.

Output: tests/unit/codeowners-pinned.test.js (new) + .planning/v4.0-MILESTONE-AUDIT.md (new, ≥100 lines with all 6 required sections) + optional ruleset PATCH evidence (if `verifier-gate`/`deps-update-gate` was missing). 3 commits in order: codeowners test → audit doc bootstrap → optional ruleset-patch evidence record.

Depends on Plan 47-03 because v4.0-MILESTONE-AUDIT.md's `human_verification:` block consumes 47-UAT-EVIDENCE.md (UAT-47-c outcome) and 47-UAT-DEFERRED.md (4 DEFERRED runbook references); the audit cannot be bootstrapped before those exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v3.1-MILESTONE-AUDIT.md
@.planning/phases/47-v4-0-cleanup/47-CONTEXT.md
@.planning/phases/47-v4-0-cleanup/47-RESEARCH.md
@.planning/phases/47-v4-0-cleanup/47-VALIDATION.md
@.planning/milestones/v3.1-phases/38-v3-1-cleanup-integration-warnings-nyquist-human-uat/38-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- CODEOWNERS verbatim contents + gh api ruleset shape + audit YAML template -->

.github/CODEOWNERS (verbatim, verified at planning time 2026-06-01):
  # .github/CODEOWNERS
  # Phase 39 (CLEANUP-04 initial setup) — single-maintainer pins for v4.0 trust invariants.
  # Phase 47 (CLEANUP-04 audit) re-verifies these are present and unchanged via gh api.
  # Last-matching-rule semantics: more specific paths MUST appear AFTER broader ones.
  # https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-security/customizing-your-repository/about-code-owners

  /src/                                       @tonyrowles
  /tests/                                     @tonyrowles
  /.github/workflows/                         @tonyrowles
  /tests/golden/                              @tonyrowles
  /tests/e2e/test-cases-quarantine.js         @tonyrowles

Order semantics (per 47-RESEARCH.md §"CODEOWNERS contents and last-matching-rule semantics" lines 961-1023):
  - 5 active rules (comment lines + blank lines filtered out)
  - Expected order: /src/ → /tests/ → /.github/workflows/ → /tests/golden/ → /tests/e2e/test-cases-quarantine.js
  - tests/golden/baseline.json matches BOTH /tests/ AND /tests/golden/ — last-matching wins (/tests/golden/), so /tests/golden/ MUST appear after /tests/
  - tests/e2e/test-cases-quarantine.js matches BOTH /tests/ AND its own line — same reasoning, its line MUST be tail-most

gh api ruleset query shape (per 47-RESEARCH.md §"CLEANUP-04 Toolchain" lines 872-959):
  Step 1: OWNER=$(gh repo view --json owner -q '.owner.login'); REPO=$(gh repo view --json name -q '.name')
  Step 2: gh api "repos/${OWNER}/${REPO}" -q '.allow_auto_merge' → expect false
  Step 3: gh api "repos/${OWNER}/${REPO}/rulesets" --jq '.[] | {id, name, target, enforcement, source_type}' → enumerate
  Step 4: RULESET_ID=$(gh api "repos/${OWNER}/${REPO}/rulesets" --jq '.[] | select(.target=="branch") | .id' | head -1)
  Step 5: gh api "repos/${OWNER}/${REPO}/rulesets/${RULESET_ID}" → inspect rules + bypass_actors + required_status_checks
  Assertions (per locked CLEANUP-04 decisions):
    - allow_auto_merge === false
    - bypass_actors.length === 0
    - enforcement === 'active'
    - required_status_checks contains entries with context 'verifier-gate' AND 'deps-update-gate' (or workflow-prefixed equivalents per Pitfall 4)
  Phase 39 created ruleset id is `17086676` per handoff — verify by enumeration; do not hardcode.

gh api ruleset PATCH shape (only if `verifier-gate` or `deps-update-gate` missing — per CLAUDE.md C1/C2/C3 gated on user approval):
  Per Pitfall 3 (47-RESEARCH.md lines 1161-1165): use PUT not PATCH; GitHub Rulesets API at this path expects FULL replacement.
  Step 1: gh api "repos/${OWNER}/${REPO}/rulesets/${RULESET_ID}" > /tmp/ruleset-current.json
  Step 2: jq '.rules |= map(if .type == "required_status_checks" then .parameters.required_status_checks |= (. + [{context: "verifier-gate", integration_id: null}, {context: "deps-update-gate", integration_id: null}] | unique_by(.context)) else . end)' /tmp/ruleset-current.json > /tmp/ruleset-patched.json
  Step 3: gh api -X PUT "repos/${OWNER}/${REPO}/rulesets/${RULESET_ID}" --input /tmp/ruleset-patched.json
  Step 4: re-query + verify; record before/after JSON in v4.0-MILESTONE-AUDIT.md
  Per Pitfall 4: the exact context string may be the bare job_id (`verifier-gate`) or workflow-prefixed (`V40 Verifier Gate / verifier-gate`). Before PATCH, run `gh pr checks <any-recent-pr-n>` to confirm the canonical context name format.

v4.0-MILESTONE-AUDIT.md template (per 47-RESEARCH.md §"v4.0-MILESTONE-AUDIT.md section template" lines 1027-1145 — adapt verbatim):
  YAML frontmatter:
    milestone: v4.0
    audited: <ISO>
    status: <tech_debt | passed>
    scores: { requirements: 33/33, phases: 9/9, integration: 5/5, flows: <N>/<N> }
    gaps: { requirements: [], integration: [], flows: [] }
    human_verification: [5 entries — 1 from UAT-47-c + 4 DEFERRED from 47-UAT-DEFERRED.md]
    nyquist: { compliant_phases: [...], partial_phases: [...], missing_phases: [], overall: <complete|partial> }
    branch_protection: { allow_auto_merge: <false>, ruleset: {id, bypass_actors, enforcement, required_status_checks}, codeowners_static_grep_test: PASS }
    tech_debt: [...]
  Markdown body sections (mandatory): Scores / Cross-Phase Integration / Pre-existing Test Regressions — RESOLVED in Phase 47 CLEANUP-01 / Nyquist Coverage / Human-Verification Items / Branch Protection / Why status

INT-FIX commit SHA sourcing (for Cross-Phase Integration section):
  git log --oneline -20 | grep -E "^[0-9a-f]+ fix\(47-cleanup\): INT-FIX-(LEDGER|CAL|LOCK)" → 3 lines; capture each SHA + subject for the audit body
  TP-bundle commit SHA: git log --oneline -20 | grep -E "^[0-9a-f]+ test\(47-cleanup\): pin 5 v4\.0 ARCHITECTURE" → 1 line

Plan 47-02 per-phase outcome table sourcing:
  Read .planning/phases/47-v4-0-cleanup/47-02-SUMMARY.md "## Per-Phase Outcome Table" — 8 rows
  Compute compliant_phases / partial_phases arrays from the table

Plan 47-03 UAT outcomes sourcing:
  Read .planning/phases/47-v4-0-cleanup/47-UAT-EVIDENCE.md `## UAT-47-c` section for status PASS/FAIL + verified_at + evidence link
  Read .planning/phases/47-v4-0-cleanup/47-UAT-DEFERRED.md for the 4 DEFERRED references

CLAUDE.md C1/C2/C3 enforcement (Task 2 gating on ruleset PATCH):
  If gh api audit shows required_status_checks missing verifier-gate / deps-update-gate, DO NOT auto-PATCH. Surface to user via numbered-list fallback:
    Options:
      1) PATCH the ruleset now via `gh api -X PUT` with the diff shown
      2) Skip PATCH and record as tech_debt for v4.1 (audit notes "verifier-gate / deps-update-gate missing from required_status_checks")
      3) Manual remediation via GitHub web UI (audit notes "MANUAL FOLLOWUP REQUIRED")
  Wait for explicit user response. Do NOT pick "(Recommended)".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/unit/codeowners-pinned.test.js — vitest static-grep pins 5 CODEOWNERS paths in last-matching-rule order</name>
  <files>tests/unit/codeowners-pinned.test.js</files>
  <read_first>
    - .github/CODEOWNERS (verbatim contents — 5 active rules in canonical last-matching order)
    - .planning/phases/47-v4-0-cleanup/47-RESEARCH.md (§"Static-grep test design (vitest)" lines 979-1023 — the verbatim test template; §"Pitfall 5: CODEOWNERS reorder during edit" lines 1174-1177)
    - tests/unit/llm-ledger.test.js (lines 1036-1055 — Test 49 .gitignore static-grep — reusable pattern for file-as-text grep)
  </read_first>
  <behavior>
    - it('CODEOWNERS file exists at .github/CODEOWNERS') — `expect(fs.existsSync(CODEOWNERS_PATH)).toBe(true)`
    - it('contains exactly 5 active rules (comments + blank lines filtered out)') — `expect(rules.length).toBe(5)`
    - 5 sub-it() blocks (one per expected rule) asserting the per-line regex matches in the correct order: rule[0] matches /^\/src\/\s+@/, rule[1] matches /^\/tests\/\s+@/, rule[2] matches /^\/\.github\/workflows\/\s+@/, rule[3] matches /^\/tests\/golden\/\s+@/, rule[4] matches /^\/tests\/e2e\/test-cases-quarantine\.js\s+@/
    - it('more-specific paths appear AFTER broader ones (last-matching-rule semantics)') — find indices of `/tests/ `, `/tests/golden/`, `/tests/e2e/test-cases-quarantine.js`; assert idxTests < idxGolden AND idxTests < idxQuarantine (per Pitfall 5)
    - it('CODEOWNERS uses single maintainer pin @tonyrowles across all 5 rules') — `rules.forEach((r) => expect(r).toContain('@tonyrowles'))`
    - Total: 1 file-exists + 1 count + 5 per-rule + 1 order + 1 maintainer = 9 assertions
    - All pass against current `.github/CODEOWNERS`
  </behavior>
  <action>
    Step 1 (RED, verify): The test file doesn't yet exist — `test ! -f tests/unit/codeowners-pinned.test.js && echo "ok"`.
    Step 2 (GREEN): Create `tests/unit/codeowners-pinned.test.js` (new file). Use the verbatim template from 47-RESEARCH.md §"Static-grep test design (vitest)" lines 979-1023. Top of file:
      `import { describe, it, expect } from 'vitest';`
      `import fs from 'node:fs';`
      `import path from 'node:path';`
      `import { fileURLToPath } from 'node:url';`
      `const __dirname = path.dirname(fileURLToPath(import.meta.url));`
      `const REPO_ROOT = path.resolve(__dirname, '..', '..');`
      `const CODEOWNERS_PATH = path.join(REPO_ROOT, '.github/CODEOWNERS');`
      Top-level `describe('Phase 47 CLEANUP-04: CODEOWNERS pins (last-matching-rule order)', () => {...})`.
      Inside (read once at top):
        `const src = fs.readFileSync(CODEOWNERS_PATH, 'utf8');`
        `const rules = src.split('\n').filter(line => line.trim() && !line.trim().startsWith('#')).map(line => line.trim());`
      Then it() blocks per Behavior. The 5 per-rule its use the EXPECTED_ORDER pattern from 47-RESEARCH.md:
        `const EXPECTED_ORDER = [/^\/src\/\s+@/, /^\/tests\/\s+@/, /^\/\.github\/workflows\/\s+@/, /^\/tests\/golden\/\s+@/, /^\/tests\/e2e\/test-cases-quarantine\.js\s+@/];`
        `EXPECTED_ORDER.forEach((re, i) => { it('rule ' + (i + 1) + ' matches ' + re, () => { expect(rules[i]).toMatch(re); }); });`
      The order-invariant it() block uses findIndex per the template:
        `it('more-specific paths appear AFTER broader ones (last-matching-rule semantics)', () => { const idxTests = rules.findIndex(r => r.startsWith('/tests/ ')); const idxGolden = rules.findIndex(r => r.startsWith('/tests/golden/')); const idxQuarantine = rules.findIndex(r => r.startsWith('/tests/e2e/test-cases-quarantine.js')); expect(idxTests).toBeLessThan(idxGolden); expect(idxTests).toBeLessThan(idxQuarantine); });`
      The maintainer-pin it() block:
        `it('all 5 rules use single maintainer @tonyrowles', () => { rules.forEach((r) => expect(r).toContain('@tonyrowles')); });`
    Step 3 (GREEN verify): `npx vitest run tests/unit/codeowners-pinned.test.js --reporter=dot` — confirm all 9 assertions PASS against the current `.github/CODEOWNERS` (verified at planning time — 5 rules in canonical order, all `@tonyrowles`).
    Step 4 (COMMIT): Stage exactly `tests/unit/codeowners-pinned.test.js`. Commit message: `test(47-04): pin CODEOWNERS 5-path last-matching-rule order (Pitfall 5 regression guard)`.
  </action>
  <acceptance_criteria>
    - `test -f tests/unit/codeowners-pinned.test.js` exits 0
    - `grep -cE "^\s+it\\(" tests/unit/codeowners-pinned.test.js` returns at least 9 (1 + 1 + 5 + 1 + 1)
    - `npx vitest run tests/unit/codeowners-pinned.test.js --reporter=dot` exits 0 (all 9 assertions pass)
    - Test asserts `tonyrowles` maintainer pin: `grep -c "tonyrowles" tests/unit/codeowners-pinned.test.js` returns at least 1
    - Test asserts last-matching-rule order via findIndex: `grep -c "toBeLessThan(idxGolden)" tests/unit/codeowners-pinned.test.js` returns at least 1
    - `git log --oneline -1` shows commit subject matching `^[0-9a-f]+ test\(47-04\):`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/codeowners-pinned.test.js --reporter=dot</automated>
  </verify>
  <done>tests/unit/codeowners-pinned.test.js created with ≥9 assertions pinning the 5 CODEOWNERS paths in canonical last-matching-rule order + maintainer pin; all GREEN against current `.github/CODEOWNERS`; single atomic test-commit landed.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Live `gh api` ruleset audit — verify allow_auto_merge=false + bypass_actors=[] + required_status_checks contains verifier-gate + deps-update-gate; if missing, surface PATCH decision per CLAUDE.md C1/C2/C3</name>
  <files>(no files modified by audit; potentially patches GitHub ruleset state if user approves Option 1)</files>
  <read_first>
    - .planning/phases/47-v4-0-cleanup/47-RESEARCH.md (§"CLEANUP-04 Toolchain" lines 870-959 — full gh-api invocation sequence; §"Pitfall 3: PATCH vs PUT" lines 1161-1165; §"Pitfall 4: required-status-checks context name mismatch" lines 1167-1171; §"Pitfall 7: gh CLI silent on auth failure" lines 1186-1189)
    - $HOME/.claude/CLAUDE.md OR ./CLAUDE.md (C1/C2/C3 — verify the AskUserQuestion fallback pattern; user MUST explicitly approve PATCH; do not auto-pick "(Recommended)")
  </read_first>
  <decision>
    Conditional decision: only invoked IF the live audit reveals that `required_status_checks` is missing `verifier-gate` and/or `deps-update-gate`. The decision is whether to PATCH the ruleset NOW (Option 1) OR defer to v4.1 tech_debt (Option 2) OR delegate to manual GitHub web-UI remediation (Option 3).
  </decision>
  <context>
    Phase 39 created the v4.0-main-protection ruleset (handoff: ruleset 17086676 active). v4.0 workflows (`v40-verifier-gate.yml`, `v40-deps-update.yml`) ship new job names (`verifier-gate`, `deps-update-gate`) that may not yet be in the ruleset's required_status_checks. The audit confirms current state; if both job names are present, no action is needed (record PASS in v4.0-MILESTONE-AUDIT.md). If either is missing, decide between Options 1/2/3.
  </context>
  <options>
    <option id="option-1">
      <name>PATCH the ruleset now via gh api -X PUT</name>
      <pros>Brings the ruleset into compliance immediately; v4.0 milestone closes with clean branch_protection state; future PRs auto-trip the gate.</pros>
      <cons>Requires repo admin scope on the authenticated gh token; creates a noisy audit-log entry on GitHub; if Pitfall 4 context-name format is wrong, the PATCH succeeds but the gate doesn't fire (silent failure).</cons>
    </option>
    <option id="option-2">
      <name>Skip PATCH, record as v4.1 tech_debt</name>
      <pros>No risk of Pitfall 4 silent-failure now; defers GitHub admin operations to a separate readiness gate.</pros>
      <cons>v4.0 audit closes with branch_protection PARTIAL; the gate is not enforced until v4.1 closes the gap; new PRs in the interim do not auto-trip the new v4.0 job checks.</cons>
    </option>
    <option id="option-3">
      <name>Manual remediation via GitHub web UI</name>
      <pros>Avoids the gh-api PATCH surface entirely; the operator sees the exact context strings in the dropdown UI and selects them visually (eliminates Pitfall 4).</pros>
      <cons>Requires manual operator step; not scriptable; audit notes "MANUAL FOLLOWUP REQUIRED"; longer turnaround to closure.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - `gh auth status` exits 0 before audit (Pitfall 7 prerequisite — anonymous queries on private repos return [] silently)
    - `/tmp/47-04-ruleset-summary.json` exists after audit (gh api ruleset query captured to file)
    - `/tmp/47-04-contexts.txt` exists after audit (sorted list of required_status_checks contexts captured)
    - JSON evidence has `.enforcement` field: `jq -e .enforcement /tmp/47-04-ruleset-summary.json` exits 0
    - allow_auto_merge value captured in working notes: `gh api repos/${OWNER}/${REPO} -q .allow_auto_merge` returns `false` (expected)
    - bypass_actors length captured: `jq '.bypass_actors | length' /tmp/47-04-ruleset-summary.json` returns 0 (expected — Do not allow bypassing: ON)
    - If `verifier-gate` and `deps-update-gate` BOTH present in /tmp/47-04-contexts.txt: audit recorded PASS in working notes; no PATCH needed
    - If EITHER context missing: per CLAUDE.md C1/C2/C3, user's explicit Option choice (option-1 PATCH / option-2 defer-tech-debt / option-3 manual-web-UI) is captured in working notes; `(Recommended)` was NOT auto-picked
    - If user chose option-1 (PATCH): per Pitfall 4, the canonical context-name format was verified via `gh pr checks <recent-pr-n>` BEFORE the PATCH; per Pitfall 3, the PATCH used `gh api -X PUT` (not `-X PATCH`); /tmp/47-04-ruleset-after-patch.json captured; post-PATCH re-query confirmed both contexts now present
    - Working notes capture all values for Task 3 consumption: OWNER, REPO, RULESET_ID, allow_auto_merge, bypass_actors length, enforcement value, required_status_checks list before AND after (if PATCH applied), user's Option choice
  </acceptance_criteria>
  <action>
    Step 1: Per Pitfall 7, verify gh CLI auth: `gh auth status 2>&1`. If exit non-zero, STOP and prompt user to `gh auth login`. Anonymous queries on private repos return `[]` silently — never proceed without fresh auth.
    Step 2: Capture OWNER + REPO: `OWNER=$(gh repo view --json owner -q '.owner.login')`; `REPO=$(gh repo view --json name -q '.name')`; `echo "Auditing: ${OWNER}/${REPO}"`. Record both in working notes for Task 3.
    Step 3: Verify allow_auto_merge: `AUTO_MERGE=$(gh api "repos/${OWNER}/${REPO}" -q '.allow_auto_merge')`. Expect `false`. Record value for audit.
    Step 4: Enumerate rulesets: `gh api "repos/${OWNER}/${REPO}/rulesets" --jq '.[] | {id, name, target, enforcement, source_type}' > /tmp/47-04-rulesets-enum.json`. Expect at least one ruleset with `target == 'branch'` (most likely the Phase 39 v4.0-main-protection ruleset).
    Step 5: Identify the main ruleset: `RULESET_ID=$(gh api "repos/${OWNER}/${REPO}/rulesets" --jq '.[] | select(.target=="branch") | .id' | head -1)`. Confirm RULESET_ID is non-empty; record value.
    Step 6: Inspect the ruleset: `gh api "repos/${OWNER}/${REPO}/rulesets/${RULESET_ID}" > /tmp/47-04-ruleset-current.json`. Then extract key fields: `jq '{name, enforcement, bypass_actors, required_status_checks: (.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks // [])}' /tmp/47-04-ruleset-current.json > /tmp/47-04-ruleset-summary.json`.
    Step 7: Assert state: (a) bypass_actors.length === 0 → record; (b) enforcement === 'active' → record; (c) required_status_checks contains `verifier-gate` and `deps-update-gate`. Use: `jq -r '.required_status_checks[].context' /tmp/47-04-ruleset-summary.json | sort > /tmp/47-04-contexts.txt`. Then: `grep -q '^verifier-gate$' /tmp/47-04-contexts.txt && grep -q '^deps-update-gate$' /tmp/47-04-contexts.txt && echo "PASS" || echo "MISSING"`.
    Step 8 (CONDITIONAL — only if MISSING per Step 7): Per Pitfall 4, before any PATCH, run `gh pr checks <any-recent-pr-n>` against a real PR (preferably one with v40-verifier-gate run history) to confirm the canonical context name format. If GitHub stores `V40 Verifier Gate / verifier-gate` (workflow-prefixed) instead of bare `verifier-gate`, use the prefixed form in the PATCH. Record the canonical form.
    Step 9 (CONDITIONAL — only if MISSING per Step 7): Per CLAUDE.md C1/C2/C3, surface the 3-option decision (this checkpoint's options block above). Do NOT auto-pick "(Recommended)". Wait for explicit user response indicating Option 1, 2, or 3.
    Step 10 (CONDITIONAL — only if user picks Option 1): Execute PATCH per 47-RESEARCH.md §"`gh api` ruleset PATCH shape":
      `jq '.rules |= map(if .type == "required_status_checks" then .parameters.required_status_checks |= (. + [{context: "<canonical-context-from-Step-8>", integration_id: null}, {context: "<canonical-context-from-Step-8>", integration_id: null}] | unique_by(.context)) else . end)' /tmp/47-04-ruleset-current.json > /tmp/47-04-ruleset-patched.json`
      `gh api -X PUT "repos/${OWNER}/${REPO}/rulesets/${RULESET_ID}" --input /tmp/47-04-ruleset-patched.json > /tmp/47-04-ruleset-after-patch.json`
      Re-query: `gh api "repos/${OWNER}/${REPO}/rulesets/${RULESET_ID}" --jq '.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context' > /tmp/47-04-contexts-after.txt`
      Verify the two contexts now appear; if PATCH failed (HTTP non-2xx), surface error to user and STOP.
    Step 11: Capture all evidence for Task 3 consumption: AUTO_MERGE value, OWNER, REPO, RULESET_ID, bypass_actors length, enforcement value, list of required_status_checks before AND after (if PATCH applied), Pitfall-4 canonical context format note, user's Option choice if MISSING surfaced.
  </action>
  <resume-signal>
    If audit PASSES (all 3 assertions hold AND both job names present): type "approved" to proceed to Task 3.
    If audit reveals MISSING contexts: select Option 1, 2, or 3 explicitly (do not type "(Recommended)"); after the chosen action completes (PATCH applied / tech_debt recorded / manual followup noted), type "approved" with the option ID.
  </resume-signal>
  <verify>
    <automated>bash -c 'test -f /tmp/47-04-ruleset-summary.json || { echo "audit JSON not captured"; exit 1; }; test -f /tmp/47-04-contexts.txt || { echo "contexts.txt not captured"; exit 1; }; jq -e ".enforcement" /tmp/47-04-ruleset-summary.json > /dev/null || { echo "enforcement field missing from audit"; exit 1; }; echo "audit evidence captured: $(wc -l < /tmp/47-04-contexts.txt) contexts in required_status_checks"'</automated>
  </verify>
  <done>Live `gh api` audit completed; allow_auto_merge value captured (expect false); ruleset bypass_actors + enforcement + required_status_checks state recorded; if required-status-checks was missing verifier-gate or deps-update-gate, user explicitly chose Option 1/2/3 per CLAUDE.md C1/C2/C3 (no auto-pick); if Option 1, PATCH applied via gh api -X PUT with canonical context names + before/after evidence preserved at /tmp/47-04-*.</done>
</task>

<task type="auto">
  <name>Task 3: Bootstrap .planning/v4.0-MILESTONE-AUDIT.md — consolidate Plans 47-01 (INT-FIX commits), 47-02 (Nyquist), 47-03 (UAT), and 47-04 Task 2 (branch protection) into the canonical milestone audit file</name>
  <files>.planning/v4.0-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/v3.1-MILESTONE-AUDIT.md (full file — template for shape, frontmatter keys, markdown sections)
    - .planning/phases/47-v4-0-cleanup/47-RESEARCH.md (§"v4.0-MILESTONE-AUDIT.md section template" lines 1027-1145 — verbatim adaption target)
    - .planning/phases/47-v4-0-cleanup/47-01-SUMMARY.md (3 INT-FIX commit SHAs + TP-bundle SHA — generated by Plan 47-01)
    - .planning/phases/47-v4-0-cleanup/47-02-SUMMARY.md (per-phase Nyquist outcome table; compliant_phases / partial_phases derivation)
    - .planning/phases/47-v4-0-cleanup/47-03-SUMMARY.md (UAT-47-c PASS/FAIL outcome)
    - .planning/phases/47-v4-0-cleanup/47-UAT-EVIDENCE.md (UAT-47-c full evidence section)
    - .planning/phases/47-v4-0-cleanup/47-UAT-DEFERRED.md (4 DEFERRED runbook stubs — referenced from human_verification block)
    - /tmp/47-04-ruleset-summary.json + /tmp/47-04-contexts.txt + (if PATCH) /tmp/47-04-contexts-after.txt — live audit evidence from Task 2
  </read_first>
  <acceptance_criteria>
    - File `.planning/v4.0-MILESTONE-AUDIT.md` exists at the canonical path (NOT under .planning/phases/47-*) — at-rest discoverable by the lifecycle audit-milestone step
    - File has YAML frontmatter with keys: `milestone: v4.0`, `audited: <ISO>`, `status: <tech_debt | passed>`, `scores`, `gaps`, `human_verification`, `nyquist`, `branch_protection`, `tech_debt`
    - `scores` block includes: `requirements: 33/33`, `phases: 9/9`, `integration: 5/5 # 5 ARCHITECTURE §4 touchpoints pinned by Phase 47 CLEANUP-01 (commits <SHA1>, <SHA2>, <SHA3>, <SHA4-TP-bundle>)`
    - `gaps` block: `requirements: []`, `integration: []`, `flows: []` (all closed by Phase 47)
    - `human_verification` block: 5 entries — 1 for UAT-47-c (outcome PASS|FAIL with verified_at + evidence path) + 4 DEFERRED (UAT-47-a, b, d, e) each with `sub_status: requires-push` + `runbook:` pointer to 47-UAT-DEFERRED.md section
    - `nyquist` block: `compliant_phases: [...]` lists exactly the phases stamped true by Plan 47-02; `partial_phases: [...]` lists any not stamped; `missing_phases: []`; `overall: <complete | partial>` based on compliant_phases.length === 8
    - `branch_protection` block: `allow_auto_merge: <false>`, `ruleset: { id: <id-from-Task-2>, bypass_actors: [], enforcement: active, required_status_checks: [<list-from-Task-2-with-canonical-context-names>] }`, `codeowners_static_grep_test: PASS` (asserts Task 1 vitest passes — verifiable via `npx vitest run tests/unit/codeowners-pinned.test.js`)
    - `tech_debt` block: includes any new entries from Plan 47-02 (PARTIAL Nyquist outcomes) + Plan 47-03 (UAT-47-c FAIL if any) + Task 2 (verifier-gate/deps-update-gate ruleset gap if Option 2 chosen)
    - Markdown body has these sections: `# Milestone v4.0 — Self-Healing Test Suite — Audit Report`, `## Scores`, `## Cross-Phase Integration` (lists 5 touchpoints with regression test paths + 3 INT-FIX commit SHAs from 47-01), `## Pre-existing Test Regressions — RESOLVED in Phase 47 CLEANUP-01`, `## Nyquist Coverage` (per-phase table), `## Human-Verification Items` (5 items), `## Branch Protection` (gh-api evidence), `## Why status is <tech_debt | passed>`
    - File ≥ 100 lines
    - File parses as valid YAML+markdown (YAML frontmatter block + markdown body separated by `---`)
    - Commit message: `docs(47-04): bootstrap v4.0-MILESTONE-AUDIT.md — close v4.0 milestone`
  </acceptance_criteria>
  <action>
    Step 1: Source the 4 commit SHAs from Plan 47-01. Run `git log --oneline -20 | grep -E "^[0-9a-f]+ (test|fix)\(47-cleanup\):"`. Capture: TP-bundle SHA + subject (test(47-cleanup): pin 5 v4.0 ARCHITECTURE §4 ...); INT-FIX-LEDGER SHA; INT-FIX-CAL SHA; INT-FIX-LOCK SHA.
    Step 2: Source the Plan 47-02 per-phase outcome table from `.planning/phases/47-v4-0-cleanup/47-02-SUMMARY.md`. Parse the table to determine compliant_phases (rows where nyquist_compliant=true) and partial_phases (rows where nyquist_compliant=false with gap notes).
    Step 3: Source UAT-47-c outcome from `.planning/phases/47-v4-0-cleanup/47-UAT-EVIDENCE.md`: grep `^\*\*status:\*\*` under the `## UAT-47-c` section → PASS or FAIL; capture verified_at, command, exit_code, fingerprint, evidence file reference, follow-up issue URL if FAIL.
    Step 4: Source Task 2 branch-protection evidence: read /tmp/47-04-ruleset-summary.json (enforcement, bypass_actors, required_status_checks before-PATCH); /tmp/47-04-contexts.txt (sorted context list); /tmp/47-04-contexts-after.txt if PATCH applied; AUTO_MERGE value from working notes; OWNER + REPO + RULESET_ID; user's Option choice if MISSING was surfaced.
    Step 5: Use the Write tool to create `.planning/v4.0-MILESTONE-AUDIT.md` (NEW FILE — at the project-root .planning level, NOT under .planning/phases/47-*). The file structure follows the template at 47-RESEARCH.md lines 1027-1145 with these adaptations:
      Frontmatter block (between `---` delimiters):
        `milestone: v4.0`
        `audited: <current ISO timestamp>`
        `status: <tech_debt if any tech_debt entries non-empty else passed>`
        `scores:` block with 4 lines: requirements 33/33, phases 9/9, integration 5/5 (with the 4 SHAs from Step 1 in the trailing comment), flows N/N (N = count of distinct E2E flows verified; for v4.0 milestone with 5 ARCHITECTURE §4 touchpoints, flows is 5/5 or the count reflected in roadmap)
        `gaps:` block: empty lists for requirements / integration / flows (all closed)
        `human_verification:` block: list of 5 dict entries — first is UAT-47-c with `phase: 47`, `item: "FLAKE escalation suppresses re-files (N=3 in 14 days; 30-day cooldown)"`, `outcome: PASS` (or FAIL), `verified_at: <Step-3 timestamp>`, `evidence: ".planning/phases/47-v4-0-cleanup/47-UAT-EVIDENCE.md §c"`; next 4 are DEFERRED entries for UAT-47-a, b, d, e each with `outcome: DEFERRED`, `sub_status: requires-push`, `runbook: ".planning/phases/47-v4-0-cleanup/47-UAT-DEFERRED.md §a|b|d|e"`
        `nyquist:` block: `compliant_phases:` (from Step 2), `partial_phases:` (from Step 2), `missing_phases: []`, `overall: complete` (if compliant_phases length is 8) or `partial`
        `branch_protection:` block: `allow_auto_merge: <Step-4 value>`, `ruleset:` sub-block with `id: <Step-4 RULESET_ID>`, `bypass_actors: []`, `enforcement: active`, `required_status_checks: [<list of contexts from Step 4 post-PATCH or pre-PATCH if no PATCH>]`; `codeowners_static_grep_test: PASS`
        `tech_debt:` list — empty if all CLEANUP tracks closed; otherwise entries per: PARTIAL Nyquist phases from Step 2 (e.g., `- phase: 42-nyquist-gap` / `items: ["PROMPT-04 lacks automated assertion (gap surfaced by gsd-nyquist-auditor 2026-06-XX)"]`), UAT-47-c FAIL if applicable (e.g., `- phase: 47-uat-c-fail` / `items: ["FLAKE escalation classifier did not trip on synthetic fixture; see issue <URL>"]`), ruleset gap if Step 4 Option 2 chosen (e.g., `- phase: 47-04-ruleset-gap` / `items: ["required_status_checks missing verifier-gate and/or deps-update-gate; user chose Option 2 (defer to v4.1)"]`)
      Markdown body:
        `# Milestone v4.0 — Self-Healing Test Suite — Audit Report`
        `**Audited:** <ISO>`
        `**Status:** <tech_debt | passed>`
        `**Milestone goal:** Close the LLM-driven feedback loop end-to-end — triaged GitHub issues from the v3.1 pipeline automatically produce draft PRs with proposed fixes; the affected case is re-verified on the proposed branch; on merge the quarantine entry promotes to golden — preserving human-gated approval for every patch that ships.`
        `## Scores` — table with 4 rows (Requirements 33/33; Phases 9/9; Integration 5/5; E2E Flows N/N)
        `## Cross-Phase Integration` — list of 5 touchpoints with the regression test paths (tests/unit/v4-touchpoints.test.js TP-01..TP-05) + each pointed at its producer/consumer ARCHITECTURE §4 reference
        `## Pre-existing Test Regressions — RESOLVED in Phase 47 CLEANUP-01` — 3 numbered items each prefixed `✅ RESOLVED (commit <SHA>)`: INT-FIX-LEDGER (committed ledger reset; root cause: local `npm run e2e:explore` leak), INT-FIX-CAL (1-line dynamic-month-key fix at L389), INT-FIX-LOCK (vitest static-grep pins @anthropic-ai/sdk EXACT 0.100.1 in package.json + package-lock.json)
        `## Nyquist Coverage` — table with 8 rows (phases 39-46) showing nyquist_compliant value + Status (COMPLIANT | PARTIAL) + Action ("Phase 47 cold-stamped (commit <SHA-from-47-02>)" for COMPLIANT; gap description for PARTIAL)
        `## Human-Verification Items` — bulleted list of 5 items matching the frontmatter `human_verification:` block; UAT-47-c shows outcome + verified_at; 4 DEFERRED show sub_status + runbook path
        `## Branch Protection` — bulleted assertions: `repos/<OWNER>/<REPO>.allow_auto_merge → <false> ✓ (verified via gh api)`, ruleset details (id, bypass_actors empty, enforcement active, required_status_checks list including verifier-gate + deps-update-gate or PARTIAL note), CODEOWNERS static-grep test PASS
        `## Why status is <tech_debt | passed>` — closing paragraph summarizing each CLEANUP track's resolution + any deferred items
    Step 6: Validate YAML coherence: `node -e "const fs=require('fs'); const t=fs.readFileSync('.planning/v4.0-MILESTONE-AUDIT.md','utf8'); const blocks=t.split('---\n'); if(blocks.length!==3) throw new Error('expected 1 frontmatter block, got '+(blocks.length-2)); const fm=blocks[1]; ['milestone:','audited:','status:','scores:','gaps:','human_verification:','nyquist:','branch_protection:','tech_debt:'].forEach((k) => { if(!fm.includes(k)) throw new Error('missing frontmatter key '+k); }); console.log('v4.0-MILESTONE-AUDIT YAML coherent');"`
    Step 7: Commit: stage exactly `.planning/v4.0-MILESTONE-AUDIT.md`. Commit message: `docs(47-04): bootstrap v4.0-MILESTONE-AUDIT.md — close v4.0 milestone`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/v4.0-MILESTONE-AUDIT.md'; if(!fs.existsSync(p)) throw new Error('audit file missing'); const t=fs.readFileSync(p,'utf8'); const blocks=t.split('---\n'); if(blocks.length!==3) throw new Error('YAML frontmatter malformed (expected 1 block, got '+(blocks.length-2)+')'); const fm=blocks[1]; ['milestone:','audited:','status:','scores:','gaps:','human_verification:','nyquist:','branch_protection:','tech_debt:'].forEach((k) => { if(!fm.includes(k)) throw new Error('missing frontmatter key '+k); }); const body=blocks[2]; ['## Scores','## Cross-Phase Integration','## Pre-existing Test Regressions','## Nyquist Coverage','## Human-Verification Items','## Branch Protection','## Why status is'].forEach((s) => { if(!body.includes(s)) throw new Error('missing markdown section '+s); }); const lines=t.split('\n').length; if(lines<100) throw new Error('expected ≥100 lines, got '+lines); console.log('v4.0-MILESTONE-AUDIT.md coherent: 9 required frontmatter keys + 7 required markdown sections + '+lines+' lines');"</automated>
  </verify>
  <done>.planning/v4.0-MILESTONE-AUDIT.md created at the canonical project-root .planning path; 9 required frontmatter keys present (milestone, audited, status, scores, gaps, human_verification, nyquist, branch_protection, tech_debt); 7 required markdown sections present; INT-FIX commit SHAs cross-linked; per-phase Nyquist outcomes from 47-02 SUMMARY transposed; 5 human_verification entries (1 PASS/FAIL + 4 DEFERRED); branch-protection state from Task 2 gh api evidence recorded; ≥100 lines; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local CODEOWNERS file ↔ GitHub access-control enforcement | `.github/CODEOWNERS` is a flat file pushed to GitHub which then enforces last-matching-rule semantics on PR reviews. Local file order is load-bearing for the enforcement path. |
| Local audit YAML ↔ GitHub ruleset live state | v4.0-MILESTONE-AUDIT.md `branch_protection:` block captures a SNAPSHOT of live state at audit time. Future drift (manual ruleset edits, expired bypass_actors, etc.) is not detected by this audit — only by re-running CLEANUP-04. |
| Task 2 PATCH via gh api -X PUT | Tightens GitHub access controls (adds required_status_checks); requires repo admin scope; gated on user approval per CLAUDE.md C1/C2/C3. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-04-01 | Elevation of Privilege | CODEOWNERS reorder (e.g., editor auto-format puts `/tests/golden/` before `/tests/`) → more-specific pin shadowed by broader rule → human review on tests/golden/baseline.json silently routes to wrong owner | mitigate | Task 1 vitest pins all 5 paths in canonical order via findIndex assertions; reorder trips the test at `npm run test:src`. |
| T-47-04-02 | Elevation of Privilege | Ruleset bypass_actors expanded silently (e.g., admin adds themselves as bypass) → required_status_checks circumventable | mitigate | Task 2 gh api audit asserts `bypass_actors.length === 0`; if non-zero, audit records the actors and Task 3 lists in tech_debt block. Re-audit each milestone. |
| T-47-04-03 | Tampering | Required-status-check `verifier-gate` or `deps-update-gate` silently removed (e.g., ruleset edit reverts) → regressions slip through | mitigate | Task 2 gh api audit asserts both contexts present; missing surfaces as Task 2's 3-option decision; audit records the live state. Future drift caught by re-running CLEANUP-04. |
| T-47-04-04 | Tampering | Pitfall 4: PATCH uses bare job_id `verifier-gate` but GitHub stores prefixed `V40 Verifier Gate / verifier-gate` → PATCH succeeds but the next PR doesn't trigger the required check (silent failure) | mitigate | Task 2 Step 8 mandates running `gh pr checks <recent-pr-n>` to confirm canonical context name format BEFORE any PATCH. The PATCH uses whatever exact string appears on a real PR's checks list. |
| T-47-04-05 | Tampering | Pitfall 3: PATCH uses `-X PATCH` instead of `-X PUT` → API silently no-ops OR returns 422 | mitigate | Task 2 Step 10 explicitly uses `gh api -X PUT --input <full-ruleset-json>` per Pitfall 3. If 422 returned, surface to user (do not auto-retry with PATCH). |
| T-47-04-06 | Repudiation | Audit YAML claims `verifier-gate` PATCH applied but evidence not preserved | mitigate | Task 2 Step 10 preserves /tmp/47-04-ruleset-before.json + /tmp/47-04-ruleset-patched.json + /tmp/47-04-ruleset-after-patch.json; Task 3 records both before/after in v4.0-MILESTONE-AUDIT.md `branch_protection:` block. |
| T-47-04-SC | Tampering | npm/pip/cargo installs | accept | Phase 47 installs ZERO new packages. Task 1 vitest test uses existing infrastructure; Task 2 gh CLI is host tool; Task 3 audit doc is plain markdown. |
</threat_model>

<verification>
After all 3 tasks complete:
1. tests/unit/codeowners-pinned.test.js exists and passes: `npx vitest run tests/unit/codeowners-pinned.test.js --reporter=dot` exits 0.
2. .planning/v4.0-MILESTONE-AUDIT.md exists at canonical path with 9 frontmatter keys + 7 markdown sections + ≥100 lines (Task 3 verify).
3. Live `gh api` audit evidence preserved in working notes + transcribed into audit YAML (Task 2 + Task 3).
4. `npm run test:src` exits 0 — full unit suite still green (includes new codeowners-pinned + Plan 47-01 v4-touchpoints + package-lock-pinned + INT-FIX tests + Plan 47-03 uat-deferred-runbook).
5. Git log shows Plan 47-04 commits: `git log --oneline -10 | grep -cE "(docs|test)\(47-04\):"` returns at least 2 (codeowners test + audit bootstrap; potentially a 3rd if Option 1 ruleset PATCH was applied).
6. Cross-plan integrity: 47-01-SUMMARY.md, 47-02-SUMMARY.md, 47-03-SUMMARY.md all still present and unchanged (47-04 reads but does not modify).
</verification>

<success_criteria>
- tests/unit/codeowners-pinned.test.js pins 5 CODEOWNERS paths in last-matching-rule order + maintainer pin (≥9 assertions GREEN)
- Live `gh api` audit completed: allow_auto_merge=false confirmed, ruleset bypass_actors=[] confirmed, required_status_checks audited for verifier-gate + deps-update-gate; if missing, user explicitly chose Option 1/2/3 per CLAUDE.md C1/C2/C3 (no auto-pick)
- .planning/v4.0-MILESTONE-AUDIT.md bootstrapped at canonical path with 9 frontmatter keys + 7 markdown sections + ≥100 lines
- All Phase 47 outputs cross-linked into the audit: INT-FIX commit SHAs (from 47-01), per-phase Nyquist table (from 47-02), UAT outcomes (1 PASS/FAIL + 4 DEFERRED from 47-03), branch-protection live evidence (from 47-04 Task 2)
- `npm run test:src` exits 0 (no regression to other tests)
- 2-3 commits land from this plan in order: codeowners test → audit bootstrap → optional ruleset-patch evidence
- v4.0 milestone is now closeable: audit doc is canonical input to lifecycle audit-milestone step
</success_criteria>

<output>
Create `.planning/phases/47-v4-0-cleanup/47-04-SUMMARY.md` per the standard template recording: codeowners-pinned test assertion count (≥9), live audit evidence (OWNER/REPO/RULESET_ID + bypass_actors length + required_status_checks list before/after if PATCH), v4.0-MILESTONE-AUDIT.md sections + line count, user's Option choice if MISSING was surfaced, commit SHAs (2-3 commits).
</output>
