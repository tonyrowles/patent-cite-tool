---
phase: 50-cleanup-04-readiness-gate
plan: 01
type: execute
wave: 1
depends_on: [49-01]
files_modified:
  - docs/v40-repo-config.md
  - tests/unit/v40-verifier-gate-doc.test.js
  - .planning/phases/50-cleanup-04-readiness-gate/evidence/pre-patch-ruleset.json
  - .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh
  - .planning/phases/50-cleanup-04-readiness-gate/evidence/integration-id-discovery.log
  - .planning/phases/50-cleanup-04-readiness-gate/evidence/gate-01-payload.json
  - .planning/phases/50-cleanup-04-readiness-gate/evidence/post-gate-01-ruleset.json
  - .planning/phases/50-cleanup-04-readiness-gate/evidence/gate-03-pr-state.json
  - .planning/phases/50-cleanup-04-readiness-gate/evidence/gate-03-merge-blocked.txt
  - .planning/phases/50-cleanup-04-readiness-gate/evidence/break-glass-test.json
  - .planning/phases/50-cleanup-04-readiness-gate/evidence/break-glass-test.log
  - .planning/phases/50-cleanup-04-readiness-gate/evidence/gate-02-payload.json
  - .planning/phases/50-cleanup-04-readiness-gate/evidence/post-gate-02-ruleset.json
  - .planning/phases/50-cleanup-04-readiness-gate/evidence/final-ruleset.json
  - .planning/phases/50-cleanup-04-readiness-gate/evidence/INDEX.md
  - .planning/phases/50-cleanup-04-readiness-gate/50-01-SUMMARY.md
autonomous: false
requirements_addressed: [GATE-01, GATE-02, GATE-03]
tags: [github-state, ruleset, branch-protection, break-glass, security-hardening]
user_setup: []

must_haves:
  truths:
    - "Ruleset 17086676 enforces verifier-gate AND deps-update-gate as required status checks pinned to GitHub Actions App (integration_id=15368)"
    - "Ruleset 17086676 has zero bypass_actors — no standing skip path"
    - "A test PR opened after GATE-01 cannot merge until both required checks report success; break-glass procedure committed and live-tested BEFORE bypass removal"
    - "Vitest static-grep assertion pins both job-name contexts (verifier-gate, deps-update-gate) against their YAML jobid declarations; npm test catches drift"
  artifacts:
    - path: "docs/v40-repo-config.md"
      provides: "Break-glass procedure runbook (new §7) — re-add bypass + remove bypass + 1-hour time-box + incident-log mandate"
      contains: "Break-Glass"
    - path: "tests/unit/v40-verifier-gate-doc.test.js"
      provides: "Static-grep pin for verifier-gate and deps-update-gate jobids in workflow YAML"
      contains: "verifier-gate"
    - path: ".planning/phases/50-cleanup-04-readiness-gate/evidence/post-gate-01-ruleset.json"
      provides: "Post-GATE-01 ruleset state — 5 rules including required_status_checks"
    - path: ".planning/phases/50-cleanup-04-readiness-gate/evidence/gate-03-merge-blocked.txt"
      provides: "Method B stderr proving gh pr merge (no --admin) was blocked by required status checks"
    - path: ".planning/phases/50-cleanup-04-readiness-gate/evidence/gate-03-pr-state.json"
      provides: "Method A statusCheckRollup proving both contexts registered and not SUCCESS"
    - path: ".planning/phases/50-cleanup-04-readiness-gate/evidence/break-glass-test.json"
      provides: "Idempotent add-then-remove cycle snapshots (3 GETs) proving recovery path is functional under current auth"
    - path: ".planning/phases/50-cleanup-04-readiness-gate/evidence/post-gate-02-ruleset.json"
      provides: "Post-GATE-02 ruleset state — 5 rules preserved + bypass_actors=[]"
    - path: ".planning/phases/50-cleanup-04-readiness-gate/evidence/final-ruleset.json"
      provides: "Final ruleset snapshot — closure baseline"
    - path: ".planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh"
      provides: "One-command recovery — re-PUTs the pre-patch ruleset"
    - path: ".planning/phases/50-cleanup-04-readiness-gate/evidence/INDEX.md"
      provides: "Per-file role map for all evidence artifacts"
    - path: ".planning/phases/50-cleanup-04-readiness-gate/50-01-SUMMARY.md"
      provides: "Phase closure narrative + SC mapping"
  key_links:
    - from: "evidence/pre-patch-ruleset.json"
      to: "evidence/gate-01-payload.json"
      via: "jq pipeline `((.rules + [<new required_status_checks rule>]))` constructed in Task 02"
      pattern: "required_status_checks"
    - from: "evidence/gate-01-payload.json"
      to: "Ruleset 17086676 (live GitHub state)"
      via: "gh api -X PUT --input"
      pattern: "PUT /repos/tonyrowles/patent-cite-tool/rulesets/17086676"
    - from: "tests/unit/v40-verifier-gate-doc.test.js"
      to: ".github/workflows/v40-verifier-gate.yml + v40-deps-update.yml"
      via: "fs.readFileSync + expect(...).toMatch(/^\\s{2}<jobid>:\\s*$/m)"
      pattern: "verifier-gate:|deps-update-gate:"
    - from: "GATE-03 test PR (test-only/gate-03-probe-*)"
      to: "evidence/gate-03-pr-state.json + gate-03-merge-blocked.txt"
      via: "gh pr view --json statusCheckRollup AND gh pr merge --merge (no --admin)"
      pattern: "required status check|not mergeable|base branch policy"
    - from: "evidence/break-glass-test.json"
      to: "docs/v40-repo-config.md §7 Break-Glass"
      via: "Live add-then-remove cycle on actor_id=254599900 proves the documented runbook commands are executable under current auth"
      pattern: "254599900"
---

<objective>
Make ruleset 17086676 (v4.0-main-protection) actually enforce the v4.0 trust invariant: both `verifier-gate` (jobid in v40-verifier-gate.yml:181) and `deps-update-gate` (jobid in v40-deps-update.yml:164) become required status checks pinned to GitHub Actions App (integration_id=15368), and the lone standing bypass actor (id=254599900) is removed — but ONLY after a committed break-glass runbook is live-tested and a wedged test PR proves enforcement is real while the bypass safety net is still present.

Purpose: Closes the two open trust-invariant gaps from v4.0 (CLEANUP-04 + bypass_actors=1) before Phase 51's UATs fire against the enforced ruleset.

Output: Two ruleset PUTs (one audit-log entry each), one extended Vitest file, one new docs §7, eleven evidence files, one closure SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-cleanup-04-readiness-gate/50-CONTEXT.md
@.planning/phases/50-cleanup-04-readiness-gate/50-RESEARCH.md
@.planning/phases/49-push-v4-0-integration-pr/49-INTEGRATION.md
@.planning/phases/49-push-v4-0-integration-pr/evidence/post-merge-ruleset.json
@.planning/research/PITFALLS.md
@docs/v40-repo-config.md
@.github/workflows/v40-verifier-gate.yml
@.github/workflows/v40-deps-update.yml
@.github/CODEOWNERS
@tests/unit/v40-verifier-gate-doc.test.js

<interfaces>
<!-- Live GitHub state at plan time (verified 2026-06-02 in 50-RESEARCH.md §"Live state snapshot"): -->
<!-- Ruleset 17086676: 4 rules (deletion, non_fast_forward, required_linear_history, pull_request) + 1 bypass actor (254599900/User/always). enforcement=active. -->
<!-- GitHub Actions App ID: 15368 (every check_run.app.id on merge SHA c0bb37d5... returned 15368). -->
<!-- Phase 49 merge SHA: c0bb37d5dfeb28f7ce7901fa50e36d231d6fc6e2 -->
<!-- Workflow YAML jobid declarations (line numbers verified): -->
<!--   - .github/workflows/v40-verifier-gate.yml:181 → `  verifier-gate:` (2-space indent, no name: override) -->
<!--   - .github/workflows/v40-deps-update.yml:164 → `  deps-update-gate:` (2-space indent, no name: override) -->
<!-- CODEOWNERS: .planning/ is NOT pinned (only /src/, /tests/, /.github/workflows/, /tests/golden/, /tests/e2e/test-cases-quarantine.js) — verified. -->
<!-- docs/v40-repo-config.md already has 7 numbered sections; existing §7 is "Phase 47 re-audit checklist". The new break-glass section reuses the number 7 per D-05 — the existing §7 MUST be renumbered to §8 in the SAME commit. -->

<!-- Target PUT body shapes (live-verified semantics): -->
<!-- GATE-01 body (Task 02): { "rules": [<4 existing rules verbatim> + <new required_status_checks rule>] } — 5 total rules. Top-level key `rules` is sent in FULL because PUT replaces arrays wholesale within keys it specifies. Omitting `rules` is not an option here because we are adding to it. -->
<!-- GATE-02 body (Task 05): { "bypass_actors": [] } — only this key, because PUT preserves top-level keys it does NOT see (live-confirmed). This MUST NOT include `rules` (sending an empty or stale array would clobber the 5 rules from GATE-01). -->
<!-- Break-glass live test PUT body (Task 04): { "bypass_actors": [ { "actor_id": 254599900, "actor_type": "User", "bypass_mode": "always" } ] } — identical to current state; re-add is idempotent (no-op). -->

<!-- REST verb correction (load-bearing): -->
<!-- Live probe 2026-06-02 confirmed PATCH returns HTTP 404; only PUT works for ruleset mutations. Every `gh api` mutation in this plan uses `-X PUT`. CONTEXT.md prose says "PATCH" loosely (D-01, D-15 text) — translate to PUT in commands. The GET-then-mutate-then-write operational pattern (D-03) is preserved. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 01: Commit break-glass docs §7 + pre-patch ruleset baseline + rollback.sh</name>
  <files>
    docs/v40-repo-config.md
    .planning/phases/50-cleanup-04-readiness-gate/evidence/pre-patch-ruleset.json
    .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh
  </files>
  <read_first>
    .planning/phases/50-cleanup-04-readiness-gate/50-CONTEXT.md (D-05, D-06, D-09 — break-glass location, content shape, redaction filter)
    .planning/phases/50-cleanup-04-readiness-gate/50-RESEARCH.md (§"Pattern 1", §"Security Domain", Pitfall 1 PATCH→PUT)
    .planning/research/PITFALLS.md (Pitfall 3 — break-glass mandate)
    docs/v40-repo-config.md (full file — confirm existing §7 "Phase 47 re-audit checklist" position; the new break-glass §7 replaces that slot and existing §7 is renumbered to §8)
    .planning/phases/49-push-v4-0-integration-pr/evidence/post-merge-ruleset.json (the baseline this task captures fresh from live state)
  </read_first>
  <action>
    Three sub-steps in one atomic commit; commit message: `chore(50): break-glass docs §7 + pre-patch baseline + rollback.sh`.

    Sub-step 1 — Capture pre-patch baseline (must happen FIRST so rollback.sh has its source of truth):
      mkdir -p .planning/phases/50-cleanup-04-readiness-gate/evidence
      gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 \
        > .planning/phases/50-cleanup-04-readiness-gate/evidence/pre-patch-ruleset.json
      # Local sanity assertions BEFORE proceeding (fail-fast if Phase 49 baseline drifted):
      jq -e '.rules | length == 4'         .planning/phases/50-cleanup-04-readiness-gate/evidence/pre-patch-ruleset.json
      jq -e '.bypass_actors | length == 1' .planning/phases/50-cleanup-04-readiness-gate/evidence/pre-patch-ruleset.json
      jq -e '.bypass_actors[0].actor_id == 254599900' .planning/phases/50-cleanup-04-readiness-gate/evidence/pre-patch-ruleset.json
      jq -e '.enforcement == "active"'     .planning/phases/50-cleanup-04-readiness-gate/evidence/pre-patch-ruleset.json
      If any jq assertion exits non-zero, STOP — the live state diverged from the Phase 49 handoff baseline (post-merge-ruleset.json). Surface the diff and request operator review before proceeding.

    Sub-step 2 — Write rollback.sh as a bash one-shot:
      Write `.planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh` containing:
        #!/usr/bin/env bash
        # Re-PUTs the pre-patch ruleset captured 2026-06-XX (Phase 50 Task 01).
        # Use if any GATE-01/02/03 step misconfigures ruleset 17086676.
        # One command, no flags. Requires `gh auth status` shows tonyrowles logged in.
        set -euo pipefail
        DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
        gh api -X PUT /repos/tonyrowles/patent-cite-tool/rulesets/17086676 \
          --input "$DIR/pre-patch-ruleset.json"
        echo "Ruleset 17086676 restored to pre-patch baseline (4 rules, 1 bypass actor)."
      chmod +x .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh

    Sub-step 3 — Edit docs/v40-repo-config.md to insert the break-glass runbook as §7, renumbering the existing §7 ("Phase 47 re-audit checklist") to §8.

      The new §7 has the heading exactly: `## 7. Break-Glass: temporarily re-allowing bypass on ruleset 17086676` and three subsections:

      (a) "Re-add the operator bypass actor" — a single fenced bash block with the literal command:
            gh api -X PUT /repos/tonyrowles/patent-cite-tool/rulesets/17086676 \
              --input <(echo '{"bypass_actors":[{"actor_id":254599900,"actor_type":"User","bypass_mode":"always"}]}')

      (b) "Remove the bypass actor after emergency merge" — a single fenced bash block with the literal command:
            gh api -X PUT /repos/tonyrowles/patent-cite-tool/rulesets/17086676 \
              --input <(echo '{"bypass_actors":[]}')

      (c) "Time-box & audit trail" prose paragraph stating: maximum 1 hour with bypass active; before re-adding, create `docs/incidents/<YYYY-MM-DD>-<short-slug>.md` describing why the break-glass is being used, the PR/commit being unblocked, and the planned remove-bypass timestamp; commit the incident log file alongside the re-add PUT in the SAME working session. This procedure is manual by definition — no automation.

      Then change the existing line `## 7. Phase 47 re-audit checklist` to `## 8. Phase 47 re-audit checklist`. Do not touch any other content in §1–§6 or the renumbered §8 body.

    Then stage and commit ALL three artifacts in ONE commit:
      git add docs/v40-repo-config.md \
              .planning/phases/50-cleanup-04-readiness-gate/evidence/pre-patch-ruleset.json \
              .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh
      git commit -m "chore(50): break-glass docs §7 + pre-patch baseline + rollback.sh"

    Notes:
      - No live ruleset mutation in this task — only a GET (read-only) plus filesystem writes plus git commit. autonomous=true.
      - The break-glass runbook commands use PUT (NOT PATCH per Pitfall 1 / live verification).
      - The renumber from §7 to §8 for the existing audit-checklist section is the ONLY edit to existing content; the new §7 is purely additive.
  </action>
  <verify>
    <automated>
      jq -e '.rules | length == 4 and .bypass_actors | length == 1 and .bypass_actors[0].actor_id == 254599900' .planning/phases/50-cleanup-04-readiness-gate/evidence/pre-patch-ruleset.json &&
      grep -qE '^## 7\. Break-Glass' docs/v40-repo-config.md &&
      grep -qE '^## 8\. Phase 47 re-audit checklist' docs/v40-repo-config.md &&
      grep -q '254599900' docs/v40-repo-config.md &&
      grep -q 'gh api -X PUT' docs/v40-repo-config.md &&
      grep -qE 'incidents/' docs/v40-repo-config.md &&
      test -x .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh &&
      grep -q 'gh api -X PUT' .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh &&
      git log -1 --pretty=%s | grep -qE '^chore\(50\): break-glass docs §7'
    </automated>
  </verify>
  <acceptance_criteria>
    - `evidence/pre-patch-ruleset.json` exists; `jq` asserts 4 rules, 1 bypass actor with actor_id=254599900, enforcement=active.
    - `docs/v40-repo-config.md` has `## 7. Break-Glass: temporarily re-allowing bypass on ruleset 17086676` heading.
    - The new §7 contains a `gh api -X PUT` re-add command literal that mentions actor_id 254599900, bypass_mode "always".
    - The new §7 contains a `gh api -X PUT` remove command literal with `{"bypass_actors":[]}`.
    - The new §7 contains prose mentioning a 1-hour time-box and `docs/incidents/` log file.
    - The existing audit-checklist section is renumbered: `## 8. Phase 47 re-audit checklist` (was `## 7.`).
    - `evidence/rollback.sh` exists, is executable (chmod +x), contains `gh api -X PUT` and references `pre-patch-ruleset.json`.
    - All three artifacts ship in ONE commit whose subject matches `^chore(50): break-glass docs §7`.
    - Working tree clean after commit (`git status --porcelain` empty).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 02: GATE-01 PUT (add required_status_checks) + Vitest jobid pins — SAME COMMIT</name>
  <files>
    .planning/phases/50-cleanup-04-readiness-gate/evidence/integration-id-discovery.log
    .planning/phases/50-cleanup-04-readiness-gate/evidence/gate-01-payload.json
    .planning/phases/50-cleanup-04-readiness-gate/evidence/post-gate-01-ruleset.json
    tests/unit/v40-verifier-gate-doc.test.js
  </files>
  <read_first>
    .planning/phases/50-cleanup-04-readiness-gate/50-CONTEXT.md (D-02, D-03, D-13, D-15 task 2 — integration_id discovery, GET-mutate-PUT, Vitest extension, same-commit mandate)
    .planning/phases/50-cleanup-04-readiness-gate/50-RESEARCH.md (§"Code Examples → Discovery of integration_id", §"Construct + apply GATE-01 PUT body", §"Vitest extension", Pitfalls 1/2/4 — PATCH/whole-array/integration_id=0)
    .planning/phases/50-cleanup-04-readiness-gate/evidence/pre-patch-ruleset.json (the GET output Task 01 captured — this is the source for the jq pipeline)
    tests/unit/v40-verifier-gate-doc.test.js (full file — pattern to extend; observe ESM imports + PROJECT_ROOT constant; new describe block goes at END of file)
    .github/workflows/v40-verifier-gate.yml (line 181 region — confirm `  verifier-gate:` jobid is still at 2-space indent, no `name:` override on the next line)
    .github/workflows/v40-deps-update.yml (line 164 region — confirm `  deps-update-gate:` jobid still at 2-space indent, no `name:` override)
  </read_first>
  <action>
    AUTOMATED WORK FIRST (must complete before the operator-approval checkpoint resumes). Five sub-steps; ONE atomic commit at the end.

    Sub-step 2a — Discover integration_id via Phase 49 merge SHA:
      MERGE_SHA="c0bb37d5dfeb28f7ce7901fa50e36d231d6fc6e2"
      DISCOVERY_LOG=".planning/phases/50-cleanup-04-readiness-gate/evidence/integration-id-discovery.log"
      INTEGRATION_ID=$(gh api "/repos/tonyrowles/patent-cite-tool/commits/$MERGE_SHA/check-runs" \
        --jq '[.check_runs[] | .app.id] | unique | .[]')
      {
        echo "=== integration_id discovery $(date -u +%FT%TZ) ==="
        echo "Source SHA: $MERGE_SHA (Phase 49 merge commit)"
        echo "Query: gh api /repos/.../commits/<sha>/check-runs --jq '[.check_runs[].app.id] | unique'"
        echo "Result: $INTEGRATION_ID"
        echo "Expected: 15368 (GitHub Actions App ID, https://github.com/apps/github-actions)"
        echo "NOT 26860592872 (that is the workflow-run databaseId from 49-INTEGRATION.md — conflated)"
      } > "$DISCOVERY_LOG"
      # Hard gate — STOP if discovery returns anything other than 15368:
      [[ "$INTEGRATION_ID" == "15368" ]] || { echo "FAIL: integration_id=$INTEGRATION_ID, expected 15368"; exit 1; }

    Sub-step 2b — Construct GATE-01 PUT payload from pre-patch baseline (jq append):
      PRE=".planning/phases/50-cleanup-04-readiness-gate/evidence/pre-patch-ruleset.json"
      PAYLOAD=".planning/phases/50-cleanup-04-readiness-gate/evidence/gate-01-payload.json"
      jq --argjson iid 15368 '{
        rules: ((.rules // []) + [{
          type: "required_status_checks",
          parameters: {
            required_status_checks: [
              { context: "verifier-gate",    integration_id: $iid },
              { context: "deps-update-gate", integration_id: $iid }
            ],
            strict_required_status_checks_policy: true
          }
        }])
      }' "$PRE" > "$PAYLOAD"
      # Local validation BEFORE sending to GitHub (Pitfall 2 prevention):
      jq -e '.rules | length == 5' "$PAYLOAD" || { echo "FAIL: payload should have 5 rules"; exit 1; }
      jq -e '[.rules[].type] | contains(["deletion","non_fast_forward","required_linear_history","pull_request","required_status_checks"])' "$PAYLOAD" \
        || { echo "FAIL: payload missing one of the 5 expected rule types"; exit 1; }
      jq -e '.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks | length == 2' "$PAYLOAD" \
        || { echo "FAIL: required_status_checks should have 2 contexts"; exit 1; }
      jq -e '[.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context] | sort == ["deps-update-gate","verifier-gate"]' "$PAYLOAD" \
        || { echo "FAIL: contexts must be exactly deps-update-gate + verifier-gate"; exit 1; }
      jq -e '.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].integration_id | . == 15368' "$PAYLOAD" \
        || { echo "FAIL: integration_id must be 15368 on every context (Pitfall 4)"; exit 1; }

    Sub-step 2c — Apply GATE-01 PUT (LIVE MUTATION — this is the first audit-log entry):
      gh api -X PUT /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --input "$PAYLOAD"
      gh api    /repos/tonyrowles/patent-cite-tool/rulesets/17086676 \
        > .planning/phases/50-cleanup-04-readiness-gate/evidence/post-gate-01-ruleset.json
      # Post-state assertions (Pitfall 2 — verify rules were APPENDED not REPLACED, and bypass_actors still=1):
      POST=".planning/phases/50-cleanup-04-readiness-gate/evidence/post-gate-01-ruleset.json"
      jq -e '.rules | length == 5'         "$POST" || { echo "FAIL: post-state should have 5 rules"; bash .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh; exit 1; }
      jq -e '.bypass_actors | length == 1' "$POST" || { echo "FAIL: bypass_actors should still be 1 after GATE-01 (unchanged)"; bash .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh; exit 1; }
      jq -e '[.rules[].type] | contains(["deletion","non_fast_forward","required_linear_history","pull_request","required_status_checks"])' "$POST" \
        || { echo "FAIL: post-state missing one of the 5 rule types — rollback"; bash .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh; exit 1; }
      jq -e '.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].integration_id | . == 15368' "$POST" \
        || { echo "FAIL: post-state integration_id != 15368"; bash .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh; exit 1; }

    Sub-step 2d — Extend tests/unit/v40-verifier-gate-doc.test.js with 2 new `it()` blocks inside a NEW describe block. Append at the end of the file (after the existing top-level describe closes at line 109). The new describe block name is exactly: `'Phase 50 GATE-01: required_status_checks context strings pinned in YAML'`. Inside it, declare two top-level constants for the YAML paths and two `it()` blocks:

      describe('Phase 50 GATE-01: required_status_checks context strings pinned in YAML', () => {
        const VERIFIER_GATE_YML = path.resolve(PROJECT_ROOT, '.github/workflows/v40-verifier-gate.yml');
        const DEPS_UPDATE_YML   = path.resolve(PROJECT_ROOT, '.github/workflows/v40-deps-update.yml');

        it('D11: v40-verifier-gate.yml declares `verifier-gate:` jobid (line 181 per Phase 49 audit)', () => {
          const yml = fs.readFileSync(VERIFIER_GATE_YML, 'utf8');
          expect(yml).toMatch(/^\s{2}verifier-gate:\s*$/m);
        });

        it('D12: v40-deps-update.yml declares `deps-update-gate:` jobid (line 164 per Phase 49 audit)', () => {
          const yml = fs.readFileSync(DEPS_UPDATE_YML, 'utf8');
          expect(yml).toMatch(/^\s{2}deps-update-gate:\s*$/m);
        });
      });

      Notes: reuse the existing `fs`, `path`, `PROJECT_ROOT` imports/constants already at the top of the file. Do NOT add duplicate imports. The two `toMatch` regexes pin 2-space indent + jobid + optional trailing whitespace + end-of-line — this is the strict shape verified against both workflow files.

    Sub-step 2e — Verify Vitest passes locally, then commit ALL artifacts in ONE atomic commit:
      npx vitest run tests/unit/v40-verifier-gate-doc.test.js
      # Expect: all existing tests (D1-D10) still green + 2 new tests (D11, D12) green.
      git add tests/unit/v40-verifier-gate-doc.test.js \
              .planning/phases/50-cleanup-04-readiness-gate/evidence/integration-id-discovery.log \
              .planning/phases/50-cleanup-04-readiness-gate/evidence/gate-01-payload.json \
              .planning/phases/50-cleanup-04-readiness-gate/evidence/post-gate-01-ruleset.json
      git commit -m "chore(50): GATE-01 PUT ruleset required_status_checks + Vitest jobid pins"

    THEN the operator-approval checkpoint fires (because this is a LIVE ruleset mutation — block until operator confirms before proceeding to Task 03).
  </action>
  <what-built>
    GATE-01 has landed on the live ruleset: required_status_checks rule added with verifier-gate + deps-update-gate contexts pinned to integration_id=15368. Vitest now has 2 new tests (D11, D12) pinning those jobid strings in the workflow YAML. Both ship in the SAME commit. Pre-patch baseline + payload + post-state evidence files committed.
  </what-built>
  <how-to-verify>
    1. Confirm the LIVE ruleset state (definitive truth):
       gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --jq '{rules_count: (.rules | length), bypass_count: (.bypass_actors | length), contexts: [.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context], integration_ids: [.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].integration_id] | unique}'
       Expected: `{"rules_count":5,"bypass_count":1,"contexts":["verifier-gate","deps-update-gate"],"integration_ids":[15368]}`
    2. Confirm SC-1 verbatim command from ROADMAP returns both contexts:
       gh api GET /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --jq '.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context'
       Expected: two lines `verifier-gate` and `deps-update-gate` (in any order).
    3. Run Vitest:
       npx vitest run tests/unit/v40-verifier-gate-doc.test.js
       Expected: 12 tests pass (10 existing D1-D10 + 2 new D11, D12).
    4. Inspect git log:
       git log -1 --pretty='%H %s' --stat
       Expected: ONE commit modifying both `tests/unit/v40-verifier-gate-doc.test.js` AND adding the 3 evidence files (per D-13 same-commit mandate).
    5. If anything looks wrong, run rollback BEFORE approving:
       bash .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh
       Then reply "rolled back, abort phase" instead of "approved".
  </how-to-verify>
  <resume-signal>Type "approved" once the live ruleset shows 5 rules with both contexts and integration_id=15368, and Vitest is green. Type "rolled back" if rollback.sh was executed. Type a description of the issue otherwise.</resume-signal>
  <acceptance_criteria>
    - `evidence/integration-id-discovery.log` exists and contains the literal string `15368` and the merge SHA `c0bb37d5dfeb28f7ce7901fa50e36d231d6fc6e2`.
    - `evidence/gate-01-payload.json` has `.rules | length == 5`; one rule of type `required_status_checks` with exactly 2 contexts (sorted: `["deps-update-gate","verifier-gate"]`) and integration_id=15368 on both.
    - `evidence/post-gate-01-ruleset.json` has `.rules | length == 5`, `.bypass_actors | length == 1`, `.bypass_actors[0].actor_id == 254599900` (unchanged), and the new required_status_checks rule with both contexts pinned to integration_id=15368.
    - Live ruleset state matches the post-state JSON (re-pull GET equals the file).
    - `tests/unit/v40-verifier-gate-doc.test.js` contains a `describe('Phase 50 GATE-01:` block with exactly 2 `it()` blocks named `D11` and `D12`.
    - `npx vitest run tests/unit/v40-verifier-gate-doc.test.js` exits 0 with 12 tests passing.
    - Single atomic commit subject matches `^chore\(50\): GATE-01 PUT ruleset required_status_checks \+ Vitest jobid pins`.
    - Commit touches BOTH the test file AND the evidence files (D-13 same-commit mandate verifiable via `git show --name-only HEAD`).
    - No PATCH verb appears in any executed `gh api` command transcript (operator confirms via shell history if requested).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 03: GATE-03 test PR cycle — open, two-pronged blocked-merge assertion, CLOSE (not merge)</name>
  <files>
    .planning/phases/50-cleanup-04-readiness-gate/evidence/gate-03-pr-state.json
    .planning/phases/50-cleanup-04-readiness-gate/evidence/gate-03-merge-blocked.txt
  </files>
  <read_first>
    .planning/phases/50-cleanup-04-readiness-gate/50-CONTEXT.md (D-10, D-11, D-12 — probe file location, branch name, two-pronged assertion)
    .planning/phases/50-cleanup-04-readiness-gate/50-RESEARCH.md (§"Two-pronged enforcement assertion", Pitfalls 3/5/6/7 — exit-code regex, strict policy timing, async check_suite creation, jobid vs name)
    .github/CODEOWNERS (confirm `.planning/` still non-CODEOWNED at execution time)
    .planning/phases/50-cleanup-04-readiness-gate/evidence/post-gate-01-ruleset.json (the live state we are about to exercise via a wedged PR)
  </read_first>
  <action>
    All-automated task; commit message: `chore(50): GATE-03 test PR cycle verifies enforcement (CLOSED)`.

    Sub-step 3a — Pre-flight: confirm `.planning/scratch/` is still non-CODEOWNED and re-pull live ruleset:
      grep -E '^\s*\.?/?planning' .github/CODEOWNERS && { echo "FAIL: .planning/ became CODEOWNED — abort"; exit 1; } || true
      gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --jq '.rules | length' | grep -qx 5 \
        || { echo "FAIL: ruleset no longer has 5 rules — GATE-01 may have been reverted; abort"; exit 1; }

    Sub-step 3b — Create test branch and probe file:
      ISO_TS=$(date -u +%Y%m%dT%H%M%SZ)
      BRANCH="test-only/gate-03-probe-${ISO_TS}"
      mkdir -p .planning/scratch
      echo "gate-03 probe ${ISO_TS}" > .planning/scratch/gate-03-probe.txt
      git checkout -b "$BRANCH"
      git add .planning/scratch/gate-03-probe.txt
      git commit -m "chore(50): GATE-03 probe file (will be CLOSED not merged)"
      git push -u origin "$BRANCH"

    Sub-step 3c — Open the PR (documents purpose in the body per CONTEXT discretion area):
      PR_URL=$(gh pr create --base main --head "$BRANCH" \
        --title "test-only: GATE-03 probe ${ISO_TS} (DO NOT MERGE)" \
        --body $'Phase 50 GATE-03 enforcement probe. Verifies that ruleset 17086676 blocks merge of a PR whose required status checks (verifier-gate, deps-update-gate) cannot satisfy.\n\nThis PR will be CLOSED (not merged) immediately after the two-pronged blocked-merge assertion fires. Branch will be deleted.\n\nDo not approve, do not merge. See `.planning/phases/50-cleanup-04-readiness-gate/50-01-PLAN.md` Task 03.')
      PR_NUM=$(echo "$PR_URL" | grep -oE '[0-9]+$')
      [[ -n "$PR_NUM" ]] || { echo "FAIL: could not extract PR number"; exit 1; }

    Sub-step 3d — Method A (statusCheckRollup state assertion) with poll-loop for async check_suite creation (Pitfall 6). Capture BOTH statusCheckRollup AND mergeable/mergeStateStatus to discriminate BLOCKED from BEHIND (Pitfall 5):
      EVIDENCE=".planning/phases/50-cleanup-04-readiness-gate/evidence/gate-03-pr-state.json"
      for i in 1 2 3 4 5 6 7 8; do
        gh pr view "$PR_NUM" --json statusCheckRollup,mergeable,mergeStateStatus,number,headRefName > "$EVIDENCE"
        jq -e '[.statusCheckRollup[]?|.name?] as $n
               | (["verifier-gate","deps-update-gate"]|all(. as $c | $n|index($c)))' "$EVIDENCE" \
          >/dev/null 2>&1 && break
        echo "Method A poll $i/8: contexts not yet registered, sleeping 5s..."
        sleep 5
      done

      # Hard assertion 1 — both contexts MUST be present:
      jq -e '[.statusCheckRollup[]?|.name?] as $n
             | (["verifier-gate","deps-update-gate"]|all(. as $c | $n|index($c)))' "$EVIDENCE" \
        || { echo "FAIL Method A: one or both contexts not present in statusCheckRollup after 40s"; cat "$EVIDENCE"; exit 1; }

      # Hard assertion 2 — neither context may be SUCCESS (would mean enforcement is satisfied):
      jq -e '[.statusCheckRollup[]?
              | select(.name=="verifier-gate" or .name=="deps-update-gate")
              | .state?] | all(. != "SUCCESS")' "$EVIDENCE" \
        || { echo "FAIL Method A: a required context unexpectedly SUCCESS"; cat "$EVIDENCE"; exit 1; }

      # Discriminate BLOCKED vs BEHIND (Pitfall 5) — if mergeStateStatus=BEHIND, the strict policy is firing on base-currency, not on required-checks; that is still a valid blocked state but we record it:
      MERGE_STATE=$(jq -r '.mergeStateStatus' "$EVIDENCE")
      echo "Method A: mergeStateStatus=$MERGE_STATE (BLOCKED expected; BEHIND acceptable per Pitfall 5)"

    Sub-step 3e — Method B (attempted-merge exit code + stderr pattern). NO --admin flag (would bypass the ruleset and defeat the test per Pitfall 1 of CONTEXT.md anti-patterns):
      STDERR=".planning/phases/50-cleanup-04-readiness-gate/evidence/gate-03-merge-blocked.txt"
      if gh pr merge "$PR_NUM" --merge 2> "$STDERR"; then
        echo "FAIL Method B: gh pr merge --merge succeeded but should have blocked"
        cat "$STDERR"
        exit 1
      fi
      # Belt-and-suspenders stderr regex per CONTEXT planning_context note + RESEARCH Pitfall 3 MEDIUM-confidence broadening:
      grep -qiE 'required status check|required check|failing required|not mergeable|base branch policy|protected branch' "$STDERR" \
        || { echo "FAIL Method B: stderr did not match any expected blocked-merge pattern"; cat "$STDERR"; exit 1; }

    Sub-step 3f — Close PR (NOT merge) + delete branch + clean local probe file:
      gh pr close "$PR_NUM" --delete-branch --comment "GATE-03 enforcement verified (Method A: both contexts registered, neither SUCCESS; Method B: gh pr merge --merge exited non-zero with required-check stderr). Closing per Phase 50 plan D-11."
      git checkout main
      # Local branch may already be gone if gh pr close cleaned it; ignore failure:
      git branch -D "$BRANCH" 2>/dev/null || true
      # The probe file only ever existed on the deleted branch; nothing to remove on main.

    Sub-step 3g — Stage evidence on main and commit:
      git add .planning/phases/50-cleanup-04-readiness-gate/evidence/gate-03-pr-state.json \
              .planning/phases/50-cleanup-04-readiness-gate/evidence/gate-03-merge-blocked.txt
      git commit -m "chore(50): GATE-03 test PR cycle verifies enforcement (CLOSED)"

    Notes:
      - No ruleset mutation in this task — autonomous=true. The risk surface is "test PR somehow merges" which we hard-guard against with the `if gh pr merge; then FAIL; fi` pattern.
      - If at any point the test PR somehow succeeds in merging, the break-glass procedure is still committed (Task 01) and the bypass actor is still present (Task 04 hasn't run yet) — emergency recovery is to run rollback.sh and abort the phase.
  </action>
  <verify>
    <automated>
      jq -e '[.statusCheckRollup[]?|.name?] as $n | (["verifier-gate","deps-update-gate"]|all(. as $c | $n|index($c)))' .planning/phases/50-cleanup-04-readiness-gate/evidence/gate-03-pr-state.json &&
      jq -e '[.statusCheckRollup[]?|select(.name=="verifier-gate" or .name=="deps-update-gate")|.state?] | all(. != "SUCCESS")' .planning/phases/50-cleanup-04-readiness-gate/evidence/gate-03-pr-state.json &&
      test -s .planning/phases/50-cleanup-04-readiness-gate/evidence/gate-03-merge-blocked.txt &&
      grep -qiE 'required status check|required check|failing required|not mergeable|base branch policy|protected branch' .planning/phases/50-cleanup-04-readiness-gate/evidence/gate-03-merge-blocked.txt &&
      git log -1 --pretty=%s | grep -qE '^chore\(50\): GATE-03 test PR cycle'
    </automated>
  </verify>
  <acceptance_criteria>
    - `evidence/gate-03-pr-state.json` exists and contains a `statusCheckRollup` array including BOTH `verifier-gate` and `deps-update-gate` by `.name`.
    - In that file, BOTH context entries have `.state != "SUCCESS"` (state in {PENDING, FAILURE, EXPECTED}).
    - `evidence/gate-03-merge-blocked.txt` is non-empty AND matches the case-insensitive regex `required status check|required check|failing required|not mergeable|base branch policy|protected branch` (belt-and-suspenders broadening per Pitfall 3 MEDIUM confidence). OR the `gh pr merge --merge` invocation exited non-zero AND Method A statusCheckRollup shows both contexts in non-SUCCESS state — either alone of the two prongs is sufficient if the other was indeterminate, but the script's `if gh pr merge; then FAIL; fi` already enforces non-zero exit.
    - PR was opened on a `test-only/gate-03-probe-*` branch (verifiable in `.headRefName` field of the JSON).
    - PR is now in state CLOSED (not MERGED). The branch is deleted on origin (verifiable via `gh api /repos/tonyrowles/patent-cite-tool/branches/test-only/gate-03-probe-* ` returning 404, or by `gh pr view <N> --json state,mergedAt` showing `state=CLOSED, mergedAt=null`).
    - Single atomic commit subject matches `^chore\(50\): GATE-03 test PR cycle`.
    - The throwaway probe file `.planning/scratch/gate-03-probe.txt` never appears on `main` (verifiable via `git log --all --diff-filter=A -- .planning/scratch/gate-03-probe.txt | grep -v "$BRANCH"` returning empty).
    - Working tree clean after commit.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 04: Break-glass live add-then-remove cycle (idempotent re-add of actor 254599900)</name>
  <files>
    .planning/phases/50-cleanup-04-readiness-gate/evidence/break-glass-test.json
    .planning/phases/50-cleanup-04-readiness-gate/evidence/break-glass-test.log
  </files>
  <read_first>
    .planning/phases/50-cleanup-04-readiness-gate/50-CONTEXT.md (D-07, D-08, D-09 — pre-removal live test, idempotent re-add rationale, redaction filter)
    .planning/phases/50-cleanup-04-readiness-gate/50-RESEARCH.md (§"Pattern 2: Partial-body PUT", §"Code Examples → GATE-02 PUT")
    .planning/research/PITFALLS.md (Pitfall 3 — break-glass functional confirmation BEFORE bypass removal)
    .planning/phases/50-cleanup-04-readiness-gate/evidence/post-gate-01-ruleset.json (Task 02's post-state — confirms bypass_actors still=1 with actor 254599900)
    docs/v40-repo-config.md §7 (the committed runbook this task is exercising — Task 04 runs the exact two PUT commands documented in Task 01's §7)
  </read_first>
  <action>
    AUTOMATED WORK FIRST (must complete before the operator-approval checkpoint resumes). Three snapshots + two PUTs that are both idempotent no-ops; the cycle PROVES the documented break-glass commands execute successfully under current auth WITHOUT ever leaving the ruleset in a 0-actor or 2-actor intermediate state (D-08).

    Commit message at task end: `chore(50): break-glass live add-then-remove cycle (idempotent)`.

    Sub-step 4a — Snapshot 1 (current state, expect 1 actor):
      LOG=".planning/phases/50-cleanup-04-readiness-gate/evidence/break-glass-test.log"
      SNAPSHOTS=".planning/phases/50-cleanup-04-readiness-gate/evidence/break-glass-test.json"
      REDACT="sed 's/Bearer [^\"]*/Bearer REDACTED/g'"

      {
        echo "=== break-glass live test $(date -u +%FT%TZ) ==="
        echo "Strategy: re-add the SAME existing actor (id 254599900) twice; this is idempotent at the GitHub API layer."
        echo "Goal: prove the documented §7 PUT commands work under current auth WITHOUT ever leaving the ruleset in a 0-actor or 2-actor state."
        echo ""
        echo "--- Snapshot 1: pre-cycle GET ---"
      } > "$LOG"
      gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 2>&1 | sed 's/Bearer [^"]*/Bearer REDACTED/g' >> "$LOG"

      SNAP1=$(gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --jq '{at: now | todate, rules_count: (.rules | length), bypass_actors}')

    Sub-step 4b — PUT #1: re-add the SAME actor (no-op at GitHub API). Body uses the EXACT shape from docs/v40-repo-config.md §7 sub-section (a):
      ADD_PAYLOAD='{"bypass_actors":[{"actor_id":254599900,"actor_type":"User","bypass_mode":"always"}]}'
      {
        echo ""
        echo "--- PUT #1: re-add actor 254599900 (idempotent — already present) ---"
        echo "Body: $ADD_PAYLOAD"
      } >> "$LOG"
      echo "$ADD_PAYLOAD" | gh api -X PUT /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --input - 2>&1 \
        | sed 's/Bearer [^"]*/Bearer REDACTED/g' >> "$LOG"

      SNAP2=$(gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --jq '{at: now | todate, rules_count: (.rules | length), bypass_actors}')
      {
        echo ""
        echo "--- Snapshot 2: post-PUT-1 GET ---"
      } >> "$LOG"
      gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 2>&1 | sed 's/Bearer [^"]*/Bearer REDACTED/g' >> "$LOG"

      # Hard assertion: bypass actor count is STILL 1 (no-op confirmed)
      [[ $(echo "$SNAP2" | jq '.bypass_actors | length') == "1" ]] \
        || { echo "FAIL: PUT #1 changed bypass_actors count from 1 to $(echo $SNAP2 | jq '.bypass_actors|length') — abort and rollback"; bash .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh; exit 1; }
      [[ $(echo "$SNAP2" | jq '.bypass_actors[0].actor_id') == "254599900" ]] \
        || { echo "FAIL: PUT #1 changed actor_id — abort and rollback"; bash .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh; exit 1; }
      [[ $(echo "$SNAP2" | jq '.rules | length') == "5" ]] \
        || { echo "FAIL: PUT #1 affected rules count — abort and rollback"; bash .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh; exit 1; }

    Sub-step 4c — PUT #2: re-PUT the same actor a second time (second idempotent no-op — proves the commands are deterministic, not stateful):
      {
        echo ""
        echo "--- PUT #2: re-add actor 254599900 again (second idempotent confirm) ---"
        echo "Body: $ADD_PAYLOAD"
      } >> "$LOG"
      echo "$ADD_PAYLOAD" | gh api -X PUT /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --input - 2>&1 \
        | sed 's/Bearer [^"]*/Bearer REDACTED/g' >> "$LOG"

      SNAP3=$(gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --jq '{at: now | todate, rules_count: (.rules | length), bypass_actors}')
      {
        echo ""
        echo "--- Snapshot 3: post-PUT-2 GET ---"
      } >> "$LOG"
      gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 2>&1 | sed 's/Bearer [^"]*/Bearer REDACTED/g' >> "$LOG"

      [[ $(echo "$SNAP3" | jq '.bypass_actors | length') == "1" ]] \
        || { echo "FAIL: PUT #2 final state has wrong bypass_actor count — rollback"; bash .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh; exit 1; }
      [[ $(echo "$SNAP3" | jq '.bypass_actors[0].actor_id') == "254599900" ]] \
        || { echo "FAIL: PUT #2 final state actor_id wrong — rollback"; bash .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh; exit 1; }
      [[ $(echo "$SNAP3" | jq '.rules | length') == "5" ]] \
        || { echo "FAIL: PUT #2 affected rules count — rollback"; bash .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh; exit 1; }

    Sub-step 4d — Compose break-glass-test.json snapshots file:
      jq -n --argjson s1 "$SNAP1" --argjson s2 "$SNAP2" --argjson s3 "$SNAP3" '{
        strategy: "idempotent re-add of existing actor 254599900 — no 0/2 actor window",
        snapshots: [$s1, $s2, $s3]
      }' > "$SNAPSHOTS"

      # Final integrity check — ALL three snapshots show identical bypass actor state:
      jq -e '.snapshots | length == 3 and (all(.[]; .bypass_actors | length == 1 and .[0].actor_id == 254599900) and all(.[]; .rules_count == 5))' "$SNAPSHOTS" \
        || { echo "FAIL: break-glass-test.json final integrity check failed — rollback"; bash .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh; exit 1; }

      # Bearer redaction verification — there must be ZERO non-redacted Bearer tokens in the log:
      ! grep -E 'Bearer [A-Za-z0-9_.-]{20,}' "$LOG" \
        || { echo "FAIL: break-glass-test.log contains an unredacted Bearer token — DO NOT COMMIT"; exit 1; }

    Sub-step 4e — Commit:
      git add .planning/phases/50-cleanup-04-readiness-gate/evidence/break-glass-test.json \
              .planning/phases/50-cleanup-04-readiness-gate/evidence/break-glass-test.log
      git commit -m "chore(50): break-glass live add-then-remove cycle (idempotent)"

    THEN the operator-approval checkpoint fires — operator inspects the snapshots + log before authorizing Task 05 (the irreversible bypass removal).
  </action>
  <what-built>
    Live test of the §7 break-glass PUT commands against ruleset 17086676. Three snapshots captured (pre, mid, post) all showing the same single bypass actor 254599900 — idempotent re-add confirmed working under current auth without ever creating a 0-actor or 2-actor window. The §7 runbook commands are now proven functional. Bearer tokens redacted from the log. evidence/break-glass-test.{json,log} committed.
  </what-built>
  <how-to-verify>
    1. Inspect the JSON snapshots:
       jq '.snapshots | map({bypass_count: (.bypass_actors | length), actor: .bypass_actors[0].actor_id, rules_count})' .planning/phases/50-cleanup-04-readiness-gate/evidence/break-glass-test.json
       Expected: 3 objects, all `{"bypass_count":1,"actor":254599900,"rules_count":5}`.
    2. Verify Bearer redaction:
       grep -E 'Bearer [A-Za-z0-9_.-]{20,}' .planning/phases/50-cleanup-04-readiness-gate/evidence/break-glass-test.log
       Expected: zero matches (only `Bearer REDACTED` strings should appear if any do).
    3. Confirm live ruleset is UNCHANGED from post-GATE-01 state:
       gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --jq '{rules: (.rules | length), bypass: (.bypass_actors | length), actor: .bypass_actors[0].actor_id}'
       Expected: `{"rules":5,"bypass":1,"actor":254599900}`.
    4. Confirm commit:
       git log -1 --pretty='%H %s'
       Expected: subject matches `chore(50): break-glass live add-then-remove cycle`.
    5. If anything is wrong — particularly if bypass_actors count is anything other than 1, or if Bearer tokens leaked into the log — DO NOT APPROVE. Run rollback.sh and reply with the issue.
  </how-to-verify>
  <resume-signal>Type "approved" once the 3 snapshots all show bypass_count=1/actor=254599900/rules_count=5, the log has zero unredacted Bearer tokens, and the live ruleset matches. Otherwise describe the issue.</resume-signal>
  <acceptance_criteria>
    - `evidence/break-glass-test.json` has `.snapshots | length == 3`.
    - All 3 snapshots show `bypass_actors | length == 1`, `bypass_actors[0].actor_id == 254599900`, and `rules_count == 5`.
    - `evidence/break-glass-test.log` exists, is non-empty, and contains at least two `gh api -X PUT` invocation transcripts.
    - `evidence/break-glass-test.log` contains ZERO unredacted Bearer tokens (grep `Bearer [A-Za-z0-9_.-]{20,}` returns empty).
    - Live ruleset GET re-pull shows 5 rules + 1 bypass actor (254599900) — identical to post-GATE-01 state.
    - Single atomic commit subject matches `^chore\(50\): break-glass live add-then-remove cycle`.
    - No PATCH verb in any executed `gh api` command (every mutation used PUT).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 05: GATE-02 PUT bypass_actors=[] (irreversible — removes the standing bypass)</name>
  <files>
    .planning/phases/50-cleanup-04-readiness-gate/evidence/gate-02-payload.json
    .planning/phases/50-cleanup-04-readiness-gate/evidence/post-gate-02-ruleset.json
  </files>
  <read_first>
    .planning/phases/50-cleanup-04-readiness-gate/50-CONTEXT.md (D-01, D-03 — second audit-log entry; partial-body PUT preserves rules)
    .planning/phases/50-cleanup-04-readiness-gate/50-RESEARCH.md (§"Pattern 2: Partial-body PUT", §"Code Examples → GATE-02 PUT — partial body", Pitfall 2 — whole-array replacement risk WITHIN sent keys)
    .planning/phases/50-cleanup-04-readiness-gate/evidence/break-glass-test.json (the proof that the recovery path works — required precondition for this task)
    .planning/phases/50-cleanup-04-readiness-gate/evidence/post-gate-01-ruleset.json (the source-of-truth pre-state for this PUT)
    docs/v40-repo-config.md §7 (the committed runbook — provides the recovery path if anything goes sideways after this PUT)
  </read_first>
  <action>
    AUTOMATED WORK FIRST. Lighter PUT body than GATE-01: send ONLY `{"bypass_actors": []}` and OMIT `rules` entirely. Live-verified semantics (RESEARCH §"Pattern 2"): omitted top-level keys are preserved, so the 5 rules from GATE-01 stay intact. Sending `rules: []` or any stale rules array would clobber GATE-01's required_status_checks rule (Pitfall 2 — whole-array replacement WITHIN sent keys).

    Commit message: `chore(50): GATE-02 PUT bypass_actors=[]`.

    Sub-step 5a — Precondition guard: break-glass test must have committed evidence:
      test -s .planning/phases/50-cleanup-04-readiness-gate/evidence/break-glass-test.json \
        || { echo "FAIL: Task 04 evidence missing — refuse to proceed (D-07 mandate)"; exit 1; }
      jq -e '.snapshots | length == 3' .planning/phases/50-cleanup-04-readiness-gate/evidence/break-glass-test.json \
        || { echo "FAIL: break-glass test did not capture 3 snapshots — refuse to proceed"; exit 1; }
      grep -qE '^## 7\. Break-Glass' docs/v40-repo-config.md \
        || { echo "FAIL: §7 break-glass procedure not committed — refuse to proceed (D-05 mandate)"; exit 1; }
      # Confirm live state still has the bypass actor (Task 04 left it in place):
      gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --jq '.bypass_actors | length' | grep -qx 1 \
        || { echo "FAIL: live ruleset bypass_actors != 1 before GATE-02 PUT — unexpected state; investigate"; exit 1; }

    Sub-step 5b — Construct the lighter PUT body (literal — no jq pipeline because we have no top-level merge to compute):
      PAYLOAD=".planning/phases/50-cleanup-04-readiness-gate/evidence/gate-02-payload.json"
      printf '%s\n' '{"bypass_actors":[]}' > "$PAYLOAD"
      # Local sanity:
      jq -e '. == {"bypass_actors":[]}' "$PAYLOAD" \
        || { echo "FAIL: payload shape wrong"; exit 1; }
      # Explicit assertion that the payload does NOT contain a `rules` key (Pitfall 2 prevention):
      jq -e 'has("rules") | not' "$PAYLOAD" \
        || { echo "FAIL: GATE-02 payload MUST NOT contain a rules key (would clobber GATE-01)"; exit 1; }

    Sub-step 5c — Apply GATE-02 PUT (LIVE MUTATION — this is the second and final audit-log entry; from this point forward, the operator has no standing bypass):
      gh api -X PUT /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --input "$PAYLOAD"
      gh api    /repos/tonyrowles/patent-cite-tool/rulesets/17086676 \
        > .planning/phases/50-cleanup-04-readiness-gate/evidence/post-gate-02-ruleset.json

      POST=".planning/phases/50-cleanup-04-readiness-gate/evidence/post-gate-02-ruleset.json"
      # Hard post-state assertions — rules preserved AND bypass_actors empty:
      jq -e '.rules | length == 5' "$POST" \
        || { echo "FAIL: post-state should have 5 rules — GATE-01 was clobbered! ROLLBACK NOW"; bash .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh; exit 1; }
      jq -e '.bypass_actors | length == 0' "$POST" \
        || { echo "FAIL: post-state bypass_actors should be empty — GATE-02 PUT did not take effect"; exit 1; }
      jq -e '[.rules[].type] | contains(["deletion","non_fast_forward","required_linear_history","pull_request","required_status_checks"])' "$POST" \
        || { echo "FAIL: post-state missing one of the 5 rule types"; bash .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh; exit 1; }
      jq -e '.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks | length == 2 and all(.[]; .integration_id == 15368)' "$POST" \
        || { echo "FAIL: post-state required_status_checks integrity broken"; bash .planning/phases/50-cleanup-04-readiness-gate/evidence/rollback.sh; exit 1; }
      jq -e '.enforcement == "active"' "$POST" \
        || { echo "FAIL: enforcement is no longer active"; exit 1; }

    Sub-step 5d — Commit:
      git add .planning/phases/50-cleanup-04-readiness-gate/evidence/gate-02-payload.json \
              .planning/phases/50-cleanup-04-readiness-gate/evidence/post-gate-02-ruleset.json
      git commit -m "chore(50): GATE-02 PUT bypass_actors=[]"

    THEN the operator-approval checkpoint fires — this is the last opportunity to abort before the final closure task.
  </action>
  <what-built>
    GATE-02 has landed on the live ruleset: bypass_actors is now empty array; 5 rules preserved byte-equivalent (deletion, non_fast_forward, required_linear_history, pull_request, required_status_checks-with-both-contexts-at-integration_id-15368). Two audit-log entries now exist on the ruleset (GATE-01 "rules array changed" + GATE-02 "bypass_actors emptied"). The operator @tonyrowles no longer has a standing bypass — any future emergency merge requires running the §7 break-glass procedure (which Task 04 proved works).
  </what-built>
  <how-to-verify>
    1. SC-2 verbatim assertion (ROADMAP line 206):
       gh api GET /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --jq '.bypass_actors'
       Expected: `[]`
    2. SC-1 verbatim assertion still holds (ROADMAP line 205):
       gh api GET /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --jq '.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context'
       Expected: two lines `verifier-gate` and `deps-update-gate`.
    3. Full integrity check:
       gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --jq '{rules_count: (.rules | length), bypass_count: (.bypass_actors | length), enforcement, types: [.rules[].type], contexts: [.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context] | sort, iids: [.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].integration_id] | unique}'
       Expected: `{"rules_count":5,"bypass_count":0,"enforcement":"active","types":[...all five...],"contexts":["deps-update-gate","verifier-gate"],"iids":[15368]}`.
    4. Confirm `current_user_can_bypass` field reflects no standing bypass:
       gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --jq '.current_user_can_bypass'
       Expected: `"never"` (was `"always"` before this task).
    5. If `rules_count != 5` OR `bypass_count != 0`, run rollback.sh and reply with the issue. The runtime jq assertions in the script SHOULD have caught these before commit; this is a final paranoia check.
  </how-to-verify>
  <resume-signal>Type "approved" once the live ruleset shows 5 rules, bypass_actors=[], enforcement=active, current_user_can_bypass=never. Otherwise describe the issue or type "rolled back".</resume-signal>
  <acceptance_criteria>
    - `evidence/gate-02-payload.json` deep-equals `{"bypass_actors":[]}` (no `rules` key, no other top-level keys).
    - `evidence/post-gate-02-ruleset.json` has `.rules | length == 5`, `.bypass_actors | length == 0`, `.enforcement == "active"`.
    - The 5 rules in post-state contain exactly the types: deletion, non_fast_forward, required_linear_history, pull_request, required_status_checks.
    - The required_status_checks rule still pins both contexts (`verifier-gate`, `deps-update-gate`) at integration_id=15368.
    - Live ruleset GET re-pull equals the post-state JSON.
    - `.current_user_can_bypass` field reads `"never"` (was `"always"` pre-PUT) — confirms the bypass is gone for the operator.
    - Single atomic commit subject matches `^chore\(50\): GATE-02 PUT bypass_actors=\[\]`.
    - Both ROADMAP SC-1 and SC-2 verbatim `gh api` commands return their expected outputs.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 06: Final ruleset snapshot + evidence/INDEX.md + 50-01-SUMMARY.md (closure)</name>
  <files>
    .planning/phases/50-cleanup-04-readiness-gate/evidence/final-ruleset.json
    .planning/phases/50-cleanup-04-readiness-gate/evidence/INDEX.md
    .planning/phases/50-cleanup-04-readiness-gate/50-01-SUMMARY.md
  </files>
  <read_first>
    .planning/phases/50-cleanup-04-readiness-gate/50-CONTEXT.md (D-15 task 6, D-17 — closure artifacts)
    .planning/phases/50-cleanup-04-readiness-gate/50-RESEARCH.md (§"Recommended Project Structure" — evidence inventory)
    .planning/ROADMAP.md §"Phase 50" lines 195-208 (4 SCs verbatim — must be quoted in SUMMARY.md)
    .planning/phases/49-push-v4-0-integration-pr/49-01-SUMMARY.md (pattern — closure narrative shape to follow)
    All Phase 50 evidence files written by Tasks 01-05 (to enumerate in INDEX.md)
    `npm test` — must pass before closure commit.
  </read_first>
  <action>
    All-automated. Three sub-steps; ONE commit at the end. Commit message: `chore(50): final ruleset snapshot + evidence/INDEX.md + SUMMARY`.

    Sub-step 6a — Capture final ruleset snapshot:
      gh api /repos/tonyrowles/patent-cite-tool/rulesets/17086676 \
        > .planning/phases/50-cleanup-04-readiness-gate/evidence/final-ruleset.json
      # Hard final-state assertions (the phase target):
      FINAL=".planning/phases/50-cleanup-04-readiness-gate/evidence/final-ruleset.json"
      jq -e '.rules | length == 5' "$FINAL"
      jq -e '.bypass_actors | length == 0' "$FINAL"
      jq -e '.enforcement == "active"' "$FINAL"
      jq -e '.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks | length == 2' "$FINAL"
      jq -e '[.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context] | sort == ["deps-update-gate","verifier-gate"]' "$FINAL"
      jq -e '.rules[] | select(.type=="required_status_checks") | all(.parameters.required_status_checks[]; .integration_id == 15368)' "$FINAL"

    Sub-step 6b — Run the full test suite to confirm the Vitest pins (D11, D12) remain green alongside everything else:
      npm test
      # Must exit 0. If any test fails, do NOT proceed to commit — investigate and surface failure.

    Sub-step 6c — Write evidence/INDEX.md as a structured per-file role map:
      Use a markdown table with columns | File | Produced By Task | Role / What It Proves |
      Enumerate every file in the evidence/ directory:
        - pre-patch-ruleset.json — Task 01 — Phase 49 baseline GET; rollback source.
        - rollback.sh — Task 01 — One-command recovery; re-PUTs pre-patch baseline.
        - integration-id-discovery.log — Task 02 — Records the gh api query and result (15368) for the GitHub Actions App ID.
        - gate-01-payload.json — Task 02 — PUT body that added the required_status_checks rule (5 rules total).
        - post-gate-01-ruleset.json — Task 02 — Live state after GATE-01 PUT; confirms append-not-replace.
        - gate-03-pr-state.json — Task 03 — Method A evidence: statusCheckRollup snapshot showing both contexts registered and not SUCCESS.
        - gate-03-merge-blocked.txt — Task 03 — Method B evidence: `gh pr merge --merge` (no --admin) stderr showing required-check rejection.
        - break-glass-test.json — Task 04 — 3 snapshots (pre, mid, post) of the idempotent re-add cycle proving §7 commands work.
        - break-glass-test.log — Task 04 — gh api transcripts with Bearer tokens redacted via `sed`.
        - gate-02-payload.json — Task 05 — Lighter PUT body `{"bypass_actors":[]}` (no `rules` key per partial-body merge).
        - post-gate-02-ruleset.json — Task 05 — Live state after GATE-02 PUT; confirms 5 rules preserved AND bypass_actors empty.
        - final-ruleset.json — Task 06 — Closure snapshot; matches phase target (5 rules / 0 bypass / enforcement=active).
        - INDEX.md — Task 06 — this file.
      Also include a "Verification commands" subsection with the 3 SC-verbatim commands from ROADMAP lines 205-208 and their expected outputs (verifier-gate + deps-update-gate; []; npm test green).

    Sub-step 6d — Write 50-01-SUMMARY.md following Phase 49 SUMMARY shape. Frontmatter MUST include `status: complete`, `plan: 01`, `requirements_addressed: [GATE-01, GATE-02, GATE-03]`, `evidence_dir: evidence/`, the deviation list (if any inline-fix commits were made beyond the planned 6), and a `provides` block for Phase 51 handoff (the live ruleset state Phase 51 must account for). Body sections:

      1. Phase summary (2-3 sentences naming the two PUTs + the wedged test PR cycle + the live break-glass test).
      2. SC closure table mapping each of the 4 ROADMAP SCs to its evidence file(s) and pass/fail status — all 4 must be PASS.
      3. Task-by-task narrative (one paragraph each, 6 tasks).
      4. Notable decisions actually exercised:
         - PUT (not PATCH) for all ruleset mutations — verb correction from RESEARCH.md
         - Lighter GATE-02 body relying on top-level merge preservation
         - Two audit-log entries via two-PUT cadence (D-01)
         - Idempotent same-actor re-add for break-glass test (D-08)
         - Vitest jobid pins shipped in SAME commit as GATE-01 PUT (D-13)
         - Renumber of existing §7 → §8 in docs/v40-repo-config.md to make room for the new break-glass §7
      5. Trust-invariant gap closure: explicitly state which v4.0 deferred items this phase resolves (the 2 STATE.md deferred tech_debt entries: `bypass_actors=1 on ruleset 17086676` and `required_status_checks rule absent`).
      6. Phase 51 handoff: a 3-bullet "what Phase 51 must know" — (a) the ruleset now enforces verifier-gate + deps-update-gate, (b) no standing bypass — UAT setup must NOT depend on admin-bypass for any throwaway PR, (c) §7 break-glass procedure is the documented emergency path.
      7. Deviations (if any) — the executor lists any inline fixes that were not in the planned 6 tasks. If none, state "None — phase executed exactly as planned."

    Sub-step 6e — Commit closure artifacts:
      git add .planning/phases/50-cleanup-04-readiness-gate/evidence/final-ruleset.json \
              .planning/phases/50-cleanup-04-readiness-gate/evidence/INDEX.md \
              .planning/phases/50-cleanup-04-readiness-gate/50-01-SUMMARY.md
      git commit -m "chore(50): final ruleset snapshot + evidence/INDEX.md + SUMMARY"
  </action>
  <verify>
    <automated>
      jq -e '.rules | length == 5 and .bypass_actors | length == 0 and .enforcement == "active"' .planning/phases/50-cleanup-04-readiness-gate/evidence/final-ruleset.json &&
      test -s .planning/phases/50-cleanup-04-readiness-gate/evidence/INDEX.md &&
      grep -q 'pre-patch-ruleset.json' .planning/phases/50-cleanup-04-readiness-gate/evidence/INDEX.md &&
      grep -q 'final-ruleset.json' .planning/phases/50-cleanup-04-readiness-gate/evidence/INDEX.md &&
      grep -q 'gate-03-merge-blocked.txt' .planning/phases/50-cleanup-04-readiness-gate/evidence/INDEX.md &&
      grep -q 'break-glass-test.json' .planning/phases/50-cleanup-04-readiness-gate/evidence/INDEX.md &&
      grep -qE '^status:\s*complete' .planning/phases/50-cleanup-04-readiness-gate/50-01-SUMMARY.md &&
      grep -q 'GATE-01' .planning/phases/50-cleanup-04-readiness-gate/50-01-SUMMARY.md &&
      grep -q 'GATE-02' .planning/phases/50-cleanup-04-readiness-gate/50-01-SUMMARY.md &&
      grep -q 'GATE-03' .planning/phases/50-cleanup-04-readiness-gate/50-01-SUMMARY.md &&
      npm test &&
      git log -1 --pretty=%s | grep -qE '^chore\(50\): final ruleset snapshot'
    </automated>
  </verify>
  <acceptance_criteria>
    - `evidence/final-ruleset.json` has `.rules | length == 5`, `.bypass_actors | length == 0`, `.enforcement == "active"`, and the required_status_checks rule with both contexts pinned to integration_id=15368.
    - `evidence/INDEX.md` exists, is non-empty, and references every evidence file written by Tasks 01-05 by name (12+ files).
    - `evidence/INDEX.md` contains a Verification subsection quoting the 3 SC-verbatim commands from ROADMAP §"Phase 50" with their expected outputs.
    - `50-01-SUMMARY.md` frontmatter has `status: complete`, `phase: 50-cleanup-04-readiness-gate`, `plan: 01`, `requirements_addressed: [GATE-01, GATE-02, GATE-03]`.
    - `50-01-SUMMARY.md` body contains an SC closure table mapping all 4 ROADMAP SCs to PASS with evidence file references.
    - `50-01-SUMMARY.md` body explicitly states which 2 v4.0 STATE.md deferred tech_debt entries this phase closes.
    - `50-01-SUMMARY.md` body contains a Phase 51 handoff section.
    - `npm test` exits 0 with all tests (including D11, D12 from Task 02) green.
    - Single atomic commit subject matches `^chore\(50\): final ruleset snapshot`.
    - Working tree clean after commit.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator CLI → GitHub REST API | `gh api` requests cross the network with operator's `tonyrowles` token (scopes: repo, workflow, read:org, gist); TLS handled by gh. |
| GitHub ruleset state → GitHub merge engine | The ruleset's required_status_checks rule + empty bypass_actors are the trust boundary between a PR's check state and whether `--merge` is permitted. This boundary is what GATE-03 actively probes. |
| Workflow YAML jobid → check-run.name | The jobid `verifier-gate:` in YAML produces a check-run with `.name == "verifier-gate"` ONLY if no `name:` override is added later. The Vitest static-grep test (D-13) defends this boundary. |
| docs/v40-repo-config.md §7 runbook → live recovery path | Operator manually executes the §7 commands during a break-glass scenario. Task 04 live-tests this boundary before Task 05 removes the bypass net. |

## STRIDE Threat Register

Phase 50 IS the threat model — every task closes a specific trust-invariant gap or mitigates an operational risk introduced by the closure itself. All threats are HIGH severity unless noted MEDIUM.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-01 | Tampering | v40-verifier-gate.yml verifier-gate job — currently unenforced as a required check; PRs that fail the affected-case re-verification can still merge | HIGH | mitigate | Task 02 GATE-01 PUT adds `required_status_checks` rule with context `verifier-gate` + integration_id=15368; verified post-PUT via jq assertion + ROADMAP SC-1 verbatim command. |
| T-50-02 | Tampering | v40-deps-update.yml deps-update-gate job — currently unenforced; dep bumps that break smoke + regression can still merge | HIGH | mitigate | Task 02 GATE-01 PUT adds `required_status_checks` rule with context `deps-update-gate` + integration_id=15368; ships in same PUT as T-50-01 mitigation. |
| T-50-03 | Elevation of Privilege | Standing bypass actor (actor_id=254599900, bypass_mode=always) — operator can self-merge any PR without satisfying required checks | HIGH | mitigate | Task 05 GATE-02 PUT sets `bypass_actors: []`; verified via ROADMAP SC-2 verbatim command + post-state assertion `.current_user_can_bypass == "never"`. |
| T-50-04 | Tampering | GATE-01 PUT body could drop the 4 existing rules silently if constructed wrong (whole-array replacement within sent `rules` key) | HIGH | mitigate | Task 02 sub-step 2b uses jq `(.rules + [<new>])` pattern; local validation `length == 5` BEFORE sending; post-PUT GET re-pull asserts 5 rules; rollback.sh re-PUTs pre-patch state on any assertion failure. RESEARCH Pitfall 2. |
| T-50-05 | Tampering | GATE-02 PUT body could drop the 5 rules if it accidentally includes `rules: []` or a stale rules array | HIGH | mitigate | Task 05 sub-step 5b explicitly asserts `jq -e 'has("rules") | not'` on the payload BEFORE sending. The PUT body is the literal `{"bypass_actors":[]}` — partial-body merge preserves omitted top-level keys (live-verified). Post-PUT GET asserts 5 rules remain. RESEARCH §"Pattern 2". |
| T-50-06 | Tampering | Wrong or omitted integration_id → ANY provider's check named "verifier-gate" satisfies the rule (spoofing surface) | HIGH | mitigate | Task 02 sub-step 2a discovers integration_id via live `gh api /commits/<sha>/check-runs --jq '.app.id'` and hard-gates on `== 15368`. Local assertion in 2b confirms payload pins 15368 on every context. RESEARCH Pitfall 4. |
| T-50-07 | Tampering | Failed GATE-03 test PR could merge accidentally if `gh pr merge --admin` is used | HIGH | accept (already mitigated structurally) | Task 03 sub-step 3e uses `gh pr merge --merge` (NO `--admin`); guarded by `if ...; then FAIL; fi` shape that REJECTS a successful merge. The probe file is on `.planning/scratch/` which never touches user-facing code. PR is force-CLOSED after assertion. |
| T-50-08 | Repudiation | Break-glass procedure used informally with no audit trail | MEDIUM | mitigate | §7 runbook (Task 01) mandates `docs/incidents/<YYYY-MM-DD>-*.md` log file BEFORE re-adding bypass; 1-hour time-box; documented as manual procedure with no automation. |
| T-50-09 | Information Disclosure | Bearer tokens leaked via gh api transcripts in `break-glass-test.log` | MEDIUM | mitigate | Task 04 sub-step 4a applies `sed 's/Bearer [^"]*/Bearer REDACTED/g'` filter to every gh api transcript before append; sub-step 4d verifies via grep that ZERO unredacted Bearer tokens remain before commit. D-09. |
| T-50-10 | Tampering | Misconfigured GATE-01 PUT could brick the ruleset and there is no rollback path | HIGH | mitigate | Task 01 captures `pre-patch-ruleset.json` BEFORE any mutation; ships `rollback.sh` that re-PUTs the baseline in one command; every subsequent task's failure paths call `bash evidence/rollback.sh` automatically. RESEARCH Pitfall 1 + §"Validation Architecture → Rollback Trigger". |
| T-50-11 | Tampering | A future workflow change could rename the `verifier-gate:` jobid OR add a `name:` override → check-run name changes → required-check context no longer matches → silent enforcement loss | MEDIUM | mitigate | Task 02 sub-step 2d adds Vitest D11/D12 static-grep tests pinning the exact 2-space-indented jobid declaration. `npm test` catches drift on every commit. Ships in SAME commit as the PUT (D-13). Pitfall 7. |
| T-50-12 | Spoofing | Failed test PR cycle could indicate enforcement misconfiguration AFTER bypass is already removed → can't recover without break-glass | MEDIUM | mitigate (by sequencing) | GATE-03 (Task 03) is WEDGED between GATE-01 (Task 02) and GATE-02 (Task 05); the bypass is STILL PRESENT during GATE-03, so a failed enforcement test still leaves emergency recovery available. D-01 two-step cadence is the structural mitigation. |

</threat_model>

<verification>
Phase-level verification (all 4 SCs must pass, sourced verbatim from ROADMAP.md lines 205-208):

1. **SC-1 (GATE-01):** `gh api GET /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --jq '.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context'` returns both `verifier-gate` and `deps-update-gate`.
2. **SC-2 (GATE-02):** `gh api GET /repos/tonyrowles/patent-cite-tool/rulesets/17086676 --jq '.bypass_actors'` returns `[]`.
3. **SC-3 (GATE-03):** Test PR opened after GATE-01 cannot merge until both required checks report success (proven by `evidence/gate-03-merge-blocked.txt` + `evidence/gate-03-pr-state.json` two-pronged assertion); break-glass PUT command committed in `docs/v40-repo-config.md` §7 AND confirmed functional via `evidence/break-glass-test.json` BEFORE GATE-02 (Task 05) lands.
4. **SC-4 (Vitest pin):** `npm test` exits 0 with D11/D12 in `tests/unit/v40-verifier-gate-doc.test.js` pinning `verifier-gate:` and `deps-update-gate:` jobid declarations in the workflow YAML; the 2 new `it()` blocks ship in the SAME commit as the GATE-01 PUT (verifiable via `git show --name-only` on the GATE-01 commit).
</verification>

<success_criteria>
Plan is complete when ALL six tasks have committed their artifacts AND:
- Live ruleset 17086676 state: 5 rules, 0 bypass_actors, enforcement=active, current_user_can_bypass=never, required_status_checks rule pins both `verifier-gate` + `deps-update-gate` at integration_id=15368.
- `npm test` exits 0 (D11 + D12 green alongside D1-D10).
- All 11+ evidence files exist in `.planning/phases/50-cleanup-04-readiness-gate/evidence/` and are indexed in `evidence/INDEX.md`.
- `50-01-SUMMARY.md` documents all 4 SCs as PASS with evidence pointers.
- `docs/v40-repo-config.md` §7 break-glass procedure committed (existing §7 renumbered to §8).
- Git log shows 6 atomic commits (one per task) on `main`, all with `chore(50):` prefix.
- No `gh api -X PATCH` ever appeared in any executed command (only `-X PUT` for ruleset mutations).
- Phase 49 deferred tech_debt entries (`bypass_actors=1 on ruleset 17086676`, `required_status_checks rule absent`) are now closeable in STATE.md.
</success_criteria>

<output>
Create `.planning/phases/50-cleanup-04-readiness-gate/50-01-SUMMARY.md` when done (Task 06 produces this).
</output>
