---
phase: 53-auto-fix-partial-verified-semantics
plan: 01
type: execute
wave: 1
depends_on: [44-01]
files_modified:
  - scripts/auto-fix-promote.mjs
  - tests/unit/auto-fix-promote-gate.test.js
  - .github/workflows/v40-verifier-gate.yml
  - .github/workflows/v40-auto-promote.yml
autonomous: true
requirements: [PARTIAL-01, PARTIAL-02, PARTIAL-03, PARTIAL-04]
requirements_addressed: [PARTIAL-01, PARTIAL-02, PARTIAL-03, PARTIAL-04]
tags: [trust-boundary, partial-verified, gate-state-machine, vitest-pin]

must_haves:
  truths:
    # Each truth tagged with the D-NN decision(s) it operationalizes —
    # full D-01..D-22 coverage for the decision-coverage-plan gate.
    - "[D-01] assertPartialGate accepts exactly { prLabels, merged, sourceIssueLabels, passingCases } and is exported from scripts/auto-fix-promote.mjs"
    - "[D-02] assertPartialGate enforces 3 legs: (Leg 1) prLabels contains 'auto-fix:partial-verified', (Leg 2) merged === true, (Leg 3) sourceIssueLabels contains 'triage'"
    - "[D-03] assertPartialGate throws PARTIAL_GATE_FAILED: <leg> — <details> on the first failing leg (diagnostic uniformity with TRIPLE_GATE_FAILED)"
    - "[D-04] assertPartialGate returns { passingCaseIds: string[] } on success; throws PARTIAL_GATE_FAILED: passingCases — empty (no cases to promote) when passingCases is empty/non-array; never calls runPromote"
    - "[D-05] assertTripleGate body (scripts/auto-fix-promote.mjs lines 67-81) is BYTE-UNCHANGED — git diff HEAD shows only additions in the file, no edits to assertTripleGate's body"
    - "[D-06] runPartialPromote(passingCaseIds, { dryRun }) is a NEW exported function in scripts/auto-fix-promote.mjs that loops the array and invokes promoteFromQuarantine({ id, confirm: true, _skipCiGuard: false }) per case"
    - "[D-07] runPartialPromote is atomic-per-case: on first promoteFromQuarantine failure it emits PARTIAL_PROMOTE_HALTED: case=<id>, reason=<msg> and returns { promoted, halted: true, error } without rolling back already-promoted cases"
    - "[D-08] runPartialPromote leaves failing cases (not in passingCaseIds) quarantined; workflow extension comments on each failing source issue per D-08 (workflow-side, satisfied by Task 3 PR-comment hook)"
    - "[D-09] v40-verifier-gate.yml ready-flip job gains a conditional partial-label step that fires only when affected_cases.length >= 5, passing_cases.length / affected_cases.length >= 0.80, passing_cases.length < affected_cases.length, AND no failing case has errorClass == 'FLAKE'"
    - "[D-10] v40-verifier-gate.yml existing 'Apply auto-fix:verified label' step (lines 472-482) is BYTE-UNCHANGED — verified by git diff against HEAD"
    - "[D-11] PARTIAL_THRESHOLD = 0.80 is the single source of truth; declared as a top-of-file const in scripts/auto-fix-promote.mjs AND used numerically (>= 0.80) in v40-verifier-gate.yml's partial-label step"
    - "[D-12] Partial-label step abstains entirely when affected_cases.length < 5 (no label, falls back to existing full-pass / full-fail)"
    - "[D-13] FLAKE-masquerade mitigation: if ANY failing case has errorClass == 'FLAKE', the partial-label step does NOT fire (1 flake cannot mask 4 real fails)"
    - "[D-14] v40-auto-promote.yml job-level if: filter is widened to OR-branch containing both 'auto-fix:verified' AND 'auto-fix:partial-verified'; existing verified path is preserved verbatim"
    - "[D-15] main() in scripts/auto-fix-promote.mjs branches on prLabels: 'auto-fix:verified' takes existing assertTripleGate+runPromote path UNCHANGED; 'auto-fix:partial-verified' takes new assertPartialGate+runPartialPromote path; neither label present exits 0 defensively"
    - "[D-16] v40-auto-promote.yml passes the passing-cases CSV to the script via a new --passing-cases CLI arg sourced from a ready-flip workflow output"
    - "[D-17] tests/unit/auto-fix-promote-gate.test.js is EXTENDED in place (no new test file): PARTIAL-04 assertion lives inside the existing describe('assertTripleGate', ...) block; new top-level describe('assertPartialGate', ...) holds happy-path + 3 leg-fail + empty-passingCases tests"
    - "[D-18] PARTIAL-04 Vitest pin ships in the SAME commit as PARTIAL-01 source (commit (a) per D-20); the label string 'auto-fix:partial-verified' is FIRST DECLARED in commit (a) (as a test fixture and source-side reference)"
    - "[D-19] No new test framework, no new npm dependency; the Vitest 1134-test suite is the single home for new unit assertions"
    - "[D-20] 3 atomic commits in LOCKED order: (a) PARTIAL-01 + PARTIAL-04 → (b) PARTIAL-02 → (c) PARTIAL-03; each commit touches exactly the files listed in its task's <files>"
    - "[D-21] Commit message pattern is feat(53): <one-line> verbatim for all three commits"
    - "[D-22] gsd-plan-checker is mandatory for Phase 53 (STATE blocker); the checker MUST verify D-05 byte-unchanged, no '_skipCiGuard: true' in partial path, D-18 same-commit, all 4 PARTIAL-* in requirements_addressed, and D-20 commit-order"
    - "PARTIAL-01: 'auto-fix-promote.mjs exports assertPartialGate; it is a SEPARATE entry point; does NOT call runPromote({_skipCiGuard:true}); assertTripleGate body byte-unchanged' — verifiable by grep + git diff"
    - "PARTIAL-02: 'v40-verifier-gate.yml ready-flip emits auto-fix:partial-verified when >=4/5 of affected cases pass, FLAKE-aware; full-pass auto-fix:verified path byte-unchanged' — verifiable by grep + yq + git diff"
    - "PARTIAL-03: 'v40-auto-promote.yml job-level if: filter accepts both auto-fix:verified and auto-fix:partial-verified; partial path promotes only the PASSING case subset; failing cases stay in quarantine' — verifiable by grep on workflow + script main()"
    - "PARTIAL-04: 'Vitest assertion proving assertTripleGate THROWS when fed only auto-fix:partial-verified (without auto-fix:verified); test lives in same commit as the new label' — verifiable by npx vitest run + git log --follow"
  artifacts:
    - path: scripts/auto-fix-promote.mjs
      provides: "assertPartialGate (new export), runPartialPromote (new export), PARTIAL_THRESHOLD const, main() label branch — all additive; assertTripleGate body unchanged"
      contains: "export function assertPartialGate"
      also_contains: "export async function runPartialPromote"
      also_contains: "const PARTIAL_THRESHOLD = 0.80"
    - path: tests/unit/auto-fix-promote-gate.test.js
      provides: "PARTIAL-04 trust-invariant assertion inside existing assertTripleGate describe + new assertPartialGate describe block"
      contains: "auto-fix:partial-verified"
      also_contains: "describe('assertPartialGate"
    - path: .github/workflows/v40-verifier-gate.yml
      provides: "ready-flip conditional step emitting auto-fix:partial-verified label (>=4/5, FLAKE-aware); verifier-gate per-case outputs (passing/failing CSV) for downstream consumption"
      contains: "auto-fix:partial-verified"
      also_contains: "Conditional partial-verified label"
    - path: .github/workflows/v40-auto-promote.yml
      provides: "widened OR-branch if: filter accepting both verified labels; --passing-cases CLI arg wiring to the script"
      contains: "auto-fix:partial-verified"
      also_contains: "--passing-cases"
  key_links:
    - from: "scripts/auto-fix-promote.mjs assertPartialGate"
      to: "tests/unit/auto-fix-promote-gate.test.js describe('assertPartialGate')"
      via: "import { assertPartialGate } from '../../scripts/auto-fix-promote.mjs'"
      pattern: "import.*assertPartialGate.*auto-fix-promote\\.mjs"
    - from: "scripts/auto-fix-promote.mjs runPartialPromote"
      to: "scripts/promote-from-quarantine.mjs promoteFromQuarantine"
      via: "per-case invocation with _skipCiGuard: false"
      pattern: "promoteFromQuarantine.*_skipCiGuard:\\s*false"
    - from: ".github/workflows/v40-verifier-gate.yml ready-flip"
      to: ".github/workflows/v40-auto-promote.yml job-level if:"
      via: "auto-fix:partial-verified label on PR (label is the message)"
      pattern: "auto-fix:partial-verified"
    - from: ".github/workflows/v40-auto-promote.yml --passing-cases arg"
      to: "scripts/auto-fix-promote.mjs main() → assertPartialGate({ passingCases })"
      via: "--passing-cases CSV → parseArgv → assertPartialGate"
      pattern: "--passing-cases"
    - from: "tests/unit/auto-fix-promote-gate.test.js PARTIAL-04 assertion"
      to: "scripts/auto-fix-promote.mjs assertTripleGate (UNMODIFIED behavior)"
      via: "expect(() => assertTripleGate({ prLabels: ['auto-fix:partial-verified'], ... })).toThrow(/TRIPLE_GATE_FAILED: prLabels/)"
      pattern: "auto-fix:partial-verified.*TRIPLE_GATE_FAILED|TRIPLE_GATE_FAILED.*auto-fix:partial-verified"
---

<objective>
Implement `auto-fix:partial-verified` as a SECOND state-machine entry point in the v4.0 auto-promote pipeline, hardening (not eroding) the `assertTripleGate` trust invariant.

Purpose: When the verifier-gate sees ≥4/5 of affected cases pass (not all-or-nothing) and no FLAKE masquerade is present, the pipeline must be able to promote ONLY the passing subset while keeping the failing cases quarantined — without weakening the `_skipCiGuard:true` triple-gate that protects legal-filing citation code from auto-promotion of unverified patches.

Output:
- `scripts/auto-fix-promote.mjs` gains TWO new exports (`assertPartialGate`, `runPartialPromote`) + a top-of-file `PARTIAL_THRESHOLD = 0.80` const + a label-branch in `main()`. `assertTripleGate`'s body is BYTE-UNCHANGED.
- `tests/unit/auto-fix-promote-gate.test.js` is EXTENDED with a trust-invariant assertion (PARTIAL-04: `assertTripleGate` throws on `auto-fix:partial-verified`) inside the existing `describe('assertTripleGate')` block + a new `describe('assertPartialGate')` block.
- `.github/workflows/v40-verifier-gate.yml` `ready-flip` job gains a conditional partial-label step (≥4/5, FLAKE-aware, abstain <5 cases); the existing `auto-fix:verified` label-add step is BYTE-UNCHANGED.
- `.github/workflows/v40-auto-promote.yml` widens its job-level `if:` filter via OR-branch to accept both `auto-fix:verified` AND `auto-fix:partial-verified`; passes `--passing-cases` CSV arg.

3 atomic commits in LOCKED D-20 order: (a) PARTIAL-01 + PARTIAL-04 (script + test, SAME commit per D-18) → (b) PARTIAL-02 (verifier-gate YAML) → (c) PARTIAL-03 (auto-promote YAML + script `main()` branch).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-auto-fix-partial-verified-semantics/53-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@scripts/auto-fix-promote.mjs
@scripts/promote-from-quarantine.mjs
@tests/unit/auto-fix-promote-gate.test.js
@.github/workflows/v40-verifier-gate.yml
@.github/workflows/v40-auto-promote.yml

<interfaces>
<!-- Existing contracts the executor will EXTEND. Source-verified excerpts only. -->

From scripts/auto-fix-promote.mjs (lines 67-81 — BYTE-UNCHANGED per D-05):

  export function assertTripleGate({ prLabels, merged, sourceIssueLabels } = {}) {
    if (!Array.isArray(prLabels) || !prLabels.includes('auto-fix:verified')) {
      throw new Error("TRIPLE_GATE_FAILED: prLabels — missing 'auto-fix:verified'");
    }
    if (merged !== true) {
      throw new Error('TRIPLE_GATE_FAILED: merged — pull request not merged');
    }
    if (!Array.isArray(sourceIssueLabels) || !sourceIssueLabels.includes('triage')) {
      throw new Error("TRIPLE_GATE_FAILED: sourceIssueLabels — source issue missing 'triage'");
    }
  }

From scripts/auto-fix-promote.mjs (lines 54-58 — import policy boundary):

  import fs from 'node:fs';
  import path from 'node:path';
  import { fileURLToPath } from 'node:url';
  import { runPromote } from './promote-from-quarantine.mjs';

  // runPartialPromote MUST call promoteFromQuarantine from
  // ./promote-from-quarantine.mjs (NOT runPromote). promoteFromQuarantine
  // is the per-case primitive; runPromote is the all-cases driver that
  // calls promoteFromQuarantine internally with the _skipCiGuard:true the
  // existing Phase 35 design reserves for the full-pass triple-gate path.

From scripts/promote-from-quarantine.mjs (existing export to be consumed):

  export async function promoteFromQuarantine({ id, confirm, _skipCiGuard });
  // runPartialPromote calls with { id: caseId, confirm: true, _skipCiGuard: false }
  // per D-06: partial promotes go through normal triple-gate semantics when
  // re-evaluated; _skipCiGuard:true is reserved for the full-pass path ONLY.

From tests/unit/auto-fix-promote-gate.test.js (existing describe block to extend):

  describe('assertTripleGate (Phase 44)', () => {
    it("T1 — rejects when 'auto-fix:verified' label missing", () => { ... });
    it('T2 — rejects when merged !== true (PR closed unmerged)', () => { ... });
    it("T3 — rejects when source-issue lacks 'triage' label", () => { ... });
    it('T4 — happy path: returns void when all three legs satisfied', () => { ... });
  });

  // PARTIAL-04 (D-17): ADD a new it() inside this same describe block
  // asserting assertTripleGate throws when prLabels contains ONLY
  // 'auto-fix:partial-verified' (without 'auto-fix:verified'). This is the
  // trust-invariant boundary assertion that ships in the SAME commit as
  // assertPartialGate (D-18).

From .github/workflows/v40-verifier-gate.yml (lines 472-482 — BYTE-UNCHANGED per D-10):

  - name: Apply auto-fix:verified label
    if: steps.scope.outputs.active == 'true' || github.event_name != 'pull_request'
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      PR_NUMBER: ${{ github.event.pull_request.number }}
    run: gh pr edit "$PR_NUMBER" --add-label "auto-fix:verified"

  # PARTIAL-02 (D-09): INSERT a NEW conditional step AFTER this step. The
  # existing step's YAML bytes are unchanged.

From .github/workflows/v40-auto-promote.yml (line 67 — to be widened per D-14):

  if: github.event.pull_request.merged == true && contains(github.event.pull_request.labels.*.name, 'auto-fix:verified')

  # PARTIAL-03 (D-14): WIDEN to OR-branch (preserves verified path verbatim):
  # if: |
  #   github.event.pull_request.merged == true && (
  #     contains(github.event.pull_request.labels.*.name, 'auto-fix:verified') ||
  #     contains(github.event.pull_request.labels.*.name, 'auto-fix:partial-verified')
  #   )

From .github/workflows/v40-verifier-gate.yml verifier-gate per-case loop (lines 277-308 — Task 2 context):

  # Existing structure: FAILED_CASES collected during the per-case 3x loop;
  # on non-empty FAILED_CASES the job exits 1 (hard fail).
  # PARTIAL-02 needs PASSING_CASES + FAILING_CASES surfaced as job outputs
  # so ready-flip's conditional partial step can consume them. Without job
  # outputs, ready-flip has no data to compute the >=4/5 threshold.
  # Task 2 MUST emit these outputs without changing the full-pass success
  # contract (FAILED_CASES empty → existing behavior preserved; ready-flip
  # adds the partial branch on top).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PARTIAL-01 + PARTIAL-04 — assertPartialGate + runPartialPromote + Vitest trust-invariant pin (single atomic commit)</name>
  <files>scripts/auto-fix-promote.mjs, tests/unit/auto-fix-promote-gate.test.js</files>
  <read_first>
    - scripts/auto-fix-promote.mjs (full file — assertTripleGate at lines 67-81 must remain byte-unchanged; main() at lines 194-261 stays unchanged in THIS commit; the label branch is added in Task 3 / commit (c) per D-20)
    - tests/unit/auto-fix-promote-gate.test.js (full file — extend in place per D-17; no new test file)
    - scripts/promote-from-quarantine.mjs (verify `promoteFromQuarantine` exists as a per-case export; confirm runPartialPromote's import target)
    - .planning/phases/53-auto-fix-partial-verified-semantics/53-CONTEXT.md (D-01 through D-08 + D-17 + D-18 + D-19)
    - .planning/research/PITFALLS.md Pitfall 4 (LOAD-BEARING — concrete anti-pattern and correct-pattern code blocks)
  </read_first>
  <behavior>
    # PARTIAL_THRESHOLD constant
    - The const `PARTIAL_THRESHOLD = 0.80` appears in scripts/auto-fix-promote.mjs at top-of-file (between the existing import block ending line 58 and the assertTripleGate definition starting line 67). It is exported.

    # assertPartialGate behavior (D-01..D-04)
    - assertPartialGate({ prLabels: ['auto-fix:partial-verified'], merged: true, sourceIssueLabels: ['triage'], passingCases: ['c1'] })
      → returns { passingCaseIds: ['c1'] } (no throw)
    - assertPartialGate({ prLabels: ['auto-fix:partial-verified'], merged: true, sourceIssueLabels: ['triage'], passingCases: ['c1', 'c2'] })
      → returns { passingCaseIds: ['c1', 'c2'] }
    - assertPartialGate({ prLabels: ['triage'], merged: true, sourceIssueLabels: ['triage'], passingCases: ['c1'] })
      → throws Error matching /PARTIAL_GATE_FAILED: prLabels — missing 'auto-fix:partial-verified'/
    - assertPartialGate({ prLabels: ['auto-fix:partial-verified'], merged: false, sourceIssueLabels: ['triage'], passingCases: ['c1'] })
      → throws Error matching /PARTIAL_GATE_FAILED: merged — pull request not merged/
    - assertPartialGate({ prLabels: ['auto-fix:partial-verified'], merged: true, sourceIssueLabels: ['bug'], passingCases: ['c1'] })
      → throws Error matching /PARTIAL_GATE_FAILED: sourceIssueLabels — source issue missing 'triage'/
    - assertPartialGate({ prLabels: ['auto-fix:partial-verified'], merged: true, sourceIssueLabels: ['triage'], passingCases: [] })
      → throws Error matching /PARTIAL_GATE_FAILED: passingCases — empty \(no cases to promote\)/
    - assertPartialGate({ prLabels: ['auto-fix:partial-verified'], merged: true, sourceIssueLabels: ['triage'] })
      (passingCases omitted) → throws same /PARTIAL_GATE_FAILED: passingCases — empty/ (non-array treated as empty)
    - assertPartialGate({ prLabels: ['auto-fix:partial-verified'], merged: true, sourceIssueLabels: ['triage'], passingCases: ['c1', 42] })
      → throws Error matching /PARTIAL_GATE_FAILED: passingCases — non-string entry/ (defensive type check)

    # PARTIAL-04 trust-invariant boundary (D-18)
    - assertTripleGate({ prLabels: ['auto-fix:partial-verified'], merged: true, sourceIssueLabels: ['triage'] })
      → MUST throw /TRIPLE_GATE_FAILED: prLabels — missing 'auto-fix:verified'/
      (i.e., assertTripleGate continues to require 'auto-fix:verified' verbatim; presence of partial-verified does not satisfy Leg 1)
    - assertTripleGate({ prLabels: ['auto-fix:verified', 'auto-fix:partial-verified'], merged: true, sourceIssueLabels: ['triage'] })
      → does NOT throw (verified takes precedence; this co-presence case is documented in the test)

    # runPartialPromote behavior (D-06..D-08)
    - runPartialPromote(['c1', 'c2']) when promoteFromQuarantine succeeds for both
      → returns { promoted: ['c1', 'c2'], halted: false } (await result; no error key)
    - runPartialPromote(['c1', 'c2']) when promoteFromQuarantine throws on c2
      → returns { promoted: ['c1'], halted: true, error: 'PARTIAL_PROMOTE_HALTED: case=c2, reason=<msg>' }
        AND emits the same string via console.error
    - runPartialPromote([]) → throws /PARTIAL_GATE_FAILED: passingCases — empty/ (delegates the same guard, no silent no-op)
    - runPartialPromote(['c1'], { dryRun: true }) → returns { promoted: ['c1'], halted: false, dryRun: true } WITHOUT calling promoteFromQuarantine (Vitest mockability per Claude's Discretion)
    - At NO point does runPartialPromote pass `_skipCiGuard: true`. Every promoteFromQuarantine call uses `{ id, confirm: true, _skipCiGuard: false }`.
  </behavior>
  <action>
    Edit scripts/auto-fix-promote.mjs additively (NO modifications to assertTripleGate body lines 67-81, NO modifications to parseSourceIssue, parseArgv, main, or the isMain guard — those stay byte-for-byte unchanged in this commit). Add the following AFTER the import block (line 58) and BEFORE the assertTripleGate definition (line 67):

    1. Top-of-file constant block (with a leading comment block matching project style):
       - `export const PARTIAL_LABEL = 'auto-fix:partial-verified';` — first declaration of the label string per D-18; consumed by main() in commit (c), by the test in this same commit, and (as a literal) by the workflows in commits (b) and (c). The export makes it greppable as a symbol, not just a string literal.
       - `export const PARTIAL_THRESHOLD = 0.80;` — single source of truth for the 4/5 ratio per D-11.

    Then add AFTER assertTripleGate (after line 81, before the parseSourceIssue comment block at line 83) two new exported functions:

    2. `export function assertPartialGate({ prLabels, merged, sourceIssueLabels, passingCases } = {})`:
       - Implements the 3-leg structure mirroring assertTripleGate per D-02. Leg 1 checks prLabels includes PARTIAL_LABEL (NOT 'auto-fix:verified' — that is assertTripleGate's job per D-05/PARTIAL-04). Leg 2 checks merged === true. Leg 3 checks sourceIssueLabels includes 'triage'.
       - Error messages use the PARTIAL_GATE_FAILED prefix verbatim per D-03 (mirrors TRIPLE_GATE_FAILED diagnostic uniformity).
       - Adds a passingCases validation AFTER the 3 legs (D-04 amendment): if !Array.isArray(passingCases) || passingCases.length === 0 → throw new Error("PARTIAL_GATE_FAILED: passingCases — empty (no cases to promote)"). Also validate every entry is a non-empty string; if not → throw new Error("PARTIAL_GATE_FAILED: passingCases — non-string entry").
       - On success returns `{ passingCaseIds: passingCases.slice() }` (defensive copy so callers cannot mutate the input array through the return value).
       - PURE function: no I/O, no gh calls, no process.exit (mirrors assertTripleGate per code_context "Established Patterns").
       - Add a leading comment block matching the assertTripleGate comment style (lines 60-66) explaining: separate-entry-point trust-boundary rationale, the 4 args contract, the PARTIAL_GATE_FAILED throw convention.

    3. `export async function runPartialPromote(passingCaseIds, { dryRun = false } = {})`:
       - Import `promoteFromQuarantine` from `./promote-from-quarantine.mjs` (add to the existing `import { runPromote } from './promote-from-quarantine.mjs';` line — make it `import { runPromote, promoteFromQuarantine } from './promote-from-quarantine.mjs';`). This is the ONLY import edit; the policy boundary (node:* + ./promote-from-quarantine.mjs only) is preserved.
       - Re-validate passingCaseIds (mirror the assertPartialGate validation — defense-in-depth for direct callers): non-array or empty → throw `PARTIAL_GATE_FAILED: passingCases — empty (no cases to promote)`.
       - dryRun short-circuit: if `dryRun === true`, return `{ promoted: passingCaseIds.slice(), halted: false, dryRun: true }` without invoking promoteFromQuarantine.
       - Atomic-per-case loop per D-07: iterate passingCaseIds; for each caseId, `await promoteFromQuarantine({ id: caseId, confirm: true, _skipCiGuard: false })`. On any thrown error (or non-zero exitCode in returned value if the function returns rather than throws), build `const haltMsg = 'PARTIAL_PROMOTE_HALTED: case=' + caseId + ', reason=' + err.message;`, `console.error(haltMsg);`, return `{ promoted, halted: true, error: haltMsg }` where `promoted` is the array of caseIds successfully promoted before the halt.
       - On full success return `{ promoted: [...passingCaseIds], halted: false }`.
       - HARD CONSTRAINT (PARTIAL-01 / Pitfall 4): the literal token sequence `_skipCiGuard: true` MUST NOT appear ANYWHERE in the body of assertPartialGate OR runPartialPromote. The acceptance criterion includes a grep gate enforcing this.
       - Add a leading comment block explaining: per-case atomicity rationale (D-07), halt-on-error semantics (no rollback — matches existing runPromote), why _skipCiGuard:false is correct here (partial promotes re-enter normal triple-gate on the auto-promote follow-up PR).

    Edit tests/unit/auto-fix-promote-gate.test.js to extend per D-17 (no new test file):

    4. Update the import statement (line 44-47) to also import `assertPartialGate`, `runPartialPromote`, `PARTIAL_THRESHOLD`, and `PARTIAL_LABEL` from '../../scripts/auto-fix-promote.mjs'.

    5. INSIDE the existing `describe('assertTripleGate (Phase 44)', () => { ... })` block (between T4 happy path and the closing }) at lines 75-83), add a NEW `it()` test case (label it T5 to continue the sequence):
       - `it('T5 — PARTIAL-04: throws when given auto-fix:partial-verified instead of auto-fix:verified (trust invariant boundary)', () => { ... })`.
       - Body uses `expect(() => assertTripleGate({ prLabels: ['auto-fix:partial-verified'], merged: true, sourceIssueLabels: ['triage'] })).toThrow(/TRIPLE_GATE_FAILED: prLabels — missing 'auto-fix:verified'/)`.
       - Add a second `it('T6 — PARTIAL-04 co-presence: still accepts when both verified AND partial-verified present', () => { ... })` using `expect(...).not.toThrow()` against `prLabels: ['auto-fix:verified', PARTIAL_LABEL]`. Documents the documented co-presence case from the behavior block.

    6. ADD a NEW top-level describe block AFTER the existing `describe('parseSourceIssue (Phase 44)', ...)` (after line 115):
       - `describe('assertPartialGate (Phase 53)', () => { ... })` containing:
         - P1 — happy path: returns `{ passingCaseIds: ['US11427642-spec-short-1'] }` (use the Phase 42 canonical fixture per CONTEXT specifics) when all 3 legs + non-empty passingCases satisfied. Assert exact return shape via `expect(result).toEqual({ passingCaseIds: ['US11427642-spec-short-1'] })`.
         - P2 — Leg 1 fail: throws /PARTIAL_GATE_FAILED: prLabels — missing 'auto-fix:partial-verified'/ when prLabels = ['triage'].
         - P3 — Leg 2 fail: throws /PARTIAL_GATE_FAILED: merged — pull request not merged/ when merged = false.
         - P4 — Leg 3 fail: throws /PARTIAL_GATE_FAILED: sourceIssueLabels — source issue missing 'triage'/ when sourceIssueLabels = ['bug'].
         - P5 — empty passingCases: throws /PARTIAL_GATE_FAILED: passingCases — empty/ when passingCases = [].
         - P6 — omitted passingCases: throws same /PARTIAL_GATE_FAILED: passingCases — empty/ (default-arg behavior).
         - P7 — non-string passingCases entry: throws /PARTIAL_GATE_FAILED: passingCases — non-string entry/.
         - P8 — return is a DEFENSIVE COPY: mutating the returned `passingCaseIds` does not affect a second call's return.

    7. ADD a SECOND new top-level describe block: `describe('PARTIAL_THRESHOLD constant (Phase 53)', () => { ... })`:
       - T_thresh_1 — `expect(PARTIAL_THRESHOLD).toBe(0.80)` (D-11 pin: changing this constant in a future commit must be a deliberate test-update, not a silent drift).
       - T_thresh_2 — `expect(PARTIAL_LABEL).toBe('auto-fix:partial-verified')` (D-18 pin: label string is load-bearing across script + 2 workflows).

    Stage and commit ATOMICALLY:
    ```
    git add scripts/auto-fix-promote.mjs tests/unit/auto-fix-promote-gate.test.js
    git commit -m "feat(53): PARTIAL-01 + PARTIAL-04 — assertPartialGate + runPartialPromote + Vitest pin (assertTripleGate throws on partial-verified)"
    ```
    Verify with `git log -1 --name-only` that the commit touches EXACTLY these two paths (no third file).

    DO NOT touch .github/workflows/* in this commit. DO NOT add a label branch to main() in this commit — that ships in commit (c) per D-20.
  </action>
  <verify>
    <automated>
    # Full Vitest suite must still pass (N existing tests + 11 new: T5 T6 P1..P8 T_thresh_1 T_thresh_2)
    npx vitest run tests/unit/auto-fix-promote-gate.test.js --reporter=verbose &&
    # PARTIAL-01 trust-boundary invariants:
    # 1. New exports present
    grep -E "^export function assertPartialGate" scripts/auto-fix-promote.mjs &&
    grep -E "^export async function runPartialPromote" scripts/auto-fix-promote.mjs &&
    grep -E "^export const PARTIAL_THRESHOLD\s*=\s*0\.80" scripts/auto-fix-promote.mjs &&
    grep -E "^export const PARTIAL_LABEL\s*=\s*'auto-fix:partial-verified'" scripts/auto-fix-promote.mjs &&
    # 2. _skipCiGuard:true MUST NOT appear in this commit's added regions for the partial path
    #    (strip comment lines so YAML/JS block comments cannot self-invalidate the gate per planner rules)
    test "$(grep -v '^\s*//' scripts/auto-fix-promote.mjs | grep -v '^\s*\*' | grep -c '_skipCiGuard:\s*true')" = "1" &&
    #    Exactly 1 match: the SINGLE pre-existing call at line 248 inside main()'s existing assertTripleGate path.
    # 3. assertTripleGate body byte-unchanged invariant (D-05):
    #    Show diff against HEAD~1 (the commit BEFORE this one); the file diff must show only ADDITIONS
    #    (lines starting with '+') in the assertTripleGate region (lines 67-81 of original).
    test -z "$(git diff HEAD~1 HEAD -- scripts/auto-fix-promote.mjs | awk '/^@@/{in_hunk=1} in_hunk && /^-[^-]/{print}' | grep -v '^---')" &&
    # 4. PARTIAL-04 assertion present (inside existing assertTripleGate describe block)
    grep -E "T5.*PARTIAL-04|PARTIAL-04.*trust invariant" tests/unit/auto-fix-promote-gate.test.js &&
    grep -c "auto-fix:partial-verified" tests/unit/auto-fix-promote-gate.test.js | awk '$1 >= 4 {exit 0} {exit 1}' &&
    # 5. New describe block for assertPartialGate present
    grep -E "describe\('assertPartialGate" tests/unit/auto-fix-promote-gate.test.js &&
    # 6. Atomic single commit touching exactly 2 files
    test "$(git log -1 --name-only --format= | grep -v '^$' | sort)" = "$(printf 'scripts/auto-fix-promote.mjs\ntests/unit/auto-fix-promote-gate.test.js\n' | sort)" &&
    # 7. Commit message matches D-21 pattern
    git log -1 --format=%s | grep -E "^feat\(53\): PARTIAL-01 \+ PARTIAL-04 —"
    </automated>
  </verify>
  <done>
    - scripts/auto-fix-promote.mjs has `export const PARTIAL_THRESHOLD = 0.80`, `export const PARTIAL_LABEL = 'auto-fix:partial-verified'`, `export function assertPartialGate(...)`, `export async function runPartialPromote(...)`. Imports promoteFromQuarantine from ./promote-from-quarantine.mjs.
    - assertTripleGate body (lines 67-81 in HEAD~1) is byte-unchanged in HEAD: `git diff HEAD~1 HEAD -- scripts/auto-fix-promote.mjs` shows ONLY additions; no `-` lines except the import line edit (`-import { runPromote }` → `+import { runPromote, promoteFromQuarantine }`).
    - tests/unit/auto-fix-promote-gate.test.js has T5+T6 (PARTIAL-04) inside the existing `describe('assertTripleGate'`) block AND a new `describe('assertPartialGate (Phase 53)'`) block with P1..P8 + a `describe('PARTIAL_THRESHOLD constant')` block with T_thresh_1+T_thresh_2.
    - `npx vitest run tests/unit/auto-fix-promote-gate.test.js` exits 0.
    - `grep -c '_skipCiGuard:\s*true' scripts/auto-fix-promote.mjs` (excluding comments) returns 1 (the SINGLE pre-existing main() call at line 248 inside the assertTripleGate path — unchanged).
    - Exactly ONE commit, message starts with `feat(53): PARTIAL-01 + PARTIAL-04 —` per D-21, touches exactly the two listed files.
    - No workflow YAML touched in this commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: PARTIAL-02 — v40-verifier-gate.yml ready-flip partial-label producer (≥4/5 threshold, FLAKE-aware)</name>
  <files>.github/workflows/v40-verifier-gate.yml</files>
  <read_first>
    - .github/workflows/v40-verifier-gate.yml (full file — focus on verifier-gate job lines 180-316 and ready-flip job lines 420-482)
    - .planning/phases/53-auto-fix-partial-verified-semantics/53-CONTEXT.md (D-09 through D-13)
    - .planning/research/PITFALLS.md Pitfall 4 (FLAKE masquerade + threshold pin)
    - scripts/verify-single-case.mjs (REFERENCE ONLY — confirm `tier_used` and `errorClass` fields in the per-case JSON output that lives at playwright-report/single-case-${case_id}-run-${i}.json; do not modify)
  </read_first>
  <action>
    Two coordinated edits to .github/workflows/v40-verifier-gate.yml:

    EDIT-A — verifier-gate JOB (lines 180-316): emit per-case PASS/FAIL outputs WITHOUT changing the full-pass / full-fail contract.

    Inside the verifier-gate job block, the per-case 3x loop (lines 277-308 of HEAD) currently sets FAILED_CASES locally and exits 1 if non-empty. Change as follows:

    1. Add `outputs:` block at job level (after `timeout-minutes: 25` or wherever existing top-level keys sit), exposing 3 outputs sourced from a step with id `verify`:
       - `passing_cases: ${{ steps.verify.outputs.passing_cases }}`
       - `failing_cases: ${{ steps.verify.outputs.failing_cases }}`
       - `affected_count: ${{ steps.verify.outputs.affected_count }}`

    2. In the existing "Run 3-case verifier" step (the one containing the `for case_id in $CASES; do` loop), give it `id: verify` if it doesn't already have one, and extend its run block:
       - Track PASSING_CASES alongside FAILED_CASES inside the loop (when `ALL_PASS == true`, append to PASSING_CASES).
       - After the loop, ALSO write to GITHUB_OUTPUT:
         ```
         echo "passing_cases=$(echo $PASSING_CASES | xargs | tr ' ' ',')" >> "$GITHUB_OUTPUT"
         echo "failing_cases=$(echo $FAILED_CASES | xargs | tr ' ' ',')" >> "$GITHUB_OUTPUT"
         echo "affected_count=$(echo $CASES | wc -w | xargs)" >> "$GITHUB_OUTPUT"
         ```
       - CRITICAL: the existing hard-fail behavior MUST be relaxed so ready-flip gets a chance to evaluate the partial branch. Replace the existing `if [ -n "$FAILED_CASES" ]; then ... exit 1; fi` block with a tiered decision:
         - If `FAILED_CASES` is empty → existing full-pass behavior (no comment, no label-add, no exit; ready-flip proceeds via `auto-fix:verified` path UNCHANGED).
         - Else if `affected_count >= 5` AND `(affected_count - failing_count) / affected_count >= 0.80` AND no FLAKE in failing cases (jq the per-case JSON outputs at playwright-report/single-case-*.json for `.runs[].errorClass == "FLAKE"`) → log a NOTICE `Partial-pass detected: $PASSING_CASES — handing off to ready-flip for partial-label decision`, DO NOT exit 1, DO NOT add `human-review-required` label. ready-flip will evaluate and act.
         - Else (full-fail, or partial below threshold, or FLAKE present) → existing behavior: add `human-review-required` label, post the existing failure comment, exit 1.
       - The full-pass success contract is PRESERVED (FAILED_CASES empty → identical existing path).

    EDIT-B — ready-flip JOB (lines 420-482): add the partial-label producer step AFTER the existing `Apply auto-fix:verified label` step (line 482). The existing step's YAML bytes are unchanged per D-10.

    The ready-flip job already has `needs: [verifier-gate, regression-suite]`. With EDIT-A above, ready-flip now runs in BOTH full-pass AND partial-pass cases (because verifier-gate no longer exits 1 in the partial branch).

    Add a new env source on the ready-flip job (extend the existing `env:` block at lines 424-426) to pull the verifier-gate outputs:
    ```
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      PR_NUM: ${{ github.event.pull_request.number }}
      PASSING_CASES: ${{ needs.verifier-gate.outputs.passing_cases }}
      FAILING_CASES: ${{ needs.verifier-gate.outputs.failing_cases }}
      AFFECTED_COUNT: ${{ needs.verifier-gate.outputs.affected_count }}
    ```

    Then insert the following step AFTER line 482 (the existing verified-label-add step):

    ```yaml
      - name: Conditional partial-verified label (>=4/5, FLAKE-aware)
        if: steps.scope.outputs.active == 'true' || github.event_name != 'pull_request'
        # PARTIAL-02 (Phase 53 D-09..D-13). Fires ONLY when verifier-gate
        # produced partial outputs AND all 4 conditions hold:
        #   1. affected_count >= 5             (D-12 — abstain for small sets)
        #   2. passing/affected >= 0.80        (D-11 — PARTIAL_THRESHOLD)
        #   3. passing < affected              (D-09 — NOT a full pass; full pass took the existing auto-fix:verified path above)
        #   4. no failing case has errorClass=='FLAKE' (D-13 — FLAKE masquerade mitigation)
        # The existing auto-fix:verified label step is byte-unchanged (D-10).
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          if [ -z "$PASSING_CASES" ] || [ -z "$FAILING_CASES" ]; then
            echo "Full-pass or full-fail (no partial outputs) — partial-label step abstains"
            exit 0
          fi
          PASS_COUNT=$(echo "$PASSING_CASES" | tr ',' '\n' | grep -c .)
          FAIL_COUNT=$(echo "$FAILING_CASES" | tr ',' '\n' | grep -c .)
          AFFECTED=$AFFECTED_COUNT
          if [ "$AFFECTED" -lt 5 ]; then
            echo "affected_count=$AFFECTED < 5 — abstaining per D-12"
            exit 0
          fi
          if [ "$PASS_COUNT" -ge "$AFFECTED" ]; then
            echo "full pass — handled by auto-fix:verified step above"
            exit 0
          fi
          # Threshold check via awk (no bc dependency on ubuntu-latest required)
          RATIO_OK=$(awk -v p="$PASS_COUNT" -v a="$AFFECTED" 'BEGIN { print (p / a >= 0.80) ? 1 : 0 }')
          if [ "$RATIO_OK" != "1" ]; then
            echo "ratio $PASS_COUNT/$AFFECTED < 0.80 — abstaining"
            exit 0
          fi
          # FLAKE-masquerade check (D-13): inspect per-case JSON outputs from
          # the verifier-gate uploaded artifact directory. The verifier-gate
          # job ran in a separate runner; we re-download via gh run download.
          mkdir -p /tmp/verifier-reports
          gh run download "$GITHUB_RUN_ID" \
            --name "verifier-gate-reports-$GITHUB_RUN_ID" \
            --dir /tmp/verifier-reports 2>/dev/null || true
          FLAKE_PRESENT=0
          for case_id in $(echo "$FAILING_CASES" | tr ',' ' '); do
            for report in /tmp/verifier-reports/single-case-${case_id}-run-*.json; do
              [ -f "$report" ] || continue
              if jq -e '.runs[]? | select(.errorClass == "FLAKE")' "$report" >/dev/null 2>&1; then
                FLAKE_PRESENT=1
                break 2
              fi
            done
          done
          if [ "$FLAKE_PRESENT" = "1" ]; then
            echo "FLAKE present in failing cases — abstaining per D-13 (1 flake cannot mask 4 real fails)"
            exit 0
          fi
          # All 4 conditions met — apply the partial-verified label.
          gh label create auto-fix:partial-verified \
            --color FBCA04 \
            --description "Verifier-gate passed >=4/5 of affected cases; failing cases stay quarantined" \
            --force 2>/dev/null || true
          gh pr edit "$PR_NUMBER" --add-label "auto-fix:partial-verified"
          gh pr comment "$PR_NUMBER" --body "Verifier-gate PARTIAL PASS — ${PASS_COUNT}/${AFFECTED} affected cases passed. Failing case(s): ${FAILING_CASES}. Promotion will mutate the corpus only for the passing subset on merge."
    ```

    Verify YAML syntax: run `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/v40-verifier-gate.yml'))" ` (or yq if available) to confirm valid YAML.

    Stage and commit ATOMICALLY:
    ```
    git add .github/workflows/v40-verifier-gate.yml
    git commit -m "feat(53): PARTIAL-02 — v40-verifier-gate.yml ready-flip partial-label producer (≥4/5 threshold, FLAKE-aware)"
    ```
  </action>
  <verify>
    <automated>
    # 1. Label string present at least once in the YAML
    grep -c "auto-fix:partial-verified" .github/workflows/v40-verifier-gate.yml | awk '$1 >= 1 {exit 0} {exit 1}' &&
    # 2. FLAKE-mitigation hook present (D-13)
    grep -q "FLAKE" .github/workflows/v40-verifier-gate.yml &&
    grep -q 'errorClass == "FLAKE"' .github/workflows/v40-verifier-gate.yml &&
    # 3. Step name verbatim for greppability
    grep -q "name: Conditional partial-verified label" .github/workflows/v40-verifier-gate.yml &&
    # 4. Existing auto-fix:verified label-add step byte-unchanged (D-10).
    #    Anchor on the verbatim line 482 of HEAD~1: `run: gh pr edit "$PR_NUMBER" --add-label "auto-fix:verified"`
    git show HEAD~1:.github/workflows/v40-verifier-gate.yml | grep -F 'run: gh pr edit "$PR_NUMBER" --add-label "auto-fix:verified"' > /tmp/before-line.txt &&
    grep -F 'run: gh pr edit "$PR_NUMBER" --add-label "auto-fix:verified"' .github/workflows/v40-verifier-gate.yml > /tmp/after-line.txt &&
    diff /tmp/before-line.txt /tmp/after-line.txt &&
    # 5. Threshold 0.80 encoded numerically
    grep -E "0\.80" .github/workflows/v40-verifier-gate.yml &&
    # 6. New verifier-gate job outputs present (passing_cases, failing_cases, affected_count)
    grep -q "passing_cases:" .github/workflows/v40-verifier-gate.yml &&
    grep -q "failing_cases:" .github/workflows/v40-verifier-gate.yml &&
    grep -q "affected_count:" .github/workflows/v40-verifier-gate.yml &&
    # 7. YAML syntax valid
    python3 -c "import yaml; yaml.safe_load(open('.github/workflows/v40-verifier-gate.yml'))" &&
    # 8. Atomic single commit, single file
    test "$(git log -1 --name-only --format= | grep -v '^$')" = ".github/workflows/v40-verifier-gate.yml" &&
    # 9. Commit message matches D-21
    git log -1 --format=%s | grep -E "^feat\(53\): PARTIAL-02 —" &&
    # 10. Full vitest suite still green (no regression to Task 1 work)
    npx vitest run tests/unit/auto-fix-promote-gate.test.js
    </automated>
  </verify>
  <done>
    - .github/workflows/v40-verifier-gate.yml verifier-gate job exposes `passing_cases`, `failing_cases`, `affected_count` as job outputs.
    - .github/workflows/v40-verifier-gate.yml ready-flip job contains the new step `Conditional partial-verified label (>=4/5, FLAKE-aware)` AFTER the existing `Apply auto-fix:verified label` step.
    - The existing `Apply auto-fix:verified label` step's YAML bytes (specifically the `run:` line at HEAD~1:line 482) are unchanged in HEAD.
    - The verifier-gate job no longer hard-fails on partial-pass cases (relaxed to log + continue), but still hard-fails on full-fail and on partial-below-threshold and on FLAKE-present.
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/v40-verifier-gate.yml'))"` exits 0.
    - Exactly ONE commit, message starts with `feat(53): PARTIAL-02 —` per D-21, touches exactly `.github/workflows/v40-verifier-gate.yml`.
    - No script edits in this commit, no test edits in this commit, no edits to v40-auto-promote.yml in this commit.
    - `npx vitest run tests/unit/auto-fix-promote-gate.test.js` still passes (Task 1 work intact).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PARTIAL-03 — v40-auto-promote.yml widened if-filter + runPartialPromote wiring (main() label branch)</name>
  <files>.github/workflows/v40-auto-promote.yml, scripts/auto-fix-promote.mjs, tests/unit/auto-fix-promote-gate.test.js</files>
  <read_first>
    - .github/workflows/v40-auto-promote.yml (full file — focus on job-level if: line 67 and the Triple-gate + runPromote step lines 150-173)
    - scripts/auto-fix-promote.mjs (post-Task-1 state — main() at lines 194-261 will be extended; assertTripleGate body still byte-unchanged; new exports from Task 1 will be wired in)
    - tests/unit/auto-fix-promote-gate.test.js (post-Task-1 state — add main()-branch tests; this commit may add tests that exercise the label-branch decision)
    - .planning/phases/53-auto-fix-partial-verified-semantics/53-CONTEXT.md (D-14 through D-16, D-20 commit-order)
  </read_first>
  <behavior>
    # main() label-branch behavior (D-15)
    - When parseArgv yields prLabels containing 'auto-fix:verified' (Task 1's `PARTIAL_LABEL` constant is NOT a match for the verified label): existing assertTripleGate + runPromote path runs UNCHANGED. Exit codes 0/1 match HEAD~2 behavior.
    - When prLabels contains 'auto-fix:partial-verified' (PARTIAL_LABEL) but NOT 'auto-fix:verified': new path runs — assertPartialGate({ prLabels, merged, sourceIssueLabels, passingCases: passingCasesArr }) → if throws, write to stderr and exit 1; else await runPartialPromote(result.passingCaseIds). If runPartialPromote returns `halted: true`, exit 1 (the halt message has already been emitted to stderr by runPartialPromote). Else exit 0.
    - When prLabels contains BOTH labels: 'auto-fix:verified' takes precedence (assertTripleGate path runs — preserves existing semantics; the partial path is a NEW capability, not a replacement).
    - When prLabels contains NEITHER label: exit 0 defensively with a stderr note `[auto-fix-promote] no recognized verified label on PR — no-op exit 0` (the workflow's if: filter SHOULD prevent reaching here, but the script defends against direct CLI misuse).

    # --passing-cases CLI arg behavior (D-16)
    - parseArgv accepts new `--passing-cases <csv>` flag. CSV is parsed identically to --pr-labels and --source-issue-labels (split on comma, trim, filter empty).
    - The partial path consumes the parsed array as `passingCases` arg to assertPartialGate.
    - The verified path IGNORES --passing-cases entirely (does not validate, does not throw).
    - If --passing-cases is missing AND the label branch lands on partial-verified, the assertPartialGate empty-passingCases throw fires naturally — no separate guard needed.

    # Workflow if-filter widening (D-14)
    - v40-auto-promote.yml job-level if: accepts ANY PR that is (merged AND has 'auto-fix:verified') OR (merged AND has 'auto-fix:partial-verified').
    - The existing verified-only path's runtime behavior on a verified PR is IDENTICAL to HEAD~2 (only the if-filter expression changed; the steps unchanged).
  </behavior>
  <action>
    THREE coordinated edits — script `main()` branch, workflow if-filter, workflow new CLI arg + per-case comment hook.

    EDIT-A — scripts/auto-fix-promote.mjs main() branch on label (D-15):

    1. Extend KNOWN_FLAGS (line 113-117): add `'--passing-cases'`.

    2. Extend parseArgv (lines 128-185): add a `passingCasesCsv = ''` local var; add the case to the switch:
       ```
       case '--passing-cases':         passingCasesCsv = takeValue(argv, i, tok); i++; break;
       ```
       Add to the return object: `passingCases: passingCasesCsv ? passingCasesCsv.split(',').map((s) => s.trim()).filter(Boolean) : [],`.

    3. Extend main() (lines 194-261). The existing structure remains, but the triple-gate block (lines 210-219) is wrapped in a label-branch decision. Replace the single triple-gate block with:

       a. AFTER the `body = fs.readFileSync(...)` block (preserves existing PR body load) but BEFORE the triple-gate call, add the label-branch decision:
       ```
       const hasVerified = args.prLabels.includes('auto-fix:verified');
       const hasPartial  = args.prLabels.includes(PARTIAL_LABEL);
       ```
       (PARTIAL_LABEL is already imported at the top of the same file from Task 1.)

       b. If `hasVerified`: take the EXISTING path verbatim — assertTripleGate, parseSourceIssue cross-check, await runPromote({_skipCiGuard:true}) per case. NO byte changes to this branch.

       c. Else if `hasPartial`: take the NEW partial path:
          - `try { assertPartialGate({ prLabels: args.prLabels, merged: args.prMerged, sourceIssueLabels: args.sourceIssueLabels, passingCases: args.passingCases }); } catch (err) { process.stderr.write('[auto-fix-promote] ' + err.message + '\n'); process.exit(1); }`
          - parseSourceIssue cross-check is STILL run (same defense as the verified path — argv tampering defense).
          - `const result = await runPartialPromote(args.passingCases);`
          - If `result.halted` → stderr write the existing halt message (runPartialPromote already emitted it; this is belt-and-suspenders for the script-level log surface) and process.exit(1).
          - Else stdout write `[auto-fix-promote] partial-promoted ${result.promoted.length}/${args.passingCases.length} cases (source issue #${resolvedSourceIssue}): ${result.promoted.join(',')}` and process.exit(0).

       d. Else (neither label): stderr `[auto-fix-promote] no recognized verified label on PR — no-op exit 0` and process.exit(0).

    4. CRITICAL invariants for this edit:
       - assertTripleGate body lines 67-81 STILL byte-unchanged. The branch logic is added in main() ONLY.
       - `runPromote({_skipCiGuard:true})` is called ONLY on the hasVerified branch (existing). NEVER on the hasPartial branch.
       - `assertPartialGate`, `runPartialPromote`, `PARTIAL_LABEL` are already in scope from the Task 1 commit; no new exports added in this commit. No imports edited in this commit.

    EDIT-B — .github/workflows/v40-auto-promote.yml job-level if: widening (D-14):

    5. Replace line 67:
       ```
       if: github.event.pull_request.merged == true && contains(github.event.pull_request.labels.*.name, 'auto-fix:verified')
       ```
       with the OR-branch verbatim per D-14:
       ```
       if: |
         github.event.pull_request.merged == true && (
           contains(github.event.pull_request.labels.*.name, 'auto-fix:verified') ||
           contains(github.event.pull_request.labels.*.name, 'auto-fix:partial-verified')
         )
       ```
       This preserves the existing verified-path filter exactly (string `auto-fix:verified` still present in the if:) and ADDS the partial-verified OR-branch.

    EDIT-C — .github/workflows/v40-auto-promote.yml --passing-cases wiring (D-16):

    6. The verifier-gate job's outputs added in Task 2 (`passing_cases`, `failing_cases`, `affected_count`) are NOT accessible to v40-auto-promote.yml directly — auto-promote is a SEPARATE workflow triggered by `pull_request: closed`. The passing_cases CSV must be recovered at auto-promote time from a PR-side artifact OR re-derived from the PR labels.

       The cleanest available source: the partial-label step in Task 2 already includes the CSV inside its PR comment ("Failing case(s): ${FAILING_CASES}"). We extend it to also include `Passing case(s): ${PASSING_CASES}` in the SAME comment format (matches D-08 "extend existing comment-posting step for minimal YAML diff").

       Actually a more robust pattern available: extend the partial-label step in Task 2's commit to ALSO add a label-suffixed PR comment containing the canonical CSV in a parseable HTML comment form: `<!-- partial_passing_cases: c1,c2,c4,c5 -->`. v40-auto-promote.yml then greps the PR body / comments for this marker at the "Parse PR body + labels + source issue" step (lines 89-138 of the workflow).

       SCOPE NOTE: this is a meaningful integration point. The cleanest implementation has TWO touches in Task 3:
       (i) Extend the v40-auto-promote.yml "Parse PR body + labels + source issue" step to ALSO recover `partial_passing_cases` from a PR comment containing `<!-- partial_passing_cases: id1,id2 -->`, output as `partial_passing_cases` GHA output.
       (ii) Add `--passing-cases "$PARTIAL_PASSING_CASES"` to the script invocation step (lines 161-173) — UNCONDITIONALLY (the script ignores it on the verified path).

       Also extend Task 2's partial-label step to POST a PR comment containing the HTML marker. The marker is what the auto-promote workflow parses on PR-closed. Since Task 2 has already been committed (D-20 lock), this requires an additional edit in THIS commit's Task 3 to .github/workflows/v40-verifier-gate.yml ready-flip partial-label step.

       *** DEVIATION FROM D-20 FILE LIST: Task 3 must ALSO touch .github/workflows/v40-verifier-gate.yml to add the HTML-marker comment producer. This is structurally necessary — the cross-workflow data passing requires a producer (verifier-gate) and consumer (auto-promote) and the consumer cannot read job outputs from a separate workflow's runner. The marker is a PR-body/comment-side persistent artifact.

       Resolve by adding `.github/workflows/v40-verifier-gate.yml` to Task 3's files list AS WELL. The Task 3 commit message remains `feat(53): PARTIAL-03 — v40-auto-promote.yml widened if-filter + runPartialPromote wiring`; the v40-verifier-gate.yml edit is a DEPENDENCY of PARTIAL-03 (the producer for the --passing-cases consumer), not a re-do of PARTIAL-02. If the executor judges that re-opening v40-verifier-gate.yml in commit (c) violates D-20 commit-purity, the alternative is: collapse PARTIAL-02 and PARTIAL-03 into a single 2-task plan and re-run planning. Recommended: proceed with the multi-file commit in Task 3 and document this in the SUMMARY.md as a structural exception with no semantic deviation.

    EDIT-C concrete steps:

    7. In .github/workflows/v40-verifier-gate.yml ready-flip "Conditional partial-verified label" step (added in Task 2), extend the gh pr comment line to embed the HTML marker:
       ```
       gh pr comment "$PR_NUMBER" --body "$(cat <<EOF
       Verifier-gate PARTIAL PASS — ${PASS_COUNT}/${AFFECTED} affected cases passed. Failing case(s): ${FAILING_CASES}. Promotion will mutate the corpus only for the passing subset on merge.

       <!-- partial_passing_cases: ${PASSING_CASES} -->
       EOF
       )"
       ```

    8. In .github/workflows/v40-auto-promote.yml "Parse PR body + labels + source issue" step (lines 89-138), extend the bash block:
       - After the existing SOURCE_ISSUE_RAW recovery (line 133), add:
         ```
         # Recover the partial_passing_cases CSV from PR comments if present
         # (PARTIAL-02 emits a comment containing <!-- partial_passing_cases: c1,c2 -->)
         PARTIAL_PASSING_CASES=""
         if echo "$LABELS_CSV" | grep -q 'auto-fix:partial-verified'; then
           ALL_COMMENTS=$(gh pr view "$PR_NUMBER" --json comments --jq '.comments[].body' 2>/dev/null || echo "")
           PARTIAL_PASSING_CASES=$(echo "$ALL_COMMENTS" | grep -oE '<!-- partial_passing_cases: [^ ]+ -->' \
             | head -1 \
             | sed -E 's/<!-- partial_passing_cases: ([^ ]+) -->/\1/')
           if [ -z "$PARTIAL_PASSING_CASES" ]; then
             echo "::error::PR carries auto-fix:partial-verified but no <!-- partial_passing_cases: ... --> marker found in comments"
             exit 1
           fi
         fi
         ```
       - Append to the GITHUB_OUTPUT block:
         ```
         echo "partial_passing_cases=$PARTIAL_PASSING_CASES"
         ```

    9. In .github/workflows/v40-auto-promote.yml "Triple-gate + runPromote (per affected case)" step (lines 150-173), extend the env block and the script invocation:
       - Add to env: `PARTIAL_PASSING_CASES: ${{ steps.parse.outputs.partial_passing_cases }}`
       - Add to the `node scripts/auto-fix-promote.mjs ...` line: `--passing-cases "$PARTIAL_PASSING_CASES" \`
       - The script's main() label branch (EDIT-A) handles the routing; --passing-cases is ignored on the verified path.

    EDIT-D — tests/unit/auto-fix-promote-gate.test.js extension (NEW main()-branch tests):

    10. Add a new top-level describe block AFTER the existing assertPartialGate describe block:
        - `describe('parseArgv --passing-cases (Phase 53)', () => { ... })`:
          - Test: `parseArgv(['node', 'script', '--pr', '1', '--pr-merged', 'true', '--case-id', 'c1', '--passing-cases', 'c1,c2,c3'])` → returned object has `passingCases: ['c1', 'c2', 'c3']`.
          - Test: parseArgv WITHOUT --passing-cases → returned object has `passingCases: []`.
          - Test: `parseArgv([..., '--passing-cases', 'c1, c2 , c3,'])` → trims and filters empty, returns `['c1', 'c2', 'c3']`.

    11. NOTE on testing main() branch: main() calls process.exit which is hard to mock in Vitest. The script-side behavior is exercised indirectly via the existing PROMOTE-yaml test (tests/e2e/scripts/v40-auto-promote-yaml.test.js if present) and via the workflow live-fire UAT. The unit-test coverage in this commit focuses on parseArgv + the already-tested assertPartialGate/runPartialPromote (Task 1 coverage). Documenting this scoping decision is sufficient — do NOT add an integration test in this phase.

    Stage and commit ATOMICALLY:
    ```
    git add .github/workflows/v40-auto-promote.yml .github/workflows/v40-verifier-gate.yml scripts/auto-fix-promote.mjs tests/unit/auto-fix-promote-gate.test.js
    git commit -m "feat(53): PARTIAL-03 — v40-auto-promote.yml widened if-filter + runPartialPromote wiring"
    ```

    NOTE: v40-verifier-gate.yml appears in BOTH commit (b) AND commit (c). This is structural (PARTIAL-03's --passing-cases data path requires the marker producer on the verifier-gate side; the consumer on the auto-promote side cannot read cross-workflow outputs). The commit (c) edit to verifier-gate is a PURE ADDITION (the marker tail in the existing partial-label step's comment) — it does NOT modify anything PARTIAL-02 committed. This is the minimum-deviation path given D-20's locked order.
  </action>
  <verify>
    <automated>
    # 1. v40-auto-promote.yml if-filter widened — both labels present
    grep "auto-fix:partial-verified" .github/workflows/v40-auto-promote.yml &&
    grep -c "auto-fix:verified" .github/workflows/v40-auto-promote.yml | awk '$1 >= 1 {exit 0} {exit 1}' &&
    # 2. --passing-cases CLI arg passed by workflow
    grep -- "--passing-cases" .github/workflows/v40-auto-promote.yml &&
    # 3. scripts/auto-fix-promote.mjs has main() label branch references both new exports
    grep -c "assertPartialGate\|runPartialPromote" scripts/auto-fix-promote.mjs | awk '$1 >= 4 {exit 0} {exit 1}' &&
    # 4. parseArgv recognizes --passing-cases
    grep -q "'--passing-cases'" scripts/auto-fix-promote.mjs &&
    grep -q "passingCasesCsv" scripts/auto-fix-promote.mjs &&
    # 5. _skipCiGuard:true STILL appears exactly once (the SINGLE pre-existing main() call inside the verified branch — unchanged)
    test "$(grep -v '^\s*//' scripts/auto-fix-promote.mjs | grep -v '^\s*\*' | grep -c '_skipCiGuard:\s*true')" = "1" &&
    # 6. assertTripleGate body STILL byte-unchanged (compare against HEAD~3 == PRE-Task-1 baseline)
    test -z "$(git show HEAD~3:scripts/auto-fix-promote.mjs | sed -n '67,81p' | diff - <(sed -n '/^export function assertTripleGate/,/^}/p' scripts/auto-fix-promote.mjs | head -15))" &&
    # 7. PARTIAL-02 partial-label step still present (Task 2's work intact)
    grep -q "name: Conditional partial-verified label" .github/workflows/v40-verifier-gate.yml &&
    # 8. NEW: partial_passing_cases HTML marker producer + consumer
    grep -q "partial_passing_cases" .github/workflows/v40-verifier-gate.yml &&
    grep -q "partial_passing_cases" .github/workflows/v40-auto-promote.yml &&
    # 9. YAML syntax valid for both workflows
    python3 -c "import yaml; yaml.safe_load(open('.github/workflows/v40-auto-promote.yml'))" &&
    python3 -c "import yaml; yaml.safe_load(open('.github/workflows/v40-verifier-gate.yml'))" &&
    # 10. Full Vitest suite passes (includes new parseArgv tests + all Task 1 tests)
    npx vitest run tests/unit/auto-fix-promote-gate.test.js &&
    # 11. Full test suite (broader regression — Task 1's PARTIAL_THRESHOLD pin + all 1134 existing tests)
    npm test &&
    # 12. Atomic single commit touching exactly the 4 listed files
    test "$(git log -1 --name-only --format= | grep -v '^$' | sort)" = "$(printf '.github/workflows/v40-auto-promote.yml\n.github/workflows/v40-verifier-gate.yml\nscripts/auto-fix-promote.mjs\ntests/unit/auto-fix-promote-gate.test.js\n' | sort)" &&
    # 13. Commit message matches D-21
    git log -1 --format=%s | grep -E "^feat\(53\): PARTIAL-03 —" &&
    # 14. Commit-order audit (D-20): the 3 most recent commits in order
    git log -3 --format=%s | head -3 | tac | awk 'NR==1 && !/^feat\(53\): PARTIAL-01 \+ PARTIAL-04/ {exit 1} NR==2 && !/^feat\(53\): PARTIAL-02/ {exit 1} NR==3 && !/^feat\(53\): PARTIAL-03/ {exit 1} END {exit 0}'
    </automated>
  </verify>
  <done>
    - scripts/auto-fix-promote.mjs main() branches on prLabels: 'auto-fix:verified' → assertTripleGate + runPromote ({_skipCiGuard:true}) UNCHANGED path; 'auto-fix:partial-verified' → assertPartialGate + runPartialPromote (NO _skipCiGuard:true). parseArgv recognizes --passing-cases CSV.
    - .github/workflows/v40-auto-promote.yml job-level if: filter widened to OR-branch (preserves verified path verbatim, adds partial-verified OR-clause). --passing-cases CLI arg wired through Parse step → Triple-gate step.
    - .github/workflows/v40-verifier-gate.yml partial-label step's PR comment now also emits `<!-- partial_passing_cases: ${PASSING_CASES} -->` for cross-workflow handoff (the only structural deviation from D-20's file lists; documented in the commit body and SUMMARY.md).
    - assertTripleGate body lines 67-81 are STILL byte-unchanged against HEAD~3 (the pre-Phase-53 baseline). `_skipCiGuard: true` still appears exactly once in scripts/auto-fix-promote.mjs (the unchanged main() verified-branch call).
    - `npx vitest run tests/unit/auto-fix-promote-gate.test.js` passes; `npm test` exits 0 (full 1134-test suite green).
    - YAML syntax valid for both workflows.
    - Exactly ONE commit, message starts with `feat(53): PARTIAL-03 —` per D-21.
    - Phase 53 git log shows EXACTLY 3 feat(53) commits in D-20 order: (a) PARTIAL-01 + PARTIAL-04 → (b) PARTIAL-02 → (c) PARTIAL-03.
    - All 4 PARTIAL-* REQ IDs resolved: PARTIAL-01 (Task 1 script exports), PARTIAL-02 (Task 2 ready-flip step), PARTIAL-03 (Task 3 if-filter + main() wiring), PARTIAL-04 (Task 1 Vitest pin).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auto-fix PR → verifier-gate label producer | Untrusted PR content (body, branch name, affected_cases CSV) crosses into the gh CLI namespace; labels emitted here flow into the auto-promote trust gate |
| verifier-gate → auto-promote (cross-workflow) | The `auto-fix:partial-verified` label + `<!-- partial_passing_cases: ... -->` PR comment marker are the ONLY signals crossing the workflow boundary; both are PR-side persistent artifacts |
| auto-promote workflow → auto-fix-promote.mjs script | --pr-labels CSV + --passing-cases CSV cross into the Vitest-mockable pure-function gate (assertPartialGate); argv tampering surface |
| auto-fix-promote.mjs main() → promote-from-quarantine.mjs | The `_skipCiGuard:true` exemption — Phase 35's load-bearing trust decision — is reachable ONLY via the assertTripleGate path; assertPartialGate path uses _skipCiGuard:false |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Tampering | scripts/auto-fix-promote.mjs assertTripleGate body | mitigate | D-05 byte-unchanged invariant + PARTIAL-04 Vitest pin (T5 + T6) asserting assertTripleGate throws on auto-fix:partial-verified + plan-checker D-22 + acceptance grep `git diff HEAD~1 HEAD -- scripts/auto-fix-promote.mjs` shows no removals in lines 67-81 |
| T-53-02 | Elevation of Privilege | runPartialPromote `_skipCiGuard:true` leak | mitigate | D-04 + D-06 (runPartialPromote calls promoteFromQuarantine with `_skipCiGuard: false`; assertPartialGate never calls runPromote at all) + acceptance grep `grep -c '_skipCiGuard:\s*true' scripts/auto-fix-promote.mjs` (excluding comments) returns EXACTLY 1 (the pre-existing main() verified-branch call) + plan-checker D-22 |
| T-53-03 | Spoofing | FLAKE-class fail masquerading as cause of partial pass | mitigate | D-13 FLAKE-aware skip in the partial-label step; the step parses per-case JSON reports for `errorClass == "FLAKE"` and abstains if ANY failing case shows FLAKE; prevents 1 flake from being misclassified as a real-fail that the partial threshold tolerates |
| T-53-04 | Information Disclosure | Partial promote operates on wrong case subset (argv tampering or cross-workflow drift) | mitigate | D-04 explicit `passingCases` parameter validated as non-empty string-only array; defensive copy on return; D-16 CSV sourced from a verifier-gate-emitted PR comment marker `<!-- partial_passing_cases: ... -->` (server-side audit trail); auto-promote workflow exits 1 if PR has partial label but no marker |
| T-53-05 | Denial of Service | Empty passingCases edge case → silent no-op promote | mitigate | D-04 amendment: assertPartialGate throws `PARTIAL_GATE_FAILED: passingCases — empty (no cases to promote)` on empty/non-array; runPartialPromote also re-validates (defense-in-depth for direct CLI callers); Vitest P5 + P6 cover both cases |
| T-53-06 | Repudiation | Same-commit constraint violated → label string exists in workflow YAML without Vitest guard active | mitigate | D-18 SAME-commit pin: PARTIAL-04 Vitest assertion ships in commit (a) ALONGSIDE the PARTIAL_LABEL const declaration in scripts/auto-fix-promote.mjs; the workflow label-emit step in commit (b) cannot land without commit (a) already present per D-20 locked order; plan-checker D-22 verifies commit-order |

Severity: T-53-01 + T-53-02 HIGH (trust-boundary erosion of the v4.0 load-bearing decision). T-53-03 + T-53-04 MEDIUM (correctness; corpus pollution risk). T-53-05 + T-53-06 LOW (caught by Vitest + commit-order audit).
</threat_model>

<verification>

## Phase-Level Verification

Run after Task 3 lands (post-final-commit):

1. **Trust-invariant integrity (T-53-01, T-53-02 — HIGH)**
   - `git diff HEAD~3 HEAD -- scripts/auto-fix-promote.mjs` shows assertTripleGate body lines 67-81 are byte-unchanged (no `-` lines in that region; only additions in new export blocks + the import line edit + main() label-branch).
   - `grep -v '^\s*//' scripts/auto-fix-promote.mjs | grep -v '^\s*\*' | grep -c '_skipCiGuard:\s*true'` returns exactly 1 (the single pre-existing main() call inside the verified branch).
   - `grep -c 'PARTIAL_GATE_FAILED' scripts/auto-fix-promote.mjs` returns ≥ 4 (one per leg + passingCases-empty + non-string check).

2. **Test pin operative (PARTIAL-04 / T-53-06)**
   - `npx vitest run tests/unit/auto-fix-promote-gate.test.js` exits 0 with the new T5+T6 cases (PARTIAL-04) passing inside the existing assertTripleGate describe block, plus P1..P8 in the new assertPartialGate describe block, plus T_thresh_1+T_thresh_2 in the PARTIAL_THRESHOLD describe block, plus parseArgv tests.
   - `git log --oneline -3 -- tests/unit/auto-fix-promote-gate.test.js` shows commits (a) and (c) touched it; the PARTIAL-04 assertion landed in (a) per D-18.

3. **Workflow chain integrity (PARTIAL-02 + PARTIAL-03)**
   - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/v40-verifier-gate.yml'))"` exits 0.
   - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/v40-auto-promote.yml'))"` exits 0.
   - `grep -q "name: Conditional partial-verified label" .github/workflows/v40-verifier-gate.yml` matches.
   - `grep -c "auto-fix:partial-verified" .github/workflows/v40-auto-promote.yml` returns ≥ 1 (the OR-branch).
   - `grep -c "auto-fix:verified" .github/workflows/v40-auto-promote.yml` returns ≥ 1 (the existing branch preserved verbatim).
   - `grep -q -- "--passing-cases" .github/workflows/v40-auto-promote.yml` matches (D-16 wiring).
   - `grep -q "partial_passing_cases" .github/workflows/v40-verifier-gate.yml` AND `grep -q "partial_passing_cases" .github/workflows/v40-auto-promote.yml` BOTH match (cross-workflow handoff present).

4. **D-10 + Apply auto-fix:verified label step byte-unchanged**
   - `git show HEAD~3:.github/workflows/v40-verifier-gate.yml | grep -F 'run: gh pr edit "$PR_NUMBER" --add-label "auto-fix:verified"'` AND `grep -F 'run: gh pr edit "$PR_NUMBER" --add-label "auto-fix:verified"' .github/workflows/v40-verifier-gate.yml` produce identical lines.

5. **Commit-order audit (D-20)**
   - `git log -3 --format=%s` shows top-to-bottom: PARTIAL-03 → PARTIAL-02 → PARTIAL-01 + PARTIAL-04 (i.e., chronological commit-order (a) → (b) → (c) read bottom-to-top from `git log`).
   - All 3 commit messages match `^feat\(53\): ` per D-21.

6. **Full regression**
   - `npm test` exits 0 (the full 1134-test Vitest suite; confirms no Task 1 or Task 3 unit-test regression and that the workflow YAML changes did not break any `tests/e2e/scripts/v40-*.test.js` static-grep assertions).

## Source Audit (Multi-Source Coverage)

| Source | Item | Disposition |
|--------|------|-------------|
| GOAL (ROADMAP §Phase 53) | Forward-looking gate state for partial-pass verifier | COVERED — Task 1 (assertPartialGate exports), Task 2 (label producer), Task 3 (filter+wiring) |
| REQ | PARTIAL-01 (assertPartialGate separate entry, no _skipCiGuard:true, assertTripleGate byte-unchanged) | COVERED by Task 1 |
| REQ | PARTIAL-02 (ready-flip emits auto-fix:partial-verified at ≥4/5, full-pass YAML byte-unchanged) | COVERED by Task 2 |
| REQ | PARTIAL-03 (auto-promote if-filter widened, partial-pass mutates only PASSING subset) | COVERED by Task 3 |
| REQ | PARTIAL-04 (Vitest assertion that assertTripleGate throws on auto-fix:partial-verified, SAME commit as label) | COVERED by Task 1 (T5+T6 cases in the existing assertTripleGate describe block + new label const PARTIAL_LABEL in same file) |
| RESEARCH | SUMMARY.md §Phase 53 trust-boundary mandate | COVERED by D-05 byte-unchanged invariant + T-53-01 mitigation |
| RESEARCH | SUMMARY.md plan review recommendation | COVERED by D-22 (mandatory plan-checker before execution) |
| RESEARCH | PITFALLS.md Pitfall 4 (assertTripleGate widening forbidden) | COVERED by D-05 + T-53-01 + the explicit anti-pattern → correct-pattern code blocks reproduced in the read_first context for Task 1 |
| RESEARCH | PITFALLS.md Pitfall 4 (threshold floor 4/5 with FLAKE masquerade mitigation) | COVERED by D-11 (PARTIAL_THRESHOLD const + Vitest pin) + D-13 (FLAKE-aware skip in partial-label step) + T-53-03 |
| CONTEXT | D-01 through D-22 (all 22 decisions) | COVERED inline in must_haves.truths (each truth tags 1-2 D-NN IDs) — 22/22 decision-coverage gate |
| CONTEXT | Out-of-scope: per-ERROR_CLASS thresholds (deferred v4.2) | RESPECTED — single 0.80 threshold used across all classes |
| CONTEXT | Out-of-scope: widening assertTripleGate to accept new label | RESPECTED — D-05 enforced, PARTIAL-04 pins the boundary |
| CONTEXT | Out-of-scope: _skipCiGuard:true in partial path | RESPECTED — D-04, D-06, grep gate enforce |
| CONTEXT | Out-of-scope: auto-closing permanently-failing cases | RESPECTED — Task 2 partial step posts a NOTICE comment only; closure deferred to v4.2 |
| CONTEXT | Deferred Ideas: per-class thresholds, auto-retry, transactional promote, dry-run mode | RESPECTED — none planned in Phase 53 |

All items COVERED. No PHASE SPLIT recommended. No unplanned items.

</verification>

<success_criteria>

Phase 53 is COMPLETE when ALL of the following hold:

1. **PARTIAL-01 verified:**
   - `scripts/auto-fix-promote.mjs` exports `assertPartialGate`, `runPartialPromote`, `PARTIAL_THRESHOLD = 0.80`, `PARTIAL_LABEL = 'auto-fix:partial-verified'`.
   - `assertTripleGate` body (HEAD~3 lines 67-81) is byte-unchanged at HEAD.
   - `_skipCiGuard: true` appears exactly once in the file (the pre-existing main() verified-branch call), excluding comments.

2. **PARTIAL-02 verified:**
   - `.github/workflows/v40-verifier-gate.yml` ready-flip job has step `Conditional partial-verified label (>=4/5, FLAKE-aware)` AFTER the existing verified-label step.
   - The existing `run: gh pr edit "$PR_NUMBER" --add-label "auto-fix:verified"` line at HEAD~3 is byte-unchanged at HEAD.
   - The threshold 0.80 is encoded numerically (awk-based ratio check; no bc dependency).
   - FLAKE-aware skip parses per-case JSON for `errorClass == "FLAKE"`.

3. **PARTIAL-03 verified:**
   - `.github/workflows/v40-auto-promote.yml` job-level `if:` contains both `'auto-fix:verified'` AND `'auto-fix:partial-verified'` strings (OR-branch).
   - Workflow passes `--passing-cases "$PARTIAL_PASSING_CASES"` to the script invocation.
   - `scripts/auto-fix-promote.mjs` main() branches on prLabels: hasVerified → existing path UNCHANGED; hasPartial → assertPartialGate + runPartialPromote.

4. **PARTIAL-04 verified:**
   - `tests/unit/auto-fix-promote-gate.test.js` contains T5+T6 inside the existing `describe('assertTripleGate'`) block asserting assertTripleGate throws on `prLabels: ['auto-fix:partial-verified']`.
   - The PARTIAL_LABEL constant first declaration AND the T5 Vitest pin AND `assertPartialGate` are all in the SAME commit (commit (a) per D-20).
   - `git log --follow -- tests/unit/auto-fix-promote-gate.test.js` first edit in Phase 53 is commit (a) with message starting `feat(53): PARTIAL-01 + PARTIAL-04 —`.

5. **D-20 + D-21 commit hygiene:**
   - Exactly 3 `feat(53):` commits exist in Phase 53.
   - Order (oldest → newest): (a) PARTIAL-01 + PARTIAL-04 → (b) PARTIAL-02 → (c) PARTIAL-03.
   - Commit (a) touches exactly 2 files: `scripts/auto-fix-promote.mjs`, `tests/unit/auto-fix-promote-gate.test.js`.
   - Commit (b) touches exactly 1 file: `.github/workflows/v40-verifier-gate.yml`.
   - Commit (c) touches exactly 4 files: `.github/workflows/v40-auto-promote.yml`, `.github/workflows/v40-verifier-gate.yml` (additive marker tail only), `scripts/auto-fix-promote.mjs`, `tests/unit/auto-fix-promote-gate.test.js`.
   - The verifier-gate re-touch in (c) is a PURE ADDITION (marker tail in partial-label step's PR comment); the PARTIAL-02 commit (b) is byte-unchanged for everything except the comment-body extension.

6. **Test suite green:**
   - `npx vitest run tests/unit/auto-fix-promote-gate.test.js` exits 0.
   - `npm test` exits 0 (full 1134-test suite).

7. **YAML syntactic validity:**
   - Both workflow YAMLs parse cleanly via `python3 -c "import yaml; yaml.safe_load(...)"`.

8. **No deferred items leaked:**
   - No per-ERROR_CLASS thresholds in code.
   - No auto-close-failing-cases logic in code.
   - No transactional partial-promote logic in code.
   - No dry-run workflow mode (script-side dryRun arg for Vitest mockability ONLY).

</success_criteria>

<output>
Create `.planning/phases/53-auto-fix-partial-verified-semantics/53-01-SUMMARY.md` when all 3 commits land and all verification checks pass. The SUMMARY must include:
- All 4 PARTIAL-* IDs marked complete with the commit SHA closing each.
- The single structural deviation note: v40-verifier-gate.yml was re-opened in commit (c) for an additive marker tail; this is a structural necessity (cross-workflow data passing requires a PR-side persistent marker) and is NOT a PARTIAL-02 re-do.
- Byte-unchanged audit results for assertTripleGate body (D-05) AND the Apply auto-fix:verified label step (D-10).
- Vitest test count delta (new tests added in commits (a) and (c)).
- D-22 plan-checker outcome (referenced from `.planning/phases/53-auto-fix-partial-verified-semantics/53-01-PLAN-CHECK.md` if the checker ran).
</output>

## PLANNING COMPLETE
