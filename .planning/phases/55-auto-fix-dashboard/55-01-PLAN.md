---
phase: 55-auto-fix-dashboard
plan: 01
type: execute
wave: 1
depends_on: [54-01]
files_modified:
  - scripts/weekly-digest.mjs
  - tests/unit/weekly-digest-auto-fix.test.js
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/55-auto-fix-dashboard/55-01-SUMMARY.md
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]
tags: [dashboard, weekly-digest, auto-fix-metrics, summary-keys-pin]

must_haves:
  truths:
    # Truths derived goal-backward from Phase 55 SC-1..SC-3 + 25 D-NN decisions
    # (D-01..D-25 from 55-CONTEXT.md). Every decision is cited inline.

    # Data sources (D-01, D-02)
    - "Hybrid data source: cost from tests/e2e/.llm-spend-ledger.json (read via readLedger); PR-dependent metrics from a single read-only `gh search prs --label auto-fix:verified --label auto-fix:partial-verified --json number,state,mergedAt,createdAt,labels --limit 100` invocation (SC-1, D-01, D-02)"
    - "fetchAutoFixPrs RETURNS errors as `{prs: [...], fetchedAt, error}` — never throws; missing/unauthenticated `gh` degrades section to all `n/a` with stderr warning (D-02, D-15, D-18)"

    # combinedMonthlyTotalByTransport: pre-existing — IMPORT, not add (D-17 discovery clause)
    - "combinedMonthlyTotalByTransport is pre-existing at tests/e2e/lib/llm-ledger.js line 592 (returns `{combined: number, by_transport: {subscription, sdk, unknown}}`); Phase 55 IMPORTS it into scripts/weekly-digest.mjs alongside the existing readLedger/monthlyTotal/HARD_CAP_USD/LEDGER_PATH imports — no new helper is added (D-17 verified at plan time; planner inspected tests/e2e/lib/llm-ledger.js:592 and confirmed signature)"
    - "cost_per_fix uses `combinedMonthlyTotalByTransport(ledger, monthStr).combined / auto_fix_attempted` — `.combined` field is the de-duplicated month total (matches D-06 / SC-3 'not raw sum' invariant since per-issue subscription+SDK double-counting is prevented at the appendLedgerEntry level upstream)"

    # 7 metric keys VERBATIM in LOCKED order (D-03..D-09, SC-1)
    - "renderAutoFixPipelineSection emits the 7 metric keys in this VERBATIM order matching SC-1: (1) auto_fix_attempted, (2) verified_merged, (3) success_rate, (4) cost_per_fix, (5) time_to_merge_p50, (6) fix_attempts_p50, (7) flake_escalation_count (D-03, D-04, D-05, D-06, D-07, D-08, D-09)"
    - "auto_fix_attempted = count of PRs with label `auto-fix:verified` OR `auto-fix:partial-verified` regardless of merge state (D-03)"
    - "verified_merged = count of PRs with label `auto-fix:verified` AND `mergedAt !== null` (D-04)"
    - "success_rate = (verified_merged / auto_fix_attempted) * 100, formatted `XX.X%` (1-decimal); displays literal `n/a` when auto_fix_attempted === 0 (NOT 0% — distinct semantics) (D-05)"
    - "cost_per_fix = combined / auto_fix_attempted, formatted `$X.XXXX` (4-decimal USD); displays literal `n/a` when auto_fix_attempted === 0 OR combined is NaN/Infinity (D-06, SC-3)"
    - "time_to_merge_p50 = median(mergedAt − createdAt) computed ONLY over PRs with `mergedAt !== null` (filter pre-median); formatted `Xh Ym` for p50 ≥ 1h, `Xm` for p50 < 1h; displays literal `n/a` when filtered set is empty (D-07, SC-3)"
    - "fix_attempts_p50 = median count of distinct auto-fix PRs per `<!-- source_issue: N -->` body marker; PRs without a parseable source_issue marker are excluded from the p50 sample set; displays literal `n/a` when no parseable source_issue markers exist (D-08)"
    - "flake_escalation_count = count of PRs with BOTH labels `human-review-required` AND `auto-fix:partial-verified`; displays integer 0 (NOT `n/a`) when zero — count semantics tolerate the empty set (D-09)"

    # Section placement & structure (D-10, D-11)
    - "The Auto-Fix Pipeline section is appended in runDigest AFTER renderDigest returns (NOT inside renderDigest — which enforces a ≤50-line budget at line 290 that the new section would blow); the appended string is concatenated onto the markdown that is then written to reports/weekly-digest-YYYY-WNN.md and published via ghClient (D-10, D-16; planner-verified architectural constraint)"
    - "Section opens with `<details>\\n<summary>Auto-Fix Pipeline</summary>\\n\\n` and closes with `\\n</details>`; collapsed by default; markdown table has columns `| Metric | Value |` with all 7 rows in the verbatim order above (D-11)"
    - "Section includes a refresh timestamp line `_fetched <ISO-8601>_` before the table for auditability (Claude's discretion clause from 55-CONTEXT.md decisions §discretion)"

    # SUMMARY_KEYS BYTE-UNCHANGED invariant (D-12, D-13)
    - "tests/e2e/lib/llm-report.js is NOT in files_modified — Phase 55 does NOT touch it; the frozen SUMMARY_KEYS array (line 123, 7 elements: passed, wrong_citation, verifier_disagree, llm_hallucinated_selection, llm_api_error, harness_error, total_cost_usd) is byte-unchanged after the phase (D-12)"
    - "Auto-fix metrics live ONLY in the new <details> section — they are NOT pushed into SUMMARY_KEYS, NOT routed through aggregateBySummaryKey, NOT validated by validateSummaryKeys (D-13)"

    # Implementation surface (D-14..D-18)
    - "New pure-function export `renderAutoFixPipelineSection({ledger, ghPrs, now, monthlyTotalByTransport?})` in scripts/weekly-digest.mjs: takes data, returns markdown string; performs ZERO I/O inside; computes all 7 metrics with NaN/Infinity guards collapsing to literal `n/a` per D-05..D-09 fallbacks (D-14)"
    - "New function `fetchAutoFixPrs({now, execFn?})` in scripts/weekly-digest.mjs: wraps a single `gh search prs --label auto-fix:verified --label auto-fix:partial-verified --json number,state,mergedAt,createdAt,labels,body --limit 100` via child_process.execSync; injectable execFn for Vitest seam; on non-zero exit / parse error / `gh` missing → returns `{prs: [], fetchedAt: now, error: <stderr-msg>}` (D-15, D-18)"
    - "Note: `gh search prs --json` must include `body` so fix_attempts_p50 can parse the `<!-- source_issue: N -->` marker per D-08 (planner-amended field set)"
    - "runDigest (currently line 422) is extended at step (6.5) — AFTER `const md = renderDigest(...)` returns, BEFORE the file write at step (7): const ghPrsResult = fetchAutoFixPrs({now: nowDate}); if (ghPrsResult.error) process.stderr.write(`weekly-digest: auto-fix section degraded — ${ghPrsResult.error}\\n`); const autoFixSection = renderAutoFixPipelineSection({ledger: fs.existsSync(ledgerPath) ? readLedger(ledgerPath) : {months:{}}, ghPrs: ghPrsResult.prs, now: nowDate}); const finalMd = md + '\\n\\n' + autoFixSection; — finalMd then drives steps (7) and (8) (D-16)"
    - "scripts/weekly-digest.mjs adds the single new import `combinedMonthlyTotalByTransport` to the existing import group from `../tests/e2e/lib/llm-ledger.js` (lines ~39-44); no other imports change (D-17 verified)"

    # Vitest pins (D-19, D-20)
    - "tests/unit/weekly-digest.test.js does NOT exist at plan time (planner-verified: `wc -l tests/unit/weekly-digest.test.js` errored ENOENT); per D-20 fallback, the plan CREATES tests/unit/weekly-digest-auto-fix.test.js"
    - "Vitest assertion 1 (existing — re-verify): SUMMARY_KEYS.length === 7 — already pinned at tests/unit/llm-report.test.js:406; Phase 55 task 1 runs `npx vitest run tests/unit/llm-report.test.js -t \"SUMMARY_KEYS\"` and confirms PASS without modification (D-19.1)"
    - "Vitest assertion 2 (new): a rendered digest sample (renderAutoFixPipelineSection invoked with synthetic ledger + ghPrs fixtures) MUST contain the regex `<details>\\s*<summary>Auto-Fix Pipeline</summary>` (D-19.2)"
    - "Vitest assertion 3 (new): the rendered section MUST contain ALL 7 metric key strings via individual `toContain` assertions: auto_fix_attempted, verified_merged, success_rate, cost_per_fix, time_to_merge_p50, fix_attempts_p50, flake_escalation_count (D-19.3)"

    # SUMMARY_KEYS BYTE-UNCHANGED gate (acceptance — programmatic)
    - "Each commit-acceptance step runs `git diff HEAD~1 -- tests/e2e/lib/llm-report.js | wc -l` and asserts the result is 0 — proves SUMMARY_KEYS byte-unchanged across the phase (D-12 enforcement)"

    # Plan structure & commits (D-21, D-22, D-23, D-24, D-25)
    - "Single plan 55-01-PLAN.md with 3 sequential tasks in D-22 LOCKED order: T1 = feat(55) DASH-02 (helpers + Vitest) → T2 = feat(55) DASH-01+DASH-03 (runDigest wiring) → T3 = chore(55) closure (D-21, D-22)"
    - "Atomic commits per task: (a) `feat(55): DASH-02 — renderAutoFixPipelineSection + fetchAutoFixPrs + Vitest pins (SUMMARY_KEYS unchanged)`, (b) `feat(55): DASH-01 + DASH-03 — runDigest wires Auto-Fix Pipeline section (7 metrics, NaN-guarded, combinedMonthlyTotalByTransport, mergedAt filter)`, (c) `chore(55): T3 — closure (55-01-SUMMARY + STATE + ROADMAP)` (D-22)"
    - "All Phase 55 commits stay LOCAL — no `git push`; operator's v4.1 milestone-close batch PR (Phases 52-55) per Phase 52 D-25 + Phase 54 D-25 precedent (D-23)"
    - "Phase 55 is fully autonomous: zero checkpoint:human-verify, zero checkpoint:decision, zero checkpoint:human-action tasks (D-24)"
    - "gsd-plan-checker is NOT mandatory for Phase 55 — no STATE blocker cites it (D-25)"

    # Acceptance invariants
    - "`npm test` exits 0 after each of the 3 commits (no regression to existing test suite; the 2 pre-existing failures from Phase 53/54 baseline — llm-ledger.test.js Test 48 and v40-verifier-gate-yaml.test.js V2 — remain byte-identical and are documented in the SUMMARY)"
    - "Phase 55 produces zero live LLM cost (no SDK calls), zero infra mutation, exactly one read-only `gh search prs` per digest run (and only invoked at the end-to-end Vitest seam via injected execFn, not against the live repo during the phase)"
    - "Expectation documented in SUMMARY: at v4.1 ship time most metrics will display `n/a` because no live auto-fix runs have been merged yet — the wiring SHIPS in Phase 55; the data flows in once Phase 56's ledger-schema extension + first live UAT-47-a populate entries (T-55-05 INFO; not a defect)"

  artifacts:
    - path: "scripts/weekly-digest.mjs"
      provides: "Pre-existing host file extended additively: 2 new exported functions (renderAutoFixPipelineSection, fetchAutoFixPrs) + 1 new import (combinedMonthlyTotalByTransport) + runDigest step (6.5) appending the Auto-Fix Pipeline section to the rendered digest"
      exports: ["isoWeekLabel", "validateSummaryKeys", "aggregate", "renderCostLine", "renderDigest", "runDigest", "aggregateBySummaryKey", "renderAutoFixPipelineSection", "fetchAutoFixPrs"]
      contains: "renderAutoFixPipelineSection"
    - path: "tests/unit/weekly-digest-auto-fix.test.js"
      provides: "Vitest pins for D-19.2 (details/summary regex) + D-19.3 (7 metric keys present) + section degradation when fetchAutoFixPrs.error is set + cost_per_fix uses combined field"
      contains: "describe('Phase 55 DASH-01..03"
    - path: ".planning/phases/55-auto-fix-dashboard/55-01-SUMMARY.md"
      provides: "Phase closure record: 3 SC closure table; 25/25 decisions coverage; abstention reality note; commit list (3 hashes); test delta; pre-existing-failures byte-identity confirmation"
      contains: "Phase 55 — COMPLETE"
    - path: ".planning/STATE.md"
      provides: "Updated counters: completed_phases 8→9, percent 89→100; Current Position 'Phase 55 — COMPLETE / milestone v4.1 ready for closure'; milestone status flips to ready-for-closure"
      contains: "Phase 55 closed"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 55 line in v4.1 §Phases checklist toggled `[ ]` → `[x]` with completion date 2026-06-04; Progress table row updated to 1/1 Complete"
      contains: "[x] **Phase 55"

  key_links:
    - from: "scripts/weekly-digest.mjs runDigest step (6.5)"
      to: "scripts/weekly-digest.mjs renderAutoFixPipelineSection"
      via: "in-module function call after renderDigest returns; result concatenated onto md before fs.writeFileSync"
      pattern: "renderAutoFixPipelineSection\\("
    - from: "scripts/weekly-digest.mjs renderAutoFixPipelineSection cost_per_fix branch"
      to: "tests/e2e/lib/llm-ledger.js:combinedMonthlyTotalByTransport"
      via: "import in weekly-digest.mjs; call returns `{combined, by_transport}`; `.combined` field is the numerator for cost_per_fix"
      pattern: "combinedMonthlyTotalByTransport\\([^)]*\\)\\.combined"
    - from: "scripts/weekly-digest.mjs fetchAutoFixPrs"
      to: "external `gh search prs` CLI"
      via: "child_process.execSync (errors caught and RETURNED as {error: <msg>}, never thrown)"
      pattern: "gh search prs --label auto-fix:verified --label auto-fix:partial-verified"
    - from: "tests/unit/weekly-digest-auto-fix.test.js"
      to: "scripts/weekly-digest.mjs renderAutoFixPipelineSection"
      via: "import { renderAutoFixPipelineSection } from '../../scripts/weekly-digest.mjs'"
      pattern: "import.*renderAutoFixPipelineSection"
    - from: "Phase 55 acceptance gates"
      to: "tests/e2e/lib/llm-report.js SUMMARY_KEYS (byte-unchanged)"
      via: "git diff HEAD~1 -- tests/e2e/lib/llm-report.js | wc -l → must equal 0 after each commit"
      pattern: "git diff.*llm-report\\.js"
---

<objective>
Add a `<details>` collapsible "Auto-Fix Pipeline" section with 7 NaN/Infinity-guarded metrics to the weekly digest produced by `scripts/weekly-digest.mjs`, sourced from a hybrid (ledger + single `gh search prs` call) data pipeline. Preserve the frozen 7-element SUMMARY_KEYS contract from `tests/e2e/lib/llm-report.js` byte-for-byte. No new npm dependencies; no live LLM cost; no infra mutation.

Purpose: closes DASH-01, DASH-02, DASH-03 — the final non-bookkeeping requirement of milestone v4.1. The wiring ships now; data populates as Phase 56 lands the ledger-schema extension and live UATs accumulate.

Output:
- scripts/weekly-digest.mjs: +1 import (combinedMonthlyTotalByTransport), +2 exported functions (renderAutoFixPipelineSection, fetchAutoFixPrs), +1 runDigest section append (step 6.5)
- tests/unit/weekly-digest-auto-fix.test.js: new Vitest file with the 3 D-19 assertions
- 55-01-SUMMARY.md (closure), STATE.md + ROADMAP.md (Phase 55 toggled complete)
- 3 atomic LOCAL commits (2 feat(55) + 1 chore(55)); no `git push`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-auto-fix-dashboard/55-CONTEXT.md
@.planning/phases/54-multi-model-a-b/54-01-SUMMARY.md

# Host file being extended
@scripts/weekly-digest.mjs

# Invariant guard (NOT modified — re-verified)
@tests/e2e/lib/llm-report.js

# Existing helper being IMPORTED (NOT added)
@tests/e2e/lib/llm-ledger.js

# Existing Vitest pin for SUMMARY_KEYS.length === 7
@tests/unit/llm-report.test.js

<interfaces>
<!-- Pre-existing exports executors will consume directly. Extracted at plan time. -->
<!-- Source: planner-verified `wc -l` + grep against the working tree on 2026-06-04. -->

From `tests/e2e/lib/llm-report.js` (line 123) — DO NOT MODIFY:
```javascript
export const SUMMARY_KEYS = Object.freeze([
  'passed',
  'wrong_citation',
  'verifier_disagree',
  'llm_hallucinated_selection',
  'llm_api_error',
  'harness_error',
  'total_cost_usd',
]);
```

From `tests/e2e/lib/llm-ledger.js` (line 592) — IMPORT into weekly-digest.mjs:
```javascript
/**
 * @returns {{ combined: number, by_transport: { subscription: number, sdk: number, unknown: number } }}
 */
export function combinedMonthlyTotalByTransport(ledger, month = currentMonth());
```
Use `.combined` for the cost_per_fix numerator. `bucket.total_usd` is already de-duplicated at appendLedgerEntry — the "not raw sum" semantic in D-06 / SC-3 is satisfied by reading `.combined`.

From `scripts/weekly-digest.mjs` — surfaces to extend:
- Line 19-44: import block (existing imports from llm-ledger.js — add `combinedMonthlyTotalByTransport` to this group)
- Line 224 `renderCostLine` — UNCHANGED
- Line 249 `renderDigest` — UNCHANGED (enforces a ≤50-line budget at line 290; the new section would blow it — that is why D-16 directs the append into `runDigest`, NOT `renderDigest`)
- Line 422 `runDigest` — extended at step (6.5): AFTER `const md = renderDigest(...)` returns, BEFORE `fs.writeFileSync(reportPath, md)` at step (7), compute and append the Auto-Fix Pipeline section to `md`, then write/publish the combined string

From `tests/unit/llm-report.test.js` (line 406):
```javascript
expect(SUMMARY_KEYS.length).toBe(7);
```
Already passing — Phase 55 task 1 re-runs it as a regression gate; no new assertion needed for D-19.1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: feat(55) DASH-02 — renderAutoFixPipelineSection + fetchAutoFixPrs + Vitest pins (SUMMARY_KEYS unchanged)</name>
  <files>scripts/weekly-digest.mjs, tests/unit/weekly-digest-auto-fix.test.js</files>
  <behavior>
    - renderAutoFixPipelineSection({ledger, ghPrs, now}) returns a markdown string opening with `<details>\n<summary>Auto-Fix Pipeline</summary>\n\n_fetched <ISO>_\n\n| Metric | Value |\n| --- | --- |\n` and closing with `\n</details>`
    - All 7 metric rows present in LOCKED order (D-03..D-09): auto_fix_attempted, verified_merged, success_rate, cost_per_fix, time_to_merge_p50, fix_attempts_p50, flake_escalation_count
    - Empty ghPrs array → auto_fix_attempted=0, verified_merged=0, success_rate=n/a, cost_per_fix=n/a, time_to_merge_p50=n/a, fix_attempts_p50=n/a, flake_escalation_count=0 (per D-05..D-09 fallbacks; D-09 keeps 0, NOT n/a, for count semantics)
    - success_rate displays `n/a` when auto_fix_attempted===0 (not 0%)
    - cost_per_fix uses combinedMonthlyTotalByTransport(ledger, monthFromNow).combined / auto_fix_attempted; displays `n/a` when auto_fix_attempted===0 OR combined is NaN/Infinity
    - time_to_merge_p50: median over PRs with mergedAt !== null ONLY; `Xh Ym` format for p50 ≥ 60min, `Xm` for p50 < 60min
    - fix_attempts_p50: parse `<!-- source_issue: (\d+) -->` from each PR body; group by issue id; median count per distinct issue; n/a when no parseable markers
    - flake_escalation_count: count of PRs with BOTH labels `human-review-required` AND `auto-fix:partial-verified`
    - fetchAutoFixPrs({now, execFn?}) RETURNS `{prs: Array, fetchedAt: now, error: string|null}` — never throws; on execFn throw / non-JSON output / `gh` missing → returns `{prs: [], fetchedAt: now, error: <stderr message>}`
    - fetchAutoFixPrs invokes the literal command `gh search prs --label auto-fix:verified --label auto-fix:partial-verified --json number,state,mergedAt,createdAt,labels,body --limit 100` via the injected execFn (default: a thin wrapper around child_process.execSync)
    - Vitest Test 1 (D-19.1 regression gate): re-runs `expect(SUMMARY_KEYS.length).toBe(7)` import-and-assert; this test does NOT live in weekly-digest-auto-fix.test.js (it already lives in tests/unit/llm-report.test.js:406) — the task runs `npx vitest run tests/unit/llm-report.test.js -t "SUMMARY_KEYS"` as part of its automated verify step
    - Vitest Test 2 (D-19.2): rendered section matches regex `/<details>\s*<summary>Auto-Fix Pipeline<\/summary>/`
    - Vitest Test 3 (D-19.3): rendered section contains all 7 metric key strings via individual toContain assertions
    - Vitest Test 4 (degradation): fetchAutoFixPrs with an execFn that throws returns `{prs: [], error: <truthy>}` and renderAutoFixPipelineSection called with that result yields a section whose values column is all `n/a` (except flake_escalation_count = 0)
    - Vitest Test 5 (cost_per_fix invariant): a synthetic ledger with bucket.total_usd=2.40 + auto_fix_attempted=4 → cost_per_fix === `$0.6000`; verifies the `.combined` field is the numerator (not a raw iteration sum)
  </behavior>
  <action>
    Implement the two new pure-ish exports in scripts/weekly-digest.mjs and the Vitest pins per D-14, D-15, D-18, D-19, D-20. Add ONE new import to the existing import group from `../tests/e2e/lib/llm-ledger.js` at lines ~39-44: append `combinedMonthlyTotalByTransport,` to that import group — do not introduce a separate import statement (per D-17, planner-verified the function exists at llm-ledger.js:592). Place the two new functions near the bottom of weekly-digest.mjs, BEFORE the `isMain` guard at line 497, in this order: (1) renderAutoFixPipelineSection, (2) fetchAutoFixPrs. renderAutoFixPipelineSection is a PURE function — zero I/O inside; the caller supplies ledger + ghPrs. fetchAutoFixPrs accepts an injectable `execFn` parameter defaulting to a closure wrapping child_process.execSync — Vitest tests pass a fake execFn for determinism (no live `gh` calls during testing). On execFn throw, JSON.parse throw, or non-array JSON payload, fetchAutoFixPrs catches and returns `{prs: [], fetchedAt: now, error: String(err.message ?? err)}` per D-15. fetchAutoFixPrs MUST NOT process.stderr.write — let the caller (runDigest, Task 2) do the stderr warning per D-16. Inside renderAutoFixPipelineSection, derive `month` from `now.toISOString().slice(0,7)` and call `combinedMonthlyTotalByTransport(ledger, month).combined` — use `.combined` (not a manual sum across by_transport buckets) so cost_per_fix already reflects the appendLedgerEntry-side de-duplication that satisfies D-06 / SC-3. All NaN/Infinity guards (auto_fix_attempted===0, combined non-finite, empty merged-PR set, empty source_issue-marker set) collapse to the literal string `n/a` per D-05..D-08; D-09's flake_escalation_count keeps integer 0 on empty. Section markdown structure per D-11. Include `_fetched ${nowDate.toISOString()}_` line before the table (Claude's-discretion auditability clause). Create tests/unit/weekly-digest-auto-fix.test.js at the project's standard Vitest path with a single `describe('Phase 55 DASH-01..03 — Auto-Fix Pipeline section', ...)` block and the 4 inline tests (Tests 2..5; Test 1 lives in the existing llm-report.test.js and is gated separately). Use synthetic fixtures inline — do NOT read the real committed ledger. Do NOT add Test 1 here (re-running the existing assertion in two files is duplication that drifts; the verify step runs the existing test by name). Acceptance verify steps run `npx vitest run tests/unit/weekly-digest-auto-fix.test.js` → exits 0; `npx vitest run tests/unit/llm-report.test.js -t "SUMMARY_KEYS"` → exits 0; `git diff HEAD -- tests/e2e/lib/llm-report.js | wc -l` → outputs 0 (proves SUMMARY_KEYS file BYTE-UNCHANGED per D-12). Stage exactly two files (scripts/weekly-digest.mjs, tests/unit/weekly-digest-auto-fix.test.js) and commit with the LOCKED D-22 (a) message: `feat(55): DASH-02 — renderAutoFixPipelineSection + fetchAutoFixPrs + Vitest pins (SUMMARY_KEYS unchanged)`. Do NOT push (D-23).
  </action>
  <verify>
    <automated>
npx vitest run tests/unit/weekly-digest-auto-fix.test.js \
  && npx vitest run tests/unit/llm-report.test.js -t "SUMMARY_KEYS" \
  && [ "$(git diff HEAD~1 -- tests/e2e/lib/llm-report.js | wc -l)" = "0" ] \
  && grep -c "export function renderAutoFixPipelineSection" scripts/weekly-digest.mjs \
  && grep -c "export function fetchAutoFixPrs" scripts/weekly-digest.mjs \
  && grep -c "combinedMonthlyTotalByTransport" scripts/weekly-digest.mjs \
  && npm test
    </automated>
  </verify>
  <done>
    - scripts/weekly-digest.mjs exports renderAutoFixPipelineSection and fetchAutoFixPrs
    - combinedMonthlyTotalByTransport imported from ../tests/e2e/lib/llm-ledger.js
    - tests/unit/weekly-digest-auto-fix.test.js exists with 4 tests, all passing
    - SUMMARY_KEYS.length === 7 assertion still passes (no changes to llm-report.js)
    - git diff against HEAD~1 on llm-report.js shows zero lines
    - `npm test` exits 0 (modulo the 2 pre-existing failures byte-identical to Phase 54 baseline: llm-ledger.test.js Test 48 + v40-verifier-gate-yaml.test.js V2 — both documented at Phase 53/54 SUMMARY and NOT introduced by this phase)
    - Commit (a) `feat(55): DASH-02 — renderAutoFixPipelineSection + fetchAutoFixPrs + Vitest pins (SUMMARY_KEYS unchanged)` lands LOCALLY
  </done>
</task>

<task type="auto">
  <name>Task 2: feat(55) DASH-01 + DASH-03 — runDigest wires Auto-Fix Pipeline section (7 metrics, NaN-guarded, combinedMonthlyTotalByTransport, mergedAt filter)</name>
  <files>scripts/weekly-digest.mjs</files>
  <action>
    Wire `runDigest` (currently at line 422) to call the two new helpers from Task 1 and append the rendered section to the digest markdown BEFORE the file write at existing step (7). The edit inserts a new step (6.5) directly AFTER existing line 462's `const md = renderDigest({ weekLabel, agg, costLine, now: nowDate });` and BEFORE existing line 464's `fs.mkdirSync(reportsDir, { recursive: true });`. The inserted code: (1) call `const ghPrsResult = fetchAutoFixPrs({ now: nowDate });` (note: fetchAutoFixPrs is the function from Task 1 — invoked WITHOUT an execFn override in the runDigest production path, so it uses the default child_process.execSync wrapper that actually shells out to `gh`); (2) if `ghPrsResult.error !== null` → emit `process.stderr.write(\`weekly-digest: auto-fix section degraded — ${ghPrsResult.error}\n\`);` per D-16; (3) compute `const autoFixLedger = fs.existsSync(ledgerPath) ? readLedger(ledgerPath) : { months: {} };` reusing the existing ledgerPath variable already in scope from step (3); (4) call `const autoFixSection = renderAutoFixPipelineSection({ ledger: autoFixLedger, ghPrs: ghPrsResult.prs, now: nowDate });` and (5) rebind the markdown via `const finalMd = md + '\n\n' + autoFixSection;` and (6) change the two downstream references at existing line 467 (`fs.writeFileSync(reportPath, md)`) and existing line 477 (`ghClient.createDigestIssue(title, md)`) plus line 474's `ghClient.createDiscussion(title, md)` from `md` → `finalMd`. The renderDigest call at line 462 is NOT modified — its ≤50-line budget invariant from line 290 is preserved (the new section is concatenated AFTER renderDigest's own return). The cost_per_fix invariant from D-06 / SC-3 is preserved INSIDE renderAutoFixPipelineSection (Task 1) which uses `combinedMonthlyTotalByTransport(ledger, month).combined` — not a raw sum; the time_to_merge filter (mergedAt !== null) is also enforced INSIDE renderAutoFixPipelineSection. Task 2's responsibility is wiring, not metric math. Extend tests/unit/weekly-digest-auto-fix.test.js with ONE additional integration-style test: invoke `runDigest({ ghClient: <fake>, ledgerPath: <synthetic temp>, reportsDir: <tmp>, now: () => new Date('2026-06-04T12:00:00Z'), publishMode: 'issue' })` with a fake ghClient (listOpenIssuesByLabel→[], hasDiscussions→false, createDigestIssue→capture title+body); assert the captured body contains `<details>` AND `<summary>Auto-Fix Pipeline</summary>` AND all 7 metric keys (loop expect.toContain over the 7 strings). Since fetchAutoFixPrs invoked from inside runDigest WILL try to shell out to `gh`, the test EITHER (a) mocks the global by injecting an execFn through a new optional opts.fetchAutoFixPrs hook on runDigest, OR (b) accepts the real `gh` call result (likely an error in a local CI environment, which degrades the section to all-n/a — still satisfies the "all 7 metric keys present" assertion since the keys appear in the rendered table regardless of values). Recommended: introduce an `opts.fetchAutoFixPrs` hook on runDigest (defaulting to the production fetchAutoFixPrs from Task 1) so the integration test can pass a fake that returns `{prs: [<one synthetic verified-merged PR>], fetchedAt: now, error: null}` deterministically — this also strengthens runDigest's testability. The hook follows the existing `opts.ghClient` injected-deps pattern at line 426. Acceptance: `npx vitest run tests/unit/weekly-digest-auto-fix.test.js` passes (now 5 tests including the new integration test); a sanity render via a one-off `node -e` snippet confirms the section appears AFTER the existing digest body in the captured issue payload (the test asserts this via string index ordering: `body.indexOf('## Classification Breakdown') < body.indexOf('<summary>Auto-Fix Pipeline</summary>')`); `git diff HEAD~1 -- tests/e2e/lib/llm-report.js | wc -l` → 0 (still byte-unchanged per D-12); `npm test` exits 0. Stage scripts/weekly-digest.mjs AND tests/unit/weekly-digest-auto-fix.test.js, commit with the LOCKED D-22 (b) message: `feat(55): DASH-01 + DASH-03 — runDigest wires Auto-Fix Pipeline section (7 metrics, NaN-guarded, combinedMonthlyTotalByTransport, mergedAt filter)`. Do NOT push.
  </action>
  <verify>
    <automated>
npx vitest run tests/unit/weekly-digest-auto-fix.test.js \
  && [ "$(git diff HEAD~1 -- tests/e2e/lib/llm-report.js | wc -l)" = "0" ] \
  && grep -c "fetchAutoFixPrs({ now: nowDate })" scripts/weekly-digest.mjs \
  && grep -c "renderAutoFixPipelineSection({" scripts/weekly-digest.mjs \
  && grep -c "finalMd" scripts/weekly-digest.mjs \
  && npm test
    </automated>
  </verify>
  <done>
    - runDigest at scripts/weekly-digest.mjs step (6.5) calls fetchAutoFixPrs + renderAutoFixPipelineSection and writes/publishes the combined markdown (finalMd)
    - The 3 downstream `md` references (fs.writeFileSync + createDiscussion + createDigestIssue) are rewritten to `finalMd`
    - The renderDigest ≤50-line budget at line 290 is preserved (renderDigest itself is unchanged)
    - tests/unit/weekly-digest-auto-fix.test.js now has 5 tests including the runDigest integration test; all pass
    - The Auto-Fix Pipeline section appears AFTER `## Classification Breakdown` in the rendered digest body (string-index ordering asserted)
    - cost_per_fix invariant preserved (combinedMonthlyTotalByTransport().combined; from Task 1)
    - time_to_merge_p50 filter preserved (mergedAt !== null; from Task 1)
    - SUMMARY_KEYS byte-unchanged (git diff zero lines)
    - `npm test` exits 0 (same byte-identical pre-existing failures only)
    - Commit (b) `feat(55): DASH-01 + DASH-03 — runDigest wires Auto-Fix Pipeline section (7 metrics, NaN-guarded, combinedMonthlyTotalByTransport, mergedAt filter)` lands LOCALLY
  </done>
</task>

<task type="auto">
  <name>Task 3: chore(55) — closure (55-01-SUMMARY + STATE + ROADMAP)</name>
  <files>.planning/phases/55-auto-fix-dashboard/55-01-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    Write the Phase 55 closure SUMMARY and update STATE + ROADMAP. (1) Create `.planning/phases/55-auto-fix-dashboard/55-01-SUMMARY.md` with the standard get-shit-done summary structure: frontmatter (phase: 55-auto-fix-dashboard, plan: 01, status: complete, completed_at: 2026-06-04, requirements_closed: [DASH-01, DASH-02, DASH-03]); a 3-row SC closure table mapping SC-1/SC-2/SC-3 → status:CLOSED → evidence (file:line refs + Vitest test names + git diff line counts); a 25-row decisions coverage table mapping D-01..D-25 → status:HONORED → realized-in (task number + commit hash placeholder); a "Commits" section listing the 3 actual hashes from Tasks 1, 2, and this commit; a "Test delta" section noting `+5 new Vitest assertions in tests/unit/weekly-digest-auto-fix.test.js`; a "Pre-existing failures" section confirming the 2 known failures (llm-ledger.test.js Test 48 + v40-verifier-gate-yaml.test.js V2) remain byte-identical to the Phase 54 baseline (NOT introduced by Phase 55); a "T-55-05 INFO disposition" section explicitly stating that at ship time most metric VALUES will display `n/a` because no live auto-fix runs have been merged on origin/main yet — the wiring ships, the data populates as Phase 56's ledger-schema extension + the first live UAT-47-a accumulate entries; an "Out of scope confirmed deferred" section enumerating the 6 items from CONTEXT.md §Out of scope; the Phase 56 follow-up note carrying forward from Phase 54 (ledger schema extension for errorClass + outcome — UNCHANGED, no new Phase 56 items added by Phase 55). (2) Update `.planning/STATE.md` frontmatter: `progress.completed_phases: 8 → 9`, `progress.completed_plans: 8 → 9`, `progress.percent: 89 → 100`, `status: completed → completed` (unchanged at top — milestone v4.1 itself is not closed until operator's batch-push lands; status remains `completed` per milestone definition because all phases ARE planned/coded; but mark the milestone-close PR step in Pending Todos). Update `stopped_at`, `last_updated: "2026-06-04T<HH:MM>:00Z"`, `last_activity: "2026-06-04 -- Phase 55 marked complete"`. Update `Current Position` to `Phase: 55 — COMPLETE / Plan: 1 of 1 / Status: Phase 55 complete — v4.1 ready for milestone-close batch PR (Phases 52-55 + Phase 49 commits LOCAL → push)`. Update the Progress bar to `[██████████] 100%`. Add a `### Decisions` row: `[Phase 55]: Closure (2026-06-04): all 3 DASH-* REQs CLOSED. 3 atomic commits LOCAL in D-22 LOCKED order: (a) <hash> feat(55) DASH-02 helpers+Vitest → (b) <hash> feat(55) DASH-01+DASH-03 runDigest wiring → (c) <hash> chore(55) closure. scripts/weekly-digest.mjs: +1 import (combinedMonthlyTotalByTransport from llm-ledger.js:592), +2 exports (renderAutoFixPipelineSection, fetchAutoFixPrs), +1 runDigest step (6.5). 5 new Vitest assertions in tests/unit/weekly-digest-auto-fix.test.js. SUMMARY_KEYS byte-unchanged (git diff zero lines verified after each commit; D-12 enforced). renderDigest ≤50-line budget preserved (section appended AFTER renderDigest returns, in runDigest, per D-16). Expectation per T-55-05: most metric VALUES display `n/a` at ship time because no live auto-fix runs merged yet — wiring ships in Phase 55; data populates from Phase 56 ledger-schema extension + first live UAT-47-a. autonomous: true; gsd-plan-checker NOT mandatory per D-25. Commits stay LOCAL per D-23 — operator's v4.1 milestone-close batch PR covers Phases 52-55. v4.1 ready for closure.`. (3) Update `.planning/ROADMAP.md`: toggle the Phase 55 checkbox from `[ ] **Phase 55: Auto-Fix Dashboard**` to `[x] **Phase 55: Auto-Fix Dashboard** ... (completed 2026-06-04)` in the v4.1 §Phases checklist (line ~165). In the §Phase Details §Phase 55 section, update `**Plans**: TBD` → `**Plans**: 1 plan` and the Plans subsection row to `[x] 55-01-PLAN.md — 3-task plan completed 2026-06-04 in LOCKED D-22 order: (a) <hash> feat(55) DASH-02 helpers + Vitest → (b) <hash> feat(55) DASH-01+DASH-03 runDigest wiring → (c) <hash> chore(55) closure. All 3 DASH-* CLOSED. SUMMARY_KEYS byte-unchanged. autonomous: true.`. In the Progress table, change the `55. Auto-Fix Dashboard | v4.1 | 0/TBD | Not started | -` row to `55. Auto-Fix Dashboard | v4.1 | 1/1 | Complete | 2026-06-04`. In the §Milestones header, flip the v4.1 line from `🚧 **v4.1 Readiness Gate + Push** — Phases 48-55 (in progress)` to `🚧 **v4.1 Readiness Gate + Push** — Phases 48-55 (ready for milestone-close batch PR — all phases complete LOCAL; push pending)` — DO NOT mark v4.1 SHIPPED with a ✅ until the operator's batch-push PR actually merges on origin/main; that is a future lifecycle event, not Phase 55's responsibility. Stage the 3 closure files and commit with the LOCKED D-22 (c) message: `chore(55): T3 — closure (55-01-SUMMARY + STATE + ROADMAP)`. DO NOT push (D-23).
  </action>
  <verify>
    <automated>
test -f .planning/phases/55-auto-fix-dashboard/55-01-SUMMARY.md \
  && grep -c "status: complete" .planning/phases/55-auto-fix-dashboard/55-01-SUMMARY.md \
  && grep -c "DASH-01.*CLOSED" .planning/phases/55-auto-fix-dashboard/55-01-SUMMARY.md \
  && grep -c "DASH-02.*CLOSED" .planning/phases/55-auto-fix-dashboard/55-01-SUMMARY.md \
  && grep -c "DASH-03.*CLOSED" .planning/phases/55-auto-fix-dashboard/55-01-SUMMARY.md \
  && grep -c "completed_phases: 9" .planning/STATE.md \
  && grep -c "percent: 100" .planning/STATE.md \
  && grep -c "\[x\] \*\*Phase 55" .planning/ROADMAP.md \
  && grep -c "55. Auto-Fix Dashboard | v4.1 | 1/1 | Complete | 2026-06-04" .planning/ROADMAP.md \
  && git log --oneline -1 | grep -c "chore(55): T3 — closure"
    </automated>
  </verify>
  <done>
    - 55-01-SUMMARY.md exists with `status: complete`, 3-row SC closure table, 25-row decisions coverage table, commit list, test delta, abstention-reality T-55-05 note
    - STATE.md: completed_phases=9, completed_plans=9, percent=100; Current Position reflects Phase 55 complete; Decisions log appended with Phase 55 closure entry; milestone v4.1 marked ready-for-closure-batch-PR (not yet shipped on origin)
    - ROADMAP.md: Phase 55 checkbox `[x]`; Phase Details §Phase 55 has `**Plans**: 1 plan` and 1/1 Complete row; v4.1 milestone header still 🚧 (not ✅ — push pending)
    - Commit (c) `chore(55): T3 — closure (55-01-SUMMARY + STATE + ROADMAP)` lands LOCALLY
    - No `git push` executed (verify via `git log origin/main..HEAD --oneline | wc -l` shows ≥3 unpushed commits including the 3 from Phase 55; the milestone batch PR is a separate operator action)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| weekly-digest.mjs → external `gh` CLI | Single read-only `gh search prs` call; no API write/mutation; errors RETURNED not thrown |
| weekly-digest.mjs → tests/e2e/lib/llm-report.js | One-way import of SUMMARY_KEYS only; Phase 55 does NOT modify llm-report.js (D-12 invariant) |
| weekly-digest.mjs → tests/e2e/lib/llm-ledger.js | One-way import of combinedMonthlyTotalByTransport (pre-existing function; no schema mutation) |
| renderAutoFixPipelineSection (pure) → caller | Function performs zero I/O; all data passed in by caller; outputs deterministic markdown string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | Tampering | tests/e2e/lib/llm-report.js SUMMARY_KEYS frozen array | mitigate | D-12 invariant: file not in files_modified; Vitest pin at llm-report.test.js:406 (SUMMARY_KEYS.length === 7) gates each commit; programmatic acceptance gate `git diff HEAD~1 -- tests/e2e/lib/llm-report.js \| wc -l` must equal 0 after each Phase 55 commit (D-19.1) |
| T-55-02 | Denial of Service | rendered digest markdown when metrics are NaN/Infinity | mitigate | D-05..D-09 explicit fallback to literal `n/a` for success_rate / cost_per_fix / time_to_merge_p50 / fix_attempts_p50; D-09 keeps integer 0 for flake_escalation_count (count semantics tolerate empty); Vitest Test 4 from Task 1 pins the degradation path with an execFn that throws |
| T-55-03 | Denial of Service | weekly-digest runtime when `gh` is unavailable or unauthenticated | mitigate | D-15: fetchAutoFixPrs returns `{prs:[], error:<msg>}` instead of throwing; D-16: runDigest emits stderr warning and renders section with all `n/a`; the rest of the digest (cost line, classification breakdown, top-3, etc.) still ships — `gh` outage does NOT block the digest |
| T-55-04 | Information Disclosure | cost_per_fix value double-counting subscription + SDK transports for the same issue | mitigate | D-06 / SC-3: use `combinedMonthlyTotalByTransport(ledger, month).combined` — the `.combined` field is sourced from `bucket.total_usd` which is incremented at appendLedgerEntry time with the de-duplication discipline already in place upstream; Vitest Test 5 from Task 1 pins the formula (synthetic ledger total 2.40 / 4 attempts === $0.6000) |
| T-55-05 | (INFO) | metric value semantics at v4.1 ship time | accept | At ship time most VALUES will display `n/a` because no live auto-fix runs have been merged on origin/main yet; wiring ships in Phase 55, data populates from Phase 56's ledger-schema extension + the first live UAT-47-a; SUMMARY.md documents this explicitly as expected behavior, not a defect |

Note: No `[ASSUMED]` / `[SUS]` / `[SLOP]` package installs in this phase (zero new npm dependencies — RESEARCH.md package-legitimacy-audit gate not invoked). No live LLM cost; no infra mutation; one read-only `gh search` per digest invocation. autonomous: true (D-24); no checkpoint:human-verify gates per D-25.
</threat_model>

<verification>
## Phase-level checks (run after Task 3 commits)

1. **SUMMARY_KEYS BYTE-UNCHANGED across all 3 commits (D-12):**
   - `git diff HEAD~3 -- tests/e2e/lib/llm-report.js | wc -l` MUST return 0
2. **All 3 SCs closed:**
   - SC-1: `grep -c "<details>" scripts/weekly-digest.mjs && grep -c "<summary>Auto-Fix Pipeline</summary>" scripts/weekly-digest.mjs` — both ≥ 1
   - SC-1: All 7 metric key strings appear in renderAutoFixPipelineSection's emitted markdown (covered by Vitest Test 3)
   - SC-2: `npx vitest run tests/unit/llm-report.test.js -t "SUMMARY_KEYS"` exits 0
   - SC-3: `grep -c "combinedMonthlyTotalByTransport" scripts/weekly-digest.mjs && grep -c "mergedAt !== null" scripts/weekly-digest.mjs` — both ≥ 1
3. **No accidental scope creep:**
   - `git diff HEAD~3 --stat | grep -v "55-01" | grep -v "weekly-digest" | grep -v "STATE\|ROADMAP\|SUMMARY"` MUST be empty
4. **Commits LOCAL only (D-23):**
   - `git log origin/main..HEAD --oneline | wc -l` reports ≥ 3 unpushed commits (the Phase 55 batch is part of the larger milestone-close push set, not its own push)
5. **`npm test` exits 0** (modulo the 2 documented pre-existing failures; their failure outputs are byte-identical to the Phase 54 SUMMARY baseline)
6. **autonomous: true** — no checkpoint task encountered during execution (D-24)
</verification>

<success_criteria>
**Phase 55 is COMPLETE when ALL of the following are TRUE:**

1. `scripts/weekly-digest.mjs` exports `renderAutoFixPipelineSection` and `fetchAutoFixPrs`; imports `combinedMonthlyTotalByTransport` from `../tests/e2e/lib/llm-ledger.js`; `runDigest` step (6.5) appends the Auto-Fix Pipeline section to the digest markdown AFTER `renderDigest` returns and BEFORE `fs.writeFileSync` writes the report file — DASH-01 closed.

2. `tests/e2e/lib/llm-report.js` is byte-unchanged (`git diff HEAD~3 -- tests/e2e/lib/llm-report.js | wc -l` returns 0); the existing `SUMMARY_KEYS.length === 7` Vitest assertion at `tests/unit/llm-report.test.js:406` still passes; auto-fix metrics live ONLY in the new `<details>` section, NOT in SUMMARY_KEYS — DASH-02 closed.

3. `renderAutoFixPipelineSection` computes `cost_per_fix` as `combinedMonthlyTotalByTransport(ledger, month).combined / auto_fix_attempted` (not a raw iteration sum); `time_to_merge_p50` is computed ONLY over PRs with `mergedAt !== null`; all 7 metrics are NaN/Infinity-guarded with literal `n/a` fallbacks per D-05..D-09 — DASH-03 closed.

4. `tests/unit/weekly-digest-auto-fix.test.js` exists with 5 passing Vitest assertions (4 from Task 1 + 1 integration test from Task 2); D-19's 3 LOCKED assertions are satisfied (Test 1 = re-run of existing SUMMARY_KEYS test; Tests 2-3 = details/summary regex + 7 metric keys present in rendered output).

5. 3 atomic commits land LOCALLY in D-22 LOCKED order: (a) `feat(55): DASH-02 — ...`, (b) `feat(55): DASH-01 + DASH-03 — ...`, (c) `chore(55): T3 — closure ...`. `git log origin/main..HEAD --oneline` lists the 3 hashes among the unpushed batch. No `git push` executed during Phase 55 (D-23).

6. `.planning/STATE.md` reflects `completed_phases: 9`, `completed_plans: 9`, `percent: 100`; Current Position records Phase 55 complete; Decisions log has the Phase 55 closure entry.

7. `.planning/ROADMAP.md` Phase 55 checkbox is `[x] **Phase 55** ... (completed 2026-06-04)`; the Plans subsection shows the 1/1 entry; the milestone-level v4.1 header stays 🚧 (not ✅) until the operator's batch-push PR merges on origin/main.

8. `.planning/phases/55-auto-fix-dashboard/55-01-SUMMARY.md` exists with the 3-row SC closure table, 25-row decisions coverage (D-01..D-25 all HONORED), commit hash list, test delta, T-55-05 INFO disposition documenting the "most values display n/a at ship" expectation.

9. `npm test` exits 0 after each commit (the 2 pre-existing failures byte-identical to Phase 54 baseline are documented in the SUMMARY and NOT regressions introduced by Phase 55).
</success_criteria>

<output>
Create `.planning/phases/55-auto-fix-dashboard/55-01-SUMMARY.md` at Task 3.
</output>
