---
phase: 57-ledger-commit-branch-redirect
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/v40-cost-ledger-snapshot.yml
  - .github/workflows/v40-verifier-gate.yml
  - tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js
autonomous: true
requirements: [COMMIT-01, COMMIT-02, COMMIT-03, COMMIT-04]

must_haves:
  truths:
    - "v40-cost-ledger-snapshot.yml pushes to ledger-snapshots/daily-${SNAPSHOT_DATE} branch — git log on origin after first post-merge cron run shows a commit on a ledger-snapshots/ branch, not main"
    - "A ledger-snapshot PR (non-auto-fix/* head ref) opened against main gets SUCCESS verdict from all verifier-gate jobs via the diff-guard scope-decision fast-path — no FORBIDDEN_PATHS rejection"
    - "grep -c 'git push origin main' .github/workflows/v40-auto-fix.yml equals 1 after the refactor commit — the two-commit split in v40-auto-fix.yml is byte-unchanged"
    - "The S13 Vitest YAML contract case in tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js passes with a positive assertion pinning the ledger-snapshots/ branch prefix"
  artifacts:
    - path: ".github/workflows/v40-cost-ledger-snapshot.yml"
      provides: "Daily ledger snapshot push refspec targeting ledger-snapshots/daily-${SNAPSHOT_DATE}"
      contains: "git push origin HEAD:ledger-snapshots/daily-${{ env.SNAPSHOT_DATE }}"
    - path: ".github/workflows/v40-verifier-gate.yml"
      provides: "diff-guard job Scope decision fast-path step"
      contains: "Scope decision (auto-fix/* PRs only; fast-path SUCCESS otherwise)"
    - path: "tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js"
      provides: "S8 positive pin on ledger-snapshots/ prefix + S13 ceiling raised to 6 + Phase 57 invariants describe block"
      contains: "Phase 57 invariants"
  key_links:
    - from: ".github/workflows/v40-cost-ledger-snapshot.yml line 91"
      to: "ledger-snapshots/daily-${SNAPSHOT_DATE} branch"
      via: "git push origin HEAD:<branch> refspec"
      pattern: "git push origin HEAD:ledger-snapshots/daily-\\$\\{\\{ env.SNAPSHOT_DATE \\}\\}"
    - from: ".github/workflows/v40-verifier-gate.yml diff-guard job"
      to: "scope decision fast-path"
      via: "if [[ \"${{ github.head_ref }}\" == auto-fix/* ]] shell guard + steps.scope.outputs.active gating"
      pattern: "Scope decision \\(auto-fix/\\* PRs only"
    - from: "tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js"
      to: ".github/workflows/v40-auto-fix.yml byte-unchanged invariant"
      via: "execSync grep -c assertion"
      pattern: "grep -c 'git push origin main' \\.github/workflows/v40-auto-fix\\.yml"
---

<objective>
Phase 57 refactors the daily cost-ledger-snapshot workflow to push a `ledger-snapshots/daily-${SNAPSHOT_DATE}` branch instead of `main` (Phase 50 ruleset 17086676 blocks direct-to-main pushes for the `github-actions[bot]` actor), adds a `Scope decision` fast-path step to the `v40-verifier-gate.yml` diff-guard job so non-`auto-fix/*` PRs (specifically the new ledger-snapshot PRs) do not trip FORBIDDEN_PATHS regex 5 `/^tests\/e2e\/\.llm-spend-ledger\.json$/`, and updates the S13 Vitest YAML contract test to pin both the new push target and the byte-unchanged `v40-auto-fix.yml` invariant. The LOAD-BEARING anti-feature (Pitfall 1) — `v40-auto-fix.yml`'s direct-to-main ledger commit at line 170 — stays byte-unchanged and is pinned by a new Vitest assertion.

Purpose: Unblock the daily ledger heartbeat under Phase 50 ruleset constraints while preserving the two-commit-split that keeps auto-fix PR diffs clean against the diff-guard FORBIDDEN_PATHS regex bank.

Output: Three modified files in a single atomic `feat(57): redirect cost-ledger-snapshot push to ledger-snapshots/* branch (COMMIT-01..04)` commit; Vitest YAML contract suite stays green (18 → 19+ tests, all pass) and pins both new push target + Pitfall 1 anti-feature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/57-ledger-commit-branch-redirect/57-CONTEXT.md
@.planning/phases/57-ledger-commit-branch-redirect/57-RESEARCH.md
@.planning/phases/57-ledger-commit-branch-redirect/57-VALIDATION.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Critical line-number references the executor needs verbatim. -->
<!-- Sourced from research §Sources + direct file reads at planning time. -->

`.github/workflows/v40-cost-ledger-snapshot.yml`:
- Line 27-29: existing static workflow-level concurrency block `group: v40-cost-ledger-snapshot` + `cancel-in-progress: false` (LEAVE UNTOUCHED per RESEARCH "User Constraint Refinement")
- Line 64: `SNAPSHOT_DATE` is written to `$GITHUB_ENV` inside the "Capture snapshot summary" step
- Line 87-90: `git config user.name/email` + `git add tests/e2e/.llm-spend-ledger.json docs/v40-ledger-dashboard.md` + `git diff --cached --quiet || git commit -m "[skip ci] ..."` (LOAD-BEARING `[skip ci]` marker; LEAVE BYTE-UNCHANGED — S13 byte-parity peer is e2e-weekly-digest.yml:106-110)
- Line 91: bare `git push` — THIS IS THE ONLY LINE TO CHANGE in this file

`.github/workflows/v40-verifier-gate.yml`:
- Lines 44-46: `on: pull_request: types: [opened, synchronize, reopened]` (no `branches:` filter since Phase 51.1 ea45a47 — ledger-snapshot PRs WILL trigger this workflow)
- Lines 65-183: diff-guard job (Job 1) — currently has NO Scope decision step
- Line 76: existing `- uses: actions/checkout@v4` — Scope decision step inserts BEFORE this
- Lines 105-112: "Diff-size cap" step — already has `if: startsWith(github.head_ref, 'auto-fix/') || github.event_name != 'pull_request'` (leave existing `if:` clause in place — RESEARCH §Example 2 researcher note: minimal-diff strategy)
- Line 139-143: "Diff-guard regex bank (forbidden paths)" step — currently UNGATED; add `if:` clause
- Lines 145-150: "Test-count invariant" step — already gated (leave existing `if:`)
- Lines 171-183: "Label + comment on rejection" step — `if: failure()`; widen to `if: (steps.scope.outputs.active == 'true' || github.event_name != 'pull_request') && failure()`
- Lines 208-217: existing `verifier-gate` job Scope decision step — COPY VERBATIM into diff-guard job
- Existing scope-decision step count: 3 (lines 208/414/514 — verifier-gate, regression-suite, ready-flip jobs)

`.github/workflows/v40-auto-fix.yml`:
- Line 170: `git push origin main` — UNIQUE occurrence (grep -c returns 1); MUST stay UNIQUE and BYTE-UNCHANGED (Pitfall 1)

`.github/workflows/e2e-weekly-digest.yml`:
- Lines 106-110: byte-parity peer for the snapshot's git-config block; final line 110 is bare `git push` (snapshot's new explicit `git push origin HEAD:...` differs here — adds 2 lines to S13 diff)

`tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js`:
- Lines 96-99: S8 — currently asserts `expect(yaml).toContain('git push')` + `expect(yaml).not.toMatch(/git push\s+--force/)`. Both must be REWRITTEN to assert positive `ledger-snapshots/` prefix while keeping the no-force negative.
- Lines 175-208: S13 — sed-driven byte-diff vs e2e-weekly-digest.yml with `<=4` ceiling. Raise to `<=6` (new push line differs across both sides of the unified diff = +2 lines).
- End of describe block (line 210): NEW `describe('Phase 57 invariants', ...)` block contains 2 new test cases (COMMIT-02 scope-decision count >=4; COMMIT-04 v40-auto-fix.yml push-count =1).
- File imports `execSync` from `node:child_process` already at line 38 — reuse for new grep-based assertions.

`scripts/check-diff-guard.mjs`:
- FORBIDDEN_PATHS regex 5: `/^tests\/e2e\/\.llm-spend-ledger\.json$/` — the exact regex that rejects ledger-snapshot PRs without COMMIT-02's fast-path. NOT modified by Phase 57.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight baseline capture + S13 baseline confirmation (Wave 0)</name>
  <files>(no file mutations — read-only baseline capture into shell state)</files>
  <read_first>
    - tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js (full file; need to confirm 18 baseline tests pass)
    - .github/workflows/v40-auto-fix.yml lines 165-175 (confirm `git push origin main` at line 170 is the unique occurrence pre-Phase-57)
  </read_first>
  <action>
    Capture the Phase 57 baseline commit ref so COMMIT-04's byte-unchanged invariant for v40-auto-fix.yml can be verified after Wave 1 commits land. Record `git rev-parse HEAD` as PHASE_57_BASELINE in the execution SUMMARY scratch area (do NOT commit anything in this task).

    Then run the existing S13 contract suite to establish that the 18 pre-Phase-57 tests pass at the current baseline (per RESEARCH §Test Framework: 127ms, 18 tests green). This is the regression baseline against which Task 2 + Task 3 must hold.

    Per VALIDATION.md Wave 0 Requirements: this is a no-mutation pre-flight gate that unblocks Wave 1 task execution. If S13 baseline FAILS at this step, halt — the underlying file has drifted and Phase 57's S8/S13 edits cannot rely on the documented pre-edit state.

    Confirm via grep that the unique `git push origin main` occurrence in v40-auto-fix.yml currently equals exactly 1 — this establishes the Pitfall 1 anti-feature baseline.
  </action>
  <verify>
    <automated>CI=true npx vitest run tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js 2>&1 | grep -E '(Test Files|Tests)' | grep -v 'failed' &amp;&amp; test "$(grep -c 'git push origin main' .github/workflows/v40-auto-fix.yml)" = "1" &amp;&amp; git rev-parse HEAD</automated>
  </verify>
  <acceptance_criteria>
    - `CI=true npx vitest run tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js` exits 0 with 18 tests passing (baseline regression marker)
    - `grep -c 'git push origin main' .github/workflows/v40-auto-fix.yml` returns exactly `1`
    - `git rev-parse HEAD` captured as PHASE_57_BASELINE (recorded in execution scratch; used by Task 3's COMMIT-04 verification)
    - No working tree mutations; `git status` reports clean
  </acceptance_criteria>
  <done>Baseline ref captured; 18/18 S13 contract tests green pre-edit; v40-auto-fix.yml push count = 1 pre-edit. Wave 1 unblocked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Workflow edits — COMMIT-01 push refspec + COMMIT-02 diff-guard scope decision</name>
  <files>
    - .github/workflows/v40-cost-ledger-snapshot.yml
    - .github/workflows/v40-verifier-gate.yml
  </files>
  <read_first>
    - .github/workflows/v40-cost-ledger-snapshot.yml lines 79-91 (Commit daily ledger snapshot step; LEAVE lines 87-90 byte-unchanged, change ONLY line 91)
    - .github/workflows/v40-verifier-gate.yml lines 60-183 (full diff-guard job body — insertion point + existing if-clause inventory)
    - .github/workflows/v40-verifier-gate.yml lines 208-217 (existing verifier-gate Scope decision step — copy verbatim into diff-guard)
  </read_first>
  <behavior>
    - After Task 2: `grep 'git push origin HEAD:ledger-snapshots/daily-' .github/workflows/v40-cost-ledger-snapshot.yml` returns exactly 1 line (replacing the bare `git push` at line 91)
    - After Task 2: `grep -v '^#' .github/workflows/v40-verifier-gate.yml | grep -c 'Scope decision (auto-fix/\* PRs only'` returns 4 (was 3; new diff-guard step makes 4)
    - After Task 2: `v40-cost-ledger-snapshot.yml` still contains `[skip ci]` (S7 LOAD-BEARING marker untouched), still contains the byte-equal `git add tests/e2e/.llm-spend-ledger.json docs/v40-ledger-dashboard.md` line (S15), still contains the static concurrency group `group: v40-cost-ledger-snapshot` (not date-keyed — RESEARCH §User Constraint Refinement Option 1)
    - After Task 2: `v40-auto-fix.yml` BYTE-UNCHANGED (`git diff <PHASE_57_BASELINE> -- .github/workflows/v40-auto-fix.yml` empty)
    - After Task 2: `grep -c 'git push origin main' .github/workflows/v40-auto-fix.yml` still equals 1
  </behavior>
  <action>
    Edit `.github/workflows/v40-cost-ledger-snapshot.yml` to satisfy COMMIT-01:
    - Replace the bare `git push` at line 91 with `git push origin HEAD:ledger-snapshots/daily-${{ env.SNAPSHOT_DATE }}` (RESEARCH §Code Examples Example 1; `SNAPSHOT_DATE` is in scope at line 91 via the same `${{ env.SNAPSHOT_DATE }}` expression already used in the commit message at line 90).
    - DO NOT touch lines 87-90 (git config user.name/email + git add + idempotent guard + commit message) — byte-parity with `e2e-weekly-digest.yml:106-110` is enforced by S13.
    - DO NOT touch the workflow-level concurrency block at lines 27-29 (static group is correct per RESEARCH §User Constraint Refinement Option 1 — date-keyed group would break workflow-evaluation-time scope and lose schedule-vs-dispatch race guard).
    - DO NOT add `--force` (S8 negative assertion `not.toMatch(/git push\s+--force/)` continues to hold).

    Edit `.github/workflows/v40-verifier-gate.yml` to satisfy COMMIT-02:
    - Insert a new `Scope decision (auto-fix/* PRs only; fast-path SUCCESS otherwise)` step as the FIRST step in the diff-guard job (before the existing `- uses: actions/checkout@v4` at line 76). Copy the step body VERBATIM from lines 208-217 (existing verifier-gate Scope decision step), including the `id: scope`, `if: github.event_name == 'pull_request'` step-level guard, and the `if [[ "${{ github.head_ref }}" == auto-fix/* ]]` shell condition writing `active=true/false` to `$GITHUB_OUTPUT`.
    - Gate the PREVIOUSLY UNGATED steps in the diff-guard job with `if: steps.scope.outputs.active == 'true' || github.event_name != 'pull_request'`:
      * `- uses: actions/checkout@v4` (line 76)
      * `- uses: actions/setup-node@v4` (line 81)
      * `Install dependencies` step (line 86)
      * `Ensure human-review-required label exists` step (line 94)
      * `Diff-guard regex bank (forbidden paths)` step (line 139) — THIS is the load-bearing addition; without it, ledger-snapshot PRs hit FORBIDDEN_PATHS regex 5.
    - For the `Label + comment on rejection` step at line 171 (currently `if: failure()`), widen to `if: (steps.scope.outputs.active == 'true' || github.event_name != 'pull_request') && failure()`.
    - LEAVE existing `if: startsWith(github.head_ref, 'auto-fix/') || github.event_name != 'pull_request'` clauses on the `Diff-size cap` step (line 112) and `Test-count invariant` step (line 150) IN PLACE — RESEARCH §Example 2 researcher note: minimal-diff strategy. These evaluate equivalently to the new scope-output check.

    DO NOT modify `.github/workflows/v40-auto-fix.yml` — Pitfall 1 LOAD-BEARING anti-feature (per D-scope-lock and RESEARCH §Pitfall 1). The two-commit split at line 150-172 (with line 170 `git push origin main`) lands the ledger entry on main BEFORE the auto-fix PR branch is created — without it, every future auto-fix PR's diff would include `tests/e2e/.llm-spend-ledger.json` which trips FORBIDDEN_PATHS regex 5 (`/^tests\/e2e\/\.llm-spend-ledger\.json$/`).

    DO NOT stage or commit changes in this task — the atomic feat(57) commit is created in Task 3 after the test file is also updated.
  </action>
  <verify>
    <automated>grep -c 'git push origin HEAD:ledger-snapshots/daily-${{ env.SNAPSHOT_DATE }}' .github/workflows/v40-cost-ledger-snapshot.yml | grep -q '^1$' &amp;&amp; ! grep -E '^[[:space:]]+git push[[:space:]]*$' .github/workflows/v40-cost-ledger-snapshot.yml &amp;&amp; test "$(grep -v '^#' .github/workflows/v40-verifier-gate.yml | grep -c 'Scope decision (auto-fix/\* PRs only')" = "4" &amp;&amp; test "$(grep -c 'git push origin main' .github/workflows/v40-auto-fix.yml)" = "1" &amp;&amp; git diff --quiet .github/workflows/v40-auto-fix.yml &amp;&amp; grep -q 'group: v40-cost-ledger-snapshot' .github/workflows/v40-cost-ledger-snapshot.yml &amp;&amp; grep -q '\[skip ci\]' .github/workflows/v40-cost-ledger-snapshot.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'git push origin HEAD:ledger-snapshots/daily-${{ env.SNAPSHOT_DATE }}' .github/workflows/v40-cost-ledger-snapshot.yml` returns exactly `1`
    - No bare `git push` line (regex `^\s*git push\s*$`) remains in `v40-cost-ledger-snapshot.yml`
    - `grep -v '^#' .github/workflows/v40-verifier-gate.yml | grep -c 'Scope decision (auto-fix/\* PRs only'` returns exactly `4` (was 3 pre-Phase-57)
    - `grep -c 'git push origin main' .github/workflows/v40-auto-fix.yml` still returns `1` (COMMIT-04 baseline preserved)
    - `git diff --quiet .github/workflows/v40-auto-fix.yml` exits 0 (v40-auto-fix.yml BYTE-UNCHANGED vs baseline; Pitfall 1 honored)
    - `grep 'group: v40-cost-ledger-snapshot' .github/workflows/v40-cost-ledger-snapshot.yml` returns 1 line (static concurrency group preserved — RESEARCH User Constraint Refinement Option 1)
    - `grep '\[skip ci\]' .github/workflows/v40-cost-ledger-snapshot.yml` returns at least 1 line (LOAD-BEARING S7 marker preserved)
    - Working tree shows exactly 2 modified files: `v40-cost-ledger-snapshot.yml` + `v40-verifier-gate.yml`; nothing staged yet
  </acceptance_criteria>
  <done>Both workflow files edited per COMMIT-01 + COMMIT-02 spec; v40-auto-fix.yml byte-unchanged (COMMIT-04 invariant held); no commit yet — Task 3 finalizes atomic commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test updates (COMMIT-03 + COMMIT-04 pin) + atomic feat(57) commit</name>
  <files>
    - tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js
  </files>
  <read_first>
    - tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js lines 96-99 (current S8 body to rewrite)
    - tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js lines 175-208 (current S13 body — ceiling at line 207 raises 4 → 6)
    - tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js lines 1-50 (imports — `execSync` already imported at line 38; reuse)
  </read_first>
  <behavior>
    - S8 (`-t 'S8'`) — green; new positive assertion pins `git push origin HEAD:ledger-snapshots/daily-${{ env.SNAPSHOT_DATE }}` literal; bare `git push` line absence asserted via `not.toMatch(/^\s*git push\s*$/m)`; `--force` negative still holds
    - S13 (`-t 'S13'`) — green at the raised `<=6` ceiling; the sed-driven diff between the snapshot and digest now has 3 differences × 2 sides = 6 changed lines (git add path + commit message + git push refspec)
    - New describe block `Phase 57 invariants` contributes ≥2 new test cases:
      (a) `expect(yaml).toContain('Scope decision (auto-fix/* PRs only')` — but parameterized against the verifier-gate YAML (loaded via `fs.readFileSync` on `.github/workflows/v40-verifier-gate.yml`) and asserting the substring appears at least 4 times via a regex-match-all count (or equivalent) — pins COMMIT-02 against regression
      (b) `expect(execSync(...).trim()).toBe('1')` — pins `grep -c 'git push origin main' .github/workflows/v40-auto-fix.yml` = 1 per RESEARCH §Code Examples Example 4 — pins COMMIT-04 anti-feature
    - All 18 pre-existing tests still pass; suite total is now 18 + 2 (new Phase 57 block) = 20 tests minimum
    - `npm test` exits 0 modulo the documented pre-existing failures in `tests/e2e/scripts/v40-verifier-gate-yaml.test.js` (Phase 51.1 unfinished — Phase 60 CLEAN-02 will fix) and any other documented pre-existing failures
  </behavior>
  <action>
    Edit `tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js` to satisfy COMMIT-03 + add COMMIT-04 pin:

    1. **Rewrite S8 (lines 96-99)** to positively assert the new push target per RESEARCH §Code Examples Example 3:
       - Replace the bare `expect(yaml).toContain('git push')` with `expect(yaml).toContain('git push origin HEAD:ledger-snapshots/daily-${{ env.SNAPSHOT_DATE }}')`.
       - Keep the existing `expect(yaml).not.toMatch(/git push\s+--force/)` (defense — no force-push regression).
       - Add a NEW negative `expect(yaml).not.toMatch(/^\s*git push\s*$/m)` to guarantee no bare push line remains.
       - Update the test title to reflect the new behavior: `'S8 — git push to ledger-snapshots/* branch (NO --force, NO bare push)'` (or equivalent — keep `S8` prefix for traceability).

    2. **Raise S13 ceiling (line 207)** from `expect(changedLines.length).toBeLessThanOrEqual(4)` to `expect(changedLines.length).toBeLessThanOrEqual(6)` per RESEARCH §Pitfall 4 — the explicit `git push origin HEAD:ledger-snapshots/...` line differs from the digest's bare `git push` at line 110, adding 1 changed line on each side of the unified diff (total +2 lines on top of the existing +4 from git add path + commit message).
       - Update the S13 inline comment block (lines 175-187) to mention the new 3-difference accounting (git add path + commit message + git push refspec).

    3. **Append a new `describe('Phase 57 invariants', ...)` block** after the existing describe block closes at line 210, containing:
       - Read the verifier-gate YAML via `fs.readFileSync(path.resolve(PROJECT_ROOT, '.github/workflows/v40-verifier-gate.yml'), 'utf8')` at the top of the new describe block (parallel to the existing `beforeAll`).
       - **Test (a) — COMMIT-02 pin:** Assert the diff-guard job has its own Scope decision step. Use a non-comment-filtered count: `const matches = verifierYaml.split('\n').filter(l => !l.trim().startsWith('#')).join('\n').match(/Scope decision \(auto-fix\/\* PRs only/g) || []; expect(matches.length).toBeGreaterThanOrEqual(4);` (was 3 pre-Phase-57: verifier-gate / regression-suite / ready-flip; Phase 57 adds the 4th in diff-guard). Hygiene-compliant per Nyquist rule (filters comment lines, no bare `== 0` gate). Title: `'COMMIT-02 — v40-verifier-gate.yml has Scope decision step in diff-guard job (count >= 4)'`.
       - **Test (b) — COMMIT-04 anti-feature pin (RESEARCH Example 4):** `const out = execSync("grep -c 'git push origin main' .github/workflows/v40-auto-fix.yml", { encoding: 'utf8' }).trim(); expect(out).toBe('1');` Title: `'COMMIT-04 — v40-auto-fix.yml retains EXACTLY ONE \`git push origin main\` (Pitfall 1)'`.

    4. **Run the test suite to confirm green** before staging:
       - `CI=true npx vitest run tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js` exits 0 with ≥20 tests passing.

    5. **Create the atomic commit** including all three modified files:
       - `git add .github/workflows/v40-cost-ledger-snapshot.yml .github/workflows/v40-verifier-gate.yml tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js`
       - Commit message: `feat(57): redirect cost-ledger-snapshot push to ledger-snapshots/* branch (COMMIT-01..04)` with body referencing COMMIT-01 (line 91 push refspec), COMMIT-02 (diff-guard scope decision), COMMIT-03 (S8 + S13 + new describe block), COMMIT-04 (v40-auto-fix.yml byte-unchanged invariant pinned).
       - Verify post-commit: `git diff PHASE_57_BASELINE -- .github/workflows/v40-auto-fix.yml` is empty (Pitfall 1 verified end-to-end).

    DO NOT modify `.github/workflows/v40-auto-fix.yml`. DO NOT add new npm dependencies (4-milestone zero-new-deps streak target — RESEARCH Standard Stack).
  </action>
  <verify>
    <automated>CI=true npx vitest run tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js 2>&amp;1 | tee /tmp/p57-vitest.log | grep -E '^[[:space:]]*Tests[[:space:]]+[0-9]+ passed' &amp;&amp; CI=true npx vitest run tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js -t 'S8' &amp;&amp; CI=true npx vitest run tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js -t 'S13' &amp;&amp; CI=true npx vitest run tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js -t 'Phase 57' &amp;&amp; test "$(grep -c 'git push origin main' .github/workflows/v40-auto-fix.yml)" = "1" &amp;&amp; git log -1 --pretty=%s | grep -q 'feat(57)' &amp;&amp; git diff HEAD~1 --name-only | sort | diff - <(printf '.github/workflows/v40-cost-ledger-snapshot.yml\n.github/workflows/v40-verifier-gate.yml\ntests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js\n')</automated>
  </verify>
  <acceptance_criteria>
    - `CI=true npx vitest run tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js` exits 0 with ≥20 tests passing (was 18; +2 minimum from Phase 57 invariants block)
    - `CI=true npx vitest run ... -t 'S8'` exits 0 (positive ledger-snapshots/ assertion green)
    - `CI=true npx vitest run ... -t 'S13'` exits 0 at raised `<=6` ceiling
    - `CI=true npx vitest run ... -t 'Phase 57'` exits 0 (both new invariant tests pass)
    - `grep -c 'git push origin main' .github/workflows/v40-auto-fix.yml` returns exactly `1` post-commit (COMMIT-04 end-to-end)
    - `git log -1 --pretty=%s` matches `feat(57):` prefix
    - `git diff HEAD~1 --name-only` returns exactly these 3 paths (sorted): `.github/workflows/v40-cost-ledger-snapshot.yml`, `.github/workflows/v40-verifier-gate.yml`, `tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js`
    - `git diff PHASE_57_BASELINE -- .github/workflows/v40-auto-fix.yml` produces zero output bytes (the file is byte-identical to the Wave 0 captured baseline)
    - No new entries in `package.json` `dependencies` or `devDependencies` (4-milestone zero-new-deps streak preserved)
  </acceptance_criteria>
  <done>Atomic `feat(57):` commit lands containing all four COMMIT-* requirements with green Vitest suite; v40-auto-fix.yml byte-unchanged invariant verified against the Wave 0 baseline ref.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → origin remote | `git push` from the runner under `contents: write` token; ruleset 17086676 enforces the policy on `~DEFAULT_BRANCH`; the new `ledger-snapshots/*` prefix has no ruleset coverage |
| PR head_ref (`ledger-snapshots/*` or arbitrary) → verifier-gate diff-guard job | `github.head_ref` value is attacker-influenceable via branch naming; scope-decision shell guard isolates it via env-var-hop pattern (no direct interpolation into security-bearing logic) |
| `${{ env.SNAPSHOT_DATE }}` (computed in Capture step at line 64) → push refspec at line 91 | Value flows through `$GITHUB_ENV` env-var-hop established at line 64 — CWE-94 defense pattern (RESEARCH §Security Domain V5; matches existing line 90 usage) |
| `v40-auto-fix.yml` ledger-commit step (line 150-172) → `main` directly | LOAD-BEARING two-commit split keeps ledger entry OUT of auto-fix PR diff (Pitfall 1); Phase 57 must NOT cross this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering (CWE-94) | `v40-cost-ledger-snapshot.yml` line 91 — `${{ env.SNAPSHOT_DATE }}` interpolation into `git push` refspec | mitigate | `SNAPSHOT_DATE` is derived from `currentIsoDay()` in `tests/e2e/lib/llm-ledger.js` (deterministic ISO-8601 string; no user input). The env-var-hop via `$GITHUB_ENV` at line 64 is the established CWE-94 defense pattern (matches existing line 90 usage in commit message). No additional mitigation required. |
| T-57-02 | Tampering | `v40-verifier-gate.yml` diff-guard job — FORBIDDEN_PATHS regex 5 would reject any PR head containing `tests/e2e/.llm-spend-ledger.json` | mitigate | COMMIT-02's Scope decision step + step-gating `if:` clauses make the diff-guard regex bank UNREACHABLE for non-`auto-fix/*` head refs (fast-path SUCCESS); the `if [[ "${{ github.head_ref }}" == auto-fix/* ]]` shell guard is verbatim from the existing 3 sibling steps (lines 208/414/514), preserving the uniform trust invariant. |
| T-57-03 | Tampering (LOAD-BEARING) | `v40-auto-fix.yml` line 150-172 two-commit ledger split | mitigate | COMMIT-04 byte-unchanged invariant pinned by (a) Task 3 `git diff PHASE_57_BASELINE` assertion at commit time + (b) new Vitest case `Phase 57 invariants — COMMIT-04` executing `grep -c 'git push origin main' .github/workflows/v40-auto-fix.yml | == '1'` on every test run. Any future PR touching this file would fail the new Vitest pin. |
| T-57-04 | Denial of Service (self-DoS via `[skip ci]` bypass) | `v40-cost-ledger-snapshot.yml` line 90 commit message `[skip ci]` marker | mitigate | LOAD-BEARING `[skip ci]` token at line 90 is NOT modified by Phase 57 (only line 91 changes). S7 Vitest assertion `expect(yaml).toContain('[skip ci]')` continues to gate this — preserved verbatim. Without the marker, the new commit on `ledger-snapshots/*` could trigger `ci.yml` via push-event coverage; the marker prevents it. |

(No `T-57-SC` supply-chain threat — Phase 57 installs ZERO new packages per RESEARCH §Package Legitimacy Audit. The 4-milestone zero-new-deps streak target is honored.)
</threat_model>

<verification>
Phase-level checks (run before declaring Phase 57 complete):

1. **Tasks 1-3 all green:** All three task `<verify>` blocks pass per their automated commands.
2. **Phase-level Vitest:** `CI=true npx vitest run tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js` exits 0 with ≥20 tests passing.
3. **Phase-level invariant — COMMIT-04 end-to-end:** `git diff PHASE_57_BASELINE -- .github/workflows/v40-auto-fix.yml` produces zero bytes; `grep -c 'git push origin main' .github/workflows/v40-auto-fix.yml` returns `1`.
4. **Phase-level invariant — atomic commit shape:** `git log -1 --pretty=%s` matches `^feat(57):`; `git diff HEAD~1 --name-only | wc -l` returns exactly 3.
5. **Phase-level test-suite floor:** `npm test` exits 0 modulo the documented pre-existing failure in `tests/e2e/scripts/v40-verifier-gate-yaml.test.js` V2 (Phase 51.1 unfinished; Phase 60 CLEAN-02). No NEW failures introduced.
6. **Phase-level zero-new-deps:** `git diff HEAD~1 -- package.json package-lock.json` produces zero output (no dependency additions).
7. **Phase-level deferred-UAT acknowledgement:** Per VALIDATION.md §Manual-Only Verifications, the live cron run verification + ledger-snapshot PR verifier-gate verification are Phase 59 UAT-47-d responsibilities; Phase 57 only locks the YAML invariants needed for that UAT to pass.
</verification>

<success_criteria>
1. `v40-cost-ledger-snapshot.yml` pushes to `ledger-snapshots/daily-${SNAPSHOT_DATE}` — observable post-merge via `git ls-remote --heads origin 'ledger-snapshots/*'` (Phase 59 UAT-47-d evidence target).
2. A ledger-snapshot PR (non-`auto-fix/*` head ref) opened against main gets a SUCCESS verdict from all verifier-gate jobs via the diff-guard scope-decision fast-path — no FORBIDDEN_PATHS rejection (Phase 59 UAT-47-d evidence target; Phase 57 locks the YAML invariants needed).
3. `grep -c 'git push origin main' .github/workflows/v40-auto-fix.yml` equals `1` after the refactor commit — the two-commit split in `v40-auto-fix.yml` is byte-unchanged (Pitfall 1 honored; pinned by Vitest).
4. The S13 Vitest YAML contract case in `tests/e2e/scripts/v40-cost-ledger-snapshot-yaml.test.js` passes with a positive assertion pinning the `ledger-snapshots/` branch prefix (S8 rewrite) and the raised `<=6` ceiling (S13 mechanical adjustment).
</success_criteria>

<output>
Create `.planning/phases/57-ledger-commit-branch-redirect/57-01-SUMMARY.md` when done, capturing:
- The atomic commit hash for `feat(57): ...`
- Confirmation that `v40-auto-fix.yml` was byte-unchanged (Pitfall 1)
- Vitest test count delta (18 → ≥20)
- Any researcher Open Questions resolved or carried forward (notably Open Question 3 — dashboard reachability from main — flagged for operator)
- Operator next step: Phase 58 (Promote Outcome Ledger Entry) OR Phase 59 (UAT sweep gated by 56+57+58 all on origin/main)
</output>
