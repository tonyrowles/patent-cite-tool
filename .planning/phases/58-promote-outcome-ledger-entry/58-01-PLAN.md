---
phase: 58-promote-outcome-ledger-entry
plan: 01
type: execute
wave: 1
depends_on: ["58-00"]
files_modified:
  - scripts/auto-fix-promote.mjs
  - tests/unit/auto-fix-promote-gate.test.js
autonomous: true
requirements: [PROMOTE-01, PROMOTE-02, PROMOTE-03, PROMOTE-04]
must_haves:
  truths:
    - "scripts/auto-fix-promote.mjs imports appendLedgerEntry + LEDGER_PATH from '../tests/e2e/lib/llm-ledger.js' (and ONLY those two symbols)"
    - "IMPORTS POLICY comment block (lines 21-30) names ../tests/e2e/lib/llm-ledger.js in the ALLOWED list; the audit grep pattern excludes it from the violation set"
    - "parseArgv recognizes --fingerprint and --error-class; both default to null when absent"
    - "On verified-branch success at line 446, appendLedgerEntry writes {source:'auto-fix-promoted', outcome:'pass', errorClass, fingerprint, issueId, prNumber, iso, model, cost_usd:0, tokens_in:0, tokens_out:0, phase:'58-promote', transport:'subscription'} BEFORE process.exit(0)"
    - "On verified-branch runPromote-non-zero failure at line 440, appendLedgerEntry writes {source:'auto-fix-failed', outcome:'fail', errorClass, fingerprint, issueId, prNumber, reason:'runPromote exitCode=<n>', iso, model, cost_usd:0, tokens_in:0, tokens_out:0, phase:'58-promote', transport:'subscription'} BEFORE process.exit(1)"
    - "assertTripleGate body (lines 89-103) sha256 still equals 5311c1d5364b1b8558d44029432bfb0d5164c19fcc38b9b2d6fcd539c2e0c40f after all edits"
    - "_skipCiGuard:\\s*true non-comment grep count remains exactly 1 (line 434 only) after all edits"
    - "tests/unit/auto-fix-promote-gate.test.js gains 4 new describe blocks (IMPORTS POLICY, assertTripleGate body byte-unchanged, main outcome ledger writes, _skipCiGuard non-comment count); test count grows from 26 to AT LEAST 30"
    - "Triple-gate failure paths (lines 374/404/419/425) do NOT write outcome entries — semantically distinct from promotion-failed-mid-flow (RESEARCH §8)"
  artifacts:
    - path: "scripts/auto-fix-promote.mjs"
      provides: "Outcome ledger writes on promotion success/failure + IMPORTS POLICY narrowing + argv extensions"
      contains: "import { appendLedgerEntry, LEDGER_PATH }, args.fingerprint, args.errorClass, source: 'auto-fix-promoted', source: 'auto-fix-failed'"
    - path: "tests/unit/auto-fix-promote-gate.test.js"
      provides: "Vitest enforcement for PROMOTE-01..04 + _skipCiGuard count invariant"
      contains: "describe('IMPORTS POLICY"
  key_links:
    - from: "scripts/auto-fix-promote.mjs main() verified-branch success"
      to: "tests/e2e/lib/llm-ledger.js appendLedgerEntry"
      via: "direct import; written BEFORE process.exit(0) at line 446"
      pattern: "source: 'auto-fix-promoted'"
    - from: "scripts/auto-fix-promote.mjs main() verified-branch failure (line 440)"
      to: "tests/e2e/lib/llm-ledger.js appendLedgerEntry"
      via: "direct import; written BEFORE process.exit(1) at line 440"
      pattern: "source: 'auto-fix-failed'"
    - from: "tests/unit/auto-fix-promote-gate.test.js IMPORTS POLICY describe"
      to: "scripts/auto-fix-promote.mjs import lines"
      via: "readFileSync source + regex audit"
      pattern: "from 'node:|from './promote-from-quarantine\\\\.mjs'|from '\\\\.\\\\./tests/e2e/lib/llm-ledger\\\\.js'"
---

<objective>
Wire event-sourced outcome ledger entries into scripts/auto-fix-promote.mjs at
two insertion points (success line 446 + failure line 440), narrow the IMPORTS
POLICY to allow exactly one new import, and add four Vitest describe blocks
that enforce PROMOTE-01..04 and the _skipCiGuard load-bearing trust invariant.

Purpose: Phase 54's forward-compat detectOutcome probe in a-b-winner.mjs
recognizes outcome:'pass'/'fail' but stays in abstention because no production
code has ever written such entries. This plan ships the missing writes. Once
≥20 entries per ERROR_CLASS per model arm accumulate on origin/main (Phase 59
SWEEP-03 territory), a-b-winner.mjs exits abstention with zero code edits.

Output: Single atomic feat(58) commit (mechanically feasible per CONTEXT
"Atomic commit" decision; PROMOTE-04 byte-unchanged pin MUST ship with the
new code). All Vitest tests in tests/unit/auto-fix-promote-gate.test.js pass.
The four hard-pinned baseline constants from 58-BASELINE.txt
(ASSERT_TRIPLE_GATE_BODY_SHA256, SKIP_CI_GUARD_NONCOMMENT_COUNT,
PROMOTE_GATE_TEST_COUNT growing to ≥30, LLM_LEDGER_SHA256 unchanged) are
preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-promote-outcome-ledger-entry/58-CONTEXT.md
@.planning/phases/58-promote-outcome-ledger-entry/58-RESEARCH.md
@.planning/phases/58-promote-outcome-ledger-entry/58-VALIDATION.md
@.planning/phases/58-promote-outcome-ledger-entry/58-BASELINE.txt
@.planning/phases/58-promote-outcome-ledger-entry/58-00-SUMMARY.md
@scripts/auto-fix-promote.mjs
@tests/unit/auto-fix-promote-gate.test.js
@tests/e2e/lib/llm-ledger.js
@scripts/auto-fix.mjs
@scripts/a-b-winner.mjs

<interfaces>
<!-- Locked contracts the executor uses verbatim. No codebase exploration needed. -->

From tests/e2e/lib/llm-ledger.js (Phase 56 byte-unchanged invariant):
- `export const LEDGER_PATH: string` (line 74 IIFE)
- `export function appendLedgerEntry(ledgerPath: string, entry: object): void` (line 686 — spread-verbatim; no required-field validation)

From scripts/auto-fix.mjs:69-74 (the canonical import-path convention this plan mirrors):
- `import { appendLedgerEntry, LEDGER_PATH } from '../tests/e2e/lib/llm-ledger.js'`
  (relative path verified to resolve from `scripts/` to `tests/e2e/lib/`)

From scripts/auto-fix-promote.mjs (current state, READ-ONLY in this plan except where modified):
- IMPORTS POLICY comment block: lines 21-30
- assertTripleGate function body: lines 89-103 (15 lines, sha256 5311c1d5364b1b8558d44029432bfb0d5164c19fcc38b9b2d6fcd539c2e0c40f, 815 bytes)
- KNOWN_FLAGS Set: lines 269-277
- parseArgv switch block: lines 319-329
- parseArgv return statement: lines 339-353
- main() verified-branch runPromote call: lines 431-435
- main() runPromote-failure block: lines 436-441 (the ONLY failure path that writes an outcome entry — RESEARCH §8)
- main() verified-branch success block: lines 443-446 (the ONLY success path)
- Sole non-comment _skipCiGuard:true literal: line 434

From scripts/a-b-winner.mjs:178-189 (consumer contract — drives errorClass scope expansion):
- `isAttributable(entry)` REQUIRES `entry.errorClass` to be a non-empty string AND `entry.model` to start with 'claude-sonnet-4-6' OR 'claude-opus-4-7'
- `detectOutcome(entry)` probes `entry.outcome === 'pass'` (true) / `'fail'` (false) FIRST

From tests/unit/auto-fix-promote-gate.test.js (current state, line count 322 confirmed):
- 6 existing describe blocks, 26 existing tests
- Existing imports: vitest + auto-fix-promote.mjs symbols (assertTripleGate, assertPartialGate, runPartialPromote, parseSourceIssue, parseArgv, PARTIAL_THRESHOLD, PARTIAL_LABEL)
- Zero vi.mock calls in the file today
- No existing IMPORTS POLICY test
- No existing assertTripleGate body byte-unchanged test
- No existing outcome-entry test
- No existing _skipCiGuard grep-count test
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.1: PROMOTE-01 — IMPORTS POLICY narrowing + import + Vitest enforcement</name>
  <files>scripts/auto-fix-promote.mjs, tests/unit/auto-fix-promote-gate.test.js</files>
  <read_first>
    scripts/auto-fix-promote.mjs (lines 21-30 — IMPORTS POLICY comment block; lines 54-58 — current import block; line 58 — current last import is `runPromote`);
    scripts/auto-fix.mjs (lines 69-74 — canonical multi-symbol import pattern from llm-ledger.js using relative path '../tests/e2e/lib/llm-ledger.js');
    tests/unit/auto-fix-promote-gate.test.js (lines 42-52 — existing imports; lines 54-322 — existing describe blocks for namespace collision check; RESEARCH §5 confirms no existing IMPORTS POLICY assertion);
    .planning/phases/58-promote-outcome-ledger-entry/58-RESEARCH.md §4 (correct relative import path; multi-symbol convention) and §5 (test-file structure) and §6 (appendLedgerEntry signature)
  </read_first>
  <behavior>
    - Test 1 (PROMOTE-01 enforcement): readFileSync of scripts/auto-fix-promote.mjs; filter `^import .+$` lines via regex; assert that every import line matches `from 'node:` OR `from './promote-from-quarantine\.mjs'` OR `from '\.\./tests/e2e/lib/llm-ledger\.js'`. Forbidden array must equal `[]`.
    - Test 2 (positive shape): assert the new appendLedgerEntry import line literally appears in the source (`import { appendLedgerEntry, LEDGER_PATH } from '../tests/e2e/lib/llm-ledger.js'` — verbatim).
  </behavior>
  <action>
Edit `scripts/auto-fix-promote.mjs`:

(a) Replace the IMPORTS POLICY comment block at lines 21-30 with an updated block that:
    - keeps the same "ALLOWED / FORBIDDEN" structure and prose tone
    - adds `../tests/e2e/lib/llm-ledger.js` to the ALLOWED list with a one-line annotation: `(Phase 58 PROMOTE-01: appendLedgerEntry + LEDGER_PATH; function body byte-unchanged by Phase 56; auto-fix-promote.mjs runs only in CI per v40-auto-promote.yml — no leak surface)`
    - keeps the FORBIDDEN list (`tests/e2e/lib/*` EXCEPT llm-ledger.js, `src/*`, LLM drivers)
    - updates the audit grep example to the new three-pattern form: `grep -vE "from 'node:|from './promote-from-quarantine\\.mjs'|from '\\.\\./tests/e2e/lib/llm-ledger\\.js'"`
    - adds a closing sentence pointing to the new Vitest enforcement: `Enforced by tests/unit/auto-fix-promote-gate.test.js (Phase 58-added describe block 'IMPORTS POLICY (Phase 58 PROMOTE-01)').`

(b) Add a new import line in the imports block (currently lines 54-58, after the existing runPromote import) — verbatim text:
    `import { appendLedgerEntry, LEDGER_PATH } from '../tests/e2e/lib/llm-ledger.js';`
    Match the existing two-symbol-per-line convention from auto-fix.mjs:69-74 (single-line multi-symbol import is acceptable; convention is whichever is more compact at this many symbols — two symbols on one line per project precedent).

(c) Do NOT touch any other line. Do NOT touch lines 89-103 (assertTripleGate body — Phase 53 byte-unchanged invariant). Do NOT add or modify any `_skipCiGuard:\s*true` literal. Do NOT touch the partial-path code at lines 136-237 or the parseArgv body (yet — Task 1.2 owns parseArgv).

Edit `tests/unit/auto-fix-promote-gate.test.js`:

(d) At the top of the file (after the existing `import` block on lines 42-52), add three additional imports needed by the new Vitest assertions:
    `import { readFileSync } from 'node:fs';`
    `import { fileURLToPath } from 'node:url';`
    `import path from 'node:path';`

(e) Append a new describe block at the END of the file (after the existing `parseArgv --passing-cases (Phase 53)` describe on lines 298-322):
    `describe('IMPORTS POLICY (Phase 58 PROMOTE-01)', () => { ... });`
    Inside this describe, place two `it` cases:
    - it 'IP1 — only allows node:*, ./promote-from-quarantine.mjs, ../tests/e2e/lib/llm-ledger.js'
      - resolve the source via path.dirname(fileURLToPath(import.meta.url)) + '../../scripts/auto-fix-promote.mjs'
      - readFileSync utf8
      - extract all `^import .+$` lines via `source.match(/^import .+$/gm) || []`
      - filter to keep ONLY lines that fail the allowed-pattern regex `/from 'node:|from '\.\/promote-from-quarantine\.mjs'|from '\.\.\/tests\/e2e\/lib\/llm-ledger\.js'/`
      - assert the filtered (forbidden) array equals `[]`
    - it 'IP2 — positive: the appendLedgerEntry import is present with the locked verbatim shape'
      - readFileSync the source
      - assert the source contains the substring `import { appendLedgerEntry, LEDGER_PATH } from '../tests/e2e/lib/llm-ledger.js'` exactly once via `(source.match(/import \{ appendLedgerEntry, LEDGER_PATH \} from '\.\.\/tests\/e2e\/lib\/llm-ledger\.js'/g) || []).length === 1`

Per Phase 58 D-04 (atomic single commit) and CONTEXT "atomic commit" decision: do NOT commit yet — accumulate edits across Tasks 1.1, 1.2, 1.3 and commit at the end of Task 1.3.
  </action>
  <verify>
    <automated>CI=true npx vitest run tests/unit/auto-fix-promote-gate.test.js -t "IMPORTS POLICY"</automated>
  </verify>
  <acceptance_criteria>
1. `grep -nE "^import" scripts/auto-fix-promote.mjs | grep -vE "from 'node:|from './promote-from-quarantine\\.mjs'|from '\\.\\./tests/e2e/lib/llm-ledger\\.js'"` returns zero lines.
2. `grep -c "appendLedgerEntry, LEDGER_PATH" scripts/auto-fix-promote.mjs` equals 1 (the new import line).
3. `awk 'NR>=89 && NR<=103' scripts/auto-fix-promote.mjs | sha256sum | awk '{print $1}'` still equals `5311c1d5364b1b8558d44029432bfb0d5164c19fcc38b9b2d6fcd539c2e0c40f` (PROMOTE-04 not yet pinned by Vitest but baseline verified manually here).
4. `grep -nE '_skipCiGuard:\\s*true' scripts/auto-fix-promote.mjs | grep -vE '^[0-9]+:\\s*//' | wc -l` equals 1.
5. New describe block `'IMPORTS POLICY (Phase 58 PROMOTE-01)'` exists in tests/unit/auto-fix-promote-gate.test.js with exactly 2 `it` cases (IP1, IP2).
6. `CI=true npx vitest run tests/unit/auto-fix-promote-gate.test.js -t "IMPORTS POLICY"` exits 0 with 2 tests passing.
  </acceptance_criteria>
  <done>
IMPORTS POLICY narrowing complete; new import wired; Vitest enforcement assertion ships in the same edit set (commit pending until Task 1.3). The four hard-pinned baseline constants from 58-BASELINE.txt remain valid.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.2: PROMOTE-02/03 — argv extensions + outcome entry writes</name>
  <files>scripts/auto-fix-promote.mjs, tests/unit/auto-fix-promote-gate.test.js</files>
  <read_first>
    scripts/auto-fix-promote.mjs (lines 269-277 — KNOWN_FLAGS Set; lines 288-353 — parseArgv body including switch block at 319-329 and return statement at 339-353; lines 431-446 — verified-branch success path; lines 436-441 — runPromote-failure path);
    scripts/auto-fix.mjs (lines 229-233 — extractFingerprint regex `/<!-- fp: ([0-9a-f]{12}) -->/` confirming 12-hex fingerprint format; lines 419-432 — canonical event-entry shape this plan mirrors);
    tests/e2e/lib/llm-ledger.js (lines 619-738 — appendLedgerEntry contract; required fields iso/model/cost_usd; entry spread-verbatim);
    scripts/a-b-winner.mjs (lines 178-189 — isAttributable requires errorClass non-empty + model startsWith one of two arms; lines 231-238 — detectOutcome probes outcome:'pass'/'fail' first);
    .planning/phases/58-promote-outcome-ledger-entry/58-RESEARCH.md §1 (errorClass required on outcome entry), §6 (entry-shape convention), §7 (success insertion at line 441→443), §8 (failure path inventory; ONLY line 440 writes outcome entry)
  </read_first>
  <behavior>
    - Test 1 (PA4 argv): parseArgv called with `--fingerprint abc123def456 --error-class WRONG_CITATION` returns `args.fingerprint === 'abc123def456'` AND `args.errorClass === 'WRONG_CITATION'`.
    - Test 2 (PA5 argv defaults): parseArgv called WITHOUT either flag returns `args.fingerprint === null` AND `args.errorClass === null`.
    - Test 3 (outcome success): mocked runPromote returning `{exitCode: 0}` + mocked appendLedgerEntry. Invoke main()-equivalent flow with verified labels + valid fingerprint/errorClass argv. Assert appendLedgerEntry was called exactly once with `LEDGER_PATH` as first arg and an object containing `{source: 'auto-fix-promoted', outcome: 'pass', errorClass: 'WRONG_CITATION', fingerprint: 'abc123def456', issueId: 'issue-99', prNumber: 7, model: 'claude-sonnet-4-6', cost_usd: 0, phase: '58-promote', transport: 'subscription'}` AND a valid iso string (regex `/^\\d{4}-\\d{2}-\\d{2}T/`).
    - Test 4 (outcome failure): mocked runPromote returning `{exitCode: 2}` + mocked appendLedgerEntry + mocked process.exit + mocked process.stderr.write. Assert appendLedgerEntry was called with `{source: 'auto-fix-failed', outcome: 'fail', errorClass: 'GOOGLE_DOM_DRIFT', fingerprint: 'deadbeef1234', issueId: 'issue-42', prNumber: 13, reason: 'runPromote exitCode=2', model: 'claude-sonnet-4-6', cost_usd: 0, phase: '58-promote', transport: 'subscription'}` AND process.exit(1) was called AFTER the appendLedgerEntry call.
    - Test 5 (triple-gate failures do NOT write outcome): mocked appendLedgerEntry + invocation that triggers parseSourceIssue throw (line 419 path). Assert appendLedgerEntry was NEVER called (vi.mocked(appendLedgerEntry).mock.calls.length === 0). Covers RESEARCH §8 "line 440 only" decision.
  </behavior>
  <action>
Edit `scripts/auto-fix-promote.mjs`:

(a) parseArgv extension — at the KNOWN_FLAGS Set (currently lines 269-277), add two entries: `'--fingerprint'` and `'--error-class'`. Keep existing ordering convention (group with the existing PROMOTE-style flags; insert before `'--passing-cases'` for readability).

(b) parseArgv local-variable block — currently declarations lines 289-301. Add two new declarations after `let passingCasesCsv = ''`: `let fingerprint = null;` and `let errorClass = null;`.

(c) parseArgv switch block — currently lines 319-329. Add two new case branches after the existing `--passing-cases` case:
    `case '--fingerprint':           fingerprint = takeValue(argv, i, tok); i++; break;`
    `case '--error-class':           errorClass  = takeValue(argv, i, tok); i++; break;`

(d) parseArgv return statement — currently lines 339-353. Add two new fields to the returned object, between `caseId,` (line 349) and `passingCases:` (line 350-352):
    `fingerprint,`
    `errorClass,`
    Pure value forwarding; if the flag was absent, the variable is null (matches the rest of the file's "null/empty default" convention).

(e) Defensive validation (security mitigation per RESEARCH Security Domain): after the existing required-flags check (currently lines 332-337), add NEW conditional validation BEFORE the return statement:
    - If `fingerprint !== null` AND it does not match `/^[0-9a-f]{12}$/`, write `[auto-fix-promote] malformed --fingerprint (expected 12-hex)` to stderr and process.exit(2).
    - If `errorClass !== null` AND it does not match `/^[A-Z_][A-Z0-9_]*$/`, write `[auto-fix-promote] malformed --error-class (expected ERROR_CLASS naming convention)` to stderr and process.exit(2).

(f) main() verified-branch SUCCESS outcome entry — insert NEW code between line 441 (closing `}` of the `if (result.exitCode !== 0)` block) and line 443 (`process.stdout.write`). The new block (without fenced code; described as identifier list):
    - call `appendLedgerEntry(LEDGER_PATH, { ... })`
    - object literal keys IN ORDER: `iso`, `model`, `cost_usd`, `tokens_in`, `tokens_out`, `phase`, `transport`, `issueId`, `prNumber`, `fingerprint`, `errorClass`, `source`, `outcome`
    - values: `new Date().toISOString()` for iso; the string `'claude-sonnet-4-6'` for model (RESEARCH A1 canonical default); `0` for cost_usd/tokens_in/tokens_out; the string `'58-promote'` for phase; the string `'subscription'` for transport (RESEARCH A3); the template literal ``issue-${args.sourceIssue}`` for issueId; `args.pr` for prNumber; `args.fingerprint` for fingerprint; `args.errorClass` for errorClass; the string `'auto-fix-promoted'` for source; the string `'pass'` for outcome
    - precede with a one-line comment `// PHASE 58 PROMOTE-02 — outcome entry on promote success`

(g) main() verified-branch FAILURE outcome entry (RESEARCH §8: line 440 ONLY) — insert NEW code between line 439 (`process.stderr.write(...)` closing line, after the trailing `;`) and line 440 (`process.exit(1)`). The new block:
    - call `appendLedgerEntry(LEDGER_PATH, { ... })`
    - same key ordering as the success entry, with `source: 'auto-fix-failed'`, `outcome: 'fail'`, and an ADDITIONAL `reason` field at the end: the template literal ``runPromote exitCode=${result.exitCode}``
    - precede with a one-line comment `// PHASE 58 PROMOTE-03 — outcome entry on promote failure (line 440 path only — see RESEARCH §8)`

(h) Do NOT write outcome entries at any of the four pre-promotion failure paths (lines 374, 404, 419, 425). RESEARCH §8 documents the semantic rationale: pre-promotion gate rejections are not model-performance signal; the ledger is for model-performance signal.

(i) Do NOT touch lines 89-103 (assertTripleGate body). Do NOT add a second `_skipCiGuard:\s*true` literal anywhere. The new entry comments must NOT contain the literal pattern.

(j) Defensive: truncate the `reason` field to 200 chars before write (RESEARCH Security Domain "Ledger entry corruption" mitigation). For PROMOTE-03 specifically: replace `reason: ` template-literal value with a slice-to-200 form: `(\`runPromote exitCode=${result.exitCode}\`).slice(0, 200)`.

Edit `tests/unit/auto-fix-promote-gate.test.js`:

(k) Extend the existing `parseArgv --passing-cases (Phase 53)` describe block at lines 298-322 with TWO additional `it` cases (PA4, PA5) at the end of that block (preserving the existing 3 cases verbatim):
    - it 'PA4 — Phase 58: --fingerprint and --error-class are captured into args'
    - it 'PA5 — Phase 58: missing --fingerprint and --error-class default to null'
    Both use the existing REQUIRED constant in the block; PA4 appends `'--fingerprint', 'abc123def456', '--error-class', 'WRONG_CITATION'`; PA5 uses REQUIRED alone.
    PA4 asserts `result.fingerprint === 'abc123def456'` AND `result.errorClass === 'WRONG_CITATION'`.
    PA5 asserts `result.fingerprint === null` AND `result.errorClass === null`.

(l) Append a NEW describe block at the end of the file (after the IMPORTS POLICY describe added in Task 1.1) named:
    `describe('main() outcome ledger writes (Phase 58 PROMOTE-02/03)', () => { ... })`

(m) The describe block must use `vi.mock('../../scripts/promote-from-quarantine.mjs', ...)` and `vi.mock('../../tests/e2e/lib/llm-ledger.js', ...)`. The factory for promote-from-quarantine returns `{ runPromote: vi.fn() }`. The factory for llm-ledger returns `{ appendLedgerEntry: vi.fn(), LEDGER_PATH: '/tmp/test-ledger.json' }`.

(n) Place three `it` cases inside the describe:
    - it 'O1 — success path writes auto-fix-promoted entry with errorClass + fingerprint + issueId + prNumber'
      - import the mocked runPromote + appendLedgerEntry via `vi.mocked(...)`
      - mock runPromote to resolve `{exitCode: 0}`
      - re-import main from `'../../scripts/auto-fix-promote.mjs'` if necessary (use `await import` with mock setup BEFORE the import). Easier pattern: invoke main as exported function or stub `process.argv` + import as side-effecting CLI. Recommended: extract main as a named export in a follow-up phase (out of scope here); for this plan use `await import` after `vi.mock` calls and access main via dynamic re-import.
      - Alternative simpler test pattern (RECOMMENDED): stub process.argv with valid verified-branch argv (`['node', 'scripts/auto-fix-promote.mjs', '--pr', '7', '--pr-labels', 'auto-fix:verified', '--pr-merged', 'true', '--pr-body', '/dev/null', '--source-issue', '99', '--source-issue-labels', 'triage', '--case-id', 'C1', '--fingerprint', 'abc123def456', '--error-class', 'WRONG_CITATION']`), mock fs.readFileSync to return a body containing `<!-- source_issue: 99 -->`, mock process.exit to throw a sentinel error so it does not actually exit, then invoke the main async function (re-import after vi.mock). Assert appendLedgerEntry was called with `[LEDGER_PATH, { source: 'auto-fix-promoted', outcome: 'pass', errorClass: 'WRONG_CITATION', fingerprint: 'abc123def456', issueId: 'issue-99', prNumber: 7, model: 'claude-sonnet-4-6', cost_usd: 0, tokens_in: 0, tokens_out: 0, phase: '58-promote', transport: 'subscription', iso: expect.stringMatching(/^\\d{4}-\\d{2}-\\d{2}T/) }]`.
      - If the mocking approach proves brittle for the dynamic main() invocation, an acceptable simpler alternative is a STRUCTURAL test: read scripts/auto-fix-promote.mjs source, assert via regex that the substring `appendLedgerEntry(LEDGER_PATH,` appears with `source: 'auto-fix-promoted'` and `outcome: 'pass'` together within 400 chars (single insertion site). Implementer chooses whichever path produces a stable test; behavioral mock-based is preferred; structural fallback is acceptable per CONTEXT discretion on mock complexity.
    - it 'O2 — failure path (runPromote exitCode !== 0) writes auto-fix-failed entry with reason'
      - mock runPromote to resolve `{exitCode: 2}`
      - same argv stub as O1 (replace errorClass with 'GOOGLE_DOM_DRIFT', sourceIssue with 42, pr with 13, fingerprint with 'deadbeef1234')
      - mock process.exit to throw sentinel
      - assert appendLedgerEntry was called with object including `source: 'auto-fix-failed'`, `outcome: 'fail'`, `reason: 'runPromote exitCode=2'`, `errorClass: 'GOOGLE_DOM_DRIFT'`, `fingerprint: 'deadbeef1234'`, `issueId: 'issue-42'`, `prNumber: 13`
      - assert process.exit(1) was reached AFTER the appendLedgerEntry call (order via `vi.mocked(appendLedgerEntry).mock.invocationCallOrder[0] < vi.mocked(process.exit).mock.invocationCallOrder[0]` if mocking process.exit; or via call-order check on the sentinel)
      - structural fallback acceptable: regex-assert substring `appendLedgerEntry(LEDGER_PATH,` paired with `source: 'auto-fix-failed'`, `outcome: 'fail'`, `reason:` template literal in a single contiguous block in source
    - it 'O3 — triple-gate failure (parseSourceIssue throws) does NOT write any outcome entry'
      - mock runPromote (not invoked on this path)
      - argv stub with verified labels but body that fails parseSourceIssue (empty body + commit message without Fix #N)
      - assert vi.mocked(appendLedgerEntry).mock.calls.length === 0 after the main() invocation/sentinel exit
      - structural fallback acceptable: grep source for `appendLedgerEntry` count == 2 occurrences total (both inside hasVerified branch, none at lines 374/404/419/425). Use: count `\bappendLedgerEntry\(LEDGER_PATH,` regex matches in source; assert === 2.

Per CONTEXT D atomic commit: still do NOT commit; Task 1.3 closes the commit.
  </action>
  <verify>
    <automated>CI=true npx vitest run tests/unit/auto-fix-promote-gate.test.js -t "main\\(\\) outcome ledger writes" && CI=true npx vitest run tests/unit/auto-fix-promote-gate.test.js -t "parseArgv --passing-cases"</automated>
  </verify>
  <acceptance_criteria>
1. `grep -c "args.fingerprint" scripts/auto-fix-promote.mjs` returns ≥ 2 (one read in each of success + failure entries).
2. `grep -c "args.errorClass" scripts/auto-fix-promote.mjs` returns ≥ 2.
3. `grep -c "source: 'auto-fix-promoted'" scripts/auto-fix-promote.mjs` equals 1.
4. `grep -c "source: 'auto-fix-failed'" scripts/auto-fix-promote.mjs` equals 1.
5. `grep -c "outcome: 'pass'" scripts/auto-fix-promote.mjs` equals 1.
6. `grep -c "outcome: 'fail'" scripts/auto-fix-promote.mjs` equals 1.
7. `grep -cE "\\bappendLedgerEntry\\(LEDGER_PATH," scripts/auto-fix-promote.mjs` equals 2 (success + failure; NO entries at pre-promotion gates).
8. `grep -nE '_skipCiGuard:\\s*true' scripts/auto-fix-promote.mjs | grep -vE '^[0-9]+:\\s*//' | wc -l` STILL equals 1 (no second occurrence introduced).
9. `awk 'NR>=89 && NR<=103' scripts/auto-fix-promote.mjs | sha256sum | awk '{print $1}'` STILL equals `5311c1d5364b1b8558d44029432bfb0d5164c19fcc38b9b2d6fcd539c2e0c40f`.
10. KNOWN_FLAGS Set in scripts/auto-fix-promote.mjs contains both `'--fingerprint'` and `'--error-class'` (grep both literals).
11. CI=true npx vitest run with the PA4, PA5, O1, O2, O3 test names exits 0.
  </acceptance_criteria>
  <done>
Outcome entries write on the two correct insertion points (line 441→443 success, line 439→440 failure). Pre-promotion gate failures (374, 404, 419, 425) do NOT write entries. parseArgv accepts and validates the two new flags. Five new Vitest cases (PA4, PA5, O1, O2, O3) pass. Trust invariants (assertTripleGate body sha256, _skipCiGuard non-comment count == 1) preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.3: PROMOTE-04 + _skipCiGuard pin + atomic commit</name>
  <files>tests/unit/auto-fix-promote-gate.test.js, scripts/auto-fix-promote.mjs</files>
  <read_first>
    .planning/phases/58-promote-outcome-ledger-entry/58-BASELINE.txt (locked sha256 + grep-count values);
    .planning/phases/58-promote-outcome-ledger-entry/58-RESEARCH.md §3 (verbatim body — 15 lines starting at line 89) and §9 (non-comment grep-count pattern);
    scripts/auto-fix-promote.mjs (lines 89-103 — the body to be pinned; lines 1-521 — the file under audit for _skipCiGuard count);
    tests/unit/auto-fix-promote-gate.test.js (current end-of-file state after Tasks 1.1 and 1.2)
  </read_first>
  <behavior>
    - Test 1 (PROMOTE-04 verbatim body pin): readFileSync scripts/auto-fix-promote.mjs; split into lines; locate the line starting with `export function assertTripleGate`; extract that line plus the next 14 (15 total). Join with `\n`. Assert equality with a verbatim EXPECTED_BODY string built from RESEARCH §3 (15 lines, byte-for-byte).
    - Test 2 (_skipCiGuard non-comment count): readFileSync scripts/auto-fix-promote.mjs; split into lines; filter out lines matching `^\s*//`; count lines containing the regex `/_skipCiGuard:\s*true/`; assert count === 1.
  </behavior>
  <action>
Edit `tests/unit/auto-fix-promote-gate.test.js`:

(a) Append a NEW describe block at the end of the file (after the outcome-writes describe added in Task 1.2) named:
    `describe('assertTripleGate body byte-unchanged (Phase 58 PROMOTE-04)', () => { ... })`

(b) Inside this describe, place ONE `it` case:
    - it 'PROMOTE-04 — body lines 89-103 match the locked Phase 58 baseline verbatim string'
    - resolve source via `path.dirname(fileURLToPath(import.meta.url))` + relative path `'../../scripts/auto-fix-promote.mjs'`
    - readFileSync utf8
    - split into lines with `/\r?\n/`
    - find startIdx via `lines.findIndex((l) => l.startsWith('export function assertTripleGate'))`
    - assert startIdx !== -1 (defensive — if the function moved, the test fails informatively)
    - extract `body = lines.slice(startIdx, startIdx + 15).join('\n')`
    - assert `body === EXPECTED_BODY` where EXPECTED_BODY is the verbatim 15-line array.join('\n') from RESEARCH §3. The 15 lines (in this exact order, with backslash escapes for embedded quotes per RESEARCH):
      1. `export function assertTripleGate({ prLabels, merged, sourceIssueLabels } = {}) {`
      2. `  // Leg 1 — auto-fix:verified label on the merged PR.`
      3. `  if (!Array.isArray(prLabels) || !prLabels.includes('auto-fix:verified')) {`
      4. `    throw new Error("TRIPLE_GATE_FAILED: prLabels — missing 'auto-fix:verified'");`
      5. `  }`
      6. `  // Leg 2 — merged === true (the GitHub close webhook also fires for`
      7. `  // close-without-merge; this leg is what distinguishes them).`
      8. `  if (merged !== true) {`
      9. `    throw new Error('TRIPLE_GATE_FAILED: merged — pull request not merged');`
      10. `  }`
      11. `  // Leg 3 — source-issue carries triage (Phase 34 triage-classifier verdict).`
      12. `  if (!Array.isArray(sourceIssueLabels) || !sourceIssueLabels.includes('triage')) {`
      13. `    throw new Error("TRIPLE_GATE_FAILED: sourceIssueLabels — source issue missing 'triage'");`
      14. `  }`
      15. `}`
    - On assertion failure, the diff between actual and expected shows exactly which line drifted — diagnosability per CONTEXT Claude's-Discretion recommendation.

(c) Append a NEW describe block at the end of the file (after the PROMOTE-04 describe) named:
    `describe('_skipCiGuard:true non-comment grep-count invariant (Phase 58 trust pin)', () => { ... })`

(d) Inside this describe, place ONE `it` case:
    - it 'exactly one non-comment occurrence of _skipCiGuard:\\s*true (line 434 only)'
    - readFileSync the source as in (b)
    - split into lines via `/\r?\n/`
    - filter to drop pure-comment lines: `lines.filter((l) => !/^\\s*\\/\\//.test(l))`
    - count matches: `codeLines.filter((l) => /_skipCiGuard:\\s*true/.test(l)).length`
    - assert count === 1
    - per RESEARCH §9 edge-case note: trailing-comment lines containing the pattern would falsely count. The file currently has none; Phase 58 introduces none. Acknowledged limitation; the simpler heuristic is sufficient.

(e) Run the full test suite for the file locally: `CI=true npx vitest run tests/unit/auto-fix-promote-gate.test.js`. Confirm green. Confirm test count is now AT LEAST 30 (26 baseline + 2 PROMOTE-01 + 2 argv PA4/PA5 + 3 outcome O1/O2/O3 + 1 PROMOTE-04 + 1 _skipCiGuard = 35 if all five new outcome-tests are independent; minimum 30 if O1+O2+O3 are merged or counted differently).

(f) Run `CI=true npm test` and confirm: same green status as baseline (acknowledging Phase 51.1 carry-over failures in `v40-verifier-gate-yaml.test.js` and `llm-ledger.test.js` Test 48 which are NOT this phase's responsibility — those are Phase 60 and Phase 56 territories respectively; Phase 56 has already shipped the Test 48 relaxation per STATE.md).

(g) Per CONTEXT D atomic commit decision and Phase 58 D-04 ("If split, PROMOTE-04's assertTripleGate-byte-pin MUST land in the same commit as the new code"), CREATE THE SINGLE ATOMIC COMMIT now with commit message (verbatim):
    `feat(58): wire promote outcome ledger entries (PROMOTE-01..04)`
    Body:
    ```
    PROMOTE-01: narrow IMPORTS POLICY at scripts/auto-fix-promote.mjs:21-30
    to allow ../tests/e2e/lib/llm-ledger.js; add Vitest enforcement
    (IP1 + IP2) in tests/unit/auto-fix-promote-gate.test.js.

    PROMOTE-02: write {source:'auto-fix-promoted', outcome:'pass',
    errorClass, fingerprint, issueId, prNumber, ...} via
    appendLedgerEntry BEFORE process.exit(0) at line 446 of the
    verified branch.

    PROMOTE-03: write {source:'auto-fix-failed', outcome:'fail',
    errorClass, fingerprint, issueId, prNumber, reason, ...} BEFORE
    process.exit(1) on the runPromote-non-zero path (line 440 only;
    pre-promotion gate failures at 374/404/419/425 do NOT write entries
    per RESEARCH §8 semantic boundary).

    PROMOTE-04: assertTripleGate body (lines 89-103, sha256
    5311c1d5364b1b8558d44029432bfb0d5164c19fcc38b9b2d6fcd539c2e0c40f)
    pinned verbatim in Vitest; lands in this commit per CONTEXT D-04
    atomic-commit decision.

    Trust invariant: _skipCiGuard:\\s*true non-comment grep count
    remains 1 (line 434 only); pinned by new Vitest assertion.

    errorClass scope: a-b-winner.mjs's isAttributable filter
    (a-b-winner.mjs:178-189) requires errorClass on the outcome entry
    itself — added to the entry shape so a-b-winner can exit
    abstention automatically once entries accumulate (per CONTEXT
    Phase 58 Scope Adjustment 2026-06-05).

    Files: scripts/auto-fix-promote.mjs,
           tests/unit/auto-fix-promote-gate.test.js.

    Trailers per project convention.
    ```
    Use a heredoc. Stage ONLY the two files in `files_modified`. Do not stage any other paths.

(h) Final verification AFTER commit: `git diff HEAD~1 HEAD -- tests/e2e/lib/llm-ledger.js` MUST be empty (Phase 56 byte-unchanged invariant). `git diff HEAD~1 HEAD -- scripts/auto-fix.mjs` MUST be empty (Phase 56 territory, not touched here). `git diff HEAD~1 HEAD -- .github/workflows/v40-auto-fix.yml` MUST be empty (LOAD-BEARING anti-feature per Pitfall 1).
  </action>
  <verify>
    <automated>CI=true npx vitest run tests/unit/auto-fix-promote-gate.test.js && git diff HEAD~1 HEAD -- tests/e2e/lib/llm-ledger.js scripts/auto-fix.mjs .github/workflows/v40-auto-fix.yml | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
1. New describe `'assertTripleGate body byte-unchanged (Phase 58 PROMOTE-04)'` with 1 it case exists and passes.
2. New describe `'_skipCiGuard:true non-comment grep-count invariant (Phase 58 trust pin)'` with 1 it case exists and passes.
3. CI=true npx vitest run tests/unit/auto-fix-promote-gate.test.js exits 0; test count ≥ 30.
4. Single atomic commit on HEAD with title `feat(58): wire promote outcome ledger entries (PROMOTE-01..04)`.
5. Commit touches EXACTLY two files: scripts/auto-fix-promote.mjs and tests/unit/auto-fix-promote-gate.test.js. `git diff --name-only HEAD~1 HEAD` returns exactly those two paths.
6. `git diff HEAD~1 HEAD -- tests/e2e/lib/llm-ledger.js` produces zero output bytes (Phase 56 byte-unchanged invariant preserved).
7. `git diff HEAD~1 HEAD -- scripts/auto-fix.mjs` produces zero output bytes.
8. `git diff HEAD~1 HEAD -- .github/workflows/v40-auto-fix.yml` produces zero output bytes (Pitfall 1 anti-feature pin).
9. `awk 'NR>=89 && NR<=103' scripts/auto-fix-promote.mjs | sha256sum | awk '{print $1}'` STILL equals `5311c1d5364b1b8558d44029432bfb0d5164c19fcc38b9b2d6fcd539c2e0c40f`.
10. `grep -nE '_skipCiGuard:\\s*true' scripts/auto-fix-promote.mjs | grep -vE '^[0-9]+:\\s*//' | wc -l` STILL equals 1.
  </acceptance_criteria>
  <done>
Single atomic feat(58) commit landed locally. All four PROMOTE-* requirements (PROMOTE-01..04) closed by code + Vitest assertions in the same commit. _skipCiGuard load-bearing trust invariant promoted from manual close-note check to executable test. Phase 56's tests/e2e/lib/llm-ledger.js byte-unchanged invariant preserved. Plan 02 (workflow YAML wiring) can begin.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script ← workflow (argv) | --fingerprint and --error-class are untrusted CLI input; both validated via regex before write |
| script → ledger | new appendLedgerEntry imports widen IMPORTS POLICY by exactly one entry; enforced by new Vitest assertion |
| script ↔ assertTripleGate body | Phase 53 byte-unchanged trust invariant; re-pinned in this commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering (Boundary Violation) | new appendLedgerEntry import widens IMPORTS POLICY | mitigate | Comment block at lines 21-30 explicitly names ../tests/e2e/lib/llm-ledger.js in the narrowed ALLOWED list; new Vitest assertion IP1 enforces the audit grep returns zero forbidden lines; IP2 asserts the positive presence of the locked import shape |
| T-58-02 | Tampering (Silent Success Drop) | promotion succeeds at line 446 but no ledger entry | mitigate | New code at line 442-ish writes the entry BEFORE process.stdout.write/process.exit(0); Vitest O1 asserts the entry is written; structural fallback grep count of `appendLedgerEntry(LEDGER_PATH,` == 2 in source |
| T-58-03 | Tampering (Silent Failure Drop) | runPromote returns non-zero but no ledger entry | mitigate | New code at line 439-ish writes the entry BEFORE process.exit(1); Vitest O2 asserts entry + ordering; structural fallback as O1 |
| T-58-04 | A-B Winner Abstention | outcome entries dropped by a-b-winner.mjs isAttributable filter (lacks errorClass) | mitigate | --error-class argv flag plumbed through; entry shape includes errorClass field; workflow YAML (Plan 02) pre-resolves and threads the value |
| T-58-05 | Tampering (Trust Invariant Drift) | assertTripleGate body bytes drift during the edit | mitigate | New Vitest assertion in describe PROMOTE-04 pins the body to a 15-line verbatim string from RESEARCH §3 (sha256 5311c1d5364b1b8558d44029432bfb0d5164c19fcc38b9b2d6fcd539c2e0c40f). Lands in the SAME commit per Phase 58 D-04. |
| T-58-06 | Tampering (_skipCiGuard Widening) | a second `_skipCiGuard:true` literal sneaks into the file | mitigate | New Vitest assertion strips pure-comment lines and asserts exactly one match. Promotes Phase 53 manual close-note check to executable. |
| T-58-SC | Tampering (Supply Chain) | npm/pip/cargo installs | accept | Zero new dependencies introduced in this plan (project zero-new-deps invariant; RESEARCH Package Legitimacy Audit "(none)"); no slopcheck required |
</threat_model>

<verification>
- Single atomic commit `feat(58): wire promote outcome ledger entries (PROMOTE-01..04)` on HEAD
- Only two files in the commit (scripts/auto-fix-promote.mjs, tests/unit/auto-fix-promote-gate.test.js)
- LOAD-BEARING anti-features all preserved (tests/e2e/lib/llm-ledger.js, scripts/auto-fix.mjs, scripts/a-b-winner.mjs, .github/workflows/v40-auto-fix.yml, assertTripleGate body bytes 89-103, _skipCiGuard non-comment count == 1)
- `CI=true npx vitest run tests/unit/auto-fix-promote-gate.test.js` exits 0 with ≥30 tests
- `CI=true npm test` exits 0 modulo pre-existing Phase 51.1 carry-over failures (Phase 60 territory)
- All quality_gate acceptance grep counts pass per Task 1.2 acceptance criteria
</verification>

<success_criteria>
- PROMOTE-01: IMPORTS POLICY narrowed + Vitest enforcement in same commit; allow-list contains exactly 3 patterns (node:*, ./promote-from-quarantine.mjs, ../tests/e2e/lib/llm-ledger.js)
- PROMOTE-02: success-path entry shape `{source:'auto-fix-promoted', outcome:'pass', errorClass, fingerprint, issueId, prNumber, model, cost_usd, ...}` written via appendLedgerEntry(LEDGER_PATH, {...}) before process.exit(0)
- PROMOTE-03: failure-path entry shape `{source:'auto-fix-failed', outcome:'fail', errorClass, fingerprint, issueId, prNumber, reason, ...}` written before process.exit(1) at line 440 ONLY
- PROMOTE-04: assertTripleGate body (lines 89-103) sha256 still equals 5311c1d5364b1b8558d44029432bfb0d5164c19fcc38b9b2d6fcd539c2e0c40f; pinned by Vitest verbatim-string assertion
- _skipCiGuard non-comment count remains 1 (line 434); pinned by Vitest
- All four ROADMAP success criteria for Phase 58 met (a-b-winner abstention exit blocked only on accumulation, not on Phase 58 wiring)
</success_criteria>

<output>
Create `.planning/phases/58-promote-outcome-ledger-entry/58-01-SUMMARY.md` with:
- Commit SHA of feat(58) atomic commit
- Confirmation of all 10 acceptance criteria from Task 1.3
- New Vitest test count (baseline 26 → after Phase 58)
- Verified `awk 'NR>=89 && NR<=103' | sha256sum` still equals locked value
- Verified `_skipCiGuard:\s*true` non-comment count still equals 1
- Note any deviations (e.g., behavioral mock vs structural fallback for O1/O2/O3)
</output>
