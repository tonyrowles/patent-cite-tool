---
phase: 59-fixture-mutator-4-uat-re-sweep
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-00-BASELINE.md
autonomous: true
requirements: []
tags: [phase-59, fixture-mutator, baseline, wave-0]

must_haves:
  truths:
    - "PHASE_59_BASELINE = current git HEAD is captured (commit sha)"
    - "Fingerprint export verified verbatim at scripts/e2e-report-issue.mjs:78-81"
    - "quarantine-append.mjs label-add call verified at line 220-223"
    - "FORBIDDEN_PATHS bank verified as 8 entries at scripts/check-diff-guard.mjs:49-58"
    - "scripts/auto-fix.mjs Step 7 ledger writes confirmed to emit phase field hardcoded to '42-auto-fix'; scripts/auto-fix-promote.mjs outcome ledger writes confirmed to emit phase field hardcoded to '58-promote' (RESEARCH Q1 RESOLVED via Plan 59-03 — phase argv expansion threads PHASE_TAG=56-uat through workflow_dispatch into auto-fix-promote.mjs --phase argv so live UAT ledger entries on origin/main carry phase: '56-uat' per REQUIREMENTS.md SWEEP-05 literal wording; see CONTEXT.md Decision C)"
  artifacts:
    - path: ".planning/phases/59-fixture-mutator-4-uat-re-sweep/59-00-BASELINE.md"
      provides: "Wave 0 invariant baseline — anchor for downstream byte-equality checks"
      contains: "PHASE_59_BASELINE sha; verified line numbers; RESEARCH Q1 resolution"
  key_links:
    - from: ".planning/phases/59-fixture-mutator-4-uat-re-sweep/59-00-BASELINE.md"
      to: "scripts/e2e-report-issue.mjs:78-81"
      via: "captured grep output of fingerprint function signature"
      pattern: "export function fingerprint"
    - from: ".planning/phases/59-fixture-mutator-4-uat-re-sweep/59-00-BASELINE.md"
      to: "scripts/quarantine-append.mjs:218-223"
      via: "captured line range showing label-add conditional"
      pattern: "if \\(finalEntry\\.stable_runs >= STABLE_RUNS_THRESHOLD"
---

<objective>
Wave 0 verification: capture PHASE_59_BASELINE git sha and verify the four
load-bearing source coordinates that every downstream task (59-01 and 59-02)
depends on. Resolve RESEARCH Open Question 1 (does scripts/auto-fix.mjs Step 7
ledger write emit a `phase` field?) so 59-02's SWEEP-05 evidence plan is
deterministic.

Purpose: Cheap (read-only) anchor commit that prevents downstream tasks from
drifting against a moved line number. Mirrors Phase 58-00's baseline pattern.

Output: `.planning/phases/59-fixture-mutator-4-uat-re-sweep/59-00-BASELINE.md`
recording every verified coordinate + the resolution of RESEARCH Q1.

No code is modified in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-fixture-mutator-4-uat-re-sweep/59-CONTEXT.md
@.planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md
@.planning/phases/59-fixture-mutator-4-uat-re-sweep/59-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture PHASE_59_BASELINE + verify locked coordinates + resolve RESEARCH Q1</name>
  <files>.planning/phases/59-fixture-mutator-4-uat-re-sweep/59-00-BASELINE.md</files>
  <read_first>
    - scripts/e2e-report-issue.mjs:70-102 (fingerprint + supporting helpers)
    - scripts/quarantine-append.mjs:25-32 (READY_FOR_PROMOTION_LABEL + STABLE_RUNS_THRESHOLD constants)
    - scripts/quarantine-append.mjs:215-226 (atomic write + label-add conditional)
    - scripts/check-diff-guard.mjs:45-58 (FORBIDDEN_PATHS Object.freeze block)
    - scripts/auto-fix.mjs:170-180 (PHASE const declaration)
    - scripts/auto-fix.mjs:715-735 (Step 7 dispatcher ledger-write site)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §Fingerprint Computation Contract (lines 431-465)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §FORBIDDEN_PATHS Confirmation (lines 607-651)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md Open Question 1 (lines 1378-1381)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md Assumption A6 (line 1369)
  </read_first>
  <action>
    Create `.planning/phases/59-fixture-mutator-4-uat-re-sweep/59-00-BASELINE.md` with five sections:

    1. **PHASE_59_BASELINE**: run `git rev-parse HEAD` and record the resulting sha as the byte-equality anchor for plan 59-01 (mutator changes) and plan 59-02 (UAT evidence captures).

    2. **Fingerprint contract**: paste the exact lines 78-81 of `scripts/e2e-report-issue.mjs` showing `export function fingerprint(caseId, errorClass, topOfStackHash)` + body (12-hex sha256 prefix). Confirm the formula matches the one auto-fix.mjs parses at line ~231 with regex `/<!-- fp: ([0-9a-f]{12}) -->/m` (v2 short-form marker). Record that the mutator (plan 59-01) MUST import this function — re-implementation is a T-59-01 violation.

    3. **quarantine-append label-add coordinates**: paste lines 218-223 of `scripts/quarantine-append.mjs` showing the conditional `if (finalEntry.stable_runs >= STABLE_RUNS_THRESHOLD && ghClient && triageIssueNumber != null) { ghClient.addLabel(...) }`. Record `READY_FOR_PROMOTION_LABEL = 'quarantine:ready-for-promotion'` constant location (line 29). Record `STABLE_RUNS_THRESHOLD = 3` (line 30). Plan 59-01 Task 4 modifies this conditional in place; baseline freezes the surrounding bytes.

    4. **FORBIDDEN_PATHS bank**: paste lines 49-58 of `scripts/check-diff-guard.mjs` listing all 8 frozen regexes. Confirm `.planning/...` is NOT in the bank (so plan 59-01's mutator may safely write `56-MUTATOR-CLEANUP.md` to the phase dir).

    5. **RESEARCH Q1 resolution — RESOLVED via Plan 59-03**: paste line 174 of `scripts/auto-fix.mjs` showing `const PHASE = '42-auto-fix'` and one representative call site (e.g., line 419-425 `safeAppendLedger({..., phase: PHASE, ...})`). ALSO paste `scripts/auto-fix-promote.mjs:502` and `:522` showing the two `phase: '58-promote'` hardcoded literals on the outcome ledger writes (PROMOTE-02 + PROMOTE-03 paths). Resolution: Phase 59 expands scope to honor REQUIREMENTS.md SWEEP-05 literal wording ("all UAT ledger entries carry `phase: '56-uat'` for filterable production analysis"). Per the user's consistent Phase 56/58 pattern of expanding scope to honor literal REQUIREMENTS.md wording when downstream consumers depend on it, Plan 59-03 (Wave 2, parallel with 59-02) threads a `--phase` argv flag through `scripts/auto-fix-promote.mjs` (mirroring Phase 58's `--fingerprint` / `--error-class` / `--model` plumbing pattern verbatim). Default value: `'58-promote'` (preserves the current hardcoded literal at lines 502/522 so non-UAT runs are byte-unchanged on the entry shape). When invoked with `--phase 56-uat`, the outcome ledger entries committed to origin/main carry `phase: '56-uat'` and are filterable via standard jq queries WITHOUT requiring evidence-file wrappers. The workflow YAML (`.github/workflows/v40-auto-promote.yml`) gains a `workflow_dispatch.inputs.PHASE_TAG` input (default `''`) that the `Triple-gate + runPromote` step threads into `--phase` argv when non-empty; the normal `pull_request` trigger path leaves PHASE_TAG empty and the script defaults back to `'58-promote'`. SWEEP-03 and SWEEP-04 operator runbooks (plan 59-02) trigger the workflow via `gh workflow run v40-auto-promote.yml -f PHASE_TAG=56-uat` (mutator-driven full-loop runs go through the normal trigger; the PHASE_TAG only matters AT auto-promote time, not at auto-fix time — UAT runs accept that the upstream `auto-fix-api` entry carries `phase: '42-auto-fix'` because auto-fix.mjs has no `--phase` flag and Phase 59 does NOT scope-expand into auto-fix.mjs; the FILTERABLE consumer entry is the auto-fix-promote.mjs outcome entry, which is what SWEEP-05 demonstrates). See CONTEXT.md "Phase 59 Scope Adjustment Decision C" for the full rationale.

    No source files are modified. The deliverable is a single markdown file.

    Closing line: `BASELINE COMPLETE — PHASE_59_BASELINE = <sha>; downstream plans MUST verify byte-equality against this anchor.`
  </action>
  <verify>
    <automated>test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-00-BASELINE.md &amp;&amp; grep -q 'PHASE_59_BASELINE' .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-00-BASELINE.md &amp;&amp; grep -q 'fingerprint' .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-00-BASELINE.md &amp;&amp; grep -q 'FORBIDDEN_PATHS' .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-00-BASELINE.md &amp;&amp; grep -q 'RESEARCH Q1' .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-00-BASELINE.md &amp;&amp; grep -q 'RESOLVED via Plan 59-03' .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-00-BASELINE.md &amp;&amp; grep -q "58-promote" .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-00-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/59-fixture-mutator-4-uat-re-sweep/59-00-BASELINE.md` exists
    - PHASE_59_BASELINE sha is captured (40-hex git sha)
    - Fingerprint function signature pasted from scripts/e2e-report-issue.mjs:78-81 with note that re-implementation = T-59-01 violation
    - quarantine-append.mjs lines 218-223 pasted with READY_FOR_PROMOTION_LABEL + STABLE_RUNS_THRESHOLD context
    - All 8 FORBIDDEN_PATHS regexes from scripts/check-diff-guard.mjs:49-58 pasted verbatim
    - RESEARCH Q1 resolution documented: phase field IS emitted (auto-fix.mjs hardcoded '42-auto-fix'; auto-fix-promote.mjs hardcoded '58-promote' at lines 502/522); RESOLVED via Plan 59-03 which adds --phase argv to auto-fix-promote.mjs so SWEEP-05 outcome entries carry phase: '56-uat' on origin/main (per REQUIREMENTS.md SWEEP-05 literal wording)
    - Closing line: BASELINE COMPLETE with sha
    - Working tree untouched outside `.planning/phases/59-*/`: `git status --porcelain | grep -v '^?? \.planning/phases/59-' | grep -v '^M  \.planning/phases/59-'` returns empty
  </acceptance_criteria>
  <done>Baseline markdown file committed with all five sections populated and verification grep passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → planning artifact | Read-only inspection writes a derived markdown file under `.planning/`; no code or workflow paths touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Tampering | Fingerprint formula divergence (referenced from baseline) | mitigate | Baseline freezes the fingerprint source coordinates so plan 59-01 has zero ambiguity about which function to import; downstream task 59-01-T1 imports from scripts/e2e-report-issue.mjs:78-81 by name and is verified by grep |
| T-59-03 | Tampering | FORBIDDEN_PATHS bank shift (referenced from baseline) | mitigate | Baseline records the 8 regexes verbatim; if a future plan touches scripts/check-diff-guard.mjs the byte-equality anchor catches it before merge |
</threat_model>

<verification>
- `test -f` baseline markdown file
- `grep -q` for required section anchors
- Working tree clean apart from the new baseline file under `.planning/phases/59-*/`
</verification>

<success_criteria>
- BASELINE.md is committed
- Every coordinate that plan 59-01 and 59-02 cite is anchored in baseline
- RESEARCH Q1 is resolved with explicit "RESOLVED via Plan 59-03" verdict: a `--phase` argv flag is added to `scripts/auto-fix-promote.mjs` (NOT to auto-fix.mjs) so SWEEP-05 ledger entries carry `phase: '56-uat'` on origin/main per REQUIREMENTS.md literal wording; mirrors Phase 58 Decision A/B scope-expansion pattern
- No source files modified; ready to plan 59-01 against a frozen baseline
</success_criteria>

<output>
Create `.planning/phases/59-fixture-mutator-4-uat-re-sweep/59-00-SUMMARY.md` when done summarizing the PHASE_59_BASELINE sha, the verified coordinates, and the RESEARCH Q1 resolution.
</output>
