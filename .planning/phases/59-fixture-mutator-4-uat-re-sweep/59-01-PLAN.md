---
phase: 59-fixture-mutator-4-uat-re-sweep
plan: 01
type: execute
wave: 1
depends_on: ["59-00"]
files_modified:
  - tests/e2e/scripts/inject-defect.mjs
  - tests/e2e/scripts/e2e-inject-defect.test.js
  - scripts/quarantine-append.mjs
  - tests/e2e/scripts/e2e-quarantine-append.test.js
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/56-MUTATOR-CLEANUP.md
autonomous: true
requirements: [MUTATOR-01, MUTATOR-02, MUTATOR-03, MUTATOR-04, MUTATOR-05]
tags: [phase-59, fixture-mutator, work-stream-a, mutator, deterministic]

must_haves:
  truths:
    - "Operator can run `node tests/e2e/scripts/inject-defect.mjs --seed mutator-seed-1 --error-class GOOGLE_DOM_DRIFT` and a synthetic triage-labeled GitHub issue is created"
    - "Operator running the mutator twice with the same seed against an open synthetic issue HARD ABORTS with exit code 2 (not a warning) — fingerprint collision protection"
    - "After mutator runs, `git status --porcelain` shows ONLY the cleanup-evidence file under .planning/phases/59-*/56-MUTATOR-CLEANUP.md (or nothing) — zero FORBIDDEN_PATHS mutation"
    - "Synthetic quarantine entries carrying `source_triage_finding_id` starting with 'fixture-mutator-uat-47b' never receive `quarantine:ready-for-promotion` label — co-designed in same commit as inject-defect.mjs"
    - "Cleanup evidence file `56-MUTATOR-CLEANUP.md` is emitted with explicit `gh issue close`, `gh pr close --delete-branch`, and quarantine-revert commands"
    - "`CI=true npx vitest run tests/e2e/scripts/e2e-inject-defect.test.js tests/e2e/scripts/e2e-quarantine-append.test.js` exits 0"
  artifacts:
    - path: "tests/e2e/scripts/inject-defect.mjs"
      provides: "MUTATOR-01..03, 05 — synthetic-defect injector at issue-creation layer"
      contains: "import { fingerprint } from '../../../scripts/e2e-report-issue.mjs'"
      min_lines: 120
    - path: "tests/e2e/scripts/e2e-inject-defect.test.js"
      provides: "Vitest contract tests for inject-defect.mjs (I1-I8)"
      contains: "describe('inject-defect.mjs"
      min_lines: 150
    - path: "scripts/quarantine-append.mjs"
      provides: "MUTATOR-04 — source-tag suppression at line 218-223 conditional"
      contains: "fixture-mutator-uat-47b"
    - path: "tests/e2e/scripts/e2e-quarantine-append.test.js"
      provides: "MUTATOR-04 — G9 describe block extending existing test file"
      contains: "MUTATOR-04 source-tag suppression"
    - path: ".planning/phases/59-fixture-mutator-4-uat-re-sweep/56-MUTATOR-CLEANUP.md"
      provides: "MUTATOR-05 — cleanup runbook emitted by mutator on each run"
      contains: "gh issue close"
  key_links:
    - from: "tests/e2e/scripts/inject-defect.mjs"
      to: "scripts/e2e-report-issue.mjs:78-81"
      via: "ESM import of fingerprint()"
      pattern: "import .* fingerprint .* from .*scripts/e2e-report-issue\\.mjs"
    - from: "tests/e2e/scripts/inject-defect.mjs"
      to: "github (network)"
      via: "execSync of `gh issue create --label triage --label <ERROR_CLASS> --body-file -`"
      pattern: "gh issue create"
    - from: "scripts/quarantine-append.mjs:218-223"
      to: "tests/e2e/scripts/inject-defect.mjs"
      via: "matched source-tag string `fixture-mutator-uat-47b` — co-designed in SAME commit"
      pattern: "fixture-mutator-uat-47b"
---

<objective>
Ship the fixture mutator: a deterministic Node CLI that creates a synthetic
triage-labeled GitHub issue (without touching any FORBIDDEN_PATHS file in the
working tree), with co-designed source-tag suppression in
`scripts/quarantine-append.mjs` so the synthetic case can never auto-promote to
the golden corpus. Closes MUTATOR-01..05.

Purpose: Work stream A is the deterministic prerequisite for SWEEP-04 in plan
59-02 (Work stream B requires this script to drive UAT-47-b's synthetic loop)
and provides the recommended deterministic trigger path for SWEEP-03 as well.
This plan ships locally and atomically; it does NOT require any push to
origin/main and does NOT depend on PR #18 merging.

Output: One new script, one new Vitest contract file, a single-conditional
patch to `scripts/quarantine-append.mjs`, a co-located G9 test block in
`tests/e2e/scripts/e2e-quarantine-append.test.js`, and the cleanup-evidence
template file `56-MUTATOR-CLEANUP.md`.

Atomicity: All five files ship in ONE commit per MUTATOR-04 wording
("co-designed in the same commit").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/59-fixture-mutator-4-uat-re-sweep/59-CONTEXT.md
@.planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md
@.planning/phases/59-fixture-mutator-4-uat-re-sweep/59-VALIDATION.md
@.planning/phases/59-fixture-mutator-4-uat-re-sweep/59-00-BASELINE.md
@.planning/research/PITFALLS.md
@scripts/e2e-report-issue.mjs
@scripts/quarantine-append.mjs
@scripts/check-diff-guard.mjs
@tests/e2e/scripts/e2e-quarantine-append.test.js

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase + RESEARCH. -->
<!-- Executor uses these directly — no codebase exploration needed. -->

From scripts/e2e-report-issue.mjs:78-81 (REQUIRED IMPORT TARGET):
  export function fingerprint(caseId, errorClass, topOfStackHash)
  - returns 12-hex sha256 prefix of `${caseId}|${errorClass}|${topOfStackHash || ''}`

From scripts/e2e-report-issue.mjs:507-518 (REFERENCE ARGV PATTERN for issue create):
  gh issue create --title "<escaped>" --label <a> --label <b> --body-file -
  - body passed via execSync({ input: body, encoding: 'utf8' })
  - title double-quotes escaped via `.replaceAll('"', '\\"')`

From scripts/e2e-report-issue.mjs:522-535 (REFERENCE ARGV PATTERN for issue search):
  gh issue list --search '<escaped>' --state open --json number,title,body,updatedAt --limit 30
  - single-quote escape via `.replaceAll("'", "'\\''")` (T-35-03-03)

From scripts/quarantine-append.mjs:25-30 (existing constants — DO NOT REDEFINE):
  CORPUS_PATH (with QUARANTINE_CORPUS_PATH_OVERRIDE env support)
  ALLOWED_INPUT_ROOTS
  READY_FOR_PROMOTION_LABEL = 'quarantine:ready-for-promotion'
  STABLE_RUNS_THRESHOLD = 3

From scripts/quarantine-append.mjs:218-223 (EXACT EDIT POINT for MUTATOR-04):
  let addedLabel = false;
  if (finalEntry.stable_runs >= STABLE_RUNS_THRESHOLD && ghClient && triageIssueNumber != null) {
    ghClient.addLabel(triageIssueNumber, READY_FOR_PROMOTION_LABEL);
    addedLabel = true;
  }

From scripts/quarantine-append.mjs:138-148 (formatEntry — DO NOT MODIFY schema):
  Existing key order: id, patentFile, selectedText, category, stable_runs,
                      source_triage_finding_id, added_iso
  No `source` field today — use source_triage_finding_id startsWith check
  (RESEARCH §quarantine-append.mjs Suppression Edit Point lines 553-577).

From scripts/check-diff-guard.mjs:49-58 (FORBIDDEN_PATHS — DO NOT TOUCH):
  /^tests\/test-cases\.js$/
  /^tests\/golden\/baseline\.json$/
  /^tests\/e2e\/test-cases-quarantine\.js$/
  /^\.github\/workflows\/v40-[^/]*\.yml$/
  /^tests\/e2e\/\.llm-spend-ledger\.json$/
  /^\.github\/CODEOWNERS$/
  /^tests\/e2e\/\.rerun-ring-buffer\.json$/
  /^tests\/e2e\/\.flake-suppression\.json$/

ERROR_CLASS allowlist (validate --error-class argv against this):
  WRONG_CITATION, LLM_HALLUCINATED_SELECTION, WORKER_FALLBACK_FAILED,
  GOOGLE_DOM_DRIFT, HARNESS_ERROR
  (FLAKE / LLM_API_ERROR / PASS are workflow-accepted but not for synthetic
  injection; recommend GOOGLE_DOM_DRIFT default per RESEARCH §477-481.)

Source-tag (co-designed string — MUST match across all three files):
  'fixture-mutator-uat-47b'
  - inject-defect.mjs emits this in synthetic triage report run_id
  - quarantine-append.mjs:218-223 conditional checks
    finalEntry.source_triage_finding_id.startsWith('fixture-mutator-uat-47b')
  - tests reference the same literal

Mock-gh test pattern (mirror from tests/e2e/scripts/e2e-quarantine-append.test.js:50-94):
  - Write bash script `gh` to tmpDir/gh (mode 0o755)
  - Body echoes "$@" to transcript; case-matches on $1=issue, $2=list/create/edit
  - Test sets env PATH = mockGhDir + ':' + process.env.PATH
  - spawnSync('node', [SCRIPT_PATH, ...args], { env, timeout: 5000 })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tests/e2e/scripts/inject-defect.mjs (MUTATOR-01, 02, 03, 05) + emit cleanup-evidence template</name>
  <files>tests/e2e/scripts/inject-defect.mjs, .planning/phases/59-fixture-mutator-4-uat-re-sweep/56-MUTATOR-CLEANUP.md</files>
  <read_first>
    - scripts/e2e-report-issue.mjs:1-30 (import block — mirror for inject-defect.mjs)
    - scripts/e2e-report-issue.mjs:78-81 (fingerprint — import target)
    - scripts/e2e-report-issue.mjs:478-540 (makeRealGhClient reference patterns for createIssueWithLabels + listOpenWithSearch — for argv shape, NOT for direct import; mutator shells out directly via execSync)
    - scripts/quarantine-append.mjs:381-405 (ESM "is-main-module" detection pattern at the bottom of file — for invocation gate)
    - tests/e2e/scripts/e2e-quarantine-append.test.js:50-94 (mock-gh pattern — informs test design in Task 2 but useful here for argv-shape contract)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §Fingerprint Computation Contract (lines 431-465)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §`gh issue create` Argv Plumbing (lines 467-495)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §Fingerprint Collision Pre-Flight (lines 498-535)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §FORBIDDEN_PATHS Confirmation (lines 607-651)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §Code Examples 1, 2, 3 (lines 1248-1310)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §Security Domain (lines 1452-1476) — input validation regex constraints
  </read_first>
  <action>
    Create `tests/e2e/scripts/inject-defect.mjs` as a Node 22 ESM CLI script following the exact RESEARCH §Code Examples 1-3 contracts.

    Required structure (no fenced code in this directive — author the file using the cited contracts):

    1. Imports block: `node:child_process` execSync, `node:fs`, `node:path`, `node:url` fileURLToPath. Import `{ fingerprint }` from `'../../../scripts/e2e-report-issue.mjs'` — relative path from `tests/e2e/scripts/` to `scripts/`. This is the byte-identical reuse contract (T-59-01 mitigation per RESEARCH §Anti-Patterns lines 393-394).

    2. Constants block at top:
       - `ERROR_CLASSES` set literal containing exactly `WRONG_CITATION`, `LLM_HALLUCINATED_SELECTION`, `WORKER_FALLBACK_FAILED`, `GOOGLE_DOM_DRIFT`, `HARNESS_ERROR`.
       - `SOURCE_TAG = 'fixture-mutator-uat-47b'` — the co-designed string referenced by `scripts/quarantine-append.mjs:218-223` modification in Task 3.
       - `SEED_RE = /^[a-zA-Z0-9_-]+$/` — argv input validation regex per RESEARCH §Security Domain (T-Tampering on `--seed` argv).

    3. `parseArgs(argv)` helper: parses `--seed <value>`, `--error-class <value>`, AND `--phase-dir <value>` flags in the `e2e-triage-classifier.mjs` style (long-flag with whitespace separator; equals-syntax rejected with exit 2). Defaults: `seed = 'mutator-seed-1'`, `errorClass = 'GOOGLE_DOM_DRIFT'`, `phaseDir = '.planning/phases/59-fixture-mutator-4-uat-re-sweep'` (the real Phase 59 directory; Task 2 Vitest tests override this with a tmpDir via `--phase-dir <tmp>`). Validate `seed` against `SEED_RE` (reject `process.exit(2)` with stderr `[inject-defect] invalid --seed`). Validate `errorClass` against `ERROR_CLASSES.has(value)` (reject `process.exit(2)` with stderr `[inject-defect] unknown --error-class`). `phaseDir` does NOT need regex validation (the only consumer is `fs.writeFileSync` / `fs.appendFileSync` in `emitCleanupEvidence` against a path computed under it; mutator runs locally and operator owns the argv). Thread `phaseDir` through to both `verifyWorkingTreeClean` and `emitCleanupEvidence`.

    4. `computeFingerprint({ seed, errorClass })` helper: `caseId = `synthetic-${seed}``; returns `fingerprint(caseId, errorClass, null)` — passing `null` per RESEARCH §Fingerprint Computation Contract Option 1 (CLEAN, recommended).

    5. `collisionCheckOrAbort({ fp })` helper (MUTATOR-02): builds marker `<!-- fp: ${fp} -->`; shell-escapes via `.replaceAll("'", "'\\''")`; runs `execSync(`gh issue list --search '${escaped}' --state open --json number --limit 5`, { encoding: 'utf8' })`. Parses JSON; if `result.length > 0`, writes stderr `[inject-defect] HARD ABORT: open issue #${result[0].number} already carries fp ${fp}. Either close it first or use a different --seed.` and `process.exit(2)`. Exit code 2 distinguishes "collision abort" from "runtime error" (exit 1) per RESEARCH §Hard-abort logic line 535.

    6. `buildBody({ fp, caseId, seed, errorClass })` helper: returns the multi-line string with line 1 = `<!-- fp: ${fp} -->`, blank line, `### Reproducer`, `case-id: ${caseId}`, `seed: ${seed}`, `error-class: ${errorClass}`, `### Synthetic Defect` section explaining this is a UAT-47-b synthetic and stating `Source: ${SOURCE_TAG}`. Line 1 marker MUST be exact per RESEARCH §Marker placement line 458 (auto-fix.mjs:231 regex `/<!-- fp: ([0-9a-f]{12}) -->/m`).

    7. `createIssue({ caseId, errorClass, body })` helper (MUTATOR-01): mirrors `scripts/e2e-report-issue.mjs:507-518` pattern — `gh issue create --title "[fixture-mutator] ${caseId}: ${errorClass}" --label triage --label ${errorClass} --body-file -`, body passed via `{ input: body, encoding: 'utf8' }`. Title is constructed from validated argv so no quote-escape vulnerability; still apply `.replaceAll('"', '\\"')` defensively per the e2e-report-issue.mjs pattern. Parse issue number from `/issues/(\d+)$` regex. Return `{ issueNum, url }`.

    8. `verifyWorkingTreeClean({ phaseDir })` helper (MUTATOR-03): runs `git status --porcelain`; splits to lines; constructs `allowedPattern` regex matching `?? ${phaseDir}/56-MUTATOR-CLEANUP.md` (escape the phaseDir for regex). For every line not matching the allowedPattern, FATAL exit. Per RESEARCH §Recommended verification snippet (lines 640-650).

    9. `emitCleanupEvidence({ phaseDir, issueNum, fp, seed, errorClass, sourceTag })` helper (MUTATOR-05): writes (APPEND mode per Open Question 3 recommendation; create file with header if missing) to `${phaseDir}/56-MUTATOR-CLEANUP.md` a section block per invocation. Each section contains:
       - `## Run YYYY-MM-DDTHH:MM:SSZ — issue #${issueNum} fp ${fp}` header
       - Bulleted metadata (seed, errorClass, sourceTag)
       - Triple-backtick bash block with the literal `gh issue close ${issueNum} --reason "not planned"` command
       - Triple-backtick bash block with the literal `gh pr close <PR_NUMBER> --delete-branch` placeholder command and a brief note ("populate after auto-fix loop opens PR")
       - Triple-backtick bash block with the quarantine-revert pattern: `git checkout -b chore/sweep-06-cleanup-<TS>` then a `node tests/e2e/scripts/uat-cleanup.mjs --revert-quarantine --source-tag ${sourceTag}` placeholder
       - A "fingerprint search" line: `gh issue list --search '<!-- fp: ${fp} -->'`
       (The cleanup runbook is the manifest SWEEP-06 reads; plan 59-02 SWEEP-06 task will create the actual uat-cleanup.mjs script.)

    10. Main IIFE / `main()` function: orchestrates parseArgs → compute fingerprint → collision check → build body → create issue → verify working tree → emit cleanup. Threads the `phaseDir` value from parseArgs into BOTH `verifyWorkingTreeClean({ phaseDir })` and `emitCleanupEvidence({ phaseDir, issueNum, fp, seed, errorClass, sourceTag: SOURCE_TAG })`. Prints `[inject-defect] issue #${issueNum} created with fingerprint ${fp}` to stdout (this stdout shape is parsed by 59-02 SWEEP-03 and SWEEP-04 runbooks — KEEP STABLE). Exit 0 on success.

    11. ESM entry-point gate at bottom (mirrors scripts/quarantine-append.mjs:381-383 pattern): `if (process.argv[1] !== undefined && fileURLToPath(import.meta.url) === path.resolve(process.argv[1])) { main(process.argv); }`. This makes the script unit-testable (helpers can be imported without auto-executing).

    12. Export the helper functions (`parseArgs`, `computeFingerprint`, `collisionCheckOrAbort`, `buildBody`, `createIssue`, `verifyWorkingTreeClean`, `emitCleanupEvidence`, `SOURCE_TAG`, `ERROR_CLASSES`) so Task 2's Vitest contract tests can import them. Match the export style of `scripts/quarantine-append.mjs` (named exports).

    Per CONTEXT.md "smallest-diff" preference, do NOT introduce additional abstractions (no ghClient class, no wrapper functions for execSync, no logger). The script is glue around three execSync calls and one file write.

    SECONDARY FILE: also create the initial `.planning/phases/59-fixture-mutator-4-uat-re-sweep/56-MUTATOR-CLEANUP.md` skeleton header (a single H1 line plus a brief "this file is appended to by `tests/e2e/scripts/inject-defect.mjs` on every invocation" note). The first real Run section gets appended at execution time. This makes the file exist at planning-time as a tracked artifact reference (per RESEARCH §Recommended Project Structure line 271 listing).
  </action>
  <verify>
    <automated>test -f tests/e2e/scripts/inject-defect.mjs &amp;&amp; test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/56-MUTATOR-CLEANUP.md &amp;&amp; grep -E "import \{ ?fingerprint ?\} from .*scripts/e2e-report-issue\.mjs" tests/e2e/scripts/inject-defect.mjs &amp;&amp; grep -E "SOURCE_TAG\s*=\s*['\"]fixture-mutator-uat-47b['\"]" tests/e2e/scripts/inject-defect.mjs &amp;&amp; grep -v '^#' tests/e2e/scripts/inject-defect.mjs | grep -c 'gh issue list' | { read N; [ "$N" -ge 1 ]; } &amp;&amp; grep -v '^#' tests/e2e/scripts/inject-defect.mjs | grep -c 'gh issue create' | { read N; [ "$N" -ge 1 ]; } &amp;&amp; grep -E "git status --porcelain" tests/e2e/scripts/inject-defect.mjs &amp;&amp; node -e "import('./tests/e2e/scripts/inject-defect.mjs').then(m =&gt; { if (typeof m.computeFingerprint !== 'function') process.exit(1); if (m.SOURCE_TAG !== 'fixture-mutator-uat-47b') process.exit(1); const fp = m.computeFingerprint({ seed: 'mutator-seed-1', errorClass: 'GOOGLE_DOM_DRIFT' }); if (!/^[0-9a-f]{12}$/.test(fp)) process.exit(1); console.log('OK fp=' + fp); }).catch(e =&gt; { console.error(e); process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/scripts/inject-defect.mjs` exists
    - File imports `fingerprint` from `scripts/e2e-report-issue.mjs` (byte-identical reuse — T-59-01 mitigation)
    - `SOURCE_TAG = 'fixture-mutator-uat-47b'` literal present
    - At least one `gh issue list` and one `gh issue create` execSync call
    - `git status --porcelain` check present (MUTATOR-03)
    - Helper functions exported as named exports for Task 2 to import
    - `node -e import.then(...)` smoke check: computeFingerprint returns 12-hex for deterministic seed inputs; SOURCE_TAG equals 'fixture-mutator-uat-47b'
    - Cleanup-evidence skeleton file `56-MUTATOR-CLEANUP.md` exists
    - No file in FORBIDDEN_PATHS modified: `git diff --name-only HEAD~ -- tests/test-cases.js tests/golden/baseline.json tests/e2e/test-cases-quarantine.js '.github/workflows/v40-*.yml' tests/e2e/.llm-spend-ledger.json .github/CODEOWNERS tests/e2e/.rerun-ring-buffer.json tests/e2e/.flake-suppression.json` returns empty (run after commit only — staged-only check at task time: `git diff --cached --name-only -- tests/test-cases.js tests/golden/baseline.json tests/e2e/test-cases-quarantine.js '.github/workflows/v40-*.yml' tests/e2e/.llm-spend-ledger.json .github/CODEOWNERS tests/e2e/.rerun-ring-buffer.json tests/e2e/.flake-suppression.json` returns empty)
  </acceptance_criteria>
  <done>inject-defect.mjs executes its parseArgs/computeFingerprint helpers as importable ESM; cleanup-evidence skeleton file is present; zero FORBIDDEN_PATHS file modifications.</done>
</task>

<task type="auto">
  <name>Task 2: Author Vitest contract tests tests/e2e/scripts/e2e-inject-defect.test.js (I1-I8 covering MUTATOR-01, 02, 03, 05)</name>
  <files>tests/e2e/scripts/e2e-inject-defect.test.js</files>
  <read_first>
    - tests/e2e/scripts/e2e-quarantine-append.test.js:1-95 (mock-gh test scaffold, spawnSync helper, env-injection pattern — the canonical template per RESEARCH §Reference 1)
    - tests/e2e/scripts/e2e-quarantine-append.test.js:96-203 (existing describe block structure G1-G8 — match the pattern)
    - tests/e2e/scripts/e2e-explore-ci-guard.test.js:25-63 (spawnSync hermetic env pattern — alternate reference)
    - tests/e2e/scripts/inject-defect.mjs (the script under test — authored in Task 1)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §Recommended test coverage (lines 688-710)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §Vitest Patterns (lines 654-712)
  </read_first>
  <action>
    Create `tests/e2e/scripts/e2e-inject-defect.test.js` mirroring the structure of `tests/e2e/scripts/e2e-quarantine-append.test.js`. Use `describe` / `it` from `vitest`, NOT `jest`. Test layout:

    A. **Setup (beforeEach):**
       - Create `mockGhDir` via `fs.mkdtempSync(path.join(os.tmpdir(), 'pct-inject-mockgh-'))`.
       - Write a bash mock-gh binary to `${mockGhDir}/gh` mode 0o755 mirroring lines 53-65 of e2e-quarantine-append.test.js. Default behavior: `gh issue list` echoes `[]` (no collision); `gh issue create` echoes a fake URL `https://github.com/test/test/issues/142`. Log all args to `${mockGhDir}/gh-transcript.txt`.
       - Create `phaseDirOverride` tmpDir for cleanup-evidence file writes (so tests do not touch the real `.planning/phases/59-*/` dir). Tests pass the tmpDir via the `--phase-dir` argv flag (Task 1 item 3 parses it and threads through to emitCleanupEvidence). Smoke check via `spawnHelper(['--seed', 'mutator-seed-1', '--error-class', 'GOOGLE_DOM_DRIFT', '--phase-dir', phaseDirOverride])`.
       - Initialize the tmpDir as a git repo (`git init`, `git config user.email`, `git config user.name`, `git commit --allow-empty -m initial`) so the `git status --porcelain` MUTATOR-03 check has a working tree.

    B. **spawnHelper(args, extraEnv)**: runs `spawnSync('node', [SCRIPT_PATH, ...args], { env: { ...process.env, PATH: mockGhDir + ':' + process.env.PATH, ...extraEnv }, cwd: tmpGitRepoDir, encoding: 'utf8', timeout: 10000 })`.

    C. **afterEach**: `fs.rmSync(mockGhDir, { recursive: true, force: true })` and likewise for tmpGitRepoDir.

    D. **Describe blocks (8 tests total, mirroring RESEARCH §Recommended test coverage):**

       1. `describe('inject-defect.mjs — fingerprint determinism (MUTATOR-01)', () => { ... })`:
          - **I1**: import helper directly (`import { computeFingerprint } from './inject-defect.mjs'`). Call twice with `{ seed: 'mutator-seed-1', errorClass: 'GOOGLE_DOM_DRIFT' }`. Assert results equal AND match `/^[0-9a-f]{12}$/`.
          - **I2**: call with different seeds; assert results differ AND each matches `/^[0-9a-f]{12}$/`.

       2. `describe('inject-defect.mjs — collision check (MUTATOR-02 / Pitfall 6)', () => { ... })`:
          - **I3**: override mock-gh to echo `[{"number":42}]` on `gh issue list`. spawnHelper with `--seed mutator-seed-collision --error-class GOOGLE_DOM_DRIFT`. Assert `result.status === 2` AND `result.stderr` matches `/HARD ABORT/`.
          - **I4**: default mock-gh (returns `[]`). spawnHelper succeeds (`result.status === 0`); transcript contains `issue create` after the `issue list`.

       3. `describe('inject-defect.mjs — gh argv plumbing (MUTATOR-01)', () => { ... })`:
          - **I5**: After successful spawn (default mock), read transcript; assert it contains BOTH `--label triage` AND `--label GOOGLE_DOM_DRIFT` (or whichever errorClass argv was passed). Use `expect(transcript).toMatch(/--label triage/)` and `expect(transcript).toMatch(/--label GOOGLE_DOM_DRIFT/)`.
          - **I6**: After successful spawn, the mock-gh transcript shows `issue create` arg. The body content is passed via stdin (`--body-file -`), so confirm `--body-file -` appears in transcript. Assert `expect(transcript).toMatch(/issue create.*--body-file -/)`.

       4. `describe('inject-defect.mjs — FORBIDDEN_PATHS gate (MUTATOR-03)', () => { ... })`:
          - **I7**: After successful spawn in clean tmp git repo, run `execSync('git status --porcelain', { cwd: tmpGitRepoDir })` and assert ONLY entries matching `^\?\? \.planning/phases/59-[^/]+/56-MUTATOR-CLEANUP\.md$` appear (or zero entries). The mutator should NOT modify any tracked file. Note: the cleanup file is written to `phaseDirOverride` not into the tmpGitRepoDir — assert that no `.planning/phases/...` file appears in `tmpGitRepoDir`'s `git status` output BUT the cleanup file exists at `phaseDirOverride/56-MUTATOR-CLEANUP.md`.

       5. `describe('inject-defect.mjs — cleanup evidence emission (MUTATOR-05)', () => { ... })`:
          - **I8**: After successful spawn, read `${phaseDirOverride}/56-MUTATOR-CLEANUP.md`. Assert contains: `gh issue close`, `gh pr close --delete-branch`, `gh issue list --search`, and the literal `fixture-mutator-uat-47b` source-tag string. Assert the fingerprint computed in I1 appears in the file (proving per-invocation append).

    E. **Vitest run command (smoke per LOCAL-Gate Pitfall):** ensure tests can be executed via `CI=true npx vitest run tests/e2e/scripts/e2e-inject-defect.test.js`. The `CI=true` is required because Phase 56's safeAppendLedger guard treats non-CI invocations as fatal — but this script does NOT call appendLedgerEntry so it's actually safe; still, follow project convention.

    Do NOT include fenced code blocks in the action prose itself — author the test file using the RESEARCH §Recommended test coverage structure (lines 690-710) and the e2e-quarantine-append.test.js pattern as the bytewise template.

    File size target: 200-280 lines (similar to e2e-quarantine-append.test.js's 203 lines for 8 tests).
  </action>
  <verify>
    <automated>CI=true npx vitest run tests/e2e/scripts/e2e-inject-defect.test.js 2&gt;&amp;1 | tail -20 &amp;&amp; CI=true npx vitest run tests/e2e/scripts/e2e-inject-defect.test.js --reporter=verbose 2&gt;&amp;1 | grep -E "✓|PASS" | wc -l | { read N; [ "$N" -ge 8 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/scripts/e2e-inject-defect.test.js` exists
    - 8 test cases pass (I1-I8) covering MUTATOR-01, 02, 03, 05
    - Tests use the mock-gh bash binary pattern (no real `gh` calls)
    - Tests run hermetically in a tmp git repo (no host working-tree contamination)
    - `CI=true npx vitest run tests/e2e/scripts/e2e-inject-defect.test.js` exits 0
    - No FORBIDDEN_PATHS file modified by test execution: `git status --porcelain | grep -E "(tests/test-cases\\.js|tests/golden/baseline\\.json|tests/e2e/test-cases-quarantine\\.js|\\.github/workflows/v40-|tests/e2e/\\.llm-spend-ledger\\.json|\\.github/CODEOWNERS|tests/e2e/\\.rerun-ring-buffer\\.json|tests/e2e/\\.flake-suppression\\.json)"` returns empty
  </acceptance_criteria>
  <done>Vitest test file passes locally with 8 green tests covering all four MUTATOR-01/02/03/05 contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Patch scripts/quarantine-append.mjs:218-223 with source-tag suppression conditional (MUTATOR-04) + extend tests/e2e/scripts/e2e-quarantine-append.test.js with G9 describe block</name>
  <files>scripts/quarantine-append.mjs, tests/e2e/scripts/e2e-quarantine-append.test.js</files>
  <read_first>
    - scripts/quarantine-append.mjs:1-50 (imports, constants — confirm READY_FOR_PROMOTION_LABEL + STABLE_RUNS_THRESHOLD context)
    - scripts/quarantine-append.mjs:138-148 (formatEntry — confirms source_triage_finding_id is in canonical key order)
    - scripts/quarantine-append.mjs:218-223 (exact edit point — the smallest-possible diff per RESEARCH §quarantine-append.mjs Suppression Edit Point lines 553-577)
    - scripts/quarantine-append.mjs:295-367 (the main triage-loop that constructs source_triage_finding_id from triageReport.run_id — confirms `run_id: 'fixture-mutator-uat-47b'` propagates as the entry's source_triage_finding_id prefix)
    - tests/e2e/scripts/e2e-quarantine-append.test.js:177-203 (existing G8 describe block — the closest sibling pattern for G9)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §quarantine-append.mjs Suppression Edit Point (lines 539-603)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §Code Examples 4 (lines 1311-1325)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §Pitfall 8 / MUTATOR-04 (referenced throughout)
  </read_first>
  <action>
    **Part A — scripts/quarantine-append.mjs patch (smallest-diff per RESEARCH §quarantine-append.mjs Suppression Edit Point and per CONTEXT D-08 wording on co-design):**

    At lines 218-223, modify the existing conditional from:
      `if (finalEntry.stable_runs >= STABLE_RUNS_THRESHOLD && ghClient && triageIssueNumber != null) {`
    to add an `isFixtureMutator` boolean computed immediately above and `&& !isFixtureMutator` appended to the condition. The boolean: `const isFixtureMutator = typeof finalEntry.source_triage_finding_id === 'string' && finalEntry.source_triage_finding_id.startsWith('fixture-mutator-uat-47b');`

    Per RESEARCH §555-572 the resulting block becomes:
      - Comment block introducing MUTATOR-04 / Pitfall 8 with a one-line reference to `tests/e2e/scripts/inject-defect.mjs` co-design.
      - `const isFixtureMutator = ...` line.
      - Modified `if (...)` with `&& !isFixtureMutator` clause.
      - Body unchanged (`ghClient.addLabel(...)`; `addedLabel = true;`).

    DO NOT modify `formatEntry` (line 138-148). DO NOT add a new `source` field to the schema. The discriminator is `source_triage_finding_id.startsWith('fixture-mutator-uat-47b')` because the existing triage-loop at lines 295-367 propagates `triageReport.run_id` into `source_triage_finding_id` as the prefix (per RESEARCH §Co-design contract line 576). This satisfies MUTATOR-04 with zero-schema-change per CONTEXT preference.

    Preserve the rest of the function byte-unchanged.

    **Part B — extend tests/e2e/scripts/e2e-quarantine-append.test.js with G9 describe block (MUTATOR-04 co-designed test, SAME commit per REQUIREMENTS.md MUTATOR-04 verbatim):**

    Append (after G8 line 203 close) a new describe block:

      `describe('quarantine-append CLI — MUTATOR-04 source-tag suppression (G9)', () => { ... })`

    With one test (G9-a, plus optional G9-b negative-control):

      **G9-a**: Seed a fresh corpus override with an entry whose `source_triage_finding_id: 'fixture-mutator-uat-47b-iter-1'` and `stable_runs: 2`. Run spawnAppend once; assert `r.status === 0`. Assert transcript does NOT contain `--add-label.*quarantine:ready-for-promotion` even though the run pushed stable_runs to 3 (above threshold).

      **G9-b** (recommended negative control): Seed a fresh corpus override with an entry whose `source_triage_finding_id: 'real-pipeline-2026-06-05-iter-1'` (non-mutator prefix) and `stable_runs: 2`. Run spawnAppend once; assert transcript DOES contain `--add-label quarantine:ready-for-promotion` (proves the suppression is targeted, not broad).

    The corpus-override seeding pattern: mirror G8's `fs.writeFileSync(corpusOverridePath, '// AUTO-MANAGED\nexport const TEST_CASES_QUARANTINE = [{ ... }];\n')`. The seed entry shape must match `formatEntry` canonical key order (id, patentFile, selectedText, category, stable_runs, source_triage_finding_id, added_iso). Use the same id as the triage-report fixture's first finding so the upsert path hits "found existing" (increments stable_runs) instead of "insert new" (would set stable_runs=1).

    Use the existing G8 test as the template — same beforeEach, same spawnAppend helper, same transcript-inspection pattern.

    DO NOT modify any earlier test in the file. Only APPEND the new describe block(s) at the end.

    **Atomicity:** Part A + Part B + the new files from Tasks 1-2 ship in ONE commit. Per MUTATOR-04: "inject-defect.mjs and quarantine-append.mjs source-tag strings co-designed in the SAME commit." The commit author runs the full Vitest suite locally before committing.
  </action>
  <verify>
    <automated>grep -n 'fixture-mutator-uat-47b' scripts/quarantine-append.mjs | wc -l | { read N; [ "$N" -ge 1 ]; } &amp;&amp; grep -n 'isFixtureMutator' scripts/quarantine-append.mjs | wc -l | { read N; [ "$N" -ge 1 ]; } &amp;&amp; grep -nE '&amp;&amp; !isFixtureMutator' scripts/quarantine-append.mjs | wc -l | { read N; [ "$N" -ge 1 ]; } &amp;&amp; grep -n 'MUTATOR-04 source-tag suppression' tests/e2e/scripts/e2e-quarantine-append.test.js &amp;&amp; CI=true npx vitest run tests/e2e/scripts/e2e-quarantine-append.test.js 2&gt;&amp;1 | tail -10 &amp;&amp; CI=true npx vitest run tests/e2e/scripts/e2e-quarantine-append.test.js --reporter=verbose 2&gt;&amp;1 | grep -c "G9" | { read N; [ "$N" -ge 1 ]; } &amp;&amp; git diff scripts/quarantine-append.mjs | grep -E "^-.*addLabel" | wc -l | { read N; [ "$N" -eq 0 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/quarantine-append.mjs` contains `fixture-mutator-uat-47b` literal (matches Task 1's SOURCE_TAG)
    - `scripts/quarantine-append.mjs` contains `isFixtureMutator` const
    - The label-add conditional contains `&& !isFixtureMutator`
    - `ghClient.addLabel(...)` line is byte-unchanged (git diff shows zero `-` lines on `addLabel`)
    - `formatEntry` function (lines 138-148) is byte-unchanged: `git diff scripts/quarantine-append.mjs | grep -E "^\-.*formatEntry"` returns empty
    - `tests/e2e/scripts/e2e-quarantine-append.test.js` contains G9 describe block with `MUTATOR-04 source-tag suppression` literal in the describe name
    - `CI=true npx vitest run tests/e2e/scripts/e2e-quarantine-append.test.js` exits 0 (all G1-G8 still pass + G9 passes)
    - All Phase 59 Vitest contract tests pass together: `CI=true npx vitest run tests/e2e/scripts/e2e-inject-defect.test.js tests/e2e/scripts/e2e-quarantine-append.test.js` exits 0
    - No FORBIDDEN_PATHS file modified: same staged-only check as Tasks 1+2
  </acceptance_criteria>
  <done>quarantine-append.mjs source-tag suppression patch applied with smallest-possible diff; G9 test extends existing file in same commit as inject-defect.mjs per MUTATOR-04 co-design contract; full Vitest suite for inject-defect + quarantine-append green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator local shell → github API | Mutator shells out to `gh issue create`/`gh issue list`; argv constructed from validated `--seed` and `--error-class` inputs |
| inject-defect.mjs ↔ scripts/quarantine-append.mjs | Co-designed source-tag literal `fixture-mutator-uat-47b`; mismatch in either file breaks the suppression invariant |
| working tree → FORBIDDEN_PATHS bank | Mutator MUST NOT touch any path in scripts/check-diff-guard.mjs:49-58; verified at runtime by `git status --porcelain` and at test time by staged-diff filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Tampering | Fingerprint formula divergence vs scripts/e2e-report-issue.mjs:78-81 | mitigate | Mutator imports `fingerprint` by name from `scripts/e2e-report-issue.mjs`; grep-verified in Task 1 verify gate; re-implementation is forbidden by Code Examples 1 in RESEARCH |
| T-59-02 | DoS | Fingerprint collision with existing open issue | mitigate | Task 1 implements `collisionCheckOrAbort` exiting 2 (hard abort, not warning) per Pitfall 6 LOAD-BEARING; Vitest I3 pins behavior |
| T-59-03 | Tampering | FORBIDDEN_PATHS mutation by mutator (Pitfall 5 LOAD-BEARING) | mitigate | Task 1 emits ONLY `.planning/phases/59-*/56-MUTATOR-CLEANUP.md`; `git status --porcelain` runtime gate + staged-diff filter at test time + Vitest I7 pins behavior |
| T-59-04 | Tampering | Auto-promotion of synthetic to golden corpus (Pitfall 8 LOAD-BEARING) | mitigate | Task 3 adds `&& !isFixtureMutator` to scripts/quarantine-append.mjs:218-223 with source-tag string co-designed in SAME commit as inject-defect.mjs; Vitest G9-a + G9-b negative-control pin the behavior |
| T-59-05 | DoS | Orphaned synthetic state (issue, branch, quarantine entry) | mitigate | Task 1 emits append-only `56-MUTATOR-CLEANUP.md` manifest; plan 59-02 SWEEP-06 reads this manifest to drive cleanup automation |
| T-59-SC | Tampering | Supply-chain (npm/pip/cargo installs) | n/a | Phase 59 installs zero new packages per RESEARCH §Package Legitimacy Audit; no slopcheck needed (the audit table is empty by design — REQUIREMENTS.md "Out of Scope" forbids new deps) |
</threat_model>

<verification>
- Task 1 verify: file exists, fingerprint import grep, SOURCE_TAG grep, gh argv greps, computeFingerprint smoke (12-hex output)
- Task 2 verify: `CI=true npx vitest run tests/e2e/scripts/e2e-inject-defect.test.js` exits 0 with 8 tests
- Task 3 verify: smallest-diff suppression patch applied; `CI=true npx vitest run tests/e2e/scripts/e2e-quarantine-append.test.js` exits 0; G9 visible in verbose output; formatEntry byte-unchanged
- Phase gate: `CI=true npx vitest run tests/e2e/scripts/e2e-inject-defect.test.js tests/e2e/scripts/e2e-quarantine-append.test.js` exits 0; `git status --porcelain` after commit matches only `.planning/phases/59-*/`, `tests/e2e/scripts/inject-defect.mjs`, `tests/e2e/scripts/e2e-inject-defect.test.js`, `scripts/quarantine-append.mjs`, `tests/e2e/scripts/e2e-quarantine-append.test.js` — zero FORBIDDEN_PATHS hits
</verification>

<success_criteria>
- `tests/e2e/scripts/inject-defect.mjs` produces a 12-hex fingerprint deterministically and creates a triage-labeled GitHub issue via `gh` shell-out (proven by mock-gh tests)
- Fingerprint collision causes hard abort with exit 2 (not a warning) — MUTATOR-02 / Pitfall 6 closed
- `git status --porcelain` after mutator execution shows no FORBIDDEN_PATHS hits — MUTATOR-03 / Pitfall 5 closed
- `scripts/quarantine-append.mjs:218-223` suppresses `quarantine:ready-for-promotion` label for entries whose `source_triage_finding_id` starts with `fixture-mutator-uat-47b` — MUTATOR-04 / Pitfall 8 closed
- `56-MUTATOR-CLEANUP.md` exists in the phase dir with `gh issue close`, `gh pr close --delete-branch`, and quarantine-revert commands — MUTATOR-05 closed
- `CI=true npx vitest run tests/e2e/scripts/e2e-inject-defect.test.js tests/e2e/scripts/e2e-quarantine-append.test.js` exits 0
- All five files ship in one atomic commit (`feat(59-01): fixture-mutator + source-tag suppression`)
- No new npm dependencies installed (zero-new-deps streak preserved)
</success_criteria>

<output>
Create `.planning/phases/59-fixture-mutator-4-uat-re-sweep/59-01-SUMMARY.md` when done, capturing:
- Commit sha
- inject-defect.mjs line count + main exports
- Diff summary for scripts/quarantine-append.mjs (line range; lines added; lines removed; formatEntry byte-unchanged verified)
- Vitest pass count (must include I1-I8 and G9-a + optional G9-b)
- Confirmation that no FORBIDDEN_PATHS file appears in the staged diff
- Pointer to plan 59-02 (which depends on Work stream A being shipped + Phases 56+57+58 being on origin/main)
</output>
