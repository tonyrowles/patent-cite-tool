---
phase: 59-fixture-mutator-4-uat-re-sweep
plan: 02
type: execute
wave: 3
depends_on: ["59-00", "59-01", "59-03"]
files_modified:
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/56-UAT-EVIDENCE.md
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/INDEX.md
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-e-pr-checks.json
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-e-pr-labels.json
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-e-pr-comments.json
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-d-run-metadata.json
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-d-pre-branches.txt
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-d-post-branches.txt
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-d-snapshot-commit.txt
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-pre-run-ledger.json
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-post-run-ledger.json
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-pr-state.json
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-verifier-gate-run.json
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-promote-run.json
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-outcome-entry.json
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-spend-delta.txt
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-phase-tag-set.txt
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-injected-issue.json
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-auto-fix-run.json
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-pr-state.json
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-verifier-gate-run.json
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-outcome-entry.json
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-promotion-suppression.txt
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-deps-update-run.json
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-phase-tag-set.txt
  - .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-quarantine-on-main.txt
  - tests/e2e/scripts/uat-cleanup.mjs
autonomous: false
requirements: [SWEEP-01, SWEEP-02, SWEEP-03, SWEEP-04, SWEEP-05, SWEEP-06]
tags: [phase-59, uat, sweep, work-stream-b, operator-runbook, live-production]

user_setup:
  - service: github
    why: "Live UAT operations against origin/main require gh CLI authenticated as a maintainer with merge permission. PR #18 must be merged AND a repo setting must be enabled before any sweep runs."
    env_vars:
      - name: GITHUB_TOKEN
        source: "gh auth login (interactive); CI uses GITHUB_TOKEN automatically"
    dashboard_config:
      - task: "Enable 'Allow GitHub Actions to create and approve pull requests' (resolves PR #18 dep-scan environmental failure per RESEARCH §Pitfall 4 Option α)"
        location: "https://github.com/tonyrowles/patent-cite-tool/settings/actions → General → Workflow permissions"
      - task: "Merge PR #18 carrying Phases 56+57+58 to origin/main"
        location: "https://github.com/tonyrowles/patent-cite-tool/pull/18 — break-glass §7 procedure if CODEOWNERS approval friction blocks merge (docs/v40-repo-config.md §3, §7)"
  - service: anthropic
    why: "SWEEP-03 (UAT-47-a) triggers a live Anthropic SDK call from v40-auto-fix.yml; expected cost is $0.50-2 per run"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "GitHub repo secret — already present per Phase 51 D-03 (verified Phase 51 closure note in STATE.md); operator does NOT set this locally; the workflow uses the secret"

must_haves:
  truths:
    - "All 4 UATs (UAT-47-e, UAT-47-d, UAT-47-a, UAT-47-b) PASS against origin/main"
    - "UAT-47-e PASS: diff-guard rejects a crafted PR touching tests/golden/baseline.json; PR closed + branch deleted"
    - "UAT-47-d PASS: v40-cost-ledger-snapshot.yml workflow_dispatch produces a new ledger-snapshots/daily-YYYY-MM-DD branch on origin (not a main commit)"
    - "UAT-47-a PASS: full end-to-end auto-fix loop produces merged PR + verifier-gate evidence + outcome ledger entry on origin/main carrying source: 'auto-fix-promoted' + errorClass + outcome: 'pass' — PRIMARY DoD"
    - "UAT-47-b PASS: fixture-mutator drives synthetic through full loop with a DIFFERENT seed than SWEEP-03; MUTATOR-04 suppression keeps the synthetic out of auto-promotion; deps-update-gate trigger smoke confirms v40-deps-update.yml pull_request trigger fires"
    - "56-UAT-EVIDENCE.md consolidates PASS/FAIL per UAT with JSON snapshots captured by gh api / gh run view"
    - "All UAT outcome ledger entries on origin/main carry `phase: '56-uat'` LIVE (filterable via jq directly against the committed ledger) per REQUIREMENTS.md SWEEP-05 literal wording; threaded via Plan 59-03's auto-fix-promote.mjs --phase argv + workflow PHASE_TAG plumbing (Decision C)"
    - "Post-UAT cleanup: test branches deleted; test PRs closed; synthetic GitHub issues closed with --reason 'not planned'; synthetic quarantine entries reverted via a follow-up cleanup PR (cannot direct-push due to FORBIDDEN_PATHS regex 3)"
    - "SWEEP-03 budget cap: spend delta < $2 between pre-run and post-run ledger snapshots; if exceeded, FAIL the UAT and document (no retry per Phase 51 D-03)"
  artifacts:
    - path: ".planning/phases/59-fixture-mutator-4-uat-re-sweep/56-UAT-EVIDENCE.md"
      provides: "SWEEP-05 — consolidated UAT evidence per REQUIREMENTS.md SWEEP-05 wording"
      contains: "Outcome Matrix"
      min_lines: 80
    - path: ".planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/INDEX.md"
      provides: "Per-UAT JSON snapshot index linking the 22 evidence files to UATs"
    - path: "tests/e2e/scripts/uat-cleanup.mjs"
      provides: "SWEEP-06 — operator-invoked cleanup automation"
      contains: "gh pr close"
  key_links:
    - from: ".planning/phases/59-fixture-mutator-4-uat-re-sweep/56-UAT-EVIDENCE.md"
      to: ".planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-*.json"
      via: "markdown links + evidence column in Outcome Matrix"
      pattern: "evidence/uat-47-"
    - from: ".planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-outcome-entry.json"
      to: "tests/e2e/.llm-spend-ledger.json on origin/main"
      via: "jq filter on source=='auto-fix-promoted' and fingerprint match"
      pattern: "source.*auto-fix-promoted"
    - from: ".planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-promotion-suppression.txt"
      to: "scripts/quarantine-append.mjs:218-223"
      via: "3-run transcript showing NO --add-label quarantine:ready-for-promotion"
      pattern: "fixture-mutator-uat-47b"
---

<objective>
Execute the 4-UAT re-sweep against origin/main in the D-13 cost-discipline
order: SWEEP-01 ($0 smoke) → SWEEP-02 ($0) → operator-approval gate → SWEEP-03
($0.50-2 primary DoD) → SWEEP-04 (mutator-driven), then SWEEP-05 evidence
consolidation and SWEEP-06 cleanup.

Purpose: This is the live operational validation that converts the v4.0/v4.1
auto-fix infrastructure from "wired" to "proven on origin/main with at least
one production fix shipped through the loop end-to-end." Primary DoD evidence
is the SWEEP-03 outcome ledger entry on origin/main.

Output: `56-UAT-EVIDENCE.md` + 22 JSON/text evidence files in
`evidence/` + `tests/e2e/scripts/uat-cleanup.mjs` operator script + 1
follow-up cleanup PR opened against origin/main.

**Task density rationale (checker scope_sanity acknowledgement):** Plan 59-02 contains 7 tasks, above the standard 2-3 task soft cap. This is intentional and accepted given the OPERATOR-RUNBOOK character of the plan: each task is a self-contained UAT runbook segment (pre-flight, SWEEP-01, SWEEP-02, paid-spend gate, SWEEP-03, SWEEP-04, SWEEP-05+SWEEP-06 consolidation). The tasks share a single execution thread (the operator) and a single failure mode (HALT-on-fail before paid spend). Splitting into separate plans would fragment the cost-discipline gate chain (D-13 sequencing: cheap smoke → paid → mutator-driven) and require operator-side coordination between plans, increasing operational risk. Density is the safer shape for live-production validation runbooks. Each task individually fits within ~10-20% context budget; the plan's autonomous: false setting means context cost is bounded by per-task operator interaction (no continuous executor context accumulation across the plan).

**Autonomous: false.** Every task here is an operator-runbook task; the
Claude executor reads the runbook, surfaces the commands and expected
evidence, and pauses at every `checkpoint:human-verify` gate for explicit
operator approval — especially before SWEEP-03 paid spend. All `gh` commands
run against the live origin repo with the operator's credentials.

**Hard pre-requisite:** Plan 59-01 (Work stream A) MUST be merged AND
Phases 56+57+58 commits MUST be on origin/main (currently in PR #18) before
ANY task in this plan executes. The plan's first task (Pre-flight) hard-gates
on these conditions and the dep-scan repo setting per RESEARCH §Pitfall 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-fixture-mutator-4-uat-re-sweep/59-CONTEXT.md
@.planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md
@.planning/phases/59-fixture-mutator-4-uat-re-sweep/59-VALIDATION.md
@.planning/phases/59-fixture-mutator-4-uat-re-sweep/59-00-BASELINE.md
@.planning/phases/59-fixture-mutator-4-uat-re-sweep/59-01-SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/milestones/v4.1-phases/51-live-readiness-uats/51-UAT-EVIDENCE.md
@.planning/milestones/v4.0-phases/47-v4-0-cleanup/47-UAT-DEFERRED.md
@docs/v40-repo-config.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Pre-flight gate — confirm PR #18 merged, repo setting enabled, working tree clean (RESEARCH §Pitfall 4 + §Pitfall 5)</name>
  <files>.planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/INDEX.md, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/pre-flight-state.json</files>
  <read_first>
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §Pre-Flight State (lines 1221-1242)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §Pitfall 4 (lines 1177-1188)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §Pitfall 5 (lines 1191-1198)
    - docs/v40-repo-config.md §3 single-maintainer / §7 break-glass
  </read_first>
  <what-built>
    Plan 59-01 has shipped (Work stream A — fixture mutator + suppression conditional). This task is the live-state gate before any UAT runs against origin/main.
  </what-built>
  <how-to-verify>
    Operator runs these commands manually (Claude executor surfaces, does NOT auto-run unattended):

    1. **Confirm PR #18 merged** (or whatever PR carries Phases 56+57+58 to origin/main):
       ```
       gh pr view 18 --repo tonyrowles/patent-cite-tool --json state,mergedAt,mergeCommit
       ```
       Expected: `state: MERGED`, non-null `mergedAt`, non-null `mergeCommit.oid`.
       Capture to `evidence/pre-flight-pr-18.json`.

    2. **Confirm repo setting enabled** (RESEARCH §Pitfall 4 Option α):
       Visit https://github.com/tonyrowles/patent-cite-tool/settings/actions → General → Workflow permissions. The radio button "Allow GitHub Actions to create and approve pull requests" MUST be CHECKED. Screenshot or note observation in `evidence/pre-flight-state.json` (field: `actions_can_create_prs: true`).

       If still disabled: operator enables it, then proceeds. If operator chooses Option β (remove dep-scan from required checks), document in evidence file as `pitfall_4_resolution: option-beta`.

    3. **Confirm working tree clean on local main**:
       ```
       git fetch origin main
       git status --porcelain
       git rev-parse main
       git rev-parse origin/main
       ```
       Expected: status empty; local main == origin/main (after PR merge + git fetch).

    4. **Confirm Work stream A is on origin/main**:
       ```
       git log origin/main --oneline -10 | grep -E "feat\\(59-01\\)|fixture-mutator"
       ```
       Expected: at least one commit matching. If empty, plan 59-01 has not been merged yet; STOP.

    5. **Confirm Phase 51.1 trigger fix is on origin/main** (RESEARCH §Pre-Flight State lines 1227-1229):
       ```
       gh api repos/tonyrowles/patent-cite-tool/contents/.github/workflows/v40-verifier-gate.yml --jq '.content' | base64 -d | grep -c 'Scope decision'
       ```
       Expected: ≥4 (one scope-decision step per gated job).

    6. **Confirm Phase 57 branch-redirect on origin/main**:
       ```
       gh api repos/tonyrowles/patent-cite-tool/contents/.github/workflows/v40-cost-ledger-snapshot.yml --jq '.content' | base64 -d | grep -E 'git push origin HEAD:ledger-snapshots'
       ```
       Expected: one matching line.

    7. **Create evidence/ subdirectory and INDEX.md**:
       ```
       mkdir -p .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/
       ```
       Then write `evidence/INDEX.md` listing the 22 planned evidence files (per the files_modified frontmatter list) with status "PENDING" each. As each UAT completes, status flips to PASS / FAIL with the filename column hyperlinked to the actual JSON file.

    8. **Capture pre-flight summary** to `evidence/pre-flight-state.json`:
       ```json
       {
         "pre_flight_run_at": "<UTC ISO>",
         "pr_18_state": "MERGED",
         "pr_18_merge_commit": "<sha>",
         "actions_can_create_prs": true,
         "pitfall_4_resolution": "option-alpha",
         "local_main_sha": "<sha>",
         "origin_main_sha": "<sha>",
         "phase_51_1_scope_decision_count": 4,
         "phase_57_branch_redirect_present": true,
         "work_stream_a_on_origin": true
       }
       ```

    If ANY of 1-6 fails: STOP and surface the failure to the operator. Do NOT proceed to SWEEP-01. The dep-scan repo setting is the most common blocker per RESEARCH §Pitfall 4.

  </how-to-verify>
  <acceptance_criteria>
    - `evidence/pre-flight-state.json` exists with all six checks recorded
    - `evidence/INDEX.md` exists listing all 22 planned evidence files
    - PR #18 (or successor) is MERGED on origin/main
    - Repo setting for actions creating PRs is enabled OR explicit option-beta resolution documented
    - Phase 51.1 trigger fix verified live on origin/main
    - Phase 57 branch-redirect verified live on origin/main
    - Plan 59-01 commit is on origin/main
    - Operator has typed explicit approval to proceed to SWEEP-01
  </acceptance_criteria>
  <resume-signal>Type "pre-flight approved — proceed to SWEEP-01" once all 6 checks have passed and evidence/pre-flight-state.json is committed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: SWEEP-01 — UAT-47-e diff-guard rejection runbook (~3 min, $0)</name>
  <files>.planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-e-pr-checks.json, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-e-pr-labels.json, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-e-pr-comments.json</files>
  <read_first>
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §UAT-47-e Runbook (SWEEP-01) (lines 716-783)
    - .planning/milestones/v4.0-phases/47-v4-0-cleanup/47-UAT-DEFERRED.md §UAT-47-e
    - .planning/milestones/v4.1-phases/51-live-readiness-uats/51-UAT-EVIDENCE.md (pattern reference)
    - scripts/check-diff-guard.mjs:49-58 (FORBIDDEN_PATHS — confirms `tests/golden/baseline.json` is regex 2)
  </read_first>
  <action>
    Execute the UAT-47-e runbook from RESEARCH §UAT-47-e Runbook (lines 742-765) verbatim. Per RESEARCH the sequence is:

    1. Set `TS=$(date -u +%Y%m%dT%H%M%SZ)` and `BRANCH="auto-fix/test-uat47e-${TS}"`.
    2. `git checkout -b "$BRANCH" main` then append a one-line comment to `tests/golden/baseline.json` (a deliberate FORBIDDEN_PATHS regex-2 violation).
    3. `git add` + `git commit` + `git push -u origin "$BRANCH"`.
    4. `gh pr create --draft --title "UAT-47-e crafted bypass (${TS})" --body "<!-- affected_cases: any -->\n\nUAT-47-e: crafted bypass test. Expect diff-guard to FAIL." --base main --head "$BRANCH" --json url --jq .url` — capture PR number from URL via `grep -oE '[0-9]+$'`.
    5. Poll `gh pr checks $PR --json bucket,name` every 30s for up to 5 minutes; wait for diff-guard bucket to become `fail`.

    **Capture 3 evidence files (per RESEARCH §Expected PASS evidence lines 767-771):**

    - `evidence/uat-47-e-pr-checks.json` ← `gh pr checks "$PR" --json bucket,name,state`
    - `evidence/uat-47-e-pr-labels.json` ← `gh pr view "$PR" --json labels`
    - `evidence/uat-47-e-pr-comments.json` ← `gh pr view "$PR" --json comments`

    **PASS heuristics (all 3 must hold per RESEARCH):**
    a. `evidence/uat-47-e-pr-checks.json` filtered with `jq '.[] | select(.name=="diff-guard") | .bucket'` returns `"fail"`.
    b. `evidence/uat-47-e-pr-labels.json` filtered with `jq '[.labels[].name]'` contains `"human-review-required"`.
    c. `evidence/uat-47-e-pr-comments.json`'s last comment body mentions `tests/golden/baseline.json`.

    **Cleanup (per RESEARCH lines 773-779):**
    ```
    gh pr close "$PR" --delete-branch
    git checkout main
    ```
    Working tree is untouched on `main` because the bad commit was only ever on the test branch. Confirm via `git status --porcelain` returns empty.

    **Update INDEX.md and 56-UAT-EVIDENCE.md (defer the full 56-UAT-EVIDENCE.md write to Task 6 — SWEEP-05; here just append PASS/FAIL status to evidence/INDEX.md row for SWEEP-01).**

    **HALT-on-fail behavior (RESEARCH §HALT-on-fail behavior lines 781-783):**
    If any of the 3 heuristics FAIL, STOP and surface to the operator. Do NOT proceed to SWEEP-02 — the trigger-correctness fix may not have landed, and SWEEP-03 would burn API budget on broken infra. Diagnostic: same workflow-trigger / scope-decision audit as Phase 51 deviation #2.
  </action>
  <verify>
    <automated>test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-e-pr-checks.json &amp;&amp; test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-e-pr-labels.json &amp;&amp; test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-e-pr-comments.json &amp;&amp; jq -e '.[] | select(.name=="diff-guard") | .bucket == "fail"' .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-e-pr-checks.json &amp;&amp; jq -e '[.labels[].name] | index("human-review-required")' .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-e-pr-labels.json &amp;&amp; jq -e '.comments[-1].body | contains("tests/golden/baseline.json")' .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-e-pr-comments.json &amp;&amp; git status --porcelain | grep -v '^?? \.planning/phases/59-' | grep -v '^M  \.planning/phases/59-' &gt; /dev/null &amp;&amp; [ -z "$(git status --porcelain | grep -v '^?? \.planning/phases/59-' | grep -v '^M  \.planning/phases/59-')" ]</automated>
  </verify>
  <acceptance_criteria>
    - All 3 evidence files committed under `evidence/uat-47-e-*.json`
    - diff-guard bucket == "fail" in pr-checks.json
    - human-review-required label present in pr-labels.json
    - PR comments mention `tests/golden/baseline.json`
    - PR closed with `--delete-branch`; branch `auto-fix/test-uat47e-*` not present on origin (`git ls-remote origin 'auto-fix/test-uat47e-*'` empty)
    - Working tree clean on local main (no FORBIDDEN_PATHS mutation)
    - evidence/INDEX.md updated with SWEEP-01 PASS row
  </acceptance_criteria>
  <done>SWEEP-01 evidence captured; diff-guard infrastructure proven to reject FORBIDDEN_PATHS violation; HALT-on-fail did not trigger.</done>
</task>

<task type="auto">
  <name>Task 3: SWEEP-02 — UAT-47-d ledger-snapshot branch-redirect runbook (~5 min, $0)</name>
  <files>.planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-d-pre-branches.txt, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-d-post-branches.txt, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-d-run-metadata.json, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-d-snapshot-commit.txt</files>
  <read_first>
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §UAT-47-d Runbook (SWEEP-02) (lines 787-854)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md Code Examples 5 (lines 1327-1340)
    - .github/workflows/v40-cost-ledger-snapshot.yml (live form on origin/main — confirm push refspec lands at `ledger-snapshots/daily-${SNAPSHOT_DATE}`)
  </read_first>
  <action>
    Execute UAT-47-d runbook from RESEARCH §UAT-47-d Runbook lines 787-846. Workflow_dispatch is preferred over real cron per Pitfall 11.

    **Pre-flight (RESEARCH lines 794-804):**

    1. Capture pre-state branches:
       ```
       git ls-remote --heads origin 'ledger-snapshots/*' > .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-d-pre-branches.txt
       ```

    2. Confirm Phase 57 branch-redirect is on origin/main (already done in Task 1 — re-confirm here):
       ```
       gh api repos/tonyrowles/patent-cite-tool/contents/.github/workflows/v40-cost-ledger-snapshot.yml --jq '.content' | base64 -d | grep -E 'git push origin HEAD:ledger-snapshots'
       ```

    **Execution (RESEARCH lines 809-824):**

    3. Trigger the workflow:
       ```
       gh workflow run v40-cost-ledger-snapshot.yml --ref main
       sleep 5
       RUN_ID=$(gh run list --workflow=v40-cost-ledger-snapshot.yml --limit 1 --json databaseId,event --jq '.[] | select(.event=="workflow_dispatch") | .databaseId')
       gh run watch "$RUN_ID" --interval 10 --exit-status
       ```
       Note Pitfall 11 note (RESEARCH lines 848-850): the snapshot push is synchronous within the workflow; `gh run watch --exit-status` is sufficient for timing.

    4. Capture run metadata:
       ```
       gh run view "$RUN_ID" --json status,conclusion,createdAt,event,headBranch > evidence/uat-47-d-run-metadata.json
       ```

    **Capture PASS evidence (RESEARCH §Expected PASS evidence lines 826-846):**

    5. Post-state branches and diff:
       ```
       git ls-remote --heads origin 'ledger-snapshots/*' > evidence/uat-47-d-post-branches.txt
       diff evidence/uat-47-d-pre-branches.txt evidence/uat-47-d-post-branches.txt
       ```
       Expected: one new line `> refs/heads/ledger-snapshots/daily-YYYY-MM-DD` (today's UTC date).

    6. No new main commit by this workflow:
       ```
       git fetch origin main
       git log --oneline origin/main -3 | grep -i 'ledger snapshot' && echo FAIL || echo PASS
       ```
       Expected: PASS.

    7. Capture snapshot commit message:
       ```
       SNAPSHOT_DATE=$(date -u +%Y-%m-%d)
       git log "origin/ledger-snapshots/daily-${SNAPSHOT_DATE}" -1 --format='%s' > evidence/uat-47-d-snapshot-commit.txt
       ```
       Expected pattern: `[skip ci] ledger snapshot YYYY-MM-DD: N invocations, $X.XX spent`.

    **Cleanup (RESEARCH §Cleanup line 852-854):**
    No cleanup required. The `ledger-snapshots/daily-YYYY-MM-DD` branch is a legitimate audit artifact. Periodic cleanup of accumulated snapshot branches is operator-owned and OUT OF v4.2 scope per CONTEXT.md Deferred Ideas.

    **HALT-on-fail behavior:** If run conclusion is not SUCCESS, or if a new commit appears on `origin/main` from this workflow, STOP and surface. Do NOT proceed to the SWEEP-03 paid checkpoint.

    Update `evidence/INDEX.md` with SWEEP-02 PASS row.
  </action>
  <verify>
    <automated>test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-d-pre-branches.txt &amp;&amp; test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-d-post-branches.txt &amp;&amp; test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-d-run-metadata.json &amp;&amp; test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-d-snapshot-commit.txt &amp;&amp; jq -e '.conclusion == "success"' .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-d-run-metadata.json &amp;&amp; diff .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-d-pre-branches.txt .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-d-post-branches.txt | grep -E '^&gt; refs/heads/ledger-snapshots/daily-' &amp;&amp; grep -E 'ledger snapshot.*invocations' .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-d-snapshot-commit.txt</automated>
  </verify>
  <acceptance_criteria>
    - All 4 evidence files captured
    - Workflow run conclusion == "success"
    - Diff between pre- and post-branches shows ≥1 new `ledger-snapshots/daily-YYYY-MM-DD` ref
    - No new commit on `origin/main` from this workflow (`git log origin/main -3 | grep -i 'ledger snapshot'` empty)
    - Snapshot commit message matches `[skip ci] ledger snapshot YYYY-MM-DD: N invocations, $X.XX spent` pattern
    - evidence/INDEX.md updated with SWEEP-02 PASS row
  </acceptance_criteria>
  <done>SWEEP-02 evidence captured; Phase 57 branch-redirect proven live; no main-branch mutation by snapshot workflow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: PAID-SPEND APPROVAL GATE — surface expected $0.50-2 cost before SWEEP-03 (CTRL-01 / RESEARCH §Pitfall 6)</name>
  <files>.planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/sweep-03-cost-acknowledgement.json</files>
  <read_first>
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-CONTEXT.md §Cost discipline (CTRL-01)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §Pitfall 6 SWEEP-03 paid spend (lines 1201-1208)
    - .planning/milestones/v4.1-phases/51-live-readiness-uats/51-CONTEXT.md D-03 "no retry past cap"
  </read_first>
  <what-built>
    SWEEP-01 and SWEEP-02 evidence files are committed and both UATs PASSED. Infrastructure is proven; the next task (SWEEP-03) triggers a paid Anthropic API call estimated at $0.50-2.
  </what-built>
  <how-to-verify>
    Surface the following to the operator and require explicit textual approval:

    > **PAID SPEND AHEAD**
    >
    > About to trigger SWEEP-03 (UAT-47-a) which will fire `v40-auto-fix.yml`
    > and make ≥1 Anthropic SDK call. Expected cost: **$0.50 – $2.00 USD**.
    > Hard ceiling: $2.00 (Phase 51 D-03 "no retry past cap" pattern).
    >
    > Pre-flight evidence captured:
    >   - SWEEP-01: PASS (`evidence/uat-47-e-*.json`)
    >   - SWEEP-02: PASS (`evidence/uat-47-d-*.json`)
    >
    > **Approve to proceed? Type one of:**
    >   - `approve` — proceed to SWEEP-03 with the deterministic mutator trigger
    >     (recommended; uses `tests/e2e/scripts/inject-defect.mjs --seed mutator-seed-sweep-03 --error-class GOOGLE_DOM_DRIFT`)
    >   - `approve-label-cycle` — proceed to SWEEP-03 via label-cycle on issue #3 (Phase 51 D-04 fallback)
    >   - `defer` — STOP here; do not run SWEEP-03 today

    Capture the operator's choice to `evidence/sweep-03-cost-acknowledgement.json`:
    ```json
    {
      "decision_at": "<UTC ISO>",
      "decision": "approve" | "approve-label-cycle" | "defer",
      "expected_cost_usd_low": 0.50,
      "expected_cost_usd_high": 2.00,
      "hard_ceiling_usd": 2.00,
      "no_retry_past_cap": true,
      "trigger_path": "mutator-seed-sweep-03" | "label-cycle-issue-3" | null
    }
    ```

    Per project CLAUDE.md ("CRITICAL: Answer verification after every AskUserQuestion call"): if the user response is empty, generic, or does not contain an explicit choice, present the same three options as a numbered plain-text list and re-ask. Do NOT assume the recommended option.

    If `defer`: STOP, do NOT proceed to SWEEP-03 or SWEEP-04. Phase remains incomplete; resume via `/gsd:execute-plan 59 --plan 02 --resume` later.
  </how-to-verify>
  <acceptance_criteria>
    - `evidence/sweep-03-cost-acknowledgement.json` exists with explicit operator decision recorded
    - If decision is `approve` or `approve-label-cycle`: proceed to Task 5
    - If decision is `defer`: plan halts; phase summary records partial completion
  </acceptance_criteria>
  <resume-signal>Type one of `approve`, `approve-label-cycle`, or `defer` per the cost-discipline gate.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: SWEEP-03 — UAT-47-a full end-to-end auto-fix loop (~10 min, $0.50-2) — PRIMARY DoD</name>
  <files>.planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-pre-run-ledger.json, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-pr-state.json, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-verifier-gate-run.json, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-promote-run.json, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-post-run-ledger.json, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-outcome-entry.json, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-spend-delta.txt, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-phase-tag-set.txt</files>
  <read_first>
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §UAT-47-a Runbook (SWEEP-03) (lines 858-974)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §Assumption A5 + A6 (lines 1368-1372) — outcome-entry shape + phase-tag strategy
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-00-BASELINE.md §RESEARCH Q1 resolution — RESOLVED via Plan 59-03 (phase-tag is LIVE on the ledger entry)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-03-SUMMARY.md — Decision C plumbing details (workflow PHASE_TAG → script --phase → live entry shape)
    - .github/workflows/v40-auto-fix.yml (live form on origin/main — confirm `on.issues.types: [labeled]` trigger and `if: github.event.label.name == 'triage'`)
    - .planning/milestones/v4.1-phases/51-live-readiness-uats/51-CONTEXT.md D-03..D-06 (idempotency precondition + 15-min hard timeout patterns)
  </read_first>
  <action>
    Execute the UAT-47-a runbook from RESEARCH §UAT-47-a Runbook lines 858-974. Decision from Task 4 selects trigger path A or B.

    **Pre-flight (RESEARCH §Pre-flight lines 864-880):**

    1. Sanity-check evidence/uat-47-e-* and evidence/uat-47-d-* exist (proves SWEEP-01, SWEEP-02 PASS).

    2. Capture pre-run ledger snapshot from origin/main:
       ```
       git fetch origin main
       git show origin/main:tests/e2e/.llm-spend-ledger.json > evidence/uat-47-a-pre-run-ledger.json
       ```

    3. **If decision == "approve" (mutator path, recommended):**
       ```
       node tests/e2e/scripts/inject-defect.mjs --seed mutator-seed-sweep-03 --error-class GOOGLE_DOM_DRIFT
       ```
       Capture the issue number from stdout (format `[inject-defect] issue #N created with fingerprint XX`). `ISSUE=N` and `FP=XX`.

       **If decision == "approve-label-cycle":** Use Phase 51 D-04 fallback (RESEARCH lines 902-911):
       ```
       gh issue view 3 --repo tonyrowles/patent-cite-tool --json labels --jq '[.labels[].name]'
       # confirm "triage" is currently NOT present (idempotency precondition per Phase 51 D-04)
       gh issue edit 3 --repo tonyrowles/patent-cite-tool --remove-label triage  # no-op if not present
       sleep 10
       gh issue edit 3 --repo tonyrowles/patent-cite-tool --add-label triage
       ISSUE=3
       FP=139f821b3bb1
       ```

    **Watch the loop (RESEARCH lines 916-940):**

    4. Stage 1 — auto-fix workflow fires:
       ```
       sleep 10
       RUN_ID=$(gh run list --workflow=v40-auto-fix.yml --limit 5 --json databaseId,event,createdAt --jq "[.[] | select(.event==\"issues\")][0].databaseId")
       timeout 900 gh run watch "$RUN_ID" --interval 15 --exit-status
       ```
       15-minute hard timeout (Phase 51 D-06). If timeout: capture state to evidence and FAIL the UAT.

    5. Stage 2 — draft PR opens:
       ```
       PR=$(gh pr list --head "auto-fix/${ISSUE}-${FP:0:8}" --json number --jq '.[0].number')
       gh pr view "$PR" --json state,isDraft,body,labels --jq '{state,isDraft,bodyHasAffectedCases: (.body | contains("affected_cases:")), labels: [.labels[].name]}' > evidence/uat-47-a-pr-state.json
       ```

    6. Stage 3 — verifier-gate runs:
       ```
       sleep 30
       VG_RUN=$(gh run list --branch "auto-fix/${ISSUE}-${FP:0:8}" --workflow=v40-verifier-gate.yml --limit 1 --json databaseId,conclusion --jq '.[0]')
       gh run view "$(echo $VG_RUN | jq -r .databaseId)" --json conclusion,createdAt,jobs > evidence/uat-47-a-verifier-gate-run.json
       ```

    **Stage 3.5 — DEFERRED (post-merge dispatch; replaces pre-merge repo-variable set):**

    Phase 59 REVIEW-FIX WR-01 removed the `vars.PHASE_TAG` fallback from `.github/workflows/v40-auto-promote.yml` (the legacy repo-variable shape silently corrupted normal pull_request auto-promote runs if a forgotten `gh variable set PHASE_TAG` was still active). The PHASE_TAG plumbing is now workflow_dispatch-only: the operator triggers a SECOND auto-promote run via `gh workflow run -f PHASE_TAG=56-uat -f pr_number=<merged-PR-number> -f merged=true` AFTER the merge fires the pull_request.closed auto-promote (which lands `phase: '58-promote'` default, not `'56-uat'`).

    For UAT-47-a (live LLM spend; this is the PRIMARY DoD), the operator follows the post-merge dispatch pattern below (Stage 5a). Stage 3.5 itself is now a no-op placeholder retained for sequencing alignment with SWEEP-04 Step 0; no `gh variable set` command is run.

    **Stage 4 — HUMAN MERGE (the only allowed human action per Pitfall 9; RESEARCH lines 936-940):**

    The executor STOPS here and surfaces to the operator: review the PR, confirm `auto-fix:verified` label is present, then run:
       ```
       gh pr merge $PR --squash --delete-branch=false
       ```
    `--delete-branch=false` is required because Phase 44 auto-promote needs the branch tip (RESEARCH §Cleanup line 973).

    **PASS evidence captured AFTER merge (RESEARCH §Expected PASS evidence lines 942-965):**

    7. Stage 5 — auto-promote fires post-merge:
       ```
       sleep 60
       PROMOTE_RUN=$(gh run list --workflow=v40-auto-promote.yml --limit 1 --json databaseId,conclusion --jq '.[0]')
       gh run view "$(echo $PROMOTE_RUN | jq -r .databaseId)" --json status,conclusion,jobs > evidence/uat-47-a-promote-run.json
       ```

    8. Stage 6 — outcome ledger entry on origin/main (LIVE phase tag per Plan 59-03 Decision C):
       ```
       git fetch origin main
       git show origin/main:tests/e2e/.llm-spend-ledger.json > evidence/uat-47-a-post-run-ledger.json
       jq --arg fp "$FP" '.months | .. | objects | .iterations[]? | select(.fingerprint==$fp and .source=="auto-fix-promoted" and .phase=="56-uat")' evidence/uat-47-a-post-run-ledger.json > evidence/uat-47-a-outcome-entry.json
       ```
       Expected shape (Phase 58 + Plan 59-03 combined): `{ source: 'auto-fix-promoted', outcome: 'pass', errorClass: 'GOOGLE_DOM_DRIFT', fingerprint: $FP, issueId: $ISSUE, prNumber: $PR, model: '...', phase: '56-uat' }`. The `phase: '56-uat'` field is LIVE on the committed ledger entry — no evidence wrapper. The evidence file may be empty `{}` if the loop did not promote OR if the operator skipped the Stage 5a workflow_dispatch re-run (post Phase 59 REVIEW-FIX WR-01, PHASE_TAG arrives via workflow_dispatch input only — there is no repo-variable fallback) — both are UAT FAIL.

    9. Stage 7 — cost ceiling check (Pitfall 10 / Phase 51 D-03):
       ```
       PRE_TOTAL=$(jq '[.months | .. | objects | .monthTotal? // 0] | add' evidence/uat-47-a-pre-run-ledger.json)
       POST_TOTAL=$(jq '[.months | .. | objects | .monthTotal? // 0] | add' evidence/uat-47-a-post-run-ledger.json)
       echo "{\"pre_total_usd\": $PRE_TOTAL, \"post_total_usd\": $POST_TOTAL, \"delta_usd\": $(echo "$POST_TOTAL - $PRE_TOTAL" | bc), \"hard_ceiling_usd\": 2.00}" > evidence/uat-47-a-spend-delta.txt
       ```

    **Cost ceiling enforcement:** If delta > $2.00, FAIL the UAT and document. Do NOT retry (Phase 51 D-03).

    **Phase-tag handling per 59-00 BASELINE Q1 RESOLVED via Plan 59-03 (Decision C):** The outcome entry's `phase` field is set LIVE on the committed ledger via Plan 59-03's `--phase` argv plumbing. SWEEP-03 invokes the workflow flow such that the auto-fix-promote.mjs run carries `phase: '56-uat'`:

    **Stage 5a — Post-merge workflow_dispatch re-run with PHASE_TAG input (executed AFTER Stage 4 merge fires the default auto-promote; supersedes the legacy pre-merge `gh variable set` shape removed by REVIEW-FIX WR-01):**

    The Stage 4 merge fires the `pull_request.closed` auto-promote which lands a `phase: '58-promote'` entry. To capture the SWEEP-05 UAT evidence the operator immediately re-dispatches the auto-promote workflow against the SAME merged PR with `PHASE_TAG=56-uat`:
    ```
    gh workflow run v40-auto-promote.yml \
       -f pr_number=$PR \
       -f merged=true \
       -f PHASE_TAG=56-uat \
       --repo tonyrowles/patent-cite-tool
    sleep 20
    DISPATCH_RUN=$(gh run list --workflow=v40-auto-promote.yml --event=workflow_dispatch --limit 1 --json databaseId,status,conclusion --jq '.[0]')
    echo "$DISPATCH_RUN" > evidence/uat-47-a-phase-tag-dispatch.json
    ```
    The workflow_dispatch trigger consumes `github.event.inputs.PHASE_TAG` directly (Phase 59 REVIEW-FIX CR-01 added `pr_number` + `merged` required inputs); the Triple-gate step env block picks up `PHASE_TAG=56-uat`; the bash conditional appends `--phase "56-uat"` to argv; `auto-fix-promote.mjs` writes a SECOND outcome ledger entry with `phase: '56-uat'` directly (the first `phase: '58-promote'` entry from the auto-trigger remains on the ledger as expected non-UAT shape).

    Expected `evidence/uat-47-a-phase-tag-dispatch.json` shape: `{"databaseId":<N>,"status":"completed","conclusion":"success"}`. If the conclusion is not `success`, STOP and surface (the Stage 6 outcome-entry capture would miss the `phase: '56-uat'` row).

    **Post-SWEEP-04 cleanup:** No repo variable to delete (Phase 59 REVIEW-FIX WR-01 removed that path). Subsequent non-UAT auto-promote runs default to `phase: '58-promote'` automatically because PHASE_TAG is only ever set via workflow_dispatch input — there is no implicit repo-level state to clear. This is no longer Task 7 territory; the cleanup is intrinsic to the trigger shape.

    **Evidence capture:** Plan 59-03's live-entry plumbing means the `evidence/uat-47-a-outcome-entry.json` file is the jq extraction from `tests/e2e/.llm-spend-ledger.json` on origin/main, filtered by `phase=='56-uat' AND fingerprint==$FP AND source=='auto-fix-promoted'`. The `phase` field appears DIRECTLY on the entry — no `uat_tag` wrapper required. SWEEP-05 (Task 7) consolidates this into 56-UAT-EVIDENCE.md.

    **Cleanup (RESEARCH §Cleanup lines 972-974):**
    UAT-47-a's PR is MERGED (the human action). No PR close required. Auto-fix branch deletion is set by `v40-auto-fix.yml:211 delete-branch: false` — Phase 44 auto-promote needs the branch tip. Auto-promote opens a follow-up `auto-promote/*` PR which the operator merges separately. If mutator path was used, synthetic issue is closed in Task 7 SWEEP-06.

    Update `evidence/INDEX.md` with SWEEP-03 PASS / FAIL row + spend delta.
  </action>
  <verify>
    <automated>test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-pre-run-ledger.json &amp;&amp; test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-post-run-ledger.json &amp;&amp; test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-pr-state.json &amp;&amp; test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-verifier-gate-run.json &amp;&amp; test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-promote-run.json &amp;&amp; test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-outcome-entry.json &amp;&amp; test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-spend-delta.txt &amp;&amp; test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-phase-tag-set.txt &amp;&amp; jq -e '.conclusion == "success"' .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-verifier-gate-run.json &amp;&amp; jq -e '.conclusion == "success"' .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-promote-run.json &amp;&amp; jq -e '.source == "auto-fix-promoted" and .outcome == "pass"' .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-outcome-entry.json &amp;&amp; jq -e '.phase == "56-uat"' .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-outcome-entry.json &amp;&amp; jq -e '.delta_usd | tonumber &lt; 2.00' .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-a-spend-delta.txt</automated>
  </verify>
  <acceptance_criteria>
    - All 7 evidence files committed under `evidence/uat-47-a-*`
    - verifier-gate-run.json conclusion == "success"
    - promote-run.json conclusion == "success"
    - outcome-entry.json contains `source: 'auto-fix-promoted'`, `outcome: 'pass'`, `errorClass`, `fingerprint`, `issueId`, `prNumber`, `model`
    - outcome-entry.json carries `phase: '56-uat'` LIVE on the committed ledger entry (per Plan 59-03 Decision C plumbing); post-merge `gh workflow run v40-auto-promote.yml -f pr_number=$PR -f merged=true -f PHASE_TAG=56-uat` re-dispatch (Phase 59 REVIEW-FIX WR-01: dispatch-input only, no repo variable) captured in evidence/uat-47-a-phase-tag-dispatch.json (file name renamed from -phase-tag-set.txt to -phase-tag-dispatch.json to reflect the new shape)
    - spend-delta.txt delta_usd < $2.00 (hard ceiling)
    - PR was MERGED (state field in pr-state.json reflects post-merge); auto-fix branch was NOT deleted (delete-branch=false per RESEARCH)
    - evidence/INDEX.md updated with SWEEP-03 PASS row + spend
  </acceptance_criteria>
  <done>UAT-47-a end-to-end loop verified live on origin/main; outcome ledger entry captured with errorClass + outcome:'pass'; PRIMARY DoD evidence shipped.</done>
</task>

<task type="auto">
  <name>Task 6: SWEEP-04 — UAT-47-b mutator-driven full loop + MUTATOR-04 suppression verification + deps-update-gate smoke</name>
  <files>.planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-injected-issue.json, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-auto-fix-run.json, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-pr-state.json, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-verifier-gate-run.json, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-outcome-entry.json, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-promotion-suppression.txt, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-deps-update-run.json, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-phase-tag-set.txt, .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-quarantine-on-main.txt</files>
  <read_first>
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §UAT-47-b Runbook (SWEEP-04) (lines 978-1017)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md Open Question 2 (lines 1383-1386) — different seed recommended
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §Pitfall 4 (deps-update-gate smoke caveat lines 999-1001)
    - tests/e2e/scripts/inject-defect.mjs (script under test — from plan 59-01)
    - scripts/quarantine-append.mjs:218-223 (live form on origin/main after plan 59-01 merge — confirms `!isFixtureMutator` is on origin)
  </read_first>
  <action>
    Execute UAT-47-b runbook from RESEARCH §UAT-47-b Runbook lines 978-1017. Reuses SWEEP-03 infrastructure with a DIFFERENT seed (Open Question 2 recommendation).

    **Step 0 — DEFERRED (post Phase 59 REVIEW-FIX WR-01: no repo variable to verify; PHASE_TAG now arrives via workflow_dispatch input only):**

    SWEEP-04 follows the same post-merge `gh workflow run -f PHASE_TAG=56-uat` re-dispatch pattern as SWEEP-03 Stage 5a. Step 0 is now a no-op placeholder retained for the original section numbering. Skip directly to Step 1.

    Legacy text (retained as a record of what changed):

    > Step 0 — Verify Plan 59-03 PHASE_TAG repo variable is STILL set (carried from SWEEP-03 Stage 3.5):
       ```
       gh variable list --repo tonyrowles/patent-cite-tool --json name,value --jq '.[] | select(.name=="PHASE_TAG")' > evidence/uat-47-b-phase-tag-set.txt
       cat evidence/uat-47-b-phase-tag-set.txt
       ```
       Expected: `{"name":"PHASE_TAG","value":"56-uat"}`. If empty (e.g., operator manually cleared between SWEEP-03 and SWEEP-04), re-set via `gh variable set PHASE_TAG --body 56-uat` and re-capture evidence. The variable MUST be set BEFORE the Step 2 Stage 4 human merge for SWEEP-04's auto-promote run to land `phase: '56-uat'`.

    **Step 1 — Run mutator with different seed (RESEARCH §1 line 988):**
       ```
       node tests/e2e/scripts/inject-defect.mjs --seed mutator-seed-sweep-04 --error-class GOOGLE_DOM_DRIFT
       ```
       Stdout: `[inject-defect] issue #M created with fingerprint YY`. `ISSUE2=M` and `FP2=YY` (distinct from SWEEP-03's ISSUE/FP).

       Capture the injected issue state:
       ```
       gh issue view "$ISSUE2" --json number,title,labels,body > evidence/uat-47-b-injected-issue.json
       ```

    **Step 2 — Same loop watching as SWEEP-03 Stages 1-7 (RESEARCH §2 line 989):**
       Stage 1 auto-fix workflow:
       ```
       sleep 10
       RUN_ID2=$(gh run list --workflow=v40-auto-fix.yml --limit 5 --json databaseId,event,createdAt --jq "[.[] | select(.event==\"issues\")][0].databaseId")
       timeout 900 gh run watch "$RUN_ID2" --interval 15 --exit-status
       gh run view "$RUN_ID2" --json status,conclusion,jobs > evidence/uat-47-b-auto-fix-run.json
       ```

       Stage 2 draft PR:
       ```
       PR2=$(gh pr list --head "auto-fix/${ISSUE2}-${FP2:0:8}" --json number --jq '.[0].number')
       gh pr view "$PR2" --json state,isDraft,body,labels > evidence/uat-47-b-pr-state.json
       ```

       Stage 3 verifier-gate:
       ```
       sleep 30
       VG_RUN2=$(gh run list --branch "auto-fix/${ISSUE2}-${FP2:0:8}" --workflow=v40-verifier-gate.yml --limit 1 --json databaseId,conclusion --jq '.[0]')
       gh run view "$(echo $VG_RUN2 | jq -r .databaseId)" --json conclusion,createdAt,jobs > evidence/uat-47-b-verifier-gate-run.json
       ```

       Stage 4 HUMAN MERGE: same as SWEEP-03 Task 5 Stage 4; operator merges `$PR2`. Surface to operator.

       Stage 5-6 outcome entry on origin/main (LIVE phase tag per Plan 59-03 Decision C):
       ```
       sleep 60
       git fetch origin main
       jq --arg fp "$FP2" '.months | .. | objects | .iterations[]? | select(.fingerprint==$fp and .source=="auto-fix-promoted" and .phase=="56-uat")' \
         <(git show origin/main:tests/e2e/.llm-spend-ledger.json) > evidence/uat-47-b-outcome-entry.json
       ```
       No evidence wrapper — `phase: '56-uat'` is LIVE on the committed entry (vars.PHASE_TAG plumbing from Plan 59-03; was already set during SWEEP-03 and is still active for SWEEP-04 since cleanup is in Task 7). The empty-result-`{}` failure mode is the same as SWEEP-03 Stage 6.

    **Step 3 — MUTATOR-04 suppression verification (RESEARCH §3 line 991):**
       After auto-promote completes and the synthetic case lands in the quarantine corpus, verify the suppression. The mutator entry's `source_triage_finding_id` starts with `fixture-mutator-uat-47b`.

       Option A — local mock-gh 3-run transcript (deterministic):
       ```
       # Use the same mock-gh test scaffold from tests/e2e/scripts/e2e-quarantine-append.test.js
       # but invoke quarantine-append.mjs CLI from a tmp dir with seeded corpus override
       # carrying source_triage_finding_id="fixture-mutator-uat-47b-iter-1" entry.
       # Run 3 times; capture transcript; assert NO --add-label quarantine:ready-for-promotion.
       ```
       Recommendation (combined approach — checker fix issue 5):

       (a) **DIRECT INSPECTION of post-SWEEP-04 origin/main quarantine corpus** (PRIMARY evidence — exercises the actual production code path that already ran on the auto-promote workflow against the synthetic mutator case):
       ```
       git fetch origin main
       git show origin/main:tests/e2e/test-cases-quarantine.js > evidence/uat-47-b-quarantine-on-main.txt
       # Parse: find the synthetic entry by source_triage_finding_id starts-with 'fixture-mutator-uat-47b'
       SYNTH_ID=$(node -e "
         const m = require('module').createRequire(import.meta.url);
         import('./.planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-quarantine-on-main-loader.mjs').catch(() => {
           // Fallback: load via vm
           const fs = require('fs');
           const src = fs.readFileSync('.planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-quarantine-on-main.txt', 'utf8');
           // Strip the AUTO-MANAGED header + export keyword to get the array literal
           const m2 = src.match(/export const TEST_CASES_QUARANTINE = (\\[[\\s\\S]*?\\]);/);
           if (!m2) { console.error('NO_MATCH'); process.exit(1); }
           const arr = JSON.parse(m2[1].replace(/'/g, '"'));
           const synth = arr.find(e => typeof e.source_triage_finding_id === 'string' && e.source_triage_finding_id.startsWith('fixture-mutator-uat-47b'));
           if (!synth) { console.error('NO_SYNTH'); process.exit(1); }
           console.log(synth.id);
         });
       ")
       # If SYNTH_ID is empty, FAIL UAT (the mutator-driven loop did not land its synthetic in quarantine)
       [ -n "$SYNTH_ID" ] && echo "SYNTH_PRESENT=$SYNTH_ID" >> evidence/uat-47-b-promotion-suppression.txt
       # Now query the synthetic case's triage issue labels on origin/main to confirm the label was NOT added
       SYNTH_ISSUE=$ISSUE2
       SYNTH_LABELS=$(gh issue view "$SYNTH_ISSUE" --json labels --jq '[.labels[].name]')
       echo "ISSUE_${SYNTH_ISSUE}_LABELS=$SYNTH_LABELS" >> evidence/uat-47-b-promotion-suppression.txt
       echo "$SYNTH_LABELS" | jq -e 'index("quarantine:ready-for-promotion") == null' &&          echo "MUTATOR-04 PASS: synthetic entry present in quarantine AND triage issue #$SYNTH_ISSUE does NOT carry quarantine:ready-for-promotion label" >> evidence/uat-47-b-promotion-suppression.txt
       ```

       This is a DIRECT production-code-path assertion: the synthetic case made it through the full mutator → triage → auto-fix → verifier → merge → auto-promote loop AND the post-merge auto-promote run actually invoked quarantine-append.mjs against the synthetic — and the suppression conditional fired (no label). This is the load-bearing MUTATOR-04 evidence.

       (b) **SECONDARY Vitest re-run against origin/main code** (defense-in-depth — confirms the suppression code is byte-equivalent on origin/main):
       ```
       git fetch origin main
       git checkout origin/main -- scripts/quarantine-append.mjs tests/e2e/scripts/e2e-quarantine-append.test.js
       CI=true npx vitest run tests/e2e/scripts/e2e-quarantine-append.test.js -t "MUTATOR-04" >> evidence/uat-47-b-promotion-suppression.txt 2>&1
       git checkout HEAD -- scripts/quarantine-append.mjs tests/e2e/scripts/e2e-quarantine-append.test.js
       ```

       Both (a) and (b) outputs accumulate in `evidence/uat-47-b-promotion-suppression.txt`. The PASS line from (a) AND the G9 Vitest PASS from (b) both being present is the SWEEP-04 MUTATOR-04 PASS gate.

    **Step 4 — Deps-update-gate smoke (RESEARCH §4 lines 992-1000; Phase 51.1 fix confirmation):**
       ```
       gh workflow run v40-deps-update.yml --ref main
       sleep 30
       DEPS_RUN=$(gh run list --workflow=v40-deps-update.yml --limit 1 --json databaseId,conclusion --jq '.[0]')
       gh run view "$(echo $DEPS_RUN | jq -r .databaseId)" --json status,conclusion,jobs > evidence/uat-47-b-deps-update-run.json
       ```
       NOTE per RESEARCH §1000: if Pitfall 4 was resolved via Option β (dep-scan removed from required checks instead of enabling the repo setting), then the auto-opened deps-update PR may not exist and the gate trigger smoke is degraded. Document the resolution path in INDEX.md.

    Update `evidence/INDEX.md` with SWEEP-04 PASS row.
  </action>
  <verify>
    <automated>test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-injected-issue.json &amp;&amp; test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-auto-fix-run.json &amp;&amp; test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-pr-state.json &amp;&amp; test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-verifier-gate-run.json &amp;&amp; test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-outcome-entry.json &amp;&amp; test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-promotion-suppression.txt &amp;&amp; test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-deps-update-run.json &amp;&amp; test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-phase-tag-set.txt &amp;&amp; jq -e '.conclusion == "success"' .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-verifier-gate-run.json &amp;&amp; jq -e '.source == "auto-fix-promoted" and .outcome == "pass"' .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-outcome-entry.json &amp;&amp; jq -e '.phase == "56-uat"' .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-outcome-entry.json &amp;&amp; grep -E "MUTATOR-04 PASS|SYNTH_PRESENT" .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-promotion-suppression.txt &amp;&amp; grep -E "G9|PASS" .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/uat-47-b-promotion-suppression.txt</automated>
  </verify>
  <acceptance_criteria>
    - All 7 evidence files captured for SWEEP-04
    - Mutator created a DIFFERENT issue than SWEEP-03 (different seed → different fingerprint → different issue#)
    - verifier-gate-run.json conclusion == "success"
    - outcome-entry.json contains `source: 'auto-fix-promoted'`, `outcome: 'pass'`, and `phase: '56-uat'` LIVE on the committed ledger entry (per Plan 59-03 Decision C plumbing; vars.PHASE_TAG from SWEEP-03 still active through SWEEP-04)
    - promotion-suppression.txt contains BOTH (a) `SYNTH_PRESENT=<id>` from direct origin/main quarantine corpus inspection + `MUTATOR-04 PASS: synthetic entry present in quarantine AND triage issue #<n> does NOT carry quarantine:ready-for-promotion label` AND (b) G9 Vitest PASS from re-run against origin/main quarantine-append.mjs (defense-in-depth)
    - deps-update-run.json captured (PASS via fast-path OR documented Pitfall 4 Option β degradation)
    - evidence/INDEX.md updated with SWEEP-04 PASS row
  </acceptance_criteria>
  <done>UAT-47-b mutator-driven loop verified; MUTATOR-04 suppression proven on origin/main; deps-update-gate trigger smoke captured.</done>
</task>

<task type="auto">
  <name>Task 7: SWEEP-05 + SWEEP-06 — consolidate 56-UAT-EVIDENCE.md and ship uat-cleanup.mjs (cleanup PR opened)</name>
  <files>.planning/phases/59-fixture-mutator-4-uat-re-sweep/56-UAT-EVIDENCE.md, tests/e2e/scripts/uat-cleanup.mjs</files>
  <read_first>
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §56-UAT-EVIDENCE.md Template (SWEEP-05) (lines 1020-1095)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §Cleanup Automation (SWEEP-06) (lines 1098-1136)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/59-RESEARCH.md §Pitfall 7 — synthetic quarantine revert (lines 1212-1218)
    - .planning/milestones/v4.1-phases/51-live-readiness-uats/51-UAT-EVIDENCE.md (frontmatter + structure template)
    - .planning/phases/59-fixture-mutator-4-uat-re-sweep/56-MUTATOR-CLEANUP.md (the manifest from plan 59-01 — read appended Run sections to discover synthetic issue numbers, branch names, and fingerprints to clean up)
    - tests/e2e/scripts/inject-defect.mjs (mirror its CLI scaffolding — parseArgs pattern, ESM is-main-module gate)
  </read_first>
  <action>
    **Part A — Write 56-UAT-EVIDENCE.md (SWEEP-05):**

    Create `.planning/phases/59-fixture-mutator-4-uat-re-sweep/56-UAT-EVIDENCE.md` mirroring `.planning/milestones/v4.1-phases/51-live-readiness-uats/51-UAT-EVIDENCE.md` structure per RESEARCH §56-UAT-EVIDENCE.md Template (lines 1020-1077).

    Frontmatter fields (RESEARCH §1027-1045):
    - phase: 59-fixture-mutator-4-uat-re-sweep
    - plan: 02
    - status: complete
    - created: <UTC date of SWEEP-01 completion>
    - completed_at: <UTC date now>
    - canonical_repo: tonyrowles/patent-cite-tool
    - requirements_addressed: [SWEEP-01, SWEEP-02, SWEEP-03, SWEEP-04, SWEEP-05, SWEEP-06]
    - sequence_followed: "SWEEP-01 → SWEEP-02 → SWEEP-03 → SWEEP-04 (D-13 cost discipline)"
    - budget_cap_usd: 5
    - budget_spent_usd: <pull from evidence/uat-47-a-spend-delta.txt>
    - related_phases: [56 ledger-schema-enabler, 57 branch-redirect-enabler, 58 outcome-entry-enabler]
    - phase_tag_strategy: live-entry (per 59-00 BASELINE Q1 RESOLVED via Plan 59-03 Decision C — phase: '56-uat' is set directly on the committed ledger entry via vars.PHASE_TAG plumbing; SWEEP-05 filterable-production-analysis goal satisfied at the live ledger layer)

    Body sections:
    1. **Outcome Matrix** (RESEARCH §1051-1057): markdown table with UAT × Status × Evidence × Notes columns. Status from each SWEEP task's PASS/FAIL determination.

    2. **Per-UAT detail** (RESEARCH §1058-1077): one section per UAT (UAT-47-e, UAT-47-d, UAT-47-a, UAT-47-b) mirroring the 51-UAT-EVIDENCE.md §UAT-47-e structure with: status, verified_at, pr_number (or run id), branch, evidence file list, heuristic_assertions table, deviations list.

    3. **Phase-tag strategy** (per 59-00 BASELINE Q1 RESOLVED via Plan 59-03 Decision C): explicit paragraph stating that `phase: '56-uat'` is set DIRECTLY on the committed `tests/e2e/.llm-spend-ledger.json` outcome entries on origin/main. This is achieved via Plan 59-03's `--phase` argv plumbing on `scripts/auto-fix-promote.mjs` (default `'58-promote'` preserves non-UAT shape byte-equivalent) combined with the `.github/workflows/v40-auto-promote.yml` dual-path env expression `PHASE_TAG: ${{ github.event.inputs.PHASE_TAG || vars.PHASE_TAG || '' }}`. SWEEP-03 Stage 3.5 + SWEEP-04 Step 0 set the repo variable `vars.PHASE_TAG=56-uat` BEFORE the human merge; Task 7 SWEEP-06 clears it after. Note: the UPSTREAM `auto-fix-api` entry (from `scripts/auto-fix.mjs`) still carries `phase: '42-auto-fix'` because Plan 59-03 deliberately scoped to auto-fix-promote.mjs only (not auto-fix.mjs — the auto-fix.mjs entry is upstream of the promote layer and a-b-winner.mjs does NOT key off its phase field). Filterable production analysis at the consumer layer (outcome entries that a-b-winner.mjs reads) uses `jq '.iterations[] | select(.phase == "56-uat")'` against origin/main directly.

    4. **Per-Pitfall closure** (mirror 51-UAT-EVIDENCE.md pattern): explicit row per Pitfall 4, 5, 6, 7, 8, 10, 11 stating closure status.

    5. **Cost ledger**: pre/post totals + delta + hard ceiling status.

    6. **Cleanup status**: list of cleanup actions completed by Part B below; pointer to the SWEEP-06 cleanup PR URL.

    **Part B — Author tests/e2e/scripts/uat-cleanup.mjs and execute SWEEP-06 (RESEARCH §1098-1136):**

    Create `tests/e2e/scripts/uat-cleanup.mjs` as an idempotent one-shot script supporting:
    - `--close-prs` — closes all test PRs matching `--search 'in:title UAT-47-e OR in:title fixture-mutator'` with `gh pr close --delete-branch`
    - `--close-issues` — closes all open synthetic issues matching `--search 'in:title fixture-mutator'` with `gh issue close --reason 'not planned' --comment "UAT-47-b synthetic — closed per SWEEP-06 cleanup"`
    - `--revert-quarantine --source-tag <tag>` — reads tests/e2e/test-cases-quarantine.js (via CORPUS_PATH-style env-overridable resolution mirror of quarantine-append.mjs:25-27), strips entries whose `source_triage_finding_id.startsWith(<tag>)`, writes back atomically using the same `atomicWriteJson` pattern + `stringifyCorpus`/`formatEntry` reuse imported from `scripts/quarantine-append.mjs`.
    - `--all` — runs all three in safe order.

    The revert-quarantine step CANNOT direct-push to main (FORBIDDEN_PATHS regex 3 + Phase 50 ruleset). The cleanup PR pattern is:
    ```
    BRANCH="chore/sweep-06-cleanup-$(date -u +%Y%m%dT%H%M%SZ)"
    git checkout -b "$BRANCH" main
    node tests/e2e/scripts/uat-cleanup.mjs --revert-quarantine --source-tag fixture-mutator-uat-47b
    git add tests/e2e/test-cases-quarantine.js
    git commit -m "chore(59): SWEEP-06 — revert synthetic quarantine entries"
    git push -u origin "$BRANCH"
    gh pr create --title "SWEEP-06 cleanup: revert synthetic quarantine entries" \
      --body "Per Phase 59 SWEEP-06 / Pitfall 11. Removes fixture-mutator-uat-47b entries from quarantine corpus."
    ```

    Operator executes this. Capture the cleanup PR number in 56-UAT-EVIDENCE.md.

    Mirror the `inject-defect.mjs` parseArgs / ESM is-main-module gate pattern. Reuse `formatEntry`, `stringifyCorpus`, and the corpus path resolution from `scripts/quarantine-append.mjs` via named ESM imports.

    **Ledger entry handling (RESEARCH §Caveat lines 1133, updated for Plan 59-03 Decision C):** Do NOT delete UAT-tagged ledger entries. The tag-based filtering pattern is the cleanup. Live ledger entries persist with `phase: '56-uat'` set DIRECTLY at the entry level (no evidence wrapper). Operator cleanup is at the repo-variable layer: `gh variable delete PHASE_TAG --repo tonyrowles/patent-cite-tool` so subsequent non-UAT auto-promote runs default back to `phase: '58-promote'`.

    **Execute the cleanup (operator runs):**
    1. `node tests/e2e/scripts/uat-cleanup.mjs --close-prs --close-issues` against origin (closes test PRs and synthetic issues).
    2. Open the cleanup PR per the BRANCH script above; merge or leave for separate operator review.
    3. Delete the Plan 59-03 PHASE_TAG repo variable: `gh variable delete PHASE_TAG --repo tonyrowles/patent-cite-tool`. Verify deletion: `gh variable list --repo tonyrowles/patent-cite-tool --json name --jq '[.[] | select(.name=="PHASE_TAG")] | length'` returns 0. Capture to `evidence/sweep-06-phase-tag-cleared.txt`.

    Update `evidence/INDEX.md` with SWEEP-05 + SWEEP-06 completion rows and the cleanup PR URL.
  </action>
  <verify>
    <automated>test -f .planning/phases/59-fixture-mutator-4-uat-re-sweep/56-UAT-EVIDENCE.md &amp;&amp; test -f tests/e2e/scripts/uat-cleanup.mjs &amp;&amp; grep -E "phase_tag_strategy.*live-entry" .planning/phases/59-fixture-mutator-4-uat-re-sweep/56-UAT-EVIDENCE.md &amp;&amp; grep -E "Outcome Matrix" .planning/phases/59-fixture-mutator-4-uat-re-sweep/56-UAT-EVIDENCE.md &amp;&amp; grep -E "UAT-47-e|UAT-47-d|UAT-47-a|UAT-47-b" .planning/phases/59-fixture-mutator-4-uat-re-sweep/56-UAT-EVIDENCE.md | wc -l | { read N; [ "$N" -ge 4 ]; } &amp;&amp; grep -E "Pitfall (4|5|6|7|8|10|11)" .planning/phases/59-fixture-mutator-4-uat-re-sweep/56-UAT-EVIDENCE.md | wc -l | { read N; [ "$N" -ge 6 ]; } &amp;&amp; grep -E "gh pr close|gh issue close|--revert-quarantine" tests/e2e/scripts/uat-cleanup.mjs &amp;&amp; node -e "import('./tests/e2e/scripts/uat-cleanup.mjs').then(m =&gt; console.log('OK')).catch(e =&gt; { console.error(e); process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    - `56-UAT-EVIDENCE.md` exists with frontmatter, Outcome Matrix, per-UAT detail sections, Pitfall closure table, cost ledger, cleanup status
    - `phase_tag_strategy: live-entry` documented in frontmatter (per Plan 59-03 Decision C)
    - All 4 UATs (47-e, 47-d, 47-a, 47-b) have per-UAT detail sections
    - At least 6 Pitfall closure rows (4, 5, 6, 7, 8, 10, 11)
    - `tests/e2e/scripts/uat-cleanup.mjs` exists with `--close-prs`, `--close-issues`, `--revert-quarantine` argv handling
    - uat-cleanup.mjs is importable (ESM module loads without error)
    - SWEEP-06 cleanup PR has been opened (URL captured in 56-UAT-EVIDENCE.md cleanup-status section)
    - Open synthetic issues count: `gh issue list --search 'in:title fixture-mutator' --state open --json number --jq 'length'` returns 0
    - Test branches deleted: `git ls-remote origin 'auto-fix/test-uat47e-*'` returns empty
    - evidence/INDEX.md final state: 22 rows + pre-flight-state.json + sweep-03-cost-acknowledgement.json + cleanup PR row, all with PASS or PASS-on-fail-documented status
  </acceptance_criteria>
  <done>56-UAT-EVIDENCE.md consolidates all 4 UATs; uat-cleanup.mjs ships; cleanup PR opened; all synthetic issues closed; test branches deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator local shell → github (origin/main) | All UAT operations execute against origin/main with operator credentials; no fork-isolation per CONTEXT Deferred Ideas |
| auto-fix workflow → Anthropic API | SWEEP-03 paid invocation; cost cap enforced via pre/post ledger snapshot delta |
| synthetic ledger entries → committed ledger | UAT entries persist on origin/main (Pitfall 10); mitigation = LIVE `phase: '56-uat'` entry-level tag (Plan 59-03 Decision C plumbing); filterable directly via jq against the committed ledger |
| synthetic quarantine entries → golden corpus | MUTATOR-04 suppression (from plan 59-01) prevents auto-promotion; SWEEP-06 cleanup PR removes the entries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-06 | DoS (cost overrun) | SWEEP-03 paid LLM invocation | mitigate | Task 4 checkpoint:human-verify gate surfaces $0.50-2 expected; Task 5 spend-delta gate hard-fails on >$2 (Phase 51 D-03 "no retry past cap") |
| T-59-07 | Tampering | Live production state (origin/main) | mitigate | All Work stream B tasks are `autonomous: false`; every PR/issue/workflow operation surfaces to operator; pre-flight Task 1 hard-gates on PR #18 merge state and repo-setting state |
| T-59-08 | Tampering | UAT contamination of ledger phase-tag mitigation (Pitfall 10) | mitigate | auto-fix-promote.mjs outcome entries carry `phase: '56-uat'` LIVE via Plan 59-03 Decision C plumbing (vars.PHASE_TAG → workflow env expression → script --phase argv); SWEEP-06 cleanup removes the repo variable so subsequent non-UAT runs default to `'58-promote'`; upstream auto-fix-api entries still carry `phase: '42-auto-fix'` (scoped to auto-fix-promote.mjs only — a-b-winner.mjs filters on the outcome-entry phase, not the upstream auto-fix-api phase) |
| T-59-09 | DoS | Orphaned synthetic GitHub issues / branches / quarantine entries | mitigate | SWEEP-06 (Task 7) closes PRs (`gh pr close --delete-branch`), closes issues (`gh issue close --reason 'not planned'`), and reverts quarantine entries via follow-up cleanup PR |
| T-59-10 | Tampering | direct push to main blocked by FORBIDDEN_PATHS regex 3 (quarantine corpus) on cleanup attempt | mitigate | Task 7 uat-cleanup.mjs requires follow-up PR pattern; never attempts `git push origin main` for FORBIDDEN_PATHS edits (matches Phase 50 ruleset + diff-guard regex 3) |
| T-59-SC | Tampering | Supply-chain (npm/pip/cargo installs) | n/a | Zero new packages in Work stream B (uat-cleanup.mjs reuses existing imports from scripts/quarantine-append.mjs); no package legitimacy gate needed |
</threat_model>

<verification>
- Task 1: pre-flight-state.json captures all 6 checks; operator approval recorded.
- Task 2 (SWEEP-01): 3 evidence files; diff-guard bucket=fail; human-review-required label; tests/golden/baseline.json mention.
- Task 3 (SWEEP-02): 4 evidence files; new ledger-snapshots branch; no main commit.
- Task 4: cost acknowledgement explicit decision recorded.
- Task 5 (SWEEP-03): 8 evidence files (incl. phase-tag-set.txt from Stage 3.5); outcome entry source=='auto-fix-promoted' + outcome=='pass' + phase=='56-uat' LIVE; spend < $2.
- Task 6 (SWEEP-04): 9 evidence files (7 original + phase-tag-set.txt + quarantine-on-main.txt); different seed than SWEEP-03; MUTATOR-04 suppression PROVEN via direct origin/main quarantine inspection + Vitest re-run defense-in-depth; deps-update-gate run captured.
- Task 7 (SWEEP-05+06): 56-UAT-EVIDENCE.md consolidated; uat-cleanup.mjs ships; cleanup PR opened; 0 open synthetic issues remain; 0 test-uat47e branches on origin.
- Phase gate: `cat .planning/phases/59-fixture-mutator-4-uat-re-sweep/evidence/INDEX.md | grep -c PASS` matches expected count; `56-UAT-EVIDENCE.md` status = complete.
</verification>

<success_criteria>
- All 4 UATs PASS with captured JSON evidence
- 56-UAT-EVIDENCE.md committed with Outcome Matrix, per-UAT detail, Pitfall closure table
- All UAT outcome ledger entries carry `phase: '56-uat'` LIVE on the committed ledger entry on origin/main (Plan 59-03 Decision C; Pitfall 10 satisfied at the live-entry layer)
- SWEEP-03 spend < $2 hard ceiling
- Post-UAT cleanup complete: 0 open synthetic GitHub issues, 0 dangling test branches, cleanup PR opened for quarantine revert
- `tests/e2e/scripts/uat-cleanup.mjs` is idempotent (running it twice does not error)
- v4.2 milestone DoD evidence (primary: SWEEP-03 outcome entry) shipped on origin/main
</success_criteria>

<output>
Create `.planning/phases/59-fixture-mutator-4-uat-re-sweep/59-02-SUMMARY.md` capturing:
- PASS/FAIL per UAT
- SWEEP-03 spend delta + hard-ceiling compliance
- SWEEP-06 cleanup PR URL
- Cross-reference to 56-UAT-EVIDENCE.md for full evidence index
- Pointer to Phase 60 (carry-along cleanup; fully independent of Phase 59 evidence)
- Confirmation that v4.2 milestone DoD has been satisfied (the auto-fix loop is now operationally validated on origin/main with at least one production fix shipped end-to-end via the SWEEP-03 outcome ledger entry)
</output>
