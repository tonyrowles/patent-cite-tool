---
phase: 61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep
plan: 03
type: execute
wave: 1
depends_on: [61-01, 61-02]
files_modified:
  - .planning/STATE.md
autonomous: true
requirements: [BUDG-01]
tags: [budget, state-md, atomic-commit, trust-invariants, integration-gate]

must_haves:
  truths:
    - ".planning/STATE.md contains a ## Budget section with 7 rows: Milestone soft cap=$15, Milestone hard ceiling=$30, Per-phase<$5, Mean per-call<$0.30, Per-issue cap=$1, Per-PR cap=$2, Per-fingerprint prompt-iter cap=$0.50 — verified via grep on each literal value"
    - "Trust invariants verified BEFORE the atomic commit: assertTripleGate body byte-equivalent to Phase 53 baseline; appendLedgerEntry body byte-unchanged; SOURCE_TAG literal 'fixture-mutator-uat-47b' at inject-defect.mjs:75 byte-unchanged; quarantine-append.mjs:239 &&!isFixtureMutator filter byte-unchanged; v40-auto-fix.yml 'git push origin main' grep count == 1"
    - "ONE atomic commit lands all Plan 01 + Plan 02 + Plan 03 changes together: 4 source files (inject-defect.mjs, e2e-inject-defect.test.js, llm-driver.js, llm-driver.test.js) + 2 NEW files (llm-driver-cost-bound.test.js, ledger-cost-bound.jsonl) + STATE.md (if Budget section drifted)"
    - "Full `npx vitest run` (entire test suite) is GREEN before the atomic commit is pushed — no failing tests, including 33 pre-existing ledger tests"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "## Budget section live with 7-row table per BUDG-01 spec; verified byte-identical to research-locked baseline"
      contains: "Milestone soft cap"
    - path: "git-commit"
      provides: "ONE atomic commit on origin/main (or local main awaiting push) covering all Phase 61 source/test/fixture/STATE.md edits"
      min_lines: 0
  key_links:
    - from: ".planning/STATE.md ## Budget section"
      to: "BUDG-01 requirement (REQUIREMENTS.md line 80)"
      via: "literal table row values $15/$30/$5/$0.30"
      pattern: "Milestone soft cap.*\\$15"
    - from: "atomic commit"
      to: "Plan 01 (DIAG) + Plan 02 (TURNS) files"
      via: "git add + git commit covering all 6 source/test files in a single SHA"
      pattern: "DIAG.*TURNS.*BUDG"
---

<objective>
Verify the `.planning/STATE.md ## Budget` section is byte-identical to the BUDG-01 spec (7 rows: $15 soft cap, $30 hard ceiling, <$5 per-phase, <$0.30 mean per-call, $1 per-issue, $2 per-PR, $0.50 per-fingerprint prompt-iter cap). If drift exists, restore. Then run trust-invariant pre-commit checks (assertTripleGate sha256, appendLedgerEntry body bytes, SOURCE_TAG literal, quarantine-append.mjs filter, v40-auto-fix.yml direct-to-main push count). Run the full Vitest suite. Stage ALL Phase 61 changes (Plan 01 + Plan 02 + Plan 03 surfaces) and create ONE atomic commit per the v4.2 SWEEP-03 Pitfall 1+2 mitigation — partial states recreate the v4.2 failure shape.

Purpose: This plan is the integration gate. It owns the BUDG-01 verify-only happy path AND the atomic-commit invariant for the Phase 61 carry-over bundle. Without a single-commit boundary, partial states (e.g., TURNS-01 lands without DIAG-01) hit `error_max_turns` (TURNS without DIAG) or `apply-check-failed` (DIAG without TURNS) — both reproduce v4.2 SWEEP-03's failure shape per Pitfalls 1+2.

Output: A single git commit (subject `feat(61): atomic carry-over bundle — DIAG + TURNS + BUDG`) containing all Phase 61 source/test/fixture/STATE.md changes, ready to push to origin/main. Plan 04 + Plan 05 (post-merge UAT runbooks) execute AFTER this commit lands on origin/main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep/61-CONTEXT.md
@.planning/phases/61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep/61-RESEARCH.md

<!-- Prior plan SUMMARYs are NOT required at write time — this plan only reads STATE.md and runs grep/test gates. -->

<interfaces>
<!-- Verified extraction from disk 2026-06-09 -->

From .planning/STATE.md:33-46 (CURRENT — VERIFY-ONLY TARGET):
```markdown
## Budget

| Cap | Value | Source |
|-----|-------|--------|
| Milestone soft cap | **$15** | BUDG-01 (v4.3 requirement) |
| Milestone hard ceiling | **$30** | PITFALLS Pitfall 9 reasoning |
| Per-phase | **< $5** | BUDG-01 distribution |
| Mean per-call (`--max-turns 5` regression) | **< $0.30** | TURNS-03 cost-bound test |
| Per-issue cap (existing) | $1 (`ISSUE_HARD_CAP_USD`) | Phase 39 LEDGER-02 |
| Per-PR cap (existing) | $2 | Phase 39 LEDGER-02 |
| Per-fingerprint prompt-iter cap | **$0.50** (`PROMPT_ITER_COST_CAP_USD`) | PITER-03 (Phase 67) |
```

BUDG-01 acceptance (per REQUIREMENTS.md line 80):
- Milestone soft cap: $15
- Milestone hard ceiling: $30
- Per-phase: < $5
- Mean per-call (TURNS-03): < $0.30
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify .planning/STATE.md ## Budget section + trust invariants (BUDG-01 verify-only)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (lines 33-46 for the Budget section — verify it matches the locked spec; lines 1-50 for frontmatter and Current Position so any edit preserves surrounding state)
    - .planning/REQUIREMENTS.md (BUDG-01 line at the end of v1 Requirements — locked value list)
    - .planning/phases/61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep/61-CONTEXT.md (Budget Section Content sub-section — confirms the table content is already present at STATE.md lines 33-46)
  </read_first>
  <action>
    Step 1 — Verify ## Budget section present and byte-identical to locked spec:
    Run these grep commands sequentially. Each must return exactly 1 (excluding comments):
      a) `grep -c "## Budget" .planning/STATE.md`
      b) `grep -c "Milestone soft cap" .planning/STATE.md` (paired with $15)
      c) `grep -c "Milestone hard ceiling" .planning/STATE.md` (paired with $30)
      d) `grep -c "Per-phase" .planning/STATE.md` (paired with < $5)
      e) `grep -c "Mean per-call" .planning/STATE.md` (paired with < $0.30)
      f) `grep -c "Per-issue cap" .planning/STATE.md` (paired with $1 / ISSUE_HARD_CAP_USD)
      g) `grep -c "Per-PR cap" .planning/STATE.md` (paired with $2)
      h) `grep -c "Per-fingerprint prompt-iter cap" .planning/STATE.md` (paired with $0.50 / PROMPT_ITER_COST_CAP_USD)

    Step 2 — IF and ONLY IF the section is missing OR any of the 7 row literals drifted: restore the locked Budget section by inserting/replacing the markdown block at lines 33-46 (or wherever it currently sits) with the verbatim content from `<interfaces>` above. Preserve surrounding sections (## Project Reference above, ## Bypass Conventions below) byte-unchanged. DO NOT add new rows; DO NOT delete the existing rows; DO NOT reorder. If the section is already present and byte-identical, this step is a no-op.

    Step 3 — Trust-invariant pre-commit grep gates (each must hold):
      a) `grep -c "fixture-mutator-uat-47b" tests/e2e/scripts/inject-defect.mjs` == 1 (SOURCE_TAG literal preserved — co-design with quarantine-append.mjs:239)
      b) `grep -c "&& !isFixtureMutator" scripts/quarantine-append.mjs` == 1 (MUTATOR-04 production-path suppression filter preserved)
      c) `grep -c "git push origin main" .github/workflows/v40-auto-fix.yml` == 1 (Phase 57 scope-lock: direct-to-main ledger commit pattern unchanged)
      d) `grep -c "ERROR_CLASSES = new Set" tests/e2e/scripts/inject-defect.mjs` == 1 (ERROR_CLASSES allowlist additive-only; this phase adds NO new classes)
      e) `grep -c "ANTHROPIC_API_KEY: ''" tests/e2e/lib/llm-driver.js` >= 1 (T-31-4 ANTHROPIC_API_KEY blanking preserved)

    Step 4 — IF any Step 3 gate fails, STOP. Do not proceed to Task 2. Surface the failure to the user with the exact grep command and observed count. This is a trust-invariant violation — must be investigated before any commit.

    Constraints:
    - The Budget section in STATE.md is owned by this phase as the "verify-only happy path" per CONTEXT.md (table is already drafted at lines 33-46 as of 2026-06-09); restoration is the fallback if drift detected during Phase 61 execution
    - Do not edit any other line in STATE.md (frontmatter, Current Position, Decisions, etc. are out of scope for Phase 61)
    - The trust-invariant grep gates in Step 3 are NON-NEGOTIABLE — Phase 61's atomic commit cannot proceed if any fails
  </action>
  <verify>
    <automated>grep -c "Milestone soft cap" .planning/STATE.md | grep -q "^1$" && grep -c "Mean per-call" .planning/STATE.md | grep -q "^1$" && grep -c "fixture-mutator-uat-47b" tests/e2e/scripts/inject-defect.mjs | grep -q "^1$" && grep -c "&& !isFixtureMutator" scripts/quarantine-append.mjs | grep -q "^1$" && grep -c "git push origin main" .github/workflows/v40-auto-fix.yml | grep -q "^1$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 8 grep commands in Step 1 return exactly 1 (Budget section + 7 rows present)
    - All 5 trust-invariant grep gates in Step 3 hold (SOURCE_TAG, isFixtureMutator filter, git push count, ERROR_CLASSES, ANTHROPIC_API_KEY blanking)
    - The verify command above prints `OK`
    - `git diff .planning/STATE.md` shows EITHER no diff (Budget section already present and byte-identical) OR a small additive diff restoring the Budget section without touching other sections
    - No other file under .planning/, .github/, scripts/, tests/, or src/ is modified in this task
  </acceptance_criteria>
  <done>STATE.md ## Budget section verified present with 7 locked rows; 5 trust-invariant grep gates hold; if drift detected the Budget section is restored and STATE.md staged for the atomic commit</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite Vitest gate + ONE atomic commit (DIAG + TURNS + BUDG)</name>
  <files>(no source files modified — git operation only)</files>
  <read_first>
    - The output of `git status -uno` (verify Plan 01 + Plan 02 files are staged or ready to stage; no surprise unrelated changes)
    - The output of `git diff --stat HEAD` (verify the diff covers exactly the 6 expected files: inject-defect.mjs, e2e-inject-defect.test.js, llm-driver.js, llm-driver.test.js, llm-driver-cost-bound.test.js NEW, ledger-cost-bound.jsonl NEW; plus optionally STATE.md if Task 1 restored the Budget section)
  </read_first>
  <action>
    Step 1 — Pre-commit gate: run the FULL Vitest suite (NOT a single file run). The atomic-commit invariant per CONTEXT.md "If a Vitest fixture fails, fix in the same commit before push" requires every test green.
    Command: `npx vitest run`
    Expected: exit 0. Pre-existing failures noted in STATE.md (`llm-ledger.test.js Test 48` runtime-mutated working copy; `v40-verifier-gate-yaml.test.js V2` Phase 51.1 unfinished test update) should be checked: if they ALREADY existed at HEAD on the base branch, they are acceptable carry-overs; if they are NEW or newly-blocking, investigate before commit. DO NOT silently skip failures. Surface to user with the failing test names.

    Step 2 — Verify file-set in the staged diff matches Phase 61 scope. Run `git status --short` and `git diff --stat HEAD`. The expected modified/added set is EXACTLY:
      - M  .planning/STATE.md (only if Task 1 restored — may be unchanged)
      - M  tests/e2e/lib/llm-driver.js (Plan 02 Task 1)
      - M  tests/e2e/scripts/e2e-inject-defect.test.js (Plan 01 Task 2)
      - M  tests/e2e/scripts/inject-defect.mjs (Plan 01 Task 1)
      - M  tests/unit/llm-driver.test.js (Plan 02 Task 1)
      - A  tests/fixtures/ledger-cost-bound.jsonl (Plan 02 Task 2 — NEW)
      - A  tests/unit/llm-driver-cost-bound.test.js (Plan 02 Task 2 — NEW)
    PLUS optionally the existing dirty file `tests/e2e/.llm-spend-ledger.json` (pre-existing M from before Phase 61 began — DO NOT include in this commit; verify it was M before Phase 61 started by checking `git log -1 --format=%H tests/e2e/.llm-spend-ledger.json` shows HEAD's last touch).

    If unexpected files appear in the staged diff (e.g., a stray edit to llm-ledger.js, fix-prompt-builder.js, auto-fix-promote.mjs, or any FORBIDDEN_PATHS file), STOP. Do not commit. Surface to user.

    Step 3 — Stage only the Phase 61 files. Use explicit `git add` with each path (NOT `git add -A` — prevents accidental inclusion of `.llm-spend-ledger.json` runtime drift or other untracked work). Commands (run in order, each absolute-path-safe):
    ```
    git add tests/e2e/scripts/inject-defect.mjs
    git add tests/e2e/scripts/e2e-inject-defect.test.js
    git add tests/e2e/lib/llm-driver.js
    git add tests/unit/llm-driver.test.js
    git add tests/unit/llm-driver-cost-bound.test.js
    git add tests/fixtures/ledger-cost-bound.jsonl
    # Only if Task 1 restored the Budget section:
    git add .planning/STATE.md
    ```

    Step 4 — Verify staged contents:
    `git diff --cached --stat` shows exactly the 6 (or 7 with STATE.md) files above. `git diff --cached -- tests/e2e/.llm-spend-ledger.json` returns empty (the runtime-mutated ledger is NOT in the commit).

    Step 5 — Create the atomic commit via HEREDOC for proper formatting. Commit subject reflects the bundled scope:
    ```
    git commit -m "$(cat <<'EOF'
    feat(61): atomic carry-over bundle — DIAG + TURNS + BUDG

    Phase 61 ships three coupled capabilities in ONE atomic commit to prevent
    the v4.2 SWEEP-03 partial-state failure shape (Pitfalls 1+2):

    - DIAG-01/02/03: inject-defect.mjs:buildBody extended with per-errorClass
      seeded diagnostic block (GOOGLE_DOM_DRIFT verbatim selectors from
      selection.js/navigation.js; WRONG_CITATION Verifier Disagreement
      template parity with issue-payload-builder.js:208-216). Determinism +
      SOURCE_TAG preservation pinned via Vitest in e2e-inject-defect.test.js.
    - TURNS-01/02/03: llm-driver.js:91-97 subscription-transport argv swaps
      --max-turns 1 → --max-turns 5 --tools Read,Glob,Grep --max-budget-usd
      0.50. SDK transport unchanged (single-turn by API design). Test 23 in
      llm-driver.test.js extended with toEqual + 6 .not.toContain exclusions
      (Edit/Bash/Write/WebFetch/--allowed-tools/--allowedTools). NEW
      llm-driver-cost-bound.test.js pins mean per-call < \$0.30 across 5
      fixture-ledger entries.
    - BUDG-01: STATE.md ## Budget section verified live (\$15 soft / \$30
      hard / <\$5 per-phase / <\$0.30 mean per-call / \$1 per-issue / \$2
      per-PR / \$0.50 per-fingerprint prompt-iter).

    Trust invariants preserved: SOURCE_TAG, isFixtureMutator filter,
    appendLedgerEntry body, assertTripleGate body, v40-auto-fix.yml
    direct-to-main push count, ANTHROPIC_API_KEY blanking.

    Plans 04/05 (UAT-01/02 live evidence) execute post-merge.

    Refs: DIAG-01 DIAG-02 DIAG-03 TURNS-01 TURNS-02 TURNS-03 BUDG-01

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```

    Step 6 — Post-commit verification: `git log -1 --stat` shows the commit landed with the expected file-set. `git diff --stat HEAD~1` lists exactly the 6-7 files. Do NOT push in this task — pushing is an operator decision (LOCAL until v4.3 milestone-close batch per Phase 53/54/55 convention OR push individually per operator's choice).

    Constraints:
    - DO NOT use `--no-verify` (pre-commit hooks run)
    - DO NOT use `git add -A` or `git add .` (explicit paths prevent accidental inclusion of `.llm-spend-ledger.json` runtime drift)
    - DO NOT amend a previous commit (Phase 61 is a new feat commit per atomic-bundle invariant)
    - DO NOT push to origin in this task (push is a separate operator decision)
    - If `npx vitest run` reports ANY failure that was NOT pre-existing on the base commit, halt and surface to user — do not commit
  </action>
  <verify>
    <automated>git log -1 --format='%s' | grep -q '^feat(61): atomic carry-over bundle' && git diff --stat HEAD~1 | grep -cE '(inject-defect\.mjs|e2e-inject-defect\.test\.js|llm-driver\.js|llm-driver\.test\.js|llm-driver-cost-bound\.test\.js|ledger-cost-bound\.jsonl)' | awk '$1>=6 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 (full suite green; pre-existing failures noted in STATE.md are not new regressions)
    - `git log -1 --format='%s'` reports a subject starting with `feat(61): atomic carry-over bundle`
    - `git diff --stat HEAD~1` lists 6 or 7 paths (6 source/test/fixture + optional STATE.md)
    - `git diff --stat HEAD~1 -- tests/e2e/.llm-spend-ledger.json` returns empty (the runtime-mutated ledger is NOT included)
    - `git diff --stat HEAD~1 -- scripts/auto-fix-promote.mjs scripts/auto-fix.mjs tests/e2e/lib/llm-ledger.js tests/e2e/lib/fix-prompt-builder.js` returns empty (Phase 61 does not touch any of these — trust-invariant scope-lock)
    - `git log -1 --format='%b'` contains literal `DIAG-01`, `DIAG-02`, `DIAG-03`, `TURNS-01`, `TURNS-02`, `TURNS-03`, `BUDG-01` (requirement traceability)
    - `git log -1 --format='%b'` contains literal `Co-Authored-By: Claude Opus 4.7 (1M context)` (standard CoauthoredBy footer)
    - Post-commit `npx vitest run` exits 0 again (sanity check after staging operation)
  </acceptance_criteria>
  <done>ONE atomic commit on local main with subject `feat(61): atomic carry-over bundle — DIAG + TURNS + BUDG`; 6-7 files staged; full Vitest suite green; Phase 61 Wave-1 atomic commit invariant satisfied; ready for operator push + Plan 04/05 UAT execution</done>
</task>

</tasks>

<verification>
After both tasks:
1. The full repo has ONE new commit (`git log -1 --format='%s'` matches `feat(61): atomic carry-over bundle …`).
2. The commit's file-set is EXACTLY the Phase 61 surface (6 mandatory files + optional STATE.md): no FORBIDDEN_PATHS or unrelated-scope files touched.
3. `npx vitest run` exits 0 both before and after the commit.
4. All 5 trust-invariant grep gates from Task 1 still hold post-commit.
5. The runtime-mutated `tests/e2e/.llm-spend-ledger.json` remains as a working-copy modification but is NOT in this commit (operator handles separately).
6. Commit body lists DIAG-01/02/03 + TURNS-01/02/03 + BUDG-01 for requirement traceability.
</verification>

<success_criteria>
- BUDG-01: STATE.md ## Budget section verified live with 7 rows ($15 / $30 / <$5 / <$0.30 / $1 / $2 / $0.50)
- Atomic-commit invariant: ONE commit lands Plan 01 + Plan 02 + Plan 03 surfaces — prevents v4.2 SWEEP-03 partial-state failure shape
- Trust invariants survive Phase 61 (assertTripleGate / appendLedgerEntry / SOURCE_TAG / isFixtureMutator filter / direct-to-main push count / ANTHROPIC_API_KEY blanking all preserved)
- Full Vitest suite green
- Ready for operator push to origin/main + Plan 04/05 live UAT execution
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git index → commit | Staging operation crosses this boundary; explicit `git add <path>` (not `-A`/`.`) prevents accidental inclusion of untracked secrets or runtime-mutated state |
| local commit → origin/main | Push is a separate operator decision; this plan does NOT push (consistent with Phase 52-55 LOCAL-commit-then-batch-push convention) |
| Vitest suite → CI gate | Pre-commit `npx vitest run` is the local gate; CI runs the same suite again on push — no asymmetry of trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-03-01 | Tampering | git add scope (accidental file inclusion) | mitigate | Explicit per-path `git add` (no `-A`/`.`); verification via `git diff --cached --stat` before commit; FORBIDDEN_PATHS scope-lock grep against scripts/auto-fix-promote.mjs / scripts/auto-fix.mjs / tests/e2e/lib/llm-ledger.js / tests/e2e/lib/fix-prompt-builder.js (these must NOT appear in the diff) |
| T-61-03-02 | Tampering | runtime-mutated tests/e2e/.llm-spend-ledger.json drift | accept | Pre-existing modification (per git status) — known runtime artifact from prior phase work; explicitly EXCLUDED from this commit via per-path `git add`; surfaced as known-state for the operator |
| T-61-03-03 | Repudiation | commit authorship / signed-off-by | mitigate | Co-Authored-By footer present in commit body per standard GSD convention; standard git signature applies (no --no-gpg-sign) |
| T-61-03-04 | Denial of Service | --no-verify bypass of pre-commit hooks | mitigate | Plan explicitly forbids `--no-verify`; hooks (lint + Vitest) run on commit; failures halt commit creation |
| T-61-03-05 | Information Disclosure | commit body contents | accept | Commit body cites public requirement IDs (DIAG/TURNS/BUDG); no secrets, env vars, or PII referenced |
| T-61-03-06 | Spoofing | partial-state commit (DIAG without TURNS) | mitigate | Plan structure REQUIRES Plan 03 wait-for-both Plan 01 and Plan 02 (`depends_on: [61-01, 61-02]`); the integration gate gathers all 6 files into ONE commit — partial-state Pitfall 1+2 mitigated by structural ordering |
| T-61-03-SC | Tampering | npm install / new dependency | mitigate | No install tasks in this plan; pre-commit `npx vitest run` would fail on any new-dependency introduction |
</threat_model>

<output>
Create `.planning/phases/61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep/61-03-SUMMARY.md` when done. Include in the summary: the commit SHA, the file-set in the commit, the full Vitest exit code, and a status flag indicating Plans 04 + 05 are now executable post-push.
</output>
