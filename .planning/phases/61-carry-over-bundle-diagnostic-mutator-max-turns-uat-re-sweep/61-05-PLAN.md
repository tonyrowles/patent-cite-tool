---
phase: 61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep
plan: 05
type: execute
wave: 2
depends_on: [61-03]
files_modified:
  - .planning/sweep-03-04-pass-evidence.yaml
autonomous: false
requirements: [UAT-02]
tags: [uat-runbook, sweep-04, gh-cli, live-api-spend, evidence-sentinel, mutator-04-invariant, post-merge]

user_setup:
  - service: github
    why: "gh CLI orchestrates issue creation, workflow runs, PR review, and merge for the synthetic WRONG_CITATION live UAT loop"
    env_vars:
      - name: GH_TOKEN
        source: "Either gh auth login (preferred) OR a Personal Access Token with repo + workflow scopes from https://github.com/settings/tokens"
    dashboard_config: []
  - service: anthropic
    why: "Auto-fix loop spends real API budget on the live UAT (subscription credit pool OR ANTHROPIC_API_KEY-backed SDK transport — operator's call per CONTEXT.md Discretion item)"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Only if SDK transport selected; otherwise the subscription transport uses local Claude CLI auth (claude /login). Source: https://console.anthropic.com/settings/keys"
    dashboard_config: []

must_haves:
  truths:
    - "ONE synthetic WRONG_CITATION issue created on origin via inject-defect.mjs --error-class WRONG_CITATION, with a known seed value recorded for evidence reproducibility"
    - "v40-auto-fix.yml workflow fires on the issue label; auto-fix dispatcher invokes Claude via either subscription or SDK transport; produces a unified-diff response"
    - "Auto-fix PR opens, passes verifier-gate (auto-fix:verified label applied), merges to origin/main via standard PR review path (NOT --admin, per BYPASS-03)"
    - "MUTATOR-04 production-path suppression invariant observed: the synthetic issue's Source: fixture-mutator-uat-47b tag causes scripts/quarantine-append.mjs:239 `&& !isFixtureMutator` filter to skip quarantine corpus contamination; verified by inspecting quarantine corpus delta before and after the UAT"
    - "Promote pipeline writes an outcome ledger entry with: errorClass='WRONG_CITATION', outcome='pass', source='auto-fix-promoted', transport='sdk' or 'subscription' (recorded as observed)"
    - ".planning/sweep-03-04-pass-evidence.yaml gains a uat_02 row containing: passed_at_iso, errorClass='WRONG_CITATION', outcome='pass', source='auto-fix-promoted', transport, issueId, prNumber, ledgerEntryIso, mutator04_filter_observed='yes' (confirming the production-path suppression filter fired)"
  artifacts:
    - path: ".planning/sweep-03-04-pass-evidence.yaml"
      provides: "Phase 68 precondition sentinel — gains uat_02 row alongside uat_01 from Plan 04; both rows are required for Phase 68 cleanup.mjs to proceed per Pitfall 8 mitigation"
      contains: "uat_02:"
  key_links:
    - from: ".planning/sweep-03-04-pass-evidence.yaml uat_02 row"
      to: "tests/e2e/.llm-spend-ledger.json outcome entry"
      via: "ledgerEntryIso field cross-reference"
      pattern: "errorClass.*WRONG_CITATION.*outcome.*pass"
    - from: "uat_02 evidence row"
      to: "scripts/quarantine-append.mjs:239 !isFixtureMutator filter"
      via: "mutator04_filter_observed field captures whether quarantine corpus was kept clean of the synthetic"
      pattern: "mutator04_filter_observed.*yes"
---

<objective>
Execute the live UAT-02 runbook (SWEEP-04 / UAT-47-b) after the Phase 61 atomic commit has landed on origin/main. Drive a synthetic WRONG_CITATION issue through the full auto-fix loop — but unlike UAT-01, this run ALSO validates the MUTATOR-04 production-path suppression invariant: `scripts/quarantine-append.mjs:239`'s `&& !isFixtureMutator` filter must prevent the synthetic case from contaminating the production quarantine corpus. Capture PASS evidence into the existing `.planning/sweep-03-04-pass-evidence.yaml` as the `uat_02` row.

Purpose: Closes the v4.2 UAT-47-b STILL-DEFERRED state (see STATE.md Deferred Items 2026-06-09 row). Live evidence that DIAG-02 Verifier Disagreement diagnostic injection causes the auto-fix loop to produce a parseable WRONG_CITATION fix while the MUTATOR-04 filter co-design (SOURCE_TAG `'fixture-mutator-uat-47b'` ⇔ quarantine-append.mjs:239 regex) actively prevents synthetic-to-corpus contamination.

Output: One closed PR on origin/main fixing the synthetic WRONG_CITATION, one ledger outcome entry, one `uat_02:` row appended to the evidence sentinel. The sentinel file then has both `uat_01` (from Plan 04) AND `uat_02` rows — the joint Phase 68 precondition.

This plan is NOT autonomous — same as Plan 04, requires gh CLI auth, live API spend, and human merge approval. Plan 04 and Plan 05 may run in parallel after their respective pre-flight checks pass (independent issue numbers, independent ledger entries, independent uat_NN rows — no shared mutable state).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep/61-CONTEXT.md
@.planning/phases/61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep/61-RESEARCH.md
@.planning/phases/61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep/61-03-SUMMARY.md
@.planning/phases/61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep/61-04-SUMMARY.md

<!-- READ-ONLY references — runbook source files -->
@tests/e2e/scripts/inject-defect.mjs
@scripts/quarantine-append.mjs
@.github/workflows/v40-auto-fix.yml
@scripts/auto-fix-promote.mjs

<interfaces>
<!-- uat_02 schema (extends the file created by Plan 04 — same shape as uat_01 plus mutator04_filter_observed field) -->

.planning/sweep-03-04-pass-evidence.yaml uat_02 block schema:
```yaml
uat_02:
  passed_at_iso: "<ISO 8601 timestamp of evidence capture>"
  errorClass: "WRONG_CITATION"
  outcome: "pass"
  source: "auto-fix-promoted"
  transport: "sdk" | "subscription"  # whichever the auto-fix loop chose at run-time
  issueId: <integer GitHub issue number>
  prNumber: <integer GitHub PR number>
  ledgerEntryIso: "<ISO 8601 timestamp of the matching outcome entry in tests/e2e/.llm-spend-ledger.json>"
  seed: "<the --seed value passed to inject-defect.mjs>"
  mutator04_filter_observed: "yes" | "no"  # MUTATOR-04 production-path suppression
  quarantine_delta_before_count: <integer rows in quarantine-spec.json before UAT>
  quarantine_delta_after_count: <integer rows in quarantine-spec.json after UAT>
  notes: "<free-text operator observations>"
```

MUTATOR-04 invariant check (post-UAT):
The quarantine corpus row count BEFORE the UAT must equal the row count AFTER the UAT. The synthetic case MUST NOT appear in the corpus. If the post-UAT row count is > pre-UAT row count, the MUTATOR-04 filter failed — `mutator04_filter_observed: "no"` and the UAT FAILS even if all other gates passed.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Pre-flight — confirm Phase 61 commit on origin/main + Plan 04 uat_01 row present + capture pre-UAT quarantine row count</name>
  <what-built>UAT-02 runs after Phase 61 atomic commit is on origin/main. Plan 04 may have already captured uat_01 (recommended) but is NOT a hard dependency for Plan 05 — both plans can interleave. The MUTATOR-04 invariant check requires capturing the quarantine corpus row count BEFORE the UAT starts, so it can be compared AFTER.</what-built>
  <how-to-verify>
    1. Verify Phase 61 commit is on origin/main (same as Plan 04 Task 1 step 1):
       ```
       git fetch origin
       git log origin/main -1 --format='%s %h'
       ```
       Expected: subject starts with `feat(61): atomic carry-over bundle — DIAG + TURNS + BUDG`. If not, STOP.

    2. Verify gh CLI auth (same as Plan 04):
       ```
       gh auth status
       ```

    3. Decide spend posture (same as Plan 04 Task 1 step 3 — OPTION A subscription OR OPTION B SDK).

    4. Verify no in-flight auto-fix PR (same check):
       ```
       gh pr list --label 'auto-fix:candidate' --state open
       ```
       Empty list expected. If a uat_01 PR is in-flight from Plan 04, wait for it to merge or close — UAT-02 starts on a clean PR queue.

    5. Capture pre-UAT quarantine row count (MUTATOR-04 baseline):
       ```
       git fetch origin main
       git show origin/main:tests/e2e/test-cases-quarantine.js | grep -c "^  {" 2>/dev/null
       # OR if quarantine is JSON:
       git show origin/main:tests/e2e/test-cases-quarantine.js | wc -l
       ```
       The exact path may be `tests/e2e/test-cases-quarantine.js` (operator: verify; this is the canonical quarantine corpus file referenced in scripts/quarantine-append.mjs). Record `QUARANTINE_PRE_COUNT` as an integer. This is the BASELINE for the MUTATOR-04 invariant check at Task 3.

    6. Verify Plan 04 status (informational — NOT blocking):
       ```
       grep -c "^uat_01:" .planning/sweep-03-04-pass-evidence.yaml 2>/dev/null || echo "0"
       ```
       If `1`: Plan 04 already captured uat_01 — recommended state. If `0`: Plan 04 has not run yet; Plan 05 may proceed independently (both rows go into the same sentinel file, the order does not matter for the Phase 68 precondition check).

    7. Verify NO prior `uat_02:` row exists (to avoid double-capture):
       ```
       grep -c "^uat_02:" .planning/sweep-03-04-pass-evidence.yaml 2>/dev/null || echo "0"
       ```
       Expected: `0`. If `1`, operator decides whether to append uat_02_retry_a or replace.

    Report back: Phase 61 commit confirmed on origin/main? gh auth OK? spend posture? QUARANTINE_PRE_COUNT? uat_01 row present (informational)? uat_02 row absent?
  </how-to-verify>
  <resume-signal>Type "approved" with the QUARANTINE_PRE_COUNT integer value recorded, or describe any pre-flight issue.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Inject synthetic WRONG_CITATION issue + observe full auto-fix loop + capture quarantine corpus delta</name>
  <what-built>The Phase 61 atomic bundle is on origin/main. inject-defect.mjs:buildBody emits the WRONG_CITATION diagnostic block with verbatim Verifier Disagreement headers (DIAG-02 template parity). The MUTATOR-04 SOURCE_TAG + quarantine filter co-design is byte-unchanged from Phase 59. This task executes the live loop AND captures the quarantine corpus delta for the MUTATOR-04 invariant check.</what-built>
  <how-to-verify>
    1. Inject the synthetic WRONG_CITATION issue (operator picks stable seed — recommended: `47b-2026-06-09`):
       ```
       node tests/e2e/scripts/inject-defect.mjs --error-class WRONG_CITATION --seed 47b-2026-06-09
       ```
       Capture stdout: the script prints the new issue URL + number. Record `ISSUE_ID`.

    2. Verify issue body contains the new diagnostic block (DIAG-02 Verifier Disagreement template parity + SOURCE_TAG):
       ```
       gh issue view $ISSUE_ID --json body --jq '.body' | grep "### Verifier Disagreement"
       gh issue view $ISSUE_ID --json body --jq '.body' | grep "Expected citation"
       gh issue view $ISSUE_ID --json body --jq '.body' | grep "Observed citation"
       gh issue view $ISSUE_ID --json body --jq '.body' | grep "Verifier tier:"
       gh issue view $ISSUE_ID --json body --jq '.body' | grep "Rerun verdict:"
       gh issue view $ISSUE_ID --json body --jq '.body' | grep "fixture-mutator-uat-47b"
       ```
       Expected: ALL 6 greps return matches. If ANY is empty, STOP — Plan 01 DIAG-02 extension is not live as expected.

    3. Watch for v40-auto-fix.yml workflow trigger and capture RUN_ID:
       ```
       gh run list --workflow=v40-auto-fix.yml --limit 5
       ```
       Record the new RUN_ID. Watch: `gh run watch $RUN_ID`.

    4. After auto-fix workflow completes, identify the auto-fix PR:
       ```
       gh pr list --label 'auto-fix:candidate' --state open --json number,title,headRefName
       ```
       Record `PR_NUMBER`.

    5. Watch verifier-gate on PR:
       ```
       gh pr checks $PR_NUMBER --watch
       ```
       Expected: `verifier-gate` succeeds; `auto-fix:verified` label applied. If fail, capture logs and decide retry strategy or document FAIL (do NOT proceed to Task 3 until UAT-02 is PASS).

    6. Merge via standard review path (NOT --admin — BYPASS-03):
       ```
       gh pr review $PR_NUMBER --approve
       gh pr merge $PR_NUMBER --squash --delete-branch
       ```

    7. Watch v40-auto-promote.yml run and capture the ledger entry:
       ```
       gh run list --workflow=v40-auto-promote.yml --limit 3
       gh run watch <new-promote-run-id>
       git fetch origin main
       git show origin/main:tests/e2e/.llm-spend-ledger.json | jq '.iterations[-5:]'
       ```
       Find the entry matching: `source: 'auto-fix-promoted'`, `outcome: 'pass'`, `errorClass: 'WRONG_CITATION'`. Record `LEDGER_ENTRY_ISO` AND `TRANSPORT`.

    8. CAPTURE POST-UAT quarantine row count (MUTATOR-04 invariant check — the load-bearing UAT-02 invariant):
       ```
       git show origin/main:tests/e2e/test-cases-quarantine.js | grep -c "^  {" 2>/dev/null
       # OR matching how Task 1 step 5 counted:
       git show origin/main:tests/e2e/test-cases-quarantine.js | wc -l
       ```
       Record `QUARANTINE_POST_COUNT`.

    9. Compute MUTATOR-04 invariant result:
       - If `QUARANTINE_POST_COUNT == QUARANTINE_PRE_COUNT`: MUTATOR-04 filter HELD (the synthetic was suppressed from corpus contamination) — `mutator04_filter_observed: "yes"` — UAT-02 fully PASSES.
       - If `QUARANTINE_POST_COUNT > QUARANTINE_PRE_COUNT`: the synthetic was added to corpus — MUTATOR-04 filter FAILED — `mutator04_filter_observed: "no"` — UAT-02 FAILS even if all other gates passed. STOP and surface to user for investigation (likely a scripts/quarantine-append.mjs:239 `&& !isFixtureMutator` filter regression OR SOURCE_TAG drift).
       - If `QUARANTINE_POST_COUNT < QUARANTINE_PRE_COUNT`: unexpected (the auto-fix removed a quarantine row) — STOP and surface.

    Report back: ISSUE_ID, RUN_ID, PR_NUMBER, LEDGER_ENTRY_ISO, TRANSPORT, seed, QUARANTINE_PRE_COUNT, QUARANTINE_POST_COUNT, mutator04_filter_observed verdict, plus observations.
  </how-to-verify>
  <resume-signal>Type "uat-02-pass" once you have all captured values AND mutator04_filter_observed == "yes". Or type "uat-02-fail" with a description of which gate failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Append uat_02 row to .planning/sweep-03-04-pass-evidence.yaml + commit</name>
  <files>.planning/sweep-03-04-pass-evidence.yaml</files>
  <read_first>
    - .planning/sweep-03-04-pass-evidence.yaml (verify it exists with schema_version header — Plan 04 Task 3 created it if uat_01 ran first; if Plan 05 ran first this task creates it; both code paths are handled below)
    - The captured values reported by the operator at Task 2 close (ISSUE_ID, PR_NUMBER, LEDGER_ENTRY_ISO, TRANSPORT, seed, QUARANTINE_PRE_COUNT, QUARANTINE_POST_COUNT, mutator04_filter_observed)
  </read_first>
  <action>
    Step 1 — If `.planning/sweep-03-04-pass-evidence.yaml` does NOT exist (Plan 05 ran before Plan 04 — valid interleave), CREATE it with the schema header:
    ```
    schema_version: 1
    created_at_iso: "<current ISO 8601 timestamp at file creation>"
    ```

    Step 2 — Append a `uat_02:` block at the END of the file. Substitute ALL placeholders with the values captured at Task 2 close. The exact YAML to append:
    ```
    
    uat_02:
      passed_at_iso: "<ISO 8601 of evidence capture — i.e., now>"
      errorClass: "WRONG_CITATION"
      outcome: "pass"
      source: "auto-fix-promoted"
      transport: "<sdk or subscription as observed>"
      issueId: <ISSUE_ID integer>
      prNumber: <PR_NUMBER integer>
      ledgerEntryIso: "<LEDGER_ENTRY_ISO captured at Task 2 step 7>"
      seed: "<seed used at Task 2 step 1>"
      mutator04_filter_observed: "yes"
      quarantine_delta_before_count: <QUARANTINE_PRE_COUNT from Task 1>
      quarantine_delta_after_count: <QUARANTINE_POST_COUNT from Task 2 step 8>
      notes: "<free-text observations: e.g., 'MUTATOR-04 filter HELD; quarantine corpus row count identical pre/post; verifier-gate green first attempt'>"
    ```

    Note: this task assumes Task 2 returned `mutator04_filter_observed: "yes"` (the load-bearing UAT-02 condition). If Task 2 returned "no", this task does NOT run — the operator addresses the MUTATOR-04 filter failure before re-attempting UAT-02 (likely a scripts/quarantine-append.mjs:239 filter regression or a SOURCE_TAG drift — neither of which is Plan 05 scope to fix; this would be a Phase 61 gap-closure).

    Step 3 — Verify YAML well-formedness AND that uat_02 row count is 1 AND uat_01 row count is 0-or-1 (depending on Plan 04 interleave):
    ```
    node -e "const yaml = require('node:fs').readFileSync('.planning/sweep-03-04-pass-evidence.yaml','utf8'); const uat01 = (yaml.match(/^uat_01:/gm) || []).length; const uat02 = (yaml.match(/^uat_02:/gm) || []).length; console.log('uat_01 rows:', uat01, '; uat_02 rows:', uat02);"
    ```
    Expected: uat_02 == 1; uat_01 ∈ {0, 1}.

    Step 4 — Stage and commit:
    ```
    git add .planning/sweep-03-04-pass-evidence.yaml
    git commit -m "$(cat <<'EOF'
    docs(61): capture UAT-02 PASS evidence (SWEEP-04 WRONG_CITATION + MUTATOR-04 invariant)

    Live UAT-02 (SWEEP-04 / UAT-47-b) PASS on origin/main — closes v4.2
    architectural deferral for fixture-mutator full loop. Synthetic
    WRONG_CITATION issue flowed end-to-end through inject-defect (Verifier
    Disagreement template parity) → v40-auto-fix → verifier-gate → merge →
    promote. MUTATOR-04 production-path suppression invariant observed:
    quarantine corpus row count identical pre/post-UAT (filter at
    scripts/quarantine-append.mjs:239 fired on Source: fixture-mutator-uat-47b
    tag as designed).

    Evidence sentinel .planning/sweep-03-04-pass-evidence.yaml gains uat_02
    row. Combined with uat_01 (Plan 04), Phase 68 precondition fully
    satisfied — uat_cleanup.mjs may proceed when Phase 68 ships.

    Refs: UAT-02

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```

    Constraints:
    - Use real captured values — DO NOT fabricate. If any value is missing, re-run the relevant Task 2 step.
    - `mutator04_filter_observed` MUST be `"yes"` for this row to be appended (the "no" path is a fail-state addressed outside this plan's scope).
    - Schema keys must match `<interfaces>` byte-for-byte (Phase 68 cleanup parses this file by key).
    - DO NOT push in this task — push timing is operator's call (likely batched with Plan 04 commit).
  </action>
  <verify>
    <automated>test -f .planning/sweep-03-04-pass-evidence.yaml && grep -c "^uat_02:" .planning/sweep-03-04-pass-evidence.yaml | grep -q "^1$" && grep -A 12 "^uat_02:" .planning/sweep-03-04-pass-evidence.yaml | grep -qE "errorClass:.*WRONG_CITATION" && grep -A 12 "^uat_02:" .planning/sweep-03-04-pass-evidence.yaml | grep -qE "outcome:.*pass" && grep -A 12 "^uat_02:" .planning/sweep-03-04-pass-evidence.yaml | grep -qE "mutator04_filter_observed:.*yes" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/sweep-03-04-pass-evidence.yaml` exists with schema_version: 1 header
    - `grep -c "^uat_02:" .planning/sweep-03-04-pass-evidence.yaml` returns exactly 1
    - The uat_02 block contains non-empty values for all 12 required keys (passed_at_iso, errorClass, outcome, source, transport, issueId, prNumber, ledgerEntryIso, seed, mutator04_filter_observed, quarantine_delta_before_count, quarantine_delta_after_count)
    - `errorClass: "WRONG_CITATION"` literal present
    - `outcome: "pass"` literal present
    - `source: "auto-fix-promoted"` literal present
    - `mutator04_filter_observed: "yes"` literal present (UAT-02 fails if this is "no")
    - quarantine_delta_before_count == quarantine_delta_after_count (verified numerically — the MUTATOR-04 invariant holds)
    - transport is exactly one of `"sdk"` or `"subscription"`
    - issueId, prNumber, quarantine counts are positive integers
    - `git log -1 --format='%s'` matches `docs(61): capture UAT-02 PASS evidence (SWEEP-04 WRONG_CITATION + MUTATOR-04 invariant)`
    - The commit touches ONLY `.planning/sweep-03-04-pass-evidence.yaml`
  </acceptance_criteria>
  <done>uat_02 PASS row appended with all 12 keys including the MUTATOR-04 invariant observation; committed locally; Phase 68 precondition sentinel now has both uat_01 (Plan 04) and uat_02 (Plan 05) rows — Phase 68 cleanup.mjs unblocked when v4.3 reaches the cleanup wave</done>
</task>

</tasks>

<verification>
After all 3 tasks:
1. One GitHub issue (`triage` + `WRONG_CITATION` labels) closed via auto-fix PR merge to origin/main.
2. One auto-fix PR closed with `auto-fix:verified` label (NOT `--admin` bypassed).
3. One ledger entry in `tests/e2e/.llm-spend-ledger.json` with `errorClass: 'WRONG_CITATION'` + `outcome: 'pass'` + `source: 'auto-fix-promoted'` + `transport: 'sdk'|'subscription'`.
4. MUTATOR-04 invariant verified: quarantine corpus row count pre-UAT equals post-UAT (the synthetic case was filtered out of the production quarantine corpus via SOURCE_TAG ⇔ `&& !isFixtureMutator` co-design).
5. `.planning/sweep-03-04-pass-evidence.yaml` has both uat_01 (from Plan 04) AND uat_02 (from Plan 05) rows.
6. A local commit `docs(61): capture UAT-02 PASS evidence` exists with the sentinel file change only.
7. Phase 68 precondition fully satisfied — Phase 68's `scripts/uat-cleanup.mjs` will pass the sentinel check when v4.3 reaches the cleanup wave.
</verification>

<success_criteria>
- UAT-02 (SWEEP-04 / UAT-47-b): PROVEN on origin/main with full ledger forensics
- MUTATOR-04 production-path suppression invariant observed live (`scripts/quarantine-append.mjs:239` filter held)
- v4.2 architectural deferral closed (UAT-47-b STILL-DEFERRED → PASS captured)
- Phase 68 precondition sentinel COMPLETE (both uat_01 + uat_02 rows present)
- BYPASS-03 discipline observed (no `--admin` bypass)
- Live spend recorded in committed ledger (probed against BUDG-01 cap)
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → gh CLI → GitHub API | Same as Plan 04 — auth via gh CLI; no token leakage in evidence sentinel |
| auto-fix workflow → Anthropic API | Same as Plan 04 — secrets in repo, never written to sentinel |
| origin/main quarantine corpus → MUTATOR-04 filter | scripts/quarantine-append.mjs:239 `&& !isFixtureMutator` is the load-bearing production-path suppression boundary; pre-UAT count vs post-UAT count is the live probe |
| origin/main ledger → uat_02 evidence row | Cross-referenced via ledgerEntryIso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-05-01 | Tampering | --admin bypass of verifier-gate | mitigate | Task 2 step 6 explicitly forbids `--admin` per BYPASS-03; Phase 62 audit-bypass-merges.mjs would surface any bypass post-hoc |
| T-61-05-02 | Tampering | quarantine corpus contamination by synthetic | mitigate | MUTATOR-04 invariant check at Task 2 step 9 compares pre-UAT vs post-UAT row count; mismatch halts the runbook before sentinel write; SOURCE_TAG literal `'fixture-mutator-uat-47b'` co-designed with scripts/quarantine-append.mjs:239 `&& !isFixtureMutator` regex (verified byte-unchanged by Plan 03 Task 1 trust-invariant grep gates) |
| T-61-05-03 | Information Disclosure | API key leakage into evidence sentinel | mitigate | Sentinel records transport label only; no key fields; acceptance criteria enforce schema purity |
| T-61-05-04 | Denial of Service | runaway API spend | mitigate | TURNS-03 cost-bound + --max-budget-usd 0.50 per-call + ISSUE_HARD_CAP_USD $1 per-issue caps; expected spend ~$0.05-0.50 |
| T-61-05-05 | Spoofing | synthetic vs real production WRONG_CITATION issue | mitigate | SOURCE_TAG + title prefix + label combination identifies synthetic for Phase 68 triple-tagged cleanup; ledger entry's source: 'auto-fix-promoted' is the legitimate-fix tag — no spoofing risk on the ledger side |
| T-61-05-06 | Repudiation | who fired the MUTATOR-04 filter | accept | Filter is automatic (no human action); the post-UAT row-count equality IS the evidence — no log scrutiny required for repudiation |
| T-61-05-SC | Tampering | npm install / new dependency | mitigate | No install tasks; gh CLI pre-installed per Plan 04 pre-flight |
</threat_model>

<output>
Create `.planning/phases/61-carry-over-bundle-diagnostic-mutator-max-turns-uat-re-sweep/61-05-SUMMARY.md` when done. Include in the summary: captured ISSUE_ID, PR_NUMBER, LEDGER_ENTRY_ISO, TRANSPORT, seed, QUARANTINE_PRE_COUNT, QUARANTINE_POST_COUNT, mutator04_filter_observed verdict, observed cost (operator best-estimate), and a status flag indicating Phase 61 is now ready for closure (assuming Plan 04 uat_01 also captured PASS). Phase 62 (next phase) can begin once both plans return PASS evidence.
</output>
