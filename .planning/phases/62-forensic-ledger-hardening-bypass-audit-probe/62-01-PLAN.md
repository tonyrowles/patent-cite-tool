---
phase: 62-forensic-ledger-hardening-bypass-audit-probe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/lib/safe-append-ledger.js
  - tests/unit/safe-append-ledger.test.js
  - scripts/auto-fix-promote.mjs
  - scripts/e2e-explore.mjs
autonomous: true
requirements: [LEDX-01, LEDX-02, LEDX-03, LEDX-04]
tags: [ledger, refactor, shared-helper, forensic-audit]

must_haves:
  truths:
    - "Shared `safeAppendLedger(ledgerPath, entry, opts)` helper exists in `tests/e2e/lib/safe-append-ledger.js`"
    - "All 4 currently-unguarded ledger-write sites (`auto-fix-promote.mjs:521,544`; `e2e-explore.mjs:262,313`) route through the shared helper"
    - "`tests/e2e/lib/llm-ledger.js:appendLedgerEntry` body BYTE-UNCHANGED (sha256 pin asserts)"
    - "`scripts/auto-fix.mjs:safeAppendLedger` (lines 143-181) BYTE-UNCHANGED — preserves Phase 60.1 L1+L2 source-grep pins at `tests/unit/auto-fix.test.js:1370-1394`"
    - "Phase 60.1 subscription-transport whitelist preserved (entries with `transport:'subscription'` pass through outside CI)"
    - "Non-canonical transport values (e.g., `'http'`) rejected at the shared helper write boundary"
    - "33 pre-existing Vitest tests in `tests/unit/llm-ledger.test.js` stay green; 50+ tests in `tests/unit/auto-fix.test.js` stay green"
  artifacts:
    - path: "tests/e2e/lib/safe-append-ledger.js"
      provides: "Shared safeAppendLedger helper + VALID_TRANSPORTS export"
      contains: "export function safeAppendLedger"
    - path: "tests/unit/safe-append-ledger.test.js"
      provides: "7 unit tests covering CI gate / subscription whitelist / CI=true / override / non-canonical transport / defaults / sha256 pin"
      contains: "T_LEDX_CI_GATE"
    - path: "scripts/auto-fix-promote.mjs"
      provides: "Rewired :521 (fail outcome) + :544 (pass outcome) using safeAppendLedger"
      contains: "safeAppendLedger(LEDGER_PATH"
    - path: "scripts/e2e-explore.mjs"
      provides: "Rewired :262 (iter) + :313 (retry) using safeAppendLedger with defaults injection"
      contains: "safeAppendLedger(LEDGER_PATH"
  key_links:
    - from: "scripts/auto-fix-promote.mjs"
      to: "tests/e2e/lib/safe-append-ledger.js"
      via: "import { safeAppendLedger }"
      pattern: "import.*safeAppendLedger.*safe-append-ledger"
    - from: "scripts/e2e-explore.mjs"
      to: "tests/e2e/lib/safe-append-ledger.js"
      via: "import { safeAppendLedger }"
      pattern: "import.*safeAppendLedger.*safe-append-ledger"
    - from: "tests/e2e/lib/safe-append-ledger.js"
      to: "tests/e2e/lib/llm-ledger.js"
      via: "import { appendLedgerEntry }"
      pattern: "import.*appendLedgerEntry.*llm-ledger"
---

<objective>
Extract the proven `safeAppendLedger` CI/override/subscription-whitelist guard pattern from `scripts/auto-fix.mjs:143-181` into a new shared helper `tests/e2e/lib/safe-append-ledger.js`, and wire all 4 currently-unguarded `appendLedgerEntry(LEDGER_PATH, ...)` call sites through it.

Purpose: Close the forensic-leak vector (4 sites that bypass the Phase 56 leak-guard) without touching the `appendLedgerEntry` body (Phase 56 additive-only invariant — would break 33 pre-existing Vitest ledger tests per PITFALLS Pitfall 3) and without touching `scripts/auto-fix.mjs:143-181` (preserves Phase 60.1 L1+L2 source-grep pins at `tests/unit/auto-fix.test.js:1370-1394` which read the auto-fix.mjs source for specific literal strings).

Output: New shared helper + new test file (7 tests) + 4 mechanical site rewires + 1 atomic commit.

CRITICAL DESIGN NOTE — RESEARCH-DRIVEN CORRECTION: CONTEXT.md decisions section (line 33) suggests turning the local `auto-fix.mjs` `safeAppendLedger` into a thin pass-through. RESEARCH.md (Open Question 1 + Pitfall 62-C + lines 824-828) explicitly recommends NOT migrating it. This plan follows RESEARCH.md: `scripts/auto-fix.mjs:143-181` is left UNCHANGED. The shared helper exists as a NEW file consumed by `auto-fix-promote.mjs` + `e2e-explore.mjs` ONLY. LEDX-01 is satisfied because the shared helper file exists and IS consumed by the 4-site fix list. The "two sources of truth" risk (Pitfall 62-C) is mitigated by leaving the proven local wrapper alone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/62-forensic-ledger-hardening-bypass-audit-probe/62-CONTEXT.md
@.planning/phases/62-forensic-ledger-hardening-bypass-audit-probe/62-RESEARCH.md

<interfaces>
Key contracts the executor needs. All verified at HEAD.

From scripts/auto-fix.mjs:143-181 (verbatim port source — DO NOT EDIT THIS FILE):
```javascript
// Lines 155-180 are the verbatim-port block for the shared helper:
const inCi =
  process.env.CI === 'true' || process.env.GITHUB_ACTIONS === 'true';
const hasOverride =
  typeof process.env.E2E_LEDGER_PATH_OVERRIDE === 'string' &&
  process.env.E2E_LEDGER_PATH_OVERRIDE.trim().length > 0;
const isSubscriptionLocal = entry && entry.transport === 'subscription';
if (!inCi && !hasOverride && !isSubscriptionLocal) {
  throw new Error(
    `safeAppendLedger refused: cannot write to ${LEDGER_PATH} ...`
  );
}
appendLedgerEntry(LEDGER_PATH, entry);
```

From tests/e2e/lib/llm-ledger.js (BYTE-UNCHANGED invariant target):
```javascript
export const LEDGER_PATH = /* IIFE resolves to tests/e2e/.llm-spend-ledger.json */;
export function appendLedgerEntry(ledgerPath, entry) { /* lines 686-738 — DO NOT EDIT */ }
export function readLedger(ledgerPath) { /* used by audit script in Plan 02 */ }
```

From scripts/auto-fix-promote.mjs:521 (fail outcome — already has source+transport inline):
```javascript
appendLedgerEntry(LEDGER_PATH, {
  iso: new Date().toISOString(),
  model: args.model || 'claude-sonnet-4-6',
  cost_usd: 0, tokens_in: 0, tokens_out: 0,
  phase: args.phase || '58-promote',
  transport: 'subscription',
  issueId: `issue-${resolvedSourceIssue}`,
  prNumber: args.pr, fingerprint: args.fingerprint,
  errorClass: args.errorClass,
  source: 'auto-fix-failed', outcome: 'fail',
  reason: (`runPromote exitCode=${result.exitCode}`).slice(0, 200),
});
```

From scripts/auto-fix-promote.mjs:544 (pass outcome — same shape; source='auto-fix-promoted', outcome='pass').

From scripts/e2e-explore.mjs:262 (iter — MISSING source+transport; will rely on defaults):
```javascript
appendLedgerEntry(LEDGER_PATH, {
  iso, model: modelId, cost_usd: costUsd,
  tokens_in: parsed.rawJson?.usage?.input_tokens ?? 0,
  tokens_out: parsed.rawJson?.usage?.output_tokens ?? 0,
  iteration_n: iterationN, run_id: runId, phase: phase,
});
```

From scripts/e2e-explore.mjs:313 (retry — same shape as :262).

From tests/unit/auto-fix.test.js:1370-1394 (Phase 60.1 L1+L2 pins — these SOURCE-GREP scripts/auto-fix.mjs and MUST continue to pass; leaving auto-fix.mjs unchanged guarantees this):
```javascript
// L1 (line ~1380-1381) asserts:
expect(src).toContain("entry.transport === 'subscription'");
expect(src).toMatch(/isSubscriptionLocal/);
// L2 (line ~1392-1393) asserts:
expect(src).toContain('safeAppendLedger refused: cannot write');
expect(src).toContain('Phase 48 leak vector + Phase 56 LEDGER-02');
```

From scripts/auto-fix.mjs:205 (VALID_TRANSPORTS Set — pattern to mirror in shared helper):
```javascript
const VALID_TRANSPORTS = new Set(['sdk', 'subscription']);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared safe-append-ledger helper + 7-test Vitest file</name>
  <files>tests/e2e/lib/safe-append-ledger.js, tests/unit/safe-append-ledger.test.js</files>
  <read_first>
    - /home/fatduck/patent-cite-tool/scripts/auto-fix.mjs (lines 143-205 — verbatim port source + VALID_TRANSPORTS pattern)
    - /home/fatduck/patent-cite-tool/tests/e2e/lib/llm-ledger.js (lines 1-100 + 686-738 — LEDGER_PATH IIFE, appendLedgerEntry body, readLedger)
    - /home/fatduck/patent-cite-tool/tests/unit/llm-ledger.test.js (lines 1-200 — beforeEach/afterEach env-reset pattern; tmp-ledger fixture pattern)
    - /home/fatduck/patent-cite-tool/.planning/phases/62-forensic-ledger-hardening-bypass-audit-probe/62-RESEARCH.md (Helper Extraction Surface section + Test Strategy section lines 359-446)
  </read_first>
  <behavior>
    Test 1 — `T_LEDX_CI_GATE`: with `CI` unset, no `E2E_LEDGER_PATH_OVERRIDE`, entry `{transport:'sdk', source:'X', ...}` → `safeAppendLedger(tmpPath, entry)` THROWS matching `/safeAppendLedger refused: cannot write/`
    Test 2 — `T_PHASE60_1_HOTFIX_PRESERVED_SHARED` (LEDX-04 invariant): with `CI` unset, no override, entry `{transport:'subscription', source:'X', ...}` → call does NOT throw; file written; readLedger shows 1 iteration entry
    Test 3 — `T_LEDX_CI_PASS`: `process.env.CI = 'true'` + entry `{transport:'sdk', ...}` → does NOT throw; file written
    Test 4 — `T_LEDX_OVERRIDE_PASS`: `process.env.E2E_LEDGER_PATH_OVERRIDE = tmpPath` (truthy + .trim().length > 0) + entry `{transport:'sdk', ...}` → does NOT throw; file written
    Test 5 — `T_LEDX_INVALID_TRANSPORT`: `CI='true'` + entry `{transport:'http', ...}` → THROWS matching `/transport 'http' is not canonical/`
    Test 6 — `T_LEDX_DEFAULTS`: `CI='true'` + entry MISSING `source` + `transport`, called with `opts={defaults:{source:'e2e-explore', transport:'subscription'}}` → does NOT throw; readLedger shows entry has `source==='e2e-explore'` + `transport==='subscription'` applied via defaults
    Test 7 — `T_LEDX_APPEND_BODY_PINNED` (LEDX-03 invariant): extract `appendLedgerEntry` function body bytes from `tests/e2e/lib/llm-ledger.js` via regex `/export function appendLedgerEntry[\s\S]*?^}/m`, compute sha256 via `node:crypto`, assert it equals the literal baseline hash computed at task-execution time and pinned into the test
  </behavior>
  <action>
    Create `tests/e2e/lib/safe-append-ledger.js` (per LEDX-01) as a new ESM module that:
    - Imports `{ appendLedgerEntry }` from `'./llm-ledger.js'` (NOTE: does NOT re-export `LEDGER_PATH` — callers continue to import LEDGER_PATH directly per RESEARCH.md Anti-Pattern at line 297).
    - Exports `VALID_TRANSPORTS = new Set(['sdk', 'subscription'])` mirroring `scripts/auto-fix.mjs:205`.
    - Exports `safeAppendLedger(ledgerPath, entry, opts = {})` with the signature locked in CONTEXT.md decisions (LEDX-01). Body MUST:
      1. Build `merged = { ...entry, source: entry.source ?? opts.defaults?.source, transport: entry.transport ?? opts.defaults?.transport }`.
      2. If `merged.transport !== undefined && !VALID_TRANSPORTS.has(merged.transport)` → throw an Error with message containing the exact literal `transport '${merged.transport}' is not canonical` so Test 5's regex matches.
      3. Apply the VERBATIM CI/override/subscription-whitelist port from `scripts/auto-fix.mjs:155-180` (RESEARCH.md "Verbatim-Port Source Block" section). Required literal substrings in the throw message: `safeAppendLedger refused: cannot write to ${ledgerPath}`, `Phase 48 leak vector + Phase 56 LEDGER-02 hardening + Phase 62 LEDX-01`. Preserve the existing comment block from auto-fix.mjs:163-169 (Phase 60.1 hotfix rationale) verbatim.
      4. On gate pass, call `appendLedgerEntry(ledgerPath, merged)`.
    - Use the `isSubscriptionLocal` variable name to keep the codebase searchable (and parallel with auto-fix.mjs).
    - Do NOT add any other exports. Do NOT add a default export.
    - The accept `opts.allowOverride` reserved-flag (default `false`) is declared in the destructure but does not affect behavior in v1 — reserved for future use per CONTEXT.md decision (LEDX-01).

    Create `tests/unit/safe-append-ledger.test.js` (NEW FILE — per Pitfall 62-A, do NOT add to llm-ledger.test.js):
    - Use Vitest `describe`/`it`/`beforeEach`/`afterEach` mirroring `tests/unit/llm-ledger.test.js` env-reset pattern (referenced at RESEARCH.md line 446).
    - Each test creates a fresh tmp ledger path under `os.tmpdir()` via `fs.mkdtempSync` to isolate state.
    - `beforeEach`: snapshot then DELETE `process.env.CI`, `process.env.GITHUB_ACTIONS`, `process.env.E2E_LEDGER_PATH_OVERRIDE`.
    - `afterEach`: restore snapshotted env values; `fs.rmSync(tmpDir, { recursive: true, force: true })`.
    - Implement Tests 1-7 from `<behavior>` block above with the named `it()` titles (`T_LEDX_CI_GATE`, `T_PHASE60_1_HOTFIX_PRESERVED_SHARED`, `T_LEDX_CI_PASS`, `T_LEDX_OVERRIDE_PASS`, `T_LEDX_INVALID_TRANSPORT`, `T_LEDX_DEFAULTS`, `T_LEDX_APPEND_BODY_PINNED`).
    - For Test 7 (sha256 pin), the executor COMPUTES the baseline hash via the same regex extraction documented in RESEARCH.md lines 786-794 and pins the resulting hex string as a literal in the test. Use `crypto.createHash('sha256').update(extractedBody).digest('hex')`. Comment above the literal: `// LEDX-03 baseline computed Phase 62 plan-01 — fails on any edit to appendLedgerEntry body`.
    - Use `readLedger` from `tests/e2e/lib/llm-ledger.js` to verify written entries; assert `ledger.months[currentMonth()].iterations.length` increments correctly. `currentMonth()` can mirror the helper in `tests/unit/llm-ledger.test.js` or compute inline via `new Date().toISOString().slice(0,7)`.

    NO fenced full-implementation code is delivered here — the executor writes the source. Do NOT edit `scripts/auto-fix.mjs`, `tests/e2e/lib/llm-ledger.js`, or `tests/unit/llm-ledger.test.js`.
  </action>
  <verify>
    <automated>npm test -- tests/unit/safe-append-ledger.test.js --run</automated>
    Then:
    `npm test -- tests/unit/llm-ledger.test.js --run` (33 pre-existing tests still green; no edits to that file)
    `npm test -- tests/unit/auto-fix.test.js --run -t 'Phase 60.1'` (L1+L2 source-grep pins still green; auto-fix.mjs untouched)
    `git diff HEAD -- scripts/auto-fix.mjs tests/e2e/lib/llm-ledger.js tests/unit/llm-ledger.test.js | wc -l` → must equal `0` (RESEARCH-driven non-mutation)
    `grep -c "export function safeAppendLedger" tests/e2e/lib/safe-append-ledger.js` → `1`
    `grep -c "VALID_TRANSPORTS" tests/e2e/lib/safe-append-ledger.js` → `>= 2` (declaration + usage)
    `grep -c "safeAppendLedger refused: cannot write" tests/e2e/lib/safe-append-ledger.js` → `1`
    `grep -v '^[[:space:]]*//' tests/e2e/lib/safe-append-ledger.js | grep -c "isSubscriptionLocal"` → `>= 1` (variable reference outside comments)
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/lib/safe-append-ledger.js` exists; exports `safeAppendLedger` named function + `VALID_TRANSPORTS` Set.
    - File `tests/unit/safe-append-ledger.test.js` exists with exactly 7 `it()` blocks matching the named test IDs.
    - All 7 new tests PASS via `npm test -- tests/unit/safe-append-ledger.test.js --run`.
    - All 33 pre-existing tests in `tests/unit/llm-ledger.test.js` PASS (no file edit there).
    - Phase 60.1 L1+L2 pins in `tests/unit/auto-fix.test.js` (lines 1370-1394) PASS without edits to `scripts/auto-fix.mjs`.
    - `scripts/auto-fix.mjs`, `tests/e2e/lib/llm-ledger.js`, `tests/unit/llm-ledger.test.js` are BYTE-UNCHANGED (`git diff` is empty for these paths).
    - Throw message for non-canonical transport contains literal substring `is not canonical`.
    - Throw message for CI-gate refusal contains literal substring `safeAppendLedger refused: cannot write to` and references the LEDGER_PATH-style variable interpolation.
  </acceptance_criteria>
  <done>Shared helper + 7-test file ship together; all 7 new tests + all pre-existing LEDX-related tests green; no edits leaked into auto-fix.mjs or llm-ledger.js.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire 4 unguarded sites through safeAppendLedger (LEDX-02)</name>
  <files>scripts/auto-fix-promote.mjs, scripts/e2e-explore.mjs</files>
  <read_first>
    - /home/fatduck/patent-cite-tool/scripts/auto-fix-promote.mjs (lines 1-100 for existing imports + lines 500-560 for both wire sites at 521 and 544)
    - /home/fatduck/patent-cite-tool/scripts/e2e-explore.mjs (lines 1-60 for existing imports + lines 240-320 for both wire sites at 262 and 313)
    - /home/fatduck/patent-cite-tool/.planning/phases/62-forensic-ledger-hardening-bypass-audit-probe/62-RESEARCH.md (Site Wiring section lines 448-522 — exact before/after for each site)
    - /home/fatduck/patent-cite-tool/tests/e2e/lib/safe-append-ledger.js (just created in Task 1 — confirm import path resolution from `scripts/`)
  </read_first>
  <action>
    Rewire 4 ledger-write sites per LEDX-02. Mechanical replacements only; entry shape preserved at each site.

    Site A — `scripts/auto-fix-promote.mjs:521` (fail outcome):
    - Add (or merge into) existing import block at top of file: `import { safeAppendLedger } from '../tests/e2e/lib/safe-append-ledger.js';`
    - At line 521, replace `appendLedgerEntry(LEDGER_PATH, { ... })` with `safeAppendLedger(LEDGER_PATH, { ... })`. The entry object literal is UNCHANGED — it already contains `source: 'auto-fix-failed', transport: 'subscription'` inline; no defaults needed.

    Site B — `scripts/auto-fix-promote.mjs:544` (pass outcome):
    - Same mechanical rename: `appendLedgerEntry(...)` → `safeAppendLedger(...)` at the call site. Entry already self-tags `source: 'auto-fix-promoted', transport: 'subscription'`.
    - The existing `appendLedgerEntry` import may STAY (it might be used elsewhere in promote.mjs — DO NOT remove the import unless `grep -c "appendLedgerEntry(" scripts/auto-fix-promote.mjs` returns 0 AFTER the rewires; if it still has hits, leave the import).

    Site C — `scripts/e2e-explore.mjs:262` (iter ledger):
    - Add (or merge into) existing import block at top of file: `import { safeAppendLedger } from '../tests/e2e/lib/safe-append-ledger.js';`
    - Replace `appendLedgerEntry(LEDGER_PATH, { iso, model: modelId, cost_usd: costUsd, tokens_in: ..., tokens_out: ..., iteration_n: iterationN, run_id: runId, phase: phase, });` with `safeAppendLedger(LEDGER_PATH, { ...same entry... }, { defaults: { source: 'e2e-explore', transport: 'subscription' } });`.
    - CRITICAL: do NOT mutate the entry literal to inline source/transport. Use the `opts.defaults` mechanism per RESEARCH.md line 518 ("preserves the existing entry-shape for any test that source-greps `e2e-explore.mjs` for the entry object literal").

    Site D — `scripts/e2e-explore.mjs:313` (retry ledger):
    - Same pattern as Site C: same `opts.defaults` injection (`{ defaults: { source: 'e2e-explore', transport: 'subscription' } }`); entry literal UNCHANGED.

    Trust-invariant non-mutations (verify after task):
    - `tests/e2e/lib/llm-ledger.js:appendLedgerEntry` BYTE-UNCHANGED.
    - `scripts/auto-fix.mjs` BYTE-UNCHANGED (per RESEARCH Open Question 1 / Pitfall 62-C / line 824-828 — do NOT migrate the local wrapper).
    - 33 Vitest tests in `tests/unit/llm-ledger.test.js` stay green.
    - 50+ Vitest tests in `tests/unit/auto-fix.test.js` stay green (Phase 60.1 L1+L2 source-grep pins at lines 1370-1394 preserved).

    Add a Vitest assertion to `tests/unit/safe-append-ledger.test.js` (extends Task 1's test file by 1 it() block — same commit acceptable since this verifies wire-up):
    - `T_LEDX_SITES_WIRED`: source-grep all 3 modified script files. For `scripts/auto-fix-promote.mjs`, assert `grep -c "safeAppendLedger(LEDGER_PATH"` >= 2. For `scripts/e2e-explore.mjs`, assert `grep -c "safeAppendLedger(LEDGER_PATH"` >= 2. For `scripts/auto-fix.mjs`, assert grep count of `safeAppendLedger(LEDGER_PATH` is UNCHANGED from baseline (the original local-wrapper file still calls it once at line 181 internally; if the executor's grep counts that inner call, the assertion is `>= 1` exactly without external rewiring of auto-fix.mjs).
    - The simpler reliable assertion: `grep -rn "appendLedgerEntry(LEDGER_PATH" scripts/ | wc -l` must return exactly `1` after wire-up (the canonical call inside `scripts/auto-fix.mjs:181` — all 4 leak sites now route through safeAppendLedger). Read both scripts via `fs.readFileSync` inside the test, count occurrences with a string match, assert the totals.
  </action>
  <verify>
    <automated>npm test -- tests/unit/safe-append-ledger.test.js tests/unit/llm-ledger.test.js tests/unit/auto-fix.test.js --run</automated>
    Then:
    `grep -rn "appendLedgerEntry(LEDGER_PATH" scripts/ | wc -l` → `1` (only canonical site inside `safeAppendLedger:181` remains)
    `grep -c "safeAppendLedger(LEDGER_PATH" scripts/auto-fix-promote.mjs` → `2`
    `grep -c "safeAppendLedger(LEDGER_PATH" scripts/e2e-explore.mjs` → `2`
    `grep -c "from '\\.\\./tests/e2e/lib/safe-append-ledger.js'" scripts/auto-fix-promote.mjs` → `1`
    `grep -c "from '\\.\\./tests/e2e/lib/safe-append-ledger.js'" scripts/e2e-explore.mjs` → `1`
    `git diff HEAD -- scripts/auto-fix.mjs tests/e2e/lib/llm-ledger.js | wc -l` → `0`
    `npm run lint 2>&1 | grep -c "no-restricted-imports"` → `0` (ESLint @anthropic-ai/sdk single-entry-point preserved — Plan 01 does not touch SDK)
    `grep -c 'git push origin main' .github/workflows/v40-auto-fix.yml` → `1` (Phase 57 scope-lock preserved — Plan 01 does not touch this workflow)
  </verify>
  <acceptance_criteria>
    - All 4 sites use `safeAppendLedger(LEDGER_PATH, ...)` (verified via grep counts above).
    - The 2 e2e-explore.mjs sites pass `{ defaults: { source: 'e2e-explore', transport: 'subscription' } }` as 3rd arg.
    - The 2 auto-fix-promote.mjs sites do NOT use defaults (entries already self-tag).
    - `tests/e2e/lib/llm-ledger.js` BYTE-UNCHANGED.
    - `scripts/auto-fix.mjs` BYTE-UNCHANGED.
    - All Vitest tests in safe-append-ledger.test.js + llm-ledger.test.js + auto-fix.test.js PASS.
    - `grep -rn "appendLedgerEntry(LEDGER_PATH" scripts/ | wc -l` returns exactly `1`.
    - `T_LEDX_SITES_WIRED` test PASSES.
  </acceptance_criteria>
  <done>4 sites rewired through shared helper; canonical `appendLedgerEntry(LEDGER_PATH, ...)` call in `scripts/` reduces from 5 hits to 1 (the in-wrapper call inside auto-fix.mjs:181 stays); all related Vitest pins green; auto-fix.mjs + llm-ledger.js untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev shell → committed ledger file | The CI/override/subscription-whitelist gate prevents accidental local writes from polluting the forensic record |
| Helper module → appendLedgerEntry body | The helper delegates write to the canonical writer; helper does NOT validate beyond transport (additive-only invariant) |
| Caller scripts → shared helper | Callers may pass arbitrary entry shape; helper applies defaults but trusts caller-provided non-transport fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Tampering | `appendLedgerEntry` body | mitigate | sha256 pin (Test 7 `T_LEDX_APPEND_BODY_PINNED`) fails on any byte edit; CI test gate blocks merge |
| T-62-02 | Tampering | `scripts/auto-fix.mjs:143-181` (local wrapper preserving Phase 60.1 pins) | mitigate | `git diff` check in `<verify>` blocks asserts zero changes; L1+L2 source-grep pins (auto-fix.test.js:1370-1394) fail on literal-string drift |
| T-62-03 | Information Disclosure | Local `--force-api` runs leaking entries to committed ledger | mitigate | Verbatim port of the proven Phase 56 + Phase 60.1 CI/override/subscription guard; entries with `transport:'sdk'` REQUIRE CI or override; subscription-tagged whitelisted per Phase 60.1 hotfix |
| T-62-04 | Tampering | Non-canonical `transport` value pollutes forensic record | mitigate | New transport-validation guard rejects values outside `{'sdk','subscription'}` at write boundary (Test 5 `T_LEDX_INVALID_TRANSPORT`) |
| T-62-05 | Repudiation | e2e-explore.mjs sites previously wrote ledger entries WITHOUT source/transport, creating orphan entries | mitigate | `opts.defaults` injection at wire-up sites C+D ensures `source:'e2e-explore' + transport:'subscription'` populated on every future entry |
| T-62-06 | Tampering | Helper drift from local wrapper (Pitfall 62-C — two sources of truth) | mitigate | RESEARCH-driven decision to leave `auto-fix.mjs:143-181` UNCHANGED means only ONE source of truth in shared helper for non-auto-fix callers; auto-fix.mjs uses its own self-contained wrapper. No drift possible because they don't share code paths |
| T-62-SC | Tampering | npm/pip/cargo installs | accept | Phase 62 installs ZERO new packages (no npm install commands); RESEARCH.md "Standard Stack" verified zero new deps; Package Legitimacy Audit not applicable |
</threat_model>

<verification>
End-of-plan checks:
- `npm test --run` (full Vitest suite green; baseline ≈ 33 ledger + 50+ auto-fix + 7 new safe-append-ledger + others)
- `grep -rn "appendLedgerEntry(LEDGER_PATH" scripts/ | wc -l` → exactly `1`
- `git diff HEAD -- tests/e2e/lib/llm-ledger.js scripts/auto-fix.mjs tests/unit/llm-ledger.test.js tests/unit/auto-fix.test.js | wc -l` → exactly `0` (trust-invariant non-mutations preserved)
- `grep -c 'git push origin main' .github/workflows/v40-auto-fix.yml` → exactly `1` (Phase 57 scope-lock preserved)
- ESLint clean — no @anthropic-ai/sdk single-entry-point violations
</verification>

<success_criteria>
- LEDX-01: `tests/e2e/lib/safe-append-ledger.js` exists; `safeAppendLedger(ledgerPath, entry, opts)` signature; defaults applied via `opts.defaults`; non-canonical transport rejected.
- LEDX-02: All 4 sites (`auto-fix-promote.mjs:521,544` + `e2e-explore.mjs:262,313`) consume shared helper. `grep -rn "appendLedgerEntry(LEDGER_PATH" scripts/` returns exactly 1 hit (canonical site inside `auto-fix.mjs:181`).
- LEDX-03: `appendLedgerEntry` body BYTE-UNCHANGED (sha256 pin in Test 7); 33 pre-existing Vitest ledger tests green.
- LEDX-04: Phase 60.1 subscription-transport whitelist preserved — `T_PHASE60_1_HOTFIX_PRESERVED_SHARED` pin asserts `transport:'subscription'` passes outside CI. Phase 60.1 L1+L2 source-grep pins in `tests/unit/auto-fix.test.js:1370-1394` remain green (auto-fix.mjs unchanged).
</success_criteria>

<output>
Create `.planning/phases/62-forensic-ledger-hardening-bypass-audit-probe/62-01-SUMMARY.md` when done.
</output>
