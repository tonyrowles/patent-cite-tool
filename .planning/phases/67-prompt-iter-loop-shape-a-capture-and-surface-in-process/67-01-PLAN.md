---
phase: 67-prompt-iter-loop-shape-a-capture-and-surface-in-process
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/lib/fix-prompt-builder.js
  - scripts/auto-fix.mjs
  - scripts/check-diff-guard.mjs
  - tests/unit/fix-prompt-builder.test.js
  - tests/unit/check-diff-guard.test.js
  - tests/unit/auto-fix-prompt-iter.test.js
autonomous: true
requirements: [PITER-01, PITER-02, PITER-03, PITER-04, PITER-05]

must_haves:
  truths:
    - "buildFixPrompt accepts optional rewriteHint parameter; round 0 (no hint) returns byte-identical systemPrompt to pre-Phase-67"
    - "runDispatcher Step 10 wraps LLM dispatch → parseFencedDiff → checkDiffGuard → git apply --check in an in-process iter loop bounded by ITER_MAX_ROUNDS=2 and PROMPT_ITER_COST_CAP_USD=0.50 cumulative"
    - "Iteration triggers ONLY on apply-check-failed (Step 13) and malformed-diff:* (Step 11); sdk_error / diff-guard-violation / ciGate / capBlocked / contract-error fast-fail without retry"
    - "Ledger rows written inside the iter wrapper carry additive iter_round (0..2) integer field; pre-loop ledger writes (Step 6 idempotency, Step 7 skip-class/FLAKE) DO NOT carry iter_round"
    - "When cumulative cost hits PROMPT_ITER_COST_CAP_USD or round+1 exceeds ITER_MAX_ROUNDS, the dispatcher writes one final ledger row with errorReason='prompt-iter-budget-cap' and returns exit code 0 (graceful abstention)"
    - "FORBIDDEN_PATHS regex bank rejects tests/e2e/lib/fix-prompt-builder.js AND tests/e2e/lib/llm-router.js (length 8 → 10) — defense-in-depth ships in SAME commit as iter loop"
    - "All 7 byte-stability sha256 pins at tests/unit/fix-prompt-builder-byte-stability.test.js stay GREEN"
    - "appendLedgerEntry body BYTE-UNCHANGED (Phase 56 LEDX-03 invariant); 33 pre-existing Vitest ledger tests stay GREEN"
    - "scripts/auto-fix.mjs LOCAL safeAppendLedger wrapper body BYTE-UNCHANGED (Phase 60.1 + Phase 62 invariant — additive entry literal keys only)"
    - "PROMPT_SCAFFOLDS Object.freeze + buildScaffoldSystemPrompt body BYTE-UNCHANGED (Phase 45 invariant; new splice happens AFTER scaffold returns in buildFixPrompt)"
  artifacts:
    - path: "tests/e2e/lib/fix-prompt-builder.js"
      provides: "buildFixPrompt({errorClass, issueBody, rewriteHint}) with optional rewriteHint param; round-0 byte-identity guarded by if-block"
      contains: "rewriteHint"
    - path: "scripts/auto-fix.mjs"
      provides: "ITER_MAX_ROUNDS=2 + PROMPT_ITER_COST_CAP_USD=0.50 constants; runDispatcher Step 10 iter wrapper; iter_round propagation to 4 ledger sites + budget-cap site"
      contains: "ITER_MAX_ROUNDS"
    - path: "scripts/check-diff-guard.mjs"
      provides: "FORBIDDEN_PATHS bank extended by 2 entries (length 8 → 10)"
      contains: "fix-prompt-builder\\.js"
    - path: "tests/unit/auto-fix-prompt-iter.test.js"
      provides: "T_PROMPT_ITER_BUDGET_01 + sdk_error fast-fail + apply-check iter retry + malformed-diff iter retry + iter_round propagation tests; mocks safeAppendLedger + invokeAnthropicSdkWithLedger + invokeClaudePWithLedger via existing pattern"
      min_lines: 200
    - path: "tests/unit/fix-prompt-builder.test.js"
      provides: "rewriteHint round-0 byte-identity test + rewriteHint append test (additive only — existing 30+ tests preserved)"
    - path: "tests/unit/check-diff-guard.test.js"
      provides: "toHaveLength(8) → toHaveLength(10); F15 + F16 added for fix-prompt-builder.js and llm-router.js rejection"
  key_links:
    - from: "tests/e2e/lib/fix-prompt-builder.js:buildFixPrompt"
      to: "scripts/auto-fix.mjs runDispatcher iter wrapper"
      via: "rewriteHint parameter (called per round; undefined on round 0)"
      pattern: "rewriteHint"
    - from: "scripts/auto-fix.mjs iter wrapper"
      to: "scripts/auto-fix.mjs safeAppendLedger"
      via: "iter_round field added to entry object literal at success/malformed-diff/apply-check/diff-guard/budget-cap sites"
      pattern: "iter_round:"
    - from: "scripts/auto-fix.mjs runDispatcher"
      to: "scripts/check-diff-guard.mjs FORBIDDEN_PATHS"
      via: "diff-guard rejection still fast-fails (PITER-04 gating); new entries protect fix-prompt-builder.js + llm-router.js from auto-fix self-mutation"
      pattern: "fix-prompt-builder\\\\.js|llm-router\\\\.js"
---

<objective>
Phase 67 ships ONE atomic commit delivering all 5 PITER-* requirements: the `rewriteHint` parameter on `buildFixPrompt` (PITER-01), the in-process iteration wrapper around `runDispatcher` Step 10..13 (PITER-02), the additive `iter_round` ledger field plus `ITER_MAX_ROUNDS=2` and `PROMPT_ITER_COST_CAP_USD=0.50` constants (PITER-03), the strict trigger gating that fast-fails on `sdk_error`/diff-guard/ciGate/capBlocked (PITER-04), and the FORBIDDEN_PATHS regex bank extension that prevents the auto-fix loop from ever editing scaffold source (PITER-05).

Purpose: increases fix-rate on `apply-check-failed` and `malformed-diff:*` outcomes by re-prompting Claude with the prior attempt's failure mode, bounded by a per-fingerprint $0.50 cumulative cap to preserve cost discipline. The FORBIDDEN_PATHS extension is the non-negotiable trust-boundary contract that even a future Shape B prompt-iter ever shipped in v4.4+ cannot edit `fix-prompt-builder.js` or `llm-router.js`.

Output: 3 source files modified (`buildFixPrompt`, `runDispatcher`, `FORBIDDEN_PATHS`), 2 existing test files extended additively (preserving baseline counts), 1 new test file (`auto-fix-prompt-iter.test.js`) housing `T_PROMPT_ITER_BUDGET_01` plus companions. Single atomic commit per D-09 atomicity constraint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/67-prompt-iter-loop-shape-a-capture-and-surface-in-process/67-CONTEXT.md
@.planning/phases/67-prompt-iter-loop-shape-a-capture-and-surface-in-process/67-RESEARCH.md

# Source-of-truth files the executor MUST read before touching anything
@tests/e2e/lib/fix-prompt-builder.js
@scripts/auto-fix.mjs
@scripts/check-diff-guard.mjs
@tests/unit/fix-prompt-builder.test.js
@tests/unit/fix-prompt-builder-byte-stability.test.js
@tests/unit/check-diff-guard.test.js
@tests/unit/auto-fix.test.js

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. Executor should use these directly — no exploration needed. -->

From `tests/e2e/lib/fix-prompt-builder.js`:
- Line 117: `export function buildScaffoldSystemPrompt({ className, fixSurfaceContract })` — PURE; returns a string. NOT MODIFIED in this plan.
- Line 483: `export const PROMPT_SCAFFOLDS = Object.freeze({ ... })` — frozen registry of 7 scaffold thunks. NOT MODIFIED.
- Line 534 (current signature): `export function buildFixPrompt({ errorClass, issueBody } = {})` — TO BE EXTENDED with optional `rewriteHint`. Splice happens AFTER `const systemPrompt = scaffold();` on line 553.
- Line 561 (current return): `return { ok: true, systemPrompt, userPrompt, model: routeModel(errorClass) };` — UNCHANGED shape; `systemPrompt` is the only field that gets the hint appended.

From `scripts/auto-fix.mjs`:
- Lines 94-181: local `safeAppendLedger` wrapper — BODY UNCHANGED. New `iter_round` field flows through as additive entry-literal key.
- Lines 184-207: existing constants `PHASE`, `PHASE_46`, `SOURCE_FIX_ISSUE`, `VALID_TRANSPORTS`, `FIX_ATTEMPT_CAP`, `HUMAN_REVIEW_LABEL`. New `ITER_MAX_ROUNDS = 2` and `PROMPT_ITER_COST_CAP_USD = 0.50` constants are added in this block (after line 207, before line 213 `FLAKE_INVESTIGATION_LABEL`).
- Line 238: `extractFingerprint` — EXISTING; reused.
- Line 563: `export async function runDispatcher({ ... })` — function modified.
- Lines 770-820: Step 10 LLM dispatch (the wrap point). `sdkResult.errorReason === 'sdk_error'` fast-fail lives at line 818 BEFORE any iter retry point — PITER-04 fast-fail holds naturally.
- Lines 822-841: Step 11 `parseFencedDiff` — iter trigger #1 (malformed-diff:*).
- Lines 843-875: Step 12 `checkDiffGuard` — fast-fail (NOT an iter trigger).
- Lines 877-903: Step 13 `git apply --check` — iter trigger #2 (apply-check-failed).
- Existing `sdkResult.costUsd` is the canonical per-call cost on both transport return shapes (verified at `tests/e2e/lib/llm-driver.js:236, 461`).

From `tests/e2e/lib/llm-ledger.js`:
- Line 686: `appendLedgerEntry` body does `m.iterations.push(entry)` — entries pass through verbatim. `iter_round` flows through with zero helper-body change. BODY UNCHANGED per LEDX-03.

From `scripts/check-diff-guard.mjs`:
- Lines 49-58: `export const FORBIDDEN_PATHS = Object.freeze([ /^tests\/test-cases\.js$/, ..., /^tests\/e2e\/\.flake-suppression\.json$/ ])` — 8 entries today. Append 2 anchored entries; length becomes 10.
- Line 66: `checkDiffGuard(changedPaths)` — UNCHANGED.

From `tests/unit/check-diff-guard.test.js`:
- Line 36: `expect(FORBIDDEN_PATHS).toHaveLength(8);` — update to `toHaveLength(10)`.
- F-test pattern F1..F14 — add F15 (fix-prompt-builder.js) + F16 (llm-router.js).

From `tests/unit/fix-prompt-builder-byte-stability.test.js`:
- Lines 23-46: 7 sha256 pins hash `PROMPT_SCAFFOLDS[className]()` (the thunk). They do NOT hash `buildFixPrompt(...).systemPrompt`. SAFE — pins survive by construction because the post-scaffold append happens AFTER the thunk returns.

From `tests/unit/auto-fix.test.js`:
- Lines 53-105: hoisted-mock pattern reused verbatim in `tests/unit/auto-fix-prompt-iter.test.js` (mocks `node:child_process`, `../e2e/lib/llm-driver.js`, `../e2e/lib/llm-ledger.js`, `../e2e/lib/triage-classifier.js`).
</interfaces>

<atomicity_constraint>
**NON-NEGOTIABLE (STATE.md D-09):** All 5 PITER-* requirements ship in ONE atomic git commit. Partial states recreate the trust-erosion risk that v4.3 D-09 explicitly mitigates. The plan executor MUST:

1. Implement ALL tasks below.
2. Run the full test suite (`npm test`) GREEN before staging.
3. Stage all 6 files together.
4. Create ONE commit covering all 5 PITER-* requirements.

If any task fails verification mid-stream, fix and re-verify in-place — do NOT commit a partial state.
</atomicity_constraint>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add rewriteHint param to buildFixPrompt with round-0 byte-identity guarantee (PITER-01)</name>
  <files>tests/e2e/lib/fix-prompt-builder.js, tests/unit/fix-prompt-builder.test.js</files>
  <read_first>
    - tests/e2e/lib/fix-prompt-builder.js (the entire file — focus lines 117 buildScaffoldSystemPrompt, 483 PROMPT_SCAFFOLDS, 534-562 buildFixPrompt)
    - tests/unit/fix-prompt-builder.test.js (entire file — see existing assertion patterns for the additive cases)
    - tests/unit/fix-prompt-builder-byte-stability.test.js (entire file — lines 23-46 confirm pins hash PROMPT_SCAFFOLDS[className](), NOT buildFixPrompt(...))
    - .planning/phases/67-prompt-iter-loop-shape-a-capture-and-surface-in-process/67-RESEARCH.md (Pattern 1 + Pitfall 1 + Pitfall 2 — the round-0 byte-identity discipline)
  </read_first>
  <behavior>
    - Test 1 (round-0 byte-identity, WRONG_CITATION): `buildFixPrompt({errorClass:'WRONG_CITATION', issueBody:'x'}).systemPrompt === PROMPT_SCAFFOLDS.WRONG_CITATION()` MUST hold (no rewriteHint param).
    - Test 2 (round-0 byte-identity, empty hint): `buildFixPrompt({errorClass:'WRONG_CITATION', issueBody:'x', rewriteHint:''}).systemPrompt === PROMPT_SCAFFOLDS.WRONG_CITATION()` MUST hold (empty-string hint also short-circuits).
    - Test 3 (round-0 byte-identity, undefined hint): `buildFixPrompt({errorClass:'WRONG_CITATION', issueBody:'x', rewriteHint:undefined}).systemPrompt === PROMPT_SCAFFOLDS.WRONG_CITATION()` MUST hold.
    - Test 4 (hint appended): `buildFixPrompt({errorClass:'WRONG_CITATION', issueBody:'x', rewriteHint:'simulated stderr'}).systemPrompt` MUST start with `PROMPT_SCAFFOLDS.WRONG_CITATION()` AND end with a `</prior_attempt_feedback>` closing tag, AND contain the literal substring `'simulated stderr'` between the opening/closing `<prior_attempt_feedback>` tags.
    - Test 5 (all 5 existing scaffolds preserve round-0 identity): iterate `['WRONG_CITATION','LLM_HALLUCINATED_SELECTION','WORKER_FALLBACK_FAILED','GOOGLE_DOM_DRIFT','HARNESS_ERROR']`; each MUST satisfy `buildFixPrompt({errorClass:k, issueBody:'x'}).systemPrompt === PROMPT_SCAFFOLDS[k]()`.
    - Test 6 (rewriteHint preserves shape contract): when hint is appended, the return shape is `{ok:true, systemPrompt, userPrompt, model}` (no field added, no field removed).
    - Test 7 (skip-class still wins): `buildFixPrompt({errorClass:'PASS', rewriteHint:'foo'})` returns `{ok:false, escalate:'close-as-pass'}` — rewriteHint cannot override SKIP_CLASS_ESCALATIONS.
  </behavior>
  <action>
Edit `tests/e2e/lib/fix-prompt-builder.js`:

1. Modify the `buildFixPrompt` signature at line 534 from `export function buildFixPrompt({ errorClass, issueBody } = {})` to add an optional third destructured property: `export function buildFixPrompt({ errorClass, issueBody, rewriteHint } = {})`. Keep the default `{}` so calls with no args still work (existing tests rely on this).

2. After line 553 (`const systemPrompt = scaffold();`), convert `systemPrompt` from `const` to `let` and insert a post-scaffold append block. The append MUST be gated by `if (typeof rewriteHint === 'string' && rewriteHint.length > 0)` so that round 0 (undefined or empty hint) produces byte-identical output to today (this protects the 7 sha256 byte-stability pins).

3. The appended text is a `<prior_attempt_feedback>` block joined onto the existing scaffold result. The exact wording of the section header is at Claude's discretion per CONTEXT.md — pick language consistent with existing scaffold structure. Required structural invariants: (a) the block contains `<prior_attempt_feedback>` opening tag, (b) the hint text verbatim, (c) `</prior_attempt_feedback>` closing tag. The join MUST be a single string concatenation onto the existing `systemPrompt` variable — do NOT spread the scaffold or mutate `PROMPT_SCAFFOLDS`.

4. The return shape at line 561 stays `{ok: true, systemPrompt, userPrompt, model: routeModel(errorClass)}` — UNCHANGED. Only the `systemPrompt` value gains the appended block when a hint is provided.

5. Do NOT modify `buildScaffoldSystemPrompt` (line 117), `PROMPT_SCAFFOLDS` (line 483), or any `<CLASS>_SYSTEM` constant (lines 410-450). The splice happens exclusively inside `buildFixPrompt`.

Edit `tests/unit/fix-prompt-builder.test.js`:

6. Add a new `describe('Phase 67 PITER-01: rewriteHint parameter', () => { ... })` block at the end of the file (preserve all existing tests). Inside, add the 7 tests from `<behavior>` above. Use literal string equality (`toBe`) for byte-identity tests; use `toContain` / `toMatch` for the appended-block test.

7. Reuse the existing import line at the top of the file — do NOT add new imports. Both `buildFixPrompt` and `PROMPT_SCAFFOLDS` are already exported.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/fix-prompt-builder.test.js tests/unit/fix-prompt-builder-byte-stability.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/fix-prompt-builder.test.js` reports ALL tests pass (existing 30+ tests + 7 new tests).
    - `npx vitest run tests/unit/fix-prompt-builder-byte-stability.test.js` reports ALL 7 sha256 pins still pass.
    - `grep -c 'rewriteHint' tests/e2e/lib/fix-prompt-builder.js` returns at least 2 (signature + if-check).
    - `git diff -- tests/e2e/lib/fix-prompt-builder.js | grep -E '^[-+].*buildScaffoldSystemPrompt' | wc -l` returns 0 (helper body untouched).
    - `git diff -- tests/e2e/lib/fix-prompt-builder.js | grep -E '^[-+].*PROMPT_SCAFFOLDS\\s*=' | wc -l` returns 0 (frozen registry untouched).
    - Source assertion: `tests/e2e/lib/fix-prompt-builder.js` contains the substring `if (typeof rewriteHint === 'string' && rewriteHint.length > 0)` (the round-0 guard).
  </acceptance_criteria>
  <done>buildFixPrompt accepts optional rewriteHint; round 0 byte-identical to today; all 7 byte-stability pins still GREEN; 7 new Vitest cases GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend FORBIDDEN_PATHS regex bank + atomic-commit defense-in-depth (PITER-05)</name>
  <files>scripts/check-diff-guard.mjs, tests/unit/check-diff-guard.test.js</files>
  <read_first>
    - scripts/check-diff-guard.mjs (entire file — focus lines 49-58 FORBIDDEN_PATHS array)
    - tests/unit/check-diff-guard.test.js (entire file — focus line 36 length assertion + F1..F14 test pattern)
    - .planning/phases/67-prompt-iter-loop-shape-a-capture-and-surface-in-process/67-RESEARCH.md (Pattern 5 + Pitfall 6 — anchor strictness)
  </read_first>
  <behavior>
    - Test 1 (count update): `FORBIDDEN_PATHS.length === 10` (was 8); every element is a RegExp.
    - Test 2 (F15 reject fix-prompt-builder.js): `checkDiffGuard(['tests/e2e/lib/fix-prompt-builder.js']).ok === false` and `result.violations` contains the path string.
    - Test 3 (F16 reject llm-router.js): `checkDiffGuard(['tests/e2e/lib/llm-router.js']).ok === false` and `result.violations` contains the path string.
    - Test 4 (anchor strictness — bak suffix MUST NOT match F15): `checkDiffGuard(['tests/e2e/lib/fix-prompt-builder.js.bak']).ok === true` (anchors force exact-path match).
    - Test 5 (anchor strictness — subdir prefix MUST NOT match F16): `checkDiffGuard(['vendor/tests/e2e/lib/llm-router.js']).ok === true` (the `^` anchor refuses substring matches).
  </behavior>
  <action>
Edit `scripts/check-diff-guard.mjs`:

1. In the `FORBIDDEN_PATHS` `Object.freeze([...])` array at lines 49-58, append exactly TWO entries AFTER the existing `^tests\/e2e\/\.flake-suppression\.json$` entry: `/^tests\/e2e\/lib\/fix-prompt-builder\.js$/` and `/^tests\/e2e\/lib\/llm-router\.js$/`. Both MUST use the `^...$` anchors verbatim — no broader matchers (Pitfall 6).

2. Add a trailing comment on each new line referencing Phase 67 PITER-05 so the addition is greppable (e.g., `// Phase 67 — PITER-05`).

3. Do NOT modify the `checkDiffGuard` function body at line 66-79 or the CLI shim at line 85-103. Bank extension only.

Edit `tests/unit/check-diff-guard.test.js`:

4. At line 36, change `expect(FORBIDDEN_PATHS).toHaveLength(8);` to `expect(FORBIDDEN_PATHS).toHaveLength(10);`. Also update the surrounding `it(...)` test name from `'exports exactly 8 regex patterns'` (or current text) to `'exports exactly 10 regex patterns (8 prior + 2 Phase 67 extensions)'` for traceability.

5. Add two new `it(...)` blocks following the existing F1..F14 pattern (use the next available F-number — F15 and F16) inside the existing `describe('FORBIDDEN_PATHS bank', () => { ... })` block. F15 asserts the fix-prompt-builder.js rejection; F16 asserts the llm-router.js rejection.

6. Add two new anchor-strictness `it(...)` blocks asserting `.bak` / `vendor/` paths do NOT match the new entries (Pitfall 6 regression guard).

7. Do NOT change any other existing test assertion (preserve F1..F14 verbatim).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/check-diff-guard.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/check-diff-guard.test.js` reports ALL tests pass (existing 14+ F-tests + length-update + F15 + F16 + 2 anchor-strictness tests).
    - `grep -c '^/\\^tests' scripts/check-diff-guard.mjs` returns 10 (or use `node -e 'import("./scripts/check-diff-guard.mjs").then(m => console.log(m.FORBIDDEN_PATHS.length))'` returns `10`).
    - Source assertion: `scripts/check-diff-guard.mjs` contains both literal regex sources `/^tests\\/e2e\\/lib\\/fix-prompt-builder\\.js$/` and `/^tests\\/e2e\\/lib\\/llm-router\\.js$/` (escape pattern as written in source).
    - Source assertion: no other `^tests\\/e2e\\/lib` regex appears in the bank besides those two (verify with `grep -c 'tests/e2e/lib' scripts/check-diff-guard.mjs` returns exactly 2 within the FORBIDDEN_PATHS block; ignore comments).
  </acceptance_criteria>
  <done>FORBIDDEN_PATHS length is 10; F15 + F16 reject the two scaffold paths; F1..F14 unchanged; defense-in-depth ships in same commit as iter wrapper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: New Vitest file tests/unit/auto-fix-prompt-iter.test.js — RED scaffold for PITER-02..04 (TDD RED)</name>
  <files>tests/unit/auto-fix-prompt-iter.test.js</files>
  <read_first>
    - tests/unit/auto-fix.test.js (entire file — lines 53-105 hoisted-mock pattern + lines 138-150 setup pattern + lines 305+ per-test mockResolvedValue pattern)
    - .planning/phases/67-prompt-iter-loop-shape-a-capture-and-surface-in-process/67-RESEARCH.md (Validation Architecture — Phase Requirements → Test Map; Example B + Example C + Example D)
    - tests/e2e/lib/llm-driver.js (around lines 80-115 — invokeClaudePWithLedger return shape; around lines 510-590 — invokeAnthropicSdkWithLedger return shape; confirms costUsd on both)
    - scripts/auto-fix.mjs (entire file — to understand the import surface of runDispatcher and the existing safeAppendLedger contract)
  </read_first>
  <behavior>
    - Test A (constants exported): assert `ITER_MAX_ROUNDS === 2` and `PROMPT_ITER_COST_CAP_USD === 0.50` by importing from `scripts/auto-fix.mjs` (constants must be exported alongside `runDispatcher`).
    - Test B (apply-check iter retry — happy path): SDK mock returns ok:true llmText with a syntactically-fenceable but invalid diff; `git apply --check` mock throws; assert SDK is invoked round 0 then round 1; assert two ledger entries with `iter_round: 0` and `iter_round: 1` and `errorReason: 'apply-check-failed'`.
    - Test C (malformed-diff iter retry — happy path): SDK mock returns ok:true llmText with no fences; `parseFencedDiff` returns `{ok:false, reason:'no-fences'}`; assert SDK is invoked round 0 then round 1; assert ledger entry with `iter_round: 0` and `errorReason: 'malformed-diff:no-fences'`; assert round-1 `buildFixPrompt` call (observable as new SDK call with augmented systemPrompt containing `'no-fences'`).
    - Test D (T_PROMPT_ITER_BUDGET_01 — cap exhaustion): SDK mock returns `costUsd: 0.20` each call + apply-check throws each round. With `PROMPT_ITER_COST_CAP_USD = 0.50` and `ITER_MAX_ROUNDS = 2`: after round 0 (cumCost=0.20) + round 1 (cumCost=0.40) + round 2 (cumCost=0.60), the cap triggers. Assert `exitCode === 0` (graceful abstention); assert ledger contains an entry with `errorReason: 'prompt-iter-budget-cap'`; assert SDK invocation count is bounded `>= 2 && <= ITER_MAX_ROUNDS + 1` (== 3 in this synthetic case).
    - Test E (sdk_error fast-fail — PITER-04): SDK mock returns `{ok:false, errorReason:'sdk_error', errorMessage:'simulated API outage'}`; assert `exitCode === 1`; assert SDK was called EXACTLY 1 time (no iter retry).
    - Test F (diff-guard fast-fail — PITER-04): SDK mock returns ok:true llmText with a fenced diff that touches a FORBIDDEN_PATH (e.g., `tests/e2e/lib/fix-prompt-builder.js`); `parseFencedDiff` returns ok:true; `checkDiffGuard` returns `{ok:false, violations:[...]}`; assert `exitCode === 1`; assert SDK was called EXACTLY 1 time (no iter retry); assert ledger entry has `iter_round: 0` AND `errorReason` starting with `diff-guard-violation` (the diff-guard ledger row still carries iter_round because it lives INSIDE the wrapper, but the retry path is NOT entered).
    - Test G (iter_round field discipline — Pitfall 7): pre-loop ledger rows (Step 6 idempotency / Step 7 skip-class) MUST NOT contain `iter_round`. Use a fixture that triggers a skip-class path (e.g., `gh issue view` returns labels with `PASS`) and assert the ledger entries written do NOT have a defined `iter_round` property.
    - Test H (round-0 byte-identity through dispatch): when no retry trigger fires (apply-check succeeds round 0), the FIRST and ONLY SDK call MUST have been invoked with a systemBlock whose `text` is byte-identical to `PROMPT_SCAFFOLDS[errorClass]()` (no `<prior_attempt_feedback>` block on round 0).
  </behavior>
  <action>
Create new file `tests/unit/auto-fix-prompt-iter.test.js`:

1. Reuse the hoisted-mock pattern from `tests/unit/auto-fix.test.js:53-105` VERBATIM (copy the `vi.mock(...)` blocks for `node:child_process`, `../e2e/lib/llm-driver.js`, `../e2e/lib/llm-ledger.js`, `../e2e/lib/triage-classifier.js`). DO NOT mock `tests/e2e/lib/fix-prompt-builder.js` or `scripts/check-diff-guard.mjs` — the wrapper logic must integrate against the real modules (their unit tests cover internal behavior).

2. Import surface at module top:
   - `import { describe, it, expect, vi, beforeEach } from 'vitest';`
   - `import { execFileSync } from 'node:child_process';`
   - `import { invokeAnthropicSdkWithLedger, invokeClaudePWithLedger } from '../e2e/lib/llm-driver.js';`
   - `import { appendLedgerEntry } from '../e2e/lib/llm-ledger.js';`
   - `import { runDispatcher, ITER_MAX_ROUNDS, PROMPT_ITER_COST_CAP_USD } from '../../scripts/auto-fix.mjs';`
   - `import { PROMPT_SCAFFOLDS } from '../e2e/lib/fix-prompt-builder.js';`

3. `beforeEach`: set `process.env.CI = 'true'` (satisfies the local safeAppendLedger CI gate); call `vi.resetAllMocks()`; reset `execFileSync` to the router pattern.

4. Implement a small `setupExecFileSyncRouter(routes)` helper at the top of the file that maps `[cmd, args]` to a response function. The first matching route wins. The router shape mirrors Phase 67 RESEARCH Example C. Include router cases for `gh issue view`, `gh label create`, `git config`, `git rev-parse`, etc. — copy the minimal router shape from `tests/unit/auto-fix.test.js` setup helpers (around lines 138-300) and extend with `git apply --check` for the new flow.

5. Implement the 8 tests A..H from `<behavior>` above. Test D MUST be named exactly `T_PROMPT_ITER_BUDGET_01: ...` so the verify command can target it by `-t T_PROMPT_ITER_BUDGET_01`. Tests B/C/D use `vi.mocked(invokeAnthropicSdkWithLedger).mockResolvedValue(...)` to control SDK return shapes per round. Tests B/C also use `mockResolvedValueOnce(...)` chains where the test needs different return values between rounds.

6. Helper for ledger inspection: `const ledgerCalls = vi.mocked(appendLedgerEntry).mock.calls.map(([, e]) => e);` — match the shape from Example C in RESEARCH.

7. Test H verifies round-0 byte-identity by inspecting the first arg of `vi.mocked(invokeAnthropicSdkWithLedger).mock.calls[0][0]` — pull the `systemBlocks` (or systemPrompt) and assert string equality against `PROMPT_SCAFFOLDS['WRONG_CITATION']()` (or whichever class the fixture used).

8. AT THIS POINT the dispatcher does NOT yet have the iter wrapper — tests B/C/D/E/F/G/H will fail. The file SHOULD compile and run; Vitest reports failures for B..H. Test A (constants) will fail because the exports don't exist yet. THIS IS THE TDD RED STATE — Task 4 will make it GREEN.

9. Add a header comment block at the top of the file naming the requirement IDs covered (PITER-02, PITER-03, PITER-04) and pointing to `.planning/phases/67-prompt-iter-loop-shape-a-capture-and-surface-in-process/67-CONTEXT.md` for the locked design.
  </action>
  <verify>
    <automated>test -f tests/unit/auto-fix-prompt-iter.test.js && npx vitest run tests/unit/auto-fix-prompt-iter.test.js --reporter=verbose 2>&1 | grep -E 'Tests|Failed|Passed|T_PROMPT_ITER_BUDGET_01'</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/auto-fix-prompt-iter.test.js` exists with at least 8 `it(...)` blocks (one per behavior A..H).
    - The literal string `T_PROMPT_ITER_BUDGET_01` appears exactly once as a test name.
    - `grep -c 'PITER-0' tests/unit/auto-fix-prompt-iter.test.js` returns at least 3 (header comment references PITER-02/03/04).
    - Vitest reports the file is RED (Task 3 ships failing tests intentionally — this is the TDD RED state; Task 4 makes them GREEN). Specifically: tests A (constants import) MUST fail with a TypeError or `undefined` because `ITER_MAX_ROUNDS` is not yet exported. Tests B..H MUST fail because the wrapper doesn't exist yet.
    - Source assertion: `tests/unit/auto-fix-prompt-iter.test.js` contains the literal mock pattern `vi.mock('../e2e/lib/llm-driver.js'` (reused from auto-fix.test.js).
  </acceptance_criteria>
  <done>New Vitest file scaffolded with 8 tests covering PITER-02/03/04; file in TDD RED state (tests fail because dispatcher wrapper not yet shipped).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement iter wrapper in runDispatcher + iter_round propagation + budget-cap (PITER-02, PITER-03, PITER-04 — TDD GREEN)</name>
  <files>scripts/auto-fix.mjs</files>
  <read_first>
    - scripts/auto-fix.mjs (entire file — lines 184-220 constant block; lines 94-181 safeAppendLedger wrapper; lines 563+ runDispatcher; lines 770-820 Step 10; lines 822-841 Step 11; lines 843-875 Step 12; lines 877-903 Step 13)
    - .planning/phases/67-prompt-iter-loop-shape-a-capture-and-surface-in-process/67-RESEARCH.md (Pattern 2 verbatim; Pitfall 3, 4, 5, 7, 8; Architectural Responsibility Map)
    - .planning/phases/67-prompt-iter-loop-shape-a-capture-and-surface-in-process/67-CONTEXT.md (entire — locked accumulator + cap + gating decisions)
    - tests/unit/auto-fix-prompt-iter.test.js (created in Task 3 — to know the contract that must turn GREEN)
  </read_first>
  <behavior>
    - All 8 tests in `tests/unit/auto-fix-prompt-iter.test.js` (A..H from Task 3) MUST pass after this task.
    - Existing `tests/unit/auto-fix.test.js` baseline MUST stay GREEN (no regressions).
    - Existing `tests/unit/llm-ledger.test.js` 33-test baseline MUST stay GREEN (LEDX-03 invariant).
  </behavior>
  <action>
Edit `scripts/auto-fix.mjs`. The edit decomposes into four concrete sub-edits — implement in order:

1. **Add constants** (after line 207 `HUMAN_REVIEW_LABEL`, before line 213 `FLAKE_INVESTIGATION_LABEL`): introduce `export const ITER_MAX_ROUNDS = 2;` and `export const PROMPT_ITER_COST_CAP_USD = 0.50;` with an inline doc-comment naming PITER-03 and the per-fingerprint cumulative semantics (per CONTEXT.md "Cap enforcement point"). The `export` is required so the new Vitest file (Task 3 imports them at the module top). Use literal `0.50` (not `0.5`) to match the documented spend cap from STATE.md Budget table.

2. **Wrap Steps 10..13 in a `while` loop inside `runDispatcher`**, following Pattern 2 in RESEARCH.md VERBATIM (the executor should treat the example as the implementation contract; identifiers are at Claude's discretion per CONTEXT.md but the structure is fixed). Key requirements:
   - Instantiate `const iterState = new Map();` and `iterState.set(fingerprint, { round: 0, cumCost: 0 });` IMMEDIATELY before Step 10 (after Step 9 systemBlocks build at line ~762). Map is function-local — NEVER module-level (Pitfall 8: test purity).
   - Declare `let rewriteHint;` (undefined on round 0, so round-0 byte-identity holds per Pitfall 4).
   - The loop body MUST start with a FRESH `const built = buildFixPrompt({ errorClass, issueBody, rewriteHint });` call per round (Pitfall 4: do NOT reuse pre-loop `built`). Then rebuild `systemBlocks` per round (Pitfall 5: cache_control header must wrap the per-round systemPrompt).
   - Step 10 dispatch branches (`transport === 'subscription'` vs SDK) UNCHANGED — wrap them inside the loop.
   - After SDK call, accumulate `state.cumCost += sdkResult.costUsd ?? 0;` (use the helper-known nullish coalesce, NOT `||` since costUsd could be `0`).
   - **Fast-fail branches (PITER-04)** — the `!sdkResult.ok` branch keeps its EXISTING return semantics verbatim: `ciGate` returns 2; `capBlocked` returns 3; `errorReason === 'contract-error'` returns 2; `sdk_error` returns 1. NONE of these enter the iter retry path. Per RESEARCH Pattern 4, `sdkResult.errorReason === 'sdk_error'` fast-fails at line 818 BEFORE any retry trigger.
   - **Step 11 trigger (malformed-diff)**: when `!parsed.ok`, write a `safeAppendLedger` entry WITH `iter_round: state.round` AND `errorReason: 'malformed-diff:${parsed.reason}'`. Then check the cap: if `state.round + 1 > ITER_MAX_ROUNDS || state.cumCost >= PROMPT_ITER_COST_CAP_USD`, write a SECOND `safeAppendLedger` entry with `iter_round: state.round` AND `errorReason: 'prompt-iter-budget-cap'`, then `return 0;` (graceful abstention). Otherwise increment `state.round += 1`, set `rewriteHint = parsed.reason`, and `continue;`.
   - **Step 12 (diff-guard) — FAST-FAIL, NOT iter trigger**: when `!guard.ok`, write `safeAppendLedger` entry WITH `iter_round: state.round` AND `errorReason: 'diff-guard-violation:...'` (mirroring the existing diff-guard ledger row shape with the additive iter_round key), perform the existing `gh issue comment` side effect UNCHANGED, then `return 1;`. Do NOT enter the retry path.
   - **Step 13 trigger (apply-check-failed)**: in the `catch` block around `execFileSync('git', ['apply','--check'], ...)`, write a `safeAppendLedger` entry WITH `iter_round: state.round` AND `errorReason: 'apply-check-failed'` AND `errorMessage: stderrSnip` (existing `stderrSnip = String(err.stderr ?? err.message ?? '').slice(0, 500)` shape from line 885). Then check the cap (same condition as Step 11 trigger): if exhausted, write the budget-cap row + `return 0;`. Otherwise increment `state.round`, set `rewriteHint = stderrSnip`, `continue;`.
   - **Step 13 success**: `break;` out of the loop to proceed to Steps 14-18 UNCHANGED.

3. **Discipline guard (Pitfall 7)**: the `iter_round` key is added ONLY to ledger entries written inside the iter wrapper. The pre-loop ledger writes at Step 6 (idempotency at ~line 681-697) and Step 7 (skip-class at ~line 728-740, FLAKE dispatcher) and dry-run paths remain UNCHANGED — they MUST NOT receive an `iter_round` field. After implementing, verify with `git diff -- scripts/auto-fix.mjs | grep '^\\+.*iter_round' | wc -l` returns between 4 and 6 (success ledger row in Step 10/13 success path + Step 11 malformed-diff + Step 11 budget-cap + Step 13 apply-check + Step 13 budget-cap + optional Step 12 diff-guard row = up to 6 sites).

4. **safeAppendLedger body UNCHANGED**: the wrapper at lines 94-181 stays byte-identical. `iter_round` flows through as an additional entry-object key — additive, not validated by the wrapper. This preserves Pitfall 8 (local wrapper distinct from Phase 62 shared helper) and LEDX-03 (appendLedgerEntry body byte-unchanged).

5. **Round-0 byte-identity (Pitfall 4)**: confirm by source-grep that round 0 calls `buildFixPrompt({ errorClass, issueBody, rewriteHint })` with `rewriteHint === undefined`. The buildFixPrompt early-skip from Task 1 produces byte-identical systemPrompt when `rewriteHint` is undefined or empty. Cache_control hit-rate is preserved.

6. **Do not migrate to shared safe-append-ledger.js helper** (Pitfall 8): the local wrapper stays. No new imports from `../tests/e2e/lib/safe-append-ledger.js`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/auto-fix-prompt-iter.test.js tests/unit/auto-fix.test.js tests/unit/llm-ledger.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/auto-fix-prompt-iter.test.js` reports ALL 8 tests A..H PASS (including `T_PROMPT_ITER_BUDGET_01`).
    - `npx vitest run tests/unit/auto-fix.test.js` reports the pre-existing baseline PASS rate (no regressions vs. HEAD — same passing count).
    - `npx vitest run tests/unit/llm-ledger.test.js` reports the 33-test baseline GREEN (LEDX-03 invariant).
    - Source assertion: `grep -c '^export const ITER_MAX_ROUNDS = 2;' scripts/auto-fix.mjs` returns 1.
    - Source assertion: `grep -c '^export const PROMPT_ITER_COST_CAP_USD = 0.50;' scripts/auto-fix.mjs` returns 1.
    - Source assertion: `grep -c "errorReason: 'prompt-iter-budget-cap'" scripts/auto-fix.mjs` returns at least 2 (one per trigger site — Step 11 malformed-diff cap + Step 13 apply-check cap).
    - Source assertion: `grep -v '^[[:space:]]*//' scripts/auto-fix.mjs | grep -c 'iter_round:' ` returns between 4 and 6 (count of ledger entry sites carrying iter_round; comments excluded per grep gate hygiene). Verifies Pitfall 7 discipline.
    - Source assertion: `grep -c 'new Map' scripts/auto-fix.mjs` ≥ 1 (the new iterState Map declaration; existing Map usage may already exist — count is non-zero).
    - Source assertion: `git diff HEAD -- tests/e2e/lib/llm-ledger.js | wc -l` returns 0 (LEDX-03: appendLedgerEntry body byte-unchanged).
    - Functional assertion (Pitfall 7 discipline): in the skip-class fixture from Test G, `vi.mocked(appendLedgerEntry).mock.calls` contains at least one entry where the entry object has NO `iter_round` key defined (`entry.iter_round === undefined`).
  </acceptance_criteria>
  <done>iter wrapper live in runDispatcher; all 8 PITER-02..04 tests GREEN; baseline tests GREEN; appendLedgerEntry + local safeAppendLedger bodies byte-unchanged; constants exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Atomic-commit gate — full suite GREEN before single commit covering PITER-01..05</name>
  <files>(verification + commit only — no source edits)</files>
  <read_first>
    - .planning/phases/67-prompt-iter-loop-shape-a-capture-and-surface-in-process/67-RESEARCH.md (Pitfall 9 — atomic commit requirement; Validation Architecture — Sampling Rate)
    - .planning/STATE.md (Trust invariants section — confirms what MUST hold post-commit)
  </read_first>
  <behavior>
    - Full Vitest suite (`npm test`) is GREEN with zero pre-existing failures introduced by Phase 67.
    - All 7 byte-stability sha256 pins GREEN.
    - FORBIDDEN_PATHS bank rejects fix-prompt-builder.js + llm-router.js.
    - `T_PROMPT_ITER_BUDGET_01` GREEN.
    - `appendLedgerEntry` body byte-unchanged.
    - `assertTripleGate` body byte-unchanged (Phase 53 invariant — not touched by Phase 67 but verified as a closure check).
    - All 6 file modifications staged and committed in ONE atomic git commit referencing all 5 requirement IDs.
  </behavior>
  <action>
1. Run the quick-feedback suite to confirm green: `npx vitest run tests/unit/auto-fix-prompt-iter.test.js tests/unit/fix-prompt-builder.test.js tests/unit/fix-prompt-builder-byte-stability.test.js tests/unit/check-diff-guard.test.js tests/unit/auto-fix.test.js tests/unit/llm-ledger.test.js`.

2. Run the full suite to confirm no regressions: `npm test`. Acceptable pre-existing failures noted in STATE.md (v40-verifier-gate-yaml.test.js V2 from Phase 51.1; llm-ledger.test.js Test 48 runtime-mutated working copy) may exist — confirm against the HEAD baseline that Phase 67 introduces zero NEW failures.

3. Verify trust invariants via source diff:
   - `git diff HEAD -- tests/e2e/lib/llm-ledger.js | wc -l` MUST return 0 (LEDX-03).
   - `git diff HEAD -- scripts/auto-fix-promote.mjs | wc -l` MUST return 0 (assertTripleGate body untouched).
   - `git diff HEAD -- tests/e2e/lib/fix-prompt-builder.js | grep -E '^[-+].*PROMPT_SCAFFOLDS\\s*=' | wc -l` MUST return 0 (frozen registry intact).
   - `git diff HEAD -- tests/e2e/lib/fix-prompt-builder.js | grep -E '^[-+].*buildScaffoldSystemPrompt\\(' | wc -l` MUST return 0 in the function-body region (helper untouched).
   - `git diff HEAD -- scripts/auto-fix.mjs | grep -cE '^[+].*iter_round:' ` returns between 4 and 6 (discipline guard).

4. Stage all 6 files in ONE go: `git add tests/e2e/lib/fix-prompt-builder.js scripts/auto-fix.mjs scripts/check-diff-guard.mjs tests/unit/fix-prompt-builder.test.js tests/unit/check-diff-guard.test.js tests/unit/auto-fix-prompt-iter.test.js`.

5. Create ONE atomic commit using the HEREDOC pattern. Commit message MUST follow the existing project pattern (see recent commits in `git log --oneline -10`) and MUST name all 5 PITER-* requirement IDs:

```
feat(67): prompt-iter loop Shape A + FORBIDDEN_PATHS extension (PITER-01..05)

- PITER-01: buildFixPrompt({rewriteHint}) optional param; round-0 byte-identical
- PITER-02: runDispatcher Step 10 in-process iter wrapper (ITER_MAX_ROUNDS=2)
- PITER-03: PROMPT_ITER_COST_CAP_USD=0.50 cumulative cap; additive iter_round ledger field
- PITER-04: trigger gating — apply-check-failed + malformed-diff:* only; sdk_error fast-fails
- PITER-05: FORBIDDEN_PATHS extended to reject fix-prompt-builder.js + llm-router.js

Trust invariants preserved: PROMPT_SCAFFOLDS Object.freeze + 7 byte-stability sha256;
appendLedgerEntry body BYTE-UNCHANGED; local safeAppendLedger wrapper UNCHANGED;
assertTripleGate body UNCHANGED. Zero new npm dependencies.
```

Use the HEREDOC pattern from the role-card git protocol. Do NOT use `--no-verify` or `--no-gpg-sign`. Do NOT amend an existing commit.

6. Post-commit verification: `git log -1 --stat` shows exactly 6 files changed; `git status` is clean. `git show HEAD --stat | grep -c '^ tests\\|^ scripts'` reflects the 6 expected paths.

7. Do NOT push to origin — operator handles batch push per the STATE.md session continuity convention.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -30 && git log -1 --stat 2>&1 | head -20 && git diff HEAD -- tests/e2e/lib/llm-ledger.js | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exit code is 0 OR has exactly the same pre-existing failures as `git stash; npm test; git stash pop` baseline (zero NEW failures introduced by Phase 67).
    - `git log -1 --format=%s` returns a subject starting with `feat(67):` and containing the substring `PITER-01..05` (or each ID enumerated separately).
    - `git show HEAD --name-only | sort` returns exactly the 6 expected files: scripts/auto-fix.mjs, scripts/check-diff-guard.mjs, tests/e2e/lib/fix-prompt-builder.js, tests/unit/auto-fix-prompt-iter.test.js, tests/unit/check-diff-guard.test.js, tests/unit/fix-prompt-builder.test.js.
    - `git diff HEAD~1 -- tests/e2e/lib/llm-ledger.js | wc -l` returns 0 (LEDX-03 invariant).
    - `git diff HEAD~1 -- scripts/auto-fix-promote.mjs | wc -l` returns 0 (assertTripleGate untouched).
    - `git status --porcelain` returns empty (clean working tree).
  </acceptance_criteria>
  <done>Single atomic commit lands all 5 PITER-* requirements; full suite GREEN; trust invariants preserved; STATE.md atomicity (D-09) constraint satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → dispatcher | `sdkResult.llmText` (untrusted text from Claude) crosses into the dispatcher via `parseFencedDiff`. Hint capture (Pitfall 6 territory) re-uses this surface but bounded. |
| dispatcher → ledger | `safeAppendLedger` entries (now carrying additive `iter_round`) flow into the on-disk forensic ledger. |
| LLM-authored diff → working tree | `git apply --check` evaluates the diff against the working tree. FORBIDDEN_PATHS is the access-control gate that prevents the LLM from editing scaffold sources. |
| Auto-fix PR → main branch | Even after merge, the FORBIDDEN_PATHS rejection ensures the PR diff cannot include scaffold edits (verifier-gate workflow re-runs the check). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | Tampering | LLM-authored diff edits `tests/e2e/lib/fix-prompt-builder.js` | mitigate | PITER-05: FORBIDDEN_PATHS regex bank extended with `^tests\/e2e\/lib\/fix-prompt-builder\.js$` (Task 2). Ships in SAME atomic commit as iter wrapper (Pitfall 9). |
| T-67-02 | Tampering | LLM-authored diff edits `tests/e2e/lib/llm-router.js` | mitigate | PITER-05: FORBIDDEN_PATHS regex bank extended with `^tests\/e2e\/lib\/llm-router\.js$` (Task 2). |
| T-67-03 | Denial of Service | Iter loop spins unbounded burning API spend | mitigate | PITER-03: `ITER_MAX_ROUNDS=2` ceiling AND `PROMPT_ITER_COST_CAP_USD=0.50` cumulative cap AND existing per-issue $1 + per-PR $2 + day $10 + month $100 caps (5 layered defenses). T_PROMPT_ITER_BUDGET_01 pin (Task 4). |
| T-67-04 | Denial of Service | `sdk_error` triggers retry storm | mitigate | PITER-04: sdk_error fast-fails at line 818 BEFORE any iter trigger; Test E (Task 3/4) pins exactly 1 SDK call on sdk_error. |
| T-67-05 | Tampering | Prompt injection via `<prior_attempt_feedback>` hint capture | accept | Hint sourced from `git apply --check` stderr (trusted local subprocess) or `parseFencedDiff.reason` (constrained string set). Bounded to 500 chars. The new feedback block sits OUTSIDE the existing `<issue_body_untrusted>` envelope — the model treats it as machine-generated feedback. RESEARCH.md "Known Threat Patterns" row 2. |
| T-67-06 | Tampering | Byte-stability sha256 pins drift via accidental scaffold modification | mitigate | The 7 pins at `tests/unit/fix-prompt-builder-byte-stability.test.js:23-31` are run on EVERY commit (Sampling Rate: per-task). Round-0 byte-identity is guarded by `if (typeof rewriteHint === 'string' && rewriteHint.length > 0)` in Task 1. Pitfall 1 + Pitfall 2 mitigations active. |
| T-67-07 | Tampering | `appendLedgerEntry` body modified to add validation breaking 33 pre-existing tests | mitigate | LEDX-03 invariant preserved by adding `iter_round` ONLY at call-site object literals (Pattern 3 in RESEARCH). Task 5 verifies `git diff HEAD~1 -- tests/e2e/lib/llm-ledger.js | wc -l == 0`. |
| T-67-08 | Repudiation | Bypass via `gh pr merge --admin` on the Phase 67 PR | transfer | Phase 62 BYPASS-01/02/03 already shipped on origin/main (STATE.md ## Bypass Conventions); audit-bypass-merges.mjs surfaces any `--admin` use on `auto-fix/*` branches. Phase 67 does NOT introduce a new bypass surface. |
| T-67-SC | Tampering | npm package install with malicious dependency | mitigate | NOT APPLICABLE — Phase 67 installs zero new npm packages (RESEARCH "Package Legitimacy Audit": "Not applicable — Phase 67 installs zero new packages."). Fifth-consecutive-milestone zero-dep target preserved. No slopcheck checkpoint required. |
</threat_model>

<verification>
## Phase-level verification (after Task 5 commit lands)

1. **Quick suite** (~15s): `npx vitest run tests/unit/auto-fix-prompt-iter.test.js tests/unit/fix-prompt-builder.test.js tests/unit/fix-prompt-builder-byte-stability.test.js tests/unit/check-diff-guard.test.js tests/unit/auto-fix.test.js tests/unit/llm-ledger.test.js` — all GREEN.

2. **Full suite** (~45s): `npm test` — same pass/fail count as HEAD baseline (the two pre-existing failures noted in STATE.md from Phase 51.1 and Phase 53 closure may persist; ZERO new failures).

3. **T_PROMPT_ITER_BUDGET_01 targeted**: `npx vitest run tests/unit/auto-fix-prompt-iter.test.js -t T_PROMPT_ITER_BUDGET_01` — GREEN. This is the canonical PITER-03 cap-enforcement pin.

4. **Byte-stability pin (canonical PITER-01 regression detector)**: `npx vitest run tests/unit/fix-prompt-builder-byte-stability.test.js` — all 7 sha256 hashes GREEN.

5. **Trust invariants (source diffs)**:
   - `git diff HEAD~1 -- tests/e2e/lib/llm-ledger.js | wc -l` returns 0 (LEDX-03).
   - `git diff HEAD~1 -- scripts/auto-fix-promote.mjs | wc -l` returns 0 (assertTripleGate untouched).
   - `node -e 'import("./scripts/check-diff-guard.mjs").then(m => console.log(m.FORBIDDEN_PATHS.length))'` prints `10`.

6. **Atomic-commit check**: `git show HEAD --name-only | sort | uniq` returns exactly the 6 file paths from `files_modified` frontmatter.

7. **Requirement-ID traceability**: `git log -1 --format=%B | grep -cE 'PITER-0[1-5]'` returns >= 5 (all 5 requirement IDs named in the commit message).
</verification>

<success_criteria>
- [x] `buildFixPrompt({rewriteHint})` accepted; round 0 byte-identical to today (PITER-01)
- [x] `runDispatcher` Step 10 iter loop wraps LLM dispatch → parse → guard → apply-check (PITER-02)
- [x] `ITER_MAX_ROUNDS = 2` + `PROMPT_ITER_COST_CAP_USD = 0.50` exported; `iter_round` flows through ledger entries; `appendLedgerEntry` body byte-unchanged (PITER-03)
- [x] Iteration triggers ONLY on `apply-check-failed` + `malformed-diff:*`; `sdk_error` fast-fails with exactly 1 SDK call (PITER-04)
- [x] `FORBIDDEN_PATHS` length 10; F15 + F16 reject fix-prompt-builder.js + llm-router.js (PITER-05)
- [x] All 7 byte-stability sha256 pins GREEN
- [x] `appendLedgerEntry` body BYTE-UNCHANGED (LEDX-03 preserved — 33 pre-existing tests GREEN)
- [x] Local `safeAppendLedger` wrapper body BYTE-UNCHANGED (Phase 60.1 + Phase 62 invariant)
- [x] `assertTripleGate` body BYTE-UNCHANGED (Phase 53 invariant — verified by file diff)
- [x] `PROMPT_SCAFFOLDS` `Object.freeze` + `buildScaffoldSystemPrompt` body BYTE-UNCHANGED (Phase 45 invariant)
- [x] ZERO new npm dependencies (fifth-consecutive-milestone zero-dep target)
- [x] Single atomic git commit covering all 5 PITER-* requirements (D-09 atomicity)
- [x] `T_PROMPT_ITER_BUDGET_01` GREEN (canonical cap-enforcement pin)
- [x] Full Vitest suite shows ZERO new failures vs. HEAD baseline
</success_criteria>

<output>
Create `.planning/phases/67-prompt-iter-loop-shape-a-capture-and-surface-in-process/67-01-SUMMARY.md` when done. The SUMMARY must record:
- Which 5 PITER-* requirements landed (PITER-01, PITER-02, PITER-03, PITER-04, PITER-05)
- The single commit SHA covering all 5
- Verification of trust invariants (LEDX-03, Phase 45 byte-stability pins, Phase 53 assertTripleGate, Phase 60.1 subscription whitelist, Phase 62 local-wrapper preservation)
- Any deviation from CONTEXT.md decisions (expected: none — autonomous plan with locked design)
- Zero new dependencies confirmation (fifth-consecutive-milestone target held)
</output>
