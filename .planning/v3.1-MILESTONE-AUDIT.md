---
milestone: v3.1
audited: 2026-05-29T22:12:43Z
status: tech_debt
scores:
  requirements: 29/29
  phases: 6/6
  integration: 29/29   # all seams fully wired; 3 fragility warnings resolved in Phase 38 (INT-FIX-01/02/03)
  flows: 5/5
gaps:
  requirements: []      # no unsatisfied or orphaned requirements — FAIL gate not triggered
  integration: []       # 3 fragility warnings resolved in Phase 38 Plan 01 (commits e24be0c, fa8497d, 613a56d)
  flows: []             # all 5 E2E pipeline flows complete end-to-end
human_verification:     # outstanding live-GitHub / live-CI / live-cron confirmations (deferred verification, not gaps)
  - phase: 32
    item: "CR-04 mid-run phase-cap trip routes to exit code 6 (runtime). Logic at e2e-explore.mjs:551-562 sound by inspection; override-accepted via AskUserQuestion 2026-05-25."
  - phase: 35
    item: "Live `e2e-report-issue.mjs --source triage` against a real triage-report.json creates issue with 4 sections + line-1 fingerprint + labels [category, e2e-nightly, triage]."
  - phase: 35
    item: "quarantine-append.mjs 3x same CONFIRMED finding → source issue gets quarantine:ready-for-promotion label on run 3."
  - phase: 35
    item: "gh label list shows triage (#6F42C1) + quarantine:ready-for-promotion (#FFA500). [already confirmed by developer during Plan 00]"
  - phase: 36
    item: "Dispatch e2e-nightly.yml with real llm_run_id → steps 2-5 execute; cron path (no input) byte-identical regression."
  - phase: 36
    item: "`npm run e2e:quarantine` local empty-corpus → exits 0, Playwright reports 0 tests."
  - phase: 37
    item: "Live Monday-cron / manual workflow_dispatch of e2e-weekly-digest.yml → commits reports/weekly-digest-YYYY-WNN.md [skip ci] + files e2e-digest issue."
nyquist:
  compliant_phases: ["36"]
  partial_phases: ["32", "33", "34", "35", "37"]
  missing_phases: []
  overall: partial   # 5 of 6 phases have draft VALIDATION.md (nyquist_compliant: false); test suites are strong (678 vitest tests) but formal Nyquist not stamped
tech_debt:
  - phase: 32-human-uat-verification
    items:
      - "Pre-existing Playwright US11427642-claims-1 designed-failure (extension defect, Plan 28-05-04) — tracked for gsd-debug; not a v3.1 regression"
      - "Phase 31 schema-vs-LLM-output tension: all 10 UAT iterations classified LLM_API_ERROR/HARNESS_ERROR due to selectedText >300 chars — surfaced by design"
      - "WSL2 xdg-open exit-3 on upload helper (browser-open brittle; GitHub-side work succeeds first)"
      - "Plan 32-04 resolveRunId() semantic mismatch — workaround PLAYWRIGHT_RUN_ID env var; --run-id CLI flag deferred"
  - phase: 33-re-run-validator
    items:
      - "6 Info code-review findings (IN-01..IN-06) deferred — none block phase"
  - phase: 34-hybrid-triage-classifier
    items:
      - "5 Info code-review findings (IN-01..IN-05) intentionally deferred per review scope"
  - phase: 37-weekly-analytics-digest
    items:
      - "6 Warning + Info findings (WR-01..WR-06 + IN-01..IN-04) deferred to verify-work (commit 7d04130)"
      - "IN-01 GraphQL title shell-interpolated via replaceAll (dormant; internally-controlled input)"
      - "IN-02 dual-type now() handling; IN-04 magic literals 50/3 (spec-locked constants)"
  - phase: cross-cutting
    items:
      - "5 of 6 phases carry draft VALIDATION.md (nyquist_compliant: false) — formal Nyquist coverage not stamped despite strong vitest suites"
---

# Milestone v3.1 — LLM-Driven Product Improvement Loop — Audit Report

**Audited:** 2026-05-29T22:12:43Z
**Status:** `tech_debt` — all requirements satisfied, all E2E pipeline flows complete, all cross-phase seams wired; no critical blockers, but accumulated deferred items and outstanding live-environment confirmations warrant review before completion.

**Milestone goal:** Close the loop from v3.0's LLM exploratory testing into actionable product fixes — reproducibility-validated, hybrid-triaged findings flow into rich-context GitHub issues and a tiered quarantine→golden corpus, with a weekly analytics digest driving roadmap prioritization.

---

## Scores

| Dimension | Score | Notes |
|-----------|-------|-------|
| Requirements | 29/29 | All satisfied per VERIFICATION coverage tables + traceability; 0 unsatisfied, 0 orphaned |
| Phases | 6/6 | 3 `passed`, 3 `human_needed` (all automated must-haves verified) |
| Integration | 29/29 | All cross-phase seams fully wired; 3 fragility warnings resolved in Phase 38 |
| E2E Flows | 5/5 | Full triage pipeline traces end-to-end; 0 broken seams |

---

## Requirements Coverage (3-Source Cross-Reference)

All 29 v3.1 requirements were cross-referenced across (a) REQUIREMENTS.md traceability table, (b) per-phase VERIFICATION.md requirements coverage tables, and (c) SUMMARY.md `requirements-completed` frontmatter.

**Result: 29/29 satisfied. No unsatisfied. No orphaned. FAIL gate NOT triggered.**

| Phase | Requirements | VERIFICATION status | All SATISFIED in coverage table | Traceability `[x]` |
|-------|-------------|---------------------|----------------------------------|--------------------|
| 32 | UAT-01, UAT-02, UAT-03 | passed (4/4, 1 override) | ✓ | ✓ |
| 33 | RERUN-01..04 | passed (32/32) | ✓ | ✓ |
| 34 | TRIAGE-01..06 | passed (6/6) | ✓ | ✓ |
| 35 | ISSUE-01..04, QUAR-01, QUAR-02, QUAR-05 | human_needed (5/5 automated) | ✓ | ✓ |
| 36 | QUAR-03, QUAR-04, ORCH-01..03 | human_needed (5/5 automated) | ✓ | ✓ |
| 37 | DIGEST-01..04 | human_needed (12/12 automated) | ✓ | ✓ |

**Data-quality note (not a gap):** SUMMARY.md `requirements-completed` frontmatter is sparsely tagged — only the "anchor" plan for each requirement carries the tag (e.g. Phase 34 frontmatter lists only TRIAGE-04 across 5 plans; Phase 37 lists only DIGEST-04). This is a GSD tagging convention, not evidence of non-delivery. The VERIFICATION.md coverage tables (the deeper artifact) list every one of the 29 requirements as SATISFIED with concrete file/line/test evidence, and every requirement appears in exactly one phase's VERIFICATION table (orphan check clean). Where the frontmatter cross-check was silent due to convention, the VERIFICATION evidence governs.

---

## Cross-Phase Integration

The integration checker traced 14 cross-phase seams against actual source (scripts/*.mjs, tests/e2e/lib/*.js, quarantine.spec.js, both workflow YAMLs, package.json).

**14/14 seams WIRED · 0 broken · 0 orphaned exports · 0 missing connections.**

Verified handoffs (all schema-matched and import-resolved):
- `llm-report.json` → rerun-validator → triage-classifier → run-triage-pipeline
- `rerun-report.json` → triage-classifier (sibling auto-discovery) → run-triage-pipeline
- `triage-report.json` → issue-payload-builder / `--source triage` filer + quarantine-append
- `test-cases-quarantine.js` → quarantine.spec.js + promote-from-quarantine
- `atomicWriteJson` (rerun-validator) imported by quarantine-append + run-triage-pipeline
- `SUMMARY_KEYS` + `ERROR_CLASSES` imported by weekly-digest
- **Label contract exact-match:** producer writes `e2e-nightly` / `e2e-quarantine` / `triage` / `e2e-digest`; weekly-digest consumes `e2e-nightly` + `e2e-quarantine` — strings match
- `e2e-nightly.yml` `llm_run_id` gating covers all 5 pipeline steps; cron (no-input) path unaffected
- upload-llm-report → ingest workflow → nightly download chain (artifact name `llm-report` matches)

### Integration warnings (fragility — not breaks) — RESOLVED in Phase 38

1. ✅ **RESOLVED (commit e24be0c)** — **`QUARANTINE_REPORT_FILENAME` duplicated** (QUAR-01, QUAR-04). `quarantine.spec.js` now imports the exported constant from `scripts/e2e-report-issue.mjs`; the local re-declare is gone. A future one-sided rename surfaces as an ESM import error at spec load time instead of silent zero-filings. Regression test: `tests/unit/quarantine-spec-import.test.js`.
2. ✅ **RESOLVED (commit fa8497d)** — **`DIGEST-04` validation is self-referential** (DIGEST-04). `weekly-digest.mjs` now calls `validateSummaryKeys(aggregateBySummaryKey({nightlyIssues, quarantineIssues, monthlyTotalCostUsd}))` against real aggregated metric data. A future llm-report.js key drift produces a descriptive throw naming the missing key. Regression test: `tests/e2e/scripts/e2e-weekly-digest.test.js` (3 new it() blocks under `INT-FIX-02`).
3. ✅ **RESOLVED (commit 613a56d)** — **Quarantine failures skip artifact upload** (QUAR-03, QUAR-04). `.github/workflows/e2e-nightly.yml` upload-artifact step now has `id: upload-artifacts` and `if:` gates on all 4 step outcomes including quarantine, with the new clause INSIDE the `always() && (...)` parens (Pitfall 3). Regression test: `tests/e2e/scripts/e2e-nightly-quarantine-yaml.test.js` Y6.

---

## E2E Flows

All five pipeline flows trace end-to-end with no hard break:
1. `e2e:explore` → `llm-report.json` (UAT path)
2. Local report → ingest workflow → nightly download (UAT-03 handoff)
3. CONFIRMED finding → rerun → triage → filed GitHub issue with rich body + labels
4. CONFIRMED finding → quarantine corpus entry → non-gating quarantine.spec.js
5. Filed issues → weekly digest aggregation → Discussion/issue + committed markdown

---

## Nyquist Coverage

| Phase | VALIDATION.md | nyquist_compliant | Status | Suggested action |
|-------|---------------|-------------------|--------|------------------|
| 32 | exists (draft) | false | PARTIAL | `/gsd:validate-phase 32` |
| 33 | exists (draft) | false | PARTIAL | `/gsd:validate-phase 33` |
| 34 | exists (draft) | false | PARTIAL | `/gsd:validate-phase 34` |
| 35 | exists (draft) | false | PARTIAL | `/gsd:validate-phase 35` |
| 36 | exists (ready) | true | COMPLIANT | — |
| 37 | exists (draft) | false | PARTIAL | `/gsd:validate-phase 37` |

**Overall: PARTIAL.** Five of six phases carry a draft VALIDATION.md not stamped `nyquist_compliant`. This is a formal-coverage gap, not a test-coverage gap — the vitest suite grew from 439 → 678 tests across the milestone with extensive per-phase unit + spawnSync integration coverage. Running `/gsd:validate-phase` on the five flagged phases would retroactively close the formal gap.

---

## Outstanding Human-Verification Items (deferred, not gaps)

Seven live-environment confirmations remain across four phases. These require a real GitHub Actions runner, authenticated `gh` CLI + network, real artifacts, or a scheduled cron tick — none falsifiable by static/automated analysis. Each underlying mechanism is fully verified by the test suite; these are *expected confirmations, not suspected failures.* One (Phase 35 label existence) was already confirmed by the developer during execution.

(See `human_verification:` block in the frontmatter above for the full list.)

---

## Why `tech_debt` and not `passed`

No critical blockers exist: every requirement is satisfied, every E2E flow completes, every cross-phase seam is wired, and the integration checker found zero broken/orphaned connections. The milestone substantively achieves its definition of done.

It is **not** `passed` because the accumulated deferred work exceeds "minimal": 5 Nyquist-partial phases, 7 outstanding live-environment confirmations, and several deferred code-review findings (notably Phase 37's WR-01..06 + IN-01..04 deferred to verify-work per commit `7d04130`). The 3 integration fragility warnings were resolved in Phase 38 Plan 01 (commits e24be0c, fa8497d, 613a56d). None of the remaining items block completion, but they merit a review decision.
